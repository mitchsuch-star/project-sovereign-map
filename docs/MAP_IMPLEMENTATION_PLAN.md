# Map Implementation Plan — Real-Map Cutover (126-province Europe)

> **Status: ✅ REAL-MAP CUTOVER COMPLETE — July 2, 2026.** Slices 1–9 + Slice 7.5 + the 1805 Scenario Setup gate + the DEF-7 registry mini-pass ALL LANDED; the running game is the 126-province 1805 campaign. **The ONLY live content in this document is the Deferred / owner-row table below** — everything else is the execution record of the completed cutover. (Design gate originally approved by the user June 23, 2026.)
> Owner: Map cutover. Supersedes the "After the smoke passes → create the Map Implementation Plan" action in `docs/STATUS.md` (▶ NEXT UP: Real-Map Cutover).

> **Reviewed June 22, 2026 — verdict GO WITH CHANGES.** An independent review verified every claim against
> the real assets (PSD byte-inspection, validator run, registry parse, test grep, lookup measurement).
> Full findings + the session-by-session roadmap live in [`MAP_PLAN_REVIEW.md`](MAP_PLAN_REVIEW.md). Six
> amendments are folded into this plan:
> 1. **Naming (decision #3):** the PSD has **no text layers** — `letters` is **rasterized single-character
>    glyphs** on only 83/126 provinces (absent from Iberia). Naming is a **manual geography pass** against
>    the `Map_Napoleon_Total_War` underlay + an 1805 atlas, verified by an annotated overlay — NOT
>    letters-layer extraction (the layer is at most a weak initial-letter hint).
> 2. **Adjacency (decision #5):** measured inter-province gaps are cleanly bimodal (land ≤~15px = 72.6%,
>    sea ≥41px = 24.4%). **Auto-derive land; hand-author the ~10–20 real sea links** from a generated
>    candidate list — no auto sea-heuristic (it connects every coast-facing pair).
> 3. **Owner fills (Slice 6):** the §4.4 overlay it's modeled on early-outs on all-wired maps (never
>    load-tested here) and is a 4.1M-px per-pixel loop. Use a **fragment shader (≤126-entry owner palette)**
>    or cached per-province pixel-index — NOT a per-turn full-image mirror.
> 4. **Roster (DEF-4):** the art supports **~14–16 independents + ~10 vassals**. Promote DEF-4 to its **own
>    session** (§5.2 dispatch / §5.3 coalition friction / §5.6 trade-income cache) *before* the playtest,
>    and add a **roster design gate** after Slice 2. Britain can take a **real London** (its isles are
>    on-map); Russia/Ottoman/Sweden are the genuine proxy cases.
> 5. **Slice 2 is the critical path** — a manual historical-research pass (budget **1–2 sessions**), not an
>    ordinary slice.
> 6. **`grid_position` at 126** needs explicit pixel→grid bucketing design (Slice 4); the legacy reference
>    count is corrected below.
> 7. **1805 Scenario Setup is its own design-gate step** (end of this plan): re-authoring armies + the 1805
>    diplomatic posture for 14–16 nations is a second authoring pass, not a Slice-3 clause, and it carries
>    the campaign start-point decision. Net roadmap ≈ **11–12 sessions**.
> 8. **Second independent verification pass (June 22, 2026)** re-ran every measurement against the live
>    assets and reconfirmed all findings, then folded the remaining gaps: the legacy footprint re-measured
>    at **10,255 match-lines across 275 of 326 test files** (even larger than prior figures — the
>    fixture call is reinforced); `NATION_POWER_TIERS` already authors **13 nations**, so the roster gaps
>    are **`NATION_COLORS` + `NATION_DESIRE_PROFILES` (only 4 today)**, not tiers; `TALLEYRAND_COMMENTARY`
>    has a graceful `_default` fallback; the **Roster Design Gate is now an explicit step** (Slice 2.5);
>    Slice 6 names a **fragment shader** for per-turn owner fills; Slice 4 must **decide the `grid_position`
>    method**; and the **1805 North-African owners are authored** (Morocco independent; Algiers/Tunis/Tripoli
>    as Ottoman client-regencies — see Slice 2.5).
> 9. **Third independent verification pass (June 22, 2026)** re-ran every measurement from scratch and
>    reconfirmed all prior numbers (validator PASS; land 1,788,712 px / 43.7%; gap bimodality
>    73.6% / 2.9% / 23.5% over 35,633 samples; `letters` = single glyphs, **visually confirmed**; legacy
>    `Belgium` alone 2,344 / 133 files). Four new design items folded — none block Slice 1, all are
>    pre-Slice-3: **(N1)** `NATION_CAPITALS` is a **global** dict the legacy fixture depends on, so
>    "Britain = London" must be **scenario-scoped capitals**, not a mutation of the global (Slices 3 + 4);
>    **(N2)** `WorldState.from_scenario_file()` **already exists** (world_state.py:4790) — author the 1805
>    setup as a **scenario JSON via that loader**, not a second hardcoded init (Scenario Setup step; this
>    moots the hardcode-vs-loader question); **(N3)** the 1805 diplomatic matrix is C(16,2)=120 pairs —
>    author via **default-to-PEACE + exceptions only** (Scenario Setup step); **(N4)** `grid_position` must
>    derive `(row,col)` by **centroid spatial-rank** (unique, order-preserving), NOT a ≈12×10 coarse bucket
>    (which collides for 126 provinces and breaks `resolve_direction`) (Slice 4). Minor: `NATION_HONOR_BIAS`
>    covers only 2 nations (not 13). The broad scale fears are defused (fog is O(marshals); the distance
>    cache is on-demand; vassals are data-driven). Net roadmap unchanged at ≈11–12 sessions.
> 10. **Plan-hardening pass (June 22, 2026)** closes four residual gaps to make the cutover reversible and
>     the budgets measurable: **(G1)** the Slice-4 `region_factory` seam is selected by a config flag
>     (`SOVEREIGN_MAP=europe|legacy`); the game **bootstrap** reads it (Slice 5 sets `europe`) while
>     `WorldState()` still defaults to **legacy** for the test fixture — so the Slice-5/7 "replace outright"
>     flips become a **flag flip, instantly reversible without a code change** (rollback = `legacy`).
>     **(G2)** a standing **legacy-fixture immutability guard** test asserts `create_regions()` still
>     returns the unchanged 19 regions (names, owners, adjacency), so no future edit can silently perturb
>     the fixture the suite depends on. **(G3)** a validator **`is_coastal` ↔ sea-adjacency consistency
>     check** (every coastal province has ≥1 `sea: true` edge or borders ocean-black; no inland province
>     carries a sea edge). **(G4)** concrete budgets replace "within budget": Slice 8 turn resolution
>     **≤ 2× the 19-region baseline** (measured), Slice 6 owner-fill re-tint **≤ one frame (~16 ms)** on
>     ownership change via the shader. Net roadmap unchanged at ≈11–12 sessions.

(Review-outcome list updated from six to ten amendments: the June 22 scenario-setup follow-up (7), the second independent verification pass (8), the third independent verification pass (9), and the plan-hardening pass (10).)

## Goal

Cut the game over from the 19-region placeholder map to the **full commissioned 126-province Europe map** — rendered as a real political map, owned by nations, clickable, and playable. The smoke scene (`europe_map_smoke.tscn`) already proves the art loads and hit-testing works; this plan turns that art into the live game map.

## Locked decisions (interview, June 22, 2026)

| # | Decision | Choice |
|---|----------|--------|
| 1 | Wiring scope | **All 126 provinces playable in v1** (no greyed core) |
| 2 | Nation roster | **Every sovereign on the map, historically accurate, tiered** — great/secondary powers independent (France [player], Britain, Austria, Prussia, Russia, Spain, Ottoman, Sweden, Naples, Bavaria, Saxony, Portugal, Denmark + any other genuine 1805 sovereigns the 126 provinces cover, e.g. Papal States / Sardinia-Piedmont / Swiss); minor German/Italian statelets modeled as **historically-accurate vassals/satellites** of their 1805 patron via the existing vassal system. **Not capped at 13** — the exact roster is discovered while authoring 1805 ownership in Slice 2. |
| 3 | Province naming | **Historical names, hand-authored from geography** (the PSD `letters` layer is rasterized single-char glyphs, not names — weak initial-letter hint at most; see Review amendment 1) |
| 4 | Ownership render | **Province-shape fills via the lookup mask** (new renderer pass, mirrors the §4.4 grey overlay) |
| 5 | Adjacency | **Auto-derive land adjacency; hand-author sea links** from a generated >40px candidate list (see Review amendment 2) |
| 6 | Cutover | **Replace the 19-region placeholder outright** as the game map |
| 7 | Per-province data | **Author owner / terrain / region_type / is_coastal**; income, supply, garrison, buildings, grid derive from existing tables |

The `major / secondary / minor` power-tier taxonomy and France-as-player are already DECIDED in `docs/SCALE_READINESS_PLAN.md` §Phase 0; this plan implements them. The roster extends **beyond DG-1's illustrative 13 to every sovereign the map covers** — tiered exactly as DG-1's architecture already anticipates (it lists Netherlands/Batavia, Duchy of Warsaw, Württemberg, and Switzerland as vassals, not independents). Independents get full diplomatic agency; minors are client states of their real 1805 patron. The goal is an accurate map: every province owned by its real sovereign, every client-parent relationship modeled.

---

## The one reconciliation the gate must bless: "replace outright" vs. a green test suite

`WorldState.__init__` builds **every** world from `create_regions()` → `REGIONS_DATA` (`backend/models/world_state.py:128`). **Re-measured: 10,255 word-boundary match-lines for the 19 region names span 275 of the 326 test files** (8,343 / 241 even after excluding the `Saxony` nation/region name collision — `Belgium` 2,344, `Paris` 2,129, `Saxony` 2,034, `Waterloo` 1,312 dominate), and the pre-commit hook runs the full suite and must stay green (no `--no-verify`). The plan's original "2,462 / 190" under-counted ~4×; the larger true footprint only **strengthens** the legacy-as-fixture decision below.

A literal "delete `REGIONS_DATA`, swap in 126 provinces" detonates the suite. So **"replace outright" is honored at the _game_ level, not the _test-fixture_ level**:

- **The game ships only Europe.** The new-game bootstrap (`/new_game` → `_reset_world_state`, and the module-load `world`) builds the 126-province Europe world. The 19-region map is unreachable in play. `main.tscn` renders only Europe. This is the user's "replace outright."
- **The 19-region dataset survives as a test-only fixture.** `create_regions()` keeps returning the legacy 19 as the default `WorldState()` constructor path, so the ~275 gameplay test files that incidentally use Paris/Berlin/Vienna stay green **untouched**. Only the handful of *map-contract* tests (those that assert properties of "the game's map": `test_map_consistency.py`, `test_map_topology_endpoint.py`, `test_map_bitmap_contract.py`) migrate/extend to Europe.

This is the engineering reconciliation of decision #6 with the hook. **If the user instead wants the ~10,255 references rewritten onto Europe (a multi-thousand-edit migration), that is a different, much larger plan — flag at sign-off.** Default assumption: legacy-as-fixture.

---

## Open verification items for the gate (answer before Slice 2)

1. **Real-roster discovery + capital coverage + the patron web.** The roster is whatever the *real 1805 ownership* of the 126 provinces turns out to be — discovered while authoring Slice 2, not pre-counted. Three things to confirm there: (a) the art is "Europe" but may not extend to Russia's heartland (St. Petersburg/Moscow), the Ottoman Balkans (Constantinople), Scandinavia (Stockholm/Copenhagen), or all of Iberia (Madrid/Lisbon) — every **independent** needs ≥1 owned province and a real-or-proxy capital (precedent: `NATION_CAPITALS["Britain"] = "Netherlands"`); (b) every **minor** state needs a historically-correct **patron** so no province is orphaned and the vassal graph is accurate; (c) any sovereign whose heartland is off-map gets a proxy capital **or** an owner-row entry — it does not silently vanish. **(d) Confirmed from the rendered art (second pass):** the British Isles, Iberia, Italy + islands, the Balkans/Anatolia, Crimea, **southern** Scandinavia, and a **North-African coastal strip** are all on-map; **only the Russian heartland and far north are off-map**. So **Britain takes a real London** (retire the `Netherlands` proxy); **Russia is the clear proxy case** (frontier-province capital), with **Ottoman** (Constantinople sits at the map edge) and **Sweden** (only southern Scandinavia drawn) confirmed real-or-proxy at the **Roster Design Gate (Slice 2.5)**. **(N1, third pass) Britain=London is authored as a *scenario-scoped* capital, NOT by mutating the global `NATION_CAPITALS` dict** — that global is read by `settlement_scoring.py` (CLAUDE.md's documented Britain proxy), marshal spawns, and `covets_regions`, and the legacy 19-region test fixture depends on Britain→Netherlands. The Europe build carries its own capital map; the global stays Netherlands for the legacy fixture (see Slice 3 + Slice 4). **(e) 1805 North-African owners are authored** there too: **Morocco independent** (Alaouite sultanate, coastal proxy capital), and **Algiers / Tunis / Tripoli as Ottoman client-regencies** (nominal Porte suzerainty → modeled as Ottoman vassals; or independent Barbary minors if the Ottoman is itself a proxy).
2. **Named diplomat voices.** The Voice Bible cast is 5 (Talleyrand/Castlereagh/Hardenberg/Metternich/Einsiedel = France/Britain/Prussia/Austria/Saxony). The 8 new nations get the **chancery fallback voice** in v1 (functional, per the `resolve_named_diplomat()` → chancery rule). Named-voice authoring is a tracked owner row (DEF-1), not a v1 blocker.
3. **Save compatibility.** Replacing the map invalidates pre-cutover saves (region keys won't match). Acceptable for a prototype; recorded as owner row DEF-2 with a version bump + a clear "incompatible saves" message rather than a silent crash.

---

## Scope / non-goals

**In scope (v1):** 126 named provinces; **full-roster 1805 ownership (~14–16 independents + ~10 client/vassal states, incl. the North-African regencies)**; auto-derived land adjacency + hand-authored sea links; province-shape political fills; backend region expansion + game cutover; Godot renderer cutover; scale-ready hot-path audit at 126 regions; a manual playtest smoke.

**Non-goals (owner-rowed below, not silently dropped):** named diplomat voices for the new independents (DEF-1); save migration (DEF-2); per-province historical income hand-tuning beyond region_type defaults (DEF-3); coalition/dispatch/diplomacy density retune beyond what the full roster forces (DEF-4 → Phase 5.2/5.3/5.6); naval movement rules beyond adjacency (DEF-5).

## Cross-cutting constraints (honored in every slice)

- **Golden Rule #8 — no per-region scans in hot paths.** 126 regions is the whole point; any new helper that scans `world.regions` caches per-turn and invalidates via the `invalidate_active_nations_cache()` pattern. Slice 8 audits this explicitly.
- **One province source of truth.** The backend region set and the Godot renderer both read the **same `europe.json` registry**, so the map can never drift between them (the exact failure CLAUDE.md guards against). Slice 2 extends the registry with the gameplay fields; the backend loads them in Slice 4.
- **All numbers to Godot via `int()`**; single `world.marshals` dict; backend port 8005.
- **Workflow:** commit directly to master; the pre-commit hook (`ruff check backend/` + full pytest) stays green every slice.
- **No open-ended deferrals** (Golden Rule #9): the owner-row table below gives every deferred item a landing slice, completion definition, STATUS line, and test.

---

## DESIGN GATE — sign-off checklist — ✅ APPROVED (user, June 23, 2026)

Initial gate approved; **Slice 1 may begin.** The two downstream gates below are *in-execution* gates that come due later (notes inline).

- [x] **Decisions #1–#7** as tabled above — APPROVED.
- [x] **Legacy-as-fixture reconciliation** (game = Europe only; 19-region survives as a test fixture; gameplay tests untouched; map-contract tests migrate) — APPROVED (default; **not** the full ~10,255-reference migration).
- [x] **Open verification items 1–3** (geographic coverage / chancery-fallback voices / save-incompat) — accepted as scoped.
- [x] **Slice ordering** (low-risk-first; Slice 1 lands then pauses for review) — APPROVED.
- [x] **Render + parser method choices** — APPROVED approach: owner-fill **fragment shader** (finalize at Slice 6) + `grid_position` by **centroid spatial-rank** (finalize at Slice 4).
- [x] **Owner-row table** (DEF-1..5) — accepted as the home for non-v1 work.
- [x] **Roster Design Gate (Slice 2.5)** — ✅ **BLESSED (user, July 1, 2026):** 17 independents + 3 French satellite vassals (Holland/Kingdom of Italy/Switzerland); proxy capitals accepted as authored (Britain=London, Russia=Vilna, Ottoman=Constantinople, Sweden=Stockholm, Denmark=Copenhagen); Algiers + Egypt sliver Ottoman-owned (no separate Morocco/Tunis/Tripoli on the art). Slice 3 wired it the same day.
- [x] **1805 Scenario Setup gate** — ✅ **BLESSED (user, July 1, 2026):** start point = **war already underway** (Sept 25, 1805 anchor; France at war with exactly Britain/Austria/Russia; Sweden + Naples coalition-committed at PEACE) and all 8 open decisions approved as recommended — see §"Researched 1805 opening draft". **Implementation remains open:** the Loader + Constants pre-slice, then the `europe_1805.json` scenario authoring (the gate step's Files/Tests/Completion contract below still governs the landing).

---

## Slices (ordered low-risk-first)

Each slice: **Scope · Files · Tests · STATUS line · Completion definition.** Slices 1–2 touch only `tools/` + `assets/` (zero game logic, zero suite risk). The cutover risk is concentrated in Slices 5 and 7, each preceded by a fully-tested, un-wired build-up slice.

### Slice 1 — Adjacency derivation + registry/validator tooling ✅ LANDED *(tools/assets only — landed first; PAUSE for review)*

> **LANDED (Map Slice 1).** `build_region_key_from_psd.py` gains a pure, unit-tested `derive_adjacency()` (horizontal+vertical run-length scanlines: land ≤15px, sea-candidate 41–400px, 16–40px dead zone ignored), `apply_adjacency_to_registry()`, an `--adjacency-only` re-derivation path (no PSD / no PNG rewrite), and an `--inspect-letters` naming-seed dump. `validate_province_map.py` gains `ADJACENCY_UNKNOWN_TARGET` + `ADJACENCY_ASYMMETRY` (errors) and `ISOLATED_PROVINCE` (warning), skipped when no region declares adjacency (legacy registries stay clean). `europe.json` regenerated: **248 symmetric land edges, 0 asymmetry, 144 sea candidates, 5 land-isolated islands** (all with sea candidates → genuine islands for Slice-2 authoring). Validator run against the three assets → **0 errors, 5 island warnings**. Tests: new `tests/test_province_adjacency_derivation.py` (12) + 7 new validator checks; focused run 50 passed. **PAUSED for user review.**


- **Scope:** Extend the offline pipeline so the registry can carry a derived adjacency graph and the new gameplay fields, with validation. **No backend, no Godot, no game logic.**
  - `build_region_key_from_psd.py`: add an adjacency pass — for each province, scan the lookup image for the nearest distinct province color across the black moats (border-dilation / nearest-non-black within a tunable gap radius) to produce land adjacency; add a coarse **sea-gap heuristic** (coastal provinces within a larger radius across black get a candidate sea edge, tagged `sea: true`). Emit symmetric `adjacent` lists into `europe.json`.
  - Add a **`letters`-layer inspection** mode: reuse the existing PSD layer parser (`extract_layer`) to dump the `letters` layer (bbox +, if it is a real type layer, its text; else export a crop) as naming seeds for Slice 2. Investigative — does not block if the layer is rasterized.
  - `validate_province_map.py`: add `ADJACENCY_ASYMMETRY` (A→B but not B→A), `ADJACENCY_UNKNOWN_TARGET` (edge to a non-existent province), and `ISOLATED_PROVINCE` (no land edges) checks; tolerate the new authored fields.
- **Files:** `tools/build_region_key_from_psd.py`, `tools/validate_province_map.py`, `tools/mapgen_out/europe.json` → promote to `godot-client/project-sovereign/assets/maps/europe.json`. New `tests/test_province_adjacency_derivation.py`.
- **Tests:** unit-test the adjacency deriver on a tiny synthetic lookup (two provinces sharing a moat → symmetric edge; a province across a wide sea gap → tagged sea candidate; an isolated blob → no edges); extend `tests/test_province_map_validator.py` with the three new failure codes; regenerate + re-run `validate_province_map.py` → `PASS`.
- **STATUS line:** `Map Slice 1 LANDED — adjacency auto-derivation (land + sea-gap heuristic) + letters-layer seed extraction + 3 new validator checks; europe.json regenerated with symmetric adjacency; validator PASS.`
- **Completion:** `europe.json` carries a symmetric, validated 126-node adjacency graph; the validator gates it; suite green; ruff clean. **PAUSE for user review here.**

### Slice 2 — Province data authoring ✅ LANDED *(registry data + validation — no game logic)*

> **LANDED (Map Slice 2, July 1, 2026).** All 126 provinces authored into `europe.json`: **historical name** (unique, no `Region_NNN` placeholder left), **1805 `starting_controller`**, **terrain** ∈ `VALID_TERRAINS`, **region_type** ∈ `VALID_REGION_TYPES`, **is_coastal** (derived from the lookup image by morphological opening — 86 coastal / 40 inland), **is_capital**. The 1805 roster **discovered** is **20 nations** — 10 majors (France [player], Britain, Spain, Portugal, Austria, Prussia, Russia, Ottoman, Sweden, Denmark) + 10 minors/clients (Naples, Bavaria, Papal States, Sardinia, Holland, Hanover, Hesse, Kingdom of Italy, Switzerland, Saxony) — each with exactly one owned capital (Russia/Ottoman/Sweden carry frontier/edge proxy capitals per the plan; Britain takes a real London). **12 hand-authored sea links** connect the 5 island components (GB, Ireland, Corsica/Sardinia, Sicily, Balearics/Crete/Cyprus/Zealand singletons) to the mainland; adjacency de-duped + sorted on save. Validator PASS `--strict` (0 errors, 0 warnings). New `tests/test_europe_registry_data.py` (24 tests) pins the authored data + the new validator checks; full suite `10560 passed, 1 skipped`; ruff clean. **The owner map was visually QA'd via an owner-colored overlay before finalizing.** The classifier/overlay scaffolding was a throwaway inspection aid; `europe.json` is the committed deliverable. **PAUSE for the Roster Design Gate (Slice 2.5) — the discovered roster (independents-vs-vassals split, proxy capitals, patron web, North-African owners) is now ready for the user to bless before Slice 3 wires it.**
>
> **M4/M5 disposition — IMPLEMENTED with tests.** **M4 (omitted vs. empty `adjacent`):** `validate_adjacency` now treats a region whose `adjacent` key is *absent* as "not yet authored" — exempt from the `ISOLATED_PROVINCE` warning and from being demanded as an asymmetry back-edge — while an *explicit* `[]` is still reported isolated. This kills the mid-slice error burst. **M5 (duplicate adjacency entry):** new `DUPLICATE_ADJACENCY_ENTRY` (warning) fires on a repeated neighbour and suppresses the spurious asymmetry it would otherwise cause; the authoring pass also de-dupes every `adjacent` list on save. Both are behavior-tested in `tests/test_europe_registry_data.py`.

- **Scope:** The big hand-authoring pass, all into `europe.json`: **historical name** per province (cross-checked vs the Slice 1 letters-layer seeds), **starting_controller** = the province's **real 1805 sovereign** (authoring this across all 126 provinces is what *discovers the full roster* and which states are minors), **terrain**, **region_type**, **is_coastal**, and each nation's **capital** (`is_capital`). Hand-correct the derived adjacency where the heuristic erred. Output the canonical roster list — independents vs. minors-needing-a-patron (including the North-African coastal strip — Morocco independent, the Algiers/Tunis/Tripoli regencies as Ottoman client states; see Slice 2.5) — that Slice 3 consumes. Resolve verification item #1 (every province → a real sovereign; every independent has a capital/proxy). This is the heaviest authoring slice; it is pure registry data + validation, no game logic.
- **Files:** `godot-client/project-sovereign/assets/maps/europe.json`; `tools/validate_province_map.py` (add gameplay-field schema checks: every province has a unique non-placeholder name; `starting_controller` ∈ the authored roster set; `terrain` ∈ `VALID_TERRAINS`; `region_type` ∈ `VALID_REGION_TYPES`; every nation in the roster owns ≥1 province; every `NATION_CAPITALS` capital exists and is owned by that nation; **(G3) `is_coastal` ↔ sea-adjacency consistency — every `is_coastal` province has ≥1 `sea: true` edge or borders ocean-black, and no inland province carries a sea edge**). New `tests/test_europe_registry_data.py`.
- **Carried-over Slice 1 review findings (validator hardening — address as you see fit; these become live once Slice 2 hand-editing begins):** the Slice 1 code review deferred two `validate_adjacency` items that only bite during incremental hand-authoring. **The next session owns them and may implement, re-scope, or explicitly close each — just record the disposition here + in STATUS.**
  - **M4 — omitted vs. empty `adjacent`.** Once *any* region declares `adjacent`, `validate_adjacency` treats a region that simply hasn't been reached yet (no `adjacent` key) as `[]` → an `ISOLATED_PROVINCE` warning **plus** `ADJACENCY_ASYMMETRY` errors from every neighbor that already lists it. During a mid-slice partial edit this produces a confusing error burst. Option: distinguish "key absent = not-yet-authored" (skip/soft-warn) from "explicit `[]` = truly isolated", or accept it as a correct "authoring incomplete" signal and document that the validator is meant to run only on a fully-authored registry.
  - **M5 — duplicate entry in an `adjacent` list.** A repeated neighbor (`["Berlin","Berlin"]`) yields duplicate `ADJACENCY_ASYMMETRY` findings if the back-edge is missing. Cosmetic; the deriver emits sorted-set lists so only hand-edits hit it. Option: de-dupe per-list before checking, or add a `DUPLICATE_ADJACENCY_ENTRY` note. Since Slice 2 hand-corrects adjacency, a de-dupe/normalization pass on save is a natural home.
- **Tests:** registry-completeness test (126 named provinces, no `Region_NNN` left, all owners known, all terrains/types valid, adjacency symmetric, every capital present + correctly owned); `validate_province_map.py --strict` → `PASS`. If M4/M5 are implemented, add a behavior test per finding; if closed, note why in the STATUS line.
- **STATUS line:** `Map Slice 2 LANDED — 126 provinces authored (historical names, full-roster 1805 ownership, terrain/type/coastal, capitals); registry data-validated; validator + data tests PASS.` *(append the M4/M5 disposition — implemented-with-tests or closed-with-reason.)*
- **Completion:** `europe.json` is the complete, validated province source (render + gameplay fields); no placeholder names; coverage of the full roster confirmed; suite green.

### Slice 2.5 — ROSTER DESIGN GATE ✅ BLESSED (user, July 1, 2026) *(decision step — no code)*

> **BLESSED July 1, 2026.** The user chose **real historical vassals/masters** for the split, **accepted the authored proxy capitals**, and asked for **realism** on North Africa (answered: keep Algiers + the Egypt sliver Ottoman-owned — the art never separated Morocco/Tunis/Tripoli). The blessed roster:
> - **17 independents:** France [player], Britain, Russia, Austria, Prussia, Spain, Ottoman, Sweden, Naples, Portugal, Denmark, Bavaria, Saxony, Hanover, Hesse, Papal States, Sardinia.
> - **3 vassals of France** (the only genuine 1805 French satellite states): **Holland** (Batavian Republic), **Kingdom of Italy** (Napoleon as King), **Switzerland** (Helvetic Confederation), all `autonomy = satellite`. Bavaria/Saxony/Hanover/Hesse were sovereign in autumn 1805 → **independent** (allies/occupied are modeled via diplomacy, not vassalage).
> - **Proxy/edge capitals accepted as authored:** Britain=London (retires the Netherlands proxy), Russia=Vilna, Ottoman=Constantinople, Sweden=Stockholm, Denmark=Copenhagen.
> - **North Africa:** Algiers + Egypt sliver = Ottoman; no separate Morocco/Tunis/Tripoli provinces on the art.
>
> Slice 3 wired this exact roster into backend config the same day.

> Added by the second review pass. Decision #2's realized size (≈14–16 independents + ≈10 client/vassal states) carries large downstream cost (bilateral O(N²), dispatch/coalition density, per-nation colors/voices/desire-profiles), so the **exact roster is blessed here — after Slice 2 discovers it, before Slice 3 wires it.**

**Bless at this gate:**

- **Independents (full agency) vs. vassals (client states of a patron).** Modeling minors as vassals is what caps the O(N²) blow-up at the ~14–16 independents — decision #2's whole point.
- **Proxy capitals.** Britain takes a **real London** (the British Isles are drawn — retire `NATION_CAPITALS["Britain"]="Netherlands"`). **Russia** is the clear proxy case (heartland off-map → a frontier province, e.g. Vilna/Courland). **Ottoman** (Constantinople sits at the map edge) and **Sweden** (only southern Scandinavia drawn) are confirmed real-capital-or-proxy here.
- **The patron web** (seeded into `world.vassals` at build, Slice 3): e.g. Batavia, Helvetia/Switzerland, Kingdom of Italy, Etruria, the SW-German states (Württemberg, Baden, Hesse, Berg), the small Italian duchies (Parma, Modena, Lucca), a Polish/Warsaw entity. Every minor resolves to a historically-correct patron so no province is orphaned.
- **1805 North-African owners** (researched; the coastal strip is on-map):

  | Province area | 1805 sovereign | Model as |
  |---|---|---|
  | Morocco (Atlantic/Med. coast) | Sultanate of Morocco (Alaouite; Sultan Moulay Slimane) — **fully independent**, never Ottoman | independent **minor** (coastal proxy capital; Fez/Marrakesh are inland/off-map) |
  | Algiers | Regency of Algiers (Dey) — autonomous under **nominal Ottoman suzerainty** | **Ottoman** vassal/satellite |
  | Tunis | Regency of Tunis (Husainid Bey) — nominal Ottoman suzerainty | **Ottoman** vassal/satellite |
  | Tripoli (Tripolitania) | Regency of Tripoli (Karamanli Pasha) — nominal Ottoman suzerainty | **Ottoman** vassal/satellite |

  If the gate makes the **Ottoman** a proxy/owner-row rather than a live independent, model Algiers/Tunis/Tripoli instead as **independent Barbary minors** (they ran their own foreign policy — e.g. Tripoli's 1801–1805 war with the United States). Egypt's heartland is off-map (Ottoman; Muhammad Ali as Wāli from 1805); any coastal sliver that appears is Ottoman-owned.

- **Authoring debt the roster creates** (drives Slice 3 + DEF-1/DEF-4): `power_tier` is **already authored for 13 nations**, so tiers are mostly done; the real gaps are **`Utils.NATION_COLORS`** (8 today incl. "Neutral" → +6–9) and **`NATION_DESIRE_PROFILES`** (only 4 today: Austria/Britain/Prussia/Saxony → +9–11, else AI proposals degrade to empty desires). `TALLEYRAND_COMMENTARY` already has a `_default` fallback, so commentary degrades gracefully — **prioritize desire profiles over commentary**.
- **Completion:** the user approves the independents/vassals split, proxy capitals, patron web, and North-African owners. This is the design input Slice 3 wires; **no code lands at this gate.**

### Slice 3 — full-roster backend data ✅ LANDED *(July 1, 2026 — no map wiring yet)*

> **LANDED (Map Slice 3, July 1, 2026).** The blessed 20-nation Europe roster is wired into the backend as a **scenario-scoped** config surface that never perturbs the legacy 5-nation globals (which the ~275-file gameplay fixture + `settlement_scoring.py`'s Britain→Netherlands proxy depend on — amendment N1). `backend/nation_config.py` gains `EUROPE_ROSTER` (20), `EUROPE_NATION_CAPITALS` (matches `europe.json` `is_capital` exactly, Britain=London), `EUROPE_VASSAL_WEB` (Holland/KingdomOfItaly/Switzerland → France, `satellite`), `EUROPE_NATION_GOLD`/`EUROPE_BASE_ACTIONS`/`EUROPE_NATION_AUTHORITY` (overrides → `DEFAULT_NATION_DEFAULTS` fallback), and the `get_europe_runtime_nations` / `build_europe_enemy_nations` / `build_europe_nation_{gold,actions,authority}` / `get_europe_capital` / `get_europe_vassal_web` builders. **Additive-only** extensions to the pure `.get()` lookups (safe for legacy): `NATION_POWER_TIERS` (+8 minors, incl. the `Denmark` registry key), `NATION_DESIRE_PROFILES` (+16 profiles so no Europe nation degrades to empty AI desires), `TALLEYRAND_COMMENTARY` (flavor for Russia/Spain/Ottoman; the rest use the `_default` fallback — bespoke voice is owner-row **DEF-1**), and Godot `utils.gd` `NATION_COLORS` (+13). `backend/models/diplomat.py` gains `_EUROPE_EXTRA_DIPLOMAT_DEFINITIONS` (15 new, named ministers where documented, chancery persona otherwise — all chancery-fallback **voice** per DEF-1) + `create_europe_diplomats()` (composes the 5 named legacy templates with the 15). **`NATION_HONOR_BIAS` was deliberately NOT extended** — it multiplies live reliability-delta math (not a `.get()` lookup), so Europe honor bias is deferred to the 1805 Scenario Setup / balance pass. New `tests/test_europe_roster_config.py` (18 tests) guards roster size, registry-capital consistency, capital ownership, power tiers, diplomat coverage, desire-profile completeness, covet-province validity, vassal-patron validity, economy-builder coverage, color coverage, **and legacy-global immutability**. Full suite `10578 passed, 1 skipped`; ruff clean. The live game still runs the 19-region/5-nation world — no cutover yet (that is Slice 4/5).

- **Scope:** Make the backend *know* the full roster Slice 2 discovered so Slice 4's Europe world can reference it. **3a:** **scenario-scoped** capitals (every independent, matching the registry capitals/proxies — **N1, third pass: seed a per-Europe-world capital map at construction rather than mutating the global `NATION_CAPITALS`, which the legacy fixture + `settlement_scoring.py` depend on; the global stays Netherlands for the legacy world**), authored `power_tier` (`major`/`secondary`/`minor`) per nation, `NATION_HONOR_BIAS` as needed, gold/AP/authority config (so `RUNTIME_NATIONS` resolves to the full set), and `Utils.NATION_COLORS` extended to cover every nation rendered (Russia/Spain/Saxony already present; add ~6–9 more). **Note (second pass): `NATION_POWER_TIERS` already authors 13 nations, so `power_tier` is mostly done — the real authoring load is `NATION_COLORS` (8 today incl. "Neutral") and `NATION_DESIRE_PROFILES` (only 4 today: Austria/Britain/Prussia/Saxony → add ~9–11, else AI proposals degrade to empty desires). `TALLEYRAND_COMMENTARY` already has a `_default` fallback, so commentary degrades gracefully — prioritize desire profiles.** **3b:** the **historically-accurate vassal/satellite (client-parent) web** seeded into the world's vassal state (`vassal.py` / `world.vassals`) at build time; starting diplomats for each new independent (chancery-fallback voice — DEF-1 owns named voices); marshals via the `create_marshals_from_data` factory; `NATION_DESIRE_PROFILES` + `TALLEYRAND_COMMENTARY` per independent (CLAUDE.md "Don't Do" requires these); and 1805 starting diplomatic states/relationships.
- **Files:** `backend/nation_config.py`, `backend/models/region.py` (`NATION_CAPITALS`), `backend/models/diplomat.py`, `backend/game_logic/diplomatic_templates.py` (desire profiles + commentary), `backend/game_logic/diplomacy.py` (starting relations), `godot-client/project-sovereign/scripts/utils.gd` (`NATION_COLORS`). Tests in `tests/test_nation_config_factory.py`, new `tests/test_thirteen_nation_roster.py`.
- **Tests:** `get_runtime_nations()` returns the full roster; every independent has a capital/color/diplomat/desire-profile/commentary (parametrized "no nation missing its required config" test — the standing guard against the CLAUDE.md gap); every minor resolves to a valid patron in the vassal web; power tiers authored for all; `test_gdscript_color_centralization.py` stays green with the expanded color set.
- **STATUS line:** `Map Slice 3 LANDED — full map roster wired into backend config (every independent: capital, power_tier, color, diplomat w/ chancery fallback, desire profile + commentary; minors seeded as historical vassals/satellites; 1805 relations); roster-completeness + patron-validity tests guard every required field.`
- **Completion:** the backend roster is the full map set, fully configured (independents + historical vassal web), suite green — but the live game still runs the 19-region/5-nation world (no cutover yet).

### Slice 4 — Europe region set as an alternate backend world ✅ LANDED *(July 1, 2026 — built + tested, NOT yet the game's map)*

> **LANDED (Map Slice 4, July 1, 2026).** `create_europe_regions()` (in `region.py`) builds the validated 126-province world from the SAME `europe.json` the renderer reads: Region objects keyed by historical **name**, `adjacent` id-lists translated to names (symmetric, matches the registry), income from `REGION_TYPE_INCOME`, supply/garrison from the existing `Region` tables, and **`grid_position` via centroid spatial-rank** (amendment N4 — `derive_europe_grid_positions()`: row = north→south anchor-rank, col = west→east anchor-rank; **unique (row,col) for all 126**, N/S/E/W-preserving, no parser-algorithm refactor). A new optional `Region.grid_position` field (serialized) carries it; legacy regions read it from `REGIONS_DATA` so parity is exact, and `resolve_direction` now prefers the live region object's `grid_position` (world-scoped — the Europe and legacy maps never collide on a shared name like Paris/Berlin) with a safe fallback to the module `REGION_POSITIONS`. The **`WorldState(sovereign_map="legacy"|"europe")` seam (G1)** selects regions + the scenario-scoped roster: Europe uses its own capital map (`EUROPE_NATION_CAPITALS`, Britain=London), starting controllers, enemy roster, gold/AP/authority builders, `create_europe_diplomats()`, and seeds the 3 French satellite vassals (`_seed_europe_vassals()` maps the authored web → `vassal.py` autonomy/tribute). `WorldState()` still **defaults to legacy**, so the ~275-file gameplay fixture is untouched and the Slice-5 cutover is a reversible flag flip. Europe army placement + the full 1805 diplomatic matrix are deliberately deferred to the **1805 Scenario Setup gate** (the Europe world starts army-less — honest + turn-stable). `get_nation_capital` reads the world's own capital map; `sovereign_map` round-trips through save/load; `nation_capitals` is a derived exclusion in the serialization-enforcement guard. New `tests/test_europe_world_construction.py` (17 tests: 126 provinces, name-keyed + symmetric adjacency, derived fields, unique/ordered grid, full roster + capital ownership, vassal web, turn advance, save round-trip, direction resolution, **legacy-immutability**); full suite **`10595 passed, 1 skipped`**; ruff clean. **The live game still runs the 19-region/5-nation world — the backend cutover is Slice 5.**

- **Scope:** `create_europe_regions()` builds 126 `Region` objects from `europe.json` (owner, terrain, region_type, is_coastal, adjacency authored; income/supply/garrison/building-slots derived from `REGION_TYPE_INCOME`/`SUPPLY_BY_TYPE`/`BUILDING_SLOT_LIMITS`; `grid_position` derived from the registry anchor — **decide the method here (F5/second pass): the LLM direction parser (`strategic_parser.resolve_direction` via `REGION_POSITIONS`) reads a hand-authored `(row,col)` grid and Europe is not a grid; choose (a) bucket province centroids into a coarse grid (≈12×10) or (b) refactor the parser to real centroid bearings (cleaner, but touches a tested parser)**). **N4 (third pass) — recommend a third option (a′): derive a *unique* `(row,col)` per province by centroid spatial-rank** (col = rank of centroid-x within a latitude band, row = rank of centroid-y). A coarse ≈12×10 grid is only 120 cells for 126 provinces → guaranteed collisions, and a collision breaks `resolve_direction()`'s "move north" disambiguation (it reads a unique `(row,col)` per region, strategic_parser.py:33-39). Spatial-rank preserves N/S/E/W ordering, gives unique cells, and needs **no parser refactor**. `create_regions()` (legacy 19) is **untouched** — `WorldState()` still defaults to it. Add a `WorldState(region_factory=…)` seam so a Europe world can be constructed in tests/bootstrap without changing the default. **(G1) The factory is selected by a config flag `SOVEREIGN_MAP=europe|legacy` that the game *bootstrap* reads (Slice 5 sets `europe`); `WorldState()` still defaults to `legacy` for the test fixture. This makes the Slice-5/7 cutover a flag flip, instantly reversible (rollback = `legacy`), rather than a code edit.** Run the Golden-Rule-#8 hot-path check at 126 regions.
- **Files:** `backend/models/region.py` (`create_europe_regions`, registry loader), `backend/models/world_state.py` (region-factory seam, default unchanged). New `tests/test_europe_world_construction.py`.
- **Tests:** construct the Europe world → 126 regions, ownership matches the registry, adjacency symmetric and matches the registry, derived income/supply/garrison/grid correct, every independent present with its capital and the vassal web matching the authored patrons; a turn advances on the Europe world without error; spot-check no new per-region scan regressed a hot path (reuse `tests/test_scale_readiness_phase2.py` patterns).
- **STATUS line:** `Map Slice 4 LANDED — create_europe_regions() builds the validated 126-province world from europe.json (derived income/supply/garrison/grid); WorldState region-factory seam added, legacy default unchanged; Europe world constructs + advances a turn; suite green.`
- **Completion:** the Europe world is constructable and turn-stable in tests; legacy default and all gameplay tests untouched.

### Slice 5 — Backend game cutover ✅ LANDED *(July 1, 2026 — the backend "replace outright" flip)*

> **LANDED (Map Slice 5, July 1, 2026).** The game bootstrap is Europe: `backend/main.py` gains `_resolve_sovereign_map()` (reads the **`SOVEREIGN_MAP` env flag, default `europe`**, validated with a warned fallback; read per-call so a live process and the suite can exercise both paths) and `_build_new_world()` passes it into the G1 `WorldState(sovereign_map=…)` seam — so the module-load `world`, `_reset_world_state()`, and `/new_game` all build the **126-province Europe world** (full 20-nation roster, France player, 3 French satellite vassals, army-less pending the 1805 Scenario Setup gate). **`/map_topology` now serves the ACTIVE world's graph** (Region objects + the world's scoped starting-controllers/capital map — Britain=London on Europe, the legacy payload under rollback) instead of the module `REGIONS_DATA`. **Rollback = `SOVEREIGN_MAP=legacy`, no code change — drilled both in tests and against a live server** (126 regions/London on default boot; 19 regions/Netherlands after an env-flip restart). **DEF-2 landed:** `save_manager.FORMAT_VERSION` 2→3 with a versioned incompat message (v1/v2 saves fail gracefully, `test_save_load.py` case added). **Map-contract tests migrated to Europe:** `test_map_topology_endpoint.py` (126-province payload vs the registry + bilateral invariant + unique grid cells + a rollback-topology case), `test_map_consistency.py` (graph integrity — connected incl. sea links, bilateral, no-self, no-dupe, batch asymmetry report — now over `create_europe_regions()`; the map.gd placeholder guards remain until Slice 7 retires them), `test_map_bitmap_contract.py` (fixtures keyed off `europe.json`). **New `tests/test_map_slice5_cutover.py`** (11 tests): bootstrap default, flag validation, `/new_game`→Europe end-to-end, the G1 rollback drill, the **(G2) legacy-fixture immutability guard** — a frozen literal snapshot of all 19 regions' names/owners/adjacency + the 5-nation capital map — and a world-scoped starting-map regression. `tests/test_restart_flow.py` pinned to legacy via the flag (it tests restart mechanics against the marshal-populated fixture); a conftest autouse fixture isolates `SOVEREIGN_MAP` from dev shells. **Follow-up fold (same day):** two latent Europe-world bugs found on the post-land verification sweep — `capture_region`'s R16 threat check and the `allied_region_restored` lawful-owner lookup both read the legacy module `get_starting_controllers()` instead of the world's own starting map (on Europe, every Europe-only province name missed: recapturing own territory would accrue +2 threat; restoration credit resolved against the wrong map) — both now read `self._starting_controllers` with the `:1902` fallback pattern, regression-tested on Europe-only names (Berry/Silesia). Validator re-run PASS `--strict`; live curl verification of `/new_game` + `/map_topology` + an env-flip rollback restart. Full suite **`10609 passed, 1 skipped`**; ruff clean. Gameplay tests stay on the legacy default fixture (`WorldState()` unchanged).

- **Scope:** Repoint the **game bootstrap** to Europe: the module-load `world` and `_reset_world_state()` (`/new_game`) build `create_europe_regions()` with the full map roster (independents + vassal web) and `player_nation = France`. `/map_topology` now serves the 126-province graph automatically. Migrate the **map-contract** tests to Europe (`test_map_consistency.py`, `test_map_topology_endpoint.py`, `test_map_bitmap_contract.py`); leave gameplay tests on the legacy default fixture. Land save-incompat handling (DEF-2: version bump + clear message, no silent crash).
- **Files:** `backend/main.py` (startup `world` + `_reset_world_state`), `backend/save_manager.py` (version/incompat guard), the three map-contract test files. Possibly `backend/models/world_state.py` reset path.
- **Tests:** `/new_game` → 126-region Europe world (curl-verifiable); `/map_topology` returns 126 provinces with symmetric adjacency; the migrated map-contract tests pass against Europe; loading a pre-cutover save returns the incompat message rather than crashing; **(G2) a standing legacy-fixture immutability guard asserts `create_regions()` still returns the unchanged 19 regions (names, owners, adjacency); (G1) rollback drill: `SOVEREIGN_MAP=legacy` restores the 19-region game without a code change;** full suite green (gameplay tests still on the legacy fixture).
- **STATUS line:** `Map Slice 5 LANDED — backend game cutover: /new_game + startup build the 126-province Europe world (full map roster, France player); /map_topology serves Europe; map-contract tests migrated; save-incompat guarded; legacy 19 retained as gameplay-test fixture.`
- **Completion:** a fresh backend game **is** Europe end-to-end at the API layer; suite green; old saves fail gracefully.

### Slice 6 — Godot province-shape ownership fills ✅ LANDED *(July 1, 2026 — renderer feature, verified on the smoke scene)*

> **LANDED (Map Slice 6, July 1, 2026).** The recommended fragment-shader path shipped: `map_renderer_base.gd` gains an `OwnerFillLayer` TextureRect (same z_index as the terrain art, later sibling, below the hover highlight) whose canvas_item shader matches the sampled lookup texel against a ≤128-entry `lookup_colors` palette (per-channel epsilon 0.002 < the 8-bit step; **sea-sentinel early-out** — open water can never tint, and most fragments skip the loop) and blends the aligned `owner_colors` slot at `OWNER_FILL_STRENGTH` 0.55. **(G4 met)** `_refresh_owner_fill_palette()` — hooked into `update_all_regions`/`update_region` — is ONE ≤128-entry `PackedColorArray` uniform upload (`Utils.NATION_COLORS` + `COLOR_ENEMY_DEFAULT` fallback; unwired/controller-less → transparent); the re-tint path is source-test-pinned free of any per-pixel work, arrays always upload at exactly `OWNER_FILL_MAX_PROVINCES` (zeroed-tail hazard), and both palettes iterate the shared `_owner_fill_province_order`. The pass is gated on `_bitmap_mode` (set only in `_load_map_images()` success) — the legacy circle map is untouched. **Slice discovery:** the registry keys provinces by `Region_NNN` ids while backend map_data is name-keyed — `europe_map.gd` re-keys the registry by the `name` field at load (mirroring `create_europe_regions()`; **Slice 7 note: re-keyed `adjacent` lists still hold ids**), seeds the 1805 `starting_controller` snapshot through the real `update_all_regions()` path, and adds a smoke-only click demo cycling a province's owner via the retained seed dict (fill + tooltip stay in sync). **Live-verified** on the smoke scene (Vulkan Forward+): full 20-nation 1805 political map, sea untinted, historical hover names, click on Anjou re-tinted France→Britain next frame with matching tooltip; zero shader/script errors. Parse harness gained `MAP_CRITICAL_SCRIPTS` (scenes-dir scripts; pytest settlement mirror deliberately untouched — its staleness guard resolves `scripts/` only) and was regenerated 0 failures. `tests/test_map_owner_fill.py` (24 tests: source pins + Python behavioral mirrors over the real registry + scenes-dir harness coverage/staleness). Suite `10695 passed, 1 skipped`; ruff clean.

- **Scope:** New renderer pass that tints each province by its owner's `NATION_COLORS` color through the lookup mask. The one-time **initial paint** may mirror `_apply_unwired_grey_overlay()` (per-pixel lookup→owner→blend), but the **per-turn re-tint must NOT** (F2/second pass): the §4.4 overlay early-outs on all-wired maps (so its full-image cost is untested) and a naive 1.79M-land-px `get_pixel`/`set_pixel` mirror every ownership change is multi-second. Use a **fragment shader (lookup texture + a ≤126-entry owner-color palette uniform) — recommended — or a cached per-province pixel-index with dirty-only repaint**. The existing per-region `Panel` stylebox fill (`_refresh_region_visual`) cannot fill arbitrary province polygons, so this pass is genuinely net-new. Driven by the ownership data already flowing through `update_all_regions(map_data)`. Markers stay for marshals/garrisons/forces only (not ownership). Build and verify against `europe_map_smoke.tscn` before any game wiring.
- **Files:** `godot-client/project-sovereign/scenes/map_renderer_base.gd` (owner-fill pass + a refresh hook on ownership change), `scenes/europe_map.gd` (un-suppress ownership fills in the smoke). Tests in the existing source-level renderer suites.
- **Tests:** source-level test (mirroring `tests/test_map_renderer_cutover.py` / `test_map_unwired_overlay.py`) that the owner-fill pass maps lookup color → region → owner color and blends only owned-province pixels; Godot 4.4.1 parse harness → 0 failures; manual: the smoke scene shows provinces filled by owner.
- **STATUS line:** `Map Slice 6 LANDED — province-shape political fills (owner-tint via lookup mask, mirrors §4.4 grey overlay) added to the renderer base + verified on the Europe smoke; parse harness 0 failures.`
- **Completion:** ownership renders as real political-map fills on the smoke scene; markers reserved for forces; parse clean; **(G4) re-tint budget: an ownership change re-tints in ≤ one frame (~16 ms) via the shader palette uniform — no per-pixel CPU pass.**

### Slice 7 — Godot game cutover *(the frontend "replace outright" flip)*

> **LANDED (Map Slice 7, July 1, 2026) — plus the default-boot flip to `europe_1805.json` in the same commit (standing user decision).** **Godot cutover:** `map.gd` was rewritten in place as the Europe game map (main.tscn untouched — the smallest reversible flip) atop a new subclass chain: `map_renderer_base.gd` → `europe_map.gd` (the SHARED Europe renderer: asset paths, Region_NNN→name re-key retaining an id→name map, registry-anchor positions, marker-panel suppression) → `map.gd` (game glue: `/map_topology` handoff + Utils colors) and → new `europe_map_smoke.gd` (the seed + owner-cycle demo moved out; smoke tscn re-pointed). **Presentation decisions of record:** no persistent region labels (hover tooltips name provinces — live-verified "Champagne / France / Plains"); connection lines are the registry's 12 hand-authored `sea_links` ONLY, translated id→name (land adjacency reads as borders on the art; the re-keyed rows' `adjacent` id-lists are never drawn); **province-level fog rides the owner-fill palette** — `_refresh_owner_fill_palette()` composites `FOG_OVERLAYS[visibility]` per province (rgb lerp by fog alpha, alpha=max) ahead of the SAME single ≤128-entry uniform upload, so **G4 holds** (no per-pixel work, source-test-pinned) and the legacy circle map's FogOverlay presentation carries over. Marshal/garrison/fogged-force markers needed zero new wiring (base draws them at registry anchors). `main.gd`'s help examples updated to 1805 names ("Ney, attack Mack" / "scout Swabia" / "move to Flanders"). **Default-boot flip:** `_resolve_scenario_path()` precedence — explicit `SOVEREIGN_SCENARIO` wins (fails loudly; + preset = ValueError) → **`none` sentinel** = bare flag world (the suite-wide conftest default — zero re-baselining) → `SOVEREIGN_MAP=legacy` = no scenario (**G1 rollback re-drilled live**: 19 regions/Netherlands via env alone) → `SOVEREIGN_SMOKE_START` set = no scenario (the never-combine rule is now code) → default = the absolute repo-derived `europe_1805.json` path. **Client under backend rollback (decided):** the client ships Europe-only; legacy-named map_data entries simply don't render (guards verified) and the terminal game stays playable — a fully-legacy client is `git revert` of this commit. **Verification:** parse harness extended (MAP_CRITICAL_SCRIPTS ×4 + a scene-instantiation section — `instantiate()` without tree-entry verifies main.tscn's MapArea is scripted by map.gd) → 0 failures; `tests/test_map_slice7_cutover.py` (18 tests: 8 boot-precedence + Godot source pins + fog/sea-link mirrors + report evidence); placeholder guards retired (test_map_consistency 3, test_map_placeholder_assets 3) with successors named; **live windowed smoke on the real game**: 1805 political map w/ owner fills + fog wash, hover names, 21-marshal front, end turn → enemy phase ran (Moore took Flanders+Amsterdam via the DEF-6 walk — Slice 8's balance item, observed live; Charles attacked Bernadotte) and the map re-tinted the captures British red. Suite 10707 passed / 1 skipped; ruff clean; validator `--strict` PASS.

- **Scope:** Point the live `MapArea` at the Europe renderer (the `map.gd` node in `main.tscn` either becomes the Europe renderer or has its asset/definition paths swapped to `europe.*`), so the running game renders the 126-province map with owner fills, hover, and click. Ensure `update_all_regions`, `set_region_topology`, focus/zoom, strategic-ledger, and hotkey consumers all work against the new region set. Retire the 19-region placeholder scene from the game path (owner-rowed as DEF-3 if any test still pins it).
- **Files:** `godot-client/project-sovereign/scenes/main.tscn`, `scenes/map.gd` (or replace with a Europe map script extending the base), `scripts/main.gd` (any 19-region assumptions). Godot parse harness + headless scene-instantiation check.
- **Tests:** Godot 4.4.1 parse harness → 0 failures; headless instantiation of `main.tscn` renders the Europe map; manual smoke (F6/F5): map renders with owner fills, hover names provinces, clicks route, a turn end re-tints ownership.
- **STATUS line:** `Map Slice 7 LANDED — Godot game cutover: main.tscn renders the 126-province Europe map with owner-shape fills, hover, and click; 19-region placeholder retired from the game path; parse harness 0 failures + headless instantiation OK.`
- **Completion:** the running game **is** the full Europe map, frontend + backend; the placeholder is no longer in the game path.

### Slice 7.5 — Map presentation pass *(added July 2, 2026 from the first wide-window user playtest; owns DEF-9 / DEF-10 / DEF-11 — sequenced BEFORE the Slice 8 playtest so the balance smoke evaluates a readable map)*

> **LANDED (Map Slice 7.5, July 2, 2026) — same-day fix pass, user-blessed ("do fixes you see fit").** All four scope items shipped: **(a) DEF-9** — `min_zoom` floors at contain-fit (`_update_zoom_floor()`, recomputed per resize), boot zoom = exact contain-fit (`INITIAL_CAMERA_OVERSCAN` deleted), Camera2D hardware limits disabled (`CAMERA_LIMIT_DISABLED` wide-open; `_clamp_camera_position()` is the single centering authority — letterbox now symmetric), `BITMAP_LETTERBOX_COLOR` parchment margin (legacy circle map keeps the dark canvas), the debug overlay deleted outright. **(b) DEF-10** — `Utils.NATION_COLORS` re-authored as a set (11 entries moved: Saxony chartreuse, KingdomOfItaly bright green, Portugal purple, Denmark maroon, Switzerland rose, Naples amaranth, Bavaria pale Wittelsbach blue, Hesse lavender, PapalStates white, Sardinia cooled, Holland hotter orange) — measured on the shipped art: min pairwise blended ΔE **4.5 → 14.4**; fog hue lerp scaled by `FOG_HUE_LERP_SCALE` 0.6 (turn-1 fogged mean pairwise ΔE **11.7 → 26.5**); `COLOR_ENEMY_DEFAULT` → artificial magenta; in-shader province border pass (right/down lookup-key taps, sea-neighbor guard, no palette rescan — G4 intact). **(c) DEF-11** — new `scenes/map_label_layer.gd` (one-`_draw()` layer, world-space, fonts counter-scaled 1/zoom): nation names at radius²-weighted centroids below zoom 1.1, province names at the registry `label_anchor` above it; `Utils.display_nation_name()` + `NATION_DISPLAY_NAMES` keep internal keys off the map (R7); label data rebuilt exactly where the owner palette refreshes. **(d) DEF-12 cheap tier** — M cycles blended (0.55) → political (0.85) → terrain (0.15) via one `fill_strength` uniform write; typing guard verified (M in the command input stays text). **Verification:** `tests/test_map_slice75_presentation.py` (17 tests incl. the perceptual floor gates: blended ≥ 12 / fog ≥ 7 / fallback ≥ 30 against the pinned land mean); pins updated in test_map_renderer_cutover (overscan retired) + test_map_slice7_cutover (fog mirror carries the retention scale); `map_label_layer.gd` added to `MAP_CRITICAL_SCRIPTS` (tool + mirror); parse harness + scene instantiation **0 failures**; suite **10724 passed, 1 skipped**; ruff clean; **live windowed smoke on the real 1805 game PASS** (5136×1408 window: centered map, symmetric parchment letterbox, no void, no debug text; all 20 nation labels readable; Hanover/Hesse/Saxony/Holland distinct; M-cycle verified through all three modes; zoom-in shows province labels + Swabia hover tooltip). The `BottomLeftUI`/war-panel fixed-size scaling issue observed in the same smoke is owned as **DEF-13**. **Second same-day user feedback FOLD (July 2, 2026 — zoomed-in blur + labels reading as shrinking):** (1) the commissioned art now upscales with a mipmapped LINEAR filter (`generate_mipmaps` at load; NEAREST was blocky zoomed-in and shimmered minified) — the lookup/owner-fill layer stays NEAREST (filtering would blend province keys and break the shader's exact palette match); (2) `max_zoom` 4.0 -> 2.5 (the 2560x1600 sheet carries no information past ~2.5x); (3) the label layer was re-architected SCREEN-SPACE: world-space text rasterized glyphs at tiny world font sizes (blurry under camera scale) and constant-screen-size labels read as shrinking against the growing map — the layer now projects world anchors through the SubViewport canvas transform each draw, with fonts that GROW with zoom inside clamps (nation clampf(34*z, 22, 46); province clampf(13*z, 14, 34)) and camera pans re-projecting via a `_set_camera_position` redraw hook. Live-verified on a fresh instance at max zoom (crisp 32px province labels, smooth art) and contain-fit (nation tier). Known minor accepted v1: dense small-province labels can overlap at mid-zoom (e.g. Ile-de-France/Orleanais) — re-evaluated at the Slice 8 playtest; collision avoidance only if it impairs play. **Disposition July 2, 2026: not observed impairing play at the Slice 8 playtest; closed as accepted-v1 — reopen only on a user report.** **Third same-day user report FOLD: sea-link lines blended into the sea art** — connection lines now draw DASHED in dark map-ink (`draw_dashed_line` in `map_connection_layer.gd`; `Utils.COLOR_CONNECTION` light-grey -> Color(0.25, 0.17, 0.1, 0.8)) so the 12 registry routes read as deliberate crossings on the chart; live-verified on the Channel + Tyrrhenian/Aegean links; source+color pinned in the Slice 7.5 suite. **Pre-commit adversarial review fold (4 MINOR):** `Utils.contrast_text_color()` (luminance-gated) now picks garrison-chip text/border so light palette entries (Papal white, pale Bavaria, gold Austria) stay legible + dark outlines on the war-panel/detail score labels; both palette parsers assert entry count (a 4-arg `Color` edit can no longer silently drop a nation from the floor gates); the entry-count and DEF-10 test-column doc slips corrected.

- **Scope:** Fix the presentation defects the user hit on first look at the live 1805 map, plus the gated label decision. **(a) DEF-9 camera/void + debug overlay (defect):** floor `min_zoom` at the viewport fit ratio (recomputed in `_on_map_area_resized()`), retire `INITIAL_CAMERA_OVERSCAN` 1.18 (it guarantees out-of-map void at boot on every aspect), resolve the `_update_camera_limits()` Camera2D-hardware-limits vs `_clamp_camera_position()` centering fight (in Godot 4.4 the limits win and pin the view to limit_right/limit_top, so the entire fit deficit renders as ONE black band on the left), re-color `viewport_background` from near-black `MAP_BACKGROUND_COLOR` to the art's parchment edge tone, and delete or debug-build-gate the always-on yellow `_create_debug_label()` overlay. **(b) DEF-10 owner-color legibility:** soften the fog hue-kill in `_refresh_owner_fill_palette()` (the rgb lerp uses the FULL fog alpha — under `unknown` fog 75% of the national hue is destroyed, so the turn-1 mostly-fogged map reads as uniform grey-brown; cap the hue lerp or lerp toward a desaturated owner color while keeping the alpha wash); re-author the clustered `Utils.NATION_COLORS` entries measured confusable on the shipped art at the 0.55 blend (green 5-cluster Russia/Saxony/Portugal/KingdomOfItaly/Ottoman — Saxony↔Russia are ADJACENT at blended ΔE≈9; red 3-cluster Britain/Denmark/Switzerland; steel-blue pair Naples/Bavaria; warm-tan cluster Spain/Holland/Hanover/Hesse/PapalStates/Sardinia converging on the parchment terrain mean); change `COLOR_ENEMY_DEFAULT` (blended ΔE≈4 from Switzerland — an unmapped controller impersonates Swiss territory) to an obviously-artificial magenta; add a border pass (in-shader lookup edge detection or an offline-baked border mask layer) so province/national edges survive the fill — the PSD's painted borders are washed by the 55% overlay. **(c) DEF-11 labels (mini design gate at slice start — supersedes the Slice-7 "no persistent labels" decision of record):** zoom-LOD labels riding existing infrastructure — nation names at low zoom (radius-weighted centroid of owned provinces from `region_controllers`, recomputed where `_refresh_owner_fill_palette()` already hooks), province names at high zoom via the registry's parsed-but-unread `label_anchor` (126/126 coverage), one `draw_string_outline` pass on a MOUSE_FILTER_IGNORE layer, `_bitmap_mode`-gated. **(d)** the cheap two-position political/terrain `fill_strength` toggle IF the DEF-12 gate blesses it at the same review — otherwise DEF-12 stays a future spec with no code here.
- **Files:** `godot-client/project-sovereign/scenes/map_renderer_base.gd` (camera/zoom floor, background, debug label, fog composition, border pass, label layer), `scripts/utils.gd` (NATION_COLORS re-author — keep the backend Godot-palette mirror in `nation_config.py` + `tests/test_gdscript_color_centralization.py` in sync), `scenes/europe_map.gd` (label-layer enable). Extends `tests/test_map_owner_fill.py` + the source-pin renderer suites.
- **Tests:** source pins — fit-floored `min_zoom` / no `INITIAL_CAMERA_OVERSCAN` / limits-vs-clamp resolution / no unconditional debug label; `NATION_COLORS` pairwise perceptual-distance test at the blended fill strength (no cross-owner pair below the readability floor, adjacent-nation pairs stricter) + a fog-composition floor test (fogged-known ownership keeps a minimum hue share); label-layer source pins; Godot parse harness 0 failures; the G4 single-upload re-tint contract re-pinned (fog/border changes stay inside the palette-upload budget).
- **STATUS line:** `Map Slice 7.5 LANDED — presentation pass: camera clamped to map bounds (no void at any aspect, debug overlay removed), NATION_COLORS re-authored + fog hue-kill softened + border pass (DEF-10), labels per the blessed DEF-11 gate; parse harness 0 failures; G4 intact.`
- **Completion:** at any window aspect/zoom the camera cannot show void; the turn-1 fogged map reads as distinct nations; no confusable owner pairs on the live art; the DEF-11 gate decision executed (labels shipped or no-labels explicitly re-affirmed and the row closed); G4 preserved.

### Slice 8 — Scale & balance validation + manual playtest smoke

> ✅ **LANDED July 2, 2026** (one session with the DEF-7 registry mini-pass and Slice 9). Highlights: **(G4 measured)** bare-Europe 126-region turn = **0.49×** the 19-region legacy baseline (2.1ms vs 4.3ms median — the map itself is CHEAPER per turn; budget ≤2× passes with 4× margin); the authored 1805 campaign turn = 23ms (**5.4×** — roster workload: 9 armed AI nations/21 marshals/live coalition, not map cost), regression-pinned at 15×. **Hot-path audit:** 7 live Golden-Rule-8 violations converted to the cached indexes (`get_manpower_regen_rates` 2,520 region-visits/turn, vassal tribute, `war_status` per-HTTP-response scan, AI strategic-targets memo, garrison counts ×2, capital-proximity) + a latent **shared-adjacency-list bug fixed in `Region.__init__`** (REGIONS_DATA lists passed by reference — one in-place edit silently rewired every later world in-process; found as a test-order flake, cured with a defensive copy). **User-reported bug fixed:** `from_scenario` never ran `calculate_visibility()` — every scenario-booted world (the shipped default!) started all-"unknown", fog-washing France and showing "No intelligence" on French soil; now mirrors the constructor/save-load paths (own soil PARTIAL+, marshal locations FULL, enemy interior fogged). **DEF-6 landed** (tier garrisons + the `region_overrides` scenario key + the 12k Flanders depot). **Russia honor bias retuned 1.0 → 1.1** with the 5 DG-4 fixture pins re-derived. **Balance verdicts of record:** the Adige standoff is GEOMETRIC (Carniola↛Milan; corridor via Tyrol; 1.286 is inside mood variance, not safely under 1.3 — Charles routing to the Franconia rear is the mechanics working and historically consistent with Caldiero); Mack's turn-1 P-1 capture of Swabia is deliberate invasion mechanics (pinned); the knife-edges hold (54k = inclusive at-cap zero attrition; relation-40 alliance gap 0 never erodes — both pinned); **the Ulm knob stays at the blessed 52k band-bottom** (the 169k ring is decisive; raising Mack only re-adds pre-capture bleed). DEF-4 NOT forced (see row). Playtest smoke: 20-point scripted HTTP campaign session PASS (19/20; the 1 failure is a mock-parser-only bare-command gap, logged in BUG_FIXES.md) + the seeded 5-turn probe; `docs/MANUAL_TEST_PLAN.md` Test E is the standing Europe smoke script. Tests: `tests/test_map_slice8_balance.py` (10) + scale-suite benchmark/pin extensions + registry DEF-7/DEF-8 pins.

- **Scope:** Prove 126 regions × the full roster is performant and playable. Golden-Rule-#8 audit (grep/verify no per-region scan entered a hot path; turn-time check); AI sanity at 126 regions (`enemy_ai.py` spatial paths); coalition/dispatch density (apply Phase 5.2/5.3 collapse only if 126 regions force it — DEF-4); economy/balance sanity (incomes, supply, manpower at scale). A real playtest: start a game, issue orders, fight, run diplomacy, end several turns. **1805-scenario balance items carried from the scenario landing (July 1, 2026):** verify the Italian-front Adige standoff holds (Charles 54k vs Masséna 42k = 1.286 under the cautious 1.3 threshold but inside AI mood variance; plains Milan has no terrain defense; Charles's only approach routes through Tyrol); the knife-edge values (Charles exactly AT Carniola's 54k home cap; France|Spain relation exactly at the ALLIANCE-threshold 40); the Ulm difficulty knob (Mack landed at the 52k band-bottom for supply reasons — raising him re-adds hostile-soil bleed); DEF-6 capital-garrison differentiation; and the Russia honor-bias retune with its DG-4 fixtures. **Slice 7.5 fold residual:** check mid-zoom label overlap on the dense French small provinces during the playtest (collision avoidance only if it impairs play). *Disposition July 2, 2026: not observed impairing play at the Slice 8 playtest; closed as accepted-v1 — reopen only on a user report.*
- **Files:** `tests/test_scale_readiness_phase2.py` (extend to 126), possibly `backend/ai/enemy_ai.py`, `backend/game_logic/coalition.py`/`dispatch.py` if density forces it. `docs/MANUAL_TEST_PLAN.md` (Europe smoke script).
- **Tests:** scale test at 126 regions (**(G4) turn resolution — enemy phase + advance — ≤ 2× the 19-region baseline, measured**; no hot-path scan regression); manual playtest checklist passes.
- **STATUS line:** `Map Slice 8 LANDED — scale + balance validated at 126 regions / full roster (hot-path audit clean, AI + economy sane); Europe manual playtest smoke PASS.`
- **Completion:** the full map is performant and playable; playtest smoke recorded.

### Slice 9 — Cleanup & docs

> ✅ **LANDED July 2, 2026.** `session8_placeholder_provinces.json` retired (zero runtime consumers; its 12-test schema suite deleted with it, the 2 still-live renderer source pins moved to `test_map_renderer_cutover.py`, the unwired-overlay tests re-fixtured self-contained, the validator CLI tests re-pointed at the live `europe.json`); repo-wide `Region_NNN` audit CLEAN (ids live only in the registry/re-key machinery + their pins — enforced by `test_europe_world_construction`); docs reconciled (SAVE_FORMAT header → v3, SCALE_READINESS §4 supersession banner + §5.5 shipped-reality note + checklist rows, ADDING_CONTENT re-pointed, MODDING_FORMAT gained `region_overrides`, CLAUDE.md Current Phase rewritten, STATUS NEXT-UP → the settlement queue); DEF rows closed (2/6/7/8) or re-owned (DEF-13 → the dated UI-Scale Mini-Pass; DEF-4 → Phase 5 with the tripwire); G2 legacy-fixture guards untouched. **The real-map cutover is COMPLETE.**

- **Scope:** Retire placeholder map assets with owner rows; finalize `docs/SAVE_FORMAT_REFERENCE.md` (region-key change), `docs/STATUS.md`, `CLAUDE.md` Current Phase, `docs/SCALE_READINESS_PLAN.md` §4/§5 closeout; close DEF rows that landed; confirm the deferred ones still have homes.
- **Files:** docs + any dead placeholder assets/scenes.
- **Tests:** suite green; no dangling `Region_NNN`; docs reflect shipped behavior.
- **STATUS line:** `Map Slice 9 LANDED — placeholder assets retired (owner-rowed), docs reconciled, save-format + STATUS + CLAUDE updated; real-map cutover COMPLETE.`
- **Completion:** the cutover is closed out; every deferred item has a live owner row.

---

## Deferred / owner-row table (Golden Rule #9 — every item has a home)

| ID | Deferred item | Landing slice | Completion definition | STATUS tracking | Behavior test |
|----|---------------|---------------|-----------------------|-----------------|---------------|
| DEF-1 | Named diplomat voices for the new independents — **population corrected July 2, 2026: the 15 diplomats in `_EUROPE_EXTRA_DIPLOMAT_DEFINITIONS`** (Russia/Czartoryski, Spain/Cevallos, Ottoman/Reis Efendi, Sweden/Ehrenheim, Naples/Medici, Portugal/Araujo, Denmark/Bernstorff, Bavaria/Montgelas, PapalStates/Consalvi, Holland/Schimmelpenninck, KingdomOfItaly/Marescalchi + the chancery personas for Hanover/Hesse/Sardinia/Switzerland). There is no Morocco nation (cut at Slice 2.5; the Algiers/Egypt sliver is Ottoman) and the registry key is `Denmark`, not "Denmark-Norway". **Also folded into DEF-1 (July 2, 2026) — the loose voice/copy backlog:** the COMMITMENTS_PRESENTATION_SPEC "future copy passes" depth, the `coalition.py` `hegemony_beat_relaxation` template note, the `TALLEYRAND_COMMENTARY` `_default` gap, and a new "loyalist" Voice Bible register for 6 of the 15 | Post-cutover follow-up slice "Roster Voices" | Each new nation resolves a named diplomat per the Voice Bible (or an explicit authored chancery persona), not the bare fallback | New STATUS line when authored | A voice test per nation that the diplomat resolves + emits in-register copy |
| DEF-2 | Save migration / incompatibility — **✅ CLOSED at Slice 9 (July 2, 2026): the Slice 5 guard (`save_manager.FORMAT_VERSION=3`, clear versioned failure) is the shipped disposition; no migrator demanded** | Slice 5 (guard) → optional later migrator | Pre-cutover saves either migrate or fail with a clear versioned message (no crash) | Slice 5 STATUS line | `test_save_load.py` case: old-format save → graceful incompat message |
| DEF-3 | Per-province historical income tuning beyond region_type defaults — **PROMOTED July 2, 2026 into `docs/ECONOMY_REVISIT_SPEC.md` (slice EC-1) — that spec is now the owner; this row is the historical anchor** | Post-cutover "Economy Pass" slice → `docs/ECONOMY_REVISIT_SPEC.md` EC-1 | Authored income overrides where history diverges from the region_type default, validated | New STATUS line | Registry test that overridden incomes are within sane bounds |
| DEF-4 | Coalition friction, dispatch density, and bilateral-diplomacy O(N²) retune at the full roster — **Slice 8 disposition (July 2, 2026): NOT FORCED.** Measured: the full 1805 campaign turn (9 armed AI nations, live coalition) runs 23ms — the AI-AI phase incl. `_process_ai_ai_rivalry`'s O(N²·R) shape is ~12% of that; dispatch stayed navigable in the 5-turn probe. Owner reverts to Phase 5.2/5.3/5.6 with the 15× campaign-turn regression tripwire (`test_1805_campaign_turn_time_regression_guard`) standing watch | Phase 5.2/5.3/5.6 (Slice 8 measured, did not force) | Per `docs/SCALE_READINESS_PLAN.md` §5.2/§5.3/§5.6; no perpetual-war spiral, dispatch stays navigable, pair iteration stays per-turn cached | Slice 8 STATUS line records the measurement; Phase 5 STATUS on landing | Dense-adjacency friction test + 40-event dispatch collapse test + per-turn trade-income cache test |
| DEF-5 | Naval movement rules beyond sea adjacency — **+ FREE IRELAND rider (July 17, 2026, Nation Agendas v1.1, user-directed deferral):** the naval spec additionally owns the French liberation of Ireland — at war with Britain, a naval expedition into Ulster/Munster (both British at boot) creates the tag **Ireland** as a client nation via the NA-6 Class C creation machinery (`NATION_AGENDAS_SPEC.md` §11.4–§11.5, built by the Duchy-of-Warsaw pathfinder) with authored deck `erin_free` (guard_neutrality Ulster+Munster). Grounding: Bantry Bay 1796, the 1798 expedition, Emmet 1803 | Post-prototype "Naval" spec (Ireland rides the same spec — unreachable pre-naval, which is the whole deferral reason) | Sea edges carry movement semantics (not just graph edges) per a future spec; **Ireland: the liberation path player-reachable end-to-end (invade → clause → created client with active `erin_free` deck), GR5 (an AI France-equivalent could too)** | Future STATUS line | Movement test across a `sea: true` edge; **`test_naval_free_ireland.py` (creation + deck activation + once-only)** |
| DEF-6 | Flanders↔London walkable sea link with no naval layer — **✅ LANDED Slice 8 (July 2, 2026):** capital garrisons tier-differentiated (majors 25k / secondary 15k / minors 10k, Europe-scoped; legacy stays flat 15k; regen cap tier-aware) via `nation_config.CAPITAL_GARRISON_BY_TIER` + `WorldState.get_capital_garrison_target`, PLUS the new scenario `region_overrides` loader key stamps a 12k Flanders Channel depot (the walk-in gate is garrison ≥5k, and Flanders — non-capital — was the actual free beachhead; capital differentiation alone could not close it). Probed non-degenerate: the seeded 5-turn probe keeps Paris French; Moore's continental pressure diverts into the army-less satellites (the designed cost — France's buffer to defend) | Slice 8 (the 1805 balance validation — same pass that owns the Russia honor-bias retune) | Capital garrisons differentiated (majors 20–25k vs minors 8–10k) and the cross-Channel walk probed non-degenerate in the playtest, OR the link demoted to a DEF-5 naval-gated edge | Slice 8 STATUS line | `tests/test_map_slice8_balance.py`: turn-1..5 seeded probe (Britain never takes France unopposed; Flanders only through the fought depot), P4.5-blocked pin, and the 3-assault London-rush interception pin |
| DEF-7 | Compressed cross-Baltic **land** adjacencies in the registry — **✅ LANDED (July 2, 2026 registry mini-pass), executed over the full compressed-Baltic class discovered in the pass:** 10 edges CUT (the listed Scania↔Berlin, EastPrussia↔Finland, Posen↔Finland, Livonia↔Posen/Brandenburg, Hanover↔Oslo + same-class siblings Scania↔Brunswick/Jutland, Jutland↔Brunswick, Oldenburg↔Oslo), 4 demoted to sea links (Livonia↔Pomerania, Livonia↔Finland, Scania↔Pomerania, Finland↔Estonia), 2 connectivity sea links ADDED (Oslo↔Jutland — Denmark's Skagerrak internal route; Scania↔Gothland — Sweden's internal continuity the art broke; without them the 8-province Scandinavian cluster disconnects), and 2 kept-land decisions of record (Hanover↔Jutland = the Holstein land bridge, Estonia↔EastPrussia = the Courland stand-in). **Edge counts: `adjacent` 258 → 250 undirected; `sea_links` 12 → 18.** Validator PASS `--strict` | Registry mini-pass **before the Slice 8 playtest** (adjacency edits are scenario-safe: `europe_1805.json` authors no adjacency) | Each listed edge demoted to a hand-authored sea link or cut; validator PASS `--strict`; edge counts updated in the plan | Slice 8 STATUS line (the mini-pass rode the Slice 8 session) | `tests/test_europe_registry_data.py` DEF-7 section: cut pairs not land-adjacent, sea pairs walkable AND drawn, kept-land bridges pinned |
| DEF-8 | `is_coastal` is image-derived and over-true — **✅ LANDED with the DEF-7 mini-pass (July 2, 2026, "first consumer wins"):** the five known-wrong flags corrected to `false` (Franche-Comte, Munich, Bern, Tyrol, Milan); none was a sea-link endpoint and the only live consumers (Britain naval income/power, saturating at 3 coastal provinces) never read them. A full-126 geometric re-derivation stays owned by the DEF-5 naval spec if naval logic ever consumes the flag broadly | First consumer wins: the DEF-7 registry mini-pass if it touches the flags, else the DEF-5 naval spec | No gameplay logic consumes `is_coastal` until a review pass corrects the flags; the corrected set validated | Slice 8 STATUS line | `tests/test_europe_registry_data.py::test_def8_known_wrong_coastal_flags_corrected` pins the five |
| DEF-9 | Wide-window presentation defects (user-hit July 2, 2026): Camera2D hardware limits (`_update_camera_limits`) fight and beat the script's centering clamp — the view pins to limit_right/limit_top so the whole fit deficit renders as one black band left of the map; `INITIAL_CAMERA_OVERSCAN` 1.18 + hard `min_zoom` 0.5 guarantee out-of-map void at boot on every aspect; `viewport_background` is near-black; the yellow `_create_debug_label()` dev overlay ships unconditionally to players | Slice 7.5 (presentation pass, before the Slice 8 playtest) — **✅ LANDED July 2, 2026** | Camera cannot show void at any window aspect or zoom; any residual letterbox is symmetric and parchment-toned; debug overlay deleted or debug-build-gated | Slice 7.5 STATUS line | Source pins: fit-floored `min_zoom`, overscan retired, limits-vs-clamp resolved, no unconditional debug label |
| DEF-10 | The 20-nation owner palette was never designed as a set: measured on the shipped art at the 0.55 blend — green 5-cluster (Russia/Saxony/Portugal/KingdomOfItaly/Ottoman; adjacent Saxony↔Russia ΔE≈9), red 3-cluster (Britain/Denmark/Switzerland), Naples↔Bavaria steel blues, warm-tan 6-cluster converging on the parchment terrain; fog composition lerps away up to 75% of national hue (turn-1 map reads uniform grey-brown); `COLOR_ENEMY_DEFAULT` sits ΔE≈4 from Switzerland; no border pass survives the fill | Slice 7.5 — **✅ LANDED July 2, 2026** | No cross-owner pair below the blended readability floor (adjacent nations stricter); fogged-known ownership keeps a minimum hue share; fallback color obviously artificial; borders visible at the default blend | Slice 7.5 STATUS line | `NATION_COLORS` pairwise-distance + fog-floor + chip-contrast gates in `tests/test_map_slice75_presentation.py` (landed home; the owner-fill mirrors in `test_map_owner_fill.py` stay) |
| DEF-11 | No nation/province names on the map without hover — the user asked for labels, which **supersedes the Slice-7 "no persistent labels" decision of record**; recorded as an explicit gated decision change, never a silent edit | Slice 7.5, behind a labels mini design gate — **gate resolved zoom-LOD nation+province labels (user "do fixes you see fit", July 2, 2026); ✅ LANDED July 2, 2026** | Per the gate: labels legible on the live art without hover, or no-labels explicitly re-affirmed by the user and this row closed with the promise removed | Slice 7.5 STATUS line | Label-layer source pin + registry `label_anchor` consumption test (or the row closes with the re-affirmation) |
| DEF-12 | Map modes (political / terrain / supply / diplomatic overlays) — beyond a cheap `fill_strength` toggle, a real mode system needs fog decoupled from the owner palette (second fog-color uniform + `fog_strength`, still single-upload G4) plus mode UI/hotkey | Future "Map Modes" mini-spec + design gate, sequenced after the settlement queue resumes unless the user promotes it; **the cheap three-position toggle (M: blended/political/terrain) LANDED in Slice 7.5 (July 2, 2026)** — this row now owns only the full overlay system | Modes shipped per an approved MAP_MODES spec, OR the idea explicitly dropped at the gate and this row closed | Future STATUS line at the gate (toggle: covered by the Slice 7.5 STATUS line) | Named in the future spec; the landed toggle is pinned by `test_fill_mode_cycle_rides_the_single_uniform` |
| DEF-13 | Fixed-size HUD panels don't scale on large windows — **Slice 9 DECISION (July 2, 2026): re-owned as the "UI-Scale Mini-Pass" slice, sequenced after the settlement queue resumes unless the user promotes it.** Rationale of record: per-Control `scale` is blurry on the pinned Godot 4.4 engine (font atlases rasterize at theme size; per-viewport oversampling arrives in 4.5+); anchor-based sizing alone leaves the 182 hard-coded font-size sites (138 tscn + 44 gd) unreadable; the correct fix is window `content_scale_factor` **with the map SubViewport kept native** (drop `stretch=true`, manual `map_viewport.size = container × factor`, audit `_screen_to_world`/label projection/zoom-letterbox) — a 1.5–3 day slice touching the DEF-9-stabilized camera code and ~30 fixed-pixel scenes (top bar 36px, mailbox 600×400, ledgers 840×620, all centered modals — the class, not 2 panels), which does not fit the Slice 9 docs-cleanup scope. The screen-space map-marker question (16×16 world-unit icons at low zoom) folds into the same mini-pass. Mitigants shipped: minimizable terminal (Tab), drag-resizable war panel (≤500px/45%), screen-space zoom-growing map labels. **Mini-pass scope on landing:** content_scale_factor + native-map compensation + marker screen-space + the font-site inventory; completion = terminal/war panel/top bar/centered screens readable-proportional at ≥2560-wide with `map_viewport.size` still native (pinned); engine note — if Godot bumps to 4.5+, re-evaluate with `Viewport.oversampling` first | "UI-Scale Mini-Pass" slice (dated decision, Slice 9, July 2, 2026), after the settlement queue unless promoted | At ≥2560-wide windows the terminal, war panel, top bar, and centered screens render at readable proportional size; the map SubViewport still renders at native window resolution (pinned) | Slice 9 STATUS line records the decision; the mini-pass gets its own STATUS line on landing | `tests/test_map_slice8_balance.py::test_def13_fixed_hud_baseline_pins` freezes the pre-pass fixed offsets (main.tscn BottomLeftUI 400×270, war_status_panel.tscn 192×192) so silent changes fail; the mini-pass replaces it with the chosen-mechanism pin |

**July 2, 2026 — first wide-window user playtest feedback OWNED as DEF-9..DEF-12 above** (black void left of the map + shipped debug overlay; confusable owner colors, worst under fog; no map labels; the map-modes question). Root causes verified in code and measured on the shipped art (CIE76 ΔE under the exact `0.45*terrain + 0.55*owner` blend). **Slice 7.5 LANDED the same day (DEF-9/10/11 closed + the DEF-12 cheap toggle; see the slice banner); DEF-12's full overlay system stays gate-owned; DEF-13 (HUD panel scaling) was added from the landing smoke.** No dispositions remain floating on this feedback.

---

## Validation commands (run after any asset or code change)

```bash
# Regenerate the region key + draft registry from the PSD (only if the PSD redelivers or the deriver changes)
.venv\Scripts\python.exe tools/build_region_key_from_psd.py

# Validate the registry + bitmaps (Slice 1+ adds adjacency/gameplay-field checks)
.venv\Scripts\python.exe -m tools.validate_province_map \
  --registry godot-client/project-sovereign/assets/maps/europe.json \
  --visual   godot-client/project-sovereign/assets/maps/europe_visual.png \
  --lookup   godot-client/project-sovereign/assets/maps/europe_lookup.png --strict

# Full suite (pre-commit gate) + lint
.venv\Scripts\python.exe -m pytest tests/ -q --tb=no
ruff check backend/

# Godot parse harness (Slices 6–7) — 0 failures required
```

---

## Sequencing note

Land **Slice 1, then pause for user review** (per the slice-review cadence — first low-risk slice, then checkpoint before the big ones). Slices 2–9 proceed after that review. The two genuine "replace outright" flips (Slice 5 backend, Slice 7 frontend) are each preceded by a fully-tested, un-wired build-up slice (4 and 6) so the cutover itself is a small, reversible change rather than a big bang. The **1805 Scenario Setup design gate** (explicit step below) runs after the roster (Slice 3) and backend cutover (Slice 5) and **before the Slice 8 playtest** — its start-point decision is a prerequisite for a meaningful playtest.

---

## 1805 Scenario Setup — DESIGN GATE (explicit step) ✅ LANDED (July 1, 2026)

> **✅ SCENARIO AUTHORED + LANDED (July 1, 2026).** `godot-client/project-sovereign/assets/maps/europe_1805.json` (force-added per the `assets/maps` JSON convention) carries the full blessed opening: 21 marshals across 12 armed nations, the 7-pair WAR / 5-pair ALLIANCE matrix (sorted-pipe keys), the 29-pair relations table, the Third Coalition seed (`active_coalition`/`threat_level: 85`/`coalition_count: 1`), and the ordered France-first `starting_wars` list — live-verified to one shared instance: attackers `[Bavaria, France, Holland, KingdomOfItaly, Spain]`, defenders `[Austria, Britain, Russia]`, leaders France/Britain. Boot: `SOVEREIGN_SCENARIO=godot-client/project-sovereign/assets/maps/europe_1805.json` (repo-root relative, or absolute).
>
> **Supply-cap deviations of record** (every stack was audited against derived `supply_capacity`; the gate's named fallback — "shift Soult to Lorraine" — could not work because Ney occupied it): **(1) Ney↔Soult provinces swapped** (Davout 26k + Ney 24k = 50k on Rhineland ≤ the 60k home cap; Soult 40k solo on Lorraine; all three corps historically crossed in the Rhineland stretch — no strength trims); **(2) Mack 58k→52k** (the blessed 52k–62k Ulm knob's bottom) **+ ArchdukeCharles 65k→54k** (Carniola's home cap is 54k) — preserves the blessed **Charles > Mack** invariant with zero own-soil attrition; Mack's hostile-soil strain on Bavaria-owned Swabia (−234/turn, shrinking) is DELIBERATE invasion-in-progress flavor and test-pinned as the map's only turn-1 supply event; **(3) Milan registry terrain `mountains`→`plains`** in `europe.json` (Milan is the Po plain; the image-derived flag halved supply to 25k and bled Masséna −428/turn on the player's own dispatch — validator PASS `--strict` after the fix; the image-derived-field review class is owned by **DEF-8**). Rule of record: **own/allied-soil stacks must be attrition-free; hostile-soil attrition is invasion strain.**
>
> **Two loader folds landed with the authoring** (both world-scoped, legacy-parity-tested): **(a) omitted-`regions` injection stamps controllers + 15k capital garrisons** (mirrors `_setup_initial_control`) — previously EVERY `from_scenario` world loaded controller-less (zero income, no supply, no capturable land, France controlling 0 provinces; latent since the loader's creation, also fixed for legacy example scenarios); **(b) authored army-less nations** (the 9: Portugal/Saxony/Hanover/Hesse/PapalStates/Sardinia + 3 satellites) **are pre-seeded into `eliminated_nations_notified`** so the first end-turn doesn't announce "their forces are spent" nine times — armed nations still notify on a real elimination.
>
> **Tests:** `tests/test_europe_1805_scenario.py` (22) — owned-province placement with the three blessed off-controller carve-outs (Mack/Swabia hostile, Bernadotte/Franconia allied, Masséna/Milan satellite), full relations-dict equality, **exact 15-entry diplomatic-matrix pin** (5 ALLIANCE + 3 VASSAL + 7 WAR), satellite-movement pin, treasury pin, one-shared-instance + leaders, coalition seed, Austria subsidy target, satellite survival, the only-Mack supply pin, 3-turn advance stability, **a live enemy-phase end-turn test** (TurnManager → all 10 armed AI nations over 126 provinces, 2 turns), army-less dedupe pre-seed (Europe + authored-partial-legacy), legacy-scenario control parity, **the carried Slice-5-fold `allied_region_restored` Europe treaty-cession behavior test** (Britain cedes Holland-starting `Brabant` back → Holland's episode credits 15; `Brabant` absent from the legacy map is the discriminator; the test also documents the −90-relations PEACE-gate thaw), `SOVEREIGN_SCENARIO` boot, and the Milan deviation pin. Ruff clean; validator PASS `--strict`.
>
> **Adversarial verification fold (same day — 3-verifier workflow, 2 MAJOR + 2 MINOR + notes, all folded):** **(V1)** the Slice-4 satellite seam seeded `world.vassals` but never the pair's diplomatic state — the satellites read PEACE, which is NOT an open-movement state, locking France out of its own satellites' soil (Masséna un-reinforceable on Milan). Fixed with live-game parity: `_seed_europe_vassals` now stamps `"VASSAL"` (matching vassal.py's treaty/conquest stamps) AND the scenario authors the 3 pairs explicitly (from_dict wipes constructor states). **(V2)** the `from_dict` single-gold back-compat branch overrode France to the legacy 1200 default whenever a scenario omitted both gold keys — now world-scoped (Europe keeps its construction treasury, France 800; legacy bare-dict 1200 pin untouched). **(V3)** the Milan-plains deviation erodes the draft's "mountainous Milan" Adige-standoff wording: the cautious-AI ratio Charles 54k / Masséna 42k = 1.286 sits under the 1.3 threshold but inside the ±10% mood band, and plains Milan carries no terrain defense — mitigations: Carniola is NOT adjacent to Milan (Charles must route through ArchdukeJohn's Tyrol) and the 65k→54k trim improved the ratio; **verifying the Italian-front standoff is added to Slice 8's balance scope** (with the knife-edge notes: Charles sits exactly AT Carniola's 54k cap; France|Spain relation 40 sits exactly at the ALLIANCE threshold). Soult/Armfelt biography wording corrected. The army-less guard is deliberately scenario-global (authored-partial LEGACY scenarios also treat army-less as deliberate — test-pinned).

> Added June 22, 2026 (review follow-up). **Sequence position:** after Slice 3 (roster config) + Slice 5 (backend cutover), **before Slice 8 (playtest)** — the Europe world cannot be meaningfully playtested until it is populated with the correct 1805 armies + diplomatic posture. Captured here as an explicit step + design gate so it is **never folded silently into Slice 3**.

**Why this is its own step.** Today the entire starting state is hardcoded for the 5-nation / 19-region world: `create_starting_marshals()` + `create_enemy_marshals()` (`backend/models/marshal.py`, ~11 marshals whose `location` is an old 19-region name) and the 10-pair `diplomatic_states` / `nation_relations` matrices (`backend/models/world_state.py:381` / `:394`) plus coalition state. The Europe cutover must **re-author all of it** at the new scale — a second authoring/design pass comparable to the naming pass, not two clauses in Slice 3.

**Scope:**
- **Army placement:** every marshal `location` re-mapped to the new province names; **author marshals for the 9–11 new nations** (Russia, Spain, Ottoman, Sweden, Naples, Portugal, Denmark-Norway, Bavaria-as-independent, …) which have **zero** today — who / where / strength / personality / ability / relationships — via the data-driven `create_marshals_from_data`, not the hardcoded functions.
- **Diplomatic posture:** the 1805 state matrix across all 14–16 independents (the Third Coalition vs. France + Spain, Prussian neutrality, Ottoman stance, …), numeric relations, and any pre-formed coalition / war state. **N3 (third pass): C(16,2) = 120 pairs is too many to hand-author safely (today's matrix is 10 hardcoded pairs at `world_state.py:381`) — author it as default-to-PEACE + only the explicit exceptions** (the coalition, the Franco-Spanish alliance, active wars), not 120 literal entries.
- **The seam:** the Europe world gets its **own** marshal-placement + diplomacy-init path (parallel to the legacy `create_starting_marshals`/`create_enemy_marshals`), wired into `create_europe_regions` / bootstrap — the legacy 5-nation init stays the test fixture. Ideally this lives in the `scenario_config` loader (SCALE_READINESS_PLAN §5.5) rather than a second hardcoded init. **N2 (third pass): the loader substrate already EXISTS — `WorldState.from_scenario_file()` (`world_state.py:4790`) + `collect_scenario_nations()` / `validate_scenario_runtime_support()` (`nation_config.py`) + `backend/modding/validator.py`.** So author the 1805 setup as a **scenario JSON loaded via `from_scenario_file()`**, not a second hardcoded `create_*_marshals()` path; what is missing is the authored Europe scenario file, not the loader. This moots the "hardcode-now vs. build-the-loader" question (prior-review Decision #6).

**THE DESIGN DECISION (this gate — decide here, do not default silently):** **does the campaign start with the War of the Third Coalition already underway** (France ↔ Britain / Austria / Russia / Sweden / Naples at war at turn 1, coalition pre-seeded) **or just before it** (armed peace; the coalition forms in play)? This drives the entire starting matrix, whether a coalition is pre-formed, and where the armies sit.

- **Files:** `backend/models/marshal.py` (or a Europe scenario module / `scenario_config`), `backend/models/world_state.py` (Europe diplomacy/relations/coalition init seam), `backend/game_logic/diplomacy.py` (starting relations), possibly `backend/game_logic/coalition.py` (pre-seeded coalition). Legacy fixtures untouched.
- **Tests:** Europe world starts with every nation's marshals placed on valid **owned** provinces; the 1805 diplomatic matrix is symmetric + valid; a turn advances without error; the legacy 5-nation start is unchanged. **Plus (carried from the Slice 5 fold):** a Europe-world behavior test for the `allied_region_restored` lawful-owner path — the Slice 5 fold fixed its lookup to read the world's `_starting_controllers` but could only regression-test the capture-threat twin (the restoration path needs a real treaty territory transfer between warring Europe nations, which requires the armies/wars this gate authors). Exercise a Europe treaty cession that restores a province to its 1805 starting owner and assert the `allied_region_restored` contribution event fires with the Europe map's owner (an Europe-only province name, e.g. Silesia→Prussia).
- **STATUS line (landed):** `Map 1805 Scenario Setup LANDED — europe_1805.json authors the blessed Sept 25, 1805 opening (21 marshals / 12 armed nations, 7-war + 5-alliance matrix, Third Coalition seeded, one shared France-first war instance); supply-audited placements (Ney↔Soult swap, Mack 52k / Charles 54k, Milan terrain registry fix); loader folds: omitted-regions control injection + army-less notification guard; legacy 5-nation start retained as fixture.`
- **Completion:** a fresh Europe game opens in a correct, playable 1805 situation — armies, diplomacy, and coalition state authored and turn-stable — ready for the Slice 8 playtest.

### Researched 1805 opening draft (July 1, 2026 — ✅ GATE BLESSED: all 8 open decisions APPROVED as recommended, user, July 1, 2026)

> Produced by a 10-agent research workflow: 3 web-history researchers (belligerent timeline, orders of battle, commander availability), 4 codebase readers (marshal roster/scale, scenario loader, winnability levers, province registry), a synthesis pass, and **two adversarial verifiers — history and buildability, both `pass_with_corrections`; all corrections are folded below.** **The user blessed all 8 Open Decisions as recommended on July 1, 2026 — implementation may proceed: the Loader + Constants pre-slice lands FIRST, then the `europe_1805.json` scenario is authored against it.** Naming note discovered en route: the live loader method is **`WorldState.from_scenario`** (`world_state.py:4861`) — earlier passes wrote `from_scenario_file`.

**Start-point recommendation (THE gate decision): war already underway — with exactly THREE belligerents.** At the anchor (**Sept 25, 1805** — the Grande Armée crossing the Rhine, Ulm campaign opening) France was at war with **Britain** (since May 18, 1803), **Austria** (undeclared shooting war since Mack crossed the Inn into Bavaria Sept 8; rupture announced to the Senate Sept 23), and **Russia** (Third Coalition belligerent under the Treaty of St. Petersburg, Apr 11, 1805; Kutuzov marching west since August — first Russian combat Lambach Oct 31, first serious clash Amstetten Nov 5). **Sweden** (declares war only Oct 31) and **Naples** (secret coalition accession Sept 10 + a bad-faith *neutrality treaty with France* Sept 21) are coalition-committed but **NOT belligerent** — start both at PEACE with hostile relations seeds so the drift plays out live. **Spain** is France's ally (at war with Britain since mid-Dec 1804 — Trafalgar is 4 weeks out). **Bavaria** is at war with Austria on France's side (its army withdrew to Würzburg rather than submit). Prussia / Ottoman / Denmark / Portugal / Saxony / Hesse / Papal States / Sardinia / Hanover all neutral.

**Diplomatic state (default-PEACE + exceptions; JSON keys MUST be the sorted-pipe form, e.g. `Britain|France` — hyphenated/unsorted keys load but are never read):**
- **WAR:** France↔Britain, France↔Austria, France↔Russia, Bavaria↔Austria, Spain↔Britain, Holland↔Britain, KingdomOfItaly↔Austria. (Satellite vassalage itself is Slice-4 seeded — do not re-author.)
- **ALLIANCE:** France↔Spain, France↔Bavaria (Bogenhausen, Aug 25 — mechanically REQUIRED so French corps can stand on/march through Bavarian soil), Britain↔Russia, Britain↔Austria, and **Austria↔Russia — load-bearing:** coalition membership grants no passage (`coalition.py:1206-1209`; `OPEN_MOVEMENT_STATES` needs OPEN_BORDERS+, `diplomacy.py:61`), so without this pair Kutuzov can never legally enter Moravia and Russia idles all game.
- **Coalition seed (exact live contract keys per `coalition.py:1223-1232`):** `{"id": "coalition_1", "name": "Third Coalition", "target_nation": "France", "leader": "Britain", "members": ["Austria", "Britain", "Russia"], "formed_turn": 1, "strategic_posture": "aggressive", "posture_last_updated": 1}` + top-level `threat_level: 85` (above the 80 instant-formation tier, headroom below 90) and `coalition_count: 1`. Pre-seeding the coalition object is what switches on the 200g/turn British subsidy from turn 1. Seed the shared war via **`ensure_war_instance_for_pair()`** (`settlement_helpers`, the smoke-start path) in the loader slice — do NOT hand-author raw `war_instance` JSON (it loads as an unvalidated deep copy). All war pairs `war_start_turns` = 1 (deliberate for Britain: avoids pre-aged war-weariness handing it an instant peace-out valve).

**Army roster — 21 marshals (lean per the "add more later" directive; strengths are historical men-counts inside the proven 18k–72k statline band; every placement verified against `europe.json` ownership + adjacency; the three off-controller placements are deliberate and flagged):**

| Nation | Marshal | Pers. | Strength | Province | Historical basis (verifier-corrected) |
|---|---|---|---|---|---|
| France | Ney | aggressive | 24,000 | Lorraine | VI Corps, crossed opposite Durlach Sept 26. `movement_range: 1` — the legacy cavalry flag MOVES to Murat |
| France | Davout | cautious | 26,000 | Rhineland | III Corps, crossed at Mannheim Sept 26 |
| France | Soult | cautious | 40,000 | Rhineland | IV Corps (largest), crossed at Speyer. **Verify the 66k two-corps stack vs derived `supply_capacity`; fallback = shift Soult to Lorraine** |
| France | Lannes | aggressive | 18,000 | Franche-Comte | V Corps, crossed at Kehl Sept 25, Black Forest feint |
| France | Murat | aggressive | 22,000 | Franche-Comte | Cavalry Reserve, screened the feint. `cavalry: true, movement_range: 2` (explicit fields, not prose) |
| France | Bernadotte | cautious | 17,000 | Franconia | I Corps at Würzburg, marching from occupied Hanover. **On allied Bavaria-owned soil** (legalized by France↔Bavaria ALLIANCE). Option: fold Marmont's omitted II Corps (~20k) in → 37,000 |
| France | Massena | aggressive | 42,000 | Milan | Army of Italy on the Adige facing Charles. **On French-vassal KingdomOfItaly soil** |
| Bavaria | Deroy | literal | 22,000 | Franconia | Bavarian field army — two divisions (Deroy, Wrede) under French operational direction |
| Austria | Mack | literal | 58,000 | Swabia | Army of Germany (~72k historical, scaled to keep Charles the larger army + the pocket beatable). **On invaded Bavaria-owned soil** — models the invasion in progress. 52k–62k is the Ulm difficulty knob |
| Austria | ArchdukeCharles | cautious | 65,000 | Carniola | Austria's MAIN army (~90–96k historical, scaled; preserves Charles > Mack) behind the Adige — Carniola is the registry's absorbed NE-Italy space |
| Austria | ArchdukeJohn | cautious | 20,000 | Tyrol | Army of Tyrol at Innsbruck, hinging the two theaters |
| Russia | Kutuzov | cautious | 38,000 | Podolia | Arrival-clock abstraction: historically already in Austrian Galicia by the anchor; Podolia (his army's literal name) models the ~4–6-turn arrival lag, not the true position |
| Russia | Buxhowden | literal | 32,000 | Volhynia | Second echelon, weeks behind — one province deeper on its namesake |
| Britain | Moore | cautious | 30,000 | London | **Major-General** Sir John Moore, Shorncliffe home command (Lt-Gen only from Nov 2). **Wellington is NOT defensible** — just back from India, no command until Oct 30, first European command Dec 1805 (one brigade) |
| Prussia | Brunswick | cautious | 50,000 | Berlin | Senior field marshal / presumptive C-in-C (the 1806 supreme command projected back); armed neutrality, home cantonments |
| Prussia | Hohenlohe | cautious | 28,000 | Silesia | Senior general stationed in Silesia (army command from 1806); sits on Kutuzov's Podolia border — the tense northern flank |
| Sweden | Armfelt | cautious | 12,000 | Scania | C-in-C Swedish Pomerania 1805–07; no Swedish-Pomerania province exists (Stralsund coast is Prussian), Scania is the compromise |
| Naples | Damas | cautious | 18,000 | Naples | Reorganized the Bourbon army 1804–05; briefly expelled under French pressure in 1805, resumed command with the coalition landing |
| Denmark | Frederick | cautious | 16,000 | Jutland | Crown Prince Regent; army concentrated in Holstein (folded into Jutland) guarding armed neutrality |
| Spain | Castanos | cautious | 15,000 | Andalusia | Commandant-General, Campo de Gibraltar since 1802 — the one verified Spanish field command (Spain's 1805 effort is naval) |
| Ottoman | Abdurrahman | cautious | 24,000 | **Karaman** | Kadi Abdurrahman Pasha with ~24k Nizam-i Cedid — placed at his actual 1805 post (governor of Karaman); the Rumelia march is the April 1806 Edirne incident (verifier correction from Rumelia) |

- **Zero marshals:** Portugal, Saxony, Hesse, PapalStates, Sardinia, **Hanover** (army genuinely disbanded since Artlenburg 1803), and the 3 satellites. Sub-18k strengths (Armfelt/Castanos/Frederick/Bernadotte) are below the legacy statline floor but mechanically harmless — raise to ≥18k only if band fidelity is wanted.
- **Legacy-roster fate:** Grouchy, Drouot, Wellington, Blücher, Gneisenau, Uxbridge, Schwarzenberg, Reynier are **dropped from the 1805 scenario** (not corps commanders in 1805 / wrong theater / wrong service — Blücher was a Münster administrator, Reynier was in FRENCH service). The legacy `WorldState()` fixture keeps them untouched. Totals: France 189k in 7 field commands (six corps-equivalents + Cavalry Reserve + Army of Italy; II/VII Corps + Guard folded out for one-marshal-per-corps scale) + Bavaria 22k vs ~243k coalition on-map (Austria 143k, Russia 70k, Britain 30k) with Prussia's 78k armed-neutral.

**Relations exceptions (live scale is −100..+100 with default 0, clamped — `world_state.py:433/:1456`; legacy at-war seed is −80):** war pairs −80/−90 (Britain|France −90, Austria|France −80, France|Russia −80, Britain|Spain −80); coalition/ally warmth +40..+70 (Britain|Russia +70, Bavaria|France +60, Austria|Britain +60, Austria|Russia +60, Britain|Sweden +50, France|Spain +40, Britain|Portugal +40, Russia|Sweden +40, Naples|Russia +40, Britain|Naples +30, Prussia|Russia +30, Ottoman|Russia +20); tense neutrals −10..−70 (France|Prussia −10, France|Saxony −10, France|Portugal −10, France|PapalStates −10, France|Ottoman −20, France|Naples −30, France|Hanover −50, France|Sweden −60, France|Sardinia −70); Denmark|France +10.

**Winnability verdict: decisively winnable and historically shaped — once the blockers below land.** The opening reproduces Ulm: 5 French corps (~130k) ring Mack's 58k at the **Swabia chokepoint** (every France→Germany land entry funnels there) with Bernadotte+Deroy (39k) on his rear at Franconia — a turn-2/3 encirclement set piece; Kutuzov/Buxhöwden are 4–8 turns of marching away (the Russian clock); Charles's 65k is checked by Masséna's 42k defending mountainous Milan (the cautious-AI 1.3× threshold reproduces the Adige standoff); Britain pays subsidy behind the Flanders↔London sea link; Prussia makes the north tense. France is strong but pressed on three axes against a clock — exactly the brief. *(As-landed note, July 1, 2026: the supply-cap deviations changed the Italian axis — Charles 54k, Milan terrain corrected to plains — so the standoff now rests on the ratio 54k/42k = 1.286 under the cautious 1.3 threshold (inside AI mood variance) plus the corridor geometry (Carniola is not adjacent to Milan; Charles routes through Tyrol). Verifying it holds in play is an explicit Slice 8 balance item.)*

**⚠ LOADER + CONSTANTS PRE-SLICE — ✅ LANDED (July 1, 2026; all 8 items + the `starting_wars` loader contract; `tests/test_map_1805_loader_preslice.py`, 40 tests). Scenario-authoring notes: `regions` may simply be omitted under `"sovereign_map": "europe"` (the validated 126-province world is injected, army-less); declare wars via the ordered `starting_wars: [{"attacker": ..., "defender": ...}]` list (first entry fixes instance leaders; France-first ordering yields ONE shared coalition instance) — never raw `war_instance` JSON; the coalition seed keys remain `active_coalition`/`threat_level`/`coalition_count` per from_dict; set `SOVEREIGN_SCENARIO=<path>` to boot the scenario. Item 7 landed wider than diplomacy.py: the same world-scoped re-point (`world.get_nation_capital` / `world._starting_controllers`) was applied to every live capital/starting-controller read (diplomatic_executor war purpose, vassal loyalty garrison, war_contribution capture classification, settlement_ratify/baseline, enemy_ai safe-capture, templates capital exclusions, dispatch/ledger display, spawn/retreat, parser/objection heuristics) — behavior-identical on legacy, correct on Europe. NATION_HONOR_BIAS: Russia authored EXPLICITLY 1.0 (DG-4 spec-closure fixtures pin Russia-breaker deltas at neutral bias; re-biasing Russia is owned by the 1805 balance pass together with those fixtures). Original spec items follow:**
1. **(CRITICAL)** `validate_scenario_runtime_support` → `validate_runtime_nation_support` checks the **legacy 5-nation** `NATION_CAPITALS`/diplomats (`nation_config.py:235-262`) — hard-rejects every Europe scenario. World-scope it (validate against `EUROPE_NATION_CAPITALS` + `create_europe_diplomats()` when `sovereign_map: "europe"`).
2. `regions: "europe"` registry hook — `from_scenario` injects legacy `create_regions()` when regions are omitted (`world_state.py:4906-4910`); fill from `create_europe_regions()` instead (else all 126 region dicts must be inlined).
3. Europe-scoped `from_dict` fallbacks — omitted keys clobber Europe surfaces with legacy builders: diplomats (`:4436`), enemy_nations (`:4385`), nation_actions/authority (`:4386/:4441`), nation_gold (`:4325`), **including wiping the 3 seeded satellite vassals**. Fall back to the world's own construction-time values when `sovereign_map == "europe"`.
4. `/new_game` scenario delivery — `from_scenario` has **zero callers**; add a `SOVEREIGN_SCENARIO` env/endpoint hook through `_reset_world_state()`.
5. **`manpower_pools` for all 20 nations** (scenario field already supported; `DEFAULT_MANPOWER_POOLS` is legacy-5-only) — else 15 nations can never recruit and the war deflates after first contact ("one-punch coalition"). Size coalition pools to fund 1–2 rebuilt armies, not endless waves.
6. **`max_turns` 40 → 60+** (or implement the decided-but-unbuilt DG-5 hegemony victory): at 40 turns / 75% provinces (94 of 126 from a 28-province start) a historically dominant campaign ends in a "Time expired" defeat screen — the single highest-leverage fun fix.
7. `diplomacy.py` legacy `NATION_CAPITALS` reads (`:2814-2815` et al.) → `world.nation_capitals` — else capital war-score/liberation silently no-ops for Britain/Russia/Sweden/Ottoman (same defect class as the Slice-5 `_starting_controllers` fold). **Vienna's capture must move war score.**
8. `NATION_HONOR_BIAS` for the 15 new nations (deferred from Slice 3, owned here) + Austria AP 3→4 / fatter Russian treasury via `EUROPE_NATION_GOLD`/`EUROPE_BASE_ACTIONS` for sustained AI pressure (with DEF-3).

**Also flagged (not gate-blocking) — OWNED July 1, 2026 as DEF-6 / DEF-7 / DEF-8 in the deferred/owner-row table above:** the Flanders↔London walkable sea link (→ **DEF-6**, Slice 8 balance validation); the compressed cross-Baltic land adjacencies (→ **DEF-7**, registry mini-pass before the Slice 8 playtest); the over-true image-derived `is_coastal` (→ **DEF-8**, first consumer wins). No dispositions remain floating on this paragraph.

**Scenario JSON authoring notes (buildability verifier):** marshals as a name-keyed dict with `name`/`location` fields per the `create_marshal_from_data` contract (`marshal.py:1357-1370`); historical one-liners ride the supported `biography` field; explicit `cavalry`/`movement_range` fields (Murat), all strengths `int`; diplomatic keys sorted-pipe (`_make_diplo_key`, `world_state.py:1267-1269`).

**OPEN DECISIONS — ✅ ALL APPROVED as recommended (user, July 1, 2026):**
1. **Start point** = war underway with exactly Britain+Austria+Russia (recommended); Sweden/Naples at peace joining in play. Alt: seed Sweden as a 4th belligerent (ahistorical by 5 weeks, inert anyway with no Swedish Pomerania on-map).
2. **Naples**: de-jure neutral double-game with pro-coalition seeds (recommended; historically exact). Alt: scripted November flip — needs an event system that doesn't exist; recommend against.
3. **Hanover**: registry-true sovereign-neutral, armyless (recommended — cheap, half-accurate, preserves the Prussia-takes-Hanover diplomatic space). Alt: author French occupation (historically correct, but a detached income bloc + contradicts Bernadotte's departure).
4. **Mack on Bavaria-owned Swabia** (recommended — invasion-in-progress; needs a smoke test that the engine tolerates a hostile army in an uncontrolled province at start). Alt: set Swabia controller=Austria (legible on map; costs Bavaria income; makes Ulm a capture event).
5. **Austria↔Russia ALLIANCE** as the passage mechanism (recommended — historically exact; accepts alliance side effects binding the pair). Alt: a lighter access-only diplomatic state (new state work).
6. **Wellington→Moore swap** + dropping the anachronistic legacy eight from this scenario (parking fan-favorites as later-arrival reinforcements needs a spawn mechanic that doesn't exist — name an owner before promising it). *Disposition July 2, 2026: owner assigned — evaluate at the Marshal Content Pass design gate (`docs/MARSHAL_CONTENT_PASS_SPEC.md`); no spawn mechanic is promised until that gate opens one.*
7. **BALANCED/LOYAL personalities**: keep the live three (recommended — PERSONALITY_TRIGGERS entries were removed). Alt: wire them at this gate (Soult/Brunswick/Kutuzov natural "balanced", Deroy "loyal").
8. **Sequencing**: the Loader + Constants pre-slice above lands FIRST, then the scenario JSON is authored against it.
