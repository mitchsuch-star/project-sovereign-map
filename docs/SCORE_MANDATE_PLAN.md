# THE SCORE MANDATE — the plan after the release build (row SR)

> **Status: RULED September 26, 2026, by the user's direction — THIS DOCUMENT IS THE ROUTING AUTHORITY.** `docs/STATUS.md` ▶ NEXT UP points here; `CLAUDE.md` LIVE STATE names it. It supersedes the September 23 "Fix Updates 1–5" ordering (§6 says exactly how) and keeps the ROADMAP spine after it (Round 0 → the Victory & Objectives gate → Steam → CR-6 → Pre-EA → Trailer → EA) unchanged.
>
> **The user's brief (September 26, 2026):** *"is it included in this plan a way to get the Congress of Paris to work — you mentioned not getting there? … make the mandate raising the score in all sections, especially the weakest, and order the chunks we do this in by category, leaving bandwidth for score-increasing quick wins; then make a section after this: technology, asymmetry / national flavor, AP and DP gaps — needs design."*
>
> **The honest answer to the first question was no.** Before this document the Congress reach sat as a parked line — "the deeper reach gate is the user's" (`SYSTEMS_REFERENCE.md` §68.6, `DESIGN_REFINEMENT.md` GEV-D1) — and nothing in the Update queue moved it. **Chunk 1 below is that road**, built from what the two played campaigns actually measured, with the levers that need a design ruling handed to §4 by name instead of left in a footnote.
>
> **Evidence base.** The scores are the September 25 evening table (`docs/audits/PLAYTEST_CREATIVE_AAR_2026_09_25.md` §2), which holds the September 23 and September 12 tables' pillars plus the ones scored elsewhere (vassals — `PLAYTEST_FULL_RESCORE_2026_09_11.md`; living balance and distinct statesmen — `AI_V_SWEEP_2026_08_01.md`; agendas/formables — `INGAME_REVIEW_2026_07_25.md`). The reach measurements are GE-V's (`GE_V_PLAYED_CAMPAIGN_2026_09_25.md` §2) and VP-R1's (`VP_R1_PROBES_2026_09_25.md`, `SYSTEMS_REFERENCE.md` §68.6) plus the AAR's road (32 titled at turn 18 after Vienna, Bohemia, Hungary, Moravia and Tyrol were all held).

---

## §0 The mandate

1. **Every pillar rises; none falls.** Each chunk names the pillar it exists to raise, the rows it lands, and the arm that re-scores it. A chunk is not landed until its re-score line is written into the scoreboard (§1) with its date and its evidence.
2. **Weakest first.** The order of §2 is the order of the scoreboard's gaps: the ending (5.5), diplomacy (6.0), then the four 6.5s (first contact, economy, naval, living balance), then the 7.0s, then the 7.5s. A pillar already at target is served last (that is why most of the CRT queue moved behind the weak pillars — command holds 7.5).
3. **Chunks by category.** One pillar family per chunk. Inside a chunk the owned rows land in the order written; **30% of every chunk's session budget is a reserve for quick wins** drawn from the standing bank (§3), from ANY pillar, chosen at the chunk's start by size and score effect. A chunk that runs long drops its LAST owned row to the next chunk of its category; it never spends the reserve on owned rows.
4. **Player reports jump the queue** (kept from September 23). A row filed from an outside player's report, or from a played campaign's P1, lands ahead of the chunk in progress if it is under half a session; otherwise it heads the next chunk of its category.
5. **Design before build for §4.** Nothing under §4 (technology; asymmetry and national flavor; the AP and DP gaps) is built before its gate returns. Chunk 1 may *measure* against the AP/DP levers (it must, to know whether the road reaches 45); it may not move them.
6. **The four-file rule stands, plus one:** STATUS ▶ NEXT UP, the landing record, the rows disposed, `CLAUDE.md` LIVE STATE — and the scoreboard row in this file.
7. **Balance is attributed, never assumed.** `BASELINE_SERIES` + M1–M7 byte-identical unless a flip experiment attributes the move (the VP-R1 idiom); a chunk that moves the series without an arm is not landed.

---

## §1 The scoreboard

Scores are directional and, as before, **FOR USER CONFIRMATION** where marked. "Now" is September 25, 2026 (evening); **the Chunk 1 exit (September 26, 2026) re-scored two rows, marked ⚑.**

| Pillar | Now | Target | Gap | Chunk | What moves it (the measured cause) |
|---|---|---|---|---|---|
| **The ending** | ~~5.5~~ **6.5** ⚑ *(Chunk 1 exit, Sept 26 — FOR USER CONFIRMATION; target NOT met, residue → SR-D3)* | 7.0 | 0.5 | **1** ✅ | ~~The road to 45 titled is not there (GE-V, VP-R1, the AAR: 32 at turn 18 with the whole Austrian campaign held); a signed status quo titles nothing; a client eaten under the lord's peace costs titled soil; the paying peace removes the war that would title by treaty.~~ **Landed:** a signed status quo titles (SR-1a), the client's war is the lord's (SR-1b), every held province's road and the counsel's DP price are on the surfaces (SR-1c), the league treats when spent (SR-1d). **Still short:** the re-driven roads peak at 39 of 45 and erode (SR-1e: 37 / 31 / 16 at turn 40) — the §68.6 levers, now SR-D3's first questions. |
| **Diplomacy** | ~~6.0~~ ~~6.25~~ **6.75** ⚑ *(Chunk 2 exit, Sept 26 — FOR USER CONFIRMATION; target NOT met, residue → SR-2d; memo `docs/audits/SR_CHUNK2_EXIT_2026_09_26.md`; **SR-2d LANDED Sept 26 — the four residues disposed, the score not re-read by a slice: Chunk 3's exit re-reads it beside first contact and command**)* | 7.0 | 0.25 | **2** ✅ | The settlement editor's two verdicts; the revision table pricing the proposer's own offer; the identical proposal menu; a P1 popup that destroys its own decision (WO-32); ~~the paying peace at war-age 2~~ *(landed: SR-1d; a lord's peace covering its clients and an honest HARD_STOP label, SR-1b)*. |
| **First contact** | 6.5 | 7.5 | 1.0 | **3** | The desk shrugs at the first question a player asks; questions that order; the boolean road to the keyless delegation. |
| **Economy** | 6.5 | 7.0 | 0.5 | **5** | Admin verbs gated on military AP; the long peace with nothing to buy; the victor's Net ratchet (question (c)); the razing dominance (IGR-X9). |
| **Naval** | 6.5 | 7.0 | 0.5 | **5** | One throw of the dice is the whole road; the SHUT OUT arm never played. |
| **Living balance** | 6.5 | 7.0 | 0.5 | **7** | Nothing but the player restarts the war (PB-D1); council wars measured 0 on every seed (AI-3r §8.2). |
| **Combat legibility** | 7.0 | 7.5 | 0.5 | **4** | The garrison-blind scout; the solo assault nobody announces; "favorable" against a stacked defender; the counter-punch announced after it expires. |
| **Marshal drama** | 7.0 | 7.5 | 0.5 | **4** | Nine petitions in eighteen turns; a crown for a skirmish; the vindication verdict on the wrong battle; the petition popup's lifecycle (Antechamber). |
| **Vassals** | 7.0 ⚠ | 7.5 | 0.5 | **8** | The client has no defender of its own (VD-C); the client's petitions are the one live channel. |
| **UI/UX** | 7.0 | 7.5 | 0.5 | **9** | Contradictions that are on the screen as well as the wire (the settlement header); the auto-end with no confirm; the open sign-offs. |
| **Command & parsing** | 7.5 | 8.0 | 0.5 | **3 / 3b** | 141 of 142 offline; the Talleyrand verbs misroute; the CRT queue. |
| **Narration** | 7.5 | 8.0 | 0.5 | **6** | The dispatch's stale intel row, the marshal-bound danger flags, an armistice called a peace, foreign constructions announced as ours. |
| **AI aliveness** | 7.5 | 8.0 | 0.5 | **7** | The garrison grind and the suicide attacks; the fortify/unfortify/square dithering; a client eaten under a peace. |
| **Agendas & formables** | 8.5 | hold | — | **8** | The Proclamation has never been sighted from play; formables never reached on a played road. |
| **Directional** | ~~≈6.9~~ **≈7.0** ⚑ *(Chunk 2 exit, Sept 26: the mean of the fourteen rows, 98.25 / 14 = 7.02; Chunk 1 read 97.75 / 14)* | **≥7.5** | | | Every chunk's exit re-scores its pillar; the directional is recomputed at each exit. |

---

## §2 The chunks — by category, weakest first

Sizes are sessions; every chunk's 30% reserve (§0-3) is inside its size.

### Chunk 1 — THE ENDING: "Paris must be reachable" (≈3.0; the ending 5.5 → 7.0; diplomacy +0.25)

The road to the Congress, built from what was measured, not from the number. Order:

- ~~**SR-1a Status quo is a cession**~~ ✅ **LANDED September 26, 2026** (AAR-D2, small, the largest single effect). A province retained by a ratified status-quo clause is titled `ceded_by_treaty` (the treaty was signed for it; uti possidetis is a title everywhere in the period's practice). On the AAR road this alone is 36 at turn 9 and 37 at turn 18 with Vienna; on GE-V's Pressburg shape it is the whole "three cessions" term. Pin: a driven ratification of a status-quo peace flips the held-unsettled list to titled the same turn; the Congress line says so.
  **Landing record (rules `SYSTEMS_REFERENCE.md` §70.1; pins `tests/test_sr1a_status_quo_is_a_cession.py` 31).** The recon corrected the contract before a line was written: the treaty's terms never reach the ONE diplomatic-state setter, and the third-party peace roads (the exhausted-pair exit, the headless AI-AI settlement, the armistice thaw) never reach `note_ratification`, so the rule could not be "read the status-quo clause" — it is **read the map at the signature**: `game_end.title_status_quo_retentions` runs at `set_diplomatic_state` per pair, AFTER the state write, when a pair leaves WAR/ARMISTICE for a state above them by a SIGNED road (`SIGNED_PEACE_REASONS` — the bilateral treaty, the settlement table + the third-party peace, the exhausted-pair exit, the two vassalizations; a truce that runs out, an elimination, a repudiated treaty and the cheat write nothing — the codebase's own "a truce is not a signature"). Both directions: what each signatory's bloc holds of the other's homeland, or captured FROM the other (a conquest record naming it), is titled `treaty` with a `retained` flag, house = the signatory's lord (a satellite signing its own pair titles for its lord's house); a live SIGNED record is never overwritten; a satellite still at WAR with the ceder on its own account retains nothing until its pair is signed (SR-1b's cascade signs it by the same road). The ordering is harmless by construction: every cession applier writes its own title after the setter, and a province ceded back in the same treaty ends with the applier's record (pinned). **Three rulings on the record:** (1) a retained title is NOT reconciled — `reconciled_regions` skips it, so the loser's designs still covet the ground and the Congress is still won at the table (otherwise any white peace would silently satisfy Austria's design and the Congress would be trivial); (2) it does NOT latch the Congress's beaten-court recognition (the latch reads applied cessions, untouched); (3) the armistice thaw is excluded — no signature — and the 12-quiet-turn road still titles it. Surfaces: the ratification summary's terms list ("Status quo: Bohemia, Hungary, Moravia and Vienna stay ours by the treaty — titled." / "… stays Austrian by the treaty."), the settlement result's `status_quo_titled` + message, and a `status_quo_titled` dispatch event (MEDIUM) for the player's bloc — no new campaign-log type, no `.gd`. Serialization: the `retained` key on the existing `province_title` record (`SAVE_FORMAT_REFERENCE.md`). **Balance, measured:** `tools/_sr1_series_arms.py` — four arms, all byte-identical to `BASELINE_SERIES`; the ambient board DOES write one retention (the Britain–Spain exhausted-pair exit, one province, arm 1/3) that the AI never reads, so "never ratifies a retention" is corrected to "ratifies one it cannot see". M1–M7 byte-identical. One pin consciously flipped: `test_ge1_verification_round.py::test_a_carve_reconciles_only_the_provinces_carved` (a Tilsit carve's same peace now retains Silesia — `treaty`, retained — while the claim the pin exists for, no reconciliation, still holds).
- ~~**SR-1b The client's war is the lord's war**~~ ✅ **LANDED September 26, 2026** (AAR-1 / AAR-D1, the P1). A lord's PEACE or ARMISTICE resolves — or at least lists — its satellites' pairs with the same court (the common-peace path already resolves "N pairs"); the AI war council treats a fresh peace with the lord as a restraint on the client's pair; the offer surface states "still at war after this peace: …" where it now shows a HARD_STOP label that stops nothing (AAR-28). Effect on the road: a client's two provinces stay client soil and stay titled.
  **Landing record (AAR-D1 RULED at default (a); rules `SYSTEMS_REFERENCE.md` §70.2; pins `tests/test_sr1b_the_clients_war_is_the_lords_war.py` 21).** Measured first: nothing carried a lord's bilateral peace or truce to its clients — the settlement plan resolved same-instance client pairs only, the pair exit skipped every vassal pair by design, and a truce's expiry read each pair's OWN relation (the boot's −80 thaws to −65 in one truce: the client's war would have resumed alone while the lord's became peace). Built as ONE helper, `diplomacy.follow_the_lord` (pure read `client_pairs_that_follow`), called from the four treaty roads and NOT from the setter (elimination, the cheat and direct test writes pass through it): the bilateral ratifier (every road into `_ratify_treaty`, AI-AI included), the settlement's pair transitions (a client's pair in ANOTHER instance follows and is COUNTED among the resolved pairs), the exhausted-pair exit (the clients leave on the same truce floor), and armistice expiry (a client's truce beside its lord's is not decided on its own relation; it thaws or collapses with the lord's). Both directions — the court's clients follow it too. Every cascaded pair takes the SAME reason, so SR-1a's retention pass reads it as signed (the client's Tyrol titles for France's house at the lord's peace, pinned). (b) is subsumed: a resolved client pair is not at war, so no enemy rung can target it (the three Austrian rungs pinned; eight unattended enemy phases leave Milan and Piedmont the client's), and `cleanup_war_end` stamps the pair `resolved_turn`, so PR-1's fresh-peace floor covers it. (c) the surface: `build_war_context_snapshot` names who follows ("Our clients follow us out of the war: the Kingdom of Italy.") as a `clients_follow` fallout line beside the allies' own fallout lines, on the incoming offer AND the player's preview; the ratification aftermath says it; **AAR-28**: on an INCOMING offer the paradox conflict is a WARNING that says what the road does ("Bavaria stays at war with Austria after this peace — an ally, not a client: she fights on alone and views the separate peace unfavorably."), while the player's own outgoing peace keeps its HARD_STOP (the send road honours it) — no `.gd` (the popup already renders WARNING). Lever `THE_CLIENTS_WAR_IS_THE_LORDS_WAR`. **Balance, measured:** arms 2/3 byte-identical; the helper was called twice on the ambient board (the two pair exits) and moved nothing — no exiting court has a client. 1,148 neighbouring pins green, none flipped (the volte-face V1b docstring's "still at war with … the Kingdom of Italy" is a docstring, not an assertion).
- ~~**SR-1c The gate line teaches the road.**~~ ✅ **LANDED September 26, 2026.** For every held-unsettled province the Congress line names its shortest road to title ("Vienna — ceded if Austria signs a peace that retains it, or 12 quiet turns → turn 18"); the alarm term names what lowers it and by how much (decay, a peace, the years); the war room's counsel names the DP price of the road it recommends (AAR-D5's counsel half: "Press Austria alone — 3 DP; you have 2"). The near miss at 40–44 is legible on the ledger, the war room and the Moniteur's Congress column.
  **Landing record (rules `SYSTEMS_REFERENCE.md` §70.4; pins `tests/test_sr1c_the_gate_line_teaches_the_road.py` 20).** ONE derivation, `game_end.title_roads(world, leader)`, reads the SAME `province_title` record the count reads, so road and count cannot disagree (pinned: the turn the road names is the turn the count moves). Four roads: **quiet** (a conquest on its clock at peace with the ceder — "8 more quiet turns (titled on turn 18); a hostile army standing on it restarts the clock", and a reopened cession says so), **peace** (a conquest whose ceder is still at war — "a peace with Austria that leaves it ours titles it at the signature; otherwise 12 quiet turns from the peace": the recon corrected the contract's "12 quiet turns → turn 18", which is only true AT PEACE — the per-turn pass restarts the clock every turn of the war), **loyalty** (a satellite's own soil under 40) and **signature** (no record — no clock runs at all). Each carries a `short` clause for the paper and the desk. Surfaces: the Diplomatic Ledger's CONGRESS tab (`held_roads`, one line per province, replacing the generic "a conquest counts once …" sentence), the Strategic Ledger's Territories tab (`congress_clock.held_roads`, four under the clock line), the war room (four under the gate line, the nearest first, "… and N more on the Diplomatic Ledger's CONGRESS tab"), the question desk's Congress answer, and the Moniteur — whose Congress column now speaks at the GATE when the reach is within `gazette.NEAR_MISS_PROVINCES` (5): "THE CONGRESS OF PARIS — 42 of 45 provinces titled; 3 more and the Emperor may summon the powers. Vienna titles on turn 18." (the boot's 35 keeps the column quiet, pinned). The clock LINE itself is untouched (its shape is pinned and the driver reads its ending). **The alarm term** names its road off the tick's own helpers: "Europe's alarm below 80 (now 86 — it falls 3 a turn (one, plus one for each court at peace with us, at most three); a treaty that dissolves a league halves it)" (`congress.alarm_road`, `_calculate_threat_decay` + the league-spent rule). **The counsel names its price** (AAR-D5's counsel half): ONE quote, `diplomacy.diplomatic_price_quote` — the executor's own `get_dp_cost` over `get_transition_dp_cost` from the live state and the player's diplomat's skill, beside the points in hand — on the settlement blocker ("Press Austria alone: the separate peace. It costs 3 diplomatic points; you have 2.", the pinned sentence kept intact) and on the war room's request-terms rung ("(1 DP; you have 4)"). Two `.gd` files touched (the two ledgers' renderers); parse harness EXIT=0 (61 scripts — it caught a `:=` inference on a Variant in the first cut), boot 0 SCRIPT ERROR. No mechanics moved: series and M1–M7 untouched by construction (display only — every new function is a pure read).
- ~~**SR-1d PR-D1b — the paying peace at war-age 2**~~ ✅ **LANDED September 26, 2026** (Update 1 as written, absorbed here; its gate is P1's own coalition break-ranks clause, NOT `effective_peace_threshold`) **+ the PB-7 doc housekeeping** (the 9 rows that say OPEN but are closed, ROADMAP row 16's stale clause, the ~45 ownerless rows). The war that titles by treaty must last long enough to be won.
  **Landing record (rules `SYSTEMS_REFERENCE.md` §70.5 + §70.6; pins `tests/test_sr1d_the_league_treats_when_spent.py` 30).** Built as ruled: P1's break-ranks clause extracted as ONE helper `coalition_break_ranks_reason` (score / exhaustion / long_war / pressburg — the `long_war` limb found DEAD while extracting, subsumed by `score` since it was written; kept for byte-identical behaviour and recorded, not repaired), read by P1's clause and by `league_offer_gate`, which is applied to the OFFERING LEADER of the active coalition's war against the player only, in the producer loop and at the two `request_terms` checkpoints (the button disabled with the honest clock "exhaustion N of 80; at most ⌈(81−N)/8⌉ turns, sooner if the war turns against it"; the resolver re-checks at answer time and refuses with the same clock) — never in `_settlement_offer_eligible_for_war` (mediation shares it and stays ungated, pinned by census) and never on the player's own proposal. The rider: the producer waits a turn while a covered member's own envoy for that war is on the desk; "An Armistice treaty would be a downgrade" gets its article. **Measured on the commanded arm, three seeds, archived (`prd1b-cmd-*`): the league's first offer t4 → t9 / t10 / t11, the war ending the same turn; France 28 / 25 / 28 provinces at turn 40 — FA-D27's re-open does not fire; on this board the gate behaves as the ruling said, as Britain's exhaustion arm firing first.** Balance: `tools/_sr1_series_arms.py` grew to six arms — all byte-identical; the gate refused the ambient board's offer nine times (turns 3–11) and the passive France answers none. **The four pin families re-seated, exactly the ruling's list, each stating why:** mediation ×2 (Britain at WE 81), IQ-6's drives with THIS lever down — including the subprocess control arm, through the driver's new `--lever MODULE:NAME=0|1` (recorded in `meta.json` `levers`), because the volte-face geometry they measure no longer fits a 23-turn drive on the shipped board — and `test_iq6_volte_face.py`'s T7 committed-script drive, the fifth file of the family, which the pre-commit hook's full run caught carrying the beat ZERO times on the shipped 40-turn board (same lever through `--lever`, same reason, re-seated before the commit landed) — and the two IQ-7 slot fixtures (a queued petition needs a letter holding turn 6). **PB-7:** eight of the nine rows verified closed at HEAD with evidence (two by probe at `POST /command`, six by the landed slice's pins); **CA9-F3 measured still open on the wire** (`action_info.cost` 1 for a 2-AP march) and re-homed to SR-9; every ownerless row given a Score Mandate owner by pillar; ROADMAP row 16 corrected. `PLAYTESTING.md`'s commanded-arm table gains the row with its provenance.
- ~~**SR-1e The re-measure.**~~ ✅ **RUN September 26, 2026 — the road still stops short; the levers go to SR-D3 by name.** Re-drive the AAR road (the hand-played opening, chunked) and GE-V's played A/B with 1a–1d landed; the standing strict xfail `test_a_played_arm_reaches_forty_five_by_turn_forty` is the falsifiable target. **If a played road still stops short, the §68.6 levers — an action point for every corps, the supply cap on ally soil, the capture cascade — become the FIRST questions of §4's AP/DP gate (SR-D3), with the re-measure's numbers as the evidence.** The number 45 is not moved a third time by this plan.
  **Landing record (archives `docs/audits/playtest_digests/sr1e-{aar-road,gev-a,gev-b}` with `titled.json` series at turns 9 / 13 / 18 / 30 / 40; scripts `tools/playtest_scripts/sr1e_aar_road.json` + `sr1e_gev_b.json`, rebuilt from the archived digests' typed orders, and the committed `gev_pressburg_road.json` for A; the probe `tools/sr1e_titled_probe.py` reads the count off the run's own save through `congress.titled`, the same count the summons reads — the digest prints the clock only at the gate; pins `tests/test_vp_r1_the_road_to_forty_five.py::TestTheSR1eReMeasureIsOnTheRecord`, the xfail now reads all five archives).** **Honest limit first:** the chunked hand-played re-drive the contract asked for was not possible in this session (the previous session's chunk scripts lived in its scratchpad and are gone, and a hand-played road needs the popups answered by hand); the roads were SCRIPTED from the archived typed orders with the driver's dials answering the popups — which is why the scripted opening B is not the played B (its hand-answered popups do not survive replay; it is recorded as the road it is). **Measured, titled of 45:** the AAR road 35 / 35 / 35 / 35 / **37** (turn 9 / 13 / 18 / 30 / 40; the scripted road never stormed Vienna as the hand did — its peaces titled what it held, the last two at the end); opening A 34 / 34 / 34 / 31 / **31** (three provinces titling on turn 44 — the twelve-turn clock outlasting the window; it FELL before turn 40 on VP-R1, it completes now); opening B **39** / 37 / 37 / 35 / 16 (the peak of any road, on turn 9; the unattended tail then lost 27 → 8 provinces between turns 30 and 40). **None reaches 45; the best point on any road is 39 (B, turn 9), six short, and every road erodes from its peak** — exactly §68.6's shape. What 1a–1d did to the count: SR-1a's retentions are visible on every road's peace (the `status_quo_titled` lines in the digests), SR-1b kept every client's provinces client soil, SR-1d made the league's peace a turn-9–11 event instead of turn 4. **The number 45 is not moved. SR-D3's first questions are written below with these numbers (§4).**
- **Exit, re-scored (the four asks of the GE-V table), FOR USER CONFIRMATION:** *can it end* — yes (the Congress, the Verdict, the Falls; unchanged); *does it read as a challenge* — yes (the table, the sitting, the hold; unchanged); *can a player reach it* — **not yet**: the best re-driven road peaks at 39 of 45 and erodes, the scripted roads end 37 / 31 / 16; *does the road teach its cost* — **yes now**: every held province's road, the alarm's decay, the DP price of the counsel, a truce told as a truce, the Congress card in the School, a status quo that titles. **The ending 5.5 → 6.5 (target 7.0 NOT met — the residue is SR-D3's, by the plan's own §7 rule); diplomacy +0.25 as this chunk's header said (SR-1b, PR-D1b, AAR-28).** Chunk 1 is CLOSED; **NEXT = Chunk 2**, with SR-D3's gate slotted "immediately after Chunk 1" by §4's own rule — the user's call whether it runs before Chunk 2.
- ~~**Quick-win reserve (≈0.9):** drawn from §3 at the chunk's start.~~ ✅ **DRAWN September 26, 2026:** AAR-12 (the last admin spend leaves the day to the player), AAR-15 (a truce is announced as a truce, with its clock), the GE-V §4 nits (the Congress price names "its capital, Vilna" and puts an island court's ports lever before its capital), and the School of War's card XVII "The Congress of Paris" (18 → 19 cards, the count pin flipped consciously, the mirror updated, parse harness EXIT=0, boot 0 SCRIPT ERROR). Rules `SYSTEMS_REFERENCE.md` §70.3 + §70.7; pins `tests/test_sr1_quick_wins.py`.

**Exit:** the ending re-scored on a played arm (the four asks of the GE-V table: can it end, does it read as a challenge, can a player reach it, does the road teach its cost). Target ≥ 7 — met only if a played road reaches 45 or the gate line can state, truthfully, what would.

### Chunk 2 — DIPLOMACY: "The table tells one truth" (≈2.5; 6.0 → 7.0)

- ~~**SR-2a One verdict per screen.** The settlement editor's coverage-drop route (AAR-2): when every covered court is ≥ 50 the ratify gate reads the covered set, or the blocker names the real road and its price ("a separate peace with Austria — 3 DP"), never "Term harshness" under a "Will carry" table. The revision route carries the proposer's consent for unchanged terms and keeps the original offer answerable (AAR-3). An AI offer must score ≥ its own threshold on the player's table when laid back verbatim (AAR-3's root). The proposal menu collapses identical eased packages into one honest option (AAR-7). Ally settlement petitions get an honest arm and an expiry (AAR-26); the war label reads from active pairs (AAR-27); HARD_STOP labels stop or are renamed (AAR-28).~~ ✅ **LANDED September 26, 2026** (AAR-28 had landed with SR-1b).
  **Landing record (rules `SYSTEMS_REFERENCE.md` §71.1; pins `tests/test_sr2a_one_verdict_per_screen.py` 47; sweep `tools/_sweep_sr2a.json` 22/22 killed, 0 INERT (two pins found inert on the first sweep and replaced: the summary-vs-table disagreement, and consent riding a same-magnitude re-dial — which surfaced that the dial stamps provenance, so consent equality now ignores `authored_by`); zero `.gd`).** **AAR-2 — the root was two scorers keyed on two different courts:** the leader-level `acceptance` scored the WAR's side leader (`eligibility["accepting_leader"]`, Britain) even after a coverage drop had taken Britain off the table, while the per-court table scored the covered set (Austria 51/50) — `can_ratify` read the first and the header the second. ONE rule in two halves: (i) `settlement_validation.accepting_leader_for_coverage` — the table's accepting leader is the senior COVERED court (the side leader only while it is covered), read by the preview, the confirm dialogue's consent check and `staged_leaders`, and the ratification re-score; (ii) `settlement_staging.THE_TABLE_TELLS_ONE_TRUTH` — for a table with terms the per-court verdict IS the gate (every covered court carries, consent-aware, no hard stop) and the blocker is the table's own `carry_verdict_display` ("Will NOT carry as drafted … Holding out: Britain 32/50"), never a component label. A white peace keeps the leader gate by G4F-19's design (an empty package is exempt from the per-court gate) — its header can still read "Will NOT carry" beside a live Ratify; **recorded as residue SR-2a-X1, not absorbed** (closing it moves the white-peace gate). Pinned on the shipped board: every non-empty coverage subset of the boot war, baseline and material, Ratify live exactly when the table carries; the lever-down arm reproduces the AAR's contradiction. **AAR-3 — the root was a category error:** the revision table re-scored the OFFERING court as a stranger to its own package (Britain −32/50 for the terms Britain wrote). The revision route now stages with the accept route's FA-3 consent trio (`consenting_courts` / `consent_terms` / `consent_offer_id`), so the offering court's row reads "consents" and the verdict "Will carry — these are the terms they offered"; consent rides every restage of the SAME package (`settlement_staging.consent_kwargs_for_restage` on dials, focus, Submit for Review and Return to Terms) and lapses on the first changed term (equality over the package's SUBSTANCE — `consent_terms_equal` ignores the `authored_by` provenance the dials stamp, found when a same-magnitude re-dial read as a change); **the letter STANDS** queued behind the draft (Back Out promotes it; Accept still works) and is retired by `settlement_offers.consume_offer_by_id` at the first change or at ratification (lever `THE_LETTER_STANDS_UNTIL_THE_DRAFT_CHANGES`). Three pins that asserted consumption-at-staging were re-seated consciously (`test_settlement_incoming_offers`, `test_settlement_agency_cuts_g2c_g2f`, `test_fa_slice10_the_offer_on_the_desk`). **AAR-7:** ONE harsh transform, `diplomatic_templates.harden_proposal_terms` — the executor's `modify_harsh` arithmetic extracted verbatim and called by the button AND the menu; T1's "Harsh demands" option carries `variant: "harsh"`, hardens, then takes the G4F-9 easing every suggested peace takes; identical eased packages collapse to one option and the sentence says "Austria will sign nothing harsher today."; an eased harsh package is labelled "Firmer terms", never "Harsh demands". **AAR-26:** `settlement_offers.refresh_ally_petition_availability` derives the Grant/Honor arm from the handler's own two predicates (a mounted PROPOSE table or a suspended scoped draft) at the build, the pending-envoy poll and the mailbox activation — never baked True; the refusal ROUTES to the settlement (`must_reopen` + `reopen_target`, the client's existing road; the petition retained); and `lapse_ally_petitions_without_a_table` retires a petition at turn's end with the drafts (its table unmounted, or its war ended), with a notice on the draft-notice channel and its rail row dismissed — the FA-slice-10 caller census re-seated to NAME the fourth caller, as it asked. **AAR-27:** `_settlement_request_war_label(..., player=)` names the courts whose pair with the player is `war` and the allies still holding a live pair against them (a peace or a truce has left the table); a bare roster dict keeps the full join. **Balance:** `BASELINE_SERIES` + M1–M7 byte-identical without re-record — a fact about the harness (the passive France opens no table and files no petition), stated. Neighbouring families 2,242 green.
- ~~**SR-2b The Talleyrand verbs** (AAR-20, AAR-21 with CRT-8's mission half): "recall Talleyrand" recalls; the typed mission verb answers for the court NAMED; a relations mission with a court at war is offered on both roads or neither.~~ ✅ **LANDED September 26, 2026.**
  **Landing record (rules `SYSTEMS_REFERENCE.md` §71.2; pins `tests/test_sr2b_the_talleyrand_verbs.py` 26; sweep `tools/_sweep_sr2b.json` 8/8 killed, 0 INERT; zero `.gd`).** **The recon overturned AAR-20's diagnosis:** the running mission was never the cause. "recall Talleyrand" asked "which nation shall I approach?" because no recall word existed in the parser — the Cabinet's and the rail's button is labelled "Recall Talleyrand" but SENDS "Talleyrand, cancel mission with X", so the label the player read was not a sentence the game accepted; and "improve relations with Russia" answered about Prussia because the nation list that the recall itself had just opened (the executor's no-target proposal branch answers any nation-less sentence with its list) claimed the next line by substring (arm 1's bare containment found "russia" inside the sentence) and the resolver then matched "russia" inside "Prussia", the alphabetical first. Built: (a) the recall vocabulary (`recall`, `come home`, `bring him/Talleyrand home`, `return to Paris`, `call him back`) is the CANCEL mission INSIDE the diplomat-addressed parse (`_RECALL_RE`), so `recall Murat` (FA-S9-D1) and `recall the fleet` keep their roads and the button's own label is typable; the recall still stands behind the transit gate (IQ-4's pin holds); two corpus rows. (b) the open nation list (`expand_options` rows) may claim a line only when the line IS the court — `_line_is_only_the_court`, on arms 1 and 3 — and the resolver's containment arms match whole words (`_phrase_within`): the order runs for the court NAMED (the mission confirm for Russia), and typed "russia" picks Russia. (c) AAR-21 / CRT-8's mission half: ONE table `MISSION_ROWS_BY_STATE` mirrors the wizard's per-state rows (WAR: intelligence and undermining only — "you do not court a belligerent", the written decision the typed road never read; ALLIANCE: reassure, not court; DEFENSIVE_ALLIANCE: no courting; reassure at a full alliance only; a vassal is governed, not courted) and ONE predicate `mission_state_refusal` refuses on the typed road BEFORE the DP check (CN-3's rule: the ground before the price); a census pins that a mission is offered on both roads or neither, and a mission already running when a war starts is kept (IQ-4's rule: read at the start). Two pins re-seated consciously: `test_phase4_batch5_popups` — the 0-DP mission pin moved to Austria at PEACE, since Prussia boots at WAR and the state now speaks first; and IQ-6 T2 `test_courted_through_the_executor_every_turn` (found by the hook, not by the slice's own family run) — the courting script keeps typing "improve relations with Austria" after the volte-face ALLIANCE has ratified, and those three re-issues are now the honest refusal ("an ally is reassured, not courted"), so the all-orders-succeed assertion is scoped to the courier's turn as its own docstring states; the mission already running stays live (IQ-4's rule: the state is read at the start) and the volte-face itself is untouched. **Balance:** no AI path reads any of it; `BASELINE_SERIES` + M1–M7 byte-identical.
- ~~**SR-2c WO-32** (P1 — the vassal-rebellion popup destroys its own decision on a refused arm; Update 2's row) **+ PR-D1c and PR-D1d** (Update 5's pair — the spend forgiving aggression outside the league; a re-declared war drawing an offer at war-age 2).~~ ✅ **LANDED September 26, 2026.**
  **Landing record (rules `SYSTEMS_REFERENCE.md` §71.3; pins `tests/test_sr2c_a_refusal_keeps_the_decision.py` 7 + `tests/test_pr_d1c_the_spend_is_the_wars_own.py` 7 + `tests/test_pr_d1d_a_new_war_is_not_a_spent_league.py` 24 — the two files the rows name; sweep `tools/_sweep_sr2c.json` 18/18 killed, 0 INERT; zero `.gd`).** **WO-32 (P1)** — the rebellion modal's three handlers popped the dialogue UNCONDITIONALLY and dismissed EVERY rebellion row, then ran an arm with reachable refusals (Invest: a 3-turn cooldown, gold, DP; Garrison: 2 AP) — so on a refused turn the player was told "no", charged nothing, and lost Garrison and Accept Risk too, on a vassal one tick from rebellion. PT-A1's rule, ported from the marshal-petition channel: the arm runs FIRST; the decision is retired by SUCCESS (`vassal.A_REFUSED_ARM_KEEPS_THE_DECISION`); a refusal re-seats the modal from ONE builder the producer uses too (`build_rebellion_popup` / `rebellion_popup_for_dialogue`, the refusal on it, `vassal_rebellion_retained` on the response) so the client — which hides the modal before the answer arrives — sees it again; the rail row names its vassal (`details.vassal`) and a decision about ONE satellite dismisses ONE row (`dismiss_rebellion_row`). **PR-D1c "the spend is the war's own"** — the declaration's alarm is STAMPED on the pair it opened (`declared_by` / `declaration_alarm` inside the already-serialized war instance's `diplo_key_meta`, deep-copied on both sides of the save — the row's serialization decision, taken with zero new top-level fields), and `league_spent_alarm` keeps whole the standing alarm of every LIVE war the target DECLARED on a court outside the dissolving league (`declared_alarm_outside_the_league`, capped by the slot as it stood; the members read BEFORE the league is cleared): the row's own scenario measures 70 → 100 → **90**, not ~48; the league's own war still halves (90 → 45); a court that joined the league is the league's war; a peace ends the stamp. **PR-D1d "a new war is not a spent league"** — three rules built behind levers and MEASURED on the commanded arm with ONE French re-declaration, on Austria at loop 14 (`prd1d_redeclare.json`, `--diplomacy accept --declare-war proceed`, seed `historical`): on the historical seed France declares on turn 13 and the lever-down board offers on turn 15 — war-age 2, the filed defect; lever (b) 15 (INERT: the re-declaration is a fresh instance, so the pair's reopening is never later than the instance's birth); levers (a) and (c) both 22, when Austria breaks ranks. On austerlitz the lever-down board offers at 16 (declared 14) and (c) at 25; on marengo the lever-down board offers at 13 (declared 11) and then shows the revolving door the row is about — the script's next attacks re-declare at 14 and 24 with offers at 16 and 29 — while (c) holds the first war to 20 and the second to 36. Archives `prd1d-off-<seed>` (the explicit lever-down arm, which reproduces the pre-flip board line for line), `prd1d-c-<seed>`, `prd1d-a-historical`, `prd1d-b-historical`. ⚠ The two extra seeds' FIRST base arms were launched in the same message as the lever flip and ran with (c) already up — they matched the (c) arms line for line and were DISCARDED; the record's baselines are the explicit lever-down arms. **Chosen: lever (c)** — a war the player declared (any of the player's live pairs in it carrying the stamp; the alliance cascade brings Britain and Russia back to Austria's side, so the leader is not always the court declared on) is gated on its LEADER's break-ranks clause, exactly as the league's war is; the gate names its reason (`war_not_spent`: "Austria has not broken ranks on the war we declared … at most N turns") on the producer's refusal record, the request-terms button and the resolver; a war declared ON the player stays ungated (a court that attacks France may sue when the floor passes, as before); mediation shares no gate. Lever (b)'s substrate ships (a re-attached pair records `reopened_turn` and carries its stamp); the lever itself stays down (measured inert on the re-declaration arm: a fresh instance). **Balance:** `BASELINE_SERIES` + M1–M7 byte-identical — the passive France declares no war and answers no rebellion modal, so every seam this slice touches is inert on the ambient board — a fact about the harness, measured (63 pins green with the lever up).
- ~~**Quick-win reserve (≈0.75).**~~ ✅ **DRAWN September 26, 2026** — six bank rows, each a nit a reader feels. **AAR-22** the raw vassal tag on the guarantee refusal ("Kingdom of Italy is already under French protection", `display_nation` + `nation_adjective` at the refusal), "the **British** squadrons" (`naval_executor.intercepted_at_sea_line`, ONE sentence for the interception) and a corps already standing on an undefended enemy province "takes Bohemia where he stands" (`combat_executor`'s undefended-capture exit; Chunk 5's reserve line loses AAR-22's naval copy — taken here). **AAR-16** a foreign court's completed work names its owner in the terminal AND the campaign log — "Austria completes a market at Vienna." (`WorldState._construction_complete_message`, both the building and the watchtower arm; `campaign_log.format_event_oneliner(event, player_nation=)` with the `/campaign_log` route and Le Moniteur passing the player; our own receipt byte-identical; lever `world_state.FOREIGN_WORKS_NAME_THEIR_OWNER`). **AAR-30** the capital discount is OURS only — `economy_executor.capital_is_the_nations_own` reads `get_nation_capital`, the AI priced through the same helper (GR5: Austria at a captured Munich pays full), the recruit event's `capital_discount` / `stability_premium` flags now the terms the pricer APPLIED (lever `THE_CAPITAL_DISCOUNT_IS_OURS_ONLY`; `nation=None`, the legacy direct-call sites, keeps the region-type rule). **AAR-18** "what can I build" with no corps on our soil answers from the executor's own gate, the capital first then by income (`question_desk._first_own_region_that_can_build`; a corps on our soil is still preferred; lever `THE_DESK_BREAKS_GROUND_WITHOUT_A_CORPS`; landed here ahead of CRT-7, whose desk half it was). **AAR-14** the supply remedy never names the soil of a court we are at war with (`dispatch._ground_is_contested`, sited before the move probe; measured: an enemy CORPS on the ground was already the probe's refusal, so no second rule; lever `THE_REMEDY_NAMES_ONLY_OPEN_GROUND`). **AAR-13** the roster's Starving/Crowded flag follows the PROVINCE — `_collect_supply_attrition_turns` keys `(marshal, province)` and the run and its cause are read where he STANDS (the morning after he leaves starving Swabia for a fed Munich there is no flag); a legacy per-marshal list still reads as "wherever he was" (the fixtures' shape); lever `THE_DANGER_FLAG_FOLLOWS_THE_PROVINCE`. Rules `SYSTEMS_REFERENCE.md` §71.4; pins `tests/test_sr2_quick_wins.py` 32; sweep `tools/_sweep_sr2_reserve.json` 16/16 killed, 0 INERT; `BASELINE_SERIES` + M1–M7 byte-identical (measured — AAR-30 is the one rule that can move the AI's board, and no AI levy on the ambient run is raised at a capital-type province that is not the recruiting court's own); zero `.gd`. **One pin re-seated consciously:** `test_iq1_iq1_3_the_granary.py::TestTheHostsPriceIsNotDiscounted` — IQ1-3A2's `foreign_soil` arm had proved the host's capital (Milan) stops discounting our recruiting; AAR-30 now closes that hole from the other side on BOTH arms (Milan is not France's own seat), so the lever pin runs with AAR-30's rule down and a second pin asserts the shipped board prices Milan at the ordinary rate either way.

~~**Exit:** diplomacy re-scored on a played arm that opens the settlement table as victor and as loser, answers a mediation, and courts a court to a treaty.~~ ✅ **RUN September 26, 2026 — CHUNK 2 CLOSED** (memo `docs/audits/SR_CHUNK2_EXIT_2026_09_26.md`; archives `docs/audits/playtest_digests/sr2-exit-{victor,loser,courted,mediation,mediation-saves}`): four driven arms, then the saves hand-driven at the wire through the client's own routes — **the victor's table** (the AAR road, turn 10: Britain's white peace "France retains Bohemia; Bavaria retains Tyrol" reviewed as ONE verdict — "Will carry — these are the terms they offered", every court consenting to its own terms, ratify open), **the loser's table** (the passive France beaten to Rhineland; at turn 31 Britain's requested package — 5,406 gold, a Duchy of Normandy carved out of France, Britain and Austria retaining nine provinces — reviewed with consent and RATIFIED at the wire, the carve winning), **a mediation answered** (the Tilsit arm: Russia signs a bilateral peace on turn 8 and offers her good offices on the residual war at the end of turn 21 — read from the turn-21 save at the wire and answered), **a court courted to a treaty** (Austria, beaten and courted, proposes Open Borders 22 → Non-Aggression 25 → Defensive Alliance 28 → ALLIANCE 31 herself). **Re-scored FOR USER CONFIRMATION: diplomacy 6.25 → 6.75** (target 7.0 NOT met) — held below 7 by four legibility residues the exit itself found, filed as **SR-2d** in the §3 bank and `BUG_FIXES.md` §Score Mandate Chunk 2 exit: the letter's status-quo summary naming a province the same letter carves; a ratified settlement riding the rail as outcome REJECT / "Diplomatic Action"; the requested price moving 5,406 → 5,398 between letter and review; the Arbiter's Offer indistinguishable from Britain's own on the rail. Directional ≈7.0 (98.25 / 14). The volte-face beat did not print on the courted arm (IQ-6's T2 runs that arm with SR-1d's lever DOWN; on the shipped tree the ladder alliance lands ~turn 29–31) — a standing question for the Chunk 7 re-score, said in the memo.

- ~~**SR-2d — the four exit residues (SR-2-X2 … X5), ≈0.4.**~~ ✅ **LANDED September 26, 2026, at the head of Chunk 3 by the user's direction** (rules `SYSTEMS_REFERENCE.md` §71.5; rows `BUG_FIXES.md` §Score Mandate Chunk 2 exit; pins `tests/test_sr2d_the_letter_tells_the_truth.py` 26; sweep `tools/_sweep_sr2d.json` 18/18 killed, 0 INERT; zero `.gd`). **Reproduced first** on a re-driven loser arm (`tools/playtest_driver.py --turns 32 --diplomacy decline --settlement decline --save-at 30,31`, the exit's own series 5,177 / 5,693 / 5,508 / 5,840 / 4,946 reproduced to the gold) and hand-driven at the wire from the turn-30 save through `/command`, `/mailbox/activate` and `/respond_to_diplomatic_dialogue`. **X2** — the letter subtracts what it carves: `_derive_status_quo_lines(world, war, settlement_terms)` leaves out every province a clause of the same package moves (`cession_shaped_regions`, carves AND cessions), which is what the ratifier's after-the-appliers read already did; measured after, the letter's line and the ratified line agree province for province. **X3** — the PL-14 net names the settlement: `main._settlement_proposal_result_fields` reads `Settlement` / `ACCEPT` / "Settlement Ratified" / the war label off the ratifier's own feedback block, and the rail keeps a result's own title; every other result byte-identical. **Its rider**, found while reproducing it: the letter's own rail row ("Settlement offer from Britain (x5)") stood on the rail after the settlement it announced was ratified — it leaves with the letter now, on the accept, reject and deferred-consumption roads, keyed on the offer. **X4 — NOT REPRODUCED and CLOSED with its invariant pinned:** the letter's figure and the review's were identical on the accept route, the revision route (seed and Submit) and a delayed activation (Δ = 0 on all three; the treasury moved by exactly the letter's figure). The exit's 5,406 → 5,398 were two RUNS' letters — a probe that ends the turn without the driver's per-turn reseed prices the requested package (EC-W4, 0.15 × the payer's chest) off a chest the unseeded AI turn moved — not a re-price between letter and review; the row's diagnosis is corrected on the row. **X5** — ONE clause, `good_offices_clause(mediator)`, on the rail row (title "Russia offers good offices"), the dispatch event (`mediator` — the driver's digest can now tell), the mailbox row (the clause FIRST, so the 72-character cut cannot drop it) and the review the letter opens (`mediator` / `mediator_interest` on the staged and mounted dialogue, the header led by the clause); the Arbiter's Offer is pinned through the REAL AI diplomatic phase, and the belligerent's own letter is byte-identical. Every rule has a lever whose down arm reproduces the exit's reading. **Balance:** `BASELINE_SERIES` + M1–M7 byte-identical (1,394 gate pins green) — display only by construction: every change is a sentence, a row or a stamp on a dialogue, none a decision the AI reads. **One pin re-seated consciously, found by the pre-commit hook and not by the slice's own family run:** FA slice 10's `_mounted_settlement_dialogue` caller census (`test_fa_slice10_review_round_2026_09_05.py`) asks to be told of every new caller — X5's review stamp is the fifth, named on the pin. **The diplomacy row stays 6.75 FOR USER CONFIRMATION** — SR-2d closes the residues the exit named as what held the pillar below 7.0, but a re-score is a played arm's, not a slice's; Chunk 3's exit re-reads it beside first contact and command.

### Chunk 3 — FIRST CONTACT & COMMAND: "The desk answers" (≈2.5; first contact 6.5 → 7.5; command 7.5 → 8.0)

- **SR-3a CRT-3 "a question never orders"** (Update 2's other row) **+ CRT-7 "the desk answers what the order would do"** (AAR-17 the war question and the three shrugs; AAR-18 the build desk; AAR-19 the dishonest offers, to verify at the wire; AAR-23 the objection's undisclosed price; AAR-29 the model's invented commands; AAR-31 the reasonless objection) **+ L-D "the boolean road"** (Update 3 — the keyless delegation gated on ONE boolean; behind a user gate because guardrail (e) is CR-5's blessed scope) **+ CX5-L5-F2 + CQ-30** (Update 3's pair).
  **Part (i) — CRT-3 + CX5-L5-F2 + CQ-30 ✅ LANDED September 26, 2026** (landing record `COMMAND_ROBUSTNESS_SPEC.md` §12.8; rules `SYSTEMS_REFERENCE.md` §72.1; pins `tests/test_crt3_a_question_never_orders.py` 72 + `test_crt6_the_retreat_is_a_word.py` 21 (CRT-6's file, its first pin) + `test_crt2_the_name_is_never_replaced.py` 13 (CRT-2's file, its first pin); sweep `tools/_sweep_sr3a_i.json` 18/18 killed, 0 INERT; corpus 791/791; families 6,394 green; `BASELINE_SERIES` + M1–M7 byte-identical — the AI never parses text; ONE `.gd`, parse harness EXIT=0, boot 0 SCRIPT ERROR). **Re-measured first, every family reproduced at the wire.** CX-X1: the Cabinet's parser reads the SHARED question verdict (`THE_CABINET_READS_THE_SHARED_QUESTION`) — `why not declare war on Prussia` had staged the war-purpose chooser against a court at PEACE and `why not downgrade relations with Spain` had downgraded a real alliance for 1 DP; a question now reaches Talleyrand's advisory and outranks the mission words, and the client door moved with it (`main.gd` mirrors the parser's subject rule: `should we …` / `can we …` are SENT, `will you …` stays a claimed order — the door's own mirror pin is what found it). CXR1-2: the comma tail stands the subject arms down only when it OPENS with an order verb (a vocative keeps the question). CXR1-4: a hedge is a question (`perhaps build ships` had laid a keel); `time to …` deliberately not. CXR1-5 / N2 / N3: a leading run of up to SIX non-order words hides no deliberative question (the row measured three; `I was just wondering why not retreat` ordered a general retreat at four during the build), the bare wh-leads deliberately NOT read behind a run, and the interjections are never an address (the parser's meta-addressee arm now consults `never_an_address`). L2-7b: a court is a subject, with its adjective, its plural and its article. CX5-L5-F2: the retreat noun takes a demonstrative, a proper possessive and the "line of" bridge (`Lannes, cut off Mack's line of retreat` had retreated Lannes). CQ-30: a target word one typed mistake from a foreign commander's surname takes the fog-honest ASK arm (`Ney, attack Archduke Charls` had fought Mack). **Two pins flipped consciously, found by the families:** IQ7-X7's `maybe accept` line (a hedge fails closed — CRT-5's ruling one line early; the five real deferrals stay CRT-5's) and the Cabinet door's mirror. **L-D SKIPPED** — it sits behind a user gate the session's prompt left unfilled.
  **Part (ii) — CRT-7 "the desk answers what the order would do" + AAR-17 / AAR-19 / AAR-23 / AAR-29 / AAR-31 ✅ LANDED September 26, 2026** (landing record `COMMAND_ROBUSTNESS_SPEC.md` §12.9; rules `SYSTEMS_REFERENCE.md` §72.2; pins `tests/test_crt7_the_desk_reads_the_order.py` 110; sweep `tools/_sweep_sr3a_ii.json` 39/39 killed, 0 INERT; `BASELINE_SERIES` + M1–M7 byte-identical — display and parser only; ONE `.gd` (`objection_dialog.gd`), parse harness EXIT=0, boot 0 SCRIPT ERROR). **Re-measured first at the wire on the shipped boot** — every row reproduced (the four war questions, "is Vienna safe?", "what does Kutuzov have with him?", the truce clock and "who are my allies" all shrugged; "how is the war effort" was pointed at the Economy tab; "what happened last turn" got the raw shrug; the what-if named Kutuzov's unseen cell and mustered against our Bavarian ally and across a truce; "how much is a gun" quoted 654g with no commander of guns serving; the income sentence summed to 1,120 against a Net of +1,842; "who is winning" read three even scores for one coalition war; "what can I do" at 0 actions offered four refused orders; the boot's own counsel offered `Ney, drill`, which the game refuses). **The rule, sixteen ways, each behind a lever:** seven desk kinds (`wars` off the war banner's own rows with our purpose and their designs, `allies`, `safe` with the garrison read only in view, `truce_clock` by the engine's expiry rule, `war_effort`, `news` off the morning dispatch, "what does X have" on the strength arm) and the past-tense WH lead; the what-if fog-honest and refusing like the order (`friendly_fire_refusal`, the armistice block, the declaration warning for a court at peace); the price as `recruit_quote` (the capital first, then the cheapest levy as the order that raises it, else the executor's refusal), the counsel's levy line the same; the income sentence built from `NET_GOLD_COMPONENTS` so it sums; who is winning off `build_active_wars`; the router on whole words; the counsel reading the action points (`end turn` named at zero), CN-4's refusals (no drill beside the enemy, a two-action fortify not on one), the crossing gate (AAR-19 — `Marmont, attack Moore` never offered) and spreading its orders across the army; the insist arm's price on the payload, the sentence and the client's button (AAR-23); Berthier's live reply read back through the fast parser and the prompt handed the counsel's lines (AAR-29); the strategic objection reading its type as a verb and naming the order (AAR-31); our own fallen answered in the first person. **Rider found by the pins:** the attack refusal under a truce printed `armistice_cooldowns` — written once, never decremented — as "turns remaining"; it reads the truce's clock. **Decided:** the odds do NOT rank the counsel (a first cut promoted Soult over Ney and reddened 24 first-contact pins that quote the boot's first line — the counsel offers what the executor takes, the what-if answers the odds); DESK-11 CLOSED as F2's and pinned; DESK-12's dead half deleted; AAR-29's IQ9-X3 half stays SR-3c's. **Two pins re-seated consciously:** the CX-2 levy line on the quote (it had pinned the ledger's 450g beside an order that charged 518 — the row's finding), the session-3 armistice refusal on the truce's clock. **SR-3a is COMPLETE.**
- **SR-3b CRT-2 "the name is never replaced"** (CQ-17, CQ-29 and the rest of its rows). **✅ LANDED September 26, 2026** (landing record `COMMAND_ROBUSTNESS_SPEC.md` §12.10; rules `SYSTEMS_REFERENCE.md` §72.3; pins `tests/test_crt2_the_name_is_never_replaced.py` (49 new, 62 in the file with CQ-30's 13); corpus +11 `crt2-*` rows, 791/791 mock, the eight defect rows failing on the pre-slice tree; sweep `tools/_sweep_sr3b.json` 20/20 killed, 0 INERT at close; `BASELINE_SERIES` + M1–M7 byte-identical — structural; zero `.gd`). **Re-measured first at the wire:** `Davout, grant Ney a rente` pensioned Davout and `Davout, endow Ney with Swabia` endowed him (200 gold, irreversible); with Soult at Paris, `recruit infantry in Swabbia` / `in Atlantis` / `in Franche-Comté` / `in Austria` raised 10,000 men AT PARIS for 654 gold; build and repair answered "Specify a region" to a named place. **Built, each behind a lever:** a reward goes to its OBJECT (a fallen object answered with the desk's sentence, never "Did you mean Ney?"); accents never hide a name; the named ground of recruit / build / repair is KEPT (and repair's direct object read); the executor reads an unresolved province through the march road's region matcher and re-runs on it, the reading disclosed on every answer — never the capital. **Rider:** a hold the game placed no longer claims "Our maps read Rhineland…" (every bare hold said it). **Decided:** the recipient rule is the one sentence the sweep could observe (its narrower forms measured INERT and removed); a named marshal keeps PF-7's road. **Filed:** CQ-37 (idioms — `at once` before a clause, `hold fast`) → the reserve; CQ-38 (a reward to two marshals drops the second) → CRT-11. **Drive-by:** a pre-existing 1-in-300 dice flake in the estate battle-note test pinned.
- **SR-3c L-1** (the local-parser memo's unconditional slice, carrying IQ9-X1 and IQ9-X3 — the live-road failure stamped `parse_mode: mock`). **✅ LANDED September 26, 2026** (landing record `COMMAND_ROBUSTNESS_SPEC.md` §12.11 + the memo's §7; rules `SYSTEMS_REFERENCE.md` §72.4; pins `tests/test_l1_the_prompt_turns_around.py` (29, incl. the 16-cassette attribution) + `tests/test_cr6_retry_rescues_the_word_scan.py` (14); sweep `tools/_sweep_sr3c.json` 18/18 killed, 0 INERT at close; zero `.gd`). **The parse prompt is static first:** the same order one battle apart shares **84 → 15,771** characters (≈4,381 tokens, estimated), holding ≥ 15,921 over twelve turns once the examples were made board-independent (they had decayed the prefix to 12,199 by turn 8 — found by measuring past the contract); lever down = the pre-slice prompt byte for byte (42 of 42), which attributes the re-stamp of 16 IQ-9 cassettes; the dead addressee params wired. **IQ9-X1:** the retry's live reading of the marshal stands — `hunt down mack` answers as `pursue mack`. **IQ9-X3:** a failure names the road it came down. Prompt caching stays off (a fresh user decision, its obstacle gone). **SR-3 is COMPLETE; next the Chunk 3 reserve.**
- **Quick-win reserve (≈0.75):** the School's card for the Congress road; the boot help answering "who am I fighting".

**Exit:** first contact re-scored on a fresh-boot played arm (the ten-minute test: the questions a new player asks, answered); command re-scored on the golden corpus + a played arm.

### Chunk 4 — COMBAT LEGIBILITY & MARSHAL DRAMA: "The field says what it will cost" (≈3.0; 7.0 → 7.5 each)

- **SR-4a The field's price.** The scout names the garrison and the works (AAR-4); an assault gets its one-line muster ("the corps beside him do not join; the garrison regains 2,000 a turn", AAR-24); the counter-punch is announced when it opens (AAR-25); the muster band folds the defender's standing modifiers and the attacker's stance into "favorable/even/unfavorable" and names the stance when it hurts (AAR-32); ~~the fortify objection prices its stance shift (AAR-23 lands here if 3a did not take it)~~ *(AAR-23 taken by SR-3a part (ii), Sept 26)*.
- **SR-4b The standing-order pass** (AAR-8 the unanswerable question; AAR-9 cannon fire for a war France is not in; AAR-10 reinforcing drops the order; AAR-11 vindication on the wrong battle) **with CRT-4 "the road law read where it is quoted" and CRT-5 "an answer is read closed"** (the same seams).
- **SR-4c The drama's fuse** (AAR-D3: a laurel floor — a battle of `battle_scale.is_a_battle` size or a decisive exchange — for crown and envy; a per-marshal petition cooldown longer than the 2–3 turns measured; F4's reward fuse as the model) **+ the petition Antechamber B1** (Update 4 — `PETITION_POPUP_REVISIT_SPEC.md` §9) **and B2–B5 as the budget allows** (WO-32's popup-lifecycle family lives here too when 2c did not close it).
- **Quick-win reserve (≈0.9).**

**Exit:** both pillars re-scored on a played arm that fights a garrison, a stacked defender and a rival's battle; petitions counted per turn.

### Chunk 5 — ECONOMY & NAVAL: "The chest and the sea" (≈2.0; 6.5 → 7.0 each)

- **SR-5a The chest.** AAR-6 (admin verbs exempt from the military pre-check), AAR-12 (no auto-end on an admin spend without a word), AAR-30 (the capital discount is ours only); **question (c)'s design gate** (the victor's Net ratchet, `IMPROVEMENT_QUEUE_SPEC.md` §0.6b) held and answered; **IGR-X9** (the razing dominance on a built province) decided at the same gate; the EC-2 pass-2 residue with a home — ES-4 development, ES-7b `confer_title`, the pooled-allied-dead rider [P3-5] — either landed or struck with reasons (GR9).
- **SR-5b The second road at sea** (AAR-D7): the expedition chip names the levers that move the odds (corps size, a diversion first, a pooled escort) the way the D5 instruments name theirs; the played A2 strangulation arc and the SHUT OUT arm finally played (the standing DEF-5 evidence); NV-D3/NV-D9's naval-econ residue landed with 5a or struck.
- **Quick-win reserve (≈0.6):** ~~"the Britain squadrons" and the rest of AAR-22's naval copy~~ (✅ TAKEN by Chunk 2's reserve, Sept 26 — the whole of AAR-22 landed there; the ≈0.6 is drawn from the bank's remaining rows).

**Exit:** economy re-scored on the three arms of the IQ-1 exit (a commanded France, a passive France, a beaten France); naval on a played Descent or strangulation arc.

### Chunk 6 — NARRATION, DISPATCH & COPY: "Berthier tells the truth" (≈1.0; 7.5 → 8.0)

- **SR-6a The dispatch pass:** the intel row prefers the live full-view sighting (AAR-5); the danger flag is keyed on (marshal, province) (AAR-13); the supply remedy names only provinces a corps can march into (AAR-14); foreign constructions name their owner or stay off our tactical events (AAR-16).
- **SR-6b The copy pass** (F2's successor, NPC-12's next census slice): an armistice is an armistice (AAR-15); raw tags and in-place walk-ins (AAR-22); the war label (AAR-27 if 2a did not take it).
- **SR-6c The near miss is a story:** the Moniteur's Congress column and the Verdict's register lines know the count that was reached (with SR-1c).

**Exit:** narration re-scored on a played arm's dispatches; this chunk is also the quick-win bank's largest donor.

### Chunk 7 — AI ALIVENESS & LIVING BALANCE: "Europe acts without France" (≈2.0; 7.5 → 8.0; 6.5 → 7.0)

- **SR-7a The AI's odds gate and its dithering** (AAR-D8): a floor under the assault rung and a garrison that surrenders under 500; the cautious kit's odds gate on the AI's attack rungs (the player's delegation obeys one, the glory attack obeys one); an anti-oscillation guard for fortify/unfortify/square (the `ai_square_cooldown` idiom, now serialized).
- **SR-7b The long peace, measured and gated** (PB-D1, the ROADMAP-12 gate item): the two passive AAR arms and the commanded arm are the baseline (threat 0, 0 enemy attacks for 26–36 turns); the question for the gate is whether a promoted Revanche or an emergent design may open a war against a France at peace without a coalition (the volte-face and emergent-design machinery exist; AI-3r's council wars measured 0 because every warlike 1805 design targets the hegemon). Measure, then rule; build only the ruled arm.
- **SR-7c Dispersion** (AAR-D4, with the FA-D27 owner): the crowding threshold on major cities and capitals, or the tax lifted for corps under one lead's SUPPORT/HOLD; the retreat's friendly set includes ally and vassal soil France may enter (PC15-D1's law already knows the answer).

**Exit:** AI aliveness re-scored on a played arm; living balance on the AI-V sweep's three arms (the D1 band, at last, with a played moment).

### Chunk 8 — VASSALS & AGENDAS: "The client stands" (≈1.5; vassals 7.0 → 7.5; agendas hold 8.5)

- **SR-8a VD-C "The Contingent"** (`VASSAL_DEEPENING_SPEC.md` §9; IQ7-X1..X3): a client with a corps of its own can defend itself — the mechanical half of SR-1b's ruling.
- **SR-8b The formables on a played road:** the Proclamation has never been sighted from play (IGR-D's Q2 still open); a driven arm that carves a client through the settlement path, and the client's own petitions and Proclamation on the client screen.
- **SR-8c IQ7-D3 and the vassal ledger's petition legibility;** IQ7-D2 (the Suitor) stays declined.

**Exit:** vassals re-scored on a played arm that grants, refuses and loses a client; agendas checked on the formables arm.

### Chunk 9 — UI/UX: "What the wire says, the screen says" (≈1.0 + the user's eyes; 7.0 → 7.5)

- The settlement panel's contradictions on screen (SR-2a's client half: the header, the chips, the greyed road's reason); the auto-end confirm's client half; a counter-punch rail row; the IQ-10 frames re-shot at both Interface Scales for every surface a chunk touched; the standing sign-offs the user still owns (the end screen's four registers, F3's six surfaces, F5's header, the Congress surfaces' feel, VP-R1's muster rows).

**Exit:** UI/UX re-scored from the frames and the user's pass.

### Chunk 3b — COMMAND, the rest of the CRT queue (≈4.0; command 8.0 → hold)

CRT-6 (the retreat is a word), CRT-8 (the Cabinet's rules on every road — the mission half already in 2b), CRT-9 (the state speaks first), CRT-10 (the suggestion is honest — with or after CRT-1's landed guard), CRT-11 (the second name is heard), in `COMMAND_ROBUSTNESS_SPEC.md` §12.3's order. Placed here by the mandate: command already holds 7.5 and the weak pillars want the sessions first. Round 0's typed transcripts may pull any CRT row forward (the plan's own rule).

### Then the ROADMAP spine, unchanged in order
Round 0 (11) → the Victory & Objectives gate (12–13; it now owns PB-D1's *ruling* only, SR-7b having done the measuring) → Steam (14) → CR-6 (15) → Pre-EA (16) → Trailer (17) → EA (18). §4's three gates are held inside the spine wherever the user rules them (§4 recommends the slot for each).

---

## §3 The quick-win bank (the 30% reserve)

Small rows — each ≤ 0.25 session, each raising a score a reader can feel — drawn by any chunk at its start. A row leaves the bank when a chunk takes it; the bank is refilled from every review's P3/P4 tail. Pillar tags say what each raises.

| Row | Pillar | The win | Size |
|---|---|---|---|
| AAR-12 | Economy / UX | No auto-end on the last admin spend without a word. | 0.1 **✅ TAKEN by Chunk 1 (Sept 26).** |
| AAR-15 | Narration | An armistice is an armistice (event + headline). | 0.1 **✅ TAKEN by Chunk 1 (Sept 26).** |
| AAR-16 | Narration | Foreign constructions name their owner or stay off our events. | 0.1 **✅ TAKEN by Chunk 2 (Sept 26).** |
| AAR-22 | Narration / naval | "the Britain squadrons" → "British"; "from Bohemia into Bohemia" → "takes Bohemia where he stands"; the raw vassal tag. | 0.15 **✅ TAKEN by Chunk 2 (Sept 26).** |
| AAR-27 | Diplomacy | The war label reads from active pairs. | 0.1 **✅ TAKEN by Chunk 2, SR-2a (Sept 26).** |
| AAR-28 | Diplomacy | A HARD_STOP label stops, or is renamed. | 0.1 **✅ TAKEN by Chunk 1, SR-1b (Sept 26) — the incoming offer's paradox is a WARNING that says what it does.** |
| AAR-30 | Economy | The capital discount is ours only. | 0.05 **✅ TAKEN by Chunk 2 (Sept 26).** |
| ~~AAR-31~~ | Marshal drama | ~~A cautious objection names its concern.~~ **TAKEN — SR-3a part (ii), Sept 26, 2026** (`THE_OBJECTION_NAMES_ITS_CONCERN`). | ~~0.15~~ |
| AAR-25 | Combat legibility | The counter-punch announced when it opens. | 0.15 |
| AAR-18 | First contact | "what can I build" uses the executor's own gate. | 0.1 **✅ TAKEN by Chunk 2 (Sept 26).** |
| AAR-14 | Narration | The supply remedy names only provinces a corps can enter. | 0.1 **✅ TAKEN by Chunk 2 (Sept 26).** |
| AAR-13 | Narration | The danger flag keyed on (marshal, province). | 0.15 **✅ TAKEN by Chunk 2 (Sept 26).** |
| GE-V §4 nits | The ending | "St Petersburg / take Vilna" → "its capital, Vilna"; Britain's "or take London" after the ports lever. | 0.1 **✅ TAKEN by Chunk 1 (Sept 26).** |
| School card | First contact | A School of War card for the Congress road (the gate line's terms, in the tutor's voice). | 0.2 **✅ TAKEN by Chunk 1 (Sept 26) — card XVII.** |
| Boot help | First contact | The boot help's "who am I fighting" answer (AAR-17's desk kind `wars` LANDED in SR-3a part (ii); the boot help's own line remains). | 0.1 |
| CQ-37 | Command | An idiom is never a place or a condition: `at once` before a clause is not a contingency, `hold fast` / `stand fast` is a bare hold (filed by SR-3b, Sept 26, 2026; `BUG_FIXES.md` CQ-37). | 0.1 |
| IQ10-X residue | UX | The top bar at Interface Scale 2.0 and the petition fold, re-shot. | 0.15 |
| SR-2-X2 | Diplomacy | The requested letter's status-quo summary never names a province the same letter carves. | 0.1 **✅ TAKEN by Chunk 2, SR-2d (Sept 26).** |
| SR-2-X3 | Diplomacy / UX | A ratified settlement's rail row reads what happened, never `REJECT` / "Diplomatic Action". | 0.1 **✅ TAKEN by Chunk 2, SR-2d (Sept 26) — with the rider that the letter's own rail row leaves with the letter.** |
| SR-2-X4 | Diplomacy | The price on the letter is the price at the review (5,406 → 5,398). | 0.1 **✅ TAKEN by Chunk 2, SR-2d (Sept 26) — NOT REPRODUCED at the wire (Δ = 0 on three routes; the exit's two figures were two runs' letters); the invariant is pinned.** |
| SR-2-X5 | Diplomacy / narration | The Arbiter's Offer names its mediator on the rail and in the mailbox. | 0.1 **✅ TAKEN by Chunk 2, SR-2d (Sept 26) — and on the dispatch event and the review it opens.** |

---

## §4 NEEDS DESIGN — after the chunks (three gates; nothing built before they return)

Each gate is a session of research and a memo with the questions answered at recommended defaults, in the project's idiom (`AI_INTENT_SPEC.md` §6 / `HEALTH_CHECK_DESIGN_GATE_2026_08_14.md`). The recommended slot for each is named; the user's ruling places it. **Rule of the house for all three: authored content, not formulas** (`AI_INTENT_SPEC.md` §6 D7 — every varying value carries an authored range in `europe_1805.json`, and the AI takes the same rules by GR5, which is not the same as taking them competently — the seasons memo's lesson).

### SR-D1 Technology — "Reforms, not research"

- **What exists.** Nothing mechanical. ROADMAP Phase 8.5 lists "Light Tech/Reforms — simple upgrades: conscription, tactics, administration — Planned". The economy has the levy, the substitute market, training grounds, the Continental System decree machinery, the marshal's `administration` skill; the naval block has keels; the Cabinet has instruments. There is no research, no tree, and no period justification for one in a 40-turn 1805 campaign.
- **The question.** Should the reign have a handful of STATE REFORMS — bought once, with a currency the game already prices (DP, authority, gold, an admin action), each a named 1805-period act with a stated effect — rather than a technology tree?
- **Candidate shapes.** (i) *Reforms* (recommended): five to eight authored acts per nation with a price and a term — e.g. for France the *Corps System / the Staff* (+1 action point: the AP gap's natural lever, see SR-D3), the *Conscription Law* (the substitute market's size and price), the *Berlin Decree* (Continental System closure as a decree, not only as blockade), the *Artillery Reserve* (the artillery pool and Marmont's arm), the *Imperial Guard expansion*, the *Code* in a client (loyalty); for Prussia the *Army Reform* after a defeat (Scharnhorst — drill and the levy), for Austria the *Landwehr*, for Russia the *Serf Levy* (cheap men at low morale) — each nation's list authored in the scenario. (ii) *Tech tree*: rejected on period grounds unless the gate says otherwise. (iii) *Events* (Phase 8.5's Historical Moments) as the delivery vehicle for reforms — a reform offered as a choice at a beat.
- **Gate questions.** Q1 reforms or nothing (a tree is not on the table); Q2 the currency (DP vs authority vs gold vs an admin action — the recommendation is authority for the political acts and gold for the material ones); Q3 the count and the term (5–8 per major, permanent vs 12-turn); Q4 both boards (GR5) — and how the AI chooses (a deck order, like the agendas); Q5 whether the Staff reform IS the AP lever (SR-D3) or a separate thing; Q6 interplay with seasons (HC-6) — the Russian reforms and the General Winter are one design conversation.
- **What not to build first:** anything that changes a boot number (every reform is off at boot; `BASELINE_SERIES` byte-identical by construction).
- **Slot:** after Chunk 5 (economy) — a reform is an economy and command lever and the gate wants those pillars' numbers settled first.

### SR-D2 Asymmetry and national flavor — "France feels different from Austria"

- **What exists.** The courts already differ by authored numbers and rosters: statecraft profiles (Austria the coalition-builder, Prussia the bandwagoner, Russia the arbiter, Britain the paymaster — `AI_INTENT_SPEC.md` §3.4, scored 8.0 for "distinct statesmen"), agenda decks, the navies block, capital garrison tiers, marshal skills and abilities, the letter-book's voices (DEF-1's fallbacks for eleven courts), the paymaster's purse, the Continental System. What does NOT differ: the mechanics. Every nation recruits, marches, musters, starves and fortifies by one rulebook with authored constants; the player is always France; "National Flavor — Planned" and "Multi-Nation Play — post-EA" are the ROADMAP's only words on it.
- **The question.** Which THREE asymmetries would make the 1805 board read as five different armies rather than one army in five coats — without a second playable nation?
- **Candidate shapes.** (i) *National doctrines* as authored modifiers with a sentence: France's corps system (the muster's arrival odds and the SUPPORT lever are the mechanic — a doctrine names and tunes them), Britain's sea and subsidy (exists) plus a land army that cannot be replaced (the 5,000-man raids are that), Russia's depth (serf levies at low morale, the General Winter — HC-6 — and a retreat that does not break), Austria's archdukes (the quarrel exists; the Landwehr reform is SR-D1's), Prussia's drill (a drilled corps's bonus larger, its fragility after Jena — the emergent Revanche exists). (ii) *Historical Moments* (Phase 8.5): the Coronation, Tilsit, the Retreat — authored beats with a choice, the same machinery as the Proclamation. (iii) *Multi-nation play*: out of scope for this gate (post-EA), but every doctrine must be written so a Britain player could inherit it.
- **Gate questions.** Q1 which three first (recommendation: Russia's depth + winter, Britain's irreplaceable army, France's staff/corps doctrine); Q2 modifiers vs mechanics (a modifier is a number the AI already plays; a mechanic needs an AI rung — the seasons memo's "symmetric rules are not symmetric competence"); Q3 where they are authored (the scenario's `statecraft`/`nation_config` blocks, validator-clamped); Q4 whether Historical Moments ship with the doctrines or after; Q5 the measurement (the AI-V sweep's homogeneity guard + a played arm that can NAME each court's style).
- **What not to build first:** a second playable nation; anything the AI cannot play.
- **Slot:** after Chunk 7 (AI aliveness) — the doctrines are AI behaviour as much as player flavor, and HC-6 (seasons, deferred past Round 0) belongs in the same ruling.

### SR-D3 The AP and DP gaps — "four action points for eight corps"

- **What exists.** Four military actions and two administrative ones a turn, fixed (`world_state.max_actions` / `max_admin_actions`); a strategic order costs 2 (1 for a literal marshal and for the sovereign); unused admin actions pay 25 gold each; the turn auto-ends when both pools are spent (AAR-12). Diplomatic points: five a turn (base 3 + Talleyrand's skill + authority), the Seat +1 with the Emperor at Paris; a courtship 2 a turn, a client's petition 1, an armistice 1, a separate peace 3, a summons 2. The AI's marshals move themselves (every corps acts every turn); the player's do not.
- **The measured gaps.** §68.6: half the army idles every turn; the hand-played arms used all four and still could not both strike and cover; the AI's corps all move. AAR-D5: one client's petition (1 DP) cost the Austrian peace a turn (the chip greyed for want of a third point); the Seat's +1 needs an Emperor who is never at Paris in a campaign. The standing orders (march / hold / support) are the intended multiplier and the two played reviews used them little because each costs 2 and the tail is fragile (AAR-9, AAR-10).
- **Candidate shapes.** (a) *AP scales with the roster*: one action per two corps, floor 4, ceiling 6 — the simplest, and the one the reach measurement most wants; (b) *the Staff reform* (SR-D1) buys the fifth and sixth point — the same number, earned; (c) *standing orders cheaper*: SUPPORT at 1 AP, a march that keeps its tail through a reinforcement (SR-4b) — the multiplier the design already intends, made reliable before any new points are minted; (d) *DP banking*: unspent points carry one turn (cap 7), or the separate peace priced at 2, or the war room names the DP price of the road it recommends (SR-1c takes the last of these regardless); (e) *admin actions convertible*: an unused admin action becomes a diplomatic point instead of 25 gold.
- **Q0 — SR-1e's numbers (September 26, 2026), the FIRST questions of this gate by Chunk 1's own rule.** With SR-1a..1d landed the re-driven roads still stop short of 45: the AAR road 35 → **37** at turn 40, opening A 34 → **31** (three provinces titling on turn 44), opening B **39** at turn 9 → 16 (its unattended tail lost 27 → 8 provinces after turn 30). The best point on any road is 39, six short, and every road erodes from its peak. So the gate must answer, in this order: **Q0a** four action points for eight corps — the AAR road's turns 7–10 spent 13, 7, 6 and 12 typed orders for four actions a turn (refusals are free, the idle half of the army is not), and opening A's three held provinces title on turn 44 because the corps that took them could not also cover them; **Q0b** the 37,500 supply cap that scatters the army (opening B's peak was one concentrated strike a turn, and its erosion began when the body dispersed); **Q0c** the capture cascade (B's tail: 27 → 8 provinces in ten unattended turns — the road that was 39 at turn 9 ends at 16). Per §2 Chunk 1, 45 is not moved; per this section, nothing here is built before the gate returns.
- **Gate questions.** Q1 whether AP should move at all before Chunk 1's re-measure says the road needs it (recommendation: (c) first — it is legibility, not balance — then read SR-1e); Q2 (a) vs (b) — a free scaling or an earned reform; Q3 DP: bank, reprice, or counsel only; Q4 the AI's parity (the AI already acts with every corps; a player AP rise is a player-only change and the series will move — attribute it); Q5 the admin pool (convert, or leave).
- **What not to build first:** a fifth action point without the flip attribution (the whole reach measurement depends on it); anything that changes what a literal marshal pays.
- **Slot:** immediately after Chunk 1 IF SR-1e says the road stops short (§2, Chunk 1); otherwise after Chunk 4 (the standing-order pass is (c)'s prerequisite).

---

## §5 Measurement — how a chunk is scored

- **The arms.** Chunks 1, 2, 3, 4 and 8 re-score on a PLAYED arm (hand-driven in chunks through `tools/playtest_driver.py --script` or the wire, as GE-V and the AAR were); chunks 5, 6, 7 on driver arms plus the standing sweeps; chunk 9 on frames and the user's pass. **Save before any driver run touches a live campaign** (the AAR's lesson — `--http` opens with `POST /new_game`).
- **The table.** §1 is updated at every chunk exit with the new score, the date and the evidence path; the directional is recomputed; a score that FALLS blocks the landing until it is explained or repaired.
- **Confirmation.** Scores stay FOR USER CONFIRMATION where a session cannot run the arm that scores them (a client pass the user must see; a live-parser arm without a key).
- **Balance.** `BASELINE_SERIES` + M1–M7 byte-identical unless attributed by a flip experiment (`tools/_vpr1_series_arms.py` is the pattern).

---

## §6 What this plan supersedes, and how

- **"THE PLAN — September 23" Updates 1–5** are absorbed by category, nothing dropped: Update 1 (PR-D1b + PB-7) → **SR-1d**; Update 2 (CRT-3 + WO-32) → **SR-3a + SR-2c**; Update 3 (L-D + CX5-L5-F2 + CQ-30) → **SR-3a**; Update 4 (the petition Antechamber B1) → **SR-4c**; Update 5 (PR-D1c + PR-D1d) → **SR-2c**. CRT-2 … CRT-11 → **SR-3b, SR-4b, Chunk 3b** in §12.3's order; L-1 → **SR-3c**; the Victory & Objectives gate (ROADMAP 12) keeps PB-D1's ruling with SR-7b's measurement done for it.
- **The AAR's 32 rows and 8 design rows** are homed above by number; the September 23 rule that player reports re-order the queue stands (§0-4).
- **The ROADMAP spine after the program** is unchanged; row **SR** is inserted after row UPD in `docs/ROADMAP.md`'s road-to-EA table and the UPD row points here.
- **What is NOT in this plan:** a second playable nation; seasons (HC-6 stays deferred past Round 0 and is ruled with SR-D2); the Congress numbers (45 titled, 8 turns, alarm < 80, 50% of ports) — the plan builds the road, not a shorter finish line.

## §7 Done when

- Every pillar in §1 is at or above its target on a dated arm, and none has fallen — OR the plan says, per pillar, why the target was not met and who owns the residue.
- A played road reaches 45 titled by turn 40 (`test_a_played_arm_reaches_forty_five_by_turn_forty` un-xfailed), or SR-D3 has returned with the ruling that would.
- The three §4 gates have returned (built or struck), each with its memo.
- The scoreboard's directional reads ≥ 7.5.
