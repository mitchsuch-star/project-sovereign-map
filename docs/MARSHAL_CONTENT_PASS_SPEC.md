# Marshal Content Pass (1805 Roster Depth)

> **Status:** v1.3 — **✅ GATE BLESSED July 10, 2026** (gate record in §4 below — authoritative). Memo Q1–Q8 approved at their recommended defaults with **one amendment to Q3** (command wired immediately as "The Rally"; administration flatten + hide behind the owned MC-2b row). **MC-1a/1b/1c + MC-2 + MC-3 + MC-4 + MC-V ALL LANDED July 10, 2026** (landing records §5–§10). **The MC build is COMPLETE through MC-V; NEXT = the MC exit review, where MC-2b (administration wiring + card un-hide) re-opens.**
> **Gate memo (July 10, 2026):** `docs/audits/MC_GATE_RECOMMENDATIONS_2026_07_10.md` — panel-recommended MC-1 set (10 abilities), full MC-2 skills/trust + MC-3 relationship tables, MC-4 recommendation (defer balanced/loyal; canonize Soult-literal), and the user-added MC-V rider (personality-kit assurance for both sides + enemy-AI-per-personality evaluation, memo §6.7).
> **Origin:** July 2, 2026 re-staging audit — this gap was previously UNOWNED (Golden Rule 9). The shipped 21-marshal 1805 roster authors only name/nation/location/strength/personality/movement/biography; everything that makes marshals *people* is defaulted.
> **Vision anchor:** Marshals ARE the game ("talk to your generals", the Grouchy Moment, personality over randomness). A campaign where Ney has no Bravest of the Brave, every skill is a flat 5, and no marshal has a relationship with any other is mechanically playable but characterally empty — and it silently disables the substrate Jealousy v3.1 needs.

---

## 1. Ground truth (audited July 2, 2026)

Via `create_marshal_from_data` (`backend/models/marshal.py:1357-1385`), every 1805-scenario marshal boots with:

- **`ability = {"name": "None"}`** — zero functioning wired abilities in the shipped campaign. Ney's "Bravest of the Brave" (`combat.py:251`) and Davout's "Counter-Punch Mastery" (`marshal.py:888-891`) key off ability NAME, which scenario-Ney/Davout lack. The other wired names (Drouot, Wellington, Blucher, Uxbridge) are legacy-Waterloo-only marshals absent from the roster.
- **Flat default skills (all 5s)** — the 6-skill system provides no differentiation for the whole campaign.
- **Trust 70, zero authored relationships (all Professional)** — the coordination/objection/relationship substrate runs untextured; Jealousy v3.1's drama has no fuel.
- **Display bug (MC-0):** `marshal_overview._build_ability` gates on marshal NAME (`marshal_overview.py:28/:138`), so 1805 Ney/Davout report `ability_active=True` with ability name **"None"** in the management screen.
- Personality skew: **13 cautious / 4 aggressive / 4 literal** (was 14/4/3 until Soult `cautious → literal` at the July 5, 2026 CR-5 gate — **✅ CANONIZED as character at the MC-4 decision, July 10, 2026 (§9)**: the 1805 frame is the one campaign where Soult operated under Napoleon's direct hand as the army's supreme executor, so the §6.8 personality=character rule now holds with ZERO exceptions; a Massena `aggressive → cautious` recast was tried and reverted; see §9 + `COMMAND_ROBUSTNESS_SPEC.md` §6.8); `balanced`/`loyal` are **RETIRED reserved values — deferred by contract at MC-4 (§9)**, no longer valid to author (boot-guarded).
- Biographies are the one complete axis (21/21 authored).
- Design target already on record: `docs/ADDING_CONTENT.md:1567-1576` (1805 Roster Planning Notes — ~10-12 wired abilities across the roster) + candidate inventory in `docs/archive/SPECIAL_ABILITIES_EVALUATION.md`.

## 2. Slice plan

| Slice | Scope | Gate |
|-------|-------|------|
| **MC-0 (bug fix, independent)** — ✅ **LANDED July 4, 2026** | `marshal_overview._build_ability` now gates on a REAL ability name (`name not in ("", "None")`), not just the marshal name — so 1805 Ney/Davout (booted with `ability={"name":"None"}`) correctly report no active ability instead of an active ability literally named "None"; legacy marshals with genuine wired abilities still display. Matches the mechanics (combat wiring keys off the ability name too). The optional Ney/Davout ability restoration was deliberately NOT done here — authoring abilities into the roster is the gated MC-1 decision. `tests/test_marshal_content_mc0_ability_display.py`. | None |
| **MC-1** | Ability set design gate: pick ~10-12 wired abilities for the 21-marshal roster from the SPECIAL_ABILITIES_EVALUATION candidates (which marshals, which mechanics, wiring per the ADDING_CONTENT checklist). | **USER DESIGN GATE** |
| **MC-2** | Author skills/trust per marshal (historical differentiation — Davout's discipline, Murat's cavalry dash, Mack's… Mack-ness); registry/scenario authoring via `create_marshals_from_data`; per-marshal pin tests. | Rides MC-1 gate |
| **MC-3** | Author starting relationships (the historical web: Lannes↔Murat rivalry, Davout's aloofness, Soult's ambition) + enemy-side texture where it matters (Charles↔Mack). Prerequisite for the Jealousy v3.1 gate — its tuning must be re-derived against this roster shape (the spec's §9b still assumes the retired Waterloo pairs). | Rides MC-1 gate |
| **MC-2b** | **Added at the July 10 gate (Q3 amendment):** wire `administration` to the recruit-action seam + un-hide its card row (full contract in §4). ✅ **LANDED July 11, 2026 at the MC exit review — "The Intendance" (landing record §11).** | ✅ Landed (exit review) |
| **MC-V** — ✅ **LANDED July 10, 2026 (landing record §10)** | **Added at the July 10 gate (user rider, memo §6.7):** (a) personality-kit assurance — pin every personality type's grants for BOTH sides through the same executor path (`tests/test_mc_personality_assurance.py`, 30); (b) enemy-AI-per-personality evaluation — verified opposing generals PLAY their personality and exercise the enemy-side MC-1 abilities live; eval addendum (`docs/audits/MC_V_PERSONALITY_EVAL_2026_07_10.md`) + 5 findings routed to `BUG_FIXES.md` (headline MC-V-2 = enemy literal aliased to cautious, a design decision for the MC exit review / Jealousy gate). Assurance/eval only — zero production-code changes. | ~~Rides the blessed gate~~ ✅ LANDED |
| **MC-4** — ✅ **LANDED July 10, 2026 (landing record §9)** | Personality coverage DECIDED at the gate (Q8, memo §5): **do NOT implement `balanced`/`loyal`** — the deferral closes as a Golden Rule 9 CONTRACT (retired reserved values, boot-guarded against authoring; re-open owners = the Jealousy v3.1 gate or the MC exit review; a revived fourth type must not be named "loyal" — diplomat `loyalist` namespace collision). **Soult-literal CANONIZED as character** (the 1805 frame rehabilitates the CR-5 recast: Boulogne drillmaster, Pratzen executor — his independent-command flaws belong to Spain 1809+): the "one disclosed exception" language is retired here (§1) and in `COMMAND_ROBUSTNESS_SPEC.md` §6.8 — the personality=character rule holds with zero exceptions. Historical context preserved: the recast was made July 5, 2026 because no French marshal is a natural literal (pinned by `test_cr5_literal_arm_player_reachable`, which stays the pin); a Massena `aggressive → cautious` recast was tried and reverted; the panel had judged Davout the *best* literal fit but his cautious identity is load-bearing (Iron Resolve); Bernadotte's political unreliability is expressed via trust 40 + MC-3 edges + Eyes on a Crown, not a type. Distribution stands at **13 cautious / 4 aggressive / 4 literal**. | ~~User decision~~ ✅ DECIDED at the July 10 gate |

## 3. Interactions (original draft)

- **Jealousy v3.1:** MC-3 is effectively a prerequisite; the Jealousy gate should be sequenced after this pass, with a v3.2 addendum re-deriving scenario impact against the real roster.
- **Voice:** marshal voice tiers stay Phase 8.5 scope; this pass is data/mechanics authoring, not voice.
- **Golden rules:** all authoring flows through existing factories (`create_marshals_from_data`) — no parallel authoring path; serialization enforcement applies to any new field.

---

## 4. GATE RECORD (July 10, 2026 — authoritative)

The user blessed the gate memo (`docs/audits/MC_GATE_RECOMMENDATIONS_2026_07_10.md`) in conversation on July 10, 2026: **Q1, Q2, Q4, Q5, Q6, Q7, Q8 approved at their recommended defaults** (Bernadotte's negative ability IN; all seven French abilities IN, Moore the designated first cut; net-negative French web blessed; roster trust mean 66.7 blessed; AI-benefiting abilities blessed; Murat/Kutuzov counter-pair as authored with the ×0.5 stacking cap; MC-4 defer balanced/loyal + canonize Soult-literal + retire the §6.8 exception language, decided together with Drillmaster).

**Q3 was AMENDED at the gate** (dead admin/command stats — the memo offered flatten-both vs wire-both):

- **`command` is WIRED IMMEDIATELY — "The Rally"** (landed same day, ahead of the MC-1 slices; user directive: wire one of the two now, mechanic delegated to Claude; battle-adjacent seam chosen to avoid the just-blessed W6-11 morale curve). Single source in `marshal.py` (`get_rally_stages_per_turn`, `get_retreat_stage_penalty`; constants `RALLY_FAST_COMMAND=8`, `RALLY_POOR_COMMAND=3`, `RALLY_POOR_EXTRA_PENALTY=0.10` — in-band tunable):
  - **Command ≥ 8:** retreat AND broken recovery advance 2 stages/turn — retreat recovers in 2 turns (not 3), broken in 2 (not 4). Consumed at the single `_process_tactical_states` tick (GR5: all marshals, both sides). Rally note rides the recovery events; dispatch ETA (`_derive_marshal_status`) and the forced-retreat message are command-aware.
  - **Command ≤ 3:** retreat-recovery effectiveness penalties run 10pp deeper (−55/−40/−25); recovery TIME unchanged; the tick message shows the deepened number (shown = applied). Broken state has no effectiveness channel — the poor arm is retreat-only by design.
  - **Command 4–7:** byte-identical baseline; the shipped **1805 campaign** roster is flat-5, so it is unchanged until MC-2 lands authored values (mechanic lands ahead of data — zero balance perturbation in the running game). Scope note: the LEGACY fixture/rollback roster (`_FRENCH_MARSHALS_DATA` etc.) authors Ney command 8 / Davout 9 / Wellington 9, so the fast tier is LIVE in `SOVEREIGN_MAP=legacy` mode — deliberate (they are the inspiring-commander tier; suite green against it).
  - Tests: `tests/test_mc_q3_command_rally.py` (32). MC-2's landing re-measures nothing here (the mechanic is stage-arithmetic, not curve math), but its authored command values ACTIVATE the texture: fast tier = Davout 9, Charles 8; poor tier = Mack/Massena/Buxhowden/Hohenlohe 3.
  - **Post-landing adversarial review (same day, 3-lens find→verify, 12 confirmed findings — ALL FIXED):** the slice's first cut missed five surfaces that still hardcoded recovery numbers, all now command-aware and pinned (`TestReviewPassSurfaces`): the executor's retreat/broken action-block messages (`executor.py` — the retreat one read a phantom constant-3 attribute that never counted down, now a real countdown), the voluntary-retreat copy (`movement_executor.py` "Will recover over N turns" + the stage-0 penalty display now derived from `get_retreat_stage_penalty` with the ±10pp stance flavor riding on top), the surrounded/shattered message (`combat_executor.py` "can only recruit for N turns"), the map hover tooltip (backend now ships derived `retreat_penalty` + `broken_turns_left` in `tactical_state`; `map_renderer_base.gd` renders those instead of its hardcoded table), and a recovered-tick guard on the retreat rally note (mirrors the broken-side guard). One stale test updated: `test_marshal_overview.py` six-skills pin → the wired five (administration hidden by this gate).
- **`administration` gets flatten + HIDE (memo options A and C combined):** stays flat 5 (status quo), is REMOVED from the marshal-card payload (`marshal_overview._build_combat_stats` filter) AND the Godot skill row (`marshal_management.gd`) so no unwired stat displays at all. The value remains authored-able, serialized, and mod-valid (reserved data).
  - **MC-2b owner row (Golden Rule 9 contract), scope reduced to administration only:** wire admin to the recruit-action seam (`economy_executor._execute_recruit`) at code-verified numbers, un-hide the card row, and land MC-2's reserved admin values live. **Landing:** re-opened at the MC exit review, AFTER W6-11's recruit pricing has playtest hours. **Completion definition:** admin consumed at the recruit seam + card row restored + per-marshal pin tests + a golden-corpus check that no new player verb is added. **Tracking:** STATUS.md Next Steps carries the MC-2b line until landed. **Behavior test:** extends `test_mc_q3_command_rally.py`'s hide tests into display-restored assertions.

**Build order from here** (memo §6, MC-0 done, Q3 command slice done): ~~MC-1a~~ → ~~MC-1b~~ (both ✅ LANDED July 10, 2026 — landing record §5 below) → ~~MC-1c (Davout T2)~~ (✅ LANDED July 10, 2026 — landing record §6 below; **the ten-ability MC-1 set is COMPLETE**) → ~~MC-2 (skills/trust, admin flat+hidden)~~ (✅ LANDED July 10, 2026 — landing record §7 below) → ~~MC-3 (relationships)~~ (✅ LANDED July 10, 2026 — landing record §8 below; **the Jealousy v3.1 prerequisite is met**) → ~~MC-4 (doc surgery + boot guard)~~ (✅ LANDED July 10, 2026 — landing record §9 below) → ~~MC-V (personality assurance + enemy-AI evaluation)~~ (✅ LANDED July 10, 2026 — landing record §10 below; **the MC build is COMPLETE — NEXT = the MC exit review, MC-2b re-opens**).

---

## 5. MC-1a + MC-1b LANDING RECORD (July 10, 2026)

**MC-1a (T0 quick wins) ✅ LANDED** (`6b34f65`): Ney re-keyed to "Bravest of the Brave" in `europe_1805.json` incl. the load-bearing `"trigger": "when_attacking"` key; ArchdukeCharles activated to "Habsburg Resolve" (+3% defense — check already wired); `_WIRED_ABILITY_MARSHALS` expanded to all ten blessed MC-1 names + the legacy four (unauthored names stay inactive via the MC-0 real-name gate — Davout's card pin flips at MC-1c). `tests/test_marshal_content_mc1a_t0_abilities.py`.

**MC-1b (the T1 set) ✅ LANDED** (same day): all eight abilities wired at their blessed seams per memo §1 —

- **Soult "Drillmaster of Boulogne":** 1-turn drill, never `drilling_locked` (`tactical_executor._execute_drill` + a Drillmaster branch at the `_process_tactical_states` tick); payoff unchanged (+20%, one charge); keeps exactly one enemy phase of −25% exposure. The map tooltip ships a derived `drill_completes_this_turn` flag (Q3 pattern — never "will lock next turn" for a drill completing tonight); the order copy names the one residual limit (no stance change until the tick).
- **Lannes "Roland of the Army" / Bernadotte "Eyes on a Crown" (the arrival mirror):** ±15 in `_calculate_arrival_score` + honest muster arms (`roland_marches` / `eyes_on_a_crown` in `_muster_reason` + `MUSTER_REASON_DISPLAY`), SUPPORT +10 counter-lever pinned, standing-order hint on the Bernadotte row. Bernadotte's defiance arm: +10pp base in `calculate_defiance_chance` (5/15/35 → 15/25/40-capped).
- **Murat "First Horseman of Europe":** +5,000 pursuit, cavalry-gated, primary-attacker only, at BOTH pursuit copies; **Kutuzov "The Old Fox":** pursuit vs him halved AFTER the attacker's bonus (5,000 → 2,500, ordering pinned) at both copies + retreat-path attrition halved (`_calculate_movement_attrition`, `is_retreat` only, ×0.5 stacking cap noted in-code).
- **Massena "Child of Victory":** ×1.10 when `is_outnumbered` in `get_defense_modifier` (beside Habsburg Resolve, under the 1.75 cap), battle-report row + narrative line.
- **Charles rout extension:** new single-source `marshal.get_rout_threshold(base)` (`HABSBURG_ROUT_THRESHOLD = 15`, in-band tunable) consulted at **all three** rout-decision copies — solo `resolve_battle`, the coordinated primary flags, AND the [S62] coordinated non-primary sweep (a third copy the slice's review found still hardcoding 25) — each with the "close ranks" legibility line.
- **Moore "Shorncliffe System":** `max(RECRUIT_MORALE, 60)` at the recruit seam (GR5: one name-check serves the AI), Training Ground 70 strictly better, message rider; **intel visibility rider:** FULL-visibility intel names any known enemy's famed ability (`generate_intel_report` — "famed for 'The Old Fox'").

**Post-implementation adversarial review (4-lens find→verify workflow, 22 agents): 18 raw → 15 confirmed findings, ALL FIXED pre-commit.** The load-bearing ones: (1) the [S62] third rout copy above (HIGH); (2) coordinated pursuit mutated strength AFTER `remaining`/casualties/battle-report/campaign-log were frozen — every numeric surface now folds `actual_pursuit` in, mirroring the solo copy (and the previously-never-surfaced coordinated pursuit message now rides the description); (3) pursuit prose near the 1,000-survivor floor now carries the ACTUAL clipped figure (clamp-before-compose in both copies; the solo result's `pursuit_damage` is now actual, matching the coordinated copy's semantics); (4) **Eyes on a Crown no longer silently drains trust** — his ability-driven no-show (no SUPPORT order) is classified `eyes_on_a_crown` and exempt from the Session-61a −3 dock, mirroring the literal no-march exemption (the gate priced defiance + arrival only; a no-show UNDER a written SUPPORT order stays "low_score" and is still docked) — *design call ✅ RATIFIED by the user July 10, 2026 (recorded at the MC-1c session's step zero): the exemption stands as shipped*; (5) the post-battle failure copy names ambition, matching the muster preview; (6) the hard "WILL NOT" muster arm softens to the hedged arm at relationship ≥ +1 (where the roll is genuinely winnable); (7) coordinated pursuit floor guard `> 0` → `> 1000` (the old guard could RAISE a sub-1,000 defender to the floor) now revert-proofed by a boundary test; (8) coordinated constants numerically pinned (mutation-tested: message-substring pins alone survived 5000→500, gate-deletion, and 0.5→0.9 drift).

Tests: `tests/test_marshal_content_mc1b_t1_abilities.py` (83 across the MC files) — per-seam pins, both pursuit copies for Murat AND Kutuzov, the ordering pin, non-primary rout isolation, GR5 symmetry (enemy-initiated battle picks up defender-side Lannes; AI recruit pays Britain's treasury), fully deterministic combat fixture (fixed dice + pinned variance/fate rolls). Blessed numbers remain in-band tunable; MC-2's landing re-measures the Ney shock-9 figure and the arrival numbers at authored logistics (memo §6.4).

**Standing tuning watch (memo §1 Murat note + Q7 default, recorded July 10, 2026 at the MC-1c session's step zero):** if W6-11-era routs over-harvest in play, tune First Horseman's pursuit **+5,000 → +4,000 in-band — never add conditions**. Owner: MC-V's live enemy-AI evaluation re-checks pursuit harvests; no code change until observed. **✅ CLOSED July 11, 2026 at the MC exit review (§11): three checks, no over-harvest — the named watch retires; the number stays covered by the standing in-band-tunable rule.**

---

## 6. MC-1c LANDING RECORD (July 10, 2026 — ALL TEN BLESSED ABILITIES LIVE)

**MC-1c (Davout "Iron Resolve", the set's only T2/serialized slice) ✅ LANDED** — the tenth blessed ability; the MC-1 set is COMPLETE.

- **Mechanic as landed:** serialized `Marshal.iron_resolve_stacks` (the set's only new field; SAVE_FORMAT_REFERENCE row added, enforcement green, absent-key loads default 0 — old saves coil nothing). **+1 per fortified turn** at the `_process_tactical_states` fortify tick (accrues in growth AND decay phases — any turn spent fortified counts), cap `Marshal.IRON_RESOLVE_MAX_STACKS = 3`; his **next attack consumes ALL stacks for +8% each** (`IRON_RESOLVE_BONUS_PER_STACK = 0.08`; max +24%) **inside `get_attack_modifier()` ONLY** (GR1/GR4 — combat.py saves the count pre-call for the message, drill-bonus pattern). Both constants in-band tunable; every display surface derives its percentage from the constant (shown = applied).
- **The memo's load-bearing correction held and is pinned:** stacks SURVIVE unfortify (a fortified marshal cannot attack — the executor guard is pinned inside the coil-uncoil-spring test). **Recorded nuance:** the memo's "release always costs 1 AP" gloss is inaccurate for Davout — cautious marshals unfortify for FREE (shipped Phase 2.8 kit, deliberately untouched); the real release price is the forfeited fortify defense bonus + a turn of tempo, and the self-balancing core (peak +24% never rides a fortified posture) holds regardless.
- **Clears ("the coil only holds while he stands"):** `move_to()` (18 call sites) + every direct location-assignment seam — reinforcement arrival, both post-battle orderly-withdraw loops, capture, **release** (W6-7 ransom/peace — review fix), admin entry (redemption + debug) and debug restore, debug `set_location` — plus the rout applications. `clear_combat_transient_state` deliberately does NOT own the stack: it also runs post-combat for standing defenders, where the coil legitimately holds.
- **Legibility riders:** fortify-tick event `iron_resolve_stack` naming the exact carried bonus; battle-report attacker row "Iron Resolve (N stacks)" (snapshots run pre-consumption, memo-verified); description line threaded through BOTH combat result paths (normal + `_build_deferred_result`) AND both garrison-assault branches; marshal card status flag + map tooltip via derived `iron_resolve_stacks`/`iron_resolve_bonus_pct`/`iron_resolve_max_stacks` in `tactical_state` (Q3 pattern — Godot renders backend numbers, no hardcoded table).
- **Boundary (adjudicated at review, killed 3/3):** bombardment neither consumes nor benefits — it never reads `get_attack_modifier` (its own docstring: "NOT a battle — no counter-punch"); Iron Resolve follows the same consumable-family precedent as Counter-Punch/strategic bonuses.

**Pre-commit adversarial review (4-lens find→verify workflow — spec-fidelity / seam-completeness / correctness / test-quality, 3 adversarial skeptics per finding; 34 agents): 12 raw → 10 deduped → 7 CONFIRMED, ALL FIXED (3 killed).** The headline (HIGH, live-reproduced by the verifiers): the accrual tick keyed only off `fortified` — a flag that survives forced retreat AND capture (pre-existing: `move_to`/`_capture_marshal` never clear it) — so a routed carrier re-coiled to +24% during retreat recovery and a prisoner coiled in captivity; the tick now independently refuses retreating/broken/captured/off-field marshals. Also fixed: the W6-7 `release_captured_marshal` seam clear (MED — the one direct-assignment seam the first cut missed), the debug admin-restore clear (LOW), and four test-quality gaps (seam coverage incl. the player-reachable reinforcement-arrival clear isolated by a decisive-win drive; cap-message shown=applied at 3 stacks; the two flipped waiting pins renamed to state what they now assert; the garrison collapse-branch message pinned).

Tests: `tests/test_marshal_content_mc1c_iron_resolve.py` (47) + the two flipped MC-1a/1b pins; suite **12,410 passed / 2 skipped**, ruff clean. Discovered dead code flagged as a separate task, deliberately NOT this slice: `world_state.check_and_execute_retreats` has zero callers and calls a nonexistent `marshal.should_retreat`.

---

## 7. MC-2 LANDING RECORD (July 10, 2026 — skills + trust authored)

**MC-2 (the memo-§3 21-row skills/trust table) ✅ LANDED** — the roster stops being flat-5; every marshal is now a differentiated character.

- **Data as landed (`europe_1805.json`, all 21 marshals):** the four combat skills + `command` authored exactly per the blessed table (single-line `skills` dicts so the 21 rows stay eyeball-comparable for in-band tuning); `trust` authored as `{"value": N}` (the `Marshal.from_dict` scenario path restores it); the legacy `tactical_skill` dice-fallback field authored EQUAL to `skills.tactical` per marshal so no surface can ever show a split value. **`administration` ships FLAT 5 for all 21 per gate Q3** — the authored admin values remain reserved data in the memo §3 table; the owned MC-2b row lands them live with the recruit-seam mechanic + card un-hide.
- **The Rally texture is ACTIVATED (Q3's promise):** fast tier live = Davout 9 + ArchdukeCharles 8 (recovery 2 stages/turn); poor tier live = Mack/Massena/Buxhowden/Hohenlohe 3 (retreat penalties −55/−40/−25); everyone else byte-identical baseline. GR5: both sides through the same `marshal.py` methods.
- **Re-measures recorded (memo §6.4, all pinned):** Ney's Bravest of the Brave at authored shock 9 = 1.45 → 1.55 ≈ **+6.9% relative** (the memo's +7% figure, under the ~+10% anchor). Arrival mirror at authored logistics: **Lannes deterministic base 90** (50 + 20 log-4 + 5 aggressive + 15 Roland) vs **Bernadotte 60** (50 + 30 log-6 − 5 cautious − 15 Eyes); 75 without the ability, 70 under a written SUPPORT order; the 30-point mirror gap pinned as one assertion.
- **Balance frame pinned as suite gates:** per-skill roster means 4.95–5.10 (tactical 5.10, shock 4.95, defense 5.10, logistics 5.05, command 5.05 — global balance does not shift); **French trust mean exactly 70.0** (player objection/defiance economy net-unchanged: Davout/Lannes 85 ↔ Bernadotte 40 Strained); roster-wide 66.7 (Q5 blessed). Peaks cap at 9. Trust-40 Bernadotte verified defiance-inert (the modifier bites at ≤20/≥80) — his texture rides the V2a tier (WARY) + the MC-1b ability, as designed.
- **Visual layer (this slice's second half):** the marshal card's skills line was a cryptic flat-5-era 3-letter row (`SHO:5 DEF:5…`); it is now a character sheet — one `█░` bar row per wired skill (the diplomatic ledger's proven bar idiom), color thresholds unchanged (8+ green / 3− red), each row carrying a backend-shipped mechanic hint (`skill_notes`: "attack damage (+5%/pt)" etc. — wired seams only, admin absent per GR9). **The Rally tier ships as derived `rally_tier` + composed `rally_note`** (`marshal_overview._build_combat_stats`, derived from the marshal's OWN `get_rally_stages_per_turn`/`get_retreat_stage_penalty`/`RALLY_POOR_COMMAND` — shown = applied, Q3 pattern: Godot renders the strings verbatim and never re-implements thresholds). Card scroll estimate 320→380px for the taller cards. `MODDING_FORMAT.md` gains the `trust` field row + a wired-seam note per skill.
- **Scope boundaries held:** zero new serialized fields (skills/trust already round-trip); legacy fixture roster untouched (pinned); no golden-corpus rows (no new player verb); enemy-side skill display unchanged (map tooltip skills remain player-only; enemy flavor stays the MC-1b intel famed-for rider).

Tests: `tests/test_marshal_content_mc2_skills_trust.py` (113 — per-marshal boot pins ×3 surfaces, balance-frame gates, rally activation both sides, the §6.4 re-measures, card payload incl. shown=applied rally-note digits, legacy isolation); suite **12,523 passed / 2 skipped**, ruff clean.

---

## 8. MC-3 LANDING RECORD (July 10, 2026 — the relationship web authored)

**MC-3 (the memo-§4 13-pair starting-relationship table) ✅ LANDED** — the roster stops being all-Professional; the Jealousy v3.1 substrate has its fuel.

- **Data as landed (`europe_1805.json`):** all 13 blessed pairs authored as 26 directed edges across 14 marshal rows — both directions EXPLICIT in the JSON. The memo's "post-load symmetric-pair pass" is satisfied as authored data + a roster-wide symmetry suite gate, NOT a code mirror pass: play-time asymmetry is a supported substrate feature (the Win/Loss formula produces it; the legacy Waterloo Ney→Davout −2 / Davout→Ney −1 pair is pinned as the standing proof). `Marshal.from_dict` (the scenario path) restores the dict as-is — zero new fields, zero new mechanics.
- **Every consuming seam pre-existed — the slice turns them on with data (all pinned against the authored web):** coordination scaling ×0.0 / ×0.5 / ×1.25 at `_calculate_per_ally_coordination`; A-D4 hostile-refusal (a −2 marshal neither auto-reinforces nor musters without a written SUPPORT order — the Auerstedt no-show live on Davout–Bernadotte; the written word still overrides, which IS the fantasy); the arrival-score ±10/step (Lannes marches for Ney at deterministic 100; Bernadotte for Davout sinks to 40); the W6-4 muster-preview reason ladder; the marshal-card relationship rows (Hostile/Rival/Friendly labels, alive-filtered).
- **Reason-ladder nuance recorded:** vs a −2 primary, Bernadotte's preview reason is `hostile_refuses` — it outranks `eyes_on_a_crown` in the ladder, so the feud (not the ambition) gets named; vs merely-rival primaries (Ney, −1) his no-show stays `eyes_on_a_crown`. No trust-dock interaction: hostile marshals are filtered by eligibility BEFORE the arrival roll, so `hostile_refuses` never reaches the Session-61a dock seam. Murat still musters for Lannes (−1 halves his coordination; only −2 refuses — `aggressive_marches`): the glory-hound rides anyway.
- **Enemy-side texture (GR5, same methods, no popups — as blessed):** Charles–Mack −2 (Mack's isolation at Ulm emerges from the graph — ×0.0 pinned through the identical coordination seam), Charles–John +1 (Austria's one cooperating axis, deliberately stable), Kutuzov–Buxhowden −1 (the −10 arrival delta pinned), Brunswick–Hohenlohe −1 (Prussia would enter any war pre-fractured).
- **Web-shape discrepancy recorded (the table governs):** the memo §4 TABLE authors 9 French pairs = **7 negative / 2 positive**; the memo's Q4 prose said "5 negative / 2 positive" — a miscount of its own enumerated table. The blessed artifact is the table; Q4's substance (net-negative French web, drama over power) holds and is pinned (7 > 2).
- **MC-2 re-anchor (this slice's one test edit):** `test_marshal_content_mc2` `TestReMeasures` measured the blessed §6.4 arrival bases against primary Ney, who gained MC-3 edges with BOTH mirror marshals (Lannes +1 / Bernadotte −1) — the measurement primary is now Soult (no authored edge with either), preserving the neutral-relationship semantics of the blessed 90/60/75/70/gap-30 numbers; the relationship-STACKED numbers are pinned in the MC-3 file.
- **Validator support (the authoring contract):** `relationships` added to the marshal schema — object of name → int; out-of-range is a hard ERROR (`Marshal.from_dict` restores the dict RAW — pinned forward-compat behavior, no clamp — so a scenario authoring 50 must fail loudly); self-edge warns; scenario-level cross-ref WARNS on unknown targets (an unknown edge is inert at runtime and unknown keys are pinned forward-compat data, but in a shipped scenario it is almost always a typo silently killing the edge). `MODDING_FORMAT.md` gains the field row; the shipped scenario validates clean (pinned).
- **Scope boundaries held:** no jealousy mechanics (v3.1 stays gated — this graph is its prerequisite; the v3.2 addendum re-derives tuning against exactly this shape); no cross-nation edges (dead data — pinned absent); no +2 pairs (no pair boots jealousy-immune — pinned); the seven single-army/no-story marshals stay edge-free (pinned); legacy fixture roster untouched (pinned); no golden-corpus rows (no new player verb).

Tests: `tests/test_marshal_content_mc3_relationships.py` (47 — per-pair boot pins both directions, the exact-26-edge gate, roster-wide symmetry, web shape, all five consuming seams, card rows, the validator contract, legacy isolation); suite **12,570 passed / 2 skipped**, ruff clean.

---

## 9. MC-4 LANDING RECORD (July 10, 2026 — the two gate decisions closed as contracts)

**MC-4 (doc surgery + the boot-guard rider) ✅ LANDED** — Q8's package (memo §5), approved together at the gate, executed as blessed.

**Decision 1 — `balanced`/`loyal` are NOT implemented; the deferral closes as a Golden Rule 9 CONTRACT, not an open question.** A new personality type is a CR-5-magnitude platform slice (two V2 evaluator functions, objection templates, defiance fallback rows, strategic-doctrine forks, a user-gated delegation arm, corpus/eval re-validation, a voice bank, ~15 copy surfaces), while a half-shipped type is an incoherent chimera that **objects to nothing** — silently diluting literal's user-blessed never-objects fantasy. The two "missing" personalities are already expressed cheaper by this pass: Bernadotte's political unreliability = trust 40 + three negative MC-3 edges + Eyes on a Crown; "loyal" = high trust + the relationship web. The "unimplemented placeholders" framing is struck everywhere (`ADDING_CONTENT.md`, `MODDING_FORMAT.md`, `personality.py` comments, ROADMAP Phase 10's "evaluate before 1805" note — the condition arrived and was decided). **Re-open owners (the ONLY ones):** the Jealousy v3.1 gate (v3.2 roster addendum) or the MC exit review, on evidence that three types are expressively insufficient AFTER content lands. If a fourth type is ever revived: the on-ramp is the AI-side personality-remap precedent, and it must NOT be named "loyal" (namespace collision with the diplomat `loyalist` type — several files read `getattr(x, 'personality')` on both object kinds).

**The boot-guard rider (memo §5's "one line", landed as three arms with one source of truth):**

- **Single source:** `personality.py` `IMPLEMENTED_PERSONALITIES = {"aggressive", "cautious", "literal"}`.
- **Assert arm (scenario boot):** the modding validator's `VALID_PERSONALITIES` now equals the implemented set (was the five-value set including the retired pair) — and because `from_scenario` hard-fails on validator errors, a scenario authoring `balanced`/`loyal`/any typo **cannot boot** (pinned end-to-end: the shipped scenario with one marshal flipped to `balanced` raises).
- **Factory arm:** `create_marshal_from_data` (the data-driven roster path — legacy fixture lists, future code-authored rosters) raises `ValueError` naming the marshal and the implemented three.
- **Log arm (the residual hole, never silent):** a scenario marshal that OMITS `personality` still boots on `from_dict`'s save-compat `"balanced"` default (the minimal-scenario contract keeps the field optional) — `from_scenario` now logs a warning naming the marshal. **Save-LOAD stays deliberately unguarded and is pinned so no future "cleanup" can refuse an old save:** `from_dict` restores whatever a save carries, including the retired values.
- One example mod re-keyed (`mods/examples/custom_nations_scenario.json` Sforza `balanced` → `cautious`).

**Decision 2 — Soult-literal is CANONIZED as character; the "one disclosed exception" is retired.** The 1805 frame genuinely rehabilitates the CR-5 recast: this is the one campaign where Soult operates under Napoleon's direct hand, and there he was the supreme executor — he drilled the Boulogne camps to clockwork and delivered the Pratzen assault at the exact appointed hour; his independent-command flaws and "King Nicolas" ambition belong to Spain 1809+, outside this scenario. The canonization's three concrete legs all landed earlier in the pass: (i) MC-1b's **Drillmaster of Boulogne** makes literal-Soult read as the machine-precise first maneuverer of Europe (the memo's cross-dependency — cut one, revisit the other — was honored: both shipped); (ii) MC-2's bio touch-up ("executes the Emperor's plan to the letter, exactly on the appointed hour") is live in `europe_1805.json`; (iii) his ambition lands in trust 70 + the Soult–Ney −1 edge. This slice retired the exception language in §1 above and `COMMAND_ROBUSTNESS_SPEC.md` §6.8 (addendum) — **the §6.8 personality=character rule now holds with ZERO exceptions, which is strictly stronger.** `test_cr5_literal_arm_player_reachable` stays the pin; the MC exit review owns the live-play re-check. Rejected alternatives stand rejected: recasting Davout literal (his cautious identity is load-bearing for Iron Resolve and for the man) and adding an 8th French marshal (no 1805 candidate is a literal archetype).

**Scope boundaries held:** zero mechanics changed (the guard refuses authoring that was never legal in shipped data; all 21 marshals + the legacy fixture roster pass it byte-identically); no new serialized fields; no golden-corpus rows (no new player verb); runtime personality FALLBACKS deliberately untouched (`getattr(m, 'personality', 'balanced')` call sites, the enemy-AI/disobedience/reinforcement fallback tables, `delegation._NEUTRAL_PERSONALITIES`, `get_personality`'s unknown→BALANCED default) — they are save-compat defensive floors, not authoring surfaces, and SYSTEMS_REFERENCE's fallback-chain rows still describe them correctly.

Tests: `tests/test_marshal_content_mc4_personality_guard.py` (23 — the single-source pin, all three guard arms incl. the end-to-end cannot-boot pin, legacy-roster pass-through, the shipped-scenario silent boot, and the deliberately-unguarded save-load pins).

---

## 10. MC-V LANDING RECORD (July 10, 2026 — personality-kit assurance + enemy-AI evaluation)

**MC-V (the user-added rider, memo §6.7) ✅ LANDED** — the MC build's assurance capstone. An **assurance + evaluation** slice (route findings, do NOT fix): it pins the personality kit as a standing both-sides regression gate and routes the decision-layer asymmetries. **Zero production-code changes** — one new test file, one eval addendum, five routed findings. Full eval: `docs/audits/MC_V_PERSONALITY_EVAL_2026_07_10.md`.

- **Half (a) — personality-kit assurance (both sides):** every personality COMBAT mechanic is GR5-clean and pinned for a player-nation AND an enemy-nation marshal through the shared `get_attack_modifier`/`get_defense_modifier`/`resolve_battle`/`_execute_recruit` paths: aggressive +15% base attack + recklessness escalation (incl. an enemy reckless-cavalry marshal building recklessness through `resolve_battle`); cautious −10% bad-odds / +10% outnumbered defense + counter-punch grant AND free-attack consume (enemy cautious defender earns it, enemy attacker consumes it) + fortify caps; literal Immovable +15% hold defense + the W6-5 never-objects invariant. Every combat seam reads `self.personality` (or an ability name on the combatant) and never the nation — both-sides holds by construction (the audit found no silent breakage).
- **Half (a) — player-only asymmetries pinned as KNOWN behavior (routed MC-V-1):** the literal Precision-Execution / ambiguity combat buff is granted only from the player-gated `executor.py:1254` seam (its function is GR5-clean but no AI code invokes it — an enemy literal that takes a full AI turn carries no Precision Execution, pinned); the literal 1-AP strategic discount only discounts the player's AP pool (enemy strategic execution bypasses AP — the player-facing 1-vs-2 AP discount is pinned instead).
- **Half (b) — enemy-AI-per-personality evaluation:** aggressive is fully realized (threshold 0.7, advance, drill, turn-order, capital-assault); cautious is moderately realized (threshold 1.3, fortify/fall-back reflex — but does NOT husband the overall force); **literal collapses to cautious** — `enemy_ai._get_effective_personality` (:398) aliases literal→cautious for every AI-controlled marshal, so Mack/Buxhowden/the Bavarian & Russian literals play as cautious clones and the `literal` rows in the AI constant dicts are dead. Pinned: the aliasing (enemy literal → cautious; player literal preserved unless autonomous), the mood-threshold ordering (aggressive < 1.0 < cautious; enemy literal == enemy cautious), and one observed full-turn divergence (at ratio 0.9 an aggressive enemy attacks and the target bleeds, while a cautious OR literal enemy declines).
- **Enemy-side MC-1 abilities exercised on the AI path (observed layer; seam pins owned by mc1b §6.2):** Charles's Habsburg Resolve holds him in the (15,25] band on a player-won battle where a non-Charles cautious defender routs; Kutuzov's Old Fox halves the pursuit (5,000→2,500) when the player routs the AI-controlled defender and halves his retreat attrition; Moore's Shorncliffe floor (morale 60) applies through the AI recruit action dict into the same `_execute_recruit`.
- **Routed findings (→ `BUG_FIXES.md` §"MC-V Enemy-AI Personality Findings"):** MC-V-2 (enemy literal aliased to cautious — the **headline design decision**, owned by the MC exit review / Jealousy v3.1 gate: distinct literal enemy profile vs accept a two-archetype AI + delete dead rows), MC-V-1 (three literal grants never reach enemy literals), MC-V-3 (dead `balanced`/`loyal`/`literal` AI rows + 6 unwired trust-bonus constants — behavior-neutral cleanup), MC-V-4 (cautious doesn't husband force — net-new behavior), MC-V-5 (economy personality-blind). None is a forced fix.
- **Scope boundaries held:** zero production-code changes (assurance/eval only); no golden-corpus rows (no new player verb); the exhaustive enemy-side ability SEAM pins stay owned by mc1b (§6.2) — MC-V adds the both-sides guarantee + observed AI-decision layer, not a re-pin.
- **The standing tuning watch from §5 (First Horseman pursuit +5,000→+4,000 if W6-11-era routs over-harvest) was re-checked at this slice:** no over-harvest observed in the eval's combat probes; no code change — the watch remains open, now owned by live-play at the MC exit review *(CLOSED there July 11, 2026 — §11)*.

Tests: `tests/test_mc_personality_assurance.py` (30 — aggressive/cautious/literal kit both-sides pins, the routed player-only asymmetries, enemy-AI personality differentiation, and the enemy-side ability observed pins). Suite **12,623 passed / 2 skipped**, ruff clean. **The MC BUILD is COMPLETE through MC-V; NEXT = the MC exit review, where MC-2b (administration wiring + card un-hide) re-opens.**

---

## 11. MC EXIT REVIEW RECORD (July 11, 2026 — the pass is CLOSED)

The exit review's four owned items are all resolved; build commit `2641c23`, live-verified against a running 1805 campaign (backend + Godot client).

**MC-2b ✅ LANDED — administration wired as "The Intendance."**
- Single source `marshal.py` (`get_recruit_cost_modifier`; `INTENDANCE_THRIFTY_ADMIN=8`, `INTENDANCE_WASTEFUL_ADMIN=3`, `INTENDANCE_COST_SWING=0.15` — in-band tunable): admin ≥ 8 prices recruits ×0.85, ≤ 3 prices ×1.15, 4–7 byte-identical. Applied LAST (rounded) inside `economy_executor._calculate_recruit_cost`'s **Europe-scoped** nation-pricing block — same scoping and same N1 rationale as the W6-11 war multiplier (Davout is the legacy suite's recruit fixture; its pins must not move — verified byte-stable). Both enemy-AI affordability pre-checks (`_pick_admin_action` P1 + rebuild) pass the marshal so the AI budgets the true price (GR5).
- The memo-§3 reserved admin values are LIVE in `europe_1805.json` (mean 5.095, in the 4.95–5.10 band): thrifty tier = Davout 8 / ArchdukeCharles 8 / Moore 8; wasteful tier = Ney 3 / Murat 2 / Massena 3; Mack keeps his immaculate-memoranda 7 (baseline — his poverty stays command/tactical).
- Shown = applied: the recruit message appends the intendance note exactly when priced (`(Davout's intendance: -15%)`), events carry `intendance_pct` (int, GR2), the card row is RESTORED with `admin_tier`/`admin_note` derived from the marshal's own constants — Europe worlds only; the legacy rollback world keeps the pre-MC-2b hidden row (GR9), enforced backend-side by key omission + `marshal_management.gd` skip-if-absent.
- Code-verified numbers (contract wording): peace 200 → 170/230; war 600 → 510/690; the 1805 boot (war ×3 composing with ~45% over force limit) prices Rhineland infantry at 872 → Davout 741 / Ney 1003 — live-confirmed in the running campaign.
- Completion definition met: seam consumption + card restore + per-marshal pins + the no-new-verb golden-corpus check (`tests/test_marshal_content_mc2b_administration.py`, 49) + the Q3 hide tests extended into display-restored assertions (`test_mc_q3_command_rally.py`). SYSTEMS_REFERENCE §The Intendance added.

**MC-V-2 ✅ DECIDED + IMPLEMENTED — enemy-nation literals play LITERAL.**
- Chosen arm: the distinct literal enemy profile — but at zero new mechanics: the alias is narrowed so the already-authored literal rows (threshold 1.0, mood ±8%, encirclement 2, base score 30, decay row) simply go live. The literal→cautious conversion survives ONLY for the player's own literal marshals gone autonomous — the exact case the original docstring rationale was written for. Single source: module-level `enemy_ai.get_effective_ai_personality` (the four drifted copies all route through it; `_get_effective_personality` delegates).
- The resulting autonomous doctrine (all fall-throughs verified defined): attacks at even odds with the roster's lowest mood variance; NO cautious reflexes — no P3 defensive-stance/fortify response, no P7 threatened fall-back (stands his ground), no self-directed P5 fortify / P6 drill; P7 holds the standing disposition until the graduated stagnation breaker represents new orders arriving; P8 default = wait. Mack sits at Ulm and gives battle without improvising; Buxhowden is "always late" mechanically.
- MC-4 coherence: personality = character now holds at the DECISION layer on both sides (the combat kit always did). Live-observed: AI Deroy (Bavaria, literal) gave battle at ratio 1.1 on campaign turn 1 — above literal's worst-mood 1.08, below cautious's best-mood 1.17; the old alias would have refused.
- Pins updated consciously (never silently): enemy-literal-plays-literal, threshold divergence (1.0 vs 1.3), stands-where-cautious-falls-back, attacks-even-odds-where-cautious-declines (`test_mc_personality_assurance.py` 30→32; `test_literal_personality.py` flipped; `test_enemy_ai.py`/`test_ai_scoring.py` row cleanups).

**MC-V-1 ✅ ACCEPTED (with MC-V-2).** Precision Execution, the ambiguity combat buff, and the 1-AP order discount are command-parser economies — rewards for how the PLAYER phrases orders. The AI has no parser; its literal expression is the decision profile above. The player-only pins stand as the contract.

**MC-V-3 ✅ CLEANED.** Dead `balanced`/`loyal` rows deleted from `ATTACK_THRESHOLDS`/`MOOD_VARIANCE`/`ENCIRCLEMENT_TOLERANCE`/`BASE_SCORES` (the `.get()` defaults are the MC-4 save-compat floor — a retired-type marshal scores at the unknown-personality default) + the 6 zero-reader trust-bonus constants removed from `personality_modifiers.py`. The literal rows are NOT dead anymore (MC-V-2) and stay.

**MC-V-4 / MC-V-5 ✅ ACCEPTED AS EVALUATED.** Cautious plays its label at field-odds level; force-husbanding is a net-new AI-depth candidate — its BUG_FIXES row now names **8.EVAL** as the triage owner. Economy stays personality-blind by design (nation-level action).

**First Horseman tuning watch ✅ CLOSED.** Three checks, no over-harvest: the MC-V eval's combat probes, this session's assurance suite, and a 4-turn live campaign (no Murat-primary rout occurred; pursuit volumes elsewhere read sane). The named watch retires; +5,000 stays blessed under the standing in-band-tunable rule (§5 note: tune to +4,000, never add conditions, if live play ever shows over-harvest).

**Soult-literal live-play re-check ✅ PASSED.** Live anthropic campaign: "Soult, deal with Mack" → the CR-5 ASK arm fires in character ("Soult will not presume your meaning, Sire. 'deal with Mack' — give battle, or observe Mack?") with the first-use Berthier hint; explicit orders execute without objection (W6-5 Literal Doctrine); Drillmaster's block message (enemy adjacent) reads as rules, not reluctance. Canonization holds — character, not a patch.

**Bonus fix (found by the review's UI assurance): the client was DEAD at master.** W6-1/W6-3 added a `var headline` dispatch local to `main.gd` and `dispatch_view.gd` on top of the pre-existing BPH-D peace-settlement `var headline` — GDScript forbids the shadow, so BOTH scripts failed to parse and the frontend lost its orchestrator (no top bar, no map fills, no hotkeys, no response display) for every boot since July 10. Loop locals renamed `settlement_headline`; the client now boots clean (zero script errors) and the full MC-2b card was verified visually in-game (six skill bars incl. Administration + intendance note, expectation/estates row, relationships). Root cause: the W6 second session never booted the client. Lesson recorded in STATUS.md: any `.gd`-touching slice must include one engine boot (`--verbose`, grep `SCRIPT ERROR`) before landing.

Suite **12,677 passed / 2 skipped**, ruff clean. **THE MARSHAL CONTENT PASS IS CLOSED. NEXT = the Jealousy v3.1 gate (its MC-3 prerequisite is met; owns any literal-AI follow-on tuning), then DEF-1 / DEF-13 / 8.EVAL per the ROADMAP queue.**
