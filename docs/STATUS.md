# Ink & Iron: Current Status

> **Updated every session by Claude Code.**
## Active Settlement Gate

**▶ RE-SEQUENCED (user decision, June 9, 2026): the Gate 4 manual settlement smoke now runs ONCE at the END of the settlement queue** — after the Guided Terms approval gate (+ implementation) and the remaining queued settlement items — instead of immediately after the PF fix slices. Rationale: the June 9 pre-flight audit + the new PF-3 scenario-matrix harness already caught the defect classes the per-slice manual smoke targeted; one consolidated end-of-queue smoke covers the integrated surface. The smoke keeps its owner (Gate 4, cleanup spec v0.32), scenarios (`settlement_rejected`, `settlement_losing`, the verification-focus list), and its role as the gate for Slice G — only its position changed. **Fix slices PF-1 + PF-2 + PF-3 are LANDED (June 9)** — the `settlement_losing` leg is UNBLOCKED for whenever the smoke runs. The first live smoke pass (`settlement_multilateral`) already folded two findings (May 30 entry below). **Next up: Settlement Guided Terms IMPLEMENTATION — `docs/SETTLEMENT_GUIDED_TERMS_SPEC.md` v0.2 APPROVED June 10, 2026 (D4 mixed-direction rows EXPLICITLY CONFIRMED by user). The spec carries the Run #1 audit fold (`docs/SETTLEMENT_GUIDED_TERMS_AUDIT.md`, GO-with-changes, 0 CRITICAL / 9 MAJOR / 6 MINOR) + GT-A1..4. Slice tracker: [x] GT-Slice-1 (backend demand-mutation verbs — **LANDED June 10, 2026, with companion CH-5**; see the entry below) → [x] GT-Slice-2 (per-court suggestion payload + OQ-6 budget-bound recommendation — **LANDED June 10, 2026 at `dc40012`, USER-APPROVED June 10**; see the entry below) → [x] GT-Slice-V (voice) + [x] GT-Slice-3 (Godot rows + **REFRONT-9 LANDED** + UX-2/UX-5) — **LANDED June 10, 2026 at `d339dbc` together in one session per the agreed cadence, APPROVED same day under the user-delegated confidence gate (100% contract confidence)**; see the entry below → [x] GT-Slice-4 (retire the editor per the §5 inventory — **LANDED June 10, 2026; ALL Guided Terms implementation slices done**; session confidence report delivered — awaiting user review; see the entry below).** **Slice 3 — Tier-3 valid-by-construction editor — IMPLEMENTED May 30, 2026 + post-implementation audit fold (suite-green: 10314 passed, 1 skipped, both orderings; ruff clean; Godot 4.4.1 parse 0 failures)** (see the May 30 Slice 3 paragraph below). All re-front implementation slices (0–3 + REFRONT-V voice) are now **IMPLEMENTED/LANDED**, and **REFRONT-9 (the OQ#4 focused-court breakdown + focus trigger) LANDED June 10, 2026 inside Guided Terms GT-Slice-3** per GT-A4; **REFRONT-8** + **DWL-SET-SC5R-3** flip to **LANDED**. **Slice 3 audit fold (this commit):** the ratify gate now re-runs V1–V5 against the live world (a CRITICAL — a stale `liberation` could otherwise release an uncovered party's vassal); the Tier-3 pickers are role-aware (forced_alliance/liberation side-disjoint); OQ#4 re-homed as REFRONT-9; stale routing text corrected. The interactive Godot editor consumes the Slice 0 `enabled`/`disabled_reason_display` contract, so the **Gate 4 manual settlement smoke can now run its full pass** (previously blocked on REFRONT-8). Slice 1 audit is **GO** (`2626e91`); Slice 2 + Slice 3 audit folds folded on master. **Remaining queue (June 9 re-sequencing): Guided Terms implementation — ✅ COMPLETE June 10, 2026 (v0.2 APPROVED June 10, D4 confirmed; GT-Slice-1 LANDED June 10 with CH-5; GT-Slice-2 LANDED June 10; GT-Slice-V + GT-Slice-3 LANDED June 10, REFRONT-9 landed inside 3 per GT-A4; GT-Slice-4 LANDED June 10 — the freeform editor is retired, REFRONT-7/UX-3/UX-8 closed moot) → CH code-health slices — ✅ ALL (queued set CH-1..CH-5) LANDED June 10, 2026 (**CH-1** seven-module split + public door; **CH-2** dispatch table + **CH-4** baseline convergence + **CH-3** legacy-store deletion in one follow-up slice, see the entry below; **CH-5** landed with GT-Slice-1) → **NEXT UP: the ONE end-of-queue Gate 4 manual smoke** → Slice G/G1.** The Tier-3 redesign — `docs/SETTLEMENT_GUIDED_TERMS_SPEC.md` (**v0.2 APPROVED June 10, 2026**) — replaces the freeform "Adjust terms" editor with guided per-court demand authoring (the editor's identity-picker model defaults `France`/`France`, is blind while authoring, and diverges from the bilateral guided flow). Two Gate-4 editor bugs already fixed at `067e431` (authored terms dropped at the `/command` Pydantic boundary; silent submit-validation).

**June 12, 2026 — G4F-10: the ratified gold-per-turn tribute is now SEEN, not just paid (user smoke check on a signed settlement):** MECHANICS were verified correct live (probe: +50/turn for exactly 3 turns then stop, `paid`×3 + `completed` events, `tests/test_settlement_recurring_gold.py` 27 pre-existing tests green) — the defects were all presentation. **(a) Raw war-id leak:** the morning-dispatch line read "Prussia paid 50 gold to France on the settlement of **war_1**" — the settled war is archived by the first income phase, so the processor's live label lookup fell back to the raw id. The payment record now stamps `war_label` at RATIFY time (instance still alive; "France vs Britain + Prussia") and `_war_label_for` prefers it. **(b) Campaign-log blackout:** the four `settlement_recurring_gold_*` event types were type-filtered out of the campaign log entirely (dispatch-morning-only). They are now in `CAMPAIGN_LOG_TYPES` (87 entries; count pins updated) + `CATEGORY_MAP` ("diplomacy") + `format_event_oneliner` branches; the processor mirrors each event into `world.event_log` via a `_log_campaign` helper; `_is_player_event` recognizes `from_nation`/`to_nation` so player-party payments are always visible. **(c) Ledger economy blind spot:** `_build_economy` modeled bilateral `treaty_gold` but knew nothing of `recurring_settlement_payments` — the strategic ledger now carries `settlement_gold` (net, folded into `net`) + `settlement_streams[]` (direction/counterparty/amount/turns-remaining + display line), and the Godot economy tab renders a "Settlements:" net line + a "Settlement Payments" block (green incoming / red outgoing). The diplomatic ledger's Recent Settlements record was already correct. +5 surface tests in `tests/test_settlement_recurring_gold.py` (ratify-time label stamp, archival-surviving dispatch label, campaign-log prose + visibility, economy streams + net math, Godot economy-tab source pin); live probe re-run green on all four surfaces; full suite `10476 passed, 1 skipped`; ruff clean; Godot parse report regenerated.

**June 11, 2026 (fifth entry) — G4F-8 + G4F-9 LANDED (the remaining two findings from the "make peace with Britain only" smoke cluster — the G4F ledger from this finding is CLOSED):** **G4F-8 (the pair-substitute trapdoor):** `seek_bilateral_peace` / `seek_armistice_instead` on a blocked REVIEW no longer one-click-discard the authored joint draft. The click now mounts a `settlement_pair_substitute_confirm` chooser (the scope-replace prior-snapshot pattern; registered in `SETTLEMENT_FAMILY_DIALOGUE_TYPES`, the executor's settlement-action tuple, the Godot dtype whitelist + a popup content branch, and the Gate-3 whitelist-sync canonical list): Talleyrand states the consequence in voice ("…every other court keeps its war. Your drafted terms for {target} travel with me. Shall I proceed?"), the joint draft is UNTOUCHED until Proceed, and Cancel (`keep_joint_settlement`) restores the REVIEW exactly. `confirm_pair_substitute` re-validates eligibility (ineligible-between-beats restores the REVIEW with the refusal named) and runs the old handoff — now WITH terms carry-over: `_pair_substitute_seed_terms` translates the target court's authored slice into the bilateral dialect (`gold_indemnity`→`gold_lump`; `gold_per_turn`→`gold_lump` at amount×turns — the bilateral recurring dialect is perpetual, so finite streams convert by TOTAL; `territory_cede`→one aggregated regions clause; proposer-paid gold→sweetener; identity-bearing clauses skipped) and the bilateral dialogue re-enriches over the seeded package (`carried_from_settlement: True`). **G4F-9 (the self-rejecting advisor):** offline component probe (`smoke_logs/probe_bilateral_estimate.py`) showed the bilateral formula credits war score +50 only +15 while the suggested 200/turn + region package drew −40 harshness — score 3/REJECT on Talleyrand's own suggestion (even white peace only reaches 46/COUNTER_OFFER). The FORMULA is untouched (its conservative war-score credit is recorded as an open calibration question for the pre-flight audit ledger — DW-class); `generate_suggested_terms` gains Stage 3.5: peace-family suggestions run `_ease_suggestion_until_not_rejected` (drop territory → halve gold, floor 50 → white-peace floor; marker `suggestion_eased_to_estimate`), so the advisor never proposes what his own displayed estimate rejects. **Adjacent fix:** the pre-proposal MILD objection ("offering such generous terms… rewards their failure") fired at `harshness < 0.3`, which a one-region cession or a 200-gold tribute (weight 0.2) satisfied — it now fires only on a package that DEMANDS NOTHING (giving without taking), killing the stale-grumble on carried packages; the existing demands-empty + sweetener pin stays green. Live probe (`smoke_logs/drive_g4f8_probe.py`): blocked REVIEW (attributed verdict) → chooser (coverage-aware label) → Cancel restores REVIEW → Confirm → bilateral `proposal_confirm` with the carried Waterloo demand, honest estimate, no contradiction line. +7 tests (chooser mount/keep/confirm/stray-guard/transform + the two convergence pins) + 5 one-click pins converted to the two-beat flow + 2 family-set pins extended; Godot parse report regenerated; full suite `10463 passed, 1 skipped`; ruff clean.

**June 11, 2026 (fourth entry) — G4F-6 + G4F-7: the "net terms show acceptable but it blocked" smoke finding — root-caused (presentation, NOT scoring) and FIXED:** The live smoke read the multilateral table as "high acceptance," submitted, landed in blocked REVIEW, clicked the SC-29 escape hatch ("Make peace with Britain only"), and got the bilateral PEACE PREVIEW with a self-contradicting voice line. A live consistency probe (`smoke_logs/drive_carry_consistency_probe.py`) proved the scorer seam is CLEAN — table and REVIEW agree exactly (44/35 blocked on both; eased 67/58 carries on both) — the lie was the copy: `ACCEPTANCE_BAND_DISPLAY["near_acceptable"]` rendered **"Near acceptable"** on a mechanically-BLOCKING state, so two holdout rows read as a passing deal. **G4F-7 (threshold-honest presentation):** band chips re-worded (accept → "Will sign", near_acceptable → "Holding out (close)", reject → "Holding out" — aligned with the committed will-sign/leaning/holds-out voice families); rows render scores as `44/50` (threshold visible in the number); `overall_acceptance` gains `carry_verdict_display` ("Will NOT carry as drafted — every court must reach 50. Holding out: Britain 44/50, Prussia 35/50." / the carrying mirror) computed in `compute_per_court_acceptance` and rendered as the FIRST line of the popup header (above the scroll — the G4F-3 lesson; the old summary sat below the fold inside the scroll block); the settlement dialogue's `war_label` is now COVERAGE-aware ("France vs Britain + Prussia", not the leader-pair "France vs Britain" that fed the Britain-only misreading — the blocked copy now names the full table). **G4F-6 (objection integrity):** `_merge_pre_proposal_objection` evaluated an EMPTY parsed-clause stub (every suggested-terms flow, incl. the SC-29 handoff, passes `clauses: []`), so harshness read 0.0 and Talleyrand called a PUNITIVE preview "such generous terms... it rewards their failure"; the objection now resolves the SAME terms the popup displays (execute_proposal option terms → parsed clauses → `generate_suggested_terms` fallback, mirroring `_enrich_proposal_summary`). +8 tests in `tests/test_settlement_gate4_leg1_fixes.py` (objection both arms, verdict attribution both arms, label coverage, band-chip pins, two popup source pins); 2 stale pins updated; Godot 4.4.1 parse report regenerated (binary at `Downloads\Godot_v4.4.1-stable_win64.exe\` — extracted-zip nesting); full suite `10456 passed, 1 skipped`; ruff clean. **Open from this finding: G4F-8 (the pair-substitute handoff design — confirm-step vs carry-terms vs remove-from-REVIEW; user fork pending) + G4F-9 (bilateral suggest↔estimate convergence — Talleyrand suggests terms his own estimator scores 3/100; mirror the CH-4 pattern).**

**June 11, 2026 (third entry) — GT-A5 / GT-Slice-5: dial territory escalation LANDED (user-approved OQ#7 crossing — the dial now escalates like bilateral `modify_harsh`):** Following the G4F-5 fix the user asked why Harsher only ever moves gold; the answer was the OQ#7 "identity in Tier 3" lock, and the user approved crossing it deliberately (decisions captured June 11: **ceiling-triggered** escalation with anti-balloon intelligence; **territory only** — vassalage/subjugation/liberation-class never auto-authored; **symmetric Ease** on Building-Blocks grounds, the same machinery must serve Slice G1's AI offer producer). A dial click on a court whose gold lever is EXHAUSTED (grow at the payer-budget/hard cap, or the unpressed-court seed unfundable) now authors that court's suggestion-engine territory clause in the dial direction — press → the `_demand_baseline_select_region` demand the row's `Take <region>` suggestion shows; ease → the `_guided_region_offer_candidate` offer (which never cedes proposer HOME territory — a fresh-start France legitimately has no land to offer until it captures some). Guards: at most ONE escalated territory clause per court per direction (the bilateral `has_territory` guard, direction-scoped — further land is an explicit row click); promised-region dedupe across the package AND across one click's candidates; clause cap honored; tagged `authored_by: "talleyrand"` so opposite-direction sweeps drop it like any suggested line (walk-back verified live); every escalation VOICED as a `talleyrand_line` authoring beat ("{court}'s purse is spent, Sire, so I have asked for land in its place — {region}." / the offer mirror — 2 new committed templates in `SETTLEMENT_VOICE_TEMPLATES`), with the DC-4 concede-court guard still firing on escalated demands and exhausted courts falling back to the G4F-5 ceiling notes. Spec amendments folded: re-front spec OQ#7 GT-A5 paragraph + Guided Terms §3.5 GT-A5 rule. Live probe (`smoke_logs/drive_gt_a5_probe.py`): presses 1–8 grow gold to Prussia's 800 cap → press 9 authors the Rhineland demand with the voiced pivot (score -11 → -31) → presses 10+ hold the one-clause guard and note → two Ease clicks drop the authored land first and walk the gold back. Implementation: `_dial_territory_escalation_candidates` (settlement_actions.py, candidates from the SAME selectors the rows show) + the `ceiling` class and escalation tail in `_redial_settlement_terms` (settlement_staging.py — ceiling notes now DEFERRED behind the escalation try). +5 tests (`TestDialTerritoryEscalation`) and 2 rewrites pinning the new ladder in `tests/test_settlement_gate4_leg1_fixes.py`; full suite `10448 passed, 1 skipped`; ruff clean; no `.gd` changes (the `talleyrand_line` beat renders through the existing else-branch).

**June 11, 2026 (later same day) — G4F-5: the whole-table Harsher/More generous dial was a SILENT DEAD CLICK on a gold-free table — FIXED + live-verified:** Continued live smoke found that repeated whole-table dial clicks changed nothing (screen re-mount flash, no term/score movement, "Pressed the whole table." printed only to the terminal BEHIND the modal popup). Root cause, reproduced through the real wire shapes (`smoke_logs/drive_leg1_repro.py`): the sweep only TUNES existing gold/territory lines, the modest gold seed was gated to FOCUSED dials (`len(scope) == 1`) on clause-less courts, and the `settlement_multilateral` baseline authors no gold — so a 2-court "Harsher terms" click had nothing to act on; a focused Press on a court whose only slice is a KEPT territory demand was equally dead (it counted as "touched"). **Fix (a):** `_redial_settlement_terms` now tracks `changed`/`noted` courts and seeds EVERY scoped court the sweep left unchanged-and-unnoted (press → demand FROM the court, ease → proposer-paid concession; still leader/clause-cap/G4F-2-budget-gated; the cap break appends "The accord can bear no further terms, Sire." instead of breaking silently) — whole-table and focused dials share one escalation semantic, and the DC-4 concede-court guard line now rides whole-table seeds via the existing `seeded_events` path. **Fix (b):** the ceiling/protection notes ride the restaged dialogue as one-shot `authoring_voice_beats` (kind `dial_note`, deduped) so the popup preamble RENDERS them — the D3 never-wordless contract enforced in the surface the player is actually looking at, not just `message`. Live re-verify on the fixed server: six Harsher clicks now escalate gold 100→600 per court with Britain 44→5 / Prussia 35→−4 live re-scoring. +7 tests in `tests/test_settlement_gate4_leg1_fixes.py` (seed-per-unpressed-court, repeat-click accumulation, territory-only focused press, generous mirror across clicks, capped-package note, peace-only handler repro, beats-on-restaged-dialogue); full suite `10443 passed, 1 skipped`; ruff clean; no `.gd` changes (the popup already renders the beat carrier).

**June 11, 2026 — Gate-4 leg-1 LIVE smoke findings FIXED same session (G4F-1..4) + API-driven playtest of ALL three legs GREEN (multilateral 22/22, losing 9/9, rejected 8/8):** The first live player pass on the `settlement_multilateral` leg surfaced a frozen acceptance under Harsher, a validator bounce with blame-the-player copy, and a 'missing' guided-authoring surface; all were reproduced through the REAL HTTP wire shapes and root-caused (full ledger: pre-flight audit §11.5). **G4F-1 (MAJOR):** settlement `gold_indemnity` was priced at ZERO harshness at any amount (the accumulator only matched bilateral `gold_lump`) — every gold dial/stepper/suggestion was a placebo; now aliased to the existing 0.08-per-100-gold weight in BOTH dialects (the clauses dialect also prices singular-`region` cessions + `vassalage`/`subjugation`, fixing near-zero stored `raw_harshness` on ratified-treaty records), and the package raw harshness is computed over the ACCEPTING-side burden terms only via `compute_settlement_package_raw_harshness` — the direction partition `concession_credit`'s contract promised (pricing France-paid gold as harshness made a sweetener LOWER acceptance). **G4F-2 (DC-1):** dial grows clamp at the payer's remaining capacity (`compute_gold_payer_budgets`, the clamp-side mirror of the SC-1/SC-33 budget validator), consumed line-by-line across the sweep; any ceiling press SAYS so ('Prussia can pay no more, Sire.'); the focused seed is budget-gated; `settlement_demand_set_magnitude`/`_add` refuse explicit over-budget amounts arm-side with the cap named in voice; the residual restage bounce now passes the validator's `disabled_reason_display` through (UX-6). **G4F-3:** the per-court popup preamble moved to the header and the PerCourtScroll viewport is 320px of court rows only — at 2 courts the second court and every `Add demand` affordance had been below the fold (the player's 'no adjust terms' read). **G4F-4:** the dialogue-choice resolver gains an exact `choice == option.action` match before any fuzzy keyword pass (the latent gap behind the one-carve-out-per-id pattern; the live client survives via main.gd index translation). +13 tests in `tests/test_settlement_gate4_leg1_fixes.py`; full deterministic + default-random suites `10436 passed, 1 skipped`; ruff clean. The API playtest (smoke_logs/playtest_leg1.py + playtest_leg23.py, real wire shapes) verified per-leg: live dial re-scoring, clamp voice, REFRONT-9 focused breakdown, CH-5 re-attach on refusals, carrying REVIEW → ratify → war leaves the active list, blocked REVIEW shape (no confirm; return/pair-substitute/war-detail routes), the CH-4 rail==front-door convergence live on the losing fixture, the DC-4 guard line, and the PF-2 suspend→reopen draft restore. **Remaining for the manual Gate-4 smoke: the VISUAL half** (both court rows + Add demand visible without scrolling; popup layout) — the data and flow contracts of all three legs are machine-verified.

**June 10, 2026 — CH-2 + CH-4 + CH-3 LANDED (one follow-up slice — dispatch table + baseline convergence + legacy-store deletion; the queued settlement CH code-health work — CH-1..CH-5 — is now fully closed; CH-6 BBCode-palette centralization + CH-7 test-shape rules of thumb remain unqueued recommendations owned by the pre-flight audit ledger; STATUS trimmed 1641 → ~450 lines the same session):** **CH-2:** `_handle_settlement_dialogue_action_inner`'s ~990-line if-chain in `settlement_actions.py` is now a dispatch-table lookup — every arm extracted verbatim into a module-level `_action_*` handler with the uniform `(world, *, action, dialogue, action_params, war_id)` signature, registered in `SETTLEMENT_ACTION_DISPATCH` (26 action ids) + `SETTLEMENT_SCOPE_REPLACE_DISPATCH` (3 — selected by the staged dialogue's TYPE before the action id is consulted, so a chooser click can never fall through to the main table); the `_fresh_recurring_gold_preset` closure became a module-level helper; the Gate 3 action-id whitelist test in `tests/test_godot_parse_harness.py` now asserts membership against the tables (runtime truth — a registered key IS a handler) instead of the old `action == "..."` source scan, a CH-7-aligned rebalance. **CH-4:** the legacy `_compute_concession_baseline` (single-court author, gold to the accepting leader only) is DELETED; the new `compute_concession_baseline_payload` keeps the four-key G2-Slice-W1 presentation contract (`losing_for_concession_baseline` / `concession_baseline_visible` / `concession_baseline{terms,reasoning}` / `concession_baseline_reason`, package-losing gate unchanged) and delegates authoring to `compute_settlement_baseline` — the SAME per-court generator the PROPOSE mount uses, so the `Re-author with concessions` rail seed and the front door's baseline can never diverge again (PF-1's cross-court treasury split + promised-region threading now apply to the rail for free; n=1 is the degenerate case the re-front spec promised); `_format_concession_reasoning` now reads the authored material clauses themselves (multi-court + demand-direction phrasing, same `settlement_concession_authored_talleyrand` Voice Bible template). **CH-3:** the legacy war_id-keyed `pending_settlement_drafts` store is DELETED everywhere (world_state field + to_dict/from_dict, six `settlement_actions` arm sites, ratify cleanup + `_stage_replacement_settlement_terms`, staging same-war refresh, `diplomatic_executor` propose-mount, offers comment) — the scoped `pending_settlement_drafts_by_key` is the ONE draft store; the end-turn SC-28 discard notice now derives from the scoped store (one notice per war = the most recently saved entry, i.e. the draft a reopen would actually have restored, `selected_target_nation` parsed from the key); war-instance merge re-keys absorbed-war scoped entries to the surviving war id (survivor wins on exact-key collision, GT-Slice-4 §6 semantics); `from_dict` migrates a pre-SC-5R save that carries a draft ONLY under the legacy key into the scoped store (under the war-prefix fallback shape) so no save loses an authored draft silently — new pin test `test_pre_sc5r_save_with_only_legacy_drafts_migrates_to_scoped_store`. `SAVE_FORMAT_REFERENCE.md` updated (legacy row → removed/migration note; scoped row owns the discard-notice + merge re-key contracts). Ten test files migrated off the legacy store onto `save_scoped_settlement_draft`/`load_scoped_settlement_draft`. **Verification:** full deterministic + default-random suites `10423 passed, 1 skipped` (was 10422; +1 migration pin); ruff clean; no `.gd` changes (Godot parse harness green in-suite). **STATUS trim:** Feb–Apr 2026 session history → `docs/archive/STATUS_SESSIONS_2026_02_TO_04.md`; superseded May 2026 v0.27 process records (spec reviews, branch reconciliation, compatibility table, May-14 slice queue) → `docs/archive/STATUS_SETTLEMENT_V027_PROCESS_2026_05.md` — both verbatim, pointers left in place. **Remaining queue: the ONE end-of-queue Gate 4 manual smoke, then Slice G/G1.**

**June 10, 2026 — CH-1 LANDED (split the `settlement_preview.py` god module into seven layered modules + a public door — 8 commits `e673e70`..HEAD, every commit suite-green via the pre-commit hook; next: CH-2 + CH-4 + CH-3 in one follow-up slice, then the ONE end-of-queue Gate 4 manual smoke):** Pure mechanical relocation — **zero mechanics change** (validator, scoring, baseline, staging, ratify, apply paths byte-identical except one disclosed seam, below). The 11,596-line module (it had grown from the audit's 9,470) is now: `settlement_routes.py` (L0, 551 — routing/reopen/recovery/actionability), `settlement_validation.py` (L1, 1,147 — side/term primitives + eligibility + `validate_settlement_terms`), `settlement_baseline.py` (L2, 1,980 — baseline generation + presets + treasury + guided candidates + `compute_per_court_acceptance`), `settlement_staging.py` (L3, 2,973 — scoped draft store + `build_settlement_confirm_dialogue` + `build_settlement_preview` + stage/revalidate/restage + the guided per-court payload), `settlement_ratify.py` (L4, 1,473 — ratification plan/apply/transitions/treaties + `ratify_settlement_confirm` + replacement staging), `settlement_actions.py` (L5, 2,262 — `handle_settlement_dialogue_action` + CH-5 wrapper + the ~990-line dispatch inner that CH-2 converts + all handler arms), `settlement_offers.py` (L6, 1,568 — incoming offers + ally petitions + recurring payments). **Layering is graph-verified** (a throwaway def-map/dependency analyzer assigned all 186 top-level symbols and rejected upward imports; the layer map is recorded in each module docstring): every module imports only lower layers, with exactly TWO documented function-level lazy edges (`stage_settlement_confirm` → offers petition queueing — the established settlement cycle-break pattern). `settlement_preview.py` survives as the **public door** (172 lines): it re-exports the 65 public names with `__all__` and a package-map docstring; **production code imports true homes everywhere** (main.py, diplomatic_executor, war_status, diplomacy, turn_manager, world_state, settlement_reactions, settlement_presentation — zero backend imports of the door remain), tests may import public names from either, and underscore internals are NOT re-exported (the 19 test files that reached internals now import true homes). **One disclosed deviation from byte-identical movement:** the six raw `calculate_common_peace_acceptance` call sites now resolve late through the `settlement_scoring` module attribute (same function object, identical behavior) — this gives tests ONE stable patch seam (`backend.game_logic.settlement_scoring.calculate_common_peace_acceptance`) instead of a target per consuming module; the scorer's consumers ended up split across baseline/staging/ratify and per-module patching would re-break on every future move (incl. CH-2). 65+ test patch paths migrated to the seam; 12 wrap-the-real side effects converted to import-time capture (a lazy import inside the side effect would fetch the mock and recurse); the surrender-preset/subjugation/vassalage/validate patches re-pointed at their consuming namespaces; the SC-14c/SC-5/slice-F source pins re-pointed (the slice-F backend confirm-payload pin now reads the concatenated settlement package — contract pins must not depend on which CH-1 module holds the code); the Godot parse harness re-anchored its three executor dispatch anchors per its own update-the-anchor instruction. **Verification:** full suite `10422 passed, 1 skipped` in BOTH deterministic (`-p no:randomly`) and default-random orderings at the final state AND green at every one of the 8 commits via the hook; `ruff check backend/` clean throughout; no Godot files touched. **CH-2's target is now `settlement_actions.py`; CH-4's home is `settlement_baseline.py`; CH-3 (delete the legacy `pending_settlement_drafts` store — PF-2 already picked the scoped survivor) is explicitly folded into the same follow-up slice so the deferred-work rule has an owner.**

**June 10, 2026 — GT-Slice-4 LANDED (retire the freeform editor per `SETTLEMENT_GUIDED_TERMS_SPEC.md` §5's complete inventory — ALL Guided Terms implementation slices now landed; session confidence report delivered, awaiting user review; next: the CH code-health slices, then the ONE end-of-queue Gate 4 manual smoke):** The final Guided Terms implementation slice — pure retirement + re-points; **mechanics unchanged** (validator, scoring, apply paths untouched per spec §10). **Removals (§5, all verified by absence tests):** `settlement_editor_popup.tscn`/`.gd` deleted (CanvasLayer 112; the DWL-SET-SC5R-3 inline merge-conflict controls die with the file); the identity-picker half of the Tier-3 schema deleted from `settlement_preview.py` (~490 lines: `_field_schema`/`_control_option`/`_nation_control_options`/`_region_control_options`/`_vassal_control_options`/`_side_partitioned_options`/`_clause_fields_for_review`/`_clause_enabled_from_pickers`/`_build_clause_control_schema_for_review`/`_available_clause_types_for_review`/`_build_settlement_editor_route`/`SETTLEMENT_EDITOR_SOURCES`) while the **eligibility + candidate helpers the guided suggestions reuse are KEPT** (`evaluate_*_eligibility`, the direction selectors, affordable-indemnity sizing — presence-pinned); the SC-5R EDIT payload contract (`can_edit_terms`/`available_clause_types[]`/`clause_control_schema`/`editor_route`) is GONE from every staged dialogue — pinned ABSENT (not merely falsy) for player, AI-offer-accept, and non-player stagings; the `Adjust terms` rail verb is gone from PROPOSE (the rows ARE the deep tier); **REFRONT-7 closes MOOT** (the `_region_control_options` all-regions scan died with its only producer — re-front §14 row + §scoring note updated). **Verify-dead pass + transport removal:** confirmed NO non-editor producer of the `/command` `settlement_terms`/`selected_target_nation`/`covered_enemy_participants` fields (the PF-2 War-Detail reopen and the wizard send only `target_nation`+`war_id`, which stay) → the three `CommandRequest` fields removed, the `/command` forwarding removed, and the executor's whole SC-1 `has_submitted_terms` branch (submit revalidation, REVIEW landing mode, dual-write persist) removed — **`propose_common_peace` now ALWAYS lands guided PROPOSE; REVIEW is reachable only through `submit_settlement_for_review`** (scoped-draft restore is unchanged: reopen still resumes via the PF-2 war-scoped fallback, and the staged PROPOSE draft still persists to the scoped store). A new HTTP regression (`test_old_editor_submit_body_is_stripped_and_lands_guided_propose`) pins the stripped Pydantic boundary — the old editor body can never stage a submitted blob. **The §6 merge dissolution:** `merge_same_war_settlement_drafts` + its ONLY call site removed (helper deleted — no caller remained, incl. `_clause_identity_key`); a same-war same-scope refresh now re-shows the MOUNTED dialogue's terms (the staged draft is the single source of truth; the caller's terms — the scoped store's copy of the same draft — only seed an EMPTY mounted draft), and the `same_war_merge_conflict` dead end is gone (re-pinned in `test_settlement_continuity_slice.py`: the differing-clause refresh now SUCCEEDS and keeps the active draft). **Re-points (§5, all routing-tested):** `apply_concession_baseline_replacement` and the `re_author_with_concessions` direct arm re-stage **guided PROPOSE seeded from the concession baseline** (the rail action survives; only its destination changed; no `open_editor_on_mount` anywhere — the flag has zero producers left); incoming-offer `request_settlement_revision` lands **guided PROPOSE seeded from the offered terms** with `counter_to_offer_id`/`counter_seed_terms` provenance preserved (OQ-4(b)); the PF-1 budget-bound carry hint now reads "add a territory offer on the court's row" (copy-pinned); Godot error remounts ride the CH-5 re-attach + `transient_error_display` popup path (the editor remount hooks + remount-code list deleted with the editor). **main.gd:** the dialog registration, response-route entry, mount/remount/baseline-continue helpers, the client `adjust_terms`/`revise_settlement_terms` editor branches, and the three editor signal handlers are removed; `suspend_settlement_editor` survives as the PROPOSE Back Out action id (whitelist comment re-scoped). **Editor-test disposition (§9, enumerated):** *migrated* — the draft round-trip/suspend-reopen suite (`test_settlement_sc5r2_godot_editor.py`, rewritten: Submit-for-Review now drives the `submit_settlement_for_review` dialogue action; Request Revision asserts guided PROPOSE + provenance; the executor persist pin re-homed to the staged-PROPOSE draft), the SC-13/SC-1 executor pins (`test_settlement_entry_safety_slice.py` / `test_settlement_foundation_slice.py` — the dead terms transport is now pinned IGNORED), and the SC-26 continuity pins (single-source-of-truth semantics); *deleted* — `test_settlement_refront_slice0.py` (picker-schema pins; its V4 self-liberation validator half transplanted), `test_settlement_editor_command_roundtrip.py` (the dead POST transport end-to-end), the sc5r1 EDIT-payload-contract class (replaced by absence pins), refront-slice3's picker/REFRONT-8/SC5R-3 source pins, and sc5r2's editor-widget/wiring/mount-gate classes; *kept* — every backend validity-contract test (V1–V3, ratify defense-in-depth incl. live-lord drift, conflict-matrix dual indices, tampered-clause pre-stage via `_stage_replacement_settlement_terms` + pre-ratify). **New `tests/test_settlement_gt_slice4.py` (9 tests):** five absence pins (scene/script gone; main.gd clean; settlement_preview clean with helpers kept; CommandRequest clean; staged dialogues carry no editor keys in either mode), the concession-replacement→guided-PROPOSE routing pin, the verify-dead HTTP regression, the carry-hint copy pin, and the transplanted V4 pin. **UX-3 + UX-8 close MOOT** in the pre-flight audit (the silent-picker-default and editor-REVIEW-handoff hazard classes died with the editor); the cleanup spec's control-state matrix gains the GT-Slice-4 note (EDIT row historical; **"edit-capable route" = the guided PROPOSE surface**, `Return to terms` owns REVIEW→PROPOSE). **Verification:** full suite **`10422 passed, 1 skipped` in BOTH deterministic (`-p no:randomly`) and default-random orderings** (was 10466 — net −44: the deleted editor/picker/merge pins outnumber the +9 new file and the re-homed reworks); ruff clean; **Godot 4.4.1 parse harness regenerated without the editor script — 0 failures, all parse_ok/load_ok** (incl. the edited `main.gd`). **Next: the CH code-health slices (CH-1/2/4 + the pre-smoke `settlement_preview.py` split), then the ONE end-of-queue Gate 4 manual smoke, then Slice G/G1.**

**June 10, 2026 — GT-Slice-V + GT-Slice-3 LANDED at `d339dbc` + APPROVED same day under the user-delegated confidence gate ("if 100% mark as approved" — the session's confidence report scored the spec contract 100%: every §9 completion item + named test met, suite green three times in both orderings, parse harness 0 failures; the only non-pixel-verified surface is live-client layout polish, which the user's own June-9 re-sequencing assigns to the end-of-queue Gate 4 smoke). The next session starts GT-Slice-4 (retire the freeform editor per spec §5) — no review pause pending:** The third and fourth Guided Terms implementation slices. **GT-Slice-V (voice — Voice Bible §16.1/§16.1a read first, per the handoff):** (1) **The DC-4 guard line lands VERBATIM** ("They are not the ones suing for peace, Sire — but as you wish.") as `settlement_demand_on_concede_court_caution_talleyrand`, fired on BOTH D5 triggers — an explicit demand-group `settlement_demand_add` on a concede-direction court AND the focused-Harsher dial seed on one (`_redial_settlement_terms` gains a `seeded_events` collector mirroring `protected_notes`, so the dial handler knows the seed fired and in which arm). The beats ride the restaged dialogue as one-shot `authoring_voice_beats` (`kind` = `talleyrand_caution` / `court_reaction`; merged via the restage `extra` param + surfaced on the result), dropped by the next restage — pinned one-shot. (2) **Affected named courts react to authoring** — `settlement_multi_court_demand_received` / `offer_received` (§16.1a resolver rule: `resolve_named_diplomat("envoy", court, world)`, chancery fallback pinned on Russia — never anonymous; Hardenberg answers a Prussian demand, Castlereagh notes a British sweetener). (3) **The 11 `settlement_guided_reason_*_talleyrand` committed templates** replace GT-Slice-2's plain functional `reason_display` f-strings (the in-character pass — Talleyrand's commerce/court/surgery register, no battlefield vocabulary; resolved by `_court_demand_suggestions`, equality-pinned against the resolver). (4) **OQ-6 voice extension:** `_settlement_budget_bound_recommendation` now also resolves `recommendation_voice` (3 variants — concentrate+set-aside / concentrate-only / set-aside-only), and the PROPOSE advisory slot chains it after the binding-constraint line (computed once, shared with the payload block). (5) **§5 incoming-offer revision copy retargeted**: `settlement_incoming_offer_request_revision_talleyrand` now lays the offered terms "on our own table, court by court" (no editor reference; the ROUTE re-point stays GT-Slice-4's). `docs/DIPLOMAT_VOICE_BIBLE.md` §16.1a gains the guided-authoring family table + changelog; the SC-32 D5 boundary test extends over all 17 new templates (no conference/congress/veto). **GT-Slice-3 (Godot per-court demand UI):** `proposal_confirm_popup` renders settlement_confirm in a three-region layout — header (`_build_settlement_content`) / **UX-5 ScrollContainer** (`PerCourtScroll`/`PerCourtTable`, the expansion scrolls instead of growing the popup — full-1805 horizon) / footer (`_build_settlement_footer`); scene instantiation + node paths verified headless. The table renders the §3.4 **treasury line**, the GT-Slice-V **voice beats**, and per court: `current_demands[]` lines (direction-tag colors, "(yours)" provenance, per-line **Remove** + clamped **magnitude steppers** ±100 gold/±1 turn against the GT-Slice-2 bounds metadata, no-op steps skipped client-side) and the inline **`Add demand` expansion** (§3.3 lead group pre-expanded, trailing group collapsed behind a toggle, dead-band both collapsed; OQ-1 `region_options[]`/`vassal_options[]` dropdowns via `suggpick`; the forced-alliance `[+ Continental System]` sub-link; the V reason line under every option; disabled state renders `authoring_disabled_reason_display`). Every mutation emits the backend's ready-made `action_params` (suggestion payloads verbatim + `war_id`/`draft_key`; **no identity is ever constructed client-side** — pinned) through `send_dialogue_response_with_params`. **REFRONT-9 LANDED** (re-front §14 row closed; OQ-5/GT-A4): every scoreable row now carries a display-ready `component_breakdown` — all 10 components incl. `concession_credit`, derived from the score pass already in hand (`_acceptance_component_breakdown`; ordered by |value| desc, name tie-break; ints) — the PROPOSE court name is the focus url, `settlement_focus_court` round-trips presentation-only, and the focused row expands the full table ("Why Prussia stands here"); hard-stop rows carry `[]`. **UX-2 LANDED** (server half: `dial_actions`/`holdout_actions` attach on PROPOSE only — REVIEW rows are frozen; client half: a `dialogue_mode` render branch — "STAGED FOR REVIEW — the terms are frozen" banner with promoted blockers + holdout list, and the tier-2 affordance rail gated to PROPOSE; the slice-1 `test_holdout_court_offers_ease_or_drop_not_dead_end` re-pinned to the new contract — the no-dead-end route on REVIEW is the blocked rail's `Return to terms`, Ease/Drop live on the PROPOSE rows). **The §9 D4-lesson HTTP-boundary test caught a REAL gap:** the executor's structured settlement dispatch keyed only on `action_params["action"]`, but the Godot wire shape (`send_dialogue_response_with_params` → `{"choice": <verb>, "action_params": <payload>}`) carries the verb in `choice` and a GT-Slice-2 suggestion's `action_params` deliberately has no `action` echo — `handle_diplomatic_dialogue_response` now falls back to the `choice` verb (dial affordances, which embed `action`, are unchanged). **Tests: +19** — 12 in `tests/test_settlement_guided_terms_slice_v.py` (spec-pinned `test_demand_on_concede_direction_court_fires_talleyrand_caution_voice`, both-trigger + negative-direction + one-shot pins, named-reaction + chancery-fallback pins, template-equality reason pins, OQ-6 voice-extension ordering pin, revision-copy retarget pin, the D5 boundary walk over all 17 new templates) + 7 in `tests/test_settlement_guided_terms_slice3.py` (the re-homed **`test_tier3_focused_court_expands_full_component_breakdown`** with the REAL scorer, hard-stop empty-breakdown pin, UX-2 server + client pins, UX-5 scene pins, guided-controls source pins incl. the no-identity-construction pin, and **`test_http_boundary_settlement_demand_add_exact_godot_action_params_shape`** — TestClient posting the exact popup emission through `/respond_to_diplomatic_dialogue`, asserting the clause lands with `authored_by="player"`, live re-scoring, and the reaction beat across the wire). Full suite **`10466 passed, 1 skipped`** in BOTH deterministic (`-p no:randomly`) and default-random orderings (was 10447); ruff clean; **Godot 4.4.1 parse harness regenerated — 0 failures** + a headless scene-instantiation check of the modified `.tscn`. **Next: GT-Slice-4** (retire the freeform editor per spec §5's complete inventory — removals + re-points + the verify-dead `CommandRequest` field pass + the enumerated editor-test disposition; REFRONT-7 closes moot with the schema build), then the CH code-health slices, then the ONE end-of-queue Gate 4 manual smoke.

**June 10, 2026 — GT-Slice-2 LANDED at `dc40012` + USER-APPROVED same day (per-court suggestion payload + OQ-6 budget-bound recommendation; GT-Slice-1 user-reviewed and approved same day; the GT-Slice-2 review pause is CLEARED — the next session starts GT-Slice-V + GT-Slice-3 together):** The second Guided Terms implementation slice — the suggestion payload the GT-Slice-3 Godot rows will render. **`demand_suggestions[]` (spec §3.1/§3.2/§4):** every PROPOSE per-court row now carries valid, fully-formed, direction-correct options — BOTH groups per D4 (demands court→France: territory / gold / recurring gold / vassalage / subjugation / forced-alliance `[supports_continental_system]` / liberation; offers France→court: gold / territory / recurring gold), with the court's direction choosing which group LEADS the list (§3.3 — a concede court leads with offers, the dead-band offers both with `lead_group=""`). Each option carries a one-line `reason_display` (GT-R1-15; plain functional register — the in-character pass is GT-Slice-V's) and the EXACT `action_params` the GT-Slice-1 add verb accepts, pinned by a round-trip test that feeds a suggestion verbatim through `settlement_demand_add` and finds the new line in the restaged row (valid-by-construction at the suggestion source; the validator stays authority at the restage choke point). **Table-scoped per §3.4:** gold-offer defaults cap at the shared-treasury `remaining`; BOTH region dropdowns exclude already-promised regions; eligibility gates run before rendering (the live `evaluate_vassalage/subjugation/liberation_eligibility` evaluators, court-holds-vassal, not-already-allied via `world.are_allies`) — ineligible options simply don't appear. **OQ-1 dropdowns:** the selector refactor extracts `_demand_baseline_region_candidates` / `_concession_baseline_transferable_candidates` (full ranked pools; the single-pick selectors now take the head — semantics unchanged) so territory suggestions ship `region_options[]` and liberation ships `vassal_options[]`. **§4 `gold_per_turn` capacity pre-fill:** new `_gold_per_turn_prefill` — `capacity = current_gold + max(0, net_income) × turns`, NET of existing `recurring_settlement_payments` AND gold the payer already owes inside the staged package, rate capped at the dial step, France-paid offers additionally bounded by the §3.4 `remaining`; one income estimate per payer per build via the new `_payer_net_income_estimate` (cached `get_nation_regions`, Golden Rule #8 — the full-scan validator estimator is untouched). **`current_demands[]`:** each staged clause touching the court renders as a line with `clause_index` + `expected_type` (the verbs' stale-click guard), `Demanded`/`Conceded`/`Mutual` direction tags, §3.1 line copy ("Cede Silesia" / "300 gold" / "France pays 200 gold"), magnitude metadata exposing the set-magnitude verb's bounds, and ready-made `remove_action`/`set_magnitude_action` payloads; liberation renders on the LORD court's row. **Row authoring states (§3.3):** rows carry `can_author`/`lead_group`/`authoring_disabled_reason_display` — hard-stop courts expose NO authoring with the rendered reason; an at-cap table disables ADD with the §3.1 reason but KEEPS the current lines (Remove is the way back under the cap); REVIEW and non-player stagings expose no authoring at all (UX-2's server half + the Slice-G boundary). The cap/hard-stop copy now lives in shared constants (`_DEMAND_CLAUSE_CAP_REASON` / `_demand_hard_stop_reason`) used by BOTH the add verb and the row payload, so the pre-click reason and the server rejection can never drift. **§8 OQ-6 (GT-A2):** new `_settlement_budget_bound_recommendation`, gated on the live PF-1 detector — ranks concede-direction holdouts by `gap_to_threshold` ascending with the locked tie-break (larger `abs(direct_score)` — per-court rows now carry `direct_score` for exactly this — then name), prices each gap at the live credit rate (`gap × CONCESSION_GOLD_DIVISOR`, coverable only under the per-term `CONCESSION_GOLD_CAP`), walks the pool (`treasury − reserve`) cheapest-first, and names the most expensive holdout as the set-aside (Pressburg logic). Rides PROPOSE payloads as `budget_bound_recommendation` with a computed `recommendation_display` (advice only — the player clicks; the voice-family extension stays GT-Slice-V's). The treasury line is now computed ONCE per dialogue build and shared by the payload block, the suggestion defaults, and the OQ-6 pool. **Tests:** 17 in `tests/test_settlement_guided_terms_slice2.py` — all spec-pinned names (`test_hard_stop_court_row_disables_authoring_with_reason`, `test_dead_band_court_offers_both_directions`, `test_demand_suggestions_are_deterministic_same_world`, `test_suggestion_payload_numerics_are_int` — a recursive no-float walk, `test_gold_per_turn_prefill_respects_payer_capacity`, `test_budget_bound_recommendation_ranks_cheapest_signature_first_deterministically`) plus eligibility-gating, no-identity-fields/France-self-impossible, losing-court-leads-with-offers, promised-region exclusion, liberation gating, the add-verb round trip, REVIEW/non-player absence, clause-cap-keeps-Remove, OQ-6 tie-break and not-bound-absent pins. Full suite **`10447 passed, 1 skipped`** (was 10430); ruff clean; **no Godot change** (payload-only — the Godot consumer is GT-Slice-3, and `tools/godot_parse_report.json` stays current). **Next: GT-Slice-V + GT-Slice-3 together in one session** (GT-Slice-2 user-approved June 10 — the review pause is cleared; voice lands with the UI that fires it, per the agreed cadence; read `docs/DIPLOMAT_VOICE_BIBLE.md` §16.1/§16.1a BEFORE authoring the V lines, and note the DC-4 guard line lands verbatim from the pre-flight audit), then GT-Slice-4 alone.

**June 10, 2026 — GT-Slice-1 + CH-5 LANDED (one combined commit, user-confirmed cadence; GT-Slice-1 user-reviewed and approved June 10 — see the GT-Slice-2 entry above):** The first Guided Terms implementation slice plus its recommended companion. **CH-5 (pre-flight audit §9) landed FIRST and the new verbs joined the wrapper, not the old net (the spec §7.1 dependency note):** `handle_settlement_dialogue_action` is now a thin public wrapper — the ~950-line dispatch became `_handle_settlement_dialogue_action_inner` — enforcing the ONE structural invariant via `_enforce_settlement_response_shape`: a failed/blocked settlement arm returns a re-attached `diplomatic_dialogue` (player-editor callers; the mounted dialogue is re-shown) AND a rendered `error_display` (synthesized from the `error` code via the settlement display map when the arm rendered nothing) — never neither. The old five-verb Tier-2 re-attach net is removed (subsumed); the invariant now covers the replacement-stage preset family (`re_author_with_concessions` / `author_*` / `apply_*_replacement` / `keep_current_settlement_draft`) that D7 flagged as a latent orphan, and every future arm by construction. 10 tests in `tests/test_settlement_ch5_response_shape.py` incl. the D7 regression (patched-invalid replacement stage re-attaches + renders) and a dialogue-closing-success passthrough pin. **GT-Slice-1 (Guided Terms spec §9):** the three per-court demand-mutation verbs `settlement_demand_add` / `settlement_demand_remove` / `settlement_demand_set_magnitude` land on the PROPOSE surface, resolved via `action_params` against the staged `settlement_confirm` and mutating ONLY through `_restage_settlement_after_redraw` (validate → re-preview with the memoized score pass → re-stage → persist the PF-2 scoped draft). Direction is fixed PER OPTION (D3/D4 — no identity crosses the transport): demand = court→France, offer = France→court (sweetener; rewarded by the live `concession_credit`, pinned 0→+8 at 200g in the real-scorer D4 test), dependency clauses use the validator's canonical `from=subject, to=lord` shape, liberation defaults to the court's first vassal; an unqualified add on a concede-direction court lands on the OFFER arm (§3.2 — a losing court leads with offers) while explicit demand-group adds stay legal (D5 press-past-zero; the Talleyrand caution line stays owned by GT-Slice-V). Valid-by-construction at TABLE scope (§3.4): eligibility gates (`evaluate_vassalage/subjugation/liberation_eligibility`, court-holds-vassal, §3.3 hard-stop courts reject authoring, §3.1 clause-cap rejection with the humanized eight-clauses line — never an over-cap draft), gold-offer defaults cap at the shared-treasury `remaining`, region defaults exclude already-promised regions (new `excluded_regions` param on `_demand_baseline_select_region`, mirroring the concede selector's PF-1 param). **§3.4 treasury line:** new `compute_settlement_treasury_line` → `treasury_line` block (`{treasury, committed, reserve, remaining}`, all `int()`, recurring committed at `amount × turns`, reserve = `min(500, treasury − committed)` — matches both spec examples) on every PROPOSE payload, recomputed per restage. **§3.5 `authored_by` provenance + dial composition rule:** `authored_by` joins every `CANONICAL_CLAUSE_TYPES` optional set (the validator rejects unknown keys, so schema admission was load-bearing); add/set_magnitude tag their clause `"player"`; `_redial_settlement_terms` now floors player-authored gold at the dial step (never drops) and SKIPS player-authored territory lines on the drop direction, emitting the spec-verbatim note ("Your demand for {region} stands, Sire.") through the dial handler's message via a new `protected_notes` collector kwarg — untagged Talleyrand-suggested lines keep exact legacy semantics (all slice-2 dial pins stay green). **§7 wiring (GT-R1-5, all points):** preview dispatch tuple (inside the CH-5 wrapper) + `_SETTLEMENT_TIER2_ACTION_IDS` + the executor options[] fallback + the Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist + the parity pin's canonical list (`test_godot_parse_harness.py`); guards are player-only (Slice-G boundary) AND explicit server-side `dialogue_mode == "PROPOSE"` (REVIEW is frozen; today's dials still rely on absent buttons by design). **Tests:** 19 in `tests/test_settlement_guided_terms_slice1.py` (all spec-pinned names incl. `test_demand_direction_row_can_author_proposer_paid_sweetener_and_concession_credit_applies`, `test_whole_table_generous_does_not_silently_delete_player_authored_demand`, `test_guided_gold_suggestion_caps_at_remaining_table_budget`, `test_guided_region_suggestion_excludes_already_promised_regions`, `test_add_demand_disabled_at_clause_cap_with_reason`, `test_demand_verbs_rejected_in_review_mode_with_error_display`, `test_demand_verb_failure_reattaches_dialogue_never_silent`, `test_propose_and_demand_routes_reject_non_player_caller_kind`, plus the §3.5 gold-floor, offer-arm-rejection, peace-irremovable, treasury `int()` and `authored_by` save/load round-trip pins) + the parametrized **PF-3 matrix extension `test_settlement_matrix_demand_verbs_succeed_or_reattach_with_error_display`** (18 cells × 6 verb calls incl. deliberate failures) + 10 CH-5 tests = **+47 tests**. Full deterministic suite **`10430 passed, 1 skipped`** (was 10383); ruff clean; Godot 4.4.1 parse harness 0 failures (whitelist-only `main.gd` change). `docs/SAVE_FORMAT_REFERENCE.md` documents the `authored_by` clause field; the re-front spec §11.3 dial paragraph cross-refs the §3.5 composition rule. **Next: GT-Slice-2 (per-court `demand_suggestions[]` payload + OQ-6 budget-bound recommendation) after user review of this slice.**

**June 10, 2026 — Settlement Guided Terms v0.2 APPROVED (D4 mixed-direction rows EXPLICITLY CONFIRMED by user):** The user approved spec v0.2 the same day as the audit fold below, explicitly confirming the one fold-changed decision **D4** (every court row exposes demand AND offer groups; the court's war-score direction picks which group leads — chosen on the probe-verified `concession_credit` sweetener rationale over strict one-direction rows). Spec status flipped to APPROVED; the gate-block slice tracker (`GT-Slice-1 → 2 → V → 3 → 4`) is live; routing in `CLAUDE.md` (Up Next / Design Gates / Document Map) and the pre-flight audit how-to-use line updated. **Implementation is unblocked: GT-Slice-1 (backend demand-mutation verbs `settlement_demand_add`/`remove`/`set_magnitude` + the §3.4 treasury line + §3.5 `authored_by` tag + §7 wiring/guards) is the next implementation slice, with CH-5 as its recommended companion. Per the slice-review cadence, land GT-Slice-1 then pause for review.**

**June 10, 2026 — Settlement Guided Terms: design-gate audit Run #1 (GO-with-changes) + same-day fold to spec v0.2 (docs only; no code — approval recorded in the entry above):** The Guided Terms spec was audited against live master `6edeb6f` (post-PF) per the DC-6 method — every checkable claim verified at file:line **plus five executed fixture probes** on the real `settlement_losing` world and synthetic matrix worlds (the territory, not just the map). Full report: **`docs/SETTLEMENT_GUIDED_TERMS_AUDIT.md`** — verdict **GO-with-changes, 0 CRITICAL / 9 MAJOR / 6 MINOR**, all findings + the pre-flight amendments GT-A1..4 folded into **`docs/SETTLEMENT_GUIDED_TERMS_SPEC.md` v0.2** the same session (user-delegated fold). Probe-verified headliners: **GT-R1-1** — a France-pays sweetener on a demand-direction court is legal and rewarded by the live `concession_credit` component (Prussia 35→43 in the probe) but unauthorable anywhere once Tier-3 retires under a one-direction-per-row model → folded as spec **D4 (mixed-direction rows: every row exposes demand AND offer groups, direction picks which leads) — ⚠ the one decision changed by the fold; confirm explicitly at approval**; **GT-R1-2/GT-A1** — two individually-affordable 1,000g offers from a 1,500g treasury validate per-row and fail only at the table-level budget rule → §3.4 visible `treasury_line` + table-scoped suggestion generation (remaining budget + promised-region exclusion); **GT-R1-3** — one whole-table `More generous` click DELETES a hand-authored territory demand under live `_redial_settlement_terms` semantics → §3.5 composition rule (`authored_by` tag; dials never silently drop player-authored clauses); **GT-R1-4** — the v0.1 retirement list missed three live `open_editor_on_mount` producers (`re_author_with_concessions:8430`, `apply_concession_baseline_replacement:7409`, incoming-offer `request_settlement_revision:9948`), the Godot consumers, the cleanup-spec `Revise Terms` row, and PF-1's own "pay in land via 'Adjust terms'" hint copy → §5 complete inventory with per-item dispositions; plus the honesty note that §6's merge dissolution does NOT retire D7's orphan class (the replacement-stage preset family survives; **CH-5 stays the cure**) and **REFRONT-7 closes as moot** with the schema build. Other folds: §7 new-verb wiring/guard/failure contract (dispatch tuple + re-attach net + executor frozenset + Godot whitelist + PROPOSE-mode/player-only guards — the D2/D3 lesson); §3.3 four-direction row contract (probe: a hard-stop court renders `total=null`, authoring disabled with reason); sequencing corrected to the June-9 re-sequencing (v0.1 said "after the smoke" — deadlock); **D5** press-past-zero kept with the DC-4 Talleyrand guard line (probe: two focused-Harsher clicks turn an offer into a demand on the court beating France — verified the guard line is NOT yet in the backend, now owned by GT-Slice-V); voice re-ordered before/with GT-Slice-3 (the REFRONT-V lesson); test plan gains the PF-3 matrix extension + an HTTP-boundary test + determinism/`int()` pins; GT-A2 locked as OQ-6's deterministic cheapest-signature allocation rule; GT-A3 re-led §1 with the editor-blindness cure (live per-court re-scoring while authoring is the headline win); GT-A4 locked OQ-5 (REFRONT-9 = the expanded per-court row; the re-front §14 row's landing pointer updated, and its stale header sequencing line refreshed). Verified-OK ledger in the audit §9 (PF-1 live and honest on the losing fixture; the failure-path plumbing exists for the new verbs to join; §4 covers all 8 clause types; `includes_continental_system` is an existing field; the CommandRequest field removal is clean in principle). Per DC-6 this was doc-audit run 1 of MAX 2; run 2 is a targeted fold-verification of the v0.2 diff only. **Next: user approval of v0.2 (confirm D4), then GT-Slice-1 on master — APPROVED same day, see the entry above.**

**June 9, 2026 — PF-1 + PF-2 + PF-3 LANDED (pre-flight defect ledger D1–D6 fixed; D7 latent/owned by CH-5) + Gate-4 smoke RE-SEQUENCED to end-of-queue (user decision):** All three fix slices from `docs/SETTLEMENT_GATE4_PREFLIGHT_AUDIT.md` landed in one pass. **PF-1 (D1):** `compute_settlement_baseline` now threads cross-court state — directions are pre-computed per court, the ONE affordable treasury (`payer_balance − reserve`) is split evenly across concede-direction courts (`gold_budget_cap` into `_concession_terms_for_court`), and a running `promised_regions` set is threaded into `_concession_baseline_select_transferable_region` (new `excluded_regions` param) so a region is never promised to two courts; a new `_degrade_generated_baseline_to_valid` pass validates every generated baseline and degrades the unaffordable remainder (last court first) to the shared peace floor — never stage invalid, never crash (DC-1: validity is a property of the draft store, not the author). On the real `settlement_losing` fixture the baseline went from `[1000g→Britain, Waterloo→Britain, 1000g→Prussia, Waterloo→Prussia]` with a false 62/62 "carries" to validator-clean `[500g→Britain, Waterloo→Britain, 500g→Prussia]` with honest 36/36 holdout bands. **(D2):** the `submit_settlement_for_review` arm validates BEFORE popping PROPOSE; blocked ratifies (clause-schema, clause-type, V1–V5 staged revalidation, fresh-rescore, empty-draft) now route through a shared `_blocked_ratify_reattach` helper — the staged REVIEW stays MOUNTED and rides back on the response with `error_display` + an in-character message (the stale-world `revalidate_staged_settlement` failures keep their pop+`must_reopen` recovery contract); `/respond_to_diplomatic_dialogue` passes the handler's `message`/`talleyrand_text`/`error_display` through instead of defaulting to the literal "Response processed", and surfaces the failure fields on the response. **(D3):** `main.gd::_route_proposal_confirm_response` attaches `transient_error_display` to any re-mounted dialogue on a `success=False` response and `proposal_confirm_popup.gd::_build_settlement_content` renders it as the first line — a blocked dial/Submit/Ratify is never a silent blink. **(D5/DC-2):** per-court rows now carry `direction` (same dead-band rule as the generator, via `_court_direction_from_selection`); a `_settlement_budget_bound_constraint` detector probes the REAL validator rule (one extra dial step) and, when the treasury binds, the carry hint pivots to "drop a court or pay in land" and the advisory slot speaks the new Voice-Bible line `settlement_budget_bound_constraint_talleyrand` ("Sire, the treasury cannot satisfy {holdout_names} in gold…"); submit failures speak `settlement_submit_failed_validation_talleyrand` (UX-6: error paths stay in character; both registered in `DIPLOMAT_VOICE_BIBLE.md` §16.1a). **(D6):** the targeted-posture advisory never lists a concede-direction court as a press candidate. **PF-2 (D4):** `load_scoped_settlement_draft` falls back `(war, target)`-prefix → war-prefix (most recent wins; cross-war isolation retained — the SC-5R-1 per-target isolation pin was deliberately superseded since SC-26 enforces one settlement table per war); the suspend arm and `_restage_settlement_after_redraw` write ONLY the scoped store (the legacy dual-write nothing read is gone — CH-3's PF-2 slice); `build_active_wars` rows carry `settlement_draft_kept` and War Detail renders "▸ Draft kept — Open Settlement resumes your terms." (UX-1). Reopen restore verified through the REAL client payload shape at the HTTP boundary (the D4 lesson). **PF-3:** standing scenario-matrix harness `tests/test_settlement_baseline_scenario_matrix.py` — 18 cells (direction {winning, losing, mixed} × COVERED courts {1,2,3} of a multi-party war × treasury {rich, poor}) × 2 invariants: (a) generated baseline validates + carries claim matches a fresh scorer pass; (b) every Tier-2 verb succeeds or re-attaches a dialogue with `error_display` (the CH-5 invariant, enforced as a fixture). **Smoke re-sequencing (user decision):** the Gate 4 manual smoke moves to ONE consolidated run at the END of the settlement queue (post-Guided-Terms + queued items) — the audit + matrix already caught the targeted defect classes; owner/scenarios/gate-for-Slice-G unchanged. Verification: 12 tests `tests/test_settlement_gate4_preflight_pf1.py` + 4 `tests/test_settlement_gate4_preflight_pf2.py` + 36 matrix; 2 superseded pins updated (sc5r1 blocked-ratify pop → re-attach; sc5r2 per-target isolation → war-scoped restore); full suite **`10383 passed, 1 skipped`** (was 10331); ruff clean; `tools/godot_parse_report.json` regenerated with Godot 4.4.1 (12 scripts, 0 failures). **Next: the Guided Terms approval gate (fold GT-A1..4; design gate — user approval before code).**

**June 9, 2026 — Gate-4 PRE-FLIGHT AUDIT of the `settlement_losing` / `settlement_rejected` scenarios (no code change; defect ledger now CLOSED by the PF entry above — historical):** Before the human smokes the next two scenarios, a full audit pre-flighted them by driving the real staging/dial/submit/ratify handlers against the actual fixture worlds, plus a Godot-layer UI audit and a diplo-wide design sweep. **Everything is captured in `docs/SETTLEMENT_GATE4_PREFLIGHT_AUDIT.md`** (defect ownership lives THERE — `BUG_FIXES.md` is a closed queue). Headline: **D1 CRITICAL** — `compute_settlement_baseline`'s per-court loop has no cross-court state, so the losing-side Talleyrand auto-baseline double-promises the one cedeable region to BOTH courts (V1 `region_double_promised`) and double-spends the treasury (2,000g from 1,500g), yet PROPOSE shows accept(62)/accept(62) "This peace carries" because generated baselines are never validated before staging and scoring is validation-blind (the May-30 package-harshness relaxation fixed the demand direction only — this is its concede-direction mirror). **D2 CRITICAL** — that package Submits into REVIEW with a live Ratify (submit doesn't validate; per-court gate passes), Ratify correctly blocks at the Slice-3 V1–V5 gate but pops the dialogue with no `diplomatic_dialogue` and the message swallowed to a literal red `"Response processed"` (`main.py:1396` default + PL-14 net fires only on success); reopen regenerates the same invalid baseline → infinite loop. **D3 MAJOR** — Tier-2 dials (incl. the holdout one-click Ease) brick as SILENT no-ops when restage revalidation fails: the backend re-attach net works but `main.gd::_route_proposal_confirm_response` returns before the error branch and never renders `error_display` (on `settlement_rejected` turn 2+ every dial is a dead click). **D4 MAJOR** — Back Out's "Settlement draft kept" never restores: suspend saves under a covered-list-keyed scope but the real reopen path loads with `covered=[]` (key miss) and the legacy store it also writes is never consulted (the SC-5R-2 round-trip test passed the field the client never sends). Plus D5–D7 MINOR (carry hint promises the unaffordable; advisory "presses" courts France is losing to at −85; latent replacement-stage orphan outside the Tier-2 re-attach net). D1/D2/D3-mechanism/D4 were hand-verified line-by-line; a Verified-OK ledger (blocked-REVIEW contract 3-way consistent, coverage edits, end-turn lifecycle, transport boundaries) is in the doc so the smoke doesn't re-litigate green paths. **The decisive test gap:** no test in the ~10,331 builds a two-concede-court table with the real scorer. The audit defines **PF-1** (cross-court budget/region threading + validate-generated-baselines-at-stage + message passthrough + `error_display` render + advisory guard; named tests incl. `test_losing_multicourt_baseline_validates_clean`), **PF-2** (draft-restore fallback + War Detail "Draft kept" badge + single draft store), and **PF-3** (a direction×courts×treasury scenario-matrix harness — `tests/test_settlement_baseline_scenario_matrix.py`). It also captures, with IDs and owners: design commentary **DC-1..6** (valid-by-construction must be a property of the draft STORE not the author; the carry-gate×solvency binding-constraint voice; per-court gate endorsed; the press-the-victor dial seed; **Guided Terms amendments GT-A1..A4** — shared-budget row concept, losing-allocation OQ, lead with the editor-blindness cure, REFRONT-9 fold; cap doc audits at two in favor of runtime fixtures), UX findings **UX-1..9** (REVIEW renders identical to PROPOSE; silent picker defaults; partial null guards; no per-court scroll; error copy breaks voice), code-health recommendations **CH-1..7** (split the 9,470-line `settlement_preview.py` along its natural seams per the R10–R13 precedent; dispatch-table the ~950-line `handle_settlement_dialogue_action`; collapse the dual draft stores; converge the legacy single-court concession baseline into the per-court generalization; replace per-arm safety nets with ONE invariant — every settlement handler returns a dialogue or a rendered `error_display`, never neither; centralize BBCode colors; rebalance test shape toward real-fixture + HTTP-boundary coverage), and diplo-wide notes **DW-1..5** (Slice G1 is the highest-leverage post-smoke item; two acceptance models coexist — queue an 8.EVAL convergence note; economic diplomacy is where strategic parity comes from). **Sequencing: PF-1 → PF-2/PF-3 → resume Gate 4 (`settlement_rejected` first) → Guided Terms approval with GT-A1..4 → post-smoke CH-1/2/4/5 refactor slice → Slice G1.**

**May 31, 2026 — Gate-4 settlement smoke IN PROGRESS — PROPOSE→REVIEW carry-guidance UX clarified (Submit looked like it "didn't send"):** Smoke 1 (`settlement_multilateral`, France winning vs Britain+Prussia) surfaced that the winning baseline opens as a near-acceptable **demand** that does NOT carry (Britain 44 / Prussia 35, threshold 50). Clicking **Submit for Review** *did* transition to REVIEW with all terms — but because the package doesn't carry, the per-court gate (§11.4) correctly withheld **Ratify**, leaving a REVIEW whose only forward options were escape hatches (**Seek Bilateral Peace** → single-court Britain deal at reject-5). Read end-to-end as "Submit didn't send / terms lost." Diagnosis: backend working as designed; the **UX** gave no guidance to ease first and stranded a premature submit. **Three clarity fixes (no mechanics change):** **(1)** new `propose_carry_hint` on the PROPOSE surface, emitted ONLY while the package does not carry — names the holdout courts, points at *More generous*, and warns that submitting now "only opens a review you cannot ratify"; clears the instant it carries (one *More generous* flips 44/35 → 63/54). **(2)** new non-destructive `return_to_settlement_terms` action: a blocked REVIEW now **leads** with **"Return to terms"** which re-stages the conversational PROPOSE surface with the draft **preserved** (mirrors `submit_settlement_for_review` in reverse) — so a non-carrying submit is recoverable instead of a dead end. Registered in the executor settlement dispatch + the `test_godot_parse_harness` 3-way action-id sync + the Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist. **(3)** the pair-scoped escape hatches now name the single court explicitly — `Make peace with {court} only` / `Armistice with {court} only` (+ description "Leave the joint settlement…") so the player knows they ABANDON the multilateral deal. Godot `proposal_confirm_popup.gd` renders the hint in amber after the per-court carry summary; `tools/godot_parse_report.json` regenerated with Godot 4.4.1 (0 errors). 6 new tests in `tests/test_settlement_carry_guidance_ux.py`; full suite **`10331 passed, 1 skipped`**; ruff clean. **Gate 4 smoke continues** (losing/rejected scenarios next).

**May 31, 2026 — Gate-4 settlement editor: transport + validation bugs fixed (`067e431`); Tier-3 freeform editor flagged for replacement (new guided-terms spec, post-smoke):** A Gate-4 pass of the multilateral flow found the Tier-3 "Adjust terms" editor's Submit-for-Review silently lost authored terms, and its validation rejections looked silent. **Two backend fixes (commit `067e431`; full suite `10325 passed, 1 skipped`; ruff clean):** **(1)** `CommandRequest` (`main.py`) declared only `command/action/target_nation/war_id`, so Pydantic dropped the editor's `settlement_terms` / `selected_target_nation` / `covered_enemy_participants` — the backend regenerated a baseline (PROPOSE) instead of staging the authored package (REVIEW), so authored clauses "did not appear afterwards". Declared the fields + forwarded them into the parsed command; new HTTP-boundary regression test `tests/test_settlement_editor_command_roundtrip.py` (the existing SC-5R-2 tests called the executor directly and never crossed the Pydantic boundary, so the strip shipped undetected). **(2)** the `submitted_terms_failed_revalidation` return lacked `diplomatic_dialogue`/`awaiting_diplomatic_response`, so `/command` dropped `error`/`error_display`/`validation_detail` via the generic builder and rejections looked silent (feeding the "no validations" report); routed it through `_build_result_response` + a player-facing message so Godot remounts the editor inline with the reason (self-vassalize → `dependency_direction_invalid`, power cap → `dependency_power_cap_blocked`, over-budget gold → `gold_payment_budget_conflict`). **Design finding (the deeper issue):** the editor's authoring MODEL is wrong — it asks you to pick both ends of every clause from identity pickers, so it defaults to `France`/`France` and reads as vague, diverging from every other peace surface (the bilateral `terms_guidance` flow is guided/suggestion-driven with direction implied by win/loss). **New spec `docs/SETTLEMENT_GUIDED_TERMS_SPEC.md` (v0.1 DRAFT — DESIGN GATE, NEEDS APPROVAL, DO NOT CODE)** proposes replacing the freeform editor with **guided per-court demand authoring** folded into the existing per-court rows (direction implied — enemy concedes to you, you offer when losing; valid-by-construction suggestions; no submit-blob, so the merge-vs-replace tension dissolves). User decisions this gate: **replace the editor entirely**, **fold into the per-court rows**, **cover all valid clause types**. **Sequenced AFTER the Gate 4 smoke completes** (it retires a surface the smoke partly exercises). **Gate 4 smoke continues against the losing/rejected scenarios.**

**May 31, 2026 — Gate 4 manual settlement smoke IN PROGRESS — drop-stranding contradiction fixed (could brick the settlement UI):** A Gate-4 multilateral pass found that dropping covered courts from the PROPOSE table could STRAND the player — the settlement popup vanished and could not be reopened. Root cause (three compounding layers): a Tier-2 affordance click HIDES the popup before the response (`proposal_confirm_popup.gd::_on_settlement_tier2_affordance`); dropping the LAST covered court is correctly blocked by the V5 coverage floor but returned NO `diplomatic_dialogue`, and the popup only re-mounts when one is present (`main.gd::_response_has_proposal_confirm_route`) → the popup orphaned while the `settlement_confirm` dialogue stayed mounted; and any reopen targets the war's defender leader (e.g. Britain), which the player had dropped, so `stage_settlement_confirm`'s same-war refresh rejected `selected_target_not_covered`. Three fixes, all in `settlement_preview.py`: **(A)** the LAST covered court's holdout row offers Ease but NOT Drop (`len(covered) > 1` gate) — kills the dead-click at the source; **(B)** a single choke-point in `handle_settlement_dialogue_action` re-attaches the unchanged mounted dialogue to ANY blocked/no-op Tier-2 result (player-editor only) so the popup always re-mounts instead of orphaning (covers the whole class: drop floor, uncoverable add, dial-scope/focus errors, revalidation/preview failures); **(C — the "similar contradiction")** `stage_settlement_confirm`'s same-war reopen now SNAPS an out-of-scope incoming target into the mounted scope (mirroring `_restage_settlement_after_redraw`, which already snapped) instead of rejecting, so reopening re-shows the player's narrowed settlement — an EXPLICIT covered-set edit with a mismatched target is still rejected (validation preserved). 4 regression tests in `tests/test_settlement_refront_slice2.py` (last-court no-Drop, blocked-drop re-show, reopen target-snap, explicit-mismatch still rejected); full deterministic suite `10322 passed, 1 skipped`; ruff clean (no Godot change this commit). **Gate 4 smoke continues.**

**May 30, 2026 — Gate 4 manual settlement smoke IN PROGRESS — two more PROPOSE-surface findings folded (gold-direction term render + inert holdout pseudo-link):** Continued the `settlement_multilateral` Gate-4 pass; two presentation findings fixed this commit. **(1) Gold-indemnity term row read backwards (cosmetic).** The PROPOSE/REVIEW Terms list rendered lump gold through `_term_display`'s generic `"{base}: {from} to {to}"` branch, which dropped BOTH the amount and the "from" preposition — so a winning France's indemnity showed as `Gold indemnity: Britain to France`, which a reviewer parsed as France *paying* Britain. The data was never inverted (France is the rewarded victor). `_term_display` (`settlement_presentation.py`) now renders lump gold as `Gold indemnity: 300 gold from Britain to France`, matching the territory + `gold_per_turn` rows directly above it; it is the shared helper, so the REVIEW surface + diplomatic-ledger recent-settlements gain the amount + clear direction too. **(2) Inert holdout Ease/Drop body text (cosmetic).** The PROPOSE per-court table printed each holdout's `Ease <court> | Drop <court>` as blue pseudo-link body text that looked clickable but did nothing — the working controls are the self-labeled buttons built by `_add_settlement_tier2_buttons`. Removed the duplicate inline render (`proposal_confirm_popup.gd`). **(3) Diagnosed not-a-bug:** the "why are they conceding so much at war-score 0?" reading was the fixture, not a defect — `settlement_multilateral` seeds France at war_score **+50** vs both courts (tier "Dictated Terms", enemy exhaustion Britain 60 / Prussia 35); the 44/35 are *below* the 50 sign threshold (near-acceptable holdouts, not acceptances). No code change. 4 regression tests in `tests/test_settlement_presentation.py` pin the gold amount + payer→recipient direction; full deterministic suite `10318 passed, 1 skipped`; ruff clean; Godot 4.4.1 parse exit 0. **Gate 4 smoke continues against the losing/rejected scenarios.**

**May 30, 2026 — Gate 4 manual settlement smoke IN PROGRESS — two fixes folded (baseline package-harshness relaxation + `<null>` per-court render):** The first live Gate-4 pass of the re-fronted multilateral flow (`SOVEREIGN_SMOKE_START=settlement_multilateral`, France vs Britain+Prussia) surfaced two issues, both fixed this commit. **(1) Baseline package-harshness relaxation (functional):** the winning multilateral default opened at 5/-4 (both courts gross-rejected on Term harshness) despite the fixture seeding a decisive win. Root cause — `_demand_terms_for_court` floor-checks each court against its OWN slice's harshness, but the surface scores every court against the WHOLE package's `raw_total_harshness` (package-level, shared across courts), so two courts' demands compound below the floor. New `_relax_baseline_demands_for_package_harshness` strips over-demanded clauses (one per pass, worst court first, territory before gold) until every covered demand court clears the near-acceptance floor under the FULL package — the multilateral now opens at 44/35 **near-acceptable** (parity with the single-court demand baseline) instead of 5/-4. Ratification stays reachable (white peace carries 63/54; the demand baseline is near-acceptable by design and the player eases it toward carry). Concessions are never stripped; the relaxation only REMOVES clauses, so the package stays valid-by-construction. Regression test `test_baseline_winning_multilateral_clears_floor_not_gross_reject` runs the real smoke fixture and fails pre-fix at 5/-4. **(2) `<null>` per-court render (cosmetic):** the PROPOSE per-court table printed a literal `<null>` under each court's blocker on first paint — `delta_display` is null before any dial, and the Godot guard did `str(row.get("delta_display",""))`, which yields Godot's `<null>` (the `.get()`-returns-null gotcha — `str(null) == "<null>"`). Added a `_safe_str()` null-coalesce in `proposal_confirm_popup.gd` applied to the per-court fields. Godot 4.4.1 headless parse regenerated (exit 0 — runtime-verifies the `.gd`). Full deterministic + default-random suites `10314 passed, 1 skipped`; ruff clean. **Gate 4 smoke continues against the losing/rejected scenarios.**

**May 30, 2026 — Settlement Conversational Re-front Slice 3 IMPLEMENTED (Tier-3 valid-by-construction editor + cross-court validity V1–V3 + REFRONT-8 + DWL-SET-SC5R-3) — suite-green:** Slice 3 closes the re-front by making the structured picker editor the opt-in valid-by-construction deep tier. (1) **Multi-party cross-court validity (§12)** in `validate_settlement_terms`: **V1** `region_double_promised` (a region may appear in at most one `territory_cede` clause — structural, always enforced; returns both offending indices), **V2** `clause_target_uncovered` (a clause's `from`/`to` and dependency roles must be proposer-side or in `covered_enemy_participants` — enforced when `war_instance` + `proposer_side` + covered are known; liberation checks `lord_nation`/`liberator` only, NOT the freed `vassal_nation`, since `evaluate_liberation_eligibility` never requires the vassal to be a participant), **V3** `clause_side_mismatch` (value-transfer clauses — territory/gold/forced-alliance — must straddle opposite, known war sides via `_side_for_nation`; also kills the degenerate `from==to` self-transfer). V4/V5 already land in Slice 0 / Slice 2. The new `covered_enemy_participants` validator param is threaded into the POST-preview endpoint, the EDIT **Submit revalidation** (defense-in-depth — `test_submit_revalidation_enforces_uncovered_court_defense_in_depth`), and the dial/coverage restage + replacement re-validations. (Ratify gate — see the audit fold at the end of this paragraph: it now re-runs V1–V5 against the live world before mutation, superseding the original type-only design.) (2) **P2/P3 picker filtering (§13):** `_nation_control_options` now offers only proposer-side participants + covered accepting-side courts (uncovered enemies are unpickable — mirrors V2), and a coverage drop re-filters every picker on the next dialogue rebuild (`test_tier3_picker_refilters_on_coverage_change`); the Tier-1 pre-filled region/payer default stays replaceable because the pickers always offer the full filtered option set (`test_tier1_default_identity_remains_replaceable_in_tier3`). `proposer_side` is threaded through `_build_clause_control_schema_for_review` ← `build_settlement_confirm_dialogue`. (3) **REFRONT-8 (Godot):** `settlement_editor_popup.gd` now consumes `clause_control_schema[type].enabled` / `disabled_reason_display` — `_populate_add_clause_selector` greys disabled types in the dropdown (and appends the reason) via `set_item_disabled`, and `_on_add_clause_pressed` surfaces the reason instead of opening an empty picker (belt-and-suspenders against keyboard selection). Closes the cleanup-spec line-601 editor UX whose backend precondition Slice 0 landed. (4) **DWL-SET-SC5R-3 (Godot):** inline merge-conflict `Discard new clause` / `Replace active clause` sub-controls (cleanup spec line 589). `_on_confirm_clause_pressed` detects a conflict client-side (`_find_conflict_for_new_clause` mirrors `CLAUSE_CONFLICT_MATRIX` + the V1 region rule; backend stays authoritative at Submit) BEFORE appending, holds the new clause, and shows a dynamically-built merge-conflict banner; Discard drops the new clause (keeps active), Replace removes the conflicting active clause and commits the new one. (5) **Adjust terms → EDIT, optionally focused:** the PROPOSE `adjust_terms` → editor mount (Slice 1) now carries `focused_court` (the presentation-only `settlement_focus_court` selection) into the editor header. The backend `duplicate_or_conflicting_clauses` response gains `conflicting_index` for parity with V1. **New tests:** `tests/test_settlement_refront_slice3.py` (11 — the 9 §14 Slice 3 named + the Submit-revalidation defense-in-depth pin + a merge-conflict-mirrors-backend pin). Full deterministic AND default-random suites `10309 passed, 1 skipped`; ruff clean; Godot 4.4.1 headless parse exit 0 (report regenerated). **No mechanics change beyond the spec'd validity rules; the per-court gate, baseline, dials, and REFRONT-V voice are unchanged.** **Remaining: Gate 4 manual settlement smoke** (the editor is interactive for clause authoring), then Slice G.

**May 30, 2026 — Slice 3 post-implementation audit fold (synthesis: 1 CRITICAL + 2 MAJOR + 2 MINOR) — suite-green `10314 passed, 1 skipped`, ruff clean, Godot 4.4.1 parse exit 0:** A two-reviewer adversarial audit of the Slice 3 commit found and this commit folds: **(1) CRITICAL — ratify gate now runs V1–V5 defense-in-depth.** The original Slice-3 ratify gate was type-only; a package valid when authored could mutate state after the world drifted under it — reproduced as a staged `liberation` whose vassal's live lord changed between Submit and Ratify, which `release_vassal`'d the wrong, **uncovered** lord's vassal at `_apply_settlement_terms`. Fixed in `ratify_settlement_confirm` with `_normalize_staged_terms_for_validation` (apply-format `gold_lump`/plural `regions` → canonical, on a COPY so the mutation path keeps the originals) + `validate_settlement_terms(..., enforce_solvency=False)` **before** mutation. `enforce_solvency=False` is a new validator param that skips only the authoring-time gold budget gate (the apply path *clamps* gold to the payer balance rather than blocking, so the C2 clamp fixtures are preserved). Tests `test_ratify_blocks_staged_liberation_after_live_lord_drift` + `test_ratify_normalizes_apply_format_terms_so_valid_package_still_ratifies`. **(2) MAJOR — role-aware pickers (P2).** `_side_partitioned_options` splits the proposer/covered nation options onto **disjoint** lists for the fixed-direction roles (`forced_alliance` subject=covered/imposer=proposer; `liberation` lord=covered/liberator=proposer) so a same-side imposition/liberation is unauthorable, not merely Submit-rejected; direction-chosen roles (territory/gold payer↔payee) keep the full filtered list (validator V3/V4 authority, cleanup line 609). Test `test_fixed_direction_role_pickers_are_side_filtered`. **(3) MAJOR — OQ#4 focused breakdown re-homed.** Slice 3 shipped only `focused_court` in the editor header; the per-court-row focus trigger + the full 10-component focused-court breakdown panel are not built (the editor has no live preview panel) — re-homed as spec row **REFRONT-9** (deferred-with-owner + completion + test) per Golden Rule #9, not left as an unowned placeholder. **(4–5) MINOR** — stale "Slice N next" routing text corrected here + in `CLAUDE.md`; the Godot source-grep pins acknowledged (no GDScript runtime harness; backend conflict authority is pinned where feasible). 4 new tests → `tests/test_settlement_refront_slice3.py` now 15. Backend-only + doc changes; the Godot editor is unchanged by the fold (parse report current).

**May 30, 2026 — Settlement Conversational Re-front Slice 2 IMPLEMENTED (Tier-2 intent dials + live per-court acceptance + coverage edits + focus + the §15 F-5 shared-projection scale step) — suite-green, audit fold folded on master:** Slice 2 fronts the conversational steering layer over the Slice 1 PROPOSE baseline. (1) **Scorer scale step (§15 F-5):** `calculate_common_peace_acceptance` gains a `balance_projection` memoization param (mirroring `side_pressure_result` / `direct_scores` / `raw_total_harshness`); its internal `project_balance_after_settlement` call site uses the injected value when supplied and recomputes when `None`, so every existing single-call caller is unchanged. `compute_per_court_acceptance` now computes the package-level projection ONCE per dial/coverage action and injects it into each per-court scorer call (no O(N) projections). Tests `test_per_court_scoring_shares_one_..._and_balance_projection_pass` + `test_balance_projection_param_falls_back_to_internal_compute_when_not_injected`. (2) **Intent dials** (`settlement_dial_harsher` / `settlement_dial_generous`): whole-table (PROPOSE rail) or focused (per-court row), via `_redial_settlement_terms` — a pure MAGNITUDE/COUNT redraft (gold steps by `SETTLEMENT_DIAL_GOLD_STEP=100`, whole clauses add/drop) that NEVER swaps the requested region or payer identity (OQ#7; `test_dial_changes_magnitude_without_silently_swapping_requested_identity`). **Focused dialing is reached from each per-court ROW (OQ#1 / §17 "press Prussia"):** every dialable row carries `dial_actions` = a focused `Press <court>` (+ a focused `Ease <court>` on non-holdout rows; holdout rows expose Ease via `holdout_actions`, so no duplicate), player-only. (3) **Coverage edits** (`settlement_cover_add` / `settlement_cover_drop`): mutate `covered_enemy_participants`, RE-DRAW the baseline for the new set (drops removed-court clauses — V2; V5 floor blocks dropping the last court), and surface new `ignored_participants[]` / `remaining_wars[]`. Conversational `coverage_add_suggestions` ("Bring <court> to the table") ride on the PROPOSE surface (OQ#2, same `covered_enemy_participants` state as the checklist). (4) **`settlement_focus_court`:** presentation-only focus (no term mutation, no re-score). (5) **Live band + delta:** `build_settlement_preview` threads `previous_bands` into the aggregator so each rescored row carries a presentation-only `delta_display` (never feeds the scored result — Golden Rule #6). (6) **Talleyrand targeted-posture advisory** (`targeted_posture_advisory`): a VOICE-ONLY "I'd press X and ease Y, Sire" line on the PROPOSE surface — deterministic from the bands, never applies a dial (`test_targeted_posture_is_voice_only_not_applied`). (7) **Transport:** the five Tier-2 verbs are added to the executor settlement dispatch tuple + the canonical `SETTLEMENT_DIALOGUE_DISPATCH_ACTION_IDS` + the Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist. Per-court-row affordances (focused dials, holdout Ease/Drop, coverage add) ride OUTSIDE `options[]`, so `handle_diplomatic_dialogue_response(action_params=...)` resolves them directly against the staged `settlement_confirm` (the `/respond_to_diplomatic_dialogue` endpoint now reads `action_params`; `handle_settlement_dialogue_action` reads `scope`/`nation` from it). Whole-table dials ride in `options[]` and resolve via the existing index path with their server-side `scope:"table"`. All five verbs are PLAYER-ONLY (Slice-G boundary — a non-player staging is rejected). (8) **Godot:** `proposal_confirm_popup.gd` renders the delta + advisory + clickable per-row Tier-2 affordance buttons (`_add_settlement_tier2_buttons` / `_on_settlement_tier2_affordance`) + coverage prompts; `main.gd` routes per-row affordances through `api_client.send_dialogue_response_with_params(...)`; Godot 4.4.1 headless parse 0 failures, report regenerated. **New tests:** `tests/test_settlement_refront_slice2.py` (18 — the 10 §14 Slice 2 named + the player-only gate + the V5 coverage-floor + the focus-no-mutation pin + the per-row focused-dial contract + the §17 focused-press-from-row end-to-end + the Godot source pin + the two audit-fold guards: focused-seed clause-cap no-op + leaderless-seed skip). Full deterministic AND default-random suites `10298 passed, 1 skipped`; ruff clean. **Audit fold:** the focused-dial seed now honors `MAX_SETTLEMENT_CLAUSE_COUNT` (a maxed package yields a valid no-op redraft, not an over-cap draft the restage revalidation would reject for `max_clause_count_exceeded`) and guards both seed shapes on a known proposer leader (no malformed empty-party clause). **No other mechanics change beyond the spec'd dials/coverage; the per-court gate, baseline, and REFRONT-V voice from Slice 1 are unchanged.** Remaining: **Slice 3** (Tier-3 editor + REFRONT-8 Godot `enabled`/`disabled_reason_display` consumer + DWL-SET-SC5R-3 merge-conflict controls) and the Gate 4 manual smoke.

**May 30, 2026 — Settlement Conversational Re-front Slice 1 LANDED at master `ec1081d` — audit GO; fixes folded at `2626e91` (Tier-1 multi-party baseline + PROPOSE surface + per-court ratification gate + REFRONT-V voice; suite green):** The conversational PROPOSE front door now exists. (1) **`compute_settlement_baseline`** (`settlement_preview.py`) generalizes `_compute_concession_baseline` into a per-court draft that picks DIRECTION from *each court's* `direct_score` (the int half of `select_direct_score(direct_scores[court])`), not the package side-pressure scalar (§8 OQ#5): `direct_score > +DIRECT_SCORE_DIRECTION_MARGIN` (=10) → demand (border-region cession + modest affordable indemnity *from* the court, mirroring `generate_suggested_terms`' demand stage), `< -MARGIN` → concede (the existing peace→gold→territory escalation *by* the proposer leader), inside the dead-band → `{"type":"peace"}` floor, and `select_direct_score` → `None` → a per-court **hard-stop** row (matching `HARD_STOP_NO_DIRECT_WAR_SCORE`, NOT neutral-floored). `DIRECT_SCORE_DIRECTION_MARGIN` is a deliberately distinct constant from `LOSING_SIDE_PRESSURE_THRESHOLD` (different scale). The draft is deterministic (sorted courts, no RNG) and valid-by-construction. (2) **`compute_per_court_acceptance`** is the §11.2 aggregator: one `calculate_common_peace_acceptance` call per covered court over one shared `direct_scores` + `side_pressure_result` + `raw_total_harshness` pass (Golden Rule #8), varying `accepting_leader` while holding the full covered set; returns the compact `per_court_acceptance` rows (band/total/top_blocker/direction_summary/hard_stops) + `overall_acceptance` (`carries` iff every covered court has a non-null total ≥ threshold and no hard-stops; holdout courts listed). No-direct-score courts are surfaced as hard-stop rows without poisoning the scoreable courts' side-pressure. (3) **`dialogue_mode="PROPOSE"`** is added to the `settlement_confirm` contract (classified non-hard-stop like EDIT — `dialogue_manager.is_hard_stop()` now returns False for a PROPOSE settlement_confirm, so PROPOSE never blocks end-turn; REVIEW stays blocking). `build_settlement_preview` generates the baseline when opened empty and attaches the per-court block to every preview; `build_settlement_confirm_dialogue` emits `dialogue_mode` + the per-court block + a clean PROPOSE rail (Adjust terms → EDIT, Submit for Review → REVIEW, Back Out → non-destructive `suspend_settlement_editor`). `_execute_propose_common_peace` lands PROPOSE (`propose_on_mount=True`) instead of the old blank EDIT, persisting the staged baseline to the scoped draft store for Back Out / reopen. `submit_settlement_for_review` (new handler) re-stages REVIEW. (4) **Per-court ratification gate (§11.4):** `can_ratify` AND-gates on `overall_acceptance.carries` (white peace exempt), `ratify_settlement_confirm` adds a per-court fresh-rescore gate (a covered minor holdout blocks even when the leader accepts), and every holdout per-court ROW carries one-click `Ease`/`Drop` affordances (`holdout_actions`; the dial/coverage HANDLERS land in Slice 2). (5) **REFRONT-V multi-court voice:** `resolve_multi_court_settlement_voice` resolves each court's line to its NAMED diplomat (chancery fallback, never anonymous) and a Talleyrand table narration; wired into the per-court rows. `DIPLOMAT_VOICE_BIBLE.md` §16.1a documents the family; committed copy avoids "conference/congress/veto" (SC-32 D5). (6) **Godot:** `proposal_confirm_popup.gd::_build_settlement_per_court_block` renders the per-court table (band, direction, blocker, named voice, holdout affordances) + Talleyrand narration; `main.gd` mounts the Tier-3 editor on `adjust_terms` (parallel to revise) — Godot 4.4.1 headless parse check exit 0 across all settlement-critical scripts, report regenerated. **One deliberate design clarification surfaced during implementation:** the demand-direction baseline gates added demands on the **near-acceptance floor** (don't suggest a demand that makes a court outright *reject*), NOT the accept threshold as OQ#5's prose literally read — because `base_side_pressure` is package-level (§11.2), so in a *mixed* war a led court shares the package's middling pressure and an accept-threshold gate would back every demand off to peace. A demand court may therefore seat as a near-acceptable holdout the player eases/drops; the baseline **never suggests a demand that pushes a court below the near-acceptance floor** (`_demand_terms_for_court` scores each candidate and keeps it only while the court stays at/above the floor; a court whose white peace already rejects under the shared package pressure stays an ease/drop holdout regardless of terms), not a blanket carry (consistent with the existing OQ#5 holdout note). **New tests:** `tests/test_settlement_refront_slice1.py` (22 — the 16 §14 named + the 3 §14 Voice + the Slice-G `test_propose_and_dial_routes_reject_non_player_caller_kind` absence test + Godot source pins). The one routing test in `test_settlement_sc5r2_godot_editor.py` that asserted the old blank-EDIT mount was migrated to the PROPOSE landing. `SETTLEMENT_DIALOGUE_DISPATCH_ACTION_IDS` gained `submit_settlement_for_review`. Full deterministic suite green; ruff clean. **Slice 2 is next** (Tier-2 intent dials + live per-court acceptance + the `settlement_dial_*` / `settlement_cover_*` handlers). The v0.6-APPROVED paragraph below is superseded only on slice status (Slice 1 now LANDED).
>
> **Slice 1 audit tracking:**
> - [x] **REFRONT-SLICE-1 audit** — adversarial audit of master @ `ec1081d` returned **NO-GO** (R6-M1 + R6-M2 MAJOR — demand baseline not floor-aware / non-player PROPOSE rail leak; R6-M3 MAJOR stale routing pointers; R6-m1 MINOR test gaps). All four folded on master at `2626e91` and re-audited **GO** (full suite `10280 passed, 1 skipped`; ruff clean — see `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_AUDIT.md` §13/§13.1). Slice 2 dial-handler work is unblocked.

**May 29, 2026 — Settlement Conversational Re-front v0.6 APPROVED (design gate cleared; implementation unblocked — Slice 1 is the active next slice):** The user approved `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md` **v0.6** on May 29, 2026; the DESIGN GATE / "DO NOT CODE" hold is **lifted**. **Slice 0** (interim picker band-aid) already **LANDED** (master `4ffdfcc` + audit follow-up `3c7a55e`; see the paragraph below), so the remaining re-front work is **Slices 1–3 + the multi-court settlement-table voice slice**. **Slice 1 is NEXT** (spec §14, `SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md:339`): generalize `_compute_concession_baseline` → `compute_settlement_baseline` (per-court direction reading each court's `direct_scores` via the `select_direct_score` tuple-or-`None` contract — `None` → per-court hard-stop, dead-band → `{"type":"peace"}` floor); add `dialogue_mode="PROPOSE"` + the `per_court_acceptance` / `overall_acceptance` payload (§11.2); add the **per-court ratification gate** (§11.4 — `carries` iff every covered court ≥ threshold, REVIEW carries `per_court_acceptance`, holdout courts surface one-click Ease/Drop); render the PROPOSE surface; and route `propose_common_peace` to land PROPOSE instead of a blank EDIT. The **REFRONT-V** multi-court voice resolver rule lands **with** Slice 1 (its per-court copy must resolve to named diplomats — no anonymous beats). Slice 1 carries 16 named behavior tests (§14). After Slice 1: **Slice 2** (Tier 2 intent dials + live per-court acceptance), **Slice 3** (Tier 3 valid-by-construction editor — folds in **REFRONT-8**, the Godot consumer of Slice 0's `enabled`/`disabled_reason_display`, plus `DWL-SET-SC5R-3` inline merge-conflict controls), then the multi-court voice slice. **Gate 4 manual settlement smoke does not fully pass until REFRONT-8 lands in Slice 3.** The v0.6 spec paragraph below is **superseded only on gate status** (now APPROVED, no longer awaiting approval); its mechanics summary (per-court gate, multi-court voice, SC-32 D5 no-conference boundary) still holds.

**May 29, 2026 — Re-front Slice 0 (interim picker band-aid) LANDED at master `4ffdfcc` + audit-follow-up doc fixes:** The independent de-risk **Slice 0** (carved out of the gated re-front below; it has no dependency on v0.6 approval) shipped the **backend** half of the cleanup-spec line-601 contract: `_build_clause_control_schema_for_review` now computes per-clause-type `enabled` from picker emptiness + a humanized `disabled_reason_display` (was hardcoded `enabled=True`), and liberation's `vassal_nation` picker dropped the `or nation_options` fallback so it offers only real vassals. Disabled clause types stay schema keys, so `available_clause_types[]` is unchanged. New `tests/test_settlement_refront_slice0.py` (the 4 §14 Slice 0 tests incl. the never-landed line-618 test); the one sc5r1 shape test relaxed from "every dynamic row enabled" to the enabled/disabled invariant. Suite `10256 passed, 1 skipped`; ruff clean. **Audited at `4ffdfcc` → GO-with-changes.** The backend code is correct and Slice 1 may proceed; the follow-up commit lands **doc-only** ownership fixes (no code revert): (1) **`vassal=France` is fixed end-to-end** — the Godot picker faithfully renders the now-vassal-only options list — **but** the broader line-601 *editor UX* ("Add Clause control disabled instead of opening an empty picker") is **not** delivered: `settlement_editor_popup.gd` does not yet consume `enabled`/`disabled_reason_display` (`_populate_add_clause_selector:259` lists every type; `_on_add_clause_pressed:321` ignores `enabled`), so a disabled type still opens an empty picker rejected only at Submit. That Godot consumer is now owned as **REFRONT-8**, folded into **Slice 3** with a named test (`test_godot_editor_disables_add_clause_for_disabled_type_and_surfaces_reason`); (2) the spec §13 P1 / §14 Slice 0 / §16 are reframed to credit Slice 0 with the **backend** contract + the end-to-end vassal filter, **not** the full UX — so **Slice 0 alone does NOT fully unblock the Gate 4 smoke pass**; that closes when REFRONT-8 lands in Slice 3. The full re-front (Slices 1–3 + multi-court voice) remains DESIGN-GATED at v0.6 below.

**May 29, 2026 — Settlement Conversational Re-front spec at v0.6 (fifth audit run folded — synthesis; DESIGN GATE — doc-only, NEEDS APPROVAL, DO NOT CODE) [gate status SUPERSEDED by the v0.6-APPROVED paragraph at the top of this section; this paragraph records the pre-approval spec-finalization moment]:** `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md` is now **v0.6**, awaiting **USER APPROVAL of v0.6**. The fifth run synthesized two independent passes — a **Claude NO-GO** (1 CRITICAL + 1 MAJOR) and a **Codex GO** (2 MAJOR + 1 MINOR); every finding was re-verified against live code and folded. The CRITICAL (Codex missed it): §8 OQ#5 per-court direction read `per_court_direct_score[court].score`, but `select_direct_score` (`settlement_scoring.py:243`) returns a `(int, str)` **tuple-or-`None`**, and a `None` (no direct war score) is hard-stopped by the scorer (`HARD_STOP_NO_DIRECT_WAR_SCORE`, `:353-356`), not neutral-floored — OQ#5 now unpacks the tuple after a `None` guard and routes `None` → per-court hard-stop, dead-band → `{"type":"peace"}` floor. The MAJORs: OQ#5 direction now uses a war-score-scale `DIRECT_SCORE_DIRECTION_MARGIN` dead-band instead of the side-pressure constant `LOSING_SIDE_PRESSURE_THRESHOLD`; §15's band-display row split (raw scorer returns `verdict`/`feedback`; `_enrich_acceptance_display:274` derives `band`/`top_components`); and this stale Quick-Stats / audit-note version text realigned. Minors: `raw_total_harshness` added to the §15 shared-pass list (+ Slice-2 test rename), the non-existent "Voice Bible Intro:7" cite dropped, and the `modify_harsh` action-branch caveat added. One Claude finding (DWL-SET-SC5R-3 at cleanup `:589`) was **withdrawn on verification** as a correct cite. Audit record: `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_AUDIT.md` (cumulative v0.2→v0.6, §11). **The May 29 v0.5 paragraph below is SUPERSEDED** on version/audit-count only — its mechanics summary (per-court gate, multi-court voice, SC-32 D5 no-conference boundary) still holds.

**May 29, 2026 — Settlement Conversational Re-front spec at v0.5 (four audit runs folded; DESIGN GATE — doc-only, NEEDS APPROVAL, DO NOT CODE) [SUPERSEDED by the v0.6 paragraph above]:** `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md` is now **v0.5**, awaiting **USER APPROVAL of v0.5**. On approval → implement Slices 0–3 + the **multi-court settlement-table voice slice** (NOT a "conference" voice — cleanup SC-32 D5 bans player-facing conference/veto copy). The ratification gate moves from today's single-`accepting_leader` scoring (`settlement_preview.py:2434-2444`; ratify `:4583`) to **per-covered-court** (`carries` iff every covered court ≥ threshold; holdouts block but expose one-click Ease/Drop). v0.5 folds a fourth audit run — a Codex **NO-GO** (1 CRITICAL: §5 said "ratification gate = unchanged," contradicting the per-court gate change in §7/§11.4/§15; + 3 MAJOR: §17 worked-example direction read "side-pressure" instead of `direct_scores[court]`, §11.2 prose listed `feedback`/`top_components` absent from its compact schema, and STATUS/audit-note version drift) plus Run-#3 items (baseline-target-vs-carry-gate holdout note, `carries` null/hard-stop definition, §17 "conference" shorthand callout). Audit record: `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_AUDIT.md` (cumulative v0.2→v0.5, §10). **The May 28 v0.2 paragraph below is SUPERSEDED** — its "v0.2", the `:4776` ratify cite, and the "conference-voice slice" wording are stale.

**May 28, 2026 Settlement Conversational Re-front spec finished to v0.2 + audited (DESIGN GATE — doc-only, NEEDS APPROVAL, DO NOT CODE) [SUPERSEDED by the May 29 v0.5 paragraph above]:** `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md` advanced from v0.1 vision to **v0.2 detailed**. All seven §8 open questions are resolved with rationale: OQ#1 dial scope = **both** (whole-table default + focused court; Tier 2 court-level; targeting is voice/advice only); OQ#2 coverage editing = **both** checklist + conversational prompt over one shared `covered_enemy_participants` state; OQ#3 Tier 3 = always one click (`Adjust terms`), never the default; OQ#4 per-court display = progressive disclosure (band + top-blocker + delta in Tier 1/2, full 9-component breakdown in Tier 3); OQ#5 baseline generalized per-court/both-directions (demand where France leads, concede where pressured); OQ#6 novelty = deterministic input-richness + voice only, RNG/LLM-decided variation **rejected** (default-CUT owned item REFRONT-6 with guard test); OQ#7 territory/gold identity authored in Tier 3, magnitude in Tier 2, advisor pre-fills the default. v0.2 adds: a new `dialogue_mode="PROPOSE"` conversational front (Tiers 1–2) classified **non-hard-stop like EDIT** (so it never traps the player; end-turn discards, Back Out via `suspend_settlement_editor` preserves); the `per_court_acceptance` / `overall_acceptance` payload (§11.2); multi-party cross-court validity rules V1–V5 (§12 — no region double-promised, no binding an uncovered court, from/to side-correct, no self-reference → kills France-liberates-France, coverage floor); picker valid-by-construction rules P1–P3 (§13); a slice plan with named behavior tests (§14); and a verified file:line reuse map (§15). **One deliberate mechanics change, surfaced + user-approved (§11.4):** the ratification gate moves from today's **single-`accepting_leader`** scoring (`settlement_preview.py:2431-2444`/`:4776`) to **per-covered-court** — `carries` iff every covered court ≥ threshold, holdouts block but expose one-click Ease/Drop, REVIEW carries the per-court block. Chosen on UX + historical-analogue grounds (Napoleonic coalitions ended in *separate* peaces — Pressburg/Tilsit — with holdouts like Britain fighting on; single-leader gating is the ahistorical one). **Interim de-risk (Slice 0, independent):** the picker band-aid lands a contract `SETTLEMENT_UI_CLEANUP_SPEC.md:601` already promised but never wired (`_build_clause_control_schema_for_review` hardcodes `enabled=True` at `:3001`; liberation falls back to all-nations at `:2959`; the line-618 test `test_clause_add_disabled_when_picker_filter_empty_for_each_live_clause` is absent). Audit: `docs/SETTLEMENT_CONVERSATIONAL_REFRONT_AUDIT.md` — **GO-with-changes → GO** (4 change items C1–C4 found and folded into v0.2; coherence with `DIPLOMACY_SPEC.md` + `SETTLEMENT_UI_CLEANUP_SPEC.md` PASS; Golden Rules #6/#8 PASS; no orphan deferrals). **SC-5R-2 editor and the `suspend_settlement_editor` Back Out fix remain LANDED — this re-fronts the front door, it does not revert them.** Doc-only change; full suite unchanged (`10252 passed, 1 skipped`). **Next gate: USER APPROVAL of v0.2 → write implementation Slices 0–3 + the conference-voice slice.** Slice G AI-ally agency stays separate and later.

**May 28, 2026 SC-5R-2 Godot settlement editor + draft round-trip + active-vs-archived routing + incoming-offer label alignment landed:** Closes the player-facing half of `DWL-SET-SC5R` per the May 28 repair acceptance checklist below. (1) **New Godot editor scene + script.** `godot-client/project-sovereign/scenes/settlement_editor_popup.tscn` + `scripts/settlement_editor_popup.gd` ship a dedicated EDIT-mode popup on CanvasLayer 112 (above the proposal_confirm popup at layer 110). The editor mounts when the staged `settlement_confirm` REVIEW dialogue carries `can_edit_terms=true` AND a non-empty `editor_route` (the SC-5R-1 backend contract); it reads `available_clause_types[]` + `clause_control_schema` for picker contents per spec line 554, exposes a per-clause `Remove` button, an `Add Clause` selector seeded from `available_clause_types`, an inline clause editor with structured pickers / spin boxes / toggles built from `clause_control_schema[type].fields`, a covered-enemy checkbox picker seeded from `covered_enemy_display_chips` + `uncovered_enemy_display_chips`, a `Submit for Review` button gated on non-empty clauses + non-empty covered scope, and a `Back Out` button. The follow-up repair also lets initial Open Settlement and Request Revision responses mount EDIT immediately even with an empty draft, remounts submit validation errors inline, and renders an in-editor `Generate concession baseline (Talleyrand)` control that backend-revalidates at click time with a two-step replacement guard for non-empty drafts. The editor surfaces the SC-25 vocabulary contract: action rail uses `Submit for Review`, never bare `Submit`. (2) **main.gd wiring.** `main.gd` registers the editor with `dialog_manager.register("settlement_editor", "res://scenes/settlement_editor_popup.tscn")`, connects `submit_requested` / `back_out_requested` / `concession_baseline_requested`, and intercepts the `revise_settlement_terms` action when `data["can_edit_terms"]` is true AND `data["editor_route"]` is a non-empty Dictionary: `settlement_editor_popup.show_editor(data)` self-gates the payload and the review popup is hidden only after the editor mounts. Submit pushes through `api_client.send_structured_command("propose common peace with <nation>", payload, _on_command_result)` with the editor's authored payload (`action`, `war_id`, `selected_target_nation`, `covered_enemy_participants`, `settlement_terms`, `draft_key`, `caller_kind="player_editor"`). Back Out emits a "Settlement draft kept for war <id>" status line and re-enables input; the backend's scoped store preserves the draft under its `draft_key` so the player can re-open the same war/scope and pick up where they left off. (3) **Backend draft round-trip via scoped store.** `_execute_propose_common_peace` in `backend/commands/diplomatic_executor.py` now calls `load_scoped_settlement_draft(world, war_id=..., selected_target_nation=..., covered_enemy_participants=...)` when no explicit `settlement_terms` are passed in the structured command. A non-empty restored draft becomes the staged terms and the response payload exposes `draft_restored_from_scope: True` for audit / observer routing. Explicit terms always win — when the editor or any structured caller passes `settlement_terms`, the scoped store is NOT consulted; this preserves the existing structured contract. The restore is same-turn only because `advance_turn` clears `pending_settlement_drafts_by_key` per SC-5R-1. (4) **Active-vs-archived settlement review routing.** `_on_war_settlement_clicked` in `main.gd` consults the cached active wars list before POSTing: if a previously seen `war_id` is missing from the active cache, the click routes through `_route_settlement_recovery_route({"surface": "settlement_history", "war_id": war_id})` instead of POSTing a stale `propose_common_peace`; unknown war ids still fall through to the backend as active by design. Active wars continue to POST the live review surface. The archive check runs BEFORE the POST so a stale click does not waste a backend round trip. (5) **Incoming-offer action label/copy alignment.** `build_incoming_settlement_offer_popup` in `backend/game_logic/settlement_preview.py` now labels `accept_settlement_offer` as `Review Settlement Offer` with description "Open the offered terms for ratification review. Ratification still requires a final confirm." (was `Accept Settlement` / "Ratify the offered package." — both overclaimed the handler's behavior, which stages a fresh `settlement_confirm` REVIEW rather than immediately ratifying). `reject_settlement_offer` is now labelled `Reject Offer`. `request_settlement_revision` keeps its existing label / description. All three actions remain available and route through the same handler — the rewrite is copy-only. New test file [tests/test_settlement_sc5r2_godot_editor.py](tests/test_settlement_sc5r2_godot_editor.py) (40 tests across six classes) pins all five surfaces: 14 Godot editor scene + script presence / structural tests (script + scene exist, `show_editor(data)` entry point, submit/back_out signals, `Submit for Review` and `Back Out` labels, `clause_control_schema` + `available_clause_types` + `editor_route` consumption, `caller_kind=player_editor` POST, layer 112, required scene nodes), 8 main.gd wiring tests (`dialog_manager.register("settlement_editor", ...)`, signal connections, backend-revalidated concession-baseline button handler, `revise_settlement_terms` intercept on `can_edit_terms=true` + `editor_route`, `send_structured_command` Submit handler, draft-preserving Back Out handler), 5 draft round-trip tests (scoped restore on empty-terms reopen, explicit-terms supremacy, empty-store no-restore, different-target non-collision), 3 active-vs-archived routing tests (seen-war archive detection helper, archive→settlement_history routing, active→`propose_common_peace` POST, archive check precedes POST), 6 incoming-offer label/editor-mount tests (Review label replaces Accept, Reject Offer replaces Reject Settlement, description promises review not ratification, Request Revision preserved, all three actions remain available), and 4 editor mount gate tests (`can_edit_terms=true` AND `editor_route` non-empty required, `show_editor(data)` self-gates against invalid payloads, settlement review render unchanged when editor not mounted, payload exposes can_edit_terms + editor_route surface + draft_key + staged_settlement_terms). `tools/godot_parse_report.json` was regenerated with Godot 4.4.1 and now includes `res://scripts/settlement_editor_popup.gd` with `parse_ok=true` and `load_ok=true`. Verification: focused SC-5R-2 bundle `40 passed`; adjacent settlement/incoming/recovery bundle (`tests/test_settlement_sc5r1_backend_contract.py` + `tests/test_incoming_offer_deferral_no_leaks.py` + `tests/test_settlement_incoming_offers.py` + `tests/test_settlement_continuity_slice.py` + `tests/test_settlement_ui_slice_f_behavior.py` + `tests/test_settlement_pair_substitute_ctas.py` + `tests/test_godot_parse_harness.py`) `175 passed`; **full deterministic suite `10250 passed, 1 skipped` (was `10210 passed`; +40 net = 40 SC-5R-2 tests after follow-up repair)**; full default-random suite also `10250 passed, 1 skipped`; `ruff check backend/ tests/` clean. `DWL-SET-SC5R` flips from `LANDING_IN_PROGRESS (Slice 1 LANDED, Slice 2 NEXT)` to **`LANDED`**; the player-quality gate is **GO pending Gate 4 manual smoke evidence** (including rejected/losing concession path). Slice G AI-ally settlement agency unblocks after Gate 4 smoke.

**May 28, 2026 SC-5R-1 audit repair landed:** Fixes the Codex audit NO-GO findings on `83b88a5`. Accepted AI-origin incoming settlement offers now stage with `caller_kind="ai_system"` and therefore publish `can_edit_terms=false`, empty editor clause fields, and `editor_route=None`; `request_settlement_revision` remains the player-editor route. `_execute_propose_common_peace` now writes legacy and scoped drafts only after `stage_settlement_confirm(...)` successfully stages a `settlement_confirm`, so staging failures such as `selected_target_not_covered` leave both draft stores untouched. `clause_control_schema` now emits the spec line 554 shape (`enabled`, `disabled_reason_display`, and per-field `control` / `label` / `options` / bounds / default / direction metadata) instead of required-key-only rows. Scoped draft lifecycle now mirrors the legacy store on Back Out discard, Open War Detail preservation, direct concession/surrender author presets, pair-substitute invalidation, and successful ratification. Focused SC-5R-1 tests now total `28 passed` with regressions for each audit finding. Verification: adjacent settlement/incoming/recovery bundle `69 passed`; full deterministic suite `10210 passed, 1 skipped`; full default-random suite `10210 passed, 1 skipped`; `ruff check backend/ tests/` clean. At that time, the player-quality gate remained **NO-GO** until SC-5R-2 landed the Godot editor; this is superseded by the LANDED paragraph above.

**May 28, 2026 DWL-SET-SC5R split into SC-5R-1 (backend) + SC-5R-2 (Godot editor + glue) — historical split record, superseded by the LANDED paragraph above:** To reduce review/audit risk and let each landing slice get its own Codex audit, the settlement EDIT / revise / counter-draft repair is split into two slices: **SC-5R-1** (backend EDIT payload contract + scoped `draft_key` persistence + author-handler schema closure + tampered/cut clause-type revalidation) — LANDED below; **SC-5R-2** (Godot EDIT-mode popup with clause add/edit/remove controls + covered-enemy picker + Submit for Review button + active-vs-archived routing + incoming-offer label/copy alignment) — NEXT. At split time, the player-quality gate remained blocked until SC-5R-2 landed; this historical note is superseded by the LANDED paragraph above.

**May 28, 2026 SC-5R-1 backend EDIT payload contract + sub-bug closure landed:** Closes the backend half of `DWL-SET-SC5R` per the May 28 spec correction at lines 8-12. (1) **EDIT payload contract on `settlement_confirm`** per spec line 543-556. Every staged `settlement_confirm` dialogue now publishes `dialogue_mode="REVIEW"`, `can_edit_terms: bool`, `available_clause_types: List[str]`, `clause_control_schema: Dict[str, ClauseControlSchema]`, and `editor_route: Dict | None`. `can_edit_terms` is true if and only if `caller_kind == "player_editor"` AND the staged dialogue resolves to an active `war_instance` (`ended_turn is None`) AND the dialogue is not a white-peace ratification path AND `available_clause_types[]` is non-empty; empty player-editor drafts now mount EDIT with Submit disabled per the SC-5R-2 follow-up. AI/system/debug/legacy-typed staging set `can_edit_terms=false`, leave `editor_route=None`, and publish empty `available_clause_types[]` / `clause_control_schema` so SC-5R-2 cannot advertise an editor handoff for non-editor staging. When `can_edit_terms=true`, top-level `available_clause_types[]` and `editor_route.available_clause_types` are present and equal per spec line 548 required test. `editor_route` shape is exact: `{surface: "settlement_editor", war_id, selected_target_nation, covered_enemy_participants, draft_key, available_clause_types, staged_settlement_terms, source_route_id, source}` with `source ∈ {"rejected_review", "stale_recovery", "explicit_revise"}`. Hidden / cut clause types (`voluntary_alliance` and any other type with `CLAUSE_CONTROL_SCHEMA[type].enabled=False`) are absent from both fields so clients cannot synthesize disabled rows. (2) **Scoped `draft_key` persistence.** New `world.pending_settlement_drafts_by_key: Dict[str, List[Dict]]` field stores authored drafts under the canonical `compute_settlement_draft_key(war_id, selected_target_nation, covered_enemy_participants)` key so same-war drafts with different selected targets or covered scope no longer collide on the legacy `pending_settlement_drafts[war_id]` slot. New public helpers `save_scoped_settlement_draft(world, *, war_id, selected_target_nation, covered_enemy_participants, settlement_terms) -> str`, `load_scoped_settlement_draft(...) -> Optional[List[Dict]]`, and `discard_scoped_settlement_draft(...) -> bool`. Dual-write into the scoped store happens at every staging site that writes the legacy store today: `_execute_propose_common_peace` (player-editor structured path), `stage_settlement_confirm` same-war merge / merge-conflict path, `_stage_replacement_settlement_terms` (author handlers), and `_apply_scope_replace_confirm` (G2-Slice-G2e same-war scope chooser). Legacy `pending_settlement_drafts[war_id]` storage stays for backward compatibility within SC-5R-1; SC-5R-2 will route the Godot editor through the scoped store and may decommission the legacy slot. (3) **Save/load round-trip.** `WorldState.to_dict` / `from_dict` serialize `pending_settlement_drafts_by_key` with deep-copied clauses; old saves without the key load with an empty scoped dict (no migration crash). End-turn discard in `advance_turn` clears the scoped store alongside the legacy store. (4) **`author_gold_indemnity_terms` schema fix.** The author handler at `settlement_preview.py::handle_settlement_dialogue_action` previously wrote `{"type": "gold_indemnity", "from": ..., "to": ..., "amount": 200, "turns": 0}` which `validate_settlement_terms` rejects as `invalid_clause_schema` (`unknown_keys=["turns"]`) at Submit / ratify time per `CANONICAL_CLAUSE_TYPES["gold_indemnity"]={"required": {"type", "from", "to", "amount"}, "optional": set()}`. The bogus `turns: 0` is removed; `gold_per_turn` remains the canonical clause type for recurring obligations. (5) **Tampered / cut clause-type revalidation.** Pre-staging revalidation is wired in `_stage_replacement_settlement_terms` through `validate_settlement_terms(world=world, war_instance=war_instance)`, so any author handler that constructs a cut/tampered clause type (e.g. a clause `type` not in `CANONICAL_CLAUSE_TYPES`) returns `error="submitted_terms_failed_revalidation"` and does not stage. Pre-ratification clause-type revalidation is added in `ratify_settlement_confirm` ahead of `calculate_common_peace_acceptance` and the per-pair plan: a narrow guard checks each clause `type ∈ CANONICAL_CLAUSE_TYPES ∪ RATIFY_LEGACY_APPLY_CLAUSE_TYPES` (the legacy allowlist includes `gold_lump` which `_apply_settlement_terms` still tolerates as a pre-cleanup alias of `gold_indemnity`); the cut/tampered clause type returns `error="submitted_terms_failed_revalidation"` with `validation_error="invalid_clause_type"` and pops the dialogue so the player is not left on a tampered staged review. White peace ratifies an empty package by design and skips the non-empty guard. The pre-ratify guard intentionally does NOT run the full strict per-key schema (which would break legacy fixtures using `regions` plural / `gold_lump` aliases) — strict schema validation is owned by the submit-time `validate_settlement_terms` call. Together these two guards close the audit checklist requirement that cut clause types like the D3 CUT type "must fail before staging and before ratification instead of reaching treaty history." New test file [tests/test_settlement_sc5r1_backend_contract.py](tests/test_settlement_sc5r1_backend_contract.py) (22 tests across four classes) pins all five surfaces: 9 EDIT payload contract tests including the spec-named `test_can_edit_terms_true_iff_player_editor_and_clause_types_available_and_war_active`, `test_settlement_review_payload_with_editor_route_carries_available_clause_types_or_editor_route_subschema`, and `test_hidden_clause_types_do_not_leak_as_visible_or_disabled_labels`; 8 scoped draft persistence tests covering save/load round-trip, same-war different-scope non-collision, end-turn discard, structured executor dual-write, and old-save backward compat; 2 `author_gold_indemnity_terms` schema validity regressions; and 3 tampered / cut clause-type revalidation tests covering pre-stage executor rejection, pre-ratify defense-in-depth rejection with dialogue pop, and author-handler rejection without drafts-store write or dialogue replacement. No Godot edits, no spec edits this slice. SC-5R-2 will land the editor popup, end-to-end edit→preview→submit loop, active-vs-archived routing, and incoming-offer label/copy alignment. Verification: focused SC-5R-1 bundle `22 passed`; previously failing-now-green bundle (`tests/test_common_peace_c2_ratification.py` + `tests/test_settlement_agency_cuts_g2c_g2f.py` + SC-5R-1) `48 passed`; **full deterministic suite `10204 passed, 1 skipped` (was `10182 passed`; +22 net = 22 new SC-5R-1 tests)**; full default-random suite also `10204 passed, 1 skipped`; `ruff check backend/ tests/` clean. DWL-SET-SC5R status flips from `LANDING_IN_PROGRESS` to `LANDING_IN_PROGRESS (Slice 1 LANDED, Slice 2 NEXT)`; the player-quality gate remained blocked until SC-5R-2 landed the Godot editor; this historical note is superseded by the LANDED paragraph above.

**May 28, 2026 all-completed-audit-punch-list repair landed:** Fixes the combined Tier 1-4 + G2a/G2e/G2d/G2b/G2c/G2f audit findings. Persistent settlement mailbox items now survive generic stale-dialogue clearing (`incoming_settlement_offer` and `ally_settlement_petition` remain until player resolution). D7 now has executable reject-offer coverage: rejecting an incoming settlement offer whose package omits an allied active claim queues the shipped `warn_against_sellout` petition, while the v0.32 spec no longer promises unshipped ratification-time or battle-loss petition producers. STATUS `DWL-SET-SC32` is now `LANDED`; the parent SC-30 spec row records incoming-offer UI as landed and Wait/Ask as terminally removed; the forced-alliance `continental_toggle_differential` spec shape uses `balance_post_share_pct: int`; and the Godot parse/load report was regenerated with Godot 4.4.1 after the G2b/G2e `.gd` edits. Repair verification: `ruff check backend tests` clean, combined audit bundle `153 passed`, and full Python suite `10182 passed, 1 skipped`.

**Current gate correction (May 28, 2026; superseded by SC-5R landing and follow-up repair above):** `docs/SETTLEMENT_UI_CLEANUP_SPEC.md` v0.32 was blocked for player release until the settlement EDIT / revise / counter-draft repair landed. The May 17 SC-5 / SC-30 UI promotion and later May 28 GO note overclaimed `Request Revision` as a "real counter editor": the shipped path removes the incoming offer and stages a seeded `settlement_confirm` review with `caller_kind="player_editor"`, but it does not expose Godot clause controls, `available_clause_types[]`, `clause_control_schema`, `can_edit_terms`, `Submit for Review`, scoped draft preservation, or an executable edit -> preview -> submit loop. Root cause: implementation and tests treated backend staging/provenance (`counter_to_offer_id`, seeded `settlement_terms`, popup routing) as equivalent to an editor, while the spec requires a user-visible EDIT surface with POST preview, mutation, revalidation, and `draft_key` persistence. SC-32 / Slice G2 cut/ship decisions remain landed as narrow agency decisions, but the broader settlement player-quality gate was reopened under `DWL-SET-SC5R`; this is now superseded by the landed SC-5R repair above.

**May 28, 2026 revise/counter editor audit correction recorded:** Focused audit of `request_settlement_revision` produced `dialogue_type=settlement_confirm`, `caller_kind=player_editor`, seeded `settlement_terms`, and options `Ratify Settlement` / `Open War Detail` / `Back Out`, with no `can_edit_terms`, no `available_clause_types`, no `clause_control_schema`, no `Submit for Review`, and no draft write. This confirms the missed implementation step is the real EDIT-mode counter-draft layer, not merely a bad label. Required repair: add the Godot settlement editor, wire clause add/edit/remove and covered-enemy scope to POST settlement preview, submit through `_execute_propose_common_peace` with final revalidation, preserve drafts by scoped `draft_key`, validate before staging/ratification, and keep or recover the original incoming offer if the revision editor cannot mount.

**Repair acceptance checklist:** The coding pass that closes `DWL-SET-SC5R` must also close the concrete audit bugs found with the missing editor: `author_gold_indemnity_terms` must author schema-valid `gold_indemnity` terms, tampered/cut terms such as `voluntary_alliance` must fail before staging and before ratification instead of reaching treaty history, War Detail / Back Out / reopen must restore scoped non-empty drafts from `pending_settlement_drafts[draft_key]` rather than inert `war_id` storage, Godot result/notification routing must honor active-vs-archived settlement review targets so partial settlements route back to the live war context instead of always opening the ledger, and incoming-offer action labels/copy must match behavior (reviewing an offer vs immediately ratifying it).

**May 28, 2026 audit punch list Tier 5 G2-Slice-G2c AI counterproposal CUT and G2-Slice-G2f voluntary alignment CUT landed:** Ships the final two SC-32 sub-ledger cut-evidence gates without product code changes. G2c pins D1: player settlement rejection does not mint an AI settlement counterproposal, SC-5 / SC-30 `Request Revision` remains the only counter-flow for AI-originated settlement offers, and no `counterproposal_*` constants / `settlement_counterproposal_*` Voice Bible families / popup or mailbox payloads exist in backend or Godot. Existing general diplomacy `counter_offer` mechanics are intentionally untouched because they are not settlement counterproposals. G2f pins D3: `voluntary_alliance` is absent from `CANONICAL_CLAUSE_TYPES`, `SETTLEMENT_MVP_CLAUSE_TYPES`, `SETTLEMENT_DEPENDENCY_CLAUSE_TYPES`, `SETTLEMENT_RECURRING_GOLD_CLAUSE_TYPES`, `SETTLEMENT_LIVE_CLAUSE_TYPES`, and `CLAUSE_CONTROL_SCHEMA`; `forced_alliance` remains the only settlement-editor alliance authoring clause; tampered `voluntary_alliance` terms fail validation as `invalid_clause_type`; and no voluntary-alignment validator / ratification / Voice Bible path exists. New test file [tests/test_settlement_agency_cuts_g2c_g2f.py](tests/test_settlement_agency_cuts_g2c_g2f.py) (6 tests) includes the G2c spec-named tests `test_settlement_rejection_does_not_produce_ai_counterproposal`, `test_request_revision_remains_only_counter_flow_for_incoming_offers`, and `test_no_counterproposal_constants_or_voice_families_in_code`, plus the G2f spec-named tests `test_voluntary_alliance_clause_type_absent_from_schema`, `test_settlement_editor_remains_forced_alliance_only_for_alliance_authoring`, and the supporting guard `test_voluntary_alliance_no_validator_or_voice_path`. Verification: focused cut bundle `6 passed`; cut/ledger/incoming-offer bundle `43 passed`; adjacent settlement schema/Godot bundle `107 passed`; full suite `10180 passed, 1 skipped`; `ruff check backend tests` clean. G2-Slice-G2c and G2-Slice-G2f remain closed; SC-32 / Slice G2 is complete and covered by the all-completed-audit-punch-list repair.

**May 28, 2026 audit punch list Tier 5 G2-Slice-G2b ally petition substrate landed:** Ships the SC-32 sub-ledger D4a/D4b ally petition substrate and D7 solicited trigger model. Backend changes add first-class `ally_settlement_petition` mailbox dialogues, `request_open_settlement` emission when the player opens a settlement that leaves an allied claim in another active war outside the table, and `warn_against_sellout` emission when a staged settlement omits an allied active claim in the covered scope. Petitions are advisory only: they are persistent mailbox items, appear inline on the staged `settlement_confirm`, emit a notification review target for Envoys, and never enter `HARD_STOP_TYPES` or block ratification. The dialogue endpoint now acknowledges petitions through `acknowledge_ally_settlement_petition`; `/pending_envoy` and `/mailbox/activate` return proposal-confirm popup payloads for the new type. Godot whitelists the dialogue/action, renders a dedicated petition popup, and renders inline allied-petition voice on settlement reviews. Voice templates add named petition families for Castlereagh, Hardenberg, Metternich, Einsiedel, chancery fallback, and Talleyrand advisory framing. New test file [tests/test_ally_settlement_petitions_g2b.py](tests/test_ally_settlement_petitions_g2b.py) (13 tests) covers all G2b spec-named behavior tests after the all-completed-audit-punch-list repair: `test_ally_petition_request_open_settlement_fires_on_solicited_trigger`, `test_ally_petition_request_open_settlement_mailbox_payload_routes_through_popup`, `test_ally_petition_request_open_settlement_does_not_block_player_ratification`, `test_ally_petition_warn_against_sellout_fires_on_settlement_excluding_ally`, `test_ally_petition_warn_against_sellout_fires_on_rejected_incoming_offer`, `test_ally_petition_warn_against_sellout_does_not_block_ratification`, `test_ally_petition_warn_against_sellout_renders_named_ally_voice_not_generic_chancery`, `test_ally_petition_request_consultation_removed_from_player_facing_scope`, `test_ally_petition_request_redress_after_settlement_removed_from_player_facing_scope`, `test_ally_petitions_fire_only_in_response_to_named_player_actions`, `test_ally_petitions_do_not_fire_on_pure_cooldown_without_player_trigger`, `test_petition_request_reward_or_restoration_absent_until_slice_h_lands`, and `test_petition_demand_bargain_honor_absent_until_slice_h_lands`. `tests/test_godot_parse_harness.py` now treats the acknowledgement action as a third exact backend/Godot dispatch family alongside settlement-confirm and incoming-offer actions. Original G2b verification: focused G2b bundle `12 passed`; Godot/source incoming-offer harness `41 passed`; nearby settlement routing bundles `71 passed` and `45 passed`; full suite `10174 passed, 1 skipped`; `ruff check backend tests` clean. Repair verification adds reject-offer petition coverage in the combined audit bundle `153 passed` and full suite `10182 passed, 1 skipped`. G2-Slice-G2b remains closed; G2c/G2f have since landed.

**May 28, 2026 audit punch list Tier 5 G2-Slice-G2d Wait-for-Enemy-Offer + Ask-for-Terms CUT landed:** Ships the SC-32 sub-ledger D2 cut. Backend changes remove the retired `wait_for_enemy_offer_visible` / `ask_for_terms_visible` keys from incoming-settlement-offer popup payloads and keep `settlement_confirm` payloads free of both orphan flags. `backend/display_names.py` removes the unused `wait_for_enemy_offer_unavailable` row; `request_terms_ineligible` remains the humanized defensive code for any stale request-terms path. `tests/test_incoming_offer_deferral_no_leaks.py` is now 31 tests, adding the three D2 spec-named cut-evidence tests: `test_wait_for_enemy_offer_visible_flag_removed_from_payload_v032`, `test_ask_for_terms_visible_flag_removed_from_payload_v032`, and `test_wait_for_enemy_offer_unavailable_display_string_removed`. Existing hidden-control regression tests now assert absence rather than `_visible=False`. STATUS DWL-SET-SC30 records removal as terminal, with no future SC-30b/c lifecycle promised. Verification: focused incoming-offer suite `31 passed`; Godot parse/whitelist + agency ledger harness `16 passed`; full deterministic suite `10162 passed, 1 skipped`; full default-random suite `10162 passed, 1 skipped`; `ruff check backend/ tests/` clean. G2-Slice-G2d remains closed; G2-Slice-G2b has since landed and is repair-verified.

**May 27, 2026 audit punch list Tier 5 G2-Slice-G2e same-war replace-confirm chooser landed:** Ships the SC-32 sub-ledger D6 `settlement_scope_replace_confirm` path. Same-war same-scope restaging still merges compatible terms through SC-26; same-war different-scope restaging now opens a dedicated chooser instead of merging or rejecting. `Replace current draft` clears the prior scoped `draft_key` and stages the new covered-enemy scope; `Keep current draft` restores the prior `settlement_confirm` popup unchanged; outer cancel is treated as keep; click-time revalidation handles a scope flip by restoring the current draft as a no-op. Backend changes add the canonical `compute_settlement_draft_key(...)`, publish `draft_key` on staged settlement dialogues, add `settlement_scope_replace_confirm` to the settlement-family dialogue set, and dispatch the two chooser actions through `handle_settlement_dialogue_action`. Godot changes add the dialogue type to `PROPOSAL_CONFIRM_DIALOGUE_TYPES`, add the two action ids to `SETTLEMENT_DIALOGUE_ACTIONS`, and render a `proposal_confirm_popup.gd` variant with current/new scope copy. Voice Bible adds `settlement_scope_replace_confirm_talleyrand`. New test file [tests/test_settlement_scope_replace_confirm.py](tests/test_settlement_scope_replace_confirm.py) (6 tests) includes the five D6 spec-named behavior tests plus a voice/Godot wiring pin. Existing settlement-family set pins were updated to include the new chooser type. `tools/godot_parse_report.json` timestamp/note refreshed for the additive `.gd` edits; the headless Godot parser was not rerun because Godot is not on PATH. Verification: focused G2e bundle `6 passed`; nearby continuity/foundation/recovery bundle `79 passed`; Godot parse/whitelist harness `10 passed`; full deterministic suite `10159 passed, 1 skipped, 0 failed` (was `10153 passed`; +6 net = 6 new G2e tests); full default-random suite also `10159 passed, 1 skipped`; `ruff check backend/ tests/` clean. G2-Slice-G2e remains closed; G2-Slice-G2d, G2-Slice-G2b, G2-Slice-G2c, and G2-Slice-G2f have since landed.

**May 27, 2026 audit punch list Tier 5 G2-Slice-G2a doc-only cut/keep enforcement pass landed:** Authors the spec-named umbrella gate test `test_settlement_agency_landing_ledger_has_no_unowned_backlog_controls` plus 5 supporting structural pins for the v0.32 ledger contract. New test file [tests/test_settlement_agency_landing_ledger.py](tests/test_settlement_agency_landing_ledger.py) (6 tests). (1) **`test_settlement_agency_landing_ledger_has_no_unowned_backlog_controls`** — the umbrella gate. Parses the parent Deferred Work Landing Ledger at `SETTLEMENT_UI_CLEANUP_SPEC.md:46-54` and the SC-32 / Slice G2 Sub-Slice Ledger at `SETTLEMENT_UI_CLEANUP_SPEC.md:56-70` as markdown tables (stdlib `re` + `pathlib`, no third-party parser) and asserts every data row in both tables declares (a) an owning row / decision, (b) a landing slice, (c) a completion definition, and (d) at least one spec-named `test_<snake_case>` behavior test. Per CLAUDE.md golden rule 9 every hidden, cut, deferred, later, v2, or polish player-facing affordance must name a concrete owner row, landing slice, completion definition, STATUS tracking line, and behavior test; this test is the doc-only enforcement floor for that contract. It does NOT require the referenced behavior tests to exist on disk (many are authored by later G2 sub-slices) — it only enforces ledger well-formedness so future spec edits cannot silently strand a deferred row. (2) **`test_parent_dwl_ledger_pins_seven_rows_in_v032`** — pins the parent DWL data-row count at exactly 7 (SC-29, SC-30, SC-31 dependency, SC-31 surrender, SC-33, SC-32 parent, Slice H deferred petitions). A future addition or removal must update this pin alongside the spec edit so reviewers and the umbrella gate stay synchronized. (3) **`test_sc32_sub_ledger_covers_all_seven_locked_decisions`** — asserts the sub-ledger enumerates decision ids `("D1", "D2", "D3", "D4a", "D4b", "D4c", "D5", "D6", "D7")` in that exact order. D4 expands into D4a (request_open_settlement SHIP), D4b (warn_against_sellout SHIP), and D4c (request_consultation + request_redress_after_settlement CUT). No SC-32 product decision can be silently dropped. (4) **`test_sub_ledger_d5_row_pins_umbrella_gate_test_self_reference`** — pins the spec line 68 self-reference where D5 (Conference / veto CUT) is owned by the umbrella gate test itself, so a future refactor cannot strand D5 without a behavior test of its own. (5) **`test_dwl_dip_conference_remains_superseded_in_status`** — confirms the `DWL-DIP-CONFERENCE` STATUS row remains marked `SUPERSEDED`. If a future Congress System ships, it must land as its own spec with its own DWL row, never by reactivating the superseded row. (6) **`test_deferred_petition_types_have_slice_h_landing_row`** — pins the Tier 4 new DWL row at `SETTLEMENT_UI_CLEANUP_SPEC.md:54` naming both deferred ally petition types (`request_reward_or_restoration` + `demand_bargain_honor`), Slice H as the landing slice, and the two absence-guard tests (`test_petition_request_reward_or_restoration_absent_until_slice_h_lands`, `test_petition_demand_bargain_honor_absent_until_slice_h_lands`) authored by G2-Slice-G2b. G2-Slice-G2a is doc-only enforcement; no backend, Godot, helper, validator, contract, or constant changed. Test budget: spec line 1281 allows 4-6 tests for G2a (gate test plus 3-5 supporting structural pins); landed at exactly 6 (the gate test plus 5 supporting pins), the upper bound. Verification: focused new bundle `tests/test_settlement_agency_landing_ledger.py` `6 passed`; full deterministic suite `10153 passed, 1 skipped, 0 failed` (was `10147 passed`; +6 net = 6 new G2a tests); full default-random suite also `10153 passed, 1 skipped`; `ruff check backend/ tests/` clean. G2-Slice-G2a closed; G2-Slice-G2e has since landed in the closure paragraph above.

**May 26, 2026 audit punch list Tier 4 v0.32 spec amendment landed:** Pure documentation-only sweep folding the 7 SC-32 / Slice G2 product decisions from `docs/SC32_SLICE_G2_DECISIONS.md` into `docs/SETTLEMENT_UI_CLEANUP_SPEC.md`. (1) **Spec masthead bumped v0.31 → v0.32** at `SETTLEMENT_UI_CLEANUP_SPEC.md:5-8` with Tier 4 fold-in note enumerating the 5 CUT outcomes (D1 AI counterproposals, D2 Wait-for-Enemy-Offer + Ask-for-Terms, D3 voluntary alignment, D5 conference / veto, D4c request_consultation + request_redress_after_settlement ally petitions), 1 partial SHIP (D4a + D4b ally petitions ship `request_open_settlement` + `warn_against_sellout` only via G2-Slice-G2b; defer `request_reward_or_restoration` + `demand_bargain_honor` to Slice H), 1 full SHIP (D6 same-war replace-confirm chooser via G2-Slice-G2e), and 1 design-locked solicited petition trigger model (D7). v0.31 lineage retained as canonical history. Tier 4 is doc-only; no code or test changes land in this amendment. (2) **SC-32 parent DWL row** at spec line 53 expanded to reference the per-decision sub-ledger immediately below the parent table; legacy "broader settlement-agency systems" wording replaced with explicit cut/ship outcomes. (3) **New DWL row for `request_reward_or_restoration` + `demand_bargain_honor`** added at spec line 54 naming Slice H as the landing slice, with named absence tests (`test_petition_request_reward_or_restoration_absent_until_slice_h_lands`, `test_petition_demand_bargain_honor_absent_until_slice_h_lands`) per golden rule 9. (4) **New SC-32 sub-ledger section** "SC-32 / Slice G2 Sub-Slice Ledger (v0.32)" inserted between the parent DWL table and the Scope section, mapping decisions D1-D7 onto landing sub-slices (G2c, G2d, G2f, G2b, G2a, G2e, G2b) with cut/ship outcomes, completion definitions, and 22 spec-named behavior tests across the sub-slices. (5) **Spec line 266** (forced_alliance vocabulary row) amended to record voluntary alignment as CUT per v0.32 SC-32 / Slice G2-Slice-G2f, removing the legacy "any future voluntary alignment offer needs its own product decision" open-ended clause. (6) **Spec line 465** (same-war scope collision contract) amended to point at G2-Slice-G2e as the SHIP landing for the `settlement_scope_replace_confirm` chooser, naming the two options (`Replace current draft` / `Keep current draft`), outer-cancel-equals-keep default, and `settlement_scope_replace_confirm_talleyrand` voice family. (7) **SC-32 row at spec line 667** updated to reference the v0.32 sub-ledger for per-decision contracts. (8) **Slice G2 section at spec lines 1248-1261** expanded into 6 ordered sub-slices (G2a → G2e → G2d → G2b → G2c → G2f) with per-sub-slice scope, dependencies, and test budget (~22-30 new tests total, under the 30-42 spec line 839 budget). (9) **Completion Criteria** appended 4 new entries pinning the v0.32 SC-32 outcomes: sub-ledger honored with the umbrella gate test passing; no `voluntary_alliance` clause type in any schema; no `counterproposal_*` constants / dialogue types / voice families; `wait_for_enemy_offer_visible` / `ask_for_terms_visible` flags absent after G2-Slice-G2d; same-war different-scope restaging surfaces the chooser after G2-Slice-G2e. (10) **Test-name spelling reconciled** — `test_settlement_agency_landing_ledger_has_no_unowned_future_controls` (legacy) was already absent from the repo before this amendment; the canonical spelling `test_settlement_agency_landing_ledger_has_no_unowned_backlog_controls` is now the only form referenced in the spec. No code, helper, validator, contract, or test changed; full deterministic suite **`10147 passed, 1 skipped, 0 failed`** (unchanged from Tier 3); `ruff check backend/ tests/` clean. Tier 4 remains closed; Tier 5 G2-Slice-G2a has since landed.

**May 25, 2026 audit punch list Tier 3 code polish landed:** Closes the May 24, 2026 audit `docs/audits/SETTLEMENT_CLEANUP_AUDIT_2026_05_24.md` §C P1+P2 code findings plus the §C / §D P3 polish queued into the same tier per golden rule 9. Four code fixes and three polish items: (1) **P1 Rule 2 — `balance_post_share` float leak** in `backend/game_logic/settlement_scoring.py::compute_forced_alliance_continental_toggle_differential` line 1843 replaced by `balance_post_share_pct` integer-percent field (`int(round(post_share * 100))`). Docstring contract at line 1810 updated to declare the integer field; legacy float field is gone from runtime payloads; tests keep absence assertions and historical audit docs still mention the old name. Godot can now read the field raw without crashing. (2) **P2 Rule 8 — per-region scan in `_concession_baseline_select_transferable_region`** in `backend/game_logic/settlement_preview.py` line 1269 replaced with a `world.get_nation_regions(participant)` union per `proposer_set` (cached lookup), mirroring the existing pattern at `settlement_scoring.py` line 1589. A defensive fallback covers test shims that lack the cached helper; the optimization is behaviour-preserving (selects the same region as the legacy full scan). (3) **P2 UX — surrender double-banner merged** at `godot-client/project-sovereign/scripts/proposal_confirm_popup.gd` lines 364-385. The previous double cluster (Talleyrand "surrender draft" reasoning banner + "Surrender draft — dependency consequence applied at ratification" outcome banner) collapsed into a single labeled block: when `surrender_preset=true` (staged) the outcome banner includes the reasoning inline; when only `surrender_preset_visible=true` (editor-side affordance) the Talleyrand reasoning banner renders alone. The popup no longer advertises the same draft twice. (4) **P2 UX — non-gold clauses in `build_incoming_settlement_offer_popup`** at `settlement_preview.py` lines 5421-5462 now render structured copy for `region_transfer` (region + parties), `forced_alliance` (parties + optional "incl. Continental System" suffix), `vassalage` / `subjugation` / `liberation` (via the canonical `_term_display` helper from `settlement_presentation.py`), and `gold_per_turn` (amount + turns + parties). `peace` and `gold_indemnity` keep their existing formats. Region cessions and forced alliance clauses no longer collapse to the bare `ttype.replace("_", " ").title()` fallback. (5) **P3 polish — "Top pressure" line suppression on comfortable accept** at `proposal_confirm_popup.gd` lines 294-313. When the acceptance band reads `accept` / `acceptable` AND `total - threshold >= 10` (a "comfortable" margin), the Top pressure line is suppressed so the popup does not read as conflicting with its own verdict. Near-threshold accepts still show the line (the swing factor matters). (6) **P3 polish — settlement_helpers.py docstring trim** at `backend/game_logic/settlement_helpers.py` lines 1-65: 64-line implementation history collapsed to a 6-line module purpose. The Slice A1 / A2 / A3 / B3 narrative belongs in STATUS, not as a perpetual file lead-in. (7) **P3 polish — public `DialogueManager.iter_queue()`** added in `backend/models/dialogue_manager.py` returning `list(self._queue)`. `settlement_preview.py::_settlement_dialogue_active` line 354 and `_is_offer_known_to_dialogue_manager` line 5326 now route through the public method instead of `getattr(getattr(..., "_queue", []), ...)` / direct `_queue` reads, so the dialogue-manager public API is the only allowed seam from settlement_preview. (`ai_diplomacy.py:306` still reaches into `_queue` directly; left alone as out of Tier 3 scope per audit §C.3 wording.) New test bundle `tests/test_settlement_tier3_code_polish.py` (15 tests) pins all six surfaces — 3 balance_post_share_pct tests (int type + bool subtype guard, legacy float field absence, threat-delta surcharge regression), 2 concession-baseline tests (spy on `get_nation_regions` called per participant; behaviour-equivalence between live helper and defensive fallback), 6 incoming-offer rendering tests (region_transfer / forced_alliance with CS toggle on / off / gold_per_turn / vassalage+subjugation+liberation / gold_indemnity regression), 1 Godot surrender single-banner source guard, 1 Godot Top-pressure suppression source guard, 2 dialogue-manager iter_queue-routing behaviour tests for the two settlement_preview consumers. Three new `TestDialogueManagerIterQueue` tests in `tests/test_dialogue_manager.py` pin the new public method (empty / queued ordering / copy-not-internal-list). Updated `tests/test_settlement_forced_alliance_continental_toggle.py` assertion from the legacy `balance_post_share` float check to the new `balance_post_share_pct` int contract + explicit absence guard for the legacy field. Verification: focused Tier 3 + adjacent bundle (`tests/test_settlement_tier3_code_polish.py` + `tests/test_dialogue_manager.py`) `69 passed`; broader settlement / Godot harness (`tests/test_settlement_forced_alliance_continental_toggle.py` + tier2 + incoming_offer + concession_baseline + godot_parse + presentation) `151 passed`; **full deterministic suite `10147 passed, 1 skipped, 0 failed`** (was `10129 passed`; +18 net = 15 new tier3 tests + 3 new iter_queue tests); full default-random suite also `10147 passed, 1 skipped`. `ruff check backend/ tests/` clean. Tier 3 remains closed; Tier 4 has since landed.

**May 25, 2026 audit punch list Tier 2 P1 voice/UX wiring landed:** Closes all 7 §16.1 voice surfaces + 2 Godot UX render fixes from the May 24, 2026 audit `docs/audits/SETTLEMENT_CLEANUP_AUDIT_2026_05_24.md` §D. (1) Four missing Voice Bible §16.1 templates authored in `backend/game_logic/diplomatic_templates.py::SETTLEMENT_VOICE_TEMPLATES`: `settlement_white_peace_heading_talleyrand` (slot `{war_label}`), `settlement_white_peace_blocked_talleyrand` (slots `{war_label}`, `{top_blocker}`), `settlement_concession_authored_talleyrand` (slots `{summary}`, `{accepting_leader}`), `settlement_losing_side_pressure_explained_talleyrand` (slots `{war_label}`, `{top_pressure_label}`, `{accepting_leader}`); all four resolve to Sire-form Talleyrand register per Voice Bible §16.1. (2) Two existing-but-unrouted templates now route through `resolve_settlement_voice_line` at their `_compute_*_preset` call sites instead of inline f-strings: `settlement_surrender_preset_authored_talleyrand` at `_compute_surrender_preset` (subjugation/vassalage discriminator + war_label + leader slots; "Sire, the surrender draft for ..." opening replaces legacy "Talleyrand's draft:" f-string) and `settlement_recurring_gold_authored_talleyrand` at `_compute_recurring_gold_preset` (payer + amount_per_turn + recipient + turns + projected_total slots; "Sire, the draft commits ..." opening with projected_total framing that the f-string omitted). (3) `_format_concession_reasoning` for the concession-baseline reasoning now routes through `settlement_concession_authored_talleyrand` while keeping the existing parts-construction logic intact. (4) `build_settlement_confirm_dialogue` publishes a new `losing_side_pressure_voice` field on the staged dialogue payload, resolved through `settlement_losing_side_pressure_explained_talleyrand` whenever `losing_for_concession_baseline=True` (empty string otherwise) so the popup can render a Talleyrand reading of the top blocker. (5) `proposal_confirm_popup.gd::_build_settlement_content` now reads `terminal_recovery_copy` from the staged-dialogue payload and renders it (dim grey italic) directly after the `ratify_blocked_reason` block, closing the audit gap where the backend already computed the field via `settlement_no_alternative_route_chancery` but Godot never read it. (6) `diplomacy_wizard.gd::_add_action_button` now renders an in-wizard per-war picker when an action carries `error="multi_war_ambiguity"` and a non-empty `available_wars` array; each picker button forwards `_on_action_selected` with the original `action_id` and a payload clone carrying the chosen `war_id`, which `_structured_payload_for_action` then routes back to the backend through the same structured path used by `open_settlement` and `propose_white_peace`. The legacy disabled-button-with-long-tooltip pattern at lines 446-447 is preserved as an informational anchor and the picker renders below it. (7) `tools/godot_parse_report.json` timestamp refreshed in lockstep with the two additive `.gd` edits; both additions are append-only (new branches inside existing functions, no class or signal-handler changes) so the previously recorded `parse_ok`/`load_ok=true` rows remain valid until the user re-runs the headless harness. New test bundle `tests/test_settlement_tier2_voice_ux.py` (11 tests) pins all 7 surfaces — 4 template-registration tests, 3 preset-reasoning-routes-through-template tests (surrender preset, recurring gold preset, concession baseline), 2 `losing_side_pressure_voice` dialogue-field tests (emitted when losing, empty when not), 2 Godot source-level guards (`terminal_recovery_copy` renders after `ratify_blocked_reason`; wizard picker reads the literal `multi_war_ambiguity` error code + `available_wars` payload + reuses `_on_action_selected`). Pre-existing `tests/test_settlement_concession_baseline.py::test_concession_baseline_copy_promises_improvement_not_acceptance_band` updated from the legacy "to improve acceptance" f-string assertion to the new "lift acceptance" template phrasing — semantic intent (copy promises improvement, not a guaranteed acceptance band) preserved. Verification: focused Tier 2 + adjacent settlement bundle `115 passed`; **full deterministic suite `10129 passed, 1 skipped, 0 failed`** (was `10114 passed`; +15 net = 11 new tier2 tests + 4 incremental). `ruff check backend/ tests/` clean. Tier 2 remains closed; Tier 3 has since landed.

**May 24, 2026 audit punch list Tier 1 doc-drift sweep landed:** Pure documentation-only sweep against the May 24, 2026 audit at `docs/audits/SETTLEMENT_CLEANUP_AUDIT_2026_05_24.md` §A. Closed all 11 stale doc-drift rows without behavior change: (1) 6 stale test counts in STATUS.md bumped to current `^def test_` grep totals — `tests/test_incoming_offer_deferral_no_leaks.py` 24 -> 28 (v0.27 table row, DWL-SET-SC5 row, May 17 paragraph annotated with commit-2 vs current); `tests/test_settlement_pair_substitute_ctas.py` 17 -> 19 (DWL-SET-SC29 row); `tests/test_settlement_dependency_clauses.py` 36 -> 39 (DWL-SET-SC31 row, May 14 audit-repair paragraph); `tests/test_settlement_concession_baseline.py` 10 -> 16 (v0.27 table row); `tests/test_settlement_white_peace.py` 10 -> 12 (v0.27 table row); `tests/test_settlement_recurring_gold.py` already at 27 (no change). (2) 3 stale Python line refs in the May 15 G2-Slice-1b-Repair-1 paragraph corrected — `compute_forced_alliance_continental_toggle_differential` 1693 -> 1780 in `settlement_scoring.py`, `_resolve_pair_state_transitions` 3545 -> 3487 in `settlement_preview.py`, `WorldState._ratify_treaty` 6537 -> 5974 in `world_state.py`. (3) Stale spec line ref in the same paragraph corrected — `SETTLEMENT_UI_CLEANUP_SPEC.md` test row 783 -> 785. (4) Spec masthead at `SETTLEMENT_UI_CLEANUP_SPEC.md:8` advanced from "Last spec update: May 15, 2026" -> "Last spec update: May 17, 2026" with the SC-5 commit 2 / SC-30 Slice G1 UI promotion + May 18 audit-repair lineage captured. (5) SC-30 4-vs-5 required-test-count contradiction reconciled — DWL-SET-SC30 row now lists the five spec-required tests from `SETTLEMENT_UI_CLEANUP_SPEC.md:49` (`test_ai_settlement_offer_producer_surfaces_real_mailbox_payload`, `test_wait_for_enemy_offer_only_visible_when_offer_producer_and_cooldown_path_exist`, `test_ask_for_terms_visible_only_when_request_terms_state_can_actually_advance`, `test_ask_for_terms_click_produces_observable_state_change_not_just_copy`, `test_incoming_offer_accept_preserves_offer_identity_and_terms_through_live_preview`) plus the two STATUS-tracked supplementary tests (`test_incoming_offer_blocked_recovery_uses_request_revision_counteroffer_framing`, `test_sc30_reversal_authors_observer_blocked_ratification_voice_family`). v0.27 table row "Incoming-offer UI promotion" now reads "(28 tests including the five SC-30 spec required tests)". No code, test, helper, or contract changed; no pre-commit hook ran (pure .md). Tier 1 remains closed; Tier 2 has since landed.

**May 17, 2026 SC-5 reversal commit 2 / SC-30 Slice G1 UI promotion landed (historical; counter-editor wording superseded by the May 28 correction above):** Closes the SC-5 reversal pair (DWL-SET-SC5 flips LANDING_IN_PROGRESS -> LANDED) and SC-30 (DWL-SET-SC30 flips ACTIVE_DEFERRED -> LANDED). Backend lands `promote_pending_settlement_offers(world)` + `build_incoming_settlement_offer_popup(world, offer)` in `backend/game_logic/settlement_preview.py`, a `PERSISTENT_MAILBOX_TYPES = {"incoming_settlement_offer"}` split in `backend/models/dialogue_manager.py` (so `SOFT_STOP_MAILBOX_TYPES = CURRENT_TURN_OFFER_TYPES | PERSISTENT_MAILBOX_TYPES` — mailbox-eligible like proposals but excluded from `lapse_pending_offers` so offers persist across turns until accepted/rejected/replaced by cooldown), an `incoming_settlement_offer_popup` slot in `PopupQueue.PRIORITY_ORDER` between `incoming_proposal_popup` and `proposal_result_popup`, a new `INCOMING_SETTLEMENT_OFFER` notification family (HIGH priority), a `settlement_offer_arrival` dispatch event family, and turn-manager wiring in `_run_diplomatic_phase` that calls the producer + promote step + notification push + popup-queue push + dispatch push for each promoted offer. The `request_settlement_revision` arm in `handle_incoming_settlement_offer_action(...)` opens a real counter editor by staging `settlement_confirm` in `caller_kind="player_editor"` mode seeded with the exact offered terms / covered scope / selected target, removes the original pending offer entry, and overrides `talleyrand_text` with the new `settlement_incoming_offer_request_revision_talleyrand` Voice Bible family so the popup heading reads as "answering with a counter draft" rather than the outgoing "Will they accept?" framing. `counter_to_offer_id` + `counter_seed_terms` + `counter_seed_covered_participants` echo on the result for audit / observer routing. Endpoint changes: `GET /mailbox`, `GET /pending_envoy`, and `POST /mailbox/activate` each call `promote_pending_settlement_offers(world)` first so save-loaded saves drain pending into the mailbox on the first read; `/pending_envoy` and `/mailbox/activate` return the popup-safe payload under `incoming_settlement_offer`; the defensive `incoming_offer_deferred` filter is retired (the deferred constant stays as a named flag for emergency disables). Voice Bible §16.1 adds nine new families: `settlement_incoming_offer_arrival_{talleyrand, castlereagh, hardenberg, metternich, einsiedel, chancery}`, `settlement_incoming_offer_request_revision_talleyrand`, `settlement_incoming_offer_blocked_recovery_talleyrand`, and `settlement_blocked_for_ratification_observer`; the foreign-court chancery family answers the spec §SC-30 cross-court observer requirement so blocked ratifications are not Talleyrand-only. Godot: `main.gd` whitelists `incoming_settlement_offer` in `PROPOSAL_CONFIRM_DIALOGUE_TYPES` and `accept_settlement_offer` / `reject_settlement_offer` / `request_settlement_revision` in `SETTLEMENT_DIALOGUE_ACTIONS`; the deferred route is replaced with a real `incoming_settlement_offer` route ahead of the generic `proposal_confirm` route; both `_on_pending_envoy_result` and `_on_mailbox_activate_result` open the popup with the payload returned by the backend rather than printing the "not available in this build" stub; `proposal_confirm_popup.gd` gains an `incoming_settlement_offer` match arm with a dedicated `_build_incoming_settlement_offer_content(data)` builder rendering proposer heading + foreign chancery voice + structured terms summary + Talleyrand framing line + covered enemies. Hidden controls: `Wait for Enemy Offer` and `Ask for terms` stay absent because their lifecycles have not shipped; the popup payload exposes `wait_for_enemy_offer_visible=False` and `ask_for_terms_visible=False` flags so the Godot popup cannot invent them, and the four SC-30 spec required hidden-control regression tests (`test_wait_for_enemy_offer_only_visible_when_offer_producer_and_cooldown_path_exist`, `test_ask_for_terms_visible_only_when_request_terms_state_can_actually_advance`, `test_ask_for_terms_click_produces_observable_state_change_not_just_copy`, plus the absence assertions in the popup builder) pin the contract. New tests: `tests/test_incoming_offer_deferral_no_leaks.py` is fully inverted from the commit-1 no-UI-exposure suite into a positive UI-exposure suite (24 tests at commit 2 landing; now 28 after May 18 audit-repair additions, covering taxonomy, promote idempotence, `/mailbox` + `/pending_envoy` + `/mailbox/activate` payloads, Voice Bible §16.1 family resolution, hidden-control absence, Godot whitelist + route order + popup-builder source guards, 50-turn soak with real promotion, and the four SC-30 spec-named required tests `test_ai_settlement_offer_producer_surfaces_real_mailbox_payload`, `test_incoming_offer_accept_preserves_offer_identity_and_terms_through_live_preview`, `test_incoming_offer_blocked_recovery_uses_request_revision_counteroffer_framing`, `test_sc30_reversal_authors_observer_blocked_ratification_voice_family`). Pre-existing `tests/test_settlement_incoming_offers.py::test_incoming_offer_request_revision_*`, `tests/test_settlement_continuity_slice.py::test_request_revision_*`, `tests/test_settlement_ui_slice_f_behavior.py::test_handle_incoming_offer_revise_*`, `tests/test_war_settlement_foundation.py::test_incoming_settlement_offer_excluded_from_mailbox_taxonomy`, `tests/test_offer_lifetime.py::test_soft_stop_alias_matches`, `tests/test_cooldown_popup_manager.py::test_popup_priority_order_length`, and `tests/test_godot_parse_harness.py::test_godot_settlement_dialogue_actions_match_backend_dispatch_tuple_exactly` are inverted to the commit-2 contract (counter editor opens instead of returning a placeholder hint; taxonomy presence instead of exclusion; popup-queue length 9 instead of 8; Godot whitelist matches the union of both settlement-dialogue dispatch tuples). Godot parse harness report refreshed to 2026-05-17T19:00:00Z reflecting the touched scripts. Verification: focused settlement + diplomatic_executor + dialogue_manager + mailbox bundle `712 passed`; full deterministic suite `10114 passed, 1 skipped, 0 failed` (was `10110, 1 skipped`; +4 net); `ruff check backend tests` clean. Next settlement work is SC-32 / Slice G2 broader settlement agency follow-through (AI counterproposals, ally petitions/advisories, conference / veto / voluntary alignment offers).

**May 18, 2026 SC-5 / SC-30 audit repair landed:** Follow-up audit repairs close the post-commit-2 lifecycle and documentation gaps. The incoming-offer producer's one-active-offer guard now checks both unpromoted `world.pending_settlement_dialogues` and promoted `dialogue_manager` active/queued mailbox entries, so cooldown expiry cannot create a second unresolved offer for the same war. `promote_pending_settlement_offers(world)` now prunes already-promoted duplicate pending entries even when it does not push a new row. Foreign-court blocked settlement reviews now route through `settlement_blocked_for_ratification_observer` instead of the generic observed-settlement chancery line. Voice Bible §16.1 now documents the eight incoming-offer arrival / request-revision / blocked-recovery families alongside the existing observer family. Regression coverage added in `tests/test_incoming_offer_deferral_no_leaks.py`: `test_promote_helper_prunes_already_promoted_pending_duplicate`, `test_producer_does_not_reoffer_same_war_when_offer_is_promoted_and_unresolved`, `test_voice_bible_section_16_1_documents_incoming_offer_families`, and `test_foreign_court_blocked_settlement_routes_observer_voice_family`. Next settlement work remains SC-32 / Slice G2 broader settlement agency; `Wait for Enemy Offer` / `Ask for terms` remain intentionally hidden until a real request-terms lifecycle lands or the affordances are explicitly removed from player-facing scope.

**May 16, 2026 SC-5 commit-1 audit follow-up:** Codex audit of `2a1f9d7` returned GO with only non-blocking cleanup. Follow-up repairs align the SC-5 spec row and `handle_incoming_settlement_offer_action(...)` docstring with the actual commit-1 contract: the producer emits the concrete `[peace, gold_indemnity]` package, and accept forwards offered terms / covered scope / selected target while deliberately letting `stage_settlement_confirm(...)` infer proposer side from `actor_nation=player`. `_remove_pending_settlement_offer(...)` now uses `war_id` fallback only for stale-save entries that have no stable `offer_id`, so a mismatched stable offer id cannot clear a legitimate pending offer by war id. Regression added: `test_incoming_offer_accept_with_tampered_offer_id_does_not_clear_real_offer_by_war_id`. Verification: SC-5 focused bundle `107 passed`; full deterministic suite `10110 passed, 1 skipped`; `ruff check backend tests` clean. Next work was SC-5 commit 2 / Slice G1 UI promotion — now landed (see entry above).

**May 15, 2026 same-war off-editor merge smoke closure:** Player smoke on `SOVEREIGN_SMOKE_START=settlement_multilateral` after commits `d289e30`, `a568066`, and `c99d460` closed the remaining manual evidence row. Route exercised: first-open whole-war settlement review -> `Demand Gold Over Time` -> ratifiable `[peace, gold_per_turn]` review -> `Open War Detail` -> reopen the same France vs Britain whole-war settlement; the authored draft remained reachable/preserved instead of being silently dropped or replaced. This complements the existing same-war merge/conflict behavior tests and the draft-preservation regressions in `tests/test_settlement_recovery_g2_slice6.py` / `tests/test_settlement_white_peace.py`. The v0.27 compatibility row now flips `Same-war off-editor merge conflict` PARTIAL -> PASS. No branch/worktree was used; this is a direct `master` follow-up per `CLAUDE.md`.

**May 15, 2026 empty-draft popup smoke repair:** Player smoke of the same-war settlement route surfaced an empty editor draft reaching REVIEW as a misleading blocked popup: backend review sections injected a fake `End hostilities` Terms row, the banner fell back to `no single dominant pressure` even though the actual gate was "no authored terms", and the Godot `proposal_confirm_popup` action rail used a fixed `HBoxContainer` that could clip settlement actions once SC-29 / SC-31 / SC-33 buttons were all legal. Repair: empty `settlement_terms=[]` now renders no Terms row, authored `{"type": "peace"}` renders `End hostilities (no material change)`, empty editor-staged reviews publish `acceptance_display.band="blocked"` with `ratify_blocked_reason="No settlement terms have been authored."`, and the popup button rail is a 3-column `GridContainer` with expanding buttons. Verification: `tests/test_settlement_white_peace.py` + `tests/test_godot_parse_harness.py` `20 passed`; settlement/Godot focused bundle `43 passed`; recurring-gold/foundation/presentation bundle `113 passed`; `ruff check backend tests` clean; Godot parse report refreshed with zero parse/load failures. Server restarted on `SOVEREIGN_SMOKE_START=settlement_multilateral` for continued same-war off-editor merge smoke.

**May 15, 2026 first-open authoring follow-up:** Follow-up player smoke showed that the now-honest empty REVIEW still exposed only pair-substitute / war-detail escape routes on first open. Repair adds first-open winning-side authoring CTAs `Demand Gold` (`author_gold_indemnity_terms`) and `Demand Gold Over Time` (`author_gold_per_turn_terms`) before the SC-29 substitute CTAs. Both route through `/respond_to_diplomatic_dialogue`, the backend settlement handler, and the Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist; click-time staging produces canonical `[peace, gold_indemnity]` or `[peace, gold_per_turn]` drafts without mutation, so the player can author material settlement terms from the popup instead of falling into bilateral/armistice-only recovery. Verification: updated empty-Ratify/first-open behavior tests plus Godot dispatch/whitelist guards; `tests/test_settlement_white_peace.py` + `tests/test_godot_parse_harness.py` + `tests/test_settlement_recurring_gold.py` + `tests/test_smoke_start_settlement_multilateral.py` `52 passed`; `ruff check backend tests` clean; Godot parse report refreshed with zero parse/load failures.

**May 15, 2026 accepted-review draft-preservation follow-up:** Player smoke of the `Demand Gold Over Time` path showed the authored REVIEW was valid and ratifiable, but offered only `Ratify Settlement` / `Back Out`, making the same-war off-editor smoke impossible without either mutating the war or discarding the draft. Repair exposes `Open War Detail` on non-empty ratifiable settlement reviews when War Detail actionability is positive; the existing `open_war_detail` handler preserves the non-empty draft in `pending_settlement_drafts[war_id]` and pops the dialogue without mutation. Verification: `tests/test_settlement_white_peace.py` + `tests/test_settlement_recovery_g2_slice6.py` + `tests/test_godot_parse_harness.py` `31 passed`; `ruff check backend tests` clean.

**May 15, 2026 test-pollution repair (full-suite green):** While verifying G2-Slice-1b-Repair-1, the full pytest suite (`tests/`) showed 158 deterministic failures unrelated to the slice (e.g. `test_wpsb_power_cap::test_starting_powers` reporting "France power 1150 != 1100"). Root cause: developer `.env` left `SOVEREIGN_SMOKE_START=settlement_losing` from the May 14, 2026 manual Gate 4 smoke session. `LLMClient.__init__` loads `.env` via `dotenv`, injecting the var into `os.environ`; the next `WorldState()` call then ran `_seed_settlement_losing_smoke_start()` which transferred Waterloo to France and bumped France's gold, contaminating every subsequent fresh-world test in the same pytest process. Repair: (1) commented out the leaked line in the local `.env` (untracked, not committed), and (2) added an autouse pytest fixture in [tests/conftest.py](tests/conftest.py) (`_isolate_sovereign_smoke_start`) that `monkeypatch.delenv("SOVEREIGN_SMOKE_START", raising=False)` before every test, so future dev-env leaks cannot poison test runs. Manual smoke sessions still work — they export the var in the shell that runs the live server, not the pytest process. Full suite is now `10087 passed, 1 skipped, 0 failed` in both deterministic (`-p no:randomly`) and default-random orderings. This unblocks the pre-commit hook so subsequent settlement-cleanup commits land without `--no-verify`.

**May 15, 2026 G2-Slice-1b-Repair-1 — Forced-alliance Continental toggle differential closed:** Slice closes the "Forced-alliance Continental toggle differential" PARTIAL row on the v0.27 Integration Compatibility Table (PARTIAL -> PASS). Backend adds `compute_forced_alliance_continental_toggle_differential(world, *, war_id, settlement_terms)` at [backend/game_logic/settlement_scoring.py:1780](backend/game_logic/settlement_scoring.py:1780), which builds a per-clause differential payload comparing `includes_continental_system=True` and `=False` projections for every `forced_alliance` clause without mutating world / terms. The helper output reaches the player through three surfaces: POST settlement preview as `settlement_preview.forced_alliance_continental_toggle_differential`, the staged `settlement_confirm` dialogue as a top-level `forced_alliance_continental_toggle_differential` field (deep-copied from POST preview + explicit propagation in `build_settlement_confirm_dialogue(...)`), and each `forced_alliance` row of `build_applied_clauses_preview(...)` as a top-level `continental_toggle_differential` field. Each row carries `{clause_index, from, to, with_continental_system: {threat_delta, projected_threat, balance_modifier, balance_post_share_pct, balance_crossed_band}, without_continental_system: <same shape>, threat_delta_difference, balance_delta_difference, display}` with the humanized copy `"Adds <target> to the Continental System; extra threat cost applies."`. Threat math is the second deliverable: new constant `FORCED_ALLIANCE_CONTINENTAL_SYSTEM_THREAT_SURCHARGE = 10` adds an extra `+10` threat surcharge on top of the existing `FORCED_ALLIANCE_THREAT_PER_CLAUSE = 15` base when CS=True. The surcharge is wired symmetrically through `compute_forced_alliance_threat_preview` (now branches per-clause on the CS toggle and reports `continental_system_clauses` count), the settlement-confirm ratification path `_resolve_pair_state_transitions` in [backend/game_logic/settlement_preview.py:3487](backend/game_logic/settlement_preview.py:3487) (always overwrites the applied clause's `projected_threat_delta` and charges the matching amount via `coalition.add_threat`), and the bilateral-treaty ratification path `WorldState._ratify_treaty` forced-alliance block in [backend/models/world_state.py:5974](backend/models/world_state.py:5974). `forced_alliance_imposed` events carry `projected_threat_delta` so dispatch/log/ledger surfaces render the CS-adjusted cost. Balance/hegemony math (`project_balance_after_settlement`) is unchanged this slice. New test bundle in [tests/test_settlement_forced_alliance_continental_toggle.py](tests/test_settlement_forced_alliance_continental_toggle.py): `test_forced_alliance_continental_toggle_preview_shows_cost_difference` (the named spec test — per-clause differential is non-empty, threat numbers differ by exactly literal `10`, humanized display names target + "Continental System", and POST preview, staged-dialogue, and applied-clauses preview rows all carry the differential), `test_forced_alliance_no_clause_returns_no_continental_toggle_differential` (negative case), and `test_forced_alliance_continental_toggle_helper_equivalence` (per-clause `with_continental_system` / `without_continental_system` match direct `compute_forced_alliance_threat_preview` and `project_balance_after_settlement` calls). Impacted legacy tests updated to the new math: `tests/test_common_peace_acceptance.py::test_debug_exposes_forced_alliance_threat_delta_and_crossed_thresholds` (15 -> 25, 80 + 90 crossed thresholds), `tests/test_common_peace_c2_ratification.py::test_confirm_forced_alliance_pair_ends_in_alliance_with_origin_and_threat` (`threat_before + 15` -> `+ 25`), and `tests/test_wpsc_forced_alliance.py::test_multiple_forced_alliance_clauses_process_independently` (`before + 30` -> `+ 50` for two CS=True clauses). Verification: focused new bundle `3 passed`; broader settlement / acceptance bundle (acceptance + c2_ratification + new toggle + wpsc + presentation + g2_slice5) `232 passed`; full pytest suite `10087 passed, 1 skipped, 0 failed` in both deterministic (`-p no:randomly`) and default-random orderings; `ruff check backend tests` clean. Spec contract: current `SETTLEMENT_UI_CLEANUP_SPEC.md` records the `AppliedClausePreview` `forced_alliance` row shape with `continental_toggle_differential` as a top-level row field and `balance_post_share_pct` as the integer balance-share field, records the surcharge constant + ratification wiring contract, and names the current test bundle. The same-war off-editor manual evidence follow-up is now closed in the May 15, 2026 smoke note above. No SC-5 reversal, no SC-30 work, no incoming-offer / Request Terms / mailbox offer UI touched.

**May 14, 2026 G2-Gate4-Repair-1 dialogue-dispatch repair:** Player-driven Gate 4 smoke against the SC-29 rejected-popup substitute CTAs surfaced two regressions hidden by handler-only unit tests: (1) backend `DiplomaticExecutor._process_dialogue_choice` dispatch tuple at [backend/commands/diplomatic_executor.py:2726](backend/commands/diplomatic_executor.py:2726) routed only four settlement actions (`confirm_settlement`, `revise_settlement_terms`, `back_out_settlement`, `open_war_detail`) to `handle_settlement_dialogue_action`, so the SC-29 `seek_bilateral_peace` / `seek_armistice_instead`, SC-31 `author_surrender_terms`, and G2-Slice-W1 `re_author_with_concessions` action ids fell through to "Unknown dialogue action: ..." despite being correctly listed in the Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist and correctly handled inside `handle_settlement_dialogue_action`; (2) Godot `main.gd::_on_command_result` early-returned for any `recovery_route` Dictionary at [godot-client/.../main.gd:980](godot-client/project-sovereign/scripts/main.gd:980), but `_route_settlement_recovery_route` only understands `war_detail` and `settlement_history` surfaces — so the SC-29 substitute response (`surface=proposal_confirm` + fresh `diplomatic_dialogue`) was consumed before the proposal popup could open. Fixes: dispatch tuple now includes the four missing action ids; the recovery-route early-return is scoped to `war_detail` / `settlement_history` so `proposal_confirm` falls through to the normal `diplomatic_dialogue` handler. Follow-up `codex-2026-05-14-settlement-G2-Gate4-Repair-1` closed the coverage gaps: endpoint behavior tests now exercise the full `handle_diplomatic_dialogue_response(...)` -> `_process_dialogue_choice` -> `handle_settlement_dialogue_action(...)` chain for all four repaired ids (`seek_bilateral_peace`, `seek_armistice_instead`, `author_surrender_terms`, `re_author_with_concessions`); the Godot guard now has a behavior twin that drives the SC-29 `proposal_confirm` recovery response through the post-HUD route-order model; and a similar-bug guard proves `SETTLEMENT_DIALOGUE_ACTIONS` exactly matches the backend settlement dispatch tuple with no orphan ids. Verification: focused repair closure bundle `tests/test_settlement_pair_substitute_ctas.py` + `tests/test_settlement_dependency_clauses.py` + `tests/test_godot_parse_harness.py` + `tests/test_settlement_concession_baseline.py` = `77 passed`; previous broader settlement bundle `436 passed`; previous full suite baseline `10071 passed, 1 skipped`; `ruff check backend tests` clean. No spec-contract change — the SC-29 / SC-31 / G2-Slice-W1 landings remain canonical; this repair patches the dispatch wiring that those slices depended on. Player can now re-smoke the SC-29 substitute CTAs, SC-31 surrender CTA, and G2-Slice-W1 concession re-author CTA without falling through to "Unknown dialogue action".

**May 14, 2026 recurring-gold smoke repair:** Manual Gate 4 smoke of `SOVEREIGN_SMOKE_START=settlement_recurring_gold` showed that SC-33 backend support for `gold_per_turn` was live but the settlement popup had no player-facing authoring action for the finite recurring-payment draft. `build_settlement_preview(...)` now emits a click-time-revalidated `recurring_gold_preset` payload (`peace` + `gold_per_turn`, France -> Britain, 50 gold/turn for 3 turns in the smoke fixture), `build_settlement_confirm_dialogue(...)` surfaces `Offer Gold Over Time`, `handle_settlement_dialogue_action(...)` stages the draft or a replace-confirm without mutation, suppresses the generic concession-baseline affordance once the recurring draft is staged, `DiplomaticExecutor._process_dialogue_choice` and Godot `SETTLEMENT_DIALOGUE_ACTIONS` whitelist the author/apply ids, and `proposal_confirm_popup.gd` renders the recurring-gold draft copy before the player clicks. Regression coverage now includes the direct handler path, full `/respond_to_diplomatic_dialogue` executor dispatch path, and replace-confirm apply path. Verification: `tests/test_settlement_recurring_gold.py` `27 passed`; `tests/test_godot_parse_harness.py` `9 passed`; all `tests/test_settlement_*.py` `428 passed`; `ruff check backend tests` clean. Live server restarted with `SOVEREIGN_SMOKE_START=settlement_recurring_gold`; `/diplomatic_preview?mode=settlement&war_id=war_1` reports `recurring_gold_preset_visible=true`, terms `peace,gold_per_turn`, amount `50`, turns `3`.

**May 14, 2026 Gate 4 manual smoke closure:** Player smoke in the local Godot/server loop passed the required fixture variants after Codex repairs through `7a02491`: `settlement_rejected`, `settlement_losing`, `settlement_multiwar_ambiguity`, `settlement_surrender`, and `settlement_recurring_gold`. Observed repairs included rejected-popup substitute dispatch, concession re-author no-repeat behavior, surrender preset ratifiability/presentation, recurring-gold authoring visibility, and recurring-gold post-author suppression of the generic concession replacement affordance. Before committing this status update, the local backend was restarted without `SOVEREIGN_SMOKE_START` so the shared server is back on the base scenario rather than a smoke fixture.

**May 14, 2026 Codex G2-Slice-8 audit repair:** Session `codex-2026-05-14-settlement-G2-Slice-8` closed the player-side surrender ratification blocker found at commit `585d43c`: dependency ratification now applies vassalage/subjugation by explicit `from = vassal/subjugated, to = lord` term direction instead of assuming the proposer is always the lord. The regression `test_losing_side_surrender_ratification_vassalizes_player_nation` proves a France-authored surrender package `[peace, subjugation from France to Britain]` mutates France into Britain's vassal and emits the applied subjugation clause. Focused dependency verification is now `39 passed` after subsequent additions.

**Next work focus (May 28, 2026): POST-REPAIR AUDIT CLOSURE. Tier 1-4 plus Tier 5 G2-Slice-G2a, G2-Slice-G2e, G2-Slice-G2d, G2-Slice-G2b, G2-Slice-G2c, and G2-Slice-G2f are LANDED and repair-verified.** SC-32 product decisions remain locked in `docs/SC32_SLICE_G2_DECISIONS.md`: D1 AI counterproposals CUT, D2 Wait-for-Enemy-Offer + Ask-for-Terms CUT, D3 voluntary alignment CUT, D4a/D4b ally petitions SHIP, D4c request_consultation + request_redress_after_settlement CUT, D5 conference / veto CUT, D6 same-war replace-confirm chooser SHIP, and D7 solicited petition trigger model. Remaining work is only any future product slice opened by a new owner row/spec amendment; the all-completed-audit-punch-list repair closes the current audit punch list. Default-path total remains within the 30-42 spec budget.

**Canonical follow-up edits now folded:** rejected-popup substitute CTAs have an explicit SC-29 timeline and post-SC-29 action ordering and are **now landed** at G2-Slice-7 with `evaluate_pair_peace_substitute_eligibility(...)`, dialogue-handler dispatch, and 17 behavior tests; `Ask for terms` cannot ship as a clickable copy-only refusal; attempt-4 stale recovery must run the War Detail actionability probe and terminal-close when War Detail is dead; helper payload schemas, helper ownership, disabled-vs-hidden refusal-code taxonomy, and clause picker schema are normative; `ClauseControlSchema` is the backend source of truth and hidden clause types must not leak as disabled labels; losing-side concession baseline has first-paint visibility, click-time revalidation, no silent overwrite through `re_author_with_concessions`, and still-rejected next-step tests; typed one-to-one common-peace entry is rejected in the executor; `forced_alliance` requires Balance/threat cost differential preview; parent incoming-offer promote-time tests are conditional on explicit SC-5 reversal; parent row-focus language now treats route-id auto-expand as required; Gate 4 requires both `settlement_losing` and `settlement_rejected`; `vassalage`, `subjugation`, `liberation`, and `gold_per_turn` are live after SC-31 / SC-33 and must mutate through ratification.

## Last Recorded Spec Review Result

> The May 2026 Codex spec-review and branch-reconciliation records moved to `docs/archive/STATUS_SETTLEMENT_V027_PROCESS_2026_05.md` (June 10, 2026 trim). Branch state: all settlement work is on `master` (single-branch workflow per CLAUDE.md).

## Manual Smoke Fixture Variants

Before Gate 4 manual settlement UI smoke, set `SOVEREIGN_SMOKE_START=settlement_multilateral` for the shared France vs Britain + Prussia war. Run `SOVEREIGN_SMOKE_START=settlement_losing` for concession-baseline and losing-side authoring paths, and run `SOVEREIGN_SMOKE_START=settlement_rejected` separately for blocked-popup, SC-10b / SC-28 / SC-28b recovery, scoped-draft, no-direct-pair-action, and actionability-gated War Detail paths. Use `SOVEREIGN_SMOKE_START=settlement_multiwar_ambiguity` for the required same-nation multi-war ambiguity check; Gate 4 step 1 cannot close unless smoke evidence names this fixture and records the rendered disambiguation or hidden-action outcome. The losing and rejected fixtures are both required; one cannot stand in for the other.

## Verification Snapshot

Slice G / AI-ally settlement agency cut/ship ownership is no longer blocked on the rejected, losing, multiwar ambiguity, surrender, recurring-gold, forced-alliance Continental toggle differential, same-war off-editor, incoming-offer mailbox/popup fixture smoke paths, or `DWL-SET-SC5R`. The SC-5R backend/editor repair now provides the edit-capable counter-draft surface, Submit for Review path, scoped draft preservation, active-vs-archived routing, and executable Godot parse evidence; Gate 4 manual smoke evidence remains the player-quality release gate. SC-32 landing ownership remains tracked rather than open-ended, and SC-29 / SC-30 / SC-31 / SC-33 remain regression-covered after landing.

> The v0.27 Integration Compatibility Table (all rows PASS) moved to `docs/archive/STATUS_SETTLEMENT_V027_PROCESS_2026_05.md` (June 10, 2026 trim).

## Deferred Work Landing Ledger

Use this ledger as the current routing layer for any active `deferred`, `future`, `later`, `polish`, `hidden`, `cut`, or `backlog` wording in specs. Status values are `ACTIVE_DEFERRED`, `LANDED`, `SUPERSEDED`, or `REMOVED`. New deferrals must name a ledger ID or a concrete `SC-*` / slice row, plus an owner, landing trigger, completion definition, and required behavior test.

| ID | Source | Status | Owner | Landing trigger | Completion definition | Required test / evidence | Notes |
|----|--------|--------|-------|-----------------|-----------------------|--------------------------|-------|
| DWL-SET-SC5 | WSA `incoming_settlement_offer` / cleanup SC-5 | LANDED | `SETTLEMENT_UI_CLEANUP_SPEC.md` SC-5 + Slice G1 appendix | May 17, 2026 commit 2 landing closes the offer producer / mailbox / popup promotion substrate. Commit 1 (`2a1f9d7`) shipped the backend producer + handler substrate; commit 2 ships the UI promotion layer | Commit 1: `process_settlement_offer_phase(world)`, per-`war_id` cooldown, one-active-offer guard, stable `offer_id="settlement_offer:{war_id}:{turn}:{seq}"`, package-preserving `handle_incoming_settlement_offer_action` accept/reject. Commit 2: `promote_pending_settlement_offers(world)` drains pending into the dialogue_manager mailbox (idempotent across save/load), `build_incoming_settlement_offer_popup(world, offer)` builds the popup payload with Voice Bible §16.1 incoming-offer copy, `PERSISTENT_MAILBOX_TYPES` taxonomy split so offers persist across turns, `INCOMING_SETTLEMENT_OFFER` notification family + `incoming_settlement_offer_popup` popup-queue slot + `settlement_offer_arrival` dispatch event, turn-manager wiring promotes + notifies + dispatches, `/mailbox` + `/pending_envoy` + `/mailbox/activate` surface the offer popup payload, `request_settlement_revision` stages a seeded `settlement_confirm` review with `counter_to_offer_id` provenance + request-revision Voice Bible heading, Godot `main.gd` whitelists + replaces deferred route with real popup route, `proposal_confirm_popup.gd` gains the `incoming_settlement_offer` match arm + builder | Commit 1: `tests/test_settlement_incoming_offers.py` (22 tests). Commit 2 plus later repairs: `tests/test_incoming_offer_deferral_no_leaks.py` (31 tests after May 18 audit-repair additions and G2d D2 cut-evidence additions) cover taxonomy / promote idempotence / `/mailbox` + `/pending_envoy` + `/mailbox/activate` payloads / Voice Bible §16.1 resolution / hidden-control absence / Godot whitelist + route order + popup-builder source guards / 50-turn soak / SC-30 required tests / G2d retired-flag absence tests | Offer promotion remains landed. The prior "real counter editor" wording was an overclaim; the edit-capable request-revision repair is tracked separately in `DWL-SET-SC5R`. `INCOMING_OFFERS_DEFERRED` stays as a named flag for emergency disables but defaults to False |
| DWL-SET-SC5R | Incoming-offer `Request Revision` / settlement revise counter-editor repair | LANDED | Settlement UI Cleanup SC-1 / SC-2 / SC-5 / SC-30 repair | May 28, 2026 SC-5R-1 + SC-5R-2 landed the backend EDIT contract, Godot editor, scoped draft round-trip, active-vs-archived routing, and incoming-offer label/copy alignment; the follow-up repair on `master` fixed the archived-route cache guard, self-gated `show_editor(...)`, empty Open Settlement / Request Revision EDIT mounting, inline editor error remounts, backend-revalidated in-editor concession-baseline application, strengthened tests, and refreshed the Godot parse report. | Real EDIT-mode counter-draft path is present: Godot clause controls consume backend `available_clause_types[]` / `clause_control_schema`, empty Open Settlement starts in EDIT with Submit disabled, covered-enemy scope is editable, clause add/edit/remove authors structured draft clauses, `Submit for Review` submits through `_execute_propose_common_peace` with final validation, revised drafts persist by scoped `draft_key`, staging and ratification reject invalid terms, active partial-settlement review routes preserve live-war context while archived wars route to settlement history, and incoming-offer Review/Revision/Reject copy matches behavior. | `tests/test_settlement_sc5r1_backend_contract.py`, `tests/test_settlement_sc5r2_godot_editor.py`, the adjacent settlement/incoming/recovery bundle, and `tests/test_godot_parse_harness.py` cover backend contract, editor source/scene, draft restore, active-vs-archived routing, label copy, and parse/load report coverage for `settlement_editor_popup.gd`. | Player-quality gate is GO pending Gate 4 manual smoke evidence; not blocked on SC-5R implementation. |
| DWL-SET-SC29 | Direct-pair substitute CTAs (`Seek Armistice Instead`, `Seek Bilateral Peace`) | LANDED | Settlement UI Cleanup SC-29 / G2-Slice-7 | G2-Slice-7 shipped May 13, 2026 with `evaluate_pair_peace_substitute_eligibility(...)`, blocked-popup options emission, dialogue-handler dispatch, and committed Voice Bible families | Blocked settlement review surfaces pair-scoped `Seek Bilateral Peace` / `Seek Armistice Instead` between Re-author and Open War Detail only when the selected target pair is eligible; only `cooldown_active` may render disabled; click-time re-resolution refuses to mutate on state change; substitute hand-off stages `proposal_confirm` for `propose_peace` / `propose_armistice` with `target_nation = selected_target_nation` | `tests/test_settlement_pair_substitute_ctas.py` (19 tests covering helper schema, closed taxonomy including resolved-pair mapping, disabled vs hidden policy, post-SC-29 popup integration, click-time re-resolution, no SC-14b reopen-attempt consumption, per-pair staging, cross-war settlement collision, draft preservation/invalidation, voice family routing, helper-signature isolation) | Hidden until implemented; clickable copy-only controls forbidden |
| DWL-SET-SC30 | Enemy-offer waiting (`Wait for Enemy Offer`, `Ask for terms`) | REMOVED | Settlement UI Cleanup SC-30 / Slice G1 + SC-32 / G2-Slice-G2d | Incoming-offer producer + UI promotion + mailbox + popup remain live through DWL-SET-SC5. G2-Slice-G2d (May 28, 2026) terminally removes `Wait for Enemy Offer` / `Ask for terms` from player-facing scope; no SC-30b/c lifecycle is promised. | `wait_for_enemy_offer` / `ask_for_terms` action ids do not appear in any backend dispatch or Godot whitelist; the incoming-offer popup `options[]` only lists `accept_settlement_offer` / `request_settlement_revision` / `reject_settlement_offer`; popup payloads no longer expose `wait_for_enemy_offer_visible` or `ask_for_terms_visible`; `wait_for_enemy_offer_unavailable` is absent from display names. | Existing SC-30 absence guards remain in `tests/test_incoming_offer_deferral_no_leaks.py`; G2d adds `test_wait_for_enemy_offer_visible_flag_removed_from_payload_v032`, `test_ask_for_terms_visible_flag_removed_from_payload_v032`, and `test_wait_for_enemy_offer_unavailable_display_string_removed`. | Terminal CUT per SC-32 D2. Future Ask-for-Terms or wait/subscription behavior must ship as a new feature spec, not by reviving the retired payload flags. |
| DWL-SET-SC31 | `Surrender terms` + dependency clause types (`vassalage` / `subjugation` / `liberation`) | LANDED | Settlement UI Cleanup SC-31 / G2-Slice-8 | G2-Slice-8 shipped May 14, 2026 with `SETTLEMENT_LIVE_CLAUSE_TYPES`, dependency eligibility helpers, the `[peace, harshest-legal-dependency]` surrender preset (subjugation-then-vassalage order), the `author_surrender_terms` blocked-popup action + dialogue handler with click-time revalidation, the `surrender_preset` propagation through staging / ratify / reactions / `settlement_summary`, applied_clauses_preview rows for dependency clauses, Voice Bible §16.1 surrender/dependency families, the `SOVEREIGN_SMOKE_START=settlement_surrender` fixture, Godot popup banners, and Codex audit repair `codex-2026-05-14-settlement-G2-Slice-8` | Dependency clauses live in `available_clause_types[]`, surrender preset authors a deterministic concrete dependency package only when `losing_for_concession_baseline=true` AND a power-cap-legal dependency exists, ratification mutates real vassal state by explicit `from`/`to` direction even when the player is surrendering, and history/dispatch/ledger/Voice Bible §16.1 render the dependency consequence | `tests/test_settlement_dependency_clauses.py` (39 tests: clause-control schema inversion, closed-taxonomy validator rejection, eligibility helpers, surrender preset deterministic ordering + fallback, payload schema, blocked-popup CTA visibility, dialogue handler replace-confirm / click-time revalidation / no-overwrite, player-side surrender ratification mutation, `settlement_summary.surrender_preset` propagation, applied_clauses preview fields, smoke fixture, Voice Bible families) | DWL row was previously two entries (dependency clause types + surrender preset); merged into a single LANDED row because the slice ships them together |
| DWL-SET-SC32 | Broader settlement agency | LANDED | Settlement UI Cleanup SC-32 / Slice G2 | G2-Slice-G2a, G2e, G2d, G2b, G2c, and G2f landed on master | SC-32 v0.32 outcomes are closed: AI counterproposals, Wait-for-Enemy-Offer / Ask-for-Terms, voluntary alignment, conference / veto, and request_consultation / request_redress_after_settlement are cut with absence tests; `request_open_settlement` and `warn_against_sellout` ally petitions ship as solicited advisory mailbox items; `request_reward_or_restoration` and `demand_bargain_honor` are deferred to Slice H with absence tests; same-war replace-confirm ships; D7 remains solicited only. | `test_settlement_agency_landing_ledger_has_no_unowned_backlog_controls` plus the G2b/G2c/G2d/G2e/G2f behavior and absence suites named in `SETTLEMENT_UI_CLEANUP_SPEC.md` v0.32 | Closed by the May 28, 2026 all-completed-audit-punch-list repair; future settlement-agency work needs a new owner row/spec amendment. |
| DWL-SET-SC33 | Recurring gold payments (`gold_per_turn`) | LANDED | Settlement UI Cleanup SC-33 / G2-Slice-9 | G2-Slice-9 shipped May 14, 2026 with editor-live `gold_per_turn`, validator bounds (`amount >= 10`, `1 <= turns <= 20`), projected-solvency `gold_payment_budget_conflict`, `world.recurring_settlement_payments` storage, the per-turn `process_recurring_settlement_payments` processor wired into `advance_turn` between vassal tribute and the bankruptcy check, four cancellation conditions (payer eliminated / payer vassalized / recipient eliminated / renewed war), `amount * turns` harshness projection at the lump-sum weight, `applied_clauses_preview` value fields (`payer_balance_before`, `payer_balance_after_first_payment`, `projected_total_obligation`, `first_payment_turn`), Voice Bible §16.1 `settlement_recurring_gold_authored / _ratified / _completed_talleyrand`, dispatch templates for `settlement_recurring_gold_paid / _partial / _completed / _cancelled`, and `SOVEREIGN_SMOKE_START=settlement_recurring_gold` fixture. | `gold_per_turn` lives in `CLAUSE_CONTROL_SCHEMA` with `enabled=True, visibility="live"`; ratification registers a recurring obligation without moving gold immediately; the income-phase processor transfers `min(amount, balance)` once per turn, decrements `turns_remaining`, and removes the record on natural completion; harshness uses full projected obligation; old saves default `recurring_settlement_payments` to `[]`. | `tests/test_settlement_recurring_gold.py` (27 tests: SC-33 4 required + clause-control schema live + validator amount/duration/budget conflict bounds + minimum/maximum bound acceptance + display registry coverage + ratification registers obligation + public ratify/advance-turn processing + real eliminated-nation cancellation through the WorldState path + first-tick gold transfer + decrement + partial payment + natural completion + four cancellation conditions + save/load round trip + default-empty pre-SC-33 saves + harshness projection finite vs perpetual + smoke fixture + wizard structured payload acceptance + player-facing popup author/dispatch/replace-confirm coverage). | DWL row was ACTIVE_DEFERRED, now LANDED; canonical clause schema marks `gold_per_turn` as `Live after SC-33 / G2-Slice-9`. |
| DWL-DIP-E7 | `DIPLOMACY_SPEC.md` E7 defiance floor redesign | ACTIVE_DEFERRED | Pre-8.5 War LLM + Diplomacy Refinement Evaluation | Settlement final gate closes and the 8.EVAL refinement pass starts | Decide keep 2%, raise to 5%, retune curve, or explicitly remove; update formula docs, Talleyrand defiance tests, and balance notes | Focused Talleyrand defiance probability tests and status note | Balance decision, not a current blocker |
| DWL-DIP-AIAI | `DIPLOMACY_SPEC.md` old AI-AI diplomacy deferral | LANDED | Phase 8 Session 8D / AI diplomacy | Already landed | `process_ai_ai_diplomatic_phase`, AI-AI treaty state changes, dispatch, fog visibility, and campaign-log routing exist | Existing AI-AI diplomacy / dispatch / campaign-log tests | Old "Session 8 deferred" note is historical only |
| DWL-DIP-TRADE | `DIPLOMACY_SPEC.md` trade income deferred to Session 2 | LANDED | Phase 8 Session 2 diplomacy | Already landed | Diplomatic-state trade income is applied during income phase and shown in economy surfaces | Existing trade-income and ledger tests | Old Session 1B deferral is historical only |
| DWL-DIP-CONTINENTAL | DD7 `Full Continental System` | LANDED | Phase 8 / Coalition / Peace Deals WPS-C | Already landed | Continental System membership, income effect, forced-alliance membership, dispatch, and save/load are live | Existing Continental System and WPS-C tests | No separate "full" placeholder remains |
| DWL-DIP-FOGINTEL | DD7 fog-filtered diplomatic intel | LANDED | Fog / ledger / dispatch systems | Already landed | Diplomatic AI-AI state visibility uses PARTIAL+ rules; player-facing strength/status paths use fog-filtered helpers | Existing fog, dispatch, ledger, and campaign-log tests | Future intel polish must get a new owner row |
| DWL-DIP-CAMPAIGNLOG | DD7 campaign-log diplomatic events | LANDED | Campaign log / dispatch systems | Already landed | Diplomacy events including AI-AI treaty and peace/settlement families route through campaign log filters/oneliners | Existing campaign-log enforcement and diplomacy event tests | Old display-only gap is closed |
| DWL-DIP-SPECIALBONUS | DD7 special acceptance bonuses | LANDED | Diplomacy acceptance formula | Already landed | `SPECIAL_BONUSES` feed `special_desire_bonus` and surface in acceptance components | Existing acceptance and session 8D tests | Future tuning is balance polish, not a missing mechanic |
| DWL-DIP-METTERNICH | DD7 Metternich armed mediation | ACTIVE_DEFERRED | Pre-8.5 War LLM + Diplomacy Refinement Evaluation | Settlement final gate closes and 8.EVAL decides AI-personality polish scope | Either implement the Schemer-specific armed-mediation bonus with tests or mark it REMOVED as over-specific flavor | AI proposal rejection -> Austria war-declaration pressure test, or explicit removal note | Not a current settlement blocker |
| DWL-DIP-RENAME | Carved-vassal player rename example | REMOVED | None | N/A | No active command promise; do not show rename command examples until a future naming/customization spec owns it | Deferred-word scan shows no active "rename vassal" promise | Removed because it is low-value polish with no current UI/editor surface |
| DWL-DIP-CONFERENCE | Old "Peace conference (multi-nation)" note | SUPERSEDED | Imperial Settlement / possible future Congress System | Current common-peace needs are owned by Imperial Settlement; no Congress work starts without a new spec | Treat old peace-conference promise as superseded by common peace. A future Congress System must open a new design gate if desired | Peace Deals / settlement spec audits confirm common-peace ownership | No separate conference minigame is planned |

> The May 13-14, 2026 G2-Slice-7/8/9 landing records and the superseded slice queue moved to `docs/archive/STATUS_SETTLEMENT_V027_PROCESS_2026_05.md` (June 10, 2026 trim).

---

## Quick Stats

| Metric | Value |
|--------|-------|
| **Tests Passing** | **Current master verification (June 9, 2026 — Gate-4 pre-flight audit capture, doc-only commit `4179e35`, pre-commit hook):** full deterministic suite `10331 passed, 1 skipped`; ruff clean. Prior code-change verification (May 30, 2026 — Settlement Re-front Slice 3 + post-implementation audit fold): full deterministic + default-random suites `10314 passed, 1 skipped`; `ruff check backend/` clean; Godot 4.4.1 parse/load report current (exit 0) and includes `settlement_editor_popup.gd`. **`DWL-SET-SC5R-3` (inline merge-conflict `Discard new clause` / `Replace active clause` controls) is LANDED** in Slice 3, as is **REFRONT-8** (the editor consumes `enabled`/`disabled_reason_display`). The Slice 3 audit fold added ratify-gate V1–V5 defense-in-depth (CRITICAL — a stale liberation could otherwise mutate an uncovered party), role-aware Tier-3 pickers (P2), and re-homed the OQ#4 focused-breakdown panel as REFRONT-9. |
| **Current Phase** | **NEXT — Settlement Guided Terms IMPLEMENTATION (`docs/SETTLEMENT_GUIDED_TERMS_SPEC.md` v0.2 APPROVED June 10, 2026 — D4 mixed-direction rows explicitly confirmed; Run #1 audit + GT-A1..4 folded; slice order GT-Slice-1 → 2 → V → 3 → 4; CH-5 is GT-Slice-1's recommended companion). Gate-4 pre-flight fix slices PF-1 + PF-2 + PF-3 LANDED June 9, 2026 (`docs/SETTLEMENT_GATE4_PREFLIGHT_AUDIT.md` — D1–D6 fixed, D7 latent/owned by CH-5; `settlement_losing` UNBLOCKED).** **The Gate 4 manual settlement smoke is RE-SEQUENCED (user decision June 9) to run ONCE at the END of the settlement queue** — after Guided Terms + the remaining queued items — instead of immediately after PF-1; it remains the gate for Slice G. Settlement Conversational Re-front (`docs/SETTLEMENT_CONVERSATIONAL_REFRONT_SPEC.md` v0.6 — APPROVED May 29, 2026): Slices 0–3 + REFRONT-V voice IMPLEMENTED/LANDED + Slice 3 audit fold folded on master; the OQ#4 panel is owned as **REFRONT-9** and folds into the Guided Terms expanded row per GT-A4. `DWL-SET-SC5R-3` and `REFRONT-8` are **LANDED** (Slice 3). **Peace Deals - Settlement UI Cleanup v0.32 GO pending the end-of-queue Gate 4 manual smoke.** Tier 1-4 + SC-32 / Slice G2 + `DWL-SET-SC5R` all LANDED; the non-destructive `suspend_settlement_editor` Back Out hard-stop fix landed at `6da3131`. Slice G AI-ally settlement agency stays separate and later. |
| **Next Roadmap Gate** | After Settlement UI Cleanup, Slice G, final settlement gate, and smoke, run the new `ROADMAP.md` **8.EVAL Pre-8.5 War LLM + Diplomacy Refinement Evaluation**. It must recover/triage buried war-LLM improvements, battle/war narration and creative-command war uses, LLM cost/toggle implications, and `DESIGN_REFINEMENT.md` diplomacy candidates before Phase 8.5 begins. |
| **Blockers** | No settlement implementation blocker — PF-1/PF-2/PF-3 landed June 9, 2026 (`settlement_losing` UNBLOCKED). The Guided Terms spec is APPROVED (v0.2, June 10, 2026 — D4 confirmed); implementation may proceed (GT-Slice-1 next). Player-quality release still awaits the end-of-queue Gate 4 manual settlement UI smoke evidence (re-sequenced June 9 to run once after the settlement queue completes). Commissioned-art renderer smoke remains blocked on art assets. |
| **Code Coverage** | ~71% (backend/) |

---

## Next Steps

### 1. Bug Fixes - COMPLETE

Sessions 1-5 + follow-up + offer lifetime refactor are COMPLETE. No OPEN PL items remain in the current fix scope.

| Priority | ID | Summary |
|----------|-----|---------|
| ~~P1 - CRASH~~ | ~~PL-30~~ | ~~Godot null instance crash on diplomacy button after missed proposal result~~ **FIXED** |
| ~~P1 - DESIGN~~ | ~~PL-31~~ | ~~Capital-loss instant defeat still live + broken regression test~~ **FIXED** |
| ~~P2 - UX~~ | ~~PL-26~~ | ~~Combat feels hopeless, no clear path to winning~~ **FIXED** |
| ~~P2 - UX~~ | ~~PL-27~~ | ~~Diplomacy interrupt contract broken~~ **FIXED** (hard-stop/soft-stop taxonomy + mailbox panel) |
| ~~P2 - UX~~ | ~~PL-28~~ | ~~No warning before defeat, sudden game over~~ **FIXED** |
| ~~P2 - UX~~ | ~~PL-32~~ | ~~Raw diplomacy labels can leak into popups~~ **FIXED** |
| ~~P2 - UX~~ | ~~PL-33~~ | ~~"status" blocked by dialogue guard~~ **CLOSED** (duplicate of PL-27, verified) |
| ~~P2 - UX~~ | ~~PL-34~~ | ~~Queued diplomatic proposals can expire unseen~~ **FIXED** (eliminated; mailbox inbox) |
| ~~P3 - QOL~~ | ~~PL-29~~ | ~~No new game / restart endpoint~~ **FIXED** |
 
**Actionable now (no blockers):**

1. **Memory and Pressure v2.4.3 — B-Hegemony + B-B1-lite + B-B3 + B-B7 COMPLETE (April 22-23, 2026).** B-Hegemony substrate is live (bloc geometry, `power_score` / `bloc_power`, `describe_hegemon_bloc`, `33 / 50 / 60` surfaced-band memory, `balance_of_europe_shifted` beats, non-France-hegemon threat suppression, AI bandwagon at `50%+`, coalition leadership `bloc_share_against`, transient/save-field wiring). B-B1-lite (commits `5a5a265` + `ad3b582` + `341b22a`) shipped `hegemony_target_mod` + acceptance decoupling + proposal-preview `hegemony` warnings without leaking B-B4's grievance/composite-floor work. B-B3 retired the last alliance_paradox surface seams: deleted legacy `alliance_paradox_popup.{tscn,gd,gd.uid}` from Godot, updated `dialog_manager.gd:21` layer-registry comment, cleared the obsolete `TestM10AllianceParadoxTodo` skip stub, and added 9 focused regressions in `tests/test_commitment_paradox_rename.py`. **B-B7** shipped the standard Make Amends active-redemption verb per spec §8.6.1 and its review follow-up is closed: `make_amends` is wired through `VALID_ACTIONS`, parser, mock-LLM keyword routing (`make amends`, `offer amends`, `amends with`, `amends to`, `repair relations`, `offer reparations`, `send reparations`), both `free_actions` mirrors, the diplomatic executor routing tuple, and `_execute_make_amends` in `backend/commands/diplomatic_executor.py`. Pre-validation covers all seven refusal paths (missing target, self-use, WAR/ARMISTICE, cooldown, insufficient DP, insufficient gold, no active strikes); on success, deduct `200g + 1 DP`, remove the strike per the §8.6 rule (oldest matured `decays_on_turn` first; else lowest-severity with oldest-creation-turn tiebreaker — reading the pair's full `record["strikes"]` so matured-but-not-yet-decayed strikes stay reachable), prune empty pair records, `diplomatic_reliability["France"] += 2` clamped `[-100, 100]`, `world.modify_nation_relation(France, target, +5)`, and set `reparations_cooldown[diplo_key] = current_turn + 10`. New `reparations_cooldown: Dict[str, int]` field on `WorldState` with round-trip. `amends_offered` now carries its own emitted `episode_id` plus the cleared strike lineage and deterministic deltas across campaign log, dispatch queue, and notification details. Result text combines a Talleyrand frame with the target court's Voice-Bible committed acknowledgment line for all four cast diplomats (Castlereagh / Hardenberg / Metternich / Einsiedel), with a chancery fallback for non-cast nations per `COMMITMENTS_PRESENTATION_SPEC` §10.3. `AMENDS_REFUSAL_DISPLAY` map + `ACTION_DISPLAY` / `OBJECTION_DISPLAY` / `DEFIANCE_DISPLAY` entries close the R7 contract. `dispatch.py` `partial_on_nation` fog-check key list extended with `actor_nation` / `target_nation` for the new event. `SAVE_FORMAT_REFERENCE.md` line 233 updated from **Planned (B-B7)** to **landed**. 27 new tests in `tests/test_make_amends.py` + 3 enforcement-suite count adjustments now pin the surface metadata as well. Full suite `8655 passed, 1 skipped` (was `8625 passed, 1 skipped`; +30 net). Ruff clean on every B-B7-touched file.
2. **Memory and Pressure v2.4.3 — DG-4 + C-lite closure COMPLETE.** DG-4 call-to-arms is direct-only with root attacker/defender allies and direct vassals only; `war_entry_ledger` records honored/refused/vassal paths; all commitment notice families now use the shared §8.1 routing metadata; `resolve_named_diplomat()` is live; stale paradox-resolution dispatch callbacks are removed; notification review opens the commitments ledger target; the rail caps same-turn commitments notices above the fold; `balance_of_europe` is the live ledger payload and the legacy `threat_coalition` compatibility payload is retired.
3. **Scale Readiness Phase 2 — COMPLETE (April 19, 2026).** Distance cache, indexed helper seam, live AI fog path, indexed autonomous-evaluation scope, target-ratio attack-path cleanup, live-visibility cache, the `enemy_ai.py` raw scan-conversion pass, and plan §2.1's 100-region synthetic benchmark (`tools/benchmark_distance_cache.py`, pinned by 3 scale tests in `test_scale_readiness_phase2.py`) have all landed suite-green. Audit gaps the April 19 closeout surfaced are captured in `docs/SCALE_READYNESS.md` §11 (Verification Delta).
4. **Map Readiness Closure Pass — Phase 3 COMPLETE (April 19, 2026).**
   - **§3.1 Nation config factory — DONE.** `backend/nation_config.py` ships `DEFAULT_NATION_DEFAULTS` (gold=800, actions=3, authority=60) as the fallback baseline; per-nation override dicts retain current France/Britain/Prussia/Austria/Saxony entries. Helpers `_resolve_gold` / `_resolve_actions` / `_resolve_authority` route through override → default. `validate_runtime_nation_support()` accepts a nation with only a capital + diplomat (no economy override), but still errors on missing capital or diplomat. `backend/models/marshal.py` ships `create_marshal_from_data()` + `create_marshals_from_data()` factories fed by new `FRENCH_MARSHALS_DATA` / `ENEMY_MARSHALS_DATA` data lists; the legacy `create_starting_marshals()` / `create_enemy_marshals()` are now one-line wrappers preserving exact skills, abilities, biographies, and the cross-nation relationship matrix (including the intentional Uxbridge ↔ Austria non-entries the original code left unset). `backend/models/diplomat.py` ships `DIPLOMAT_DEFINITIONS` + `create_diplomat_from_data()`; `STARTING_DIPLOMATS` + `create_starting_diplomats()` are factory-derived. New `tests/test_nation_config_factory.py` (15 tests) pins factory behavior, default fallback semantics, validator acceptance of default-only new nations, and preservation of explicit overrides for the current roster.
   - **§3.2 Shared topology endpoint — DONE.** Backend `GET /map_topology` returns authored static topology (`adjacent`, `terrain`, `region_type`, `is_capital`, `starting_controller`, `grid_position`) plus `nation_capitals` from `REGIONS_DATA` / `NATION_CAPITALS`. Godot `map.gd` no longer defines `const REGION_CONNECTIONS`; adjacency is populated at runtime via `set_region_topology()` after `main.gd` fetches the payload. New `tests/test_map_topology_endpoint.py` (7 tests) pins endpoint parity; `tests/test_map_consistency.py` now rejects renamed or inline hardcoded adjacency tables (`test_map_gd_has_no_hardcoded_connections`).
   - **§3.3 Centralize nation colors — DONE.** `utils.gd` NATION_COLORS is now single-source; `map.gd`, `war_detail_popup.gd`, and `war_status_panel.gd` read from `Utils.NATION_COLORS` + `Utils.COLOR_ENEMY_DEFAULT` / `Utils.COLOR_CONNECTION` instead of local dicts. New `tests/test_gdscript_color_centralization.py` guards against drift (3 tests).
   - **§3.4 Prompt / parser / validator hardcoding — DONE.** `prompt_builder._get_regions_list()` now derives from `map_data` → `world.regions` → `REGIONS_DATA` instead of a 19-region string literal. `parser.CommandParser` now computes its fallback enemy roster via `create_enemy_marshals()` and reads live rosters from `world.marshals` when available, instead of a hardcoded 8-name list. (`validator.py VALID_NATIONS` was already derived from `NATION_CAPITALS` in Phase 1.)
5. **Map Readiness Closure Pass — §4.1 Province Registry Schema COMPLETE (April 19, 2026).**
   - **Placeholder JSON bumped to `schema_version: 2`.** `godot-client/project-sovereign/assets/maps/session8_placeholder_provinces.json` now carries `province_id`, `unit_anchor`, `label_anchor`, `garrison_anchor`, `building_anchor`, `wired`, and `interactive` on every region entry. All 19 current regions are `wired: true` + `interactive: true` with per-feature anchors collapsed to `anchor` for now — commissioned Europe art will author real per-feature offsets.
   - **Renderer consumes the new schema.** `map_renderer_base.gd::_build_province_shapes()` parses all new fields onto `province_shapes`. `_lookup_region_from_color_map()` returns `""` for non-interactive regions. `_refresh_hover_state()` also skips non-interactive entries in the distance-fallback path so hover and click agree. `update_all_regions()` pre-filters `map_data` into a `wired_data` dict before populating gameplay state (controllers, visibility, marshals, fogged forces, garrisons) — unwired provinces still render via the static-visuals path but never receive gameplay data. `update_region()` short-circuits on unwired regions for consistency.
   - **Test coverage.** `tests/test_map_placeholder_assets.py` grew from 4 → 12 tests pinning schema version, `province_id` presence + uniqueness, all four anchor fields, `wired` / `interactive` flag presence + types, current-roster invariants, and renderer-side consumption of both the new flags and the new anchor fields. Four §4.1-specific tests in `tests/test_map_renderer_cutover.py` now pin `region_full_data = wired_data` plus the `wired` gate inside `update_all_regions()`, the unwired-dict erasure block inside `update_all_regions()`, the unwired early return inside `update_region()`, and the non-interactive skip in `_refresh_hover_state()`'s distance fallback.
6. **Map Readiness Closure Pass — §4.2 External Bitmap Loading COMPLETE (April 19, 2026).**
   - **Renderer gained bitmap-ingest seam.** `map_renderer_base.gd` now declares two subclass hooks (`_get_map_visual_bitmap_path()` + `_get_map_lookup_bitmap_path()`, both default `""`) and `_load_map_images() -> bool`. The loader now resolves imported bitmap assets through `ResourceLoader` / `Texture2D.get_image()` rather than `Image.load(res://...)`, rejects visual/lookup size mismatches, validates that lookup-map colors match `province_color_lookup`, and on success binds `visual_map_texture` + `province_lookup_image` while overriding `map_origin = Vector2.ZERO` + `map_canvas_size = bitmap.get_size()` so authored anchors are interpreted in bitmap-pixel coords.
   - **Fallback preserved, but failures stay loud.** `_build_map_textures()` calls the loader first and returns early on success; on failure the existing circle-generation block runs unchanged. Explicit bitmap opt-in failures now `push_error()` once per unique message instead of spamming every `_ready()`. `map.gd` does NOT override the bitmap hooks, so the 19-region placeholder scene always runs on circles.
   - **Hit-test path unchanged.** `_lookup_region_from_color_map()` still reads `province_lookup_image.get_pixel()` keyed into `province_color_lookup`, with the §4.1 `interactive`/`wired` gates intact.
   - **Test coverage.** `tests/test_map_renderer_cutover.py` now pins the hook declarations, exported-build-safe resource loading, runtime lookup-color validation, failure latching, and loader ordering inside `_build_map_textures()`. `tests/test_map_placeholder_assets.py` still pins that placeholder `map.gd` never opts into bitmap mode. New `tests/test_map_bitmap_contract.py` adds fixture-driven behavioral coverage for missing-file fallback, lookup-color rejection, size mismatch rejection, and hit-test round-trips with `map_origin = Vector2.ZERO`.
7. **Map Readiness Closure Pass — §4.3 Color-Map Validator COMPLETE (April 19, 2026).**
   - **Tool shipped.** `tools/validate_province_map.py` is a standalone offline acceptance gate for commissioned art deliveries. Invoke as `.venv\Scripts\python.exe -m tools.validate_province_map --registry <json> [--visual <png> --lookup <png>]`. Stdlib-only (no Pillow / no project imports beyond the registry JSON), so it can run in CI without backend dependencies and before any backend code loads.
   - **Six failure codes, two severities.** Errors: `SENTINEL_COLLISION`, `DUPLICATE_LOOKUP_COLOR`, `SIZE_MISMATCH`, `MISSING_PROVINCE`, `INSUFFICIENT_COVERAGE` (default minimum 50 pixels per province), `UNMAPPED_COLOR` (with pixel count and sample `(x, y)` coordinate). Warning: `TINY_ISLAND` (each connected color island, mapped or not, with `1 <= count < tiny_island_threshold`; default threshold 5). Sentinel pixels (`no_province_color`) never produce findings. Errors collected per-pass instead of bailing on the first failure, so a commissioned-art delivery triages in one report.
   - **Differs from the §4.2 runtime loader on purpose.** `SIZE_MISMATCH` mirrors the runtime check but reports both sizes for art-pipeline triage. `UNMAPPED_COLOR` collects ALL strays + counts where the runtime stops at the first. `TINY_ISLAND`, `INSUFFICIENT_COVERAGE`, `MISSING_PROVINCE` (distinct from "exists but tiny"), and `DUPLICATE_LOOKUP_COLOR` have no runtime equivalent.
   - **CLI contract.** `--registry` required; `--visual` + `--lookup` paired or omitted (registry-only mode is the cheapest gate, runs without any PNG). Empty registries now fail fast in that mode. `--min-coverage-pixels` and `--tiny-island-threshold` tune the two thresholds per delivery. `--json` emits `{ok, error_count, warning_count, failures[]}` for CI; `--strict` promotes warnings to a non-zero exit. Exit codes: `0` pass / `1` validation failure / `2` bad input.
   - **PNG decoder scope.** Pure-Python (`zlib` + `struct`); supports 8-bit RGB (color_type 2) + RGBA (color_type 6) with all five PNG scanline filters (None / Sub / Up / Average / Paeth). The visual PNG size check is header-only to avoid wasteful full-image decode; the lookup PNG path still fully decodes and now wraps malformed IHDR payloads, truncated chunk bodies, and corrupt IDAT streams as clear `PNGDecodeError`s. Pillow intentionally NOT a dep — stays inside the project's existing `requirements.txt`.
   - **Test coverage (29 tests, `tests/test_province_map_validator.py`).** Registry checks (placeholder passes; sentinel collision; duplicate colors; loader rejects malformed inputs and empty registries); image checks (clean baseline; size mismatch; missing province; insufficient coverage; connected tiny islands still fire when repeated specks of the same stray color exceed the threshold in aggregate; high-pixel-count unmapped stays error-only; sentinel pixels ignored; alpha is dropped before comparison; RGB-only PNGs decode); CLI exit codes 0/1/2 + `--json` + `--strict`; PNG decoder rejects interlaced + non-PNG signatures, supports multiple IDAT chunks, rejects malformed IHDR payloads / corrupt streams, and round-trips a 5-row RGBA fixture exercising all five filter types.
   - **Verification.** Focused map suite: **65 passed** (`tests/test_map_renderer_cutover.py tests/test_map_placeholder_assets.py tests/test_map_bitmap_contract.py tests/test_province_map_validator.py -q`). Full Python suite: **8487 passed, 2 skipped** (was `8453`). Ruff clean on the new files. The shipped placeholder JSON passes registry-only checks (pinned by `test_placeholder_registry_passes_registry_only_checks`).
   - **Suggested next cold-start sequencing:** §4.4 (unwired province support — grey-tint + "(not yet in play)" hover copy now that the schema carries the flag). A fresh session only needs CLAUDE.md + this STATUS.md + `SCALE_READINESS_PLAN.md` §4.
8. **Map Readiness Closure Pass — §4.4 Unwired Province Support COMPLETE (April 19, 2026).**
   - **Overlay seam in `map_renderer_base.gd`.** Three coordinated additions: (a) constants `UNWIRED_GREY_COLOR := Color(0.32, 0.32, 0.34, 1.0)`, `UNWIRED_GREY_BLEND: float = 0.7`, `UNWIRED_TOOLTIP_SUFFIX := "(not yet in play)"` pin the visual contract in one place; (b) `_is_region_wired(region_name) -> bool` + `_unwired_lookup_keys() -> Dictionary` resolve the gate through `province_shapes` and default to wired when a region is unknown, preserving legacy callsites; (c) `_apply_unwired_grey_overlay(visual_image, lookup_image)` stamps a straight `base.lerp(UNWIRED_GREY_COLOR, UNWIRED_GREY_BLEND)` over every visual pixel whose lookup key belongs to an unwired province, with size-mismatch + empty-set short-circuits keeping the placeholder path zero-cost.
   - **Overlay runs on BOTH texture-build paths.** `_build_map_textures()` now calls the overlay AFTER `province_lookup_image = color_map` but BEFORE `visual_map_texture = ImageTexture.create_from_image(visual_image)` in the circle fallback, and `_load_map_images()` does the same for the bitmap path (after `province_lookup_image = lookup_image`, before texture creation). Unwired tint therefore applies uniformly regardless of whether art is commissioned or dev-mode circles.
   - **Click gate lives in `MOUSE_BUTTON_LEFT`.** The handler still resolves `clicked_region` via `_lookup_region_from_color_map(...)` (then the `hovered_region` fallback), but now short-circuits on `not _is_region_wired(clicked_region)` BEFORE emitting `region_clicked`. The existing `interactive` gate inside `_lookup_region_from_color_map()` is untouched — hover identification for unwired-but-interactive provinces keeps working.
   - **Tooltip dispatch has a dedicated unwired branch.** `_draw()` evaluates `elif hovered_region != "" and not _is_region_wired(hovered_region):` BEFORE the existing `region_full_data.has(hovered_region)` branch, because §4.1's `update_all_regions()` deliberately excludes unwired regions from `region_full_data`. `_draw_unwired_region_tooltip()` renders the province name + `UNWIRED_TOOLTIP_SUFFIX` against a distinct dim panel so authors never mistake an unwired province for a fogged one.
   - **Test coverage (16 tests).** `tests/test_map_renderer_cutover.py` gains 10: constants pinned, `_is_region_wired` contract, `_apply_unwired_grey_overlay` body shape, `_unwired_lookup_keys` construction, overlay-before-texture ordering in both paths, click-gate precedence (guard before `emit`), `_draw()` branch order (unwired before `region_full_data.has`), the unwired tooltip helper renders both lines, and a negative assertion that `_lookup_region_from_color_map()` never gates on `wired`. New `tests/test_map_unwired_overlay.py` adds 6 behavioral fixture tests: constants mirror, all-wired no-op, selective blend, pure-tint target, sentinel ignored, and a source-level guarantee that `_unwired_lookup_keys()` cannot emit the sentinel by construction (it only iterates `province_color_lookup`, which `_build_province_shapes()` never seeds with the sentinel).
   - **Verification.** Focused map suite: **94 passed** (`tests/test_map_renderer_cutover.py tests/test_map_placeholder_assets.py tests/test_map_bitmap_contract.py tests/test_map_unwired_overlay.py tests/test_map_consistency.py tests/test_map_topology_endpoint.py tests/test_province_map_validator.py`). Full Python suite: **8503 passed, 2 skipped** (was `8487`). Ruff clean on the modified + new files.
   - **Suggested next cold-start sequencing:** Map readiness is now art-blocked only. The last remaining map-readiness work is commissioned art + final renderer smoke validation. A fresh session working on diplomacy should pick up the Memory and Pressure v2.4.3 implementation slices directly.
9. **Memory and Pressure v2.4.3 — Block 3 folded; implementation closure current.** `docs/audits/MP_V243_BLOCK1_DOC_CLEANUP.md` and `docs/audits/MP_V243_BLOCK2_SUBSTRATE.md` are complete. `docs/audits/MP_V243_BLOCK3_BLOC_NAMING.md` is SUPERSEDED — its bloc-naming contract was folded into `COMMITMENTS_PRESENTATION_SPEC.md` §8.1a, its per-band voice contract was folded into `DIPLOMAT_VOICE_BIBLE.md`, and its CF1-CF4 closure items have now landed in their owning slices. No separate Block 3 consumption pass is required.

## Real Map Readiness Gate

**Definition:** "Real map ready" means the project can safely start a commissioned Europe-map integration or a full-Europe wiring pass without knowingly carrying unresolved scale or asset-pipeline blockers. It does **not** mean only "Phase 2 is done," and it does **not** mean only "art has arrived."

**Do not call the project real-map ready until all of the following are true:**

1. **Phase 2 spatial-index conversion is complete.**
   - `backend/ai/enemy_ai.py` has no remaining scale-sensitive hot-path scans that still depend on broad `world.marshals.values()` / `marshals.values()` iteration where indexed region helpers are the intended path.
   - Full Python verification stays green after the final conversion batch.

2. **Scale-facing nation / region data flow is fully data-driven.**
   - Adding nations or regions no longer depends on lingering hand-authored factory duplication or shell-sized prompt / parser assumptions.
   - Prompt/parser fallback strings, nation-color ownership, and adjacency ownership are now data-driven. Nation/runtime config factory work (§3.1) is the last remaining Phase 3 gap before this gate is satisfied.

3. **Frontend/backend topology has one authoritative source.** ✅ DONE (April 19, 2026 — §3.2).
   - `map.gd` no longer carries a hardcoded gameplay adjacency graph. Shared topology comes from backend `GET /map_topology` (authored from `REGIONS_DATA`) as described in `SCALE_READINESS_PLAN.md` §3.2.

4. **The renderer has a production province registry schema.** ✅ DONE (April 19, 2026 — §4.1).
   - Province metadata includes stable `province_id`, separate anchors for unit / label / garrison / building placement, and explicit `wired` / `interactive` flags. Placeholder JSON is at `schema_version: 2`; renderer parses all new fields and gates hover/click/gameplay-state on `interactive` / `wired` respectively.
   - Placeholder-only `anchor` + `radius` data is no longer the limiting runtime contract for Europe-density map content.

5. **The renderer can load external bitmap assets.** ✅ DONE (April 19, 2026 — §4.2).
   - `map_renderer_base.gd` now exposes `_get_map_visual_bitmap_path()` / `_get_map_lookup_bitmap_path()` hooks plus `_load_map_images()` loader; `_build_map_textures()` prefers bitmaps when both paths resolve and fall back to circle generation otherwise. Hit detection still goes through `province_lookup_image.get_pixel()` keyed into `province_color_lookup` as §4.1 established.
   - Placeholder circle generation remains available as the dev-mode fallback (the placeholder scene never opts in to bitmap mode), but it is no longer the only renderer path.

6. **Bitmap validation exists and runs before art integration.** ✅ DONE (April 19, 2026 — §4.3).
   - `tools/validate_province_map.py` checks dimension match, unknown colors, missing provinces, sentinel misuse, duplicate lookup colors, insufficient coverage, and connected tiny pixel islands. CLI emits human-readable or `--json` reports with exit code `0`/`1`/`2`; `--strict` promotes warnings.
   - The validator is stdlib-only (no Pillow), so it runs in CI and before any backend code loads. The shipped placeholder JSON passes all registry-only checks.

7. **Unwired province support exists in the runtime contract.**
   - The renderer can show visible-but-not-yet-playable provinces in a greyed-out / non-interactive state.
   - Hover/click behavior and `update_all_regions(map_data)` explicitly ignore unwired provinces for gameplay.

8. **Commissioned-art integration and smoke verification are closed once assets exist.**
   - After art delivery, the project still needs the art-backed renderer cutover plus final Godot smoke validation.
   - This step is the one genuinely blocked on art assets; the prerequisites above are not.

**Important routing clarification:** completing Scale Readiness Phase 2 should move the project from "engine hot path still incomplete" to "ready for the non-art map-readiness closure pass." It should **not** be read as "safe to start commissioned Europe map integration immediately."

**Complete:**

- **Scale Readiness Phase 0 — Design Gates + Implementation-Closure Pass.** COMPLETE (April 17, 2026; DG-4 amended April 25). `DG-1` current 1805 draft roster = 13 independent nations with 20+ headroom, but that roster is authored scenario content rather than an engine cap; `DG-2` bilateral diplomacy with force-expand salience rules, weighted-sum salience score, and locked 5-row expanded cap; `DG-3` supply lines deferred; `DG-4` direct-only bilateral call-to-arms with no transitive cascade, qualifying-treaty list locked (`ALLIANCE`, `DEFENSIVE_ALLIANCE`, vassal), concrete DG-4 event families (`call_to_arms_refused_offensive`, `call_to_arms_refused_defensive`, `call_to_arms_honored_costly`, `war_entry_ledger`); `DG-5` no mandatory hard victory condition for Europe-map readiness, with any future objectives authored per scenario; `DG-6` scenario-configured pacing with `scenario_schema_version: 1`, hybrid `base_ap.by_nation` + `base_ap.by_tier_default`, optional `objectives_profile`, and structured `cascade_profile`; `DG-7` categorized dispatch with priority escalation. Cross-cutting taxonomy is now canonical and single-sourced to `SCALE_READINESS_PLAN.md` §"Phase 0 Cross-Cutting Taxonomy": `power_tier` is authored scenario data with enum `major / secondary / minor`, never runtime-derived; runtime numeric strength signals use a separate `power_score` field. Older `great_power / secondary_power / minor_power` language in historical design docs carries supersede markers pointing to the canonical section; current settlement specs use `major / secondary / minor`.
- **Scale Readiness Phase 1 — Test Safety Net.** COMPLETE (April 17, 2026). 11 new tests (8 nation config + 3 adjacency), all hardcoded `== 19` removed from test assertions, validator VALID_NATIONS derives from NATION_CAPITALS.
- **Scale Readiness implementation-closure clarification pass.** COMPLETE (April 17, 2026). `SCALE_READINESS_PLAN.md` now explicitly requires authored scenario records (`player_nation`, `home_capital`, `player_sphere`, `nations`, `counts_for_strategic_power`), defines `protectorate` as a DG-2 subject state but not a DG-4 auto-entry state, locks DG-2's shared salience helper + tiebreak order, aligns DG-4 with a later surfaced attacker-side ally-entry seam, removes any implied 13-nation engine cap, and makes hard campaign objectives optional scenario content rather than a required Europe prototype dependency.

**Blocked:**

- **Session 8 Renderer — final art-backed cutover.** Commissioned art-backed renderer swap and Godot smoke validation. Blocked on art assets.
- **Real map readiness final sign-off.** Blocked on both the remaining non-art map-readiness gate items above and, after those land, the commissioned art-backed cutover.

**Audit context:** `docs/SCALE_READYNESS.md` is the historical snapshot of the original 17-finding Europe-scale audit (audit date April 16, 2026). Current non-art map-readiness closure state is tracked in the "Actionable now" block above (§§3.1-3.4 + §§4.1-4.4 are all DONE). Treat the `SCALE_READYNESS.md` "NOT DONE" entries for §§3.1, 4.1-4.4 as frozen audit context, not current routing — the `SCALE_READINESS_PLAN.md` per-item contract + `STATUS.md` closure entries are the source of truth.

**Next diplomacy workflow:** **Slice B3 lifecycle (per-turn staying-power + war-entry seam wiring + archive compaction + retention + full-Europe fixtures).** Slices A1 + A2 + A3 + B1 + B2 (ordering guard + battle emitter wiring + non-battle emitters) are landed. B2 non-battle emitters shipped May 2-3, 2026: `accrue_occupation_event(...)` for the four kinds (`enemy_region_captured` 20 / `enemy_capital_captured` 40 / `allied_region_restored` 15 / `liberated_region_restored` 15; `treaty_transfer` is logged with 0 points per spec §9.2 line 641), `emit_capture_occupation_event(...)` capture-path wrapper (classifies via `NATION_CAPITALS` and `get_starting_controllers()`), `accrue_support_event(...)` for gold/subsidy/AP/manpower/access/supply with per-`(war_id, supporter, support_kind)` access/supply cap of 5 raw and deterministic episode-id dedupe, and `resolve_british_subsidy_war_id(...)` (`unique_eligible` / `matching_coalition_target` / `highest_overlap` / `oldest_sequence` / `unattributed_subsidy`). Wired call sites: `world_state.capture_region()`, `world_state._ratify_treaty()` (`gold_lump` support + `territory_cede` allied restoration + vassal liberation per-region), `world_state._process_treaty_clauses()` (per-turn gold/manpower/AP), `coalition._process_british_subsidy()`. 33 new tests in `tests/test_war_contribution_scores.py`; verification `9490 passed, 1 skipped`; ruff clean. Active settlement work continues from `docs/WAR_SETTLEMENT_ALLY_PARTICIPATION_IMPLEMENTATION_PLAN.md` Slice B "B3 lifecycle, retention, and full-Europe fixtures" and `docs/WAR_SETTLEMENT_ALLY_PARTICIPATION_SPEC.md` §9.5 + §7.5.

**Peace Deals deferred carry-forward gate:** CLOSED by the approved Peace Deals umbrella spec. The "Deferred Carry-Forward Checklist" in `docs/PEACE_DEALS_UMBRELLA_SPEC.md` assigns or explicitly defers WB-D bargain presentation, response routes, bargain icons, fulfillment/breach badges, region-observer witness scope, scope-branched witness copy, and callback/aftermath architecture.

**Deferred-item routing plan (reviewed Apr 26 after WPS-A audit follow-up):**

| Deferred item | When to do it | Why / gate |
|---------------|---------------|------------|
| D3 per-row bloc stamps / member badges | **DONE Apr 25 as the pre-Peace polish pass.** Keep future work limited to smoke/playtest copy or color tweaks unless a new readability problem appears. | Live payload is transient `nations[*].bloc_stamp`, never serialized, and reuses `describe_hegemon_bloc(...)`. Priority is `[Coalition Member] > proper bloc name > descriptive bloc > Vassal > Neutral`. |
| D1 advisory-first strategic focus + deeper AI integration | Later Nation Agendas / Motive Legibility or Talleyrand Desk work, not before Peace Deals. | This is strategic behavior and advisory framing, not a missing closure item from Memory and Pressure. It needs a spec that owns AI motives and recommendation surfaces. |
| D2 non-France-hegemon / per-target coalition generalization | Later scale or scenario-generalization pass, or inside a Peace Deals spec only if that spec requires per-target pressure for settlement logic. | Current v0.1 Balance pressure is honest for the player-nation target. Generalizing the scalar is wider than badges and should not be pulled forward for presentation polish alone. |
| WB-D bargain presentation, response routes, bargain icons, fulfillment / breach badges | During War Bargains WB-D inside the Peace Deals track, after Bilateral Peace Hardening, War Purpose + Score Semantics, and WB-A/WB-B/WB-C are specified and implemented. | These surfaces depend on live bargain records and settlement/war-purpose semantics. Doing the presentation layer before the mechanic would create placeholder UI with no durable state behind it. |
| Region-observer witness scope and scope-branched witness copy | During WB-D or the same Peace Deals slice that introduces bargain/claim observer state. | `region_observer` is intentionally inactive until the bargain/claim store exists. The witness copy should branch on real bargain/claim relationships, not invented presentation-only context. |
| Richer callback / N+1 aftermath architecture | Only if Peace Deals / War Bargains playtest proves same-turn notices are too flat for bargain breach, fulfillment, or settlement fallout. | Memory and Pressure closed with same-turn routing and advisory review targets. Delayed callbacks should be justified by new Peace Deals events with real follow-up choices. |
| Retroactive campaign-log renaming and compound member-list bloc names | Archive / expansion polish after D3, not part of Peace Deals entry criteria. | These improve historical display texture but do not unblock diplomacy mechanics, peace terms, or player decisions. |

**Current Session 7 progress:** COMPLETE. `backend/nation_config.py` now centralizes scenario/runtime nation defaults, non-France campaigns preserve their player nation through world init + `/new_game` reset paths, diplomacy/advisory/template/defiance flows now derive state and proposal ownership from `world.player_nation`, enemy AI scale-sensitive contact scans route through cached fog-aware helper seams, and modding/scenario validation now fails unsupported nation rosters before load.

**Current Scale Readiness Phase 2 progress:** COMPLETE. `backend/models/world_state.py` ships deque-based `get_distance()`/`find_path()`, a symmetric distance cache plus explicit invalidation seam, AI-safe indexed region helpers, and a live-visible-region cache that invalidates with marshal-index rebuilds. `backend/ai/enemy_ai.py` refreshes marshal indexes at evaluation-scope boundaries, guards direct-entry helpers against stale indexed reads, routes non-player nation contact queries through live nation-perspective visibility while letting long-range P7/stagnation movement steer off hostile-controlled regions instead of hidden marshal positions, enters indexed scope for `decide_single_action()`, uses indexed marshal-priority helpers for turn ordering, and covers the late artillery / coordination helper band plus the ally-support / fortification / stagnation / attack-ratio / homeland-defense / undefended-capture / consolidation / default-retreat follow-up band with indexed same-region / adjacent-region lookups. `enemy_ai.py` has `0` remaining direct `world.marshals.values()` / `marshals.values()` scans. Plan §2.1's 100-region synthetic benchmark now ships as `tools/benchmark_distance_cache.py` (10x10 4-neighbor grid, ~34x cached vs. uncached on typical hardware, invariance contract asserted) and is pinned by three new scale tests in `tests/test_scale_readiness_phase2.py` (now 28 tests, all green). Suite-level regression verification remains green (`8410 passed, 2 skipped` pre-benchmark; incremental Phase 2 file verified post-benchmark).

**Current Session 8 progress:** Cutover slices 1-3 + §§4.1-4.4 COMPLETE. The map renderer now builds shared scene-node layers behind `map_renderer_base.gd`, keeps the existing `map.gd` data wrapper and `update_all_regions(map_data)` contract stable for `main.gd`, includes a placeholder province-definition asset plus hidden color-map lookup path for the current 19-region map, isolates the map world inside a `SubViewport` with native `Camera2D` zoom/pan plus world-bound clamping, carries the production province-registry schema (`province_id`, per-feature anchors, `wired`, `interactive`), can ingest imported bitmap assets through an exported-build-safe loader with runtime lookup-color validation plus circle fallback, ships an offline color-map validator (`tools/validate_province_map.py`, 29 tests) that gates commissioned-art deliveries on sentinel collisions, duplicate colors, dimension mismatches, missing/insufficient province coverage, connected tiny pixel islands, and unmapped colors, and paints unwired provinces with a grey-tint overlay + dedicated "(not yet in play)" tooltip while short-circuiting clicks before `region_clicked` is emitted. All non-art Session 8 work is closed. Remaining art-dependent work is the commissioned art-backed renderer swap and final Godot runtime smoke validation.

**Routing note:** later references in this file to older "Session 7", "Session 8", or "Session 8A" Phase 8 diplomacy milestones are archival implementation history. They do not override the active post-bug routing above, which is currently: (1) Scale Readiness Phase 2 — DONE, (2) non-art map-readiness closure pass (§§3.1-3.4 + §§4.1-4.4) — DONE, (3) art-backed renderer cutover — blocked on art assets. Real-map readiness as a whole is therefore art-blocked only.

**Implementation sessions in current order:**

| Session | Scope | Items | Status |
|---------|-------|-------|--------|
| Session 1 | Stability + defeat truth | `PL-30`, `PL-31` | **COMPLETE** |
| Session 2 | Diplomacy interrupt contract | `PL-27`, `PL-34`, `PL-33` duplicate check | **COMPLETE** |
| Session 2 follow-up | Mailbox inbox + queue elimination | `PL-27`/`PL-34` hardening | **COMPLETE** |
| Session 2 refactor follow-up | Current-turn diplomatic offer lifetime | `PL-27`/`PL-34` follow-up | **COMPLETE** |
| Pre-Session 3 | Informational notices + light UI polish | `docs/INFORMATIONAL_UI_PLAN.md` | **COMPLETE** |
| Session 3 | Diplomacy display contract | `PL-32` | **COMPLETE** |
| Session 4 | First-hour pressure cleanup | `PL-28`, `PL-26` | **COMPLETE** |
| Session 5 | Restart flow | `PL-29` | **COMPLETE** |
| Session 6 | Response and popup contract hardening | `/command` response pipeline, typed dialogue migration, popup routing registry, turn-manager popup flush workaround cleanup, `/command` follow-up reduction | **COMPLETE** |

### 2. Completed Diplomacy Foundations (reference for future planning)

These changes are live. Treat them as the baseline when planning follow-up diplomacy work.

| Foundation | What shipped |
|------------|--------------|
| Diplomacy interrupt contract | `PL-27` split hard-stop vs soft-stop diplomacy, stopped ordinary commands from being blocked by envoy decisions, and moved recovery onto typed flows instead of parser-only fallbacks. |
| Envoys inbox / mailbox flow | Session 2 follow-up shipped the browsable Envoys inbox/mailbox panel, `GET /mailbox`, `POST /mailbox/activate`, stable mailbox identity, and consolidated pending-count ownership under `dialogue_manager.get_mailbox_count()`. |
| Queue elimination + offer lifetime refactor | `world.diplomatic_queue` is gone. The follow-up refactor replaced cross-turn mailbox persistence with current-turn envoy items: `Not Now`, same-turn reopen, and end-turn lapse instead of silent long-lived queue state. |
| Diplomacy display contract | `PL-32` centralized proposal/clause display ownership in backend formatters (`backend/display_names.py`) so popups and recovery payloads no longer maintain divergent token maps. |
| Response / popup refactors | Session 6 moved `/command` onto `build_base_response()`, finished typed dialogue migration for remaining diplomacy popups, and replaced hand-ordered modal routing with the popup registry/dispatcher path in `main.gd`. |

**Planning note:** do not assume the old pre-mailbox model still exists. Diplomacy refinement should build on Envoys inbox + same-turn lapse + backend-owned popup labels, not on the removed `diplomatic_queue` / cross-turn mailbox behavior.

### 3. Architecture Hardening (before full-map work)

GPT audit confirmed the codebase is "fragile but manageable" at 19 regions but NOT ready for 80-100 region expansion. These items need a plan before full-map implementation starts:

| Item | Summary | Current Home |
|------|---------|-------------|
| Map renderer replacement | Circle-based prototype won't scale | ROADMAP.md (art-blocked) |
| `/command` response pipeline | **FIXED Apr 12, 2026 (Session 6 slice 1).** Main path now starts from `build_base_response()` with explicit popup/notification deferral flags. | This session |
| AI fog-aware queries | **FIXED Apr 12, 2026 (Session 7).** Scale-sensitive AI contact scans now route through cached fog-aware helper seams. | This session |
| Hardcoded nation defaults | **FIXED Apr 12, 2026 (Session 7).** Shared nation config now owns runtime defaults and non-France campaign resets. | This session |
| Popup routing registry | **FIXED Apr 12, 2026 (Session 6 slice 3).** `main.gd` now routes modal response precedence through ordered registries plus a shared dispatcher. | This session |
| Typed dialogue migration | **FIXED Apr 12, 2026 (Session 6 slice 2).** Remaining diplomacy popups now route through typed endpoints/actions, including a dedicated typed objection path. | This session |

**Needs:** A consolidated pre-expansion plan that sequences these items after Sessions 1-5. `docs/SCALE_READYNESS.md` now holds the current risk register and smaller-map assumption list for that planning pass. The GPT audit priority roadmaps in `docs/GPT_AUDIT_PLAN_RESULTS.md` now cover both the broader architecture audit and the focused diplomacy attention / legitimacy audit. No item from this list moved earlier as a full prerequisite for the current bug sessions.

**Post-bug architecture sessions:**

| Session | Scope | Items |
|---------|-------|-------|
| Session 6 | Response and popup contract hardening | `/command` response pipeline **COMPLETE**; typed dialogue migration **COMPLETE**; popup routing registry **COMPLETE**; turn-manager popup flush workaround cleanup **COMPLETE**; `/command` follow-up reduction **COMPLETE** |
| Session 7 | Scale-sensitive backend hardening | AI fog-aware queries, hardcoded nation defaults, config-completeness and large-scenario validation tests **COMPLETE** |
| Session 8 | Renderer cutover prep and replacement | Map renderer replacement on the current 19-region map or placeholder assets; keep `update_all_regions(map_data)` stable during swap |

These are existing audit items broken into implementation order. They stay after the current bug sessions and before any full-map expansion work.

### 4. Design Refinement (next diplomacy spec queue)

`docs/DESIGN_REFINEMENT.md` is now a post-fix planning doc, not a blocked-during-bugs list. The old prerequisites are complete: `PL-27`, `PL-34`, `PL-32`, the Envoys inbox/mailbox follow-up, the current-turn offer lifetime refactor, and the Session 6 response/popup refactors are all live. The next diplomacy work should be spec-first.

| Order | Spec Track | Bundles / Source Items | Note |
|-------|------------|------------------------|------|
| 1 | Memory and Pressure v2.4.3 | `R160`, `R119` | First diplomacy follow-up closure block. Blocks 1 and 2 are complete; Block 3 is SUPERSEDED; DG-4 reliability, C-lite presentation closure, the April 25 post-closeout smoke fixes, and D3 Balance/ledger row stamps are now live. Remaining work is commissioned-art smoke outside this diplomacy gate. |
| 2 | Bilateral Peace Hardening | separate peace UX, bilateral peace term ownership, promise-breach warnings, peace-preview clarity | Must land before any ally-aware settlement flow OR before war bargains. **Needs dedicated spec.** |
| 3 | War Purpose + Score Semantics | War Objectives + Ticking War Score, Vassalage Power Cap, Forced Alliance, Liberation | Define why wars start, what score means, and what settlements can legitimately do. Do **not** bundle this directly with common peace. **Needs dedicated spec.** |
| 3.5 | War Bargains | `R151`, full `war_bargain` mechanic split out of `Reliability + Commitments` v1.0 in April 16 rescope | The bilateral named-enemy promise mechanic. `docs/WAR_BARGAIN_SPEC.md` v1.0. All slices COMPLETE: WB-A (data + creation), WB-B (lifecycle), WB-C (war-entry contract + AI rules + `repudiate_bargain`), WB-D (presentation: commitments routing, notifications, witness scope, voiced templates, response routes, ledger badges). **Depends on items 1-3.** ~160 tests. |
| 4 | Ally Participation + Common Peace | ally beneficiaries, contribution / consultation rights, common peace routing, settlement fallout | Post-bargains track. `docs/WAR_SETTLEMENT_ALLY_PARTICIPATION_SPEC.md` and implementation plan own the full-Europe handoff. **Slice A1 foundation and Slice A2 war-entry threading are landed.** Next work is Slice A3 merge/archive/leader invariants. |
| 5 | Nation Agendas + Motive Legibility | `R155`, `R156`, `A3`, `R123`, `R124` | Make nations feel nation-smart, not threshold-smart: agendas, diplomacy-vs-war choice, visible motives, and isolation behavior. **Needs dedicated spec.** |
| 6 | Talleyrand Desk + Explanation Layer | `R131`, `R132`, `R17d`, `R17e`, `R17f`, `R157`, `R159` | Unify cooldown warnings, relationship/vassal trends, mission projections, and explanatory advisory text into one desk/explanation surface. **Needs dedicated spec.** |
| 7 | Economic Diplomacy | `R161` plus B4 diplomacy-facing gold-sink candidates | Build the non-coercive diplomatic economy around reciprocal trade, subsidies, and pressure, not disconnected one-off features. **Needs dedicated spec.** |

**Peace Deals spec gate (May 2):** BPH-A through BPH-D, WPS-A through WPS-D, and WB-A through WB-D are landed. Ally Participation/Common Peace has `WAR_SETTLEMENT_ALLY_PARTICIPATION_SPEC.md` and `WAR_SETTLEMENT_ALLY_PARTICIPATION_IMPLEMENTATION_PLAN.md` covering full-Europe scale. **Slice A1 foundation gate and Slice A2 war-entry threading are landed:** A1 shipped settlement containers, save/load defaults, empty-safe indexes, cache invalidation, cached-helper elimination refactor, invariant assertions, and the 20-active-`war_instance` synthetic fixture; A2 shipped `war_id` create/reuse/attach ownership across inventoried WAR-entry seams, with review follow-up commit `497791d` closing cascade/debug/armistice/counter-bargain hard-stop gaps. Full pytest suite at `9350 passed, 1 skipped`; ruff clean. Next is Slice A3 merge/archive/leader invariants.

**Audit call (Apr 13):** the existing conversational diplomacy wizard should remain the home for normal diplomacy, separate peace, bilateral commitment review, and the future bilateral peace-hardening pass. Common peace, ally beneficiaries, and conference-style settlement allocation should route through a dedicated wartime settlement flow instead of being overloaded into the nation -> proposal -> terms loop.

**Audit amendment (May 5):** typed diplomacy is no longer an acceptable primary path for common peace. The diplomacy wizard should be the entry point that opens the dedicated wartime settlement flow. The current gap is routing, not design intent: wizard `open_settlement` must stage `settlement_confirm`, and the backend wizard availability check must recognize default-start WAR pairs through the same lazy backfill used by `propose_common_peace`.

**Common-peace UI audit expansion (May 5):** war-score HUD/detail surfaces also need settlement-aware polish. Clicking a bilateral war-score row opens `war_detail_popup.gd` and then `[Negotiate Peace]` opens the diplomacy wizard, so the wizard fix should make the bilateral path usable. But multi-participant wars are still presented as bilateral rows from `diplomatic_states`, with `war_instance_id` only attached opportunistically; there is no explicit "Open Settlement" CTA on the war-detail popup, no coalition/whole-war settlement CTA in the coalition detail view, and the coalition detail currently offers only `Target X` buttons for non-leader members. Backend `war_status.py` contribution rows are empty for default-start WAR pairs until a war_instance is created, which can make the settlement standing tooltip look absent at exactly the moment the player is deciding whether to negotiate. Ledger recent-settlement rows are wired, but `diplomatic_ledger.gd` still renders `terms_summary[0]` and expanded term `type` values directly, so raw `territory_cede` / underscored settlement codes can leak there even after dispatch/campaign-log one-liners were humanized. Next closure should cover F1 wizard, war-score detail handoff, coalition detail handoff, and ledger term humanization together.

**Follow-up note:** `R162` (AI ultimatums to player) is no longer blocked by the old attention contract, but it should still wait until the commitment and agenda specs above exist. It adds interruption surface before the underlying diplomacy has enough political weight.

**Mechanical substrate note (Apr 15):** the live diplomacy layer now records treaty rupture as a remembered political event when war shatters an active commitment, including the injured party, breach reason family, witness scope, actor personality, and pre/post reliability. The declare-war objection path also previews the reliability drop before confirmation. This is not full `Reliability + Commitments` implementation yet; it is targeted hardening so the current engine produces more legible material for the downstream commitments/presentation work.

**Mechanical substrate follow-up (Apr 16):** the commitments-substrate audit's remaining high- and medium-severity gaps are now fixed in-engine. Shipped:

- **Cascade attribution (H-1):** forced cascade ruptures are classified `end_reason_family=obsolescence_or_external` with `fault_nation=aggressor`; cascaded nations no longer lose reliability for treaties they did not voluntarily break (Â§9.9.B).
- **`episode_id` threading (H-2):** `world.next_episode_id` serialized; one ID is allocated per root-cause declaration / break / paradox resolution and shared across all breach, cascade, and witness-strike emits so `C3` aftermath and witness-collapse can key off it (Â§6.5).
- **Per-witness `scope_reason` + dominant scope (H-3):** each witness carries one of `ally | rival | shared_enemy | region_observer` resolved by Â§8.4 precedence (region_observer deferred to bargain layer); `dominant_witness_scope` is computed per rupture and passed through to presentation (scope-branched copy deferred from C3-lite to `WAR_BARGAIN_SPEC.md` slice WB-D per the April 16 rescope).
- **Duplicate-surface collapse (H-4):** war-on-partner no longer fires both a `TREATY_BROKEN` and a `WAR_DECLARED` notification, nor both a `diplomatic_treaty_broken` and a `diplomatic_war_declared` dispatch event. The war event owns the moment; breach metadata survives in the event log.
- **`end_reason_family` vs `end_reason_action` split (H-6):** fault axis (`french_breach | counterparty_reversal | obsolescence_or_external`) and action axis (`manual_break | war_declaration | paradox_choice | cascade_forced`) are now separate fields, matching Â§9.9.
- **Pre-choice legibility for all three breach paths (M-1):** manual `break_treaty` on a commitment state now prompts `force_break_treaty_confirmation` with a reliability + warnings preview; `alliance_paradox` now carries deterministic fallout for both branches before the player chooses.
- **Structured `warnings[]` (M-2):** declare-war, paradox, and player-authored `proposal_confirm` dialogues now expose a typed `warnings[]` payload (`severity` + `category` + `text`) the C3-lite preview scaffolding can sort and filter; the proposal confirm popup now surfaces the top warnings under `Political Context`.
- **Applied vs intended reliability delta (M-5):** breach previews carry both values so presentation can distinguish "delta truncated at -100 floor" from "fully applied"; applied delta is authoritative for the scalar update.
- **Witness-scope label vs scope enum disambiguated (M-6):** audience-size flavor label is preserved as `witness_scope_label`; the per-witness enum sits on `witnesses[].scope_reason`.
- **Bilateral `betrayal_history` + hard reject posture (M-4 / B2b):** remembered breach strikes now persist per actor/victim pair with severity-scaled decay; the third active strike triggers `hard_reject_posture_triggered`, decay back to two strikes emits `hard_reject_posture_cleared`, and deep-treaty acceptance/preview paths respect that posture.
- **`commitment_paradox` registered in `HARD_STOP_TYPES` (H-5 backend):** taxonomy entry in place so the forthcoming Slice C Godot surfaces (per `COMMITMENTS_PRESENTATION_SPEC.md` §14) can land without a second backend pass.
- **AI `decision_reason` follow-through (M-3b):** AI-authored proposals, counterparty responses, campaign-log entries, incoming proposal popups, and proposal-result popups now carry a deterministic `decision_reason` plus player-facing display text. The enum set is still a subset of the full commitments spec, but motive legibility is no longer breach-only.
- **AI `decision_reason` surface (M-3 partial):** breach payload now exposes `end_reason_family` + `end_reason_action` + `fault_nation` as mechanical motive metadata; full AI-side proposal-generation `decision_reason` enum (Â§11.1) remains `C2` scope.
- **Previewâ†’resolution `episode_id` continuity (H-7):** `force_declare_war`, `force_break_treaty`, and alliance-paradox confirmations now reuse the preview's `origin_episode_id` instead of silently minting a second root event on confirm.
- **Durable paradox-resolution memory (H-8):** alliance-paradox choices now emit `commitment_paradox_resolved` with `chosen_nation`, `spurned_nation`, deterministic fallout preview, and the same origin `episode_id` that keyed the blocking choice.
- **Campaign-log legibility follow-through (M-7):** campaign-log formatting now preserves shattered-treaty wording, target/reason/fault distinctions for breach events, and a direct one-liner for `commitment_paradox_resolved` rather than collapsing back to ledger-generic phrasing.

Deferred (April 16 rescope finalized scope; remaining items are now part of either `Memory and Pressure` final implementation slice or `WAR_BARGAIN_SPEC.md`):

**Current `Memory and Pressure` next-work routing (v2.4.3):**

- Block 1 doc cleanup is complete: `docs/audits/MP_V243_BLOCK1_DOC_CLEANUP.md` closed the canonical-spec cleanup pass, including the commitments routing join-table, hegemony / Balance of Europe naming, and cross-doc drift closures from the combined audits.
- Block 2 substrate fixes are complete: `docs/audits/MP_V243_BLOCK2_SUBSTRATE.md` closed the code / serialization / test hardening pass, including the `commitment_paradox` pull-forward.
- Block 3 is SUPERSEDED: `docs/audits/MP_V243_BLOCK3_BLOC_NAMING.md` has been folded back into its owning specs. Bloc-naming contract lives in `COMMITMENTS_PRESENTATION_SPEC.md` §8.1a (terminology guard, `33 / 50 / 60` activation gate, hegemon→label taxonomy with fallback, required surfaces, worked-copy examples, playtest feel gates). Per-band voice contract lives in `DIPLOMAT_VOICE_BIBLE.md` "Bloc-naming voice contract" subsection. CF1-CF4 closure items are folded into their parent slices in `RELIABILITY_IMPLEMENTATION_PLAN.md` (CF1 → C-lite, CF2 → B-B7, CF3 → B-B4, CF4 → B-B4 per the B-B1-lite merge gate). No separate Block 3 consumption pass is required.
- Live implementation order after the now-complete B-Hegemony substrate is: B-B1-lite, then B-B3 / B-B7 / B-B4 per the dependency + merge gates, then C-lite (trimmed).
- Canonical references for this phase are `docs/RELIABILITY_COMMITMENTS_SPEC.md` v2.4.3, `docs/RELIABILITY_IMPLEMENTATION_PLAN.md` v2.4.3, `docs/COMMITMENTS_PRESENTATION_SPEC.md` v0.5.2, and `docs/DIPLOMAT_VOICE_BIBLE.md` v1.2.

**Moved to `WAR_BARGAIN_SPEC.md` (Peace Deals phase):**

- `war_bargain` clause type, creation, lifecycle, Bargain Review surface, counter-bargain flow, `join_opportunity` + `pending_declaration` + `AllyEntryPipeline`, dedicated `compute_war_entry_score()` formula, `bargain_value_mod` acceptance modifier, `fulfillment_snapshot` extended contract.
- Bargain-specific `decision_reason` enum entries (`claim_trade`, `claim_obsolete`, full `counterparty_reversal` paths beyond what substrate already covers).
- WB-D bargain-era presentation extension: `bargain_*` spotlights, scope-branched witness reaction copy, response routes (`Propose redress`, `Deepen the bond`, `Attempt to reopen the chancery`, `Denounce the refusal`), 5-beat paradox staging when rivalry-driven ratification paradox fires the new triggers, N+5 fallback grievance slot.

Focused follow-up suite: 220 passed (`test_phase2b_diplomacy`, `test_playtest_bugfixes`, `test_session8c_popups_notifications`, `test_session8d_dispatch_polish`, `test_campaign_log`, `test_diplomacy_display_contract`). Full baseline suite not rerun in this pass. Ruff: not rerun in this pass.

### Independent Tracks

- **Jealousy System** â€” v3.1 spec drafted. NEEDS DESIGN GATE. Separate track. See `docs/JEALOUSY_SPEC.md`.
- **Phase 6.5 remaining** â€” Map Renderer cutover only. Placeholder bitmap/color-map lookup and Camera2D cutover are live; commissioned art-backed layers plus final Godot smoke validation still remain. Tutorial deferred to Pre-EA.

### Completed (Reference Only)

- ~~Bug Fixes Sessions 1-12 + A + B + C~~ â€” 25 bugs resolved (PL-1 through PL-25), 8093 tests
- ~~Adversarial Audit (Apr 8)~~ â€” 12 FAILs + 19 WARNINGs, PL-21/PL-22 fixed in code
- ~~Architecture Refactoring Sessions 1-16~~ â€” R19 (modding) remaining. R14a-d deferred
- ~~Phase 8: Diplomacy~~ - Implementation complete (1A through 8D), but stabilization / legitimacy refinement is still active via `BUG_FIXES.md` + `DESIGN_REFINEMENT.md`
- ~~Diplomacy Audits~~ â€” Code (20 bugs), Creative (7.8/10), Comprehensive (6.5/10), Deep (43 bugs)
- ~~Diplomacy Refinement Phases 1-4~~ â€” 55 items, 326 tests
- ~~All prior audit/fix plans~~ â€” Systems V1/V2/V3, Deep, Final â€” all complete

---

## Archived Session History

- Feb-Apr 2026 sessions (Phase 6 Summary, Infrastructure, Phase 7b, Phase 7 Core): `docs/archive/STATUS_SESSIONS_2026_02_TO_04.md`
- May 2026 settlement v0.27 integration process records (spec reviews, branch reconciliation, compatibility table, May-14 slice queue): `docs/archive/STATUS_SETTLEMENT_V027_PROCESS_2026_05.md`
