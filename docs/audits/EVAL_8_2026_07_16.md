# 8.EVAL — Pre-8.5 Evaluation Gate: Decision Record

**Held:** July 16, 2026 (user delegated the keep/defer/drop calls: *"make the decisions for it knowing i want a good game"*).
**Method:** a 13-agent verification workflow re-verified every docket item against master AS OF TODAY (several rows dated from July 3 and the codebase absorbed EC / W6 / MC / Jealousy / Marshalate / ES-7 second pass / Vassal Deepening / the whole Combat Overhaul program since). Every claim below is code-verified with file:line evidence gathered July 16; ~1.6M verification tokens. This memo is the **authoritative gate record**; ROADMAP §Current Phase Queue and the owned rows point here.

**Decision bias applied (the user's brief):** favor player-felt fun, legibility, and believability now; defer structural/risky work to named pre-EA passes; drop what later slices already delivered or superseded — never silently (GR9: every defer names an owner row and landing).

---

## 1. The verdict in one table

| Item | What it is | Verified state on master | Decision |
|---|---|---|---|
| **AUD-b** | P2 armistice trigger counts battle-free turns from WAR START — whole Third Coalition sues by turn ~5-7 with zero combat | **Still fully live** (`ai_diplomacy.py:440-451` no battle check; `:840-845` no coalition gate on P2) | **KEEP 8.5 — Batch Q, top priority** |
| **AUD-c** | AI settlement offers always demand the PLAYER pay the indemnity regardless of who is losing | **Still live** (`_settlement_offer_build_terms` `:1995-1996` hard-codes direction; eligibility never reads war score) | **KEEP 8.5 — Batch Q** (merged with the AI-P3-P6 carve-out below — same producer) |
| **AI-P3-P6** (carve-out) | War-score-aware multi-party settlement-offer terms (winning player sees concessions) | Producer is war-score-blind (`:1972-1999`, `:2002-2049`) | **KEEP 8.5 — folded into the AUD-c slice** |
| **AUD-g** | Talleyrand's military-advantage bands off-by-one (+ dead "even" tier + grammar) | **Still live, byte-identical to July 3**, and W6-9 made it a headline `assess` output repeated per nation (`diplomatic_advisory.py:1026-1035`) | **KEEP 8.5 — Batch Q** |
| **S5-1** | Enemy AI square↔fortify self-cancelling loop (Moore burns whole nation-turns) | **Still live** (P2.5 has no `fortified` guard contra `TACTICAL_TRIANGLE_SPEC:240`; `_auto_break_square` never sets `ai_square_cooldown`) | **KEEP 8.5 — Batch Q** |
| **S5-2** | Raw camelCase marshal keys ("ArchdukeCharles") in combat/movement rejection copy | **Still live at all three seams** (`combat_executor.py:3154-3159`, `strategic_executor.py:390-395`, `movement_executor.py:226-231`; `humanize_entity_name` exists unused) | **KEEP 8.5 — Batch Q** |
| **S5-3** | DOTATION_EXPECTATION rail notice goes stale (contradicts same-response dispatch) | **Still live** (single creation site `world_state.py:4263-4298`; no refresh on growth/erosion/met) | **KEEP 8.5 — Batch Q** |
| **S5-5** | Bombard-no-guns guard bypassable post-objection; fixed-corps copy conflates the CO-4 cap with corps size | **Bypass confirmed** (`_execute_bombardment` never checks `marshal.artillery`; `meta_executor.py:1902-1909` calls it unguarded). Copy half: the row's "uncapped count" wording is inverted — the real defect is `:415-417` printing post-cap NEW_TROOPS as the corps size | **KEEP 8.5 — Batch Q** (single-source artillery guard inside `_execute_bombardment`) |
| **S5-D2** | MOVE_TO prints a route through closed neutral territory; player learns at the border hop after paying 2 AP | **Still live** (issuance path never consults `_region_passable_for`; only the PF-8 stall handler reports it) | **KEEP 8.5 — Batch Q** (issuance-time honesty rider only; stall handoff unchanged) |
| **AUD-e** (doc half) | ENEMY_AI_REFERENCE.md contract diverges from the shipped selection sort | Sort ignores `action_priority` by design-in-practice; **Enemy AI held 8.0 across 5 sweeps under the shipped sort** | **KEEP 8.5 — Batch Q, DOCS ONLY** (canonize the code; reconcile `ENEMY_AI_REFERENCE.md:127/:211/:616-630` + the `_select_next_marshal_action` docstring). **Behavior rebuild DROPPED** — reordering all 19 nations' AI to satisfy a stale doc risks the program's strongest pillar for zero player ask |
| **DWL-DIP-E7** | Talleyrand defiance floor (2% = dormant at boot authority 100) | **Confirmed dormant**: at authority ≥80 the curve computes 0.00 and the 2% floor IS the rate — the whole sabotage/discovery/confrontation/redemption arc + two shipped popups are near-dead content. Spec §3a also drifted (still documents the PL-23-retired trust term) | **KEEP 8.5 — DECIDED: authority-banded floor** (the Jealousy §0.2-11b boot-dormancy precedent, not a flat raise — e.g. floor ~5% at authority ≥70 easing to the curve below; exact numbers in-band at build). Fix the spec drift in the same slice; verify the sabotage-discovery popup live once |
| **DWL-DIP-METTERNICH** | DD8-3 armed mediation (rejected Schemer peace proposal → Austrian war pressure); ledger row demands implement-or-REMOVED | **Never built**; the spec'd "+5 coalition bonus" concept no longer maps to code; rejecting an AI proposal today costs nothing but a 3-turn cooldown | **KEEP 8.5 — DECIDED: BUILD, re-specced small**: rejecting a Schemer-authored **peace-family** proposal (armistice/peace only — never subsidy/trade asks) applies a once-per-rejection, 5-turn-expiring war-pressure/threat marker for that nation on the existing coalition-threat substrate. AI-only, GR5-clean, anti-stacking guarded. Closes the consequence-free-rejection gap AND the GR9 doc debt in `DIPLOMACY_SPEC.md §5c` |
| **VS5-LIVE-RATIFY** | Live wizard-path settlement ratification with `vassal_transfer` (CO exit-sweep obligation) | Backend seam fully live + 31 tests; **the ratification has never run live**; staging blocker = 1805 boots zero enemy-held vassals and cheat `create_vassal` hardcodes lord France | **KEEP 8.5 — run FIRST in Batch Q** (add a debug-gated `lord` param to cheat `create_vassal` for staging; run the recipe in §3 below; any defect found gets its own scoped fix) |
| **S5-D1** | Bare "attack" bypasses every gate (CR-2 clarification, W6-4 muster preview, objections) | **All four bypass claims verified live** (`combat_executor.py:5800-5814` auto-commit; `:5903/:5945/:5969` drop the `command=` kwarg so the muster gate structurally can't arm; objections need `marshal_name`; the clarification seam never fires because auto-pick succeeds) | **KEEP 8.5 — behind the CR-6 mini-gate** (it flips long-standing WAD pins in `test_auto_assign_attack.py` and adds a step to a currently-instant verb — that's a design decision the user blesses, per the standing "CR-6 needs its own gate" note. Gate questions pre-staged in §4) |
| **DR-5** Nation Agendas | DESIGN_REFINEMENT queue item 5 | **Half-stale charter**: the motive-LEGIBILITY half landed via W6-9/W6-10/UI-6/DEF-1; `nation_concerns` is obsolete (zero code presence, superseded by the hegemony machinery). The AGENDA half (R123 econ triggers, R124 isolation plays, A3 war-vs-diplomacy, personality-driven target choice/persistence, R156 "diplomacy strategically optional") is genuinely unbuilt | **PROMOTED — it IS Phase 8.5.** 8.5 is "Events, Goals & National Identity"; the re-scoped agenda core (R123/R124/A3/R155-residual/R156) becomes the phase's headline **user design gate**. Charter rewritten at this gate: strike the landed/obsolete text |
| **DR-6** Talleyrand Desk | DESIGN_REFINEMENT queue item 6 | **~6 of 7 collapsed items LANDED piecemeal** (R131 cooldown pre-check, R132 vassal transparency + ledger trend, R17d DP breakdown, R17e trend arrows, R17f mission projection, R157 voice depth — all with file:line evidence in the verification record) | **DROP — close as landed.** The one live residual, **R159 (screens should teach mechanics)**, is RE-HOMED as the named **Pre-EA Onboarding & Teaching Pass** row (GR9) — natural companion to `TUTORIAL_SCRIPT.md` |
| **DW-2** | Dual-acceptance-model convergence + war-score credit calibration | Both models still coexist and have **diverged further** (bilateral `war_score*0.3` at `diplomacy.py:6548` vs settlement `×0.65` clamped at `settlement_scoring.py:596-609`); ~30 bilateral call sites; five sweeps never surfaced a live defect | **SPLIT.** Convergence half: **RETIRED as intentional architecture** — the two models serve genuinely different shapes (pairwise proposal vs multilateral side-pressure package) and share the war-score substrate; recorded in the preflight-audit row so the "debt" framing dies. Calibration half (0.3 vs 0.65): **DEFER → Pre-EA Balance Pass** (co-homed with the STATUS:537 Europe-balance items) |
| **AUD-a** | Envoy-flood anti-spam retune | **Fixed twice over since routing**: the F8 bandwagon cap (July 6, `turn_manager.py:440-450`) + W6-10's 6-turn type/lapse cooldowns and ask variety; incoming-diplomacy pillar measured 4→7 | **DROP — closed as landed.** (Dead `QUEUE_MAX_SIZE` constant noted for the S5-D3 hygiene batch) |
| **AUD-d** | M3 territory sweetener rebuild | Removal stands; dead `NATION_DESIRES` territory rows are inert; the settlement table already does territory-rich peace end-to-end; the rebuild's wiring seams (BPH `territory_cede`, VS-3 worth-scaling) now exist | **DEFER → Pre-EA Diplomacy Polish Pass** (rebuild the counter-offer arm reusing VS-3 worth-scaling; delete the two dead desire rows either way) |
| **AUD-f** | `_evaluate_marshal` side-effect-free refactor remainder | **Confirmed unfixed**: threat-responder claims (`:1651`, no own-claim exemption at `:2639-2641`) + serialized `ai_refortify_cooldown` writes during candidate evaluation. Subtle, zero sweep sightings, balance-sensitive to touch | **DEFER → Pre-EA AI Correctness Pass** (deferred-commit metadata pattern; M1–M7 harness + `test_ai_audit_2026_07.py` as the regression gate; bundle with nothing else — AUD-e's behavior half is dropped) |
| **MC-V-4** | Cautious enemy AI doesn't husband overall force | Confirmed net-new-behavior candidate, not a bug; designs against the anti-stagnation machinery; Enemy AI 8.0 held | **DEFER → Pre-EA AI Depth Pass** (needs its own design gate defining husbanding vs the stagnation breaker; **drop cleanly if no such pass materializes** — no player-facing promise is outstanding) |
| **S5-4** | DialogueManager.preempt silent drop at QUEUE_CAP + stale docstring | Confirmed; requires a 20-deep queue (pathological); `push()` has the identical pre-existing drop | **DEFER → Pre-EA Dialogue Robustness item** (fix push+preempt together with overflow-to-mailbox). The one-line docstring refresh rides Batch Q |
| **S5-D3** | Hygiene batch: 3× edit-distance impls, byte-mirrored VS-4 predicate, leaf lazy-imports | All three confirmed present; zero player impact; the VS-4 mirror is the real latent hazard (shown≠applied if one copy drifts) | **DEFER → next refactoring slice (Pre-EA)**; VS-4 single-sourcing named the priority element; fold in AUD-a's dead constant |
| **arch-plan #23** | Fixed AI nation order (Britain first-mover) | Confirmed unchanged; LOW-rated; invisible to the player (only biases AI vs AI); **the deterministic order is load-bearing for the M1–M7 sweep harness** | **DROP — canonized as intentional** ("great powers move first"). Re-open only on an observed intra-coalition symptom |

## 2. Wave 4/5 standalone menu + residual routed items (dispositions)

**Already handled, closed here:** R117 (landed W6-9), R59+R153 (superseded by the W6-5 Literal Doctrine), R154 (resolved: breaker pre-existed + W6-11 symmetric morale).

**Stay with their existing owners (unchanged):** R26 Continental System → `ECONOMY_REVISIT_SPEC` EC-5 · R161 one-time trade → EC-8 · R158 parse-confidence → CR-7 backlog · R162 AI ultimatums → stays gated behind the Nation-Agendas gate (its own standing note) · AI-P3-P6 residual trio (AI-AI war declaration, AI conquest-vassalization, cross-AI threat assessment) → ROADMAP **8c AI Strategic Depth** row (pre-EA; needs GR5 + design gate).

**DEFER → Pre-EA Diplomacy & Flavor Content Menu** (one named row, each item keeps its R-row as the completion definition): R22 marriage alliances · R25 vassal personality events · R27 secret treaties · R35 player counter-offers on plain bilateral incoming offers · R118 enhanced acceptance preview · R127 nation-specific advisory intel · R133 point-of-no-return popup.

**DROP (superseded or not worth a slot — explicit strikes):** R32 peace conferences (the landed Imperial Settlement multilateral arc IS multi-nation peace) · the numeric `power_score` signal (authored `power_tier` shipped; nothing missed it) · R24 signing ceremonies · R33 puppet rulers (the vassal depth track owns this fantasy) · R36 personal summits.

**7b residuals routed here long ago (disposed with named owners, not deep-verified this gate):** Gneisenau Staff Work → Pre-EA Diplomacy & Flavor Content Menu (Prussian content family) · cross-nation coordination → ROADMAP 8c row · battle/war narration toggle → **DROP** (superseded — narration re-scored 3.5→7.5→8.0 across W6 + CO) · creative-command war uses → CR-7 backlog.

## 3. Phase 8.5 opening shape (this gate's output)

1. **Batch Q — "Quick Wins & Honest Counsel"** (all S-effort, sequenced first, single session-or-two): VS5-LIVE-RATIFY exercise (first — it may file fixes) → AUD-b armistice guard (≥1 battle record on the pair, or count-from-last-battle, WITH a lengthened no-contact escape so contactless wars keep an exit) → AUD-c + carve-out (war-score-aware offer direction/terms) → AUD-g band realignment (+grammar, +parity pins) → S5-1 → S5-2 → S5-3 → S5-5 → S5-D2 → DWL-DIP-E7 (authority-banded floor + spec-drift fix + one live popup verify) → DWL-DIP-METTERNICH (small build per §1) → AUD-e doc reconciliation → S5-4 docstring line.
   *VS5 recipe:* debug-gated `lord` param on cheat `create_vassal` (`meta_executor.py:2177`) → stage an Austria-lord vassal + France-vs-Austria war with winning posture → F1 wizard → propose common peace → add the "Claim {court}'s vassal" suggestion → review → confirm → ratify → verify lord re-key/loyalty-30/tribute re-home/dispatch line/VASSALS tab/no 500s.
2. **CR-6 mini-gate (user)** — S5-D1 bare-attack gating is the concrete candidate; gate questions: (a) multi-contact bare "attack" → `build_marshal_choice_clarification` (single-contact keeps instant auto-pick)? (b) thread `command=` through `_execute_general_attack_combat`/`_execute_auto_assign_attack` so the W6-4 muster gate arms (AI/strategic/post-objection callers keep None, GR5)? (c) objection routing for auto-picked marshals? Flipping `test_auto_assign_attack.py` pins is conscious, at the gate.
   > ✅ **HELD + LANDED July 16, 2026.** All four questions blessed (a/b/c + E-CA-4 subsumed by b). Built via resolve-and-rewrite at the dispatch seam (`CombatExecutor.resolve_auto_attack`) — a bare/auto attack is rewritten to a specific named attack so it flows through the entire named-attack pipeline. No pins flipped (gating is at the dispatch seam; `test_auto_assign_attack.py` stays green); new pins `test_cr6_bare_attack_gating.py`. **Authoritative record: `COMMAND_ROBUSTNESS_SPEC.md` §7.**
3. **The 8.5 design gate proper (user)** — Events, Goals & National Identity, with the re-scoped **Nation Agendas** core (R123/R124/A3/R155-residual/R156) as the diplomacy centerpiece. Propose-then-build per the standing expansions-welcome rule.

## 4. New named pre-EA rows created by this gate (GR9 homes)

Recorded in `DESIGN_REFINEMENT.md` §8.EVAL Dispositions: **Pre-EA Balance Pass** (DW-2 credit calibration; co-homed with STATUS:537) · **Pre-EA AI Correctness Pass** (AUD-f) · **Pre-EA AI Depth Pass** (MC-V-4; gate-first, drop-if-no-pass) · **Pre-EA Dialogue Robustness** (S5-4) · **Pre-EA Onboarding & Teaching Pass** (R159) · **Pre-EA Diplomacy Polish Pass** (AUD-d) · **Pre-EA Diplomacy & Flavor Content Menu** (R22/R25/R27/R35/R118/R127/R133 + Gneisenau) · next refactoring slice (S5-D3).

**8.EVAL is CLOSED.** Every routed row now has a disposition + owner; nothing on the docket remains open-ended.
