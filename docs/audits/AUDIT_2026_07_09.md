# Comprehensive Codebase Audit — July 9, 2026

> Fable-led correctness sweep per `docs/AUDIT_GUIDELINE.md` (§5–§7), running the §10 chunk order.
> Baseline at start: master `2efcbe2`, **11,780 passed / 1 skipped**, ruff clean (verified before first fix).
>
> Sections per §9: **Fixes applied** (one row each) and **Escalations** (one block each).
> Architecture observations are tagged `architecture-refactor` and mirrored to `ARCHITECTURE_REFACTORING_PLAN.md`.

---

## Chunk 1 — World State, Serialization & Save/Load (§7.1)

### Fixes applied

| # | Component | Defect | Fix | Test | Commit |
|---|-----------|--------|-----|------|--------|
| 1.1 | WorldState.from_dict | `diplomatic_points` missing-key fallback hardcoded `4`, violating the documented item-3 convention (omitted keys read the world's construction-time value) and contradicting the single-source DP formula (`calculate_dp`: base 3 + Talleyrand skill≥8 +1 + authority≥60 +1 = 5). Live effect (verified by booting the scenario): the shipped 1805 campaign starts turn 1 with 4 DP (scenario omits the key; `from_scenario` → `from_dict`), then regens to 5 every later turn. | Fallback now reads `world.diplomatic_points` (constructor value), matching the `max_turns`/`nation_actions`/`nation_authority` pattern on adjacent lines. | Stale-default pins updated 4→5 (`test_audit_2_3.py` ×2, `test_session_2_diplomacy.py::test_backward_compat_no_dp`); new regression pin `test_europe_1805_scenario.py::test_turn_one_dp_matches_constructor_and_regen_formula` | chunk-1 commit |

**Chunk-1 verification:** world-level round-trip smoke (to_dict → json → from_dict → to_dict parity) run on legacy, bare-Europe, loaded 1805 scenario, and a 3-turn-advanced legacy world — all PASS. §7.1 verify (`pytest -k "serialization or save or fog_of_war"`, 513 tests) green.

---

## Chunk 2 — Core command→resolution loop (§6.1–§6.4)

### §6.1 Combat & Multi-Marshal

**Fixes applied**

| # | Component | Defect | Fix | Test | Commit |
|---|-----------|--------|-----|------|--------|
| 2.1 | combat.py personality messages | The aggressive-attacker personality message hardcoded **"Bravest of the Brave"** (Ney's signature ability) and the cautious-defender message **"Iron Marshal"** (Davout's epithet) — but they key off `personality`, and the 1805 roster has 4 aggressive / 13 cautious marshals, so Murat's attacks were captioned with Ney's ability etc. | Labels now name the personality ("Aggressive:" / "Cautious:"); sentence voice unchanged. | `test_audit_2026_07_09_fixes.py::TestPersonalityMessagesLabelPersonalityNotAbility` (2) | chunk-2 §6.1 commit |
| 2.2 | combat_executor coord_context + battle_report._pick_observation | `coordination_context` carried **attacker-side data only** (`type_count`, hostile/devoted classifications), while `_pick_observation` narrates "our side" — so when the player DEFENDED, an enemy attacker's combined-arms triangle (P0.5) or its internal hostile/devoted politics (P5.5/P9.5/P12) was narrated as the player's. Docstring promises "perspective is always from the player's side." | Defender coordination context captured (was computed then discarded); coord_context now side-tagged (`attacker_type_count`/`defender_type_count` + `defender_hostile_*`/`defender_devoted_allies`; legacy keys stay attacker-side for back-compat); `_pick_observation` selects the player's side; re-pick gate extended to defender-side keys. Display-only — no mechanics touched. | `test_audit_2026_07_09_fixes.py::TestBerthierCoordinationObservationsArePerspectiveAware` (4) | chunk-2 §6.1 commit |

**Swept clean:** `get_attack_modifier`/`get_defense_modifier` single-source + one-shot consumption ✓; Berthier snapshot taken before consumption ✓; normal vs `apply_casualties=False` path parity (1.5 outcome threshold, morale formula, fort-degradation constants identical) ✓; `_COORDINATION_FIELDS` cleanup list complete vs every transient assignment ✓; eligibility quad (alive/not-broken/not-retreated/recovery==0) identical across all 4 filter sites ✓; `_distribute_casualties` proportional/int/capped ✓; coordination tutorial correctly player-gated ✓.

### §6.2 Strategic Orders

**Fixes applied**

| # | Component | Defect | Fix | Test | Commit |
|---|-----------|--------|-----|------|--------|
| 2.3 | strategic_executor._handle_first_step_blocked | The literal-branch "no alternate route — break order" arm cleared `strategic_order` **without** the paired `holding_position`/`hold_region` clear (§6.2 invariant: every order-clear clears them together; leftover hold state corrupts the +15% literal-hold defense modifier). Its sibling reached-objective arm 30 lines up pairs correctly. | Added the paired clear. | `test_audit_2026_07_09_fixes.py::TestOrderBreakClearsHoldState` | chunk-2 §6.2–6.4 commit |

**Swept clean:** all 36 `strategic_order = None` sites audited — every other unpaired-looking site clears hold via `move_to()` (which breaks Immovable on a real move) or `clear_combat_transient_state()`, or can only clear order types that never set hold (PURSUE/SUPPORT/MOVE_TO peace-cancellations, SUPPORT-on-broken-target, eliminated-marshal targets) ✓; all 15 `pending_interrupt` builders stamp the `'marshal'` key (the July-7 HIGH fix holds everywhere) ✓; `new_state` stripped at all 3 strategic.py report seams ✓; PURSUE/SUPPORT marshal-name targets resolved to `target_marshal.location` before every pathfind ✓; CR-5 gate coverage structurally intact (6 per-turn `_inferred_attack_gate` sites + 2 `_inferred_first_step_gate` sites, `delegation_inferred` serialized) ✓; mock mode gate (`parse_resolved_to_action` returns False for `mode=="mock"`) ✓. Verify: 1,490 `-k "strategic or cr5 or command_robustness"` green.

### §6.3 Command Parsing & LLM Robustness

No defects found. This component absorbed five adversarial audit rounds July 4–7 (CR-3/CR-4/CR-5/CR-5b + the whole-slice live-backend audit), so the sweep leaned on the standing gates: **parser eval harness 407/407 mock-corpus green** (3 `live_only` rows skipped as designed); `.get().lower()` None-trap scan across the pipeline clean — `target_stance` (the one genuinely optional field consumed with `.lower()`) is only included in the command dict when truthy (`schemas.py` lift), so the present-but-None trap can't fire; action fields reaching disobedience have passed validation (bypass hierarchy).

### §6.4 Objection, Defiance, Trust, Authority & Vindication

No defects found. Swept: defiance-chance **sign audit** clean (high authority −10% suppresses / low authority +10% emboldens / low trust +15% / high trust −10% / vindication +10% per stack — all directions match V2b; hard cap 0.40; literal bypass; cooldown) ✓; pre-validation + AP before objection structure intact in executor.py (documented inline) ✓; all 12 `objection_actions` have handlers in `_execute_post_objection` incl. strategic routing ✓; `_OBJECTION_DISPLAY`/`_DEFIANCE_DISPLAY` cover every objection-triggerable action (the 17 VALID_ACTIONS without entries — diplomatic/vassal/strategic-keyword aliases — are not objection-triggerable, and the fallback is a legible `replace("_"," ")`, not a raw key) ✓; trust/authority/vindication serializers verified in chunk 1 ✓.

### Escalations

_(none yet)_

### Observations (no action)

- `WorldState._action_counter` stamps a `timestamp` on `attacks_this_turn` records that **no code reads** (grep: written at `world_state.py:8684`, zero consumers). Not serialized while `attacks_this_turn` is — harmless today because `save_manager.load_game` clears `attacks_this_turn`, and nothing consumes the stamp. Candidate for deletion in a future hygiene pass (architecture-refactor, ultra-low priority).
- The serialization-enforcement test's attribute scan excludes `_`-prefixed fields, so private durable state escapes the gate. Hand-verified this chunk: `_last_advanced_turn`, `_auto_advanced_to_turn`, `_bargain_fulfillment_log`, `_next_join_opportunity_id`, `_war_entry_reroll_memory`, `_capital_proximity_last_alert`, `_prev_war_exhaustion`, `_relation_deltas_this_turn` all round-trip; `_dialogue_manager`/`_cooldown_manager`/`_popup_queue` serialize via their public keys/properties; the transient caches are correctly rebuilt-on-load.
- `settlement_route_seq` / `settlement_reopen_attempts` int-keyed inner dicts correctly re-int their keys in `from_dict` (JSON stringifies them) — verified, no defect.
