# SWEEP 5 — Combat Overhaul Phase 6 (Parser & Play-Friction) + the Sweep-5 live fixes

**Date:** July 16, 2026
**Owner:** `docs/COMBAT_OVERHAUL_SPEC.md` §2 (the Sweep) + §4 Phase 6.
**Scope measured:** Phase 6 (PF-1..PF-9 + AI-1, `3c0246a`) + the two Sweep-4 vassal parse
fixes (`7908227`) + everything else landed since Sweep 4 (the Vassal Depth track
`1082382..dd304ab`, UI-6 `8f466c9`) + **five defects this sweep live-found and fixed
in-session** (see Live evidence; the fixes are part of the state under measure).
**Predecessors:** `SWEEP_4_2026_07_15.md`, `SWEEP_3_2026_07_14.md`, `SWEEP_2_2026_07_14.md`,
`SWEEP_1_2026_07_13.md`, `SWEEP_1a_2026_07_13.md`, `SWEEP_0_2026_07_13.md`.
**Live evidence:** `docs/audits/SWEEP_5_LIVE_EVIDENCE_2026_07_16.md` (fresh 6-turn
`LLM_MODE=anthropic` playthroughs over HTTP; three capture rounds — the first two rounds
ARE the finding record, the third is the verified-fixed record).

Half A (deterministic) is the **hard gate**; Half B is the directional 12-component
adversarial LLM review (§2.2, scores jitter ±0.5).

---

## Half A — the hard gate (deterministic)

**Phase-6 parser/friction gate:**

| Test | What it pins | Result |
|---|---|---|
| `test_pf1_march_on.py` … `test_pf9_nations_treaty_scope.py` + `test_ai1_priority_order.py` (76) | all ten Phase-6 fixes | ✅ |
| `test_combat_sweep_metrics.py` (11) | M1–M7 (below) | ✅ |
| `test_sweep5_clarification_preempt.py` (6) | the typed-answer channel preempts non-hard-stop dialogues; hard-stop still refuses; displaced dialogue restored on pop | ✅ NEW |
| `test_sweep5_interrupt_press_on.py` (4) | continue-family → `attack_anyway` fallback at interrupts; `continue_order` still preferred when offered; cancel unaffected | ✅ NEW |
| `test_sweep5_parse_validation.py` (16) | invented-marshal guard (typo-tolerant, raw-text-gated); movement-target passthrough on BOTH parse paths; executor names the unknown place | ✅ NEW |
| CR-1 golden corpus — MOCK | 433/433 | ✅ |
| CR-1 golden corpus — **LIVE** | **432/432** (2 `mock_only` skips — NEW harness arm mirroring `live_only`; the V2-55 word-boundary rows pin fast-parser shapes the live LLM legitimately resolves) | ✅ |

One pin consciously flipped: `test_command_robustness_cr2_clarification.py::
test_register_only_when_slot_empty` → `test_register_preempts_non_hard_stop` (the old
any-dialogue skip was the live defect's root cause — most recent question now wins).

**Combat-metric regression guard (byte-unchanged — no combat code touched since Sweep 4):**

| ID | Metric | Sweep 4 | **Sweep 5** | Target | Verdict |
|----|--------|---------|-------------|--------|---------|
| M1 | concentration win-rate 1→5 corps | `0.000→0.818` | `0.000→0.000→0.013→0.355→0.818` | non-decreasing; 3≫1 | ✅ held |
| M1b | reinforcer contribution agg/cau/hostile | `41400/36000/0` | `41400/36000/0` | agg>cau>hostile | ✅ held |
| M2 | rout ≤2 turns 2:1 / 3:1 | `0.613/1.000` | `0.613/1.000` | ≥0.35 / ≥0.65 | ✅ held |
| M3 | net defender Δstrength/turn (2:1) | `−2749` | `−2749` | ≤ −2000 | ✅ held |
| M4 | multi-marshal report consistency | `100%` | `100%` | 100% | ✅ held |
| M5 | iron-resolve 3-stack payoff | `+0.240` | `+0.240` | ≥ +0.18 | ✅ held |
| M6 | attacker win-rate fort+mountains (GUARD) | `0.000` | `0.000` | < 0.5 | ✅ held |
| M7 | first jealousy trigger / roster run | `turn 1` | `turn 1` | ≤ 8 | ✅ held |

**Full suite 13,547 passed / 3 skipped** (13,521 + the 26 new Sweep-5 tests).
`ruff check backend/` clean. Godot headless import pass clean (0 `SCRIPT ERROR`) — the
sweep's only client-side change is three redrawn heraldry SVGs (separate flag
date-accuracy task, same session).

---

## The five live-found-and-fixed defects (the sweep's own yield)

| # | Defect (live symptom) | Fix seam | Severity |
|---|---|---|---|
| S5-1 | Typed answers dead behind ANY active dialogue — the ASK's own words ("give battle") mis-parsed to `Region 'generic' not found` when a mailbox proposal held the stack | `clarification.register_pending_clarification` → preempt non-hard-stop (DialogueManager.preempt already existed) | HIGH |
| S5-2 | "press on"/"continue" at a `contact_bad_odds`/`muster_confirm` interrupt fell through to a bewildered LLM parse (`continue_order` absent from options) | `main.py` interrupt keyword map → `attack_anyway` fallback | MED |
| S5-3 | Live LLM INVENTED a marshal for bare "attack" (Bernadotte, breaking his standing HOLD) — bypassing the designed CR-4 focus / CR-2 clarification flow | `validation.validate_parse_result` invented-marshal guard (raw-text-gated, typo-tolerant ≤2 edits) | HIGH |
| S5-4 | Unknown place name silently eaten: "move to Venetia" → `Move order requires a destination` instead of naming the place | movement-target passthrough at BOTH the LLM validation seam AND the fast parser; executor fuzzy matcher now owns the verdict | MED |
| S5-5 | Live corpus run mis-scored 2 mock-shape rows as failures | `parser_eval.py` `mock_only` support + 2 rows marked | LOW |

---

## Half B — 12-component adversarial LLM review (RAN July 16, 2026)

Ran as the 12-reviewer + adversarial-verify + synthesis workflow (**28 agents,
~3.70M tokens, 0 errors**), each reviewer grounded in the delta since Sweep 4, the
Half-A gate, and the fresh live evidence — reviewers read code directly, were told to be
adversarial about their own component's possible regression, and every med/high finding
was independently attacked by a refutation agent (CONFIRMED / REFUTED / UNCERTAIN).

### Scoreboard

| Component | Sweep 4 | **Sweep 5** | Δ | Verdict |
|---|---|---|---|---|
| Combat | 7.5 | 7.5 | 0 | held — core byte-identical; M1–M7 byte-identical; CO-6/CO-3 fired live in all three battles |
| Economy | 6.5 | 6.5 | 0 | held — byte-decoupled; turn-6 ledger Net reconciles exactly; EC-U3 anchor observed live (~56%) |
| Enemy AI | 8.0 | 8.0 | 0 | held — AI-1 relabel behavior-preserving; Moore square/fortify loop pre-existing (rungs byte-identical) |
| **Diplomacy** ◄ hold ≥8.0 | 8.0 | **8.5** | **+0.5** | **MET** — two pre-existing hegemony-projection direction bugs FIXED in the acceptance path; PF-9; VS-5 clause integrated |
| **Narration** ◄ ≥7.5 | 8.0 | **8.0** | 0 | **MET** — delta additive/net-positive; held from rising by the end-turn 500 blackout + camelCase leaks |
| Marshal Drama | 7.5 | 7.5 | 0 | held — BOTH Sweep-2 WAD evidence gaps CLOSED live (autonomous glory attack + crown lit) |
| Marshal System | 7.5 | **8.0** | +0.5 | raised — literal ASK loop restored broken→working; standing orders guarded against LLM invention; PF-4 objection economy |
| **Parsing** ◄ ≥7.5 | 7.0 | **7.5** | **+0.5** | **MET (at the floor, earned)** — the sweep's own five live-found-and-fixed defect classes |
| **UX / Flow / State** ◄ ≥7.0 | 7.0 | **7.0** | 0 | **MET** — three wedge classes killed; the raise cancelled by the (pre-existing) live end-turn 500 |
| Settlement | 7.0 | **7.5** | +0.5 | raised — vassal_transfer end-to-end w/ pricing parity (31 tests) + two real acceptance-path fixes |
| Vassals & Coalition | 6.5 | **7.5** | **+1.0** | deliberately beyond jitter — the ENTIRE Sweep-4 rec ladder (P0 VP-D1 → P1 VS-6/VS-3 → all P2s) landed + code-verified |
| Architecture | 8.0 | 8.0 | 0 | held — 8,613-insertion delta with GR1/5/6/8 discipline verified intact |

**All four Sweep-5 targets MET. Zero regressions** (every claimed one adversarially
refuted or verified pre-existing at `016bf71`). **Directional mean ≈7.6** (Sweep 4 ≈7.4);
five pillars rose on verified evidence, seven held, none fell.

### Headline — did Parsing hit ≥7.5? YES, at the floor and earned

Sweep 5 *was* the parsing phase, and the five live-found defects are part of the score,
not an embarrassment to it: the sweep's own live driving found them, fixed them
in-session, and verified the fixes by 89 targeted tests + corpus 433/433 mock /
432/432 live + the final live run in which every pending question was answerable in its
own words. What holds it at 7.5 (not 8.0): the CONFIRMED bare-"attack" auto-pick
(long-standing WAD, now the most ambiguous lethal order with the fewest gates — routed
below), the PF-8 issuance-honesty gap, and the Venetia fix's missing live HTTP capture
(post-capture; covered by the executor-level e2e test).

### Regression check — NONE (and a kill-list)

Every non-target pillar returned NO_REGRESSION; most are byte-decoupled per `git diff
016bf71..HEAD` (combat/economy/jealousy/authority/coalition cores byte-identical
committed AND working tree). The two HIGH-visibility live failures — the **end-turn
HTTP 500** and the **bare-"attack" auto-pick** — were adversarially verified
**pre-existing at `016bf71`** (held scores, did not lower them). REFUTED-and-killed:
"the reward economy has zero live exercise" (its own cited evidence file contains 151
`dotation_expectation` instances, live erosion, battle expectation notes, and a reward
dialog payload); the surviving kernel — the *transactional* half unexercised — became an
exit-run requirement (met, see the exit sweep doc).

### Prioritized recommendations (routed)

| P | Item | Home |
|---|---|---|
| **P0** | End-turn 500: early-return `enemy_phase` raw (tuple-keyed `new_state` caches crash `jsonable_encoder` after the turn advanced; also a fog leak) | **LANDED same session, post-synthesis** — `_build_result_response` now routes `enemy_phase` through `_build_visible_enemy_phase`; poison reproduced then killed; `test_sweep5_end_turn_500.py` (4) |
| P1 | Bare "attack" bypasses CR-2 + W6-4 muster gate + objections; route to marshal-choice clarification when >1 marshal in enemy contact | `DESIGN_REFINEMENT.md` §Sweep-5 (CR-family; candidate for the CR-6 gate) |
| P1 | Moore square↔fortify self-cancelling AI loop (P2.5 lacks a `fortified` guard; `_auto_break_square` skips `ai_square_cooldown`) | `BUG_FIXES.md` §Sweep-5 |
| P1 | Raw camelCase marshal keys in executor/combat/pursue copy (R7) — route through `humanize_entity_name` at the three seams | `BUG_FIXES.md` §Sweep-5 |
| P2 | Stale/contradictory `DOTATION_EXPECTATION` rail notice (static grace copy vs live dispatch in the same response) | `BUG_FIXES.md` §Sweep-5 |
| P2 | PF-8 issuance-time passability honesty (name the closed crossings when the plan routes through them) | `DESIGN_REFINEMENT.md` §Sweep-5 |
| P2 | `DialogueManager.preempt` at QUEUE_CAP silently drops the displaced soft-stop; stale docstring | `BUG_FIXES.md` §Sweep-5 |
| P2 | PF-7 lows: bombard-no-guns post-objection bypass; corps-size copy under the field-levy cap | `BUG_FIXES.md` §Sweep-5 |
| P2 | Architecture hygiene: duplicated `_edit_distance_le2`, byte-mirrored VS-4 predicate, leaf→layer lazy imports | `DESIGN_REFINEMENT.md` §Sweep-5 |

### Verdict — the Sweep-5 gate is PASSED

All four targets met on the evidence; the Half-A hard gate stands beneath it. The
synthesis conditioned the exit sweep on (1) landing the P0 500 fix — **done same
session** — and (2) scripting the three live-evidence gaps (a low-loyalty vassal run, a
settlement exercise, a reward-economy grant transaction) into the exit run — **done, see
`COMBAT_OVERHAUL_EXIT_SWEEP_2026_07_16.md`**.

*(The full unabridged synthesis, per-reviewer summaries, and verify verdicts live in the
workflow journal: session `subagents/workflows/wf_5879806d-c7d/journal.jsonl`.)*
