# WO-35 / WO-36 / WO-38 — the verification map (August 21, 2026)

> **Status: VERIFIED, NOT BUILT.** This is the read-only output of a
> verify→refute fleet pointed at the committed SHA `2112313` with a clean
> tree, plus two hand-reproductions by the lead. It exists because the build
> it specifies is a proper slice with a P1 inside it, and because **the fix
> WO-35 asks for would CREATE a soft-lock** — which is exactly the class of
> mistake this row keeps punishing. The next session builds from here.
>
> Headlines, before the detail:
>
> 1. **WO-38 (P1, NEW, hand-reproduced).** A stale strategic objection
>    hijacks the answer to a DIFFERENT marshal's objection. Measured: with
>    Ney's tactical objection on screen and a stale Davout strategic
>    objection in the slot, answering *"trust"* gave **Davout +8 trust and
>    fortified Davout**, leaving Ney's objection standing. A strategic
>    objection blocks nothing, nothing clears it at the turn boundary, and
>    `handle_objection_response` checks the strategic slot FIRST. **WO-35 is
>    its delivery mechanism** — a restored strategic objection raises no
>    modal, so the player cannot know the slot is occupied.
> 2. **WO-35's filed inference is REFUTED and its filed fix is DANGEROUS.**
>    "The modal is unreachable because the route requires `success == true`"
>    is wrong twice: the raising results ARE `success: True`, and on the
>    recovery path `success` is not the binding constraint (the block carries
>    neither `state` nor `pending_objection`). Worse, the block's `success`
>    flag gates AP consumption, the mild-objection join and the square-break
>    prepend — flipping it would be harmful. And a `/load` attach of
>    `pending_objection` **without a tactical/strategic discriminator renders
>    a modal with no buttons and no ESC exit**, because the saved dict
>    records no discriminator.
> 3. **WO-36 is NOT a `/load` attach.** It is one line in the client's
>    world-swap reset: `_redemption_recheck_turn` is never reset, so the
>    ordinary same-turn savescum skips the recovery poll for the rest of that
>    turn. Identical to the hazard IGR-F fixed for `_envoy_digest_shown_turn`
>    and left unfixed for this variable. The redemption is otherwise
>    self-healing — the poll fires at the tail of the player's first command.
> 4. **A latent soft-lock blocks the whole thing** (`main.gd`
>    `_on_commitment_paradox_choice`): input is disabled unconditionally
>    before a two-branch send, and ESC cannot open the pause menu over a
>    visible modal. One added option away from unrecoverable, and any
>    world-swap raise puts weight on it. Fix it FIRST.
> 5. **Unfiled, found in passing: a cross-campaign stash leak.**
>    `pending_redemption_data`, `pending_proclamation_data`,
>    `pending_diorama_data`, `last_battle_diorama` and
>    `_redemption_recheck_turn` all survive the world-swap reset, so a
>    previous campaign's stash can raise in a newly loaded world.
>
> The three agents disagreed about the WO-35 fix. Where they disagreed, the
> **client-side reading is the safe one** and is what the build order below
> follows.

---


## WO-35 — PARTIAL

### Evidence

OUTCOME CONFIRMED, MECHANISM HALF-WRONG, AND THE ROW IS INCOMPLETE (a third key belongs).

CONFIRMED — the state survives and /load is silent:
- pending_objection: world_state.py:6778 (to_dict), :7380 (from_dict). NOT in save_manager.py:187-212's "clear transient per-turn data" block (which clears only battles_this_turn, marshal.in_combat_this_turn, mild_concerns_this_turn, gold_spent_this_turn, threat_sources_this_turn). Measured over a real save_game->load_game: survives intact.
- pending_interrupt: marshal.py:399 (field), :1629 (to_dict), :1825 (from_dict). Measured: survives. strategic.py:1150-1156 even asserts in a comment "Serialized with the dict, so a save/load mid-interrupt keeps it" — it keeps the STATE and raises nothing.
- /load (main.py:4021-4056) attaches ONLY pending_capture_choice + capture_data (the WO-30 fix). Nothing else.
- main.gd:1863-1868 _response_has_objection_route: both branches literally require response.get("success", false). Row correct on that point.
- executor.py:645 — the objection BLOCK returns "success": False. Row correct on that point.

REFUTED — the inference "so that modal is unreachable by construction on the recovery path too":
1. The RAISING results are success: True. executor.py:1573-1578 returns success True + pending_objection True + state "awaiting_player_choice"; strategic_executor.py:1097-1100 returns success True + pending_objection True + objection. The modal is fully reachable in normal play.
2. On the recovery path `success` is NOT the binding constraint. Measured, the block's complete key set is ['awaiting_response','choices','message','objection','success'] — it carries neither `state` NOR `pending_objection`, so flipping success to True changes nothing. The row names the one thing that is not load-bearing.
3. The block's success flag is load-bearing ELSEWHERE: it gates AP consumption (executor.py:1910), the mild-objection message join (executor.py:1990) and the square-break prepend (executor.py:2029). Following the filed fix literally would be harmful.
4. The real fix path — /load's own response — is already success: True (build_base_response default, main.py:4038), so the "requires success==true" fact is a red herring for the fix.

REFUTED — "strictly worse than WO-30" (the census wording), for pending_objection specifically. It is COMPARABLE, not worse: the executor block names its own answer words (executor.py:648-655, CA9-N5) and the state is answerable — W6-0 exact-token router main.py:2262-2270 (covers pending_strategic_objection too), CA9-N5 English-meaning arm main.py:2271-2310, dialogue-keyword re-prompt main.py:2667-2690, POST /respond_to_objection -> meta_executor.py:1611-1660. Measured block text: "Ney awaits your answer, Sire — settle the objection before issuing new orders. Reply 'trust', 'insist' or 'compromise'." It is a missing-modal defect, not a dead end.

pending_interrupt IS worse in one real case. It blocks nothing (main.py:2169-2247 is a route, not a block; an options-less interrupt is silently discarded at main.py:2232). Re-surfacing at end turn requires the marshal to hold a strategic order: strategic.py:216-221 filters marshals_with_orders on m.in_strategic_mode == (strategic_order is not None) (marshal.py:854-857); only then does strategic.py:857-866 re-emit requires_input, promoted at main.py:1305-1311. So ORDER-BOUND interrupts self-heal at the next end turn; ORDER-FREE ones never do — last_stand (combat_executor.py:3008, 3058) and muster_confirm (combat_executor.py:4858) are raised from tactical combat on marshals with no strategic order and are genuinely stranded by a save/load.

NEW — a THIRD key belongs on the row: pending_strategic_objection. Survives (world_state.py:6780/:7382, measured). /load never attaches it. It BLOCKS NOTHING — executor.py:628 reads only world.pending_objection; the only other readers are meta_executor.py:1613, strategic_executor.py:2246/2267/2351, main.py:2265. MEASURED: with pending_strategic_objection set, "Ney, move to Lyon" executed normally (success=True, "Ney moves from Belgium to Lyon"). It never re-surfaces (nothing in advance_turn clears or re-raises it). And meta_executor.py:1611-1614 checks the strategic slot FIRST, so a stale strategic objection captures the answer to a later GENUINE tactical objection. The census is keyed on RESPONSE keys and this shares `pending_objection`, which is why it was invisible.

PAYLOAD IS RECONSTRUCTIBLE (the filed fix is buildable, for different reasons than filed): _show_objection_dialog (main.gd:3811-3845) reads marshal/personality/message/suggested_alternative/compromise/options/tone/concern_level/trust_gain/insist_penalty/compromise_gain out of the nested `objection` dict, and BOTH stored dicts carry all of them (executor.py:1551-1569; strategic_executor.py:1068-1092). Only trust/trust_label/vindication/authority are read off the top level, all four derived live at both raise sites. The interrupt payload is ONE key and _response_has_interrupt_route (main.gd:2292-2308) has no success requirement at all.

### Fix shape

Do NOT touch any executor `success` flag. /load's own response is already success:True, so the route fires as-is.

BACKEND — at the /load tail (main.py, beside the WO-30 block at :4053-4056):

(a) Objection, using the STRATEGIC response shape for BOTH kinds:
    obj = world.pending_strategic_objection or world.pending_objection
    if obj:
        response["pending_objection"] = True
        response["objection"] = obj
        response["marshal"] = obj.get("marshal")
        m = world.get_marshal(obj.get("marshal") or obj.get("marshal_name"))
        response["trust"] = int(m.trust.value)
        response["trust_label"] = m.trust.get_label()
        response["vindication"] = world.vindication_tracker.get_vindication_data(m.name).get("score", 0)
        response["authority"] = int(world.authority_tracker.authority)
    One shape is safe for a TACTICAL objection despite is_strategic=true: objection_dialog.gd:122-127 gates the strategic button branch on options.size() > 0, and a tactical stored objection has no `options` key, so it falls to the tactical suggested_alternative/compromise branch. Answer-time routing is world-state driven (meta_executor.py:1613), not dialog-flag driven, so both kinds resolve correctly.

(b) Interrupt — one key:
    for m in world.get_player_marshals():
        if m.pending_interrupt:
            response["pending_interrupt"] = m.pending_interrupt
            break
    /load carries no strategic_reports, so the WIN-H1 defer at main.gd:2305-2307 does not suppress it.

CLIENT — _apply_world_swap_response (main.gd:4438-4476) consults no route table; add objection + interrupt arms next to the capture arm, through the SAME predicate/route pairs (_response_has_objection_route/_route_objection_response, _response_has_interrupt_route/_route_interrupt_response), on a duplicate with "message" erased, exactly as the WO-30 capture arm does.

CENSUS — tests/test_wo_slice15_capture_question_holds.py:753-779: move pending_objection and pending_interrupt from KNOWN_SILENT_AT_LOAD into LOAD_REATTACHED, and correct the two comment strings (the "requires success==true" rationale is wrong). The census cannot see pending_strategic_objection at all (it shares the `pending_objection` response key) — add a world-attribute arm or a named note so the third slot is not invisible to the next slice.

SCOPE CALL FOR THE SLICE: the stale pending_strategic_objection that blocks nothing, never re-surfaces and hijacks /respond_to_objection ahead of a later tactical objection is a pre-existing defect this fix makes reachable-and-visible rather than causes. Either fold it (raise it at load AND give it a block mirroring executor.py:628, or clear it at load) or file it separately — but do not leave it undecided.

### Hazards

1. THE TRAP IN THE FILED ROW: "the route requires success==true and the block returns success False" reads as an instruction to flip the block's success flag. That flag gates AP consumption (executor.py:1910), the mild-objection message join (executor.py:1990) and the square-break prepend (executor.py:2029). Flipping it breaks all three, and would STILL not raise the modal (the block also lacks `state` and `pending_objection`).

2. PRECEDENCE DRIFT: the command path runs objection in _pre_hud_response_routes (main.gd:1829, index 0) and capture in _post_hud_response_routes (main.gd:1834, index 1) — objection wins. The load path currently returns early on capture (main.gd:4468-4471). A world can carry BOTH. If the objection arm is appended after the capture arm the two paths disagree about which modal the player sees. Decide consciously and pin it.

3. INPUT STATE: the WO-30 capture early-return leaves input disabled and relies on the dialog's exits re-enabling it. _show_objection_dialog (main.gd:3811) only calls set_input_enabled(true) on its NULL-guard path; I did not confirm _on_objection_response re-enables on every path. Verify before copying the early-return shape, or the player gets a modal over a dead command line.

4. STALE-STATE SEMANTICS: meta_executor.py:1689 already carries an N7 comment ("No defiance if marshal is broken/retreating (stale objection via save/load)") — stale objections are a known live condition. Raising the modal at load makes the player act on an objection whose original_order may reference a marshal/target that has since changed. Consider validating the marshal still stands (the PC15-4 tombstone idiom) before raising.

5. ORDER-FREE INTERRUPTS (last_stand, muster_confirm) never re-surface at end turn because strategic.py:216-221 filters on in_strategic_mode. Attaching only the FIRST marshal's interrupt at /load leaves a second marshal's interrupt silent again — but only one interrupt popup can be up at a time, and there is no drain. Decide whether one-at-a-time-then-rediscover is acceptable, or pin it.

6. SERIALIZATION UNVERIFIED: a strategic objection stores original_command and parsed_command (strategic_executor.py:1086-1087) which strategic_executor.py:2255-2262 re-reads to re-execute. I round-tripped a SYNTHETIC dict, not a live executor-produced one. Pin that a real strategic objection survives save/load and still re-executes.

7. `redemption_event` / world.pending_redemption is the same class (world_state.py:6779/:7381, attached only at main.py:601-608 and :3260-3264) and is filed as WO-36 — do not fold it in silently.


### Corrections to the filed row

(a) "so that modal is unreachable by construction on the recovery path too" — WRONG MECHANISM. The success flag is not the constraint; the block also carries neither `state` nor `pending_objection` (measured key set: awaiting_response, choices, message, objection, success). And the modal IS reachable in normal play: both raise sites return success True (executor.py:1574; strategic_executor.py:1098).

(b) Census wording "strictly worse than WO-30 was" — OVERSTATED for pending_objection. It is comparable: the block names its own answer words (executor.py:648-655) and the state is answerable by typed trust/insist/compromise (main.py:2262-2310) and by POST /respond_to_objection. Accurate for the ORDER-FREE half of pending_interrupt (last_stand, muster_confirm) which has no block and never re-surfaces, and accurate for the unnamed third key.

(c) The row omits a third key. pending_strategic_objection survives (world_state.py:6780/:7382), is never re-attached, and — measured — BLOCKS NOTHING (executor.py:628 reads only pending_objection; a move executed cleanly with it standing), never re-surfaces, and hijacks /respond_to_objection ahead of a later genuine tactical objection (meta_executor.py:1611-1614). It is the worst of the three and shares the `pending_objection` response key, which is exactly why the response-key-based census could not see it.

(d) The filed fix is BUILDABLE — the payload reconstructs from world state alone — but for reasons the row does not give. Build it via /load's own success:True response using the strategic shape (pending_objection: True + objection dict), never by touching an executor success flag.


## WO-36 — PARTIAL

### Evidence

## Verdict: PARTIAL — every stated FACT is true; the implied SEVERITY is wrong in both directions

### 1. `world.pending_redemption` — serialized, and survives `load_game`
- Declared `backend/models/world_state.py:925` (`Optional[Dict] = None`).
- `to_dict` `backend/models/world_state.py:6779`; `from_dict` `backend/models/world_state.py:7381`.
- `save_manager.load_game` transient block clears only `battles_this_turn`, `in_combat_this_turn`, `mild_concerns_this_turn`, `gold_spent_this_turn`, `threat_sources_this_turn` — `backend/save_manager.py:187-214`. **`pending_redemption` is NOT wiped.**
- Measured (one `python -c`, scratch save file, `SOVEREIGN_SCENARIO=none`):
  `pending_redemption survives: True {...}` · `marshal.redemption_pending survives: True` · `check_redemption_threshold(...) -> None`.

### 2. `_response_has_redemption_route` — quoted in full
`godot-client/project-sovereign/scripts/main.gd:2333-2339`
```gdscript
func _response_has_redemption_route(response: Dictionary) -> bool:
	return response.has("redemption_event") and response.redemption_event != null and not response.has("enemy_phase")

func _route_redemption_response(response: Dictionary):
	if response.success:
		_display_result(response)
	_show_redemption_dialog(response.redemption_event)
```
Exact predicate: key present AND non-null AND the response is not an end-turn response (the enemy-phase path has its own handler at `main.gd:3918/3941/3956`; `_stash_redemption` carries the identical guard, `main.gd:1941-1948`).

### 3. THE CRUX — same shape, but assigned at the WRONG TIME
The response key and the world field are **the same object**, not two shapes:
- `backend/main.py:601-608` — `response["redemption_event"] = redemption_event` and `world.pending_redemption = redemption_event`, one dict.
- Same pairing at `backend/main.py:3260-3264`, `3711-3715`, `5020-5030`.
- Producer: `disobedience._create_redemption_event` → `{type, marshal, trust, message, options}` (`backend/commands/disobedience.py:1483-1514`). `GET /pending_redemption` re-emits exactly those fields (`backend/main.py:3600-3608`), and `_on_pending_redemption_checked` reassembles them (`main.gd:1979-1992`).

So **re-attachment at `/load` is trivially possible** — one line. The row's worry ("may not be able to reconstruct it") is unfounded.

**But** the world field is written ONLY at the API boundary, while `marshal.redemption_pending = True` is set at GENERATION (`backend/commands/disobedience.py:1539`) and is itself serialized (`backend/models/marshal.py:1571`, `1755`). Both autosaves run INSIDE the executor, i.e. **before** that assignment:
- `backend/commands/meta_executor.py:473` (end turn) — result carries `redemption_event` at `:419`, but `world.pending_redemption` is not set until `backend/main.py:1407` → `_apply_command_result_layers` → `_include_command_redemption_event`.
- `backend/commands/executor.py:2244` (auto-advance).

### 4. `/load` today, and what the player experiences
- `POST /load` `backend/main.py:4021-4056`: `include_popup_passthroughs=False`, then `_fill_popup_keys_without_draining` (`backend/main.py:1497-1517`) which only `setdefault`s `PopupQueue.RESPONSE_KEYS.values()` — ten keys, `redemption_event` is **not** one (`backend/models/cooldown_manager.py:166-178`). Then the WO-30 block attaches `pending_capture_choice` / `capture_data` only. **Nothing attaches `redemption_event`.** Confirmed.
- `_apply_world_swap_response` (`main.gd:4438-4475`) consults no route table except the WO-30 capture pair. So: **silence at load.**
- **Commands are NOT blocked.** `backend/commands/executor.py` contains the string "redemption" **zero times** (`grep -c` = 0); the pending-choice refusal at `executor.py:553`/`:671` is capture-only. So the player is not stuck the way WO-30's player was.
- **It is answerable, three ways.** (a) The stash-free recovery poll: `_maybe_recover_dropped_redemption()` (`main.gd:1963-1977`) → `GET /pending_redemption` → `_show_redemption_dialog`. It is called at every control-return tail — `main.gd:2077, 2131, 2144, 2167, 2537` — and `:2537` is the tail of `_on_command_result` itself (`main.gd:2341`), on both the success and failure branches. So the audience arrives **at the end of the player's first command after loading.** (b) `POST /respond_to_redemption` (`backend/main.py:3520-3582`) reads the restored `world.pending_redemption` and works. (c) A typed `grant_autonomy` / `dismiss` is intercepted client-side and posted directly (`main.gd:1336-1343`), ungated.

### 5. One-shot, but with a second delivery channel → **self-healing in the filed case**
- `check_redemption_threshold` (`backend/commands/disobedience.py:1519-1540`) sets `marshal.redemption_pending = True` **at generation** and returns `None` forever after; cleared only on resolve (`disobedience.py:1584`) or trust recovering >20 (`marshal.py:1405-1407`). **Not re-derived per turn** — measured above.
- BUT `world.pending_redemption` survives, and `GET /pending_redemption` is polled once per turn. **So the filed scenario heals itself after one command.** That makes WO-36-as-filed a genuine P3 — a one-command silence, not a lost audience.

### 6. What the row MISSES — two strictly worse siblings its fix does not touch
**(a) The end-turn autosave records the latch without the question.** Because autosave runs before the API-boundary assignment (§3), the turn's `autosave.json` has `marshal.redemption_pending=True` and `world.pending_redemption=None`. Load that save — which is the main menu's own **Continue** arm — and `GET /pending_redemption` answers `has_pending: False` while `check_redemption_threshold` answers `None` forever. **Permanent loss, no recovery, and re-attaching the world field at `/load` does nothing for it.**

**(b) The auto-advance path never hoists the event at all.** `meta_executor.py:398-405` + `:419` hoist `redemption_event` out of `tactical_events`; the auto-advance mirror at `backend/commands/executor.py:2076-2089` hoists only `battle_report` — and `executor.py` has zero "redemption" occurrences. The producers are the cavalry/fortify limits (`world_state.py:11767`, `:11792`), which ride `_last_tactical_events` (`world_state.py:9440`) and survive the fog filter (no marshal/nation/location key → kept, `meta_executor.py:90-101`). So a player who ends the turn by spending their last AP **loses the audience with no save involved.**

**(c) Latent third:** `meta_executor.py:404` takes only the FIRST tactical redemption (`and not tactical_redemption`); a second marshal crossing in the same tick is latched and dropped.

I could NOT confirm: any of the Godot-side behaviour by running the client (all read from source); that every early return in `_on_command_result` reaches a tail that polls (I checked the enemy-phase and modal-dismissal tails, not all shapes); how often a real save carries a live `world.pending_redemption` (needs a played session — the manual-save case depends on the pause menu opening over the modal, unverified); and I did not run the suite.

### Fix shape

## The filed fix is coherent but low-value; build the seam fix instead

**Do NOT build only what WO-36 asks.** The one-line `/load` re-attach closes a one-command cosmetic silence and leaves the two permanent-loss cases open.

### Recommended: move the world-field write to GENERATION (one seam, closes all three)
In `backend/commands/disobedience.py:1539`, where `check_redemption_threshold` already mutates `marshal.redemption_pending = True`, also set `world.pending_redemption = event` before returning. Then:
- the end-turn/auto-advance autosaves carry it (kills §6a),
- `GET /pending_redemption` covers the auto-advance hole for free with **no** `executor.py` change (kills §6b),
- the four API-boundary assignments (`main.py:607, 3263, 3714, 5021`) become idempotent no-ops — leave them, they are the response-key half,
- `/load` re-attach becomes meaningful rather than decorative.

### Then close WO-36 proper, client-side, with zero backend change
In `_reset_frontend_state_for_world_swap` (`main.gd:4392-4435`) add `_redemption_recheck_turn = -1` — it is **not** reset today, so a same-turn reload (the ordinary savescum) skips the poll for the rest of that turn. This is the identical hazard IGR-F review [5] fixed for `_envoy_digest_shown_turn` at `main.gd:4426`, left unfixed for this var.
Then call `_maybe_recover_dropped_redemption()` from the tail of `_apply_world_swap_response` (`main.gd:4474`, beside `set_input_enabled(true)`).

That delivers the audience **at load** instead of one command later, reuses the endpoint written for exactly this, and needs no `/load` payload change.

### If the census pin's symmetry with WO-30 matters, add the belt too
`backend/main.py:4056`, beside the WO-30 block:
```python
if world.pending_redemption:
    response["redemption_event"] = world.pending_redemption
```
and move `redemption_event` from `KNOWN_SILENT_AT_LOAD` to `LOAD_REATTACHED` in `tests/test_wo_slice15_capture_question_holds.py:775-782`.

### Cheap drive-bys found in passing
- `main.gd:4071` tells the player to type `demand_obedience`; `backend/main.py:3541` accepts only `grant_autonomy` / `administrative_role` / `dismiss`, so the text fallback advertises a rejected verb (and `main.gd:1336` forwards it).
- `meta_executor.py:404`'s first-wins hoist (§6c).

### Hazards

## Hazards

1. **Do NOT copy WO-30's client half verbatim.** `_route_redemption_response` (`main.gd:2336-2339`) calls `_display_result(response)` when `success` — on a `/load` response that re-renders the whole load payload a second time (events, dispatch, war HUD, and `_display_result`'s own tail at `:2537`). WO-30 could get away with `raised.erase("message")` because `_show_capture_choice_dialog` prints only the message; that trick is **not sufficient** here. Call `_show_redemption_dialog(...)` (or the GET recovery) directly.

2. **Writing `world.pending_redemption` inside `check_redemption_threshold` makes the checker a world mutator.** It already mutates `marshal.redemption_pending`, so this is in character, but: it changes which event wins when two marshals cross in one tick (today `meta_executor.py:404` keeps the FIRST; the seam fix would leave the LAST in the world field while the response key still carries the first — they would disagree). Decide and pin, or fix §6c at the same time.

3. **Tests will move.** `tests/test_endpoint_wiring.py:281` (`test_no_pending_redemption`), `tests/test_bombardment.py:714/1016/1176` and `tests/test_redemption_v2b.py` assert around this flag; several set `redemption_pending = False` by hand precisely because the checker latches. Several assert `world.pending_redemption is None` after a check-only call.

4. **`BASELINE_SERIES` / M1–M7 should not move** — nothing reads `world.pending_redemption` except `GET /pending_redemption` and `POST /respond_to_redemption` (grep is exhaustive: `main.py:607/3263/3538/3714/5021`, `world_state.py:925/6779/7381`). No AI or combat path reads it. Verify rather than assume.

5. **Sibling cross-campaign leak, independent of this row:** `_reset_frontend_state_for_world_swap` (`main.gd:4407-4431`) clears `pending_redemption` (the bool) but **not** `pending_redemption_data`, `pending_proclamation_data` or `pending_diorama_data`. A stash live from the previous campaign survives a load and will be raised at the next control return — showing the OLD world's marshal, and (for redemption) also short-circuiting `_maybe_recover_dropped_redemption`'s `pending_redemption_data != null` guard so the NEW world's real question is never polled for. Fixing `_redemption_recheck_turn` without also clearing the stash leaves that path open.

6. **Severity honesty.** If this row is written up as "the same family as WO-30", that is wrong: WO-30's player was refused every subsequent order, WO-36's player loses one command's worth of silence and is then asked. The parts that deserve P1/P2 attention are §6a (autosave-before-assignment, permanent, on the Continue path) and §6b (auto-advance, permanent, no save needed) — neither of which is what the row says.


### Corrections to the filed row

## Corrections to the filed row

1. **"no /load path re-attaches it" — TRUE** (`backend/main.py:4021-4056`; `_fill_popup_keys_without_draining` covers only the ten `PopupQueue.RESPONSE_KEYS`, `cooldown_manager.py:166-178`). Confirmed.

2. **"main.gd gates the redemption modal on the response key" — TRUE BUT INCOMPLETE.** There is a **second, key-free delivery channel** the row does not mention: PT-B1's once-per-turn `GET /pending_redemption` poll (`main.gd:1963-1977` → `backend/main.py:3584-3608`), called at five control-return tails including `_on_command_result`'s own (`main.gd:2537`). Combined with the fact that **nothing blocks commands on a pending redemption** (`executor.py` contains "redemption" zero times), the load silence lasts exactly one command. **The row's severity framing is refuted: this is self-healing, a P3 copy/timing issue, not a lost audience.**

3. **The crux the prompt asked about resolves the OTHER way.** The response key is not "built fresh by a producer" — `main.py:607` assigns the *same dict* to both, so `/load` could reconstruct it in one line. The real timing problem is the reverse: the world field is written **too late** (at the API boundary, after both autosaves at `meta_executor.py:473` / `executor.py:2244`), so the save that matters most — the end-turn autosave, i.e. the main menu's Continue — records `marshal.redemption_pending=True` with `world.pending_redemption=None`. That case **is** a permanent loss, and the row's prescribed fix does nothing for it.

4. **A defect of the same family exists with no save involved:** the auto-advance turn path (`executor.py:2076-2089`) hoists `battle_report` from `tactical_events` but never `redemption_event`, unlike its end-turn sibling (`meta_executor.py:398-419`). A last-AP turn advance that triggers a cavalry/fortify redemption (`world_state.py:11767`, `:11792`) latches the marshal and drops the audience permanently.

5. **Latent third:** `meta_executor.py:404` hoists only the first tactical redemption per turn; a second marshal crossing the threshold in the same tick is latched and silently dropped.

6. Minor: the text fallback advertises `demand_obedience` (`main.gd:4071`, forwarded at `main.gd:1336`) which `backend/main.py:3541` rejects as invalid.


## wo35_wo36_client_half_general_raise — PARTIAL

### Evidence

SCOPE: "is the general raise safe" — safe but INERT, and it cannot fix WO-35/WO-36 as filed.

(1) _apply_world_swap_response NOW = main.gd:4438-4474. Body: _reset_frontend_state_for_world_swap(true) :4440; _sync_response_hud :4441; _process_active_wars :4442; tutorial_overlay.on_world_swap :4447; prints success_text + response.message :4449-4455; then slice 15's raise :4466-4471 (`if _response_has_capture_choice_route(response): var raised = response.duplicate(); raised.erase("message"); _route_capture_choice_response(raised); return`); tail `set_input_enabled(true); command_input.grab_focus()` :4473-4474. Callers: _on_load_result main.gd:4481, /new_game main.gd:5941. Input already disabled on entry via _on_load_save_selected main.gd:4388.

(2) ROUTE TABLE — _configure_response_routes main.gd:1825-1846. Machine-derived matcher/show keys:
PRE  objection (1904/…, matcher main.gd:1863-1869) match={state,pending_objection,success} show=_show_objection_dialog main.gd:3811-3856 consumes objection.* OR top-level marshal/personality/message/trust/trust_label/vindication/authority/suggested_alternative/compromise/options/tone/concern_level/trust_gain/insist_penalty/compromise_gain
PRE  glorious_charge (main.gd:1876-1877) match={pending_glorious_charge} show=main.gd:4501-4520 consumes marshal,target,recklessness
POST commitment_paradox (main.gd:1885-1891) match/show={commitment_paradox_popup}
POST capture_choice (main.gd:1893-1897) match={pending_capture_choice} show=main.gd:4254-4288 consumes capture_data,message,action_summary,game_state
POST marshal_petition (main.gd:1899-1909) match/show={marshal_petition}
POST diplomatic_objection (main.gd:2214-2222) match/show={diplomatic_objection}
POST incoming_proposal (main.gd:2224-2231) match/show={incoming_proposal} (+_dismissed_proposal_nation filter)
POST incoming_settlement_offer (main.gd:2234-2252) match/show={incoming_settlement_offer,diplomatic_dialogue}
POST proposal_confirm (main.gd:2254-2272) match={diplomatic_dialogue} show adds {success,error_display,message}
POST clarification (main.gd:2286-2290) match={state=="awaiting_clarification"} show=main.gd:4888-4907 consumes clarification_data,clarification_registered
POST interrupt (main.gd:2292-2311) match={pending_interrupt,strategic_reports} show=main.gd:4763-4776
POST diplomatic_sabotage (main.gd:2313-2321) match/show={diplomatic_sabotage}
POST vassal_rebellion (main.gd:2323-2331) match/show={vassal_rebellion_imminent}
POST redemption_event (main.gd:2333-2337) match={redemption_event,enemy_phase} show={redemption_event}

(3) /load RECONSTRUCTIBILITY — THE GOVERNING FINDING. /load (main.py:4021-4057) builds with include_popup_passthroughs=False (:4037-4043) then calls _fill_popup_keys_without_draining (:4044), whose whole body is `for response_key in PopupQueue.RESPONSE_KEYS.values(): response.setdefault(response_key, None)` (main.py:1516-1517). So EVERY queue key is present-and-NULL and every non-queue key (state, pending_objection, pending_glorious_charge, pending_interrupt, diplomatic_dialogue, redemption_event) is ABSENT. All 11 non-capture matchers test !=null or has(). => capture_choice is the ONLY route that can match a world-swap response today (main.py:4054-4056 is the slice-15 attach).
Per-route: YES (state serialises, payload complete) = commitment_paradox (world_state.py:7011,7776), marshal_petition (7400-7410, explicitly re-pushed to queue), diplomatic_objection (7151,8041), incoming_proposal (7152,8042), incoming_settlement_offer (7153,8045), proposal_confirm (dialogue mgr 6882/7554, ids round-trip dialogue_manager.py:673-679,682-696), interrupt (marshal.py:399,1629,1825), diplomatic_sabotage (7146,7998), vassal_rebellion (7147,7999), redemption_event (world_state.py:925,6779,7381 + GET /pending_redemption main.py:3584-3607), capture_choice (already done).
PARTIAL = objection (dict serialises world_state.py:6778,7380 but tactical-vs-strategic SHAPE is not recorded, and trust/authority/vindication are HUD fields absent from the dict); clarification (question is in the dialogue manager but `state` and clarification_registered are per-command).
NO = glorious_charge — marshal.pending_glorious_charge is a bare bool (marshal.py:510,1680,1877); the TARGET is stored nowhere.

(4) INPUT-RE-ENABLE AUDIT — pattern HOLDS for every candidate. No candidate popup has a close-X or bare-hide exit: every hide() is followed by an emit (objection_dialog.gd:265/270/275, redemption_dialog.gd:107/112/117, capture_choice_dialog.gd:111/116, interrupt_popup.gd:91, talleyrand_objection_popup.gd:92/97/102, incoming_proposal_popup.gd:146-161, sabotage_discovery_popup.gd:56/61, vassal_rebellion_popup.gd:61/66/71, marshal_petition_dialog.gd:142/147, proposal_confirm_popup.gd:1757/1766). ESC cannot orphan a visible modal — _unhandled_input main.gd:941-966 refuses to open pause while _is_modal_dialog_open() and never hides a modal, so a raised modal's own buttons are the ONLY exit. Network cannot orphan — api_client.gd:72,266,273,289,292 always invoke the callback; terminal handlers re-enable (_on_command_result fallthrough main.gd:2404; _on_capture_choice_response main.gd:4322 line 1; _on_glorious_charge_response :4597; _on_interrupt_response -> _process_next_interrupt :4862-4885).
THE ONE HAZARD: _on_commitment_paradox_choice main.gd:5096-5107 calls set_input_enabled(false) UNCONDITIONALLY at :5101 and only sends inside the two branches (honor_defender / break_defender_alliance). A third emitted string = input disabled, no request, and per the ESC finding no pause menu => unrecoverable. Latent only because commitment_paradox_popup.gd:68-78 emits exactly those two.
SECOND (would BECOME live if objection is raised): _show_objection_dialog main.gd:3813-3814 branches shape on pending_objection==true; a /load attach without a discriminator renders the strategic case against a flat dict => options == [] => a modal with no buttons => unrecoverable soft-lock.

(5) GENERAL SHAPE — NO. (a) Inert: nothing but capture_choice matches (§3). (b) For the 8 queue-backed routes a general backend attach means calling the DRAINING _include_popup_passthroughs at /load, which is exactly the PC15-10 F7-1 defect recorded in /load's own docstring main.py:4025-4031 ("the default drain destroyed the highest-priority popup from_dict had just RESTORED — one popup lost per load"); they already self-deliver on the next /command. (c) The remaining four differ individually. Correct contract = per-key, same predicate/route pair as the command path, backend attaches ONLY keys whose durable state is a plain world/marshal attribute rather than a PopupQueue member — today {pending_capture_choice, pending_interrupt} (+pending_objection if shape resolved).

(6) PINS. tests/test_popup_routing_registry.py: :36-60 pins 12 route ids in exact relative order as a SUBSEQUENCE (marshal_petition + incoming_settlement_offer are live but unlisted, so inserting is legal / reordering is not); :62-81 forbids inline *_popup.show_*( ONLY inside _on_command_result — _apply_world_swap_response is unconstrained, which is why slice 15 could route there; :83-91 requires _route_response_ui to keep call(matches)/call(show)/_process_active_wars/return true — note _route_response_ui main.gd:1858 calls _process_active_wars AGAIN, which _apply_world_swap_response already did at :4441. tests/test_wo_slice15_capture_question_holds.py::test_blocking_state_surface_census :737-813 requires >=12 matchers (:788) and every matcher-body response key classified into QUEUE_DELIVERED / LOAD_REATTACHED {pending_capture_choice,capture_data} / TRANSIENT_BY_DESIGN / KNOWN_SILENT_AT_LOAD :769-779. Fixing WO-35/36 REQUIRES editing :769-779 to move the keys into LOAD_REATTACHED. The census reads matcher bodies ONLY, not show bodies — capture_data is covered only because hand-listed, so a route whose payload key appears only in `show` is invisible to it. :730-735 pins the two function names slice 15 reuses.

### Fix shape

DO NOT build "raise the first matching post-HUD route at world swap". It is inert today and wrong for 8 of 12 routes.

Build instead, in this order:

1. PRE-REQUISITE (blocks everything else, 1 line): main.gd:5101 — move set_input_enabled(false) inside the two branches of _on_commitment_paradox_choice, or add a trailing else that re-enables. Given ESC cannot open the pause menu over a visible modal (main.gd:961-964), this is one added option away from an unrecoverable soft-lock, and any world-swap raise puts weight on it.

2. WO-36 — NOT a /load attach. Add `_redemption_recheck_turn = -1` and `pending_redemption_data = null` to _reset_frontend_state_for_world_swap, beside the existing `_envoy_digest_shown_turn = -1` at main.gd:4426. Zero backend change. The PT-B1 poll at main.gd:2537 then fires on the player's first command after a load, which is the designed recovery. While there, also clear pending_proclamation_data / pending_diorama_data / last_battle_diorama (the cross-campaign stash leak). Census: give redemption_event a FOURTH classification ("recovered by poll"), do not move it to LOAD_REATTACHED. Test: load into a turn already polled this session and assert the poll fires.

3. WO-35 pending_interrupt — the real fix, and the only one that earns a /load attach. Backend: at the tail of /load (beside the WO-30 stamp, main.py:4054-4056) scan world.get_player_marshals() for a marshal with pending_interrupt and stamp response["pending_interrupt"]. Client: extend the slice-15 raise in _apply_world_swap_response to consult _response_has_interrupt_route / _route_interrupt_response through the same predicate/route pair. Safe by audit: the matcher's strategic_reports deferral is vacuously satisfied at load, and _process_next_interrupt (main.gd:4862-4885) falls to set_input_enabled(true) against the just-cleared interrupt_queue. This also closes the ACTIVE harm at main.py:2169-2245 (a post-load unaddressed command being eaten as an answer to an invisible question). Census: move pending_interrupt to LOAD_REATTACHED.

4. WO-35 pending_objection — reduce to P3 and either fix the shape first or leave it declared. It already has a working text self-heal (executor.py:651-655 names the words; main.py:2668-2704 accepts them), so this is legibility, not recovery. If built, the attach MUST also stamp a tactical/strategic discriminator, because _show_objection_dialog (main.gd:3813-3814) branches on pending_objection==true and a flat dict renders options==[] — a buttonless modal with no ESC exit. Cheaper alternative that needs no new state: have the executor block also emit the keys an existing route reads, so the block re-raises the modal the way the capture block already does (executor.py:689-695) — i.e. make the objection block match the capture block's shape rather than teaching /load a new one.

5. NEVER attach the 8 queue-backed keys at /load (commitment_paradox, marshal_petition, diplomatic_objection, incoming_proposal, incoming_settlement_offer, diplomatic_sabotage, vassal_rebellion, and proposal_confirm's diplomatic_dialogue). They already self-deliver on the next /command via _include_popup_passthroughs, and delivering them at /load requires the draining passthrough — the exact PC15-10 F7-1 regression documented at main.py:4025-4031. Keep them classified QUEUE_DELIVERED.

6. glorious_charge — classify TRANSIENT_BY_DESIGN permanently, never KNOWN_SILENT. The target is not serialised anywhere (marshal.py:510 is a bare bool), so the payload cannot be rebuilt and an attach would post an empty target.

7. Pin edits required by any of the above: tests/test_wo_slice15_capture_question_holds.py:769-779 (move keys out of KNOWN_SILENT_AT_LOAD). Consider widening that census to read `show` bodies too — it reads matcher bodies only, so capture_data is covered only because it was hand-listed and a future route's payload-only key would slip through. Note _route_response_ui (main.gd:1858) calls _process_active_wars, which _apply_world_swap_response already called at :4441 — avoid the double call if the raise is widened.

### Hazards

1. SOFT-LOCK (latent, must fix first): _on_commitment_paradox_choice main.gd:5101 disables input unconditionally before a two-branch send. A third emitted choice = input disabled, no request in flight, and ESC cannot open the pause menu while a modal is visible (main.gd:961-964) => unrecoverable. Not live today only because commitment_paradox_popup.gd:68-78 emits exactly two strings.

2. SOFT-LOCK (would become live): a /load attach of pending_objection without a tactical/strategic discriminator. _show_objection_dialog main.gd:3813-3814 branches on pending_objection==true; a flat saved dict renders the strategic arm's objection.get("options", []) as [] => a modal with no buttons and no ESC exit. The saved dict records no discriminator (world_state.py:6778).

3. REGRESSION: a general backend attach for the queue family requires the DRAINING _include_popup_passthroughs at /load — precisely the PC15-10 F7-1 bug recorded in /load's own docstring (main.py:4025-4031): one restored popup destroyed per load, silently.

4. DOUBLE CALL: _route_response_ui main.gd:1858 calls _process_active_wars(response); _apply_world_swap_response already called it at :4441. Widening the raise to the shared dispatcher double-calls it.

5. ORDERING: PRE-HUD routes must not be raised the way slice 15 raised a POST-HUD one. _on_command_result runs pre-HUD BEFORE _sync_response_hud (main.gd:2381-2385), while _apply_world_swap_response syncs at :4441 — raising `objection` at world swap inverts that order.

6. UNRECONSTRUCTIBLE PAYLOAD: pending_glorious_charge's target exists nowhere in the save (marshal.py:510 bool only). Any attach posts an empty target.

7. CROSS-CAMPAIGN LEAK (unfiled): pending_redemption_data, pending_proclamation_data, pending_diorama_data, last_battle_diorama and _redemption_recheck_turn all survive _reset_frontend_state_for_world_swap (main.gd:4384-4436). A previous campaign's stash raises at the next control return in the newly loaded world; the stale _redemption_recheck_turn is simultaneously WO-36's real cause.

8. CENSUS BLIND SPOT: test_blocking_state_surface_census (tests/test_wo_slice15_capture_question_holds.py:797-805) parses MATCHER bodies only. capture_data is classified solely because it is hand-listed at :773. A future route whose payload key appears only in its `show` function passes the pin unclassified.

9. ROUTE-ORDER PIN is a subsequence check (tests/test_popup_routing_registry.py:36-60): it lists 12 ids while the live table has 14 (marshal_petition, incoming_settlement_offer unlisted). Inserting a route is legal; reordering silently reds it.

10. SILENT HIJACK (active, pre-existing, worsens the WO-35 case): with a restored pending_interrupt, main.py:2169-2245 consumes the next matching unaddressed typed command as the answer to a question the player was never shown.


### Corrections to the filed row

MECHANISM CORRECTIONS — both filed rows have the right OUTCOME and the wrong MECHANISM; for WO-36 the prescribed fix is wrong.

WO-35 / pending_objection:
- CONFIRMED: serialises (world_state.py:6778,7380); /load never re-attaches (main.py:4021-4057).
- MISLEADING: "worse — the route requires success==true while the objection block returns success: False". True as a fact, wrong as a diagnosis. executor.py:645-657 returns {"success": False, "message":..., "awaiting_response": True, "objection":..., "choices":[...]} — it omits `state` AND `pending_objection` too, and NO route anywhere reads `objection` (§2 table). Flipping success to True raises nothing. Anyone "fixing" this by relaxing the success guard changes zero behaviour and weakens a guard.
- REFUTED: "neither has WO-30's one-refused-command self-heal narrative". executor.py:651-655 names the exact answer words, derived from the same _choices list the validator uses; main.py:2668-2670 routes the bare token and main.py:2678-2704 routes one-word natural language, both into _respond_to_objection_sync. That IS the self-heal narrative. P3 legibility gap, not a P2 dead end.
- THE COMPARISON RUNS THE OTHER WAY: WO-30 was BETTER off than the row implies. The capture block returns "pending_capture_choice": True + "capture_data" (executor.py:689-695) and _response_has_capture_choice_route does NOT require success — so the first refused command RE-RAISED THE MODAL IN FULL. Slice 15 converted a one-command delay into an immediate raise; it did not recover an unreachable state.

WO-35 / pending_interrupt: CONFIRMED and UNDERSTATED. Serialised marshal.py:399/1629/1825, never attached at /load, and there is no refusal message at all — main.py:2169-2245 CONSUMES the next typed command as an interrupt answer whenever _interrupt_choice_from_text matches an option (guarded only by explicit address :2185-2211 and _addressed_fresh_order_elsewhere :2209). After a load an unaddressed "press on" is silently applied to a question never shown. This is the only member whose recovery is actively HARMFUL and the strongest candidate for the WO-35 fix.

WO-36 / redemption_event: the row's "main.gd gates the modal on the response key" is CONFIRMED BUT INCOMPLETE, and that changes the fix. PT-B1 already shipped a recovery: _maybe_recover_dropped_redemption main.gd:1963-1976 polls GET /pending_redemption (main.py:3584-3607) and is called from _on_command_result's ORDINARY TAIL at main.gd:2537 (plus dismiss tails 2077/2131/2144/2167). The audience DOES surface on the first command after a load. The real defect is a STALE LATCH: the poll returns early when _redemption_recheck_turn == current_turn (main.gd:1969) and _reset_frontend_state_for_world_swap does NOT reset _redemption_recheck_turn — while it DOES reset the structurally identical _envoy_digest_shown_turn at main.gd:4426, with a comment at 4419-4425 naming this exact failure ("Reload-the-current-turn is the ordinary savescum pattern"). Also: pending_redemption blocks NOTHING — it appears nowhere under backend/commands/. Lost moment, not stuck state.

UNFILED, found in passing: _reset_frontend_state_for_world_swap (main.gd:4384-4436) clears pending_enemy_phase_response/pending_dispatch_data/pending_strategic_response/pending_redemption/interrupt_queue/_envoy_digest_shown_turn but NOT pending_redemption_data, pending_proclamation_data, pending_diorama_data, last_battle_diorama, _redemption_recheck_turn — so a stash from the PREVIOUS campaign survives a load and raises at the next control return in the new world.

NOT CONFIRMED (stated as limits, not claims): I did not execute /load — the "all queue keys null" claim is read off main.py:4021-4057 + 1497-1517 + 379-448, not measured. No engine boot / parse harness — all client claims are source-level. Did not trace send_dialogue_response's dialogue_id comparison end to end (ids do round-trip, so the W6-0 stale guard SHOULD pass on a restored settlement dialogue). Did not read strategic.py to see whether a restored pending_interrupt re-emits a requires_input report next end turn, which would be a second delayed recovery via main.py:1303-1311. No playtest, so no frequency/impact data.
