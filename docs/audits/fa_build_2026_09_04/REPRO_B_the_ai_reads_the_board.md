AGENT B — reproduction report (read-only; all probes/outputs in scratchpad\agentB\: probe_*.py + .out.txt, series_arm.py/series_count.py + series_*/count_*.out.txt, arm_patches.py + armplugin.py + pytest_*.out.txt, thrash_*.out.txt). Fixes were tested WITHOUT touching the repo: each fix_shape was applied in-process by re-exec'ing a patched copy of the seam (arm_patches.py), then (i) the 40-turn ambient series was re-run (control arm reproduces BASELINE_SERIES byte-for-byte, so the runner is faithful) and (ii) every test file that names the seam was run under the arm via a pytest plugin. M1–M7 (tests/test_combat_sweep_metrics.py) call resolve_battle directly and never touch EnemyAI, advance_turn, the garrison resolver or _check_cavalry_limits, so NO row here can move them — a fact about the harness, not evidence.

=== FA-8 (P1) P4.25 prices the garrison, ignores the field army ===
(a) Reproduced exactly per the row: Charles 29k @Franconia, 101,000 French + 10k garrison in Munich -> `_find_garrison_attack` prints `[GARRISON ASSAULT] ArchdukeCharles attacking garrison at Munich (ratio 2.32 >= 1.35)` while `get_live_visible_enemies_in_region('Munich','Austria')` = [Ney, Lannes, Bernadotte, Massena]; P4 (`_find_attack_opportunity`) on the same board returns None (field ratio 29000/101000 = 0.29). Executed through the shared executor the order became a FIELD battle vs Ney (the first dict-order marshal, `get_enemy_at_location_for_nation`, combat_executor.py:4819): Charles 29,000 -> 19,667, Ney -824, Munich garrison untouched at 10,000.
Full decision path: the row's synthetic board does NOT reach P4.25 — Charles standing on Bavarian Franconia goes P-1 `attack Franconia`; on Austrian Tyrol he goes P3 stance_change->P5 fortify (probe_fa8/fa8b). The live loop needs the state the digest itself records ("ArchdukeCharles's DEFENSIVE stance hampers offensive operations"): already DEFENSIVE with refortify on cooldown (the stagnation unfortify) -> `_check_threats` (2473-2558) returns None -> `_evaluate_marshal` returns `attack Munich` at priority 4 (probe_fa8c C1). In that state one phase issued `attack Munich` TWICE (both resolved vs Ney; 29,000 -> 12,275, retreating) and the futility tracker wrote `ArchdukeCharles:Ney: 2` — a key only P4 reads (2838-2860); P4.25 never consults it. Ambient 40-turn run: 4 `[GARRISON ASSAULT]` orders, only 2 resolved as garrison combat — the other 2 were exactly this defect in the wild.
(b) `_find_garrison_attack` 3502-3571 (loop 3521, controller/war skip 3529-3532, crossing gate 3541-3546, ratio 3558, return 3560-3567); rung call 1980-1990; P4.5 sibling gate 3444 (OMNISCIENT `_get_hostile_marshals_in_region`); P3.7 3174; P-1 1558 and P0 1607 use the FOG-AWARE `get_live_visible_enemies_in_region`; executor garrison branch combat_executor.py 4970-4996 (`defenders` list 4972-4975, `_resolve_garrison_combat` 4992). All row line numbers within ±3.
(c) Corrections: the siblings are SPLIT between fog-aware and omniscient predicates (equivalent for an adjacent scan, since adjacency grants visibility — 3722-3726). The row's P7.5 "sibling" arms are at 3840-3856 and 3857-3883 (not 3842-3876). The futility brake is not merely "not built" — it is being WRITTEN for these battles and never read by the rung.
(d) Callers: `_find_garrison_attack` has ONE caller (1987). `_defending_strength_in_region` read at 1632 (P0) and 2792 (P4) only.
(e) GR5: the player's `attack Munich` resolves through the same 4970 branch, but the player has the CA9-F1 muster preview / attack-confirm gate that prices the field; the AI has nothing.
(f) Breaks: `tests/test_garrison_system.py::TestGarrisonAI::test_ai_p425_finds_garrison_attack` FAILS under the row's exact gate — on the legacy fixture Davout and Drouot boot IN Paris and Britain at Belgium sees Paris (measured; the sibling `TestGarrisonCombat.setup_method` moves the French away, this class does not). All other pins pass: test_ai_garrison.py, test_ai_bombardment.py, test_naval_host_rule.py (Normandy empty at boot), test_enemy_ai.py, test_tutorial_scenario.py (234/235). BASELINE_SERIES DIVERGES at index 5 (2 gate skips in 40 turns) — conscious flip-attributed re-record. Tutorial: does NOT depend on P4.25 at all — Austria and Bavaria are at PEACE in tutorial_1805.json, so the war check at 3529 skips Munich; Schwarzenberg's t3/t5 attacks on Ney are P4 priced against the men standing in Munich (Ney alone 15.5k -> attack; Ney+Senarmont 29k -> None), Davout reinforcing from Swabia at resolution; stagnation returns MOVE on that board; tutorial_lesson.json and tutorial_overlay.gd gate only on the player's own t4 battle (`_pred_battle_happened`). Sweeping P7.5 onto field pricing would also not touch the lesson.
(g) Fix: the row's one-line fog-aware skip at 3521 (before the terrain block at 3548) — "a garrisoned province with a field army is P4's business"; re-site the legacy test's French marshals (as its sibling does) and keep it as the positive control beside a new negative pin; one BASELINE_SERIES re-record; sweep the two P7.5 arms in the same slice (they also need N80's crossing gate).

=== FA-27 (P2) form square then attack, same phase + FA-N38 ===
(a) Reproduced: Austria phase = [Charles form_square, John stance_change, Mack drill, Charles attack Deroy]; Charles `square_formation` False after, `ai_square_cooldown` 0; `_auto_break_square` (tactical_executor.py 461-485) writes no cooldown (verified by source); form_square cost 1 (world_state.py:942). FA-N38 confirmed on `_counter_punch_shape`: `_find_attack_opportunity(Moore)` -> None while `_get_counter_punch_action` -> attack Murat; real phase = form_square, attack Murat, fortify, unfortify, attack Berry, attack Gascony.
(b) P2.5 1793-1841, form condition 1835-1836 (row exact), PT-F6 latch 951-954 + 1135; P3.25 call 1884-1885; P4 call 1973; P4.25 1980-1990; P4.5 ~1995; in-square stance guard 1386.
(c) Corrections, measured under an arm gating BOTH producers (the FA-N38 shape): (1) FA-N38's collision claim does NOT hold — tests/test_ai_square_thrash.py passes 333/333 because `test_production_ai_forms_square_at_most_once` only asserts SOME attack after form_square, and the P4.5 capture `attack Berry` supplies it; that pin is weaker than its docstring ("the counter-punch broke it"). (2) The two-producer gate is itself defeated on that shape: fortify -> form_square -> attack Murat, because `_get_counter_punch_action` returns None while FORTIFIED (2571-2573) and `_execute_form_square` (combat_executor.py:7350, "forming square auto-breaks fortification") unfortifies him — the strike the gate looked for reappears one action later. (3) The row's positive control (Murat 60k) is wrong: Charles still attacks — `form_square, attack Munich`, a P4.25 GARRISON ASSAULT on Munich with Murat's 60,000 standing in it (FA-8, live) — a third breaker. Six attack producers sit below P2.5 (P3.7, P3.9, P4, P4.25, P4.5, P7.5) plus P-1/INTENT above it.
(d) `_auto_break_square` callers: combat_executor 4131/4495/7597, movement 333, strategic 516, economy 519/968, tactical 187/310/620.
(e) GR5: the player forms square by choice; the break-on-act rule is shared.
(f) Nothing in the pinned files breaks (test_ai_square_thrash, test_square_formation, ca8, pc3_pc9, enemy_ai). BASELINE_SERIES diverges at index 4 EVEN with RNG state restored around the probe calls (fa27_rngsafe) — the gate removed 16 of 32 ambient squares; and as written the gate double-evaluates P4/P3.25 inside P2.5, each drawing `random.uniform` in `_get_mood_adjusted_threshold` (486-520) — an RNG shift on top of the decision change.
(g) Fix structurally, not by predicate: keep the BREAK arm and the PT-F6 latch/stance guard at P2.5, but evaluate the FORM arm only after every strike rung has declined (below P4.5, still returned at priority 2), so no strike computation is duplicated; and have `_auto_break_square` set `ai_square_cooldown = 2` for AI marshals so a broken square is not re-formed next phase. Positive-control test must move the Bavarians away AND empty/neutralise Munich's garrison.

=== FA-N6 (P2) broken army frozen only for the player ===
(a) Reproduced (legacy fixture): retreated_this_turn -> stance_change defensive (CA8-14 holding); broken=True, strength 1,000 -> P-1 `attack Lyon` at priority 0; `decide_single_action` executed it: Lyon France -> Austria, `broken` still True. Player mirror: broken Ney `attack` -> "[BROKEN] ... Only recruitment is possible" (executor.py 1623-1642, inside the player-only branch at 1454). 1805: broken 20k Mack at Vienna DRILLED (P6) — still an action the player is refused; the row's "marched and retook Bohemia" shape was not reproduced on my board (Charles at Hungary took Bohemia instead), but the mechanism is confirmed by AST: `_evaluate_marshal` never reads the ACTING marshal's `broken`.
(b) Limiter 1587-1598 (row 1583); P-1 predicate 1553-1557; INTENT capture 1486-1528 (also ungated); executor rows 1454/1623 (row 1359/1531).
(c) Corrections: the title over-claims — `_get_stagnation_action` 3761, `_find_liberation_target` 3264 and `_consider_consolidation` 3903 DO refuse a broken acting marshal; the attack/capture rungs don't. Fix_shape's "the stance/wait pair is precisely what the player's broken marshal is left with" is false — the player is left with RECRUIT ONLY; a defensive stance_change by a broken AI corps still spends AP on an action the player cannot take (acceptable, but say so).
(d) `broken` writers: combat_executor 3590; world_state 12431/12473/12613/12617.
(f) Pinned files pass (355: ca8 TestCA814, enemy_ai, recapture, retreat_system*). Ambient: 0 broken AI marshal-turns in 40 turns — BASELINE_SERIES byte-identical because the guard never bound, not because it is safe.
(g) Fix as row, plus gate the INTENT capture block (it precedes the limiter) on the same predicate.

=== FA-N7 (P2) counter-punch: no odds floor, priced against one man ===
(a) Reproduced clean: John cautious 20k DEFENSIVE + counter_punch at Tyrol, Massena 60k at Milan the ONLY adjacent enemy -> `_get_counter_punch_action` -> attack Massena at ratio 0.33 (threshold 1.30); `_evaluate_marshal` -> (attack, 3); executed free: 20,000 -> 11,443, retreating. Two 15k corps at Milan -> it picks Lannes (one-man 1.33) while P4 prices the 30k field (0.67) and declines. The counter-punch never increments `attacks_this_turn` (7331).
(b) 2560-2627; loop 2609-2616 (ratio 2610); return 2618-2625; call 1884-1885; `_evaluate_target_ratio` 2410+. Source check: no `_get_mood_adjusted_threshold`, no `_defending_strength_in_region` in the function (row exact).
(c) Caveat on the fix_shape: `_get_mood_adjusted_threshold` draws `random.uniform` (486-520); a new call site shifts the module RNG stream. Both arms (mood draw / RNG-free base threshold) diverge BASELINE_SERIES at index 14 with 3 floor refusals in 40 turns — the decision itself moves the series; re-record consciously and draw the threshold only when a target exists.
(f) Pinned files all pass (436): test_ai_square_thrash (its breaker silently becomes the P4.5 capture — see FA-27), test_counter_punch_ap_gate, test_mc_personality_assurance, test_naval_host_rule, test_enemy_ai/_behavior, ca9.
(g) Fix as row (field via `_get_hostile_marshals_in_region` + threshold floor).

=== FA-N54 (P3) `_check_cavalry_limits` is player-only ===
(a) Reproduced: Murat and Paget both DEFENSIVE + fortified with counters at 3 -> events only for Murat (stance forced + unfortify, trust 75 -> 69); Paget untouched though `_process_tactical_states` accrues his counters (1 after one tick). Guard at world_state.py 12125 (row exact); `check_redemption_threshold` refuses non-player at disobedience.py:1604 (row exact). 1805 cavalry = Murat (roster) + Paget (pool) only; legacy fixture = Ney (player) + Uxbridge (Britain).
(f) Deleting the guard: pinned files pass (244: redemption_v2b, artillery, notifications, wo41, sessions — no Uxbridge event fired); BASELINE_SERIES diverges at index 13 — Paget sat at/over the limit 24 marshal-turns in 40 and the arm forced him 7 stance + 5 unfortify times; conscious re-record. Fog: dispatch.py ~2812 drops turn events whose `nation != player_nation`, so Paget's "horses are restless" lines do not reach the player.
(c) Correction: `recruitment.commission_marshal(w, nation, name)` is not the signature (my call errored) — the test should commission through the executor's `recruit_marshal` action.
(g) Fix as row.

=== FA-N80 (P3) stagnation breaker has no crossing gate ===
(a) Reproduced: Castanos @Normandy vs Moore @London (Spain–Britain WAR), `crossing_allowed` False / verdict "shut"; boxed via `_marshal_visited_locations` -> stagnation 3 AND 5 -> `attack Moore`; P4 -> None; executor refuses ("barred — the Royal Navy commands the water with 100 sail ... against our 45"); `_record_failed_action` writes `{'Castanos': {'attack': 2}}` (737-750, called at 1109 on failure). Unboxed, the move arm wins (move Artois) — the move arms are already gated through `_can_ai_move_to` (4676+, `crossing_allowed` 4694).
(b) 3746-3885; surrounded arm 3840-3856 (loop 3844); range arm 3857-3883 (ratio 3874). Crossing hits now 1956, 2595, 2751 (is_sea_link), 2783, 3438, 3542, 4694, 4734 (row's list ±2).
(f) Pinned files pass (213; the gate is fleets-gated, legacy fixtures have no fleets). BASELINE_SERIES byte-identical structurally — 0 candidates skipped in 40 turns.
(g) Fix as row; the row's test recipe (pre-fill visited set) works as written.

=== FA-N59 (P2) garrison assault never writes the exhaustion counter ===
(a) Reproduced: `_resolve_garrison_combat` reads `get_attack_modifier()` at 2924, applies losses at 2962, both pipeline contexts pass `skip_exhaustion: True` (3009 collapse branch, 3131 hold branch) and 2852-2853 is therefore skipped; three executor assaults on Vienna all fought at modifier 1.15 (schedule 0/10/20/30% never applied), counter 0 on both branches; order dependence: field-then-garrison fights the second at 1.035, garrison-then-field at 1.15; `in_combat_this_turn`/`moved_this_turn` False after an assault (jealousy `_engaged` 699-707 reads all three).
(b) Resolver 2857; `attacker_effective <= 0` return 2930; losses 2962; `if garrison_collapsed:` 2981. Other `skip_exhaustion` sites: 3885 (bombardment, caller increments at 3854) and 6884 (`_execute_attack`'s pipeline, increments at 7331 unless counter-punch); world_state 12570 (reckless auto-charge) increments. Second resolver caller: naval_executor.py:374.
(e) GR5: the AI's P4.25 assaults are equally uncounted, and the AI re-selects the same marshal within a phase (FA-8 cycle 3: two Munich orders in one phase).
(f) The one-statement fix breaks nothing: 575 pinned tests pass (garrison_system, garrison_command, v3_session4, r1_pipeline, mc1c iron resolve, ca8 close-out incl. `test_the_resolver_never_calls_resolve_battle`, jealousy_v32, wo_slice3 floor, ai_garrison; the only red in my first run was my exec-patch defeating `inspect.getsource`). BASELINE_SERIES byte-identical structurally: 2 garrison resolutions in 40 turns, never twice in a phase.
(g) Fix as row (after 2962, before 2981); consider also `in_combat_this_turn = True` as the bombardment path does at 3855.

=== Cross-cutting ===
- Byte-identity honesty: n6/n80 identical because their guards never bound; n59 bound twice with no repeat; fa8/fa27/n7/n54 all move BASELINE_SERIES (indices 5/4/14/13) — build them together and re-record ONCE with per-arm attribution (arm_patches.py has the counters).
- The FA-27 slice must reckon with FA-8: on the row's own control shape the square-breaker is a suicidal garrison assault on an occupied province.
