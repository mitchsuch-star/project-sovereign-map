# Slice 9 review — lens R1 "attack the fix" (at master `57b08c39`, September 5, 2026)

**Method.** Code read from the frozen snapshot; probes run with the venv Python, cwd = repo, `sys.path[0]` = repo (the working tree at 57b08c39, identical to the snapshot). Every probe boots `WorldState.from_scenario(europe_1805.json)` onto `backend.main`'s three module seams (`M.world`, `M.game_state["world"]`, `M.parser = CommandParser(use_real_llm=False)`), drives the real routes with `TestClient(M.app, raise_server_exceptions=True)`, and compares against the pre-slice shape by flipping `REDEMPTION_AT_EVERY_TRUST_WRITE` / `REDEMPTION_NET_ACTIVE` in-process. Probe files + raw outputs: session scratchpad `reviewS9/R1/p*.py`, `out_p*.txt`. No git mutation, no repo edits, no mutation sweep, no full suite.

**⚠ Hygiene incident (the lens's own).** The first probe (`p1_captured.py`) ran `end turn` without `INK_IRON_SAVE_DIR`, so the end-turn autosave overwrote the repo's gitignored `saves/autosave.json`. No tracked file changed and no backend was listening on 8005 (no live session), but the menu's Continue arm would load a probe world until the user's next real end turn. Every later probe wrote to `scratchpad/reviewS9/R1/saves`.

## 1. Findings

| id | sev | file:symbol | probe → measured result | fix shape |
|---|---|---|---|---|
| **R1-1** | **P2 introduced** | `disobedience.DisobedienceSystem.check_redemption_threshold` (no liveness guard) + `world_state._check_trust_warnings` (the NET) | `world.capture_marshal(Lannes, "Austria")` → Lannes at Vienna, strength 0; `trust.set(15)`; `end turn` → response `state: awaiting_redemption_choice`, `redemption_event {Lannes, 15}`, `redemption_rows [Lannes]`, `redemption_pending True`, `world.pending_redemption = Lannes`. The erosion tick skips captives (`captured_by` → `continue`); the NET does not, and the checker guards trust/latch/autonomous/administrative/nation/cooldown but **not** `captured_by`/`strength` — the two predicates disagree. `GET /pending_redemption` → `has_pending False` (stale-cleared) while the latch stays `True`. **Every answer arm misbehaves on the prisoner:** `grant_autonomy` → 200, next turn's `independent_command_report`: *"Lannes shifts to DEFENSIVE stance at Vienna"*, trust 15→17 while captive; `administrative_role` → *"Their 0 troops await future assignment. You now have 5 actions per turn"* (+1 action bought from a prisoner), then `release_captured_marshal` → `administrative=True` at Paris with strength 5,000; `dismiss` → *"0 troops transferred to Bernadotte … (+10 Authority)"*, `destroy_marshal` refuses a captive so he stays in `world.marshals` (single ask, not repeatable — 1 ask in 14 turns on this lens's run). **Residue:** after the stale-clear the latch stands; released at 15 he is never asked again (3 turns measured); nothing in capture/release touches `redemption_pending` (grep: zero hits in world_state.py). He does *not* block the next man (Murat asked the following turn). | Put the liveness predicate (`strength > 0 and not captured_by`) INTO `check_redemption_threshold` so every seam inherits it (one source shared with `standing_redemption`); clear the named marshal's latch when `standing_redemption` discards a stale question (or on capture/release); make `/respond_to_redemption` read through `standing_redemption` instead of `world.pending_redemption` directly. |
| **R1-2** | **P1 pre-existing, widened** | `world_state.advance_turn` attrition sweep (`eliminated = [m … if m.strength <= 0 and not captured_by]`) vs `disobedience.handle_redemption_response` `administrative_role` arm | Lannes eroded to 20 (tick), asked, answered `administrative_role` → *"Their 18,000 troops await future assignment. You now have 5 actions per turn."*, `strength 0`, `location None`, `bonus_actions 1`. **Next `end turn` → `marshal_destroyed`: "Lannes has been eliminated by supply attrition at None"**, tombstone `{'cause': 'attrition', 'location': None}`, `"Lannes" in world.marshals → False`, `bonus_actions 1` / max actions 5 **persist**. The only restore is the debug cheat `admin <marshal>` (`meta_executor`). Reproduced with BOTH levers OFF via `/debug/trigger_redemption/Lannes` → pre-existing; but the landing record itself says the question was "structurally unreachable outside the cavalry-limit and strategic seams" before this slice — the slice makes the arm an ordinary path. No slice test answers `administrative_role` and then ends a turn. | Exempt `administrative` men from the attrition sweep (and any other `strength <= 0` reaper — 15 sites grep'd, several read strength only); either build the restore verb the message promises or retire the arm; pin *answer → end turn → marshal still exists, troops still frozen*. |
| **R1-3** | **P3 introduced** | `combat_executor._execute_attack` reinforcer stage (`stage_redemption(world, failing, result=result)`) vs `strategic._carry_combat_fields` / `_COMBAT_PASSTHROUGH_FIELDS` | `Ney, pursue Mack` (engages at issuance) with patched no-show Murat at 22 → Murat 19, **latched**, `world.pending_redemption = Murat`, but the `/command` response carries **no `redemption_event` anywhere** (recursive search `[]`), `state None`; the following `end turn` carries none either (NET refuses a latched man); `GET /pending_redemption → True`. The allowlist carries `reinforcement_*`, `battle_report`, `capture_*`, `battle_diorama`, `events`, `diplomatic_dialogue`, `awaiting_diplomatic_response`, `war_purpose_popup` — **neither `redemption_event` nor `state`**. Same nested-carrier shape for the autonomous report (`independent_command_report[i].result`), the strategic tick, the glory attack and the AI battle (R1-4). Client consequence: the once-per-turn poll already ran at the turn's first control return, so the question is invisible until the NEXT turn's control return; meanwhile the man takes orders (R1-7) and no other marshal can be asked (one-live). The record's "staged on the battle result so the question rides the same response as the battle" holds only for the direct `attack` path. | Add `redemption_event` + `state` to `_COMBAT_PASSTHROUGH_FIELDS` (the shape `_attach_redemption_if_needed` already uses), or stage onto the outer reply; pin the PURSUE first-step case on the real `/command` route. |
| **R1-4** | **P4 introduced** | `combat_executor._execute_attack` + `main._build_visible_enemy_phase` | Patched AI Mack→Soult with French no-show Lannes at 22: raw AI result `{'state': 'awaiting_redemption_choice', 'success': True}`, `redemption_event.marshal = Lannes`; end-turn response top level has no redemption; **both keys ride inside `enemy_phase.nations.Austria.actions[0]`** (the cleaner strips only `new_state`); `GET /pending_redemption → True`. No reader (`enemy_phase_dialog.gd` reads `action`/`events`; the fog filter reads `events`/`ai_action`/`naval_diorama`) → no misroute, a payload wart of the player's own marshal's question; delivered by the poll at the end-turn tail (correct in the common case). | Pass `result=None` when the battle's actor is not the player, or strip `state`/`redemption_event` in `_build_visible_enemy_phase`. |
| **R1-5** | **P3 widened (client, by read)** | `main.gd _on_enemy_phase_dismissed` / `_on_strategic_report_dismissed` / `_process_next_interrupt` redemption arms → `_show_pending_dispatch()` → `_show_pending_capture_choice()` | **Backend measured:** autonomous Lannes (patched `decide_single_action`) takes undefended Bohemia while Murat crosses via the NET → ONE end-turn response with `redemption_event {Murat 15}` + `pending_capture_choice True` / `capture_data Bohemia` + `enemy_phase` (and `state None` on this early-return route). **Client read:** the capture route stashes `pending_capture_response` under `enemy_phase`; the redemption arm calls `_show_pending_dispatch()`, whose last line raises the stashed capture dialog (layer 105, modal) and whose `return` value the arm ignores, then `_show_redemption_dialog()` (layer 107, no modal guard) on top → two stacked modals; answering the redemption → `_return_control_to_player` → `set_input_enabled(true)` with the capture dialog still up. Pre-slice the pairing needed a cavalry-limit redemption; now any eroded marshal + any autonomous/strategic capture. | In the three arms, if `_show_pending_dispatch()` raised the capture, stash the redemption (`pending_redemption_data`) and return; `_return_control_to_player`'s existing `_show_pending_redemption()` raises it after the capture answer. |
| R1-6 | P4 introduced | `disobedience.stage_redemption` | Unit: `result = {"state": "awaiting_player_choice", …}` → after staging `state == "awaiting_redemption_choice"` — the `redemption_event` write is guarded, the `state` write is not. Reachability today low: petition arms forward curated keys (`_apply_command_choice`) or the declare-war result; `_execute_attack`'s only earlier `state` write is a pre-battle `awaiting_clarification` early return; the client's `state`-keyed routes are objection + clarification. | `if "state" not in result: result["state"] = …` (defence in depth, same as the event guard). |
| R1-7 | P4 pre-existing, widened | `executor` (no gate) / `main.gd` typed-token intercept | Murat asked at 15 → `Murat, attack Mack` → success, battle fought (22,000→20,540, moved to Swabia); `Murat, move to Orleanais` → success; `world.pending_redemption` still Murat, GET True. Typed `grant_autonomy`/`grant autonomy` at `/command` parse as the VASSAL verb ("Specify which vassal."); `dismiss`/`administrative_role` refused — the client intercepts those tokens before sending, so this bites only headless drivers / the text fallback. | Design note: the checker never gated orders; R1-3's one-turn invisibility is the realistic window. Either gate orders for a man with a standing question or state in the record that he keeps serving. |
| R1-8 | P4 introduced, cosmetic | `main._include_command_redemption_event` vs `_build_result_response` | Main end-turn path: `state: awaiting_redemption_choice`; capture early-return path (R1-5's pairing): `redemption_event` present, `state: None`. Client reads the key, so harmless today. | Stamp `state` beside the key in `_build_result_response` (or drop the state contract). |
| R1-9 | P4 introduced, cosmetic | `turn_manager.end_turn` order (`advance_turn` before `_process_autonomous_marshals`) | The NET mints Murat's question at 15; the autonomous battle in the same tick docks him as a no-show to 12 (`reinforcement_results` low_score); the dialog quotes **15**. | Re-quote `trust` at delivery (`_include_command_redemption_event`) from the live marshal. |
| R1-10 | P4 pre-existing (PT-B1 gap), client, by read | `main.gd _process_next_interrupt` empty-queue tail | Unlike the other four tails it calls neither `_show_pending_redemption()` nor `_maybe_recover_dropped_redemption()`; a stashed redemption waits for the next command's tail. Not driven — no backend shape found carrying both a muster interrupt and a redemption on one response. | Add the two calls to the tail. |

## 2. Attempted and NOT reproducible

- **Dismiss-on-captive authority farm.** After `dismiss` on a prisoner (R1-1) the man is unlatched with cooldown +5; 14 further end turns produced **1** ask total on this lens's run — not repeatable here. Cause not chased. *(Lens R2 measured three dismissals over 14 turns with authority 50→80 on its own run; the two runs differ in the prisoner's trust path — both agree the ask should never have been mounted.)*
- **Erosion under `administrative_role`** (hypothesis 4): cannot be measured — the marshal is destroyed at the next end turn (R1-2). A direct `_process_dotation_state()` call on the admin object does erode (20→17), so the arm itself is not the guard.
- **The organic redemption+capture pairing on the unpatched AI**: 3 seeded attempts gave stance-change / move / attack-Mack, never a capture on the redemption turn (the capture came on the autonomy's 2nd turn, when the man was exempt). Forced deterministically instead (R1-5).
- **A `/command` response carrying both a muster interrupt and a redemption**: the dock happens after the battle, the muster before it — no shape found.
- **`end turn` with a pre-set `pending_capture_choice`** is refused (`success False`, "Your forces await orders"), levers on or off — so that pairing arises only from an in-tick autonomous/strategic capture.

## 3. Not checked

- Godot behaviour is by code read only (no engine run): R1-5 and R1-10 are reads of `main.gd`; the layer order (105 under 107) is from `dialog_manager.gd`/`.tscn`.
- `deferred_dialogue` (war-purpose HARD STOP) + redemption on one end turn — read only: `_return_control_to_player` raises the redemption before the deferred dialogue; not driven.
- Multi-capture turns, the W6-8 estate stage + redemption, and the strategic tick's own battle carrier (R1-3's class; measured only on the PURSUE first step and the AI battle).
- Whether the R1-2 sweep also eats an administrative marshal on `/load` (from_dict) paths.

## 4. Hypotheses confirmed working correctly

- **Two-question turns are delivered in order.** Redemption + deferred `jealousy_confrontation` petition on one end turn (measured): both keys ride; redemption first (enemy-phase tail), petition at control return; answering `acknowledge` after works. Fontainebleau + redemption on one turn: `redemption_event {Soult 20}` + `deferred_marshal_petition {fontainebleau}` both ride; `refuse` (−8 each) → the response carries Lannes's question at 12 (one-live honoured, Murat waits); `concede` → rentes 120g/turn to all three incl. the newly autonomous Soult, trust 22, latch cleared by `modify_trust`; inverted order (refuse first, then dismiss the asker) → next turn Lannes asked at 9. Neither answer invalidates the other.
- **WO-41 holds.** NET question → `/save` → `/load`: response re-attaches `redemption_event Murat`, field + latch intact, `GET` True, answerable after load. The captive's stale question is cleared on load (field None) and the latch persists — identical to in-memory.
- **The petition endpoint IS a net, and that is fine.** Murat at 0 by direct write; `acknowledge` lowered nobody → the petition response carries Murat's question (routed by `_on_marshal_petition_result → _on_command_result`; `_result_carries_question` holds the drain). Recommend the record say so explicitly.
- **Exactly one row.** Tick + NET for one man: 1 row in `tactical_events` (mirrored once in `events`; the client prints nothing for it — no `message`). **Auto-advance path** (forced: AP 1, admin AP 0, `Davout, defend`): `turn_advanced True`, `state awaiting_redemption_choice`, `redemption_event Lannes`, `enemy_phase` → delivered by the enemy-phase tail.
- **Lever-off arms reproduce the pre-slice shape** (measured on the erosion geometry and the debug-trigger path).
- **Design, not defect:** `grant_autonomy` ends with a flat +15 at neutral tier (16→31 measured), erosion continues (31→25→22) and the cooldown re-asks on re-crossing 20 — exitable by paying (concede → 22, latch cleared), and a mild free trust pump; consistent with ES-7's "paying only stops the bleed, never buys trust" (`ECONOMY_REVISIT_SPEC.md` §0.6 one-liner).
