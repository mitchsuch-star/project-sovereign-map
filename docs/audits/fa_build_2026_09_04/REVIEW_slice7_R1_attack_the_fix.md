# Slice 7 review — lens R1 "attack the fix" (at master `764d0ffc`, September 5, 2026)

Method: read the diff + snapshot, then drove ~230 sentences through the real `/command` (TestClient, mock parser swapped on all three seams, fresh 1805 world per row, levers flipped OFF/ON for pre-slice arms). No repo file touched, no suite run, no network.

## 1. Findings

| id | Sev | file:symbol | Probe → measured | Fix shape | How |
|---|---|---|---|---|---|
| **R1-1** | **P1** (introduced) | `llm_client._parse_with_mock` typo re-entry × `parser.parse` (`effective_text` → `_split_sequential_orders`, `detect_strategic_command`) | The repair rewrites ONE reader's text; the split gate and the strategic layer re-read the **unrepaired** `effective_text`. `Davout, hodl Lorraine` → parse action=hold target=Lorraine **strat=None** → *"Davout shifts to DEFENSIVE stance at Rhineland"* (AP 4→3), note says *"read 'hodl' as 'hold'"*. Control `Davout, hold Lorraine` → standing HOLD, marches to Lorraine (AP 4→2). Lever OFF → honest shrug. `Ney, hodl Lorraine until relieved` → raw `'Ney, hold Lorraine               '`, condition AND destination lost. `Ney, hodl Lorraine, then attack Mack` → **fights now** (gold −122; `_STAND_FAST_FIRST_CLAUSE_RE` doesn't see "hodl", so the then-attack exemption keeps one parse and `attack` wins); control holds Lorraine + sequel note. | Apply the repair to the text every reader sees: a pre-parse rewrite at the top of `parse()`. Keep `raw_command` = typed text. | RUN |
| **R1-2** | **P1** (left one seam over) | `parser._STAND_FAST_FIRST_CLAUSE_RE` vs `llm_client._STAY_PUT_RE` / hold-family additions | `Ney, stay here, then attack Mack` → **battle fought** (gold −132); `stay where you are, then attack Mack` (−116); `rest your men, then attack Mack` (−126). `Ney, stand your ground, then attack Mack` → parse action=attack, strat=HOLD executes, attack tail dropped **without** the one-order note. Controls `stand fast|stay put|remain in Rhineland, then attack Mack` → split + note. Lever OFF: same battles (pre-existing outcome) — the slice taught the WAIT vocabulary to one reader and not to the split gate. FA-7's own headline shape. | Derive the stand-fast gate from the same source as `_STAY_PUT_RE` + the hold family. | RUN |
| **R1-3** | P2 | `llm_client._repair_verb_typo` / `_TYPO_STOPLIST` | The blocklist misses real words: `Davout, the line held` → **defensive stance, 1 AP** ("read 'held' as 'hold'"); `Davout, we depend on you` → defensive stance 1 AP; `Ney, spout off` / `Ney, snout` → **scouts, 1 AP**; `Ney, holt` → stance; `Ney, match Mack` → *"Region 'Mack' not found. Did you mean 'La Mancha'?"*; `movie/mover/mope` → move; `hoed` → hold. **Live-mode**: the false repairs (conf 0.9) now pre-empt the model. `test_a_real_word_is_never_repaired` pins only "hole" (a stoplist member) — vacuous. | Gate positionally (repair only the token in order-verb position), plus extend the stoplist. | RUN |
| R1-4 | P2 | `llm_client._STAY_PUT_RE` (`\bremain\b`, `rest your men`) outranks fortify/stance/square branches | `Davout, remain at Rhineland and fortify` OFF: fortify (2 AP, fortified) → ON: **wait, fortify silently lost**; `rest your men and fortify` same; `Ney, remain neutral` OFF: stance_change → ON: wait; `remain on the defensive` → wait; `remain in square` → wait. | Stay-put must not claim a sentence carrying a later-branch verb; site it after them. | RUN |
| R1-5 | P2 | `llm_client._mentions_plain_retreat` (`\bretire\b`) | `retire Ney` / `retire Ney from service` / `Ney, retire Davout` → **Ney retreats, −45% effectiveness, 3 turns** (OFF: shrug). No dismissal verb exists, so "retire X" is the natural dismissal phrasing. | `retire` is a retreat only when no roster name is its object. | RUN |
| R1-6 | P2 | `strategic_parser._detect_strategic_type` / `_extract_target_text` (SUPPORT "help") | `Ney, help Davout attack Mack` OFF: attack (fought) → ON: **refused** *"Cannot find marshal 'Davout Attack Mack' to support"*. `Ney, join Davout at Lorraine` → *"Cannot find marshal 'Davout At Lorraine'"*. | The marshal must be the WHOLE object. | RUN |
| R1-7 | P2 | `llm_client.SUPPORT_OBJECT_PREFIX_RE` × `strategic_parser._friendly_forms` self-exclusion | Bare `help Davout` / `go help Davout` / `cover Davout` / `screen Davout` / `shield Davout` → Davout bound as **executor**: *"Where shall Davout march, Sire?"*. Answer with a province and the man to be helped marches away. | Add the five verbs to `SUPPORT_OBJECT_PREFIX_RE`. | RUN |
| R1-8 | P2 | `clause_guards.MODAL_LEADS_ARE_QUESTIONS` + first-person arm | `Davout, would you march to Lorraine for me` OFF: MOVE_TO (marched) → ON: **COMMAND REFERENCE**; `Ney, would you attack Mack, I want Swabia` OFF: attack → ON: help; `Ney, will you hold the line for me` → help. The record's defence holds only for sentences with no `me/us/I/my/our`. | For the three new leads read the subject, not the punctuation. | RUN |
| R1-9 | P3 | `strategic_parser.STRATEGIC_KEYWORDS["MOVE_TO"]` order | `push on to` precedes `push on toward(s)` → `Ney, push on towards Paris` → order target **"Wards Paris"**, `push on toward Lorraine` → "Ward Lorraine". | Longest-first, the list's own rule. | RUN |
| R1-10 | P3 | `main.py:3212` warning append gated on `success` | Typo note lost on refusal: `Ney, attak Kutuzov` → *"No intelligence on Kutuzov's position"* (no note); `Ney, match Mack` → a region refusal about a word the player never typed. | Append the note on refusals too (objection popups exempt). | RUN |
| R1-11 | P3 | typo re-entry `original_text = _repaired` → `raw_command` | `Ney, mrach to Paris` → `order.original_command = "move to Paris"`; the PARSE-NEG rule "raw_command is always what the player typed" is broken. | Keep the typed text as the record. | RUN |
| R1-12 | P3 | `question_desk._resolve` roster order | `who holds Brunswick?` / `who is at Brunswick?` → *"We have no word of Brunswick's whereabouts"* (the man, not the province). | who_holds/who_at prefer the region roster. | RUN |
| R1-13 | P3 | `llm_client._DESK_ADDRESS_RE` | `Berthier, end turn` / `Sire, end turn` / `Berthier, status report` → unknown shrug (`Berthier, status` works). | Apply `_desk_text` to `is_bare_end_turn`. | RUN |
| R1-14 | P3 | `_mentions_plain_retreat` " to " heuristic | `Ney, pull back towards Lorraine` / `retire towards Alsace` → tactical RETREAT (−45%) while `pull back to Lorraine` marches clean; pre-existing `fall back towards` shape, widened. | Read `towards/toward` as a destination too. | RUN |
| R1-15 | P3 | HOLD→SUPPORT reclassification ignores possessives | `Davout, protect Ney's flank` → **HOLD on phantom "Ney'S Flank" executes, 2 AP** (pre-existing); `Davout, cover Ney's retreat` → refused with phantom "Ney'S Retreat" (OFF: Davout retreated — worse). | Strip `'s <noun>` before classifying. | RUN |
| R1-16 | P4 | `parser._names_the_admiral` vs `llm_client._unresolved_address_token` | `Villeneuve, order the diversion` conf 0.55 → live LLM fallback still fires; `Nelson, order the diversion` → *"Did you mean Ney?"*. | Teach the confidence demotion the admiral. | RUN |
| R1-17 | P4 | `_NAVAL_OBJECT_RE` | Record says "exactly as its sibling always did" — sibling window is `.{0,20}`, new one `.{0,24}`. | Cosmetic. | READ/RUN |
| R1-18 | P4 | `reviewS7/probe_s7.out.txt` | The committed probe's `where is Kutuzov?` row shows the COMMAND REFERENCE; at `764d0ffc` it answers *"no word of Kutuzov's whereabouts"* — the record's probe output is stale vs the code. | Re-run the probe. | RUN |

## 2. Attempted and NOT reproducible
- `retire/pull back/withdraw/fall back to the rear` → MOVE_TO resolved to Lorraine (friendly rear), not toward the guns; `advance on the enemy` / `forward to the front` → Swabia (intended).
- Honorific: `General Ney, again`, `not you, General Davout`, clarification answers `General Davout` / `Gen. Davout` / `Marshal Davout` all bind; `General Mack, attack Vienna` / `Marshal Mack, retreat` refused by name; `General, attack Mack` / `Marshal, attack Mack` refused by the pre-existing roster-free guard; `a general advance on Swabia` → MOVE_TO Swabia + which-marshal.
- Naval: `Ney, a diversion against the blockade` / `Murat, build ships` → Admiralty refusal; `Villeneuve, blockade Brest|lay down a ship|guard home waters` accepted; `Mack, lay down a ship` refused by WO-1; `order the diversion at Ulm` quotes (by design).
- Meta list: `Ney, invest in Bavaria`, `Davout, cede Tyrol to Bavaria`, `Fouché, economy report`, `Soult|Sire, request terms from Austria`, `Ney, make amends with Prussia` — no new false clarification; diplomatic routes still claim their forms first.
- Question desk: `where is Mack, attack him` → answered, order dropped (identical class to pre-slice `help`); `who holds Swabia? attack it` → fights — the pre-existing `is_question` rule (mid-sentence `?` unread), not the desk.
- `NEVER_STRATEGIC_ACTIONS`: no legitimately strategic member found; `A_BARE_RETREAT`: `fall back north` → direction resolves (Frankfurt, PEACE refusal, pre-existing).
- Negation/deferral × typo: `do not attak Mack` refuses; `never attak, hodl` → hold; bare `attak` → which-marshal; `attak Mack, then hold Rhineland` → split + both notes.
- AI parity: `detect_strategic_command` has ONE caller (`parser.py`) — the AI never reaches `_friendly_forms`; `_admiralty_misaddressed` returns None for non-player actors.

## 3. Claims of the record checked
- "Real words one slip away from a verb — never repaired" — **FALSE** (R1-3, ten words measured).
- "the polite, unpunctuated ORDER stays an order" — TRUE for the two pinned sentences, **FALSE** as a rule (R1-8).
- "stay put / stay here / rest your men are the plain WAIT" — TRUE bare, **FALSE** before a `then attack` tail (R1-2).
- "The note rides the parser's warning seam so the player reads what was assumed" — TRUE on success, **FALSE** on refusal (R1-10).
- "raw_command is always what the player typed" — now **FALSE** for repaired parses (R1-11).
- "the two CAPTURE regexes stay marshal-only by design" — VERIFIED.
- ONE honorific composed into every address regex, zero `.gd`, `will Ney attack Mack?` no longer fights, `withdraw Ney's rente` never marches, `Berthier, status` works, `cover the retreat` stays unknown, `join forces with Saxony` keeps its route — VERIFIED.
- The golden corpus holds **no** row for any affected shape — so "corpus 647/647" says nothing about R1-1..R1-8.

## 4. Could not check
- M1–M7 / `BASELINE_SERIES` byte-identity and the full suite (forbidden; the record's argument that the harness types nothing is plausible but unverified here).
- Real live-LLM behaviour beyond the gate predicate.
- The Godot client surface (no `.gd` in the diff).

## Disposition (by the builder, September 5, 2026)
R1-1, R1-2 (P1), R1-3..R1-12, R1-14..R1-16 FIXED in the review-round commit (pins in `tests/test_fa_slice7_the_mock_speaks_plainly_2026_09_04.py::TestTheReviewRound`); R1-13 recorded NOT fixed — the client's lapse-confirm gate (`main.gd::_is_end_turn_phrasing`) mirrors the backend's bare end-turn vocabulary word for word, so widening only the backend would advance the turn behind the client's gate (`DESIGN_REFINEMENT.md` FA-R4); R1-17/R1-18 corrected in the record.
