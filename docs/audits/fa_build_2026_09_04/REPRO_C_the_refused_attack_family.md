AGENT C REPORT — "a refusal narrated as a battle" family (FA-14/15/19/20/34/68, FA-N10/N26/N40/N42/N48/N60)
Repo master 915c6a32, read-only. Scripts + outputs in this dir: repro1_fa14_15_19_20_34.py (+ .out), repro1b_fa15_cautious_twin.py, repro2_hold_sally_fa_n40_n26.py, repro3_fa68_two_interrupts.py, repro4_fa_n10_n42_n48_n60_testclient.py (TestClient; M.parser.llm.use_real_api is False asserted), probe5_pursue_refusal.py, probe6_fa20_ap_ledger.py, probe_check_interrupts.py. All 1805 boot, Ney(aggressive)/Davout(cautious) at Normandy, Moore at London, world.fleets as booted (gate: allowed=False verdict=shut ratio=0.54 coverer=Britain).

NOTE ON THE VERIFICATION TEXT: every copy of the Sept-2 corrected_reading (JSON, FINAL_AUDIT_VERIFICATION table, BUG_FIXES rows) is truncated at ~300 chars; the full narrowing does not exist in the repo. I re-derived it by reproduction below.

CURRENT LINE NUMBERS (by symbol; the rows' numbers are stale by up to +75)
strategic.py: _strategic_command_flavor 28 · _combat_carry 98 (returns exactly {'battle_report','battle_details'} at 107-108) · _COMBAT_PASSTHROUGH_FIELDS 146-172 (8 keys) · _carry_combat_fields 175 · process_strategic_orders 211 (pass-2 loop 310-316, `break` 316) · handle_response 324 (dispatch 461-471: contact/contact_bad_odds/destination_blocked → _respond_blocked_path; combat_stalemate/repeated_combat → _respond_combat_stalemate) · _respond_blocked_path 610 (attack arm 617-665) · _respond_combat_stalemate 812 (continue_order 817-831) · _execute_strategic_turn 871 (0a 877-887; interrupt latch 924-925, 941-942) · _execute_move_to 956 (enemy-ahead check 1014-1041; blocked_naval break 1120-1125) · _execute_pursue 1258 (same-region attack 1358-1370; move-failed-at-target attack 1499-1520) · _execute_hold 1616 (sally 1805-1895: execute 1831-1840, unconditional msg 1874-1879) · _execute_hold_bombardment 1911 (the correct refusal arm 2000-2008) · _check_interrupts 2247 · _complete_order 2505 · _break_order 2563 · _handle_blocked_path 2583 (aggressive DESTINATION auto-attack 2697-2720; mid-path 2763-2775; cautious 2807-2858) · _handle_combat_result 2860 (no-battle arm 2968-2976 — carries a comment naming FA-14 as UNCHANGED; stalemate arm 3015-3039; raw-enum message 3036) · _should_auto_attack 3041.
strategic_executor.py: _execute_strategic_command 458 (order assigned 1220; MOVE_TO first-step loop 1266-1318; blocked call 1279; PURSUE co-located "They're right here! Engaging!" 1261; PURSUE move-failed "spotted at … Engaging!" 1431; return dict 1656-1665) · _inferred_first_step_gate 1992 · _handle_first_step_blocked 2052 (literal 2069-2133 clears the order; aggressive 2135-2209 — attack 2155, success wrapper 2166-2174, `return result` 2175; cautious 2211-2232).
movement_executor.py auto-upgrade 442-479 (calls _handle_first_step_blocked at 467). combat_executor.py: _execute_attack 4112, crossing gate 4839-4854 returns {"success": False, "message", "blocked_naval", "naval_ratio"}; _attach_staged_war_purpose 8154. naval.py: crossing_check 1060 (shut 1126-1138, landing 1103-1123), crossing_allowed 1173, crossing_check_reach 1182. display_names.get_strategic_display 248 → "March"/"Pursue"/"Hold"/"Support". main.py: _build_result_response 462; _COMMAND_RESULT_SIMPLE_FIELDS 554-577; /command pending_interrupt early-return 3221-3223; typed interrupt route 2402-2411; /strategic_response 3893-3924. turn_manager.py 267-271 (the only production caller of the processor). Client: interrupt_popup.gd OPTION_LABELS 23-37 ("continue_order": "Continue as Ordered" :30), message default :52; main.gd _response_has_interrupt_route 2412-2428, _route_interrupt_response 2430-2431 (passes ONLY pending_interrupt), end-turn stash 2614-2618, _show_strategic_reports 4883-4917 (reads message/battle_message/outcome), _on_strategic_report_dismissed 4931-4940 (queues every requires_input REPORT), /load erases message 4669-4671; strategic_report_popup.gd _format_report 58-119 (reads 12 keys, none of them battle_report/battle_details).

════ FA-14 (VERIFIED) + FA-19 (DUPLICATE of FA-14/15) ════
Reproduced (repro1 §A): Ney MOVE_TO London, path ['London'] → report {action: combat, outcome: stalemate, requires_input: True, interrupt_type: combat_stalemate, message: "Ney attacked Moore during march but the battle was inconclusive. Continue move to?"}; battle_details.success=False, blocked_naval=Britain, message "The crossing from Normandy to London is barred — the Royal Navy commands the water with 100 sail (100 effective) against our 54 …"; battles_this_turn delta 0, event_log delta 0, Ney 24000→24000, Moore 30000→30000; order.combat_attempts 1, last_combat_result 'stalemate'; marshal.in_combat_this_turn True, marshal.last_combat_result 'stalemate'; pending_interrupt armed.
Negative control (repro1 §F, fleets cleared): a REAL battle (delta 1, Ney 19752 / Moore 26367) produces a row with the IDENTICAL top-level shape (same keys, same message) — the refusal row and a real stalemate row differ only inside battle_details.success, which no .gd file reads.
Corrections: (1) the live arm for London is the DESTINATION arm 2697-2720 (`favorable = strength ratio >= 0.7` → 0.8), not the mid-path arm the rows cite (:2763). (2) Stale lines: stalemate arm 3015-3039, message 3036, _should_auto_attack 3041-3057. (3) FA-14's fix_shape `branch on not result.get("success")` is WRONG AS WRITTEN — see "what breaks". (4) "humanize via get_strategic_display" gives "Continue March?"; `_strategic_command_flavor` gives "Continue his march?" (FA-34's prescription; better). (5) FA-19's in_combat/last_combat_result claim is real but small: `_handle_combat_result` overwrites marshal.last_combat_result with 'stalemate' after a refusal in which `_execute_attack`'s pipeline never ran, so a marshal's prior 'victory' record is clobbered (the FA-N3 test_the_marshals_own_combat_record_is_not_corrupted pins the fought case only). (6) The naval reason is doubly invisible on the end-turn path: battle_details is unrendered AND the row lacks battle_message (FA-N10).
Callers of _handle_combat_result: exactly two — strategic.py 2719 (destination arm) and 2774 (mid-path arm). Both feed `_strategic_execution` attacks.

════ FA-15 (NARROWED) ════
Reproduced both halves. Aggressive per-turn half = FA-14. Cautious first-step twin (repro1b, objection answered with command["objection_response"]="proceed"): typed "Davout, march to London" → success True, requires_input True, variable_action_cost 1, pending_interrupt {interrupt_type: contact, options: [attack, go_around, hold_position, cancel_order]}, message "Davout: 'Enemy at London. How shall I proceed, Sire?'" — asked BEFORE the crossing is consulted. Answer attack → _respond_blocked_path else-arm: "Davout attacks Moore. The crossing from Normandy to London is barred — … Assault failed — orders cancelled, marshal awaits new instructions." order_cleared True, combat_attempts +1, 0 battles, strengths unchanged. Direct seam call `ex._strategic._handle_first_step_blocked(d, [moore], 'London', w, gs)` mounts the same question.
Side finding (unfiled): the first-step cautious arm offers go_around even when blocked_region IS the destination (2220), unlike the per-turn twin (2835 omits it); answering go_around "reroutes around London" and immediately re-asks "Enemy forces discovered at London! Destination held" (repro1b).
Correction to fix_shape (the verifier's "central claim is false"): `_handle_blocked_path` is NOT the one seam both routes funnel into. The first-step twin is `strategic_executor._handle_first_step_blocked` (a separate copy; `resolve_order_destination` is shared only for the destination read), and the answered 'attack' lands in `_respond_blocked_path`. A gate in `_handle_blocked_path` alone fixes the end-turn arms and leaves the typed contact question, the FA-20 phantom order, and the "attacks Moore … Assault failed" answer untouched. Three seams, not one.

════ FA-34 (NARROWED) ════
Reproduced (repro1 §B): continue_order → order None, order_cleared True, trust 75→75, "Ney cannot break through. Orders cancelled — awaiting new instructions."; hold_position → order None, trust 75→72; cancel_order → order None, trust 75→72. Client label for continue_order = "Continue as Ordered" (interrupt_popup.gd:30). Message tail raw at 3036.
Why the row's one-seam fix (keep the order on continue_order) does NOT fix the player-visible bug on the naval case: next turn the standing order re-enters `_handle_blocked_path`'s destination arm with combat_attempts=1 → `_should_auto_attack` False → the "Bad odds at destination" ask fires: destination_blocked "Moore holds London — destination blocked. Odds unfavorable." (options attack_anyway/hold/cancel) → attack_anyway → `_respond_blocked_path` → the refusal again, now worded "Ney attacks Moore. <barred> Assault failed — orders cancelled". The player is walked from one false battle to another; only the upstream refusal gate ends it. The keep is still RIGHT for a real inconclusive battle (today "Continue as Ordered" cancels a real march too), and it is pin-free (grep "cannot break through" in tests → 0 hits).
Understated scope: two of the three buttons charge −3 trust for a battle that never happened; and on the end-turn path the popup DOES show the report's message (main.gd 4931-4939 queues the report dict), so the lie is on screen verbatim.

════ FA-20 (NARROWED) ════
Reproduced (repro1 §D + probe6): typed "Ney, march to London" (mock parser) → result success False, keys [action_info, action_summary, blocked_naval, message, naval_ratio, success], the honest naval text, variable_action_cost None, order_cleared None; ney.strategic_order = ('MOVE_TO','London',['London'], issued_turn 1) STANDING; actions_remaining 4→4 (0 AP, measured); build_strategic_ledger(w)['orders'] lists {'marshal':'Ney','order_type':'March','target':'London','path_remaining':1,'has_order':True} — the row's "shows in the Orders ledger" claim is TRUE. Next-turn processor on the phantom → combat_stalemate popup (FA-14). Mechanism confirmed by reading: order assigned at 1220 before the first step; aggressive favorable arm `return result` at 2175 without clearing; the literal arm (2087-2099) clears and returns order_cleared True.
Same defect, two more sites (unfiled): typed "Ney, pursue Moore" from Normandy (probe5 §B) → success True, "Ney pursues Moore (at London). Moore spotted at London! Engaging!\n\n<barred refusal> Ney: 'Run him to ground, then. My sabers are hungry.'" — a PURSUE left standing over a refusal (1426-1431, no success read); the co-located PURSUE arm 1256-1261 ("They're right here! Engaging!") is the same shape.
The movement_executor auto-upgrade (467) returns the same blocked_result and inherits any fix at 2175.

════ FA-N40 / FA-N26 (HOLD sally) ════
Reproduced (repro2): Ney 40k HOLD Normandy, Moore 20k at London → every pass: action 'sally', outcome '', order_status 'continues', message "Ney sallies forth to attack Moore, then returns to Normandy!", battle_message = the naval refusal, battle_details.success False / blocked_naval Britain, battles delta 0, strengths unchanged, combat_attempts 0, `_should_auto_attack(next)` True — forever, because the sally records no last_combat_enemy (only _handle_combat_result/_respond_blocked_path do). The client renders battle_message (main.gd 4908-4910), so the player reads "sallies forth to attack Moore" with "the crossing … is barred" printed under it, each turn. The return-march at 1842-1852 is a no-op (Ney never left). Negative control (fleets cleared): a real sally, delta 1, Ney 37200 / Moore 14788. `_execute_hold_bombardment` 2000-2008 is the correct sibling (`if not cleaned.get("success"): … "maintains readiness at …"`).
Side finding: a real sally's `outcome` is the raw word 'attacker_tactical_victory' (pinned by test_wo_slice17 ×4 at 649/737/950), which the client colours as neither victory nor defeat (main.gd 4913, strategic_report_popup.gd 108) — the FA-N3 vocabulary gap at the sally producer.

════ FA-N10 ════
Reproduced (repro4 §N10): a real standing-order victory row has keys [action, battle_details, battle_report, command, marshal, message, order_status, outcome, target]; message "Ney defeats Mack! Continuing."; NO battle_message; battle_details.message holds the casualty text; battle_report populated. `_combat_carry` returns exactly ['battle_details','battle_report']. The two renderers read only message/battle_message/outcome. Note the ordering hazard: N10 alone, applied today, would make FA-14's refusal row print "inconclusive" with the naval refusal underneath (the sally's current shape) — land the refusal gate first.

════ FA-N42 ════
Reproduced live: "Ney, march to Nassau" (Ney 40k Rhineland, Mack 3k Nassau) → success True, message "Ney: 'Mack bars the way!' Engaging!\n\n[Combat] …", events [] (empty), battle_report False, battle_diorama False, capture keys absent; control "Ney, attack Mack" → battle_report True, battle_diorama True, events[0].type battle. Cause: the fresh 5-key dict at 2166-2174; also `first_step_combat` never reaches the wire (not in _COMMAND_RESULT_SIMPLE_FIELDS). The capture consequence is structural: main.py routes to _build_result_response only when result carries pending_capture_choice (3228); the wrapper drops it, so a conquering first-step attack leaves world.pending_capture_choice set with no modal and the next command refused by the executor's capture block.

════ FA-N48 ════
Reproduced end-to-end: Davout contact → /strategic_response attack on Mack at Hesse-held Nassau (France at PEACE with Hesse) → response carries battle_report/battle_diorama/events but diplomatic_dialogue False, awaiting_diplomatic_response None, war_purpose_popup False, while world.pending_diplomatic_dialogue.type == 'war_purpose_selection'; next "end turn" → success False "Our purpose in this war awaits your answer, Sire — nothing was relayed. Answer with one of: 1=Conquest …". The typed router still accepts "1", but the client never renders the question. Allowlist today: ('reinforcement_messages','reinforcement_results','coordination_tutorial','battle_report','bombardment_advisory','pending_capture_choice','capture_data','battle_diorama') — no triad, no events.

════ FA-N60 ════
Reproduced: "Ney, march to Munich" → top-level message "Ney: 'Mack blocks the path at Swabia. Odds unfavorable. Your orders?'", pending_interrupt keys [enemy, interrupt_type, is_first_step, location, marshal, options] — no message. First-step-only: the end-turn path queues the REPORT dict (which has message); the sync path routes response.pending_interrupt alone (2431) and the popup defaults to "Awaiting your orders, Sire." (:52). The five omitting producers: strategic_executor 2023-2034, 2180-2189, 2212-2221; strategic.py _inferred_attack_gate and _handle_combat_result 3017-3026. Stamping "message" into the stored dict would also make the /load restore (4669-4671 erases only the top-level message) show the real line.

════ FA-68 (VERIFIED) ════
Reproduced (repro3): Bernadotte (Franconia) + Davout (Rhineland), both cautious, MOVE_TO Nassau issued last turn, battle at Swabia → ONE report ("Bernadotte: 'Cannon fire at Swabia, Sire. Investigate?'"), Davout unmoved (Rhineland→Rhineland), pending_interrupt False, path untouched, no row, no popup. `_check_interrupts` is pure and idempotent (probe_check_interrupts: two calls, identical dicts), so re-calling `_execute_strategic_turn` per deferred marshal is safe; its step 2 (924-925) latches each marshal's own pending_interrupt; the client queues every requires_input report (4931-4940). Caveat for the fix: main.py promotes only ONE marshal's pending_interrupt onto the response (1316-1321), so headless drivers see one at a time, and the typed router (2334-2411) answers the FIRST pending marshal for an unaddressed "investigate" — FA-N5's identity stamp covers addressed answers only. No test pins the break (grep alphabetical/starvation/deferred → none).

════ CALLER CENSUS + ENEMY-AI MIRROR ════
Every attack a strategic order executes under _strategic_execution — 11 sites: strategic.py 619 (_respond_blocked_path), 1358 & 1485 & 1510 (PURSUE), 1831 (sally), 1986 (bombardment — the ONE site that reads success), 2710 & 2765 (_handle_blocked_path → _handle_combat_result); strategic_executor.py 1256 (co-located PURSUE), 1426 (PURSUE move-failed), 2155 (first-step blocked). Every one except 1986 narrates the result without reading success. Additional site found: PURSUE 1370/1495/1520 pass the refusal into `_complete_order` → probe5 §A: order_status 'completed', message = the naval refusal + "Ney: 'Done — and I trust the next order has more fire in it.'", 0 battles, order gone — a pursuit "completed" without a shot.
_carry_combat_fields callers: strategic.py 641/658, meta_executor.py 1842/1917/2026, main.py 3441/3865. _handle_blocked_path callers: strategic.py 975, 1017, 1378, 1413, 1663, 1686, 2130, 2157. _handle_first_step_blocked callers: strategic_executor 1279/1340/1385/1457/1920 + movement_executor 467.
AI mirror: NONE. process_strategic_orders filters `m.nation == world.player_nation` (230-234); turn_manager 267-271 is its only production caller; enemy_ai.py's only strategic_order read is P1.2 road-home (1753-1762), which issues plain `move`; the AI never runs StrategicOrderProcessor or carries _strategic_execution. The only shared seam is _execute_attack's crossing gate (4839-4854), already symmetric. GR5 is not at stake; every fix here is player-only by construction.

════ WHAT THE ROWS' FIX SHAPES WOULD BREAK ════
• FA-14 `not result.get("success")` at the head of _handle_combat_result: tests/test_fa_n_p1_cluster_2026_09_02.py TestFAN3TheGloriousChargeIsNotADraw, test_an_artillery_annihilation_is_not_a_draw, test_a_victor_with_no_outcome_word_is_still_read, test_the_battle_is_found_when_it_is_not_the_first_event and the outcome-word parametrisation (475-486) call the function via StrategicOrderProcessor.__new__ with dicts that have NO success key ({"events":[{"type":"battle","victor":"Ney"}]}) — the guard would route them into the refusal arm: 6 green pins go red. Use `result.get("success") is False and no battle event` (FA-19's wording). Those fixtures also lack executor/holding fields, so the refusal arm must not run for them.
• FA-N10 adding battle_message inside _combat_carry: tests/test_wo_slice12_copy_sweep.py::TestWO33TheReportRidesTheRow::test_the_carry_strips_the_world pins `_combat_carry(None) == {"battle_report": None, "battle_details": None}` exactly — add the key to the non-None arm only (or flip the pin consciously). `carry["battle_details"] == {...}` stays green.
• FA-N48/N42 allowlist growth: test_creative_audit_ca8 1176-1206, test_playtest_bugfixes_2026_07_06 59-77, test_health_check_audit 388-409 all stay green (they assert absent keys are not added / source contains the helper).
• FA-N40 refusal arm on the sally: all sally pins are REAL sorties (test_wo_slice17 648/703/736/949 assert outcome 'attacker_tactical_victory'; test_strategic_ui_comprehensive 1420; test_wo_slice12 463; test_napoleon_promise_audit 472; test_strategic_executor 469 accepts sally|hold_active) — green; test_hold_aggressive_no_sally_bad_odds expects hold_active for a non-sally, consistent with the proposed refusal row.
• FA-20 clear-on-refusal at 2175: test_strategic_executor::TestFirstStepBlocked::test_first_step_aggressive_auto_attacks_good_odds asserts success True + "Engaging"/"bars the way" (a fought case) — green; test_audit_2026_07_09_fixes::TestOrderBreakClearsHoldState (literal arm) and cr5 TestFirstStepLethalGate (requires_input pins) untouched; test_tutorial_school_fixes::test_refused_move_still_clears_the_interrupt (success False → order None, pending None) is the same discipline.
• FA-34 keep-on-continue: no pin asserts the cancel; test_systems_v3_session5 224-244 and test_redemption_v2b 346-401 pin the cancel_order arm (−3 → redemption), unchanged; sweep5 test_continue_order_still_preferred_when_offered is routing only; tutorial ORDER_BOUND_INTERRUPT_TYPES must keep combat_stalemate.
• FA-68 removing the break: nothing pins it.
• Structural hazard for any refusal row that bypasses _combat_carry: test_wo_slice12 test_every_outcome_carries_the_report feeds success True — unaffected.

════ ONE COHERENT FIX: "a refusal is never narrated as a battle" ════
Single predicate in strategic.py, next to _combat_carry: `attack_was_refused(result)` = isinstance dict AND result.get("success") is False AND no events entry of type battle/glorious_charge AND no battle_result. Single row builder `refusal_row(marshal, order, result)` → {marshal, command, action: "attack_refused", order_status, message: f"{name} cannot attack {enemy} — {result['message']}", blocked_naval/blocked_diplomatic/naval_ratio carried} — never outcome, battle_message, battle_details, battle_report, requires_input; never touches combat_attempts / last_combat_* / in_combat_this_turn / pending_interrupt. Apply at every seam of the census:
 A. _handle_combat_result head (after `carry`, before the victor scan): if refused → `_break_order(marshal, world, f"{name}'s march halts — {msg}")` merged with the refusal keys (the PF-8/DEF-5 idiom at 1085-1125; a naval refusal can never succeed, the pathfinder is edge-blind). Also `_strategic_command_flavor` for the real-stalemate message (FA-34's copy half). Closes FA-14/19.
 B. Pre-gate at BOTH contact producers — _handle_blocked_path (before the personality branch) and _handle_first_step_blocked (head) — with the executor's own predicate `naval.crossing_check_reach(world, marshal.nation, marshal.location, blocked_region, int(marshal.strength))`: per-turn → the existing water's-edge break (1120-1125 wording, unreachable today whenever an enemy holds the far shore); first-step → clear the order like the literal arm (2087-2099) and return {success False, message, blocked_naval, naval_ratio, order_cleared True, first_step_blocked True} (0 AP, as a refusal already is). Never mount contact/contact_bad_odds/destination_blocked for a shore the gate refuses. Closes FA-15's dead 'attack' and FA-20's naval phantom at the source, using the same predicate _execute_attack uses (4839-4854) so the two cannot disagree. Sally candidate scan (1811-1815) gets the same filter so a barred shore is never a sally target (ends the every-turn re-attempt without touching _should_auto_attack).
 C. _handle_first_step_blocked aggressive arm 2166-2175: refused → clear the order (literal mirror) + return refusal with order_cleared True (FA-20 for non-naval refusals); success → `_carry_combat_fields({...5 keys...}, result)` after adding "events", "diplomatic_dialogue", "awaiting_diplomatic_response", "war_purpose_popup" to _COMBAT_PASSTHROUGH_FIELDS (FA-N42 + FA-N48; the copy-only-non-None rule keeps every allowlist pin green; _build_result_response pops events to the top level so it must ride there). Same treatment for the PURSUE first-step arms 1256-1261 and 1426-1431: no "Engaging!" over a refusal, say "cannot engage — <reason>", leave the order standing only if the reason is transient.
 D. _respond_blocked_path attack arm 649-665: refused → "cannot attack — <reason>", order_cleared True, no combat_attempts increment, no "attacks X … Assault failed" (FA-15/19 sibling; reachable for non-naval refusals even after B).
 E. _execute_hold sally, right after 1840: `if attack_was_refused(combat_result): return hold_active row "holds {location} — {reason}"` mirroring 2000-2008, skip the return-march and the outcome scrape (FA-N40/N26). Optionally normalise the sally's outcome word through the FA-N3 attacker/defender mapping (flips 3 WO-17 pins consciously).
 F. PURSUE 1370/1495/1520: refused → `_break_order` with the reason, never `_complete_order` (no "Done — and I trust the next order has more fire in it" over a refusal).
 G. _respond_combat_stalemate: continue_order keeps the order (only hold/cancel null it), message "presses on"; with A in place the popup fires only on a real inconclusive battle, so its buttons mean what they say (FA-34).
 H. _combat_carry non-None arm adds "battle_message": cleaned.get("message","") (FA-N10) — after A, so a refusal never rides it.
 I. process_strategic_orders pass 2: drop the break; each deferred marshal runs _execute_strategic_turn, which latches his own pending_interrupt; 0a re-reports the rest next turn (FA-68).
 J. main.gd _route_interrupt_response: fall back to response.message when pending_interrupt lacks one, AND stamp "message" into the five stored interrupt dicts (FA-N60; the stamp also fixes the /load restore).
Landing order: A+B+C+D+E+F in one slice (the predicate), then G, then H/N42/N48, then I, then J. Pin the family structurally: a census test over strategic.py + strategic_executor.py for every `"action": "attack"` execute site (11 today) that feeds each producer `{"success": False, "message": "x", "blocked_naval": "Britain"}` and asserts the row/response has no outcome/battle_message/battle_details/requires_input, contains "x", and world.battles_this_turn is unchanged — so a twelfth site cannot drift; plus the negative control that a real battle still narrates.
