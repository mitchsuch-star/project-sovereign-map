# Slice 9 review — lens R3 "the record against the code" (at master `57b08c39`, September 5, 2026)

Working tree at review time was `57b08c39` clean (HEAD == the snapshot). Probes under the session scratchpad `reviewS9/R3/` (`census.py`, `sweep_structure.py`, `probes.py`, `probes2.py`, `probes3.py`); all TestClient probes swapped `M.world` / `M.game_state["world"]` / `M.parser = CommandParser(use_real_llm=False)` on a fresh `WorldState.from_scenario(europe_1805.json)`, `SOVEREIGN_SCENARIO=none` set before import, no `PYTHONIOENCODING`. Nothing under the repo was written; no git mutation; the sweep and the full suite were not run.

## 1. Claims table

| # | Claim (where) | Verdict | Evidence |
|---|---|---|---|
| 1 | "34 tests" (block, STATUS, CLAUDE.md, commit) | **VERIFIED** | `pytest --collect-only` → 34 nodes; ran the file at HEAD: **34 passed in 3.51s**. |
| 2 | "sweep 12 mutations, 12 killed, 0 INERT" | **STRUCTURE VERIFIED / kill count UNVERIFIABLE** | 12 entries; every `old` occurs exactly once in its snapshot file; every named test node exists (`sweep_structure.py`: ALL OK). Not run. Note: for `helper/c` the first-listed test (`test_a_question_already_riding_the_result_is_kept`) cannot kill the mutation — the checker refuses Soult so the dict is never rewritten with or without the guard; the kill comes only from the synthetic `test_a_foreign_question_on_the_result_is_kept` (the record's own "isolation pin by construction"). |
| 3 | "Corpus 675/675, untouched" | **VERIFIED** | `python -m backend.ai.parser_eval` → `675/675 passed (mock mode)`, 4 live_only skipped; `tests/data/parser_golden_corpus.json` not in `git diff --name-only 2c451162 57b08c39`. |
| 4 | "zero new serialized fields" | **VERIFIED** | Diff touches 5 backend files; no `to_dict`/`from_dict` hunk in `world_state.py`; `marshal.py` untouched. |
| 5 | "zero `.gd`" | **VERIFIED** | No `.gd` in the diff file list (12 files: 5 backend, 5 docs, 1 test, 1 sweep JSON). |
| 6 | "ruff clean" | **VERIFIED** | `.venv\Scripts\python.exe -m ruff check backend/` → All checks passed. |
| 7 | "BASELINE_SERIES + M1–M7 byte-identical as MEASURED" | **VERIFIED (re-measured)** | `tests/test_combat_sweep_metrics.py` 11 passed; `tests/test_ai_intent_assurance.py::TestArmAControl::test_threat_series_is_the_standing_baseline` passed (`spawn_run` has no cache — it re-runs the 40-turn child). |
| 8 | "FA-N1's census corrected: three unchecked families, not four" | **FALSE as a count** (the correction of the strategic arm is right) | The strategic "proceed" −10 IS covered (see #9). But the census also MISSED three lowering writes with no checker on their path at `2c451162`, all net-only today: (a) the **tactical** failed-roll −3 — `meta_executor.handle_objection_response` `apply_defiance_outcome(marshal, "failed_roll", world)` runs AFTER `disobedience.handle_response`'s checker; `check_redemption_threshold` appears once in `meta_executor.py` (the defiance-fires branch only). Probe: `Davout, attack Mack` → MODERATE, quoted −5; Davout set to 26; `insist` with the roll forced to fail → **26 → 18, `redemption_event` absent from the `/respond_to_objection` response, not latched, `world.pending_redemption` None; the next `end turn` asked**. (b) `_execute_cancel` mid-march −3 (`strategic_executor.py`, no checker; none in `executor.py`/`main.py`). Probe: `Ney, march to Brittany`, `end turn`, Ney at 22, `Ney, cancel orders` → "Ney halts his march and awaits new orders.", **22 → 19, nothing asked, not latched; the next end turn asked**. (c) `vindication_tracker.resolve_battle` (`vindication.py`): −5 insist+defeat, −1 insist+draw, −2 compromise+defeat, called at pipeline step 11 (`combat_executor.py`); the only checker in `combat_executor.py` is the friendly-fire one inside `_execute_bombardment`. By the record's own definition the pre-slice count was six families plus the two small ones, not three plus two. |
| 9 | "the strategic proceed −10 is reachable only through `_handle_strategic_objection_from_endpoint`, which zeroes the penalty and runs its own final checker" | **NARROWED** | Zeroing verified (`original_command["v2_insist_penalty"] = 0` when `_trust_penalty_applied`; the proceed arm then adds 0); final checker verified; typed answer routes `/command` → `_respond_to_objection_sync` → `executor.handle_objection_response` → the strategic slot → this handler. BUT a second producer enters the arm: `jealousy._apply_command_choice` calls `_execute_strategic_command` with `"objection_response": "proceed", "v2_insist_penalty": 0` — charges 0, so not a lowering write; `_execute_post_objection`'s strategic re-entry passes the original command WITHOUT `objection_response` (re-evaluates). "Only" is false; "the only lowering entry" is true. The pin (§3) cannot see the jealousy producer. |
| 10 | "Measured: MODERATE objection quoting −5; insist charged −8 = −5 + failed-roll −3; no double charge" | **VERIFIED mechanism, NARROWED numbers** | Davout at boot trust 85: quoted −5, button −5, charged **−8**, `defiance` absent, Berthier: "-5 for the order pressed home, -3 more for the manner of his obedience" — but concern read **STRONG** on this run (mood variance). At trust 26 the same objection quoted **−15** and charged **−18**, and the final checker fired (`redemption_event`, `awaiting_redemption_choice`, latched). The −5 is the DEVOTED-tier insist penalty (`objection_v2.get_insist_penalty`: −15/−12/−10/−5), not a property of MODERATE. |
| 11 | "Lannes 23 → 20 → 17 before the fix" | **VERIFIED (arithmetic + lever-off pin)** | `expectation_for_wins(3) = 120`, satisfaction 0, `min(EROSION_MAX=3, ceil(120/50)=3) = 3`/turn; `test_levers_off_reproduce_the_measured_bleed` asserts (17, False) at HEAD with both levers down. |
| 12 | "the geometry must sit past turn 5 — at boot `turn − GRACE_TURNS` is negative and the tick reads it as no clock" | **VERIFIED mechanism, NARROWED bound** | `if marshal.expectation_grace_turn < 0:` opens a fresh clock; `GRACE_TURNS = 4`. Turn 4 already erodes (grace 0 is not < 0, elapsed 4 ≥ 4); "past turn 5" is not necessary. |
| 13 | "the erosion tick now RETURNS its events" / "on every path" | **VERIFIED** | Three `return`s in `_process_dotation_state`: `[]`, `[]`, `events`. |
| 14 | "`advance_turn` extends `tactical_events` (the list the end-turn hoist reads)" — before the fog filter, row survives | **VERIFIED** | The extend precedes `self._last_tactical_events = tactical_events`; the fog filter runs later (`meta_executor.py`, `executor.py`, `main.py`); the `{"type": "redemption_event", ...}` row has no marshal/nation/location → kept by the location-less system-event arm. `hoist_tactical_redemption` read at both turn-advance paths. |
| 15 | "`_include_command_redemption_event` on the petition endpoint was DEAD" | **VERIFIED** | `_build_result_response` forwards every key but `new_state` (so `redemption_event` + `state`); `check_redemption_threshold` writes `world.pending_redemption` at generation (`REDEMPTION_LATCH_AT_GENERATION_ACTIVE = True`); the include would add only the `[ALERT]` print. `_QUESTION_KEYS` contains `redemption_event`, so the response is non-draining (slice 6 rule). |
| 16 | "The client reads no field of `independent_command_report`" | **VERIFIED** | 0 hits across all `.gd`; the client sees the report only as the text `meta_executor` folds into `message`. |
| 17 | Rider: "reproduced on 2c451162 … `jsonable_encoder` died on a tuple-keyed map" | **premise VERIFIED / the 500 itself UNVERIFIABLE here** | After one processed `end turn`, `jsonable_encoder({"result": {"new_state": {"world": w}}})` raises `TypeError: unhashable type: 'list'`; on a FRESH boot world it does not (the tuple-keyed cache appears only after a turn is processed). Pre-slice `report_entry["result"] = action_result.get("result")` embedded the executor result whole. No checkout permitted, so the 500 is taken on the record's word. |
| 18 | "the friendly-fire sibling had always asked" | **VERIFIED** | `combat_executor.py` −5 → checker inside `_execute_bombardment`. The diff's code comment says "the friendly-fire sibling **below**" — it is ABOVE, in another function. |
| 19 | "jealousy's petition docks (shadow / confrontation rebuke / rivalry / Fontainebleau / war-weary) lower trust" (main.py comment) | **NARROWED** | `SHADOW_PROMISE_TRUST = +2` raises. Lowering docks: rebuke −5, rivalry −3/−3/−5/−3, Fontainebleau refuse −8, war-weary march −4. `handle_petition_response` has ONE caller (the endpoint) — no typed bypass of the endpoint loop. |
| 20 | "Delivery note: a question staged with NO carrier … the client's once-per-turn poll (`GET /pending_redemption`, PT-B1) delivers it" (block; SYSTEMS_REFERENCE: "no write can leave a man unasked for more than one turn") | **NARROWED / the SYSTEMS_REFERENCE sentence FALSE in this case** | Backend: probe C — `stage_redemption(world, lannes, result={})` (a carrier that never reaches the wire, the AI-phase shape) latches him and writes `world.pending_redemption`; the next `end turn` response carries **no `redemption_event`, `state` None, 0 tactical rows**, and the net cannot re-emit (the checker's latch guard) — only `GET /pending_redemption` has it. Client (`main.gd`): `_maybe_recover_dropped_redemption()` runs at five control-return tails (`_on_command_result` non-enemy-phase tail, `_return_control_to_player`, `_on_battle_diorama_dismissed`, the petition-dismissed tail, `_on_proclamation_dismissed`) and **not** on `_process_next_interrupt`'s exit; it polls **at most once per client `current_turn`**, the latch `_redemption_recheck_turn = current_turn` set at SEND; the callback drops silently if `pending_redemption or _is_modal_dialog_open()`. For an end-turn-latched question: `current_turn` is advanced in `_sync_response_hud` before the enemy dialog, the end-turn `_on_command_result` returns early, and the first poll is in `_return_control_to_player` after the enemy-phase/strategic-report dialogs → same turn, ordinarily. But `_on_enemy_phase_dismissed`/`_on_strategic_report_dismissed` call `_show_pending_dispatch()` (which raises a stashed **capture** modal) BEFORE `_return_control_to_player()` → the poll fires with that modal open, the answer is dropped, the latch is set, and no further poll happens that turn — the question waits for the NEXT turn's first control return (one full turn late, repeatable). The latch is reset only by the redemption dialog's own handler and the world swap. |
| 21 | "Murat `trust.modify(-90)` → 0, nothing asked (pre-slice)"; "Two men at 15/12 → nothing asked: no end-turn path consulted the checker" | **VERIFIED** | Trust clamps at 0 (test at HEAD); pre-slice the only all-marshal loop with the checker was `_check_cavalry_limits` (cavalry write only) — consistent with the verification report §FA-N1 and the current code. |
| 22 | "Two men crossing in one tick → exactly ONE question; the second asked once the first is answered; cooldown honoured; Mack at 10 never asked; GR5 symmetric erosion" | **VERIFIED** | Checker guards (one-live rule via `standing_redemption`, cooldown, nation); tests green at HEAD. |
| 23 | "the endpoint handler applies the penalty ITSELF for every MODERATE+ objection — the only tier that raises a popup" | **VERIFIED** | Popup + stored dict only in the `>= MODERATE` branch (`concern_level` always MODERATE+); even a sub-MODERATE stored objection (save-load) is covered by the final checker at return. |
| 24 | "three INERT on the first sweep … the endpoint's own nation filter duplicated the checker's → loop reads `get_player_marshals()`" | **VERIFIED (code shape)** | History of the sweep passes not checkable. |

## 2. Independent census (AST walk over `backend/` at the snapshot: 49 call sites — matches the report's 49; 11 negative literals, 37 variable, 1 positive literal)

| Site | Write | Path | Checker on path at 57b08c39 | Record |
|---|---|---|---|---|
| `world_state.py` `_process_dotation_state` | −1..−3 | end turn | `stage_redemption(events=)` + net | listed (tick) ✔ |
| `combat_executor.py` `_execute_attack` | −3 no-show | any attack | `stage_redemption(result=)` + net | listed ✔ |
| `jealousy.py` (rebuke, rivalry ×4, Fontainebleau refuse, war-weary march) | −5, −3×4, −5, −3, −8, −4 | `/marshal_petition_response` (sole caller) | endpoint loop + net | listed ✔ (shadow +2 wrongly named as a dock in the code comment) |
| `combat_executor.py` `_execute_bombardment` | −5 friendly fire | bombardment | in-function | covered ✔ |
| `world_state.py` `_check_cavalry_limits` ×2 | −3 | end turn | in-function | covered ✔ |
| `strategic.py` `_respond_*` ×7 | var | interrupts | `_attach_redemption_if_needed` | covered ✔ |
| `disobedience.py` `handle_response` ×3 | var | tactical objection | in-function | covered ✔ |
| `strategic_executor.py` endpoint insist | tier penalty | `/respond_to_objection` | final checker | covered ✔ |
| `strategic_executor.py` proceed arm | `v2_insist_penalty` (default −10) | endpoint (zeroed) / jealousy (0) | endpoint final checker | the "false fourth" — **correctly struck**, but "only" over-states (jealousy producer) |
| `defiance.py` failed-roll −3 via the strategic endpoint handler | −3 | strategic insist | final checker | covered ✔ |
| **`defiance.py` failed-roll −3 via `meta_executor.handle_objection_response`** | −3 | **tactical** insist, roll fails (the ~95% path) | **none after the write** — `handle_response`'s checker ran before it; `meta_executor.py` has one checker call (the defiance-fires branch) → NET only | **MISSED** (probe: 26→21→18, not asked on the response, asked next end turn) |
| `defiance.py` outcome False −5 via `meta_executor` / `strategic_executor` | −5 | defiance fires, wrong | checked in both | covered ✔ |
| **`strategic_executor.py` `_execute_cancel`** | −3 mid-march (0 on first step) | typed `cancel orders` | **none** → NET only | **MISSED** (probe: 22→19, not asked, asked next end turn) |
| **`vindication.py` `resolve_battle`** | −5 insist+defeat, −1 insist+draw, −2 compromise+defeat | post-combat pipeline step 11 (`combat_executor.py`) | **none** → NET only | **MISSED** |
| `dotation.py` `apply_estate_confiscation` | −1 | capture choice | NET only | listed (small) ✔ |
| `diplomatic_executor.py` `_apply_diplomatic_trust_reactions` | clamped ±5/turn | 4 callers (war declaration, treaty break, ultimatum, vassal created) | NET only | listed (small) ✔ |
| `vassal.py` ×2 | `40 − trust` (reset to `ASSIMILATION_TRUST = 40`) | assimilation | cannot cross 20 | n/a |
| `feedback.py` | gated `> 0` | | positive only | n/a |
| `turn_manager.py`, `strategic.py`, `defiance.py`, `strategic_executor.py` gains | positive gains | | n/a | n/a |
| `marshal.py` | the `modify_trust` implementation | | n/a | n/a |

**Conclusion:** the record lists every write it names correctly, and correctly strikes the strategic proceed entry — but its count is wrong in the other direction. Three lowering writes with no checker on their path were not in the FA-N1 census or in the slice-9 correction: the tactical failed-roll −3, the mid-march cancel −3, and vindication's −5/−1/−2. All are net-covered at HEAD (asked at the turn boundary, not on the response that charged them).

## 3. Pin quality (34 tests, `tests/test_fa26_the_question_is_asked_2026_09_05.py`)

| Test(s) | Kind | Notes |
|---|---|---|
| `TestTheErosionTickAsks` ×7 | behaviour (real `/command` end turn; direct tick call ×2) | `test_levers_off_reproduce_the_measured_bleed` reproduces the PRE-slice shape exactly (17, not latched, no rows, no `state`, `pending_redemption` None) — a real lever-off arm, not merely "not asked". `test_the_tick_stages_at_its_own_write_with_the_net_off` isolates seam 1 (write on, net off). |
| `TestTheNet` ×6 | behaviour | `test_net_off_leaves_a_direct_write_unasked` = pre-slice shape (0, not latched, nothing on the response). Two pins stand the autonomous man down to avoid a capture question — the record states this. |
| `TestThePetitionEndpointAsks` ×5 | behaviour (real `/marshal_petition_response`, `handle_petition_response` stubbed) | The stub replaces the jealousy dock, so these pins bind the ENDPOINT loop, not the docks' own arithmetic (acceptable — the docks are pre-existing). Lever-off arm reproduces pre-slice (19, silent). |
| `TestTheFailedReinforcerAsks` ×3 | behaviour (real `Ney, attack Mack`, reinforcements monkeypatched) | Lever-off arm reproduces pre-slice (19, silent). |
| `TestTheTurnAfterAutonomySerializes` ×2 | behaviour (`raise_server_exceptions` → a 500 would raise) + direct builder | The direct pin asserts the copy is stripped but does not assert the ORIGINAL contained `new_state` — if the executor result ever stops carrying it the pin goes vacuous rather than red (weak, not wrong). |
| `TestTheHelper` ×6 | behaviour (direct helper) | `test_a_foreign_question_on_the_result_is_kept` is synthetic by construction (record says so). |
| `TestTheCensus` ×5 | **text pins** (source grep) | Bind placement only. Pin 5 (`len(sites) == 2` over `\["objection_response"\] = ` in `strategic_executor.py`) **cannot see a dict-literal producer in another file** — `jealousy._apply_command_choice` exists today (penalty 0); a future producer that omits `v2_insist_penalty` charges an unchecked −10 and the pin stays green. Pins 1-4 would stay green if a staged call were disabled by argument (e.g. `events=None`) — the behaviour pins cover that today. |

Sweep: every mutation lists at least one behaviour pin; none is killable only by a `TestTheCensus` text pin. `helper/c` is killed only by the synthetic isolation pin (see claims #2).

## 4. Defects found

**R3-1 (P2, record — census count).** "Three unchecked families, not four" is false; at `2c451162` there were at least six lowering-write families with no checker on their path, three of which the record never names and which remain **net-only** (asked one turn late, not on the response that charged them): (a) tactical failed-roll −3, `backend/commands/meta_executor.py` (`apply_defiance_outcome(marshal, "failed_roll", world)` after `handle_response`'s checker); (b) mid-march cancel −3, `backend/commands/strategic_executor.py` `_execute_cancel`; (c) vindication −5/−1/−2, `backend/commands/vindication.py` `resolve_battle` via pipeline step 11 in `backend/commands/combat_executor.py`. *Repro (a):* fresh 1805 board, mock parser; `Davout, attack Mack` → MODERATE (`insist_penalty` −5); `davout.trust.set(26)`; monkeypatch `meta_executor.random.random = lambda: 0.999`; POST `/respond_to_objection {"choice":"insist"}` → trust 18, response has no `redemption_event`, `redemption_pending` False; `end turn` → asked. *Repro (b):* `Ney, march to Brittany`; `end turn`; `ney.trust.set(22)`; `Ney, cancel orders` → 19, nothing asked; `end turn` → asked. *Fix shape:* `stage_redemption(world, marshal, result=response_result)` after the tactical failed-roll write (the tactical twin of the strategic handler's final checker); `stage_redemption(world, marshal, result=<the cancel result>)` in `_execute_cancel`; `stage_redemption(world, attacker, result=result)` after step 11 in the pipeline — or, if net-only is the accepted contract for these, correct the count in the block, STATUS, CLAUDE.md, SYSTEMS_REFERENCE and the FA-N1 addendum and NAME them beside the confiscation and diplomatic writes.

**R3-2 (P3, record + client delivery).** `SYSTEMS_REFERENCE.md` §7: "so no write can leave a man unasked for more than one turn" is false for a question staged with a carrier that never reaches the wire (the AI-phase battle the block's Delivery note describes): the backend never re-emits it (probe: `stage_redemption(world, lannes, result={})`, then `end turn` → no `redemption_event`, no rows, `world.pending_redemption` still set), and the client poll it relies on is once-per-turn with the latch set at SEND and a silent drop under any open modal — `_on_enemy_phase_dismissed` raises the stashed capture modal via `_show_pending_dispatch()` immediately before `_return_control_to_player()` polls, so on exactly the turns that end with a conquest the question slips a whole turn; `_process_next_interrupt`'s exit never polls. *Fix shape (either side):* client — set `_redemption_recheck_turn` on successful delivery in `_on_pending_redemption_checked` (or re-poll from the capture dialog's close tail); backend — the end-turn hoist falls back to `standing_redemption(world)` when the tick produced no row, so a latched question is re-raised once per end turn.

**R3-3 (P4, record wording + pin scope).** "reachable only through `_handle_strategic_objection_from_endpoint`" — `jealousy._apply_command_choice` also enters the proceed arm (penalty 0). The census pin is scoped to `strategic_executor.py` and cannot see it; widen to an AST census over `backend/` for any dict carrying `"objection_response": "proceed"` and assert each carries `v2_insist_penalty == 0` or is the endpoint handler.

**R3-4 (P4, wording).** "objected MODERATE quoting −5": the −5 is the DEVOTED-tier insist penalty at boot trust 85; at trust 26 the same objection quotes −15 and charges −18; the boot run here read STRONG. State it as "at boot trust (DEVOTED tier) the quote is −5".

**R3-5 (P4, wording).** "the geometry must sit past turn 5" → turn ≥ `GRACE_TURNS` (4) suffices.

**R3-6 (P4, code comments in the diff).** `combat_executor.py` "the friendly-fire sibling **below**" — it is above (`_execute_bombardment`); `main.py` lists "shadow" among docks that LOWER trust — `SHADOW_PROMISE_TRUST = +2`.

**R3-7 (P4, test-file docstring).** Line 1 reads "FA build slice **8**"; this is slice 9.

## 5. Omissions (behaviour the diff changes that the record does not state)

- The petition endpoint now puts every player marshal to the checker on EVERY petition answer, including a `+3` one — so a marshal driven to ≤20 earlier in the same turn by an unstaged write (cancel, vindication, confiscation) is asked on the petition response rather than at the turn boundary. Benign; unstated.
- The attack seam writes `redemption_event` + `state: "awaiting_redemption_choice"` onto an **AI** attack's result during the enemy phase; that dict rides `enemy_phase.actions[*]` after `_build_visible_enemy_phase`. No consumer reads it there (names a French marshal, no fog leak) — unstated.
- The net's row rides `tactical_events`; the client's tactical loop prints only `event.message`, which the row lacks, so it renders nothing — harmless, unstated.
- `_process_dotation_state`'s signature changed `None → list` (single caller, `advance_turn`) — stated in the block only implicitly ("RETURNS its events").

## 6. Not checked

- The sweep's kill results (`tools/mutation_sweep.py` not run) and the "three inert on the first pass" history.
- The pre-slice HTTP 500 on `2c451162` itself (no checkout permitted) — only its mechanism.
- The full suite; Godot boot smoke (no `.gd` changed); anything under the live LLM.
- The FA-N1 verification-report addendum's non-count prose beyond what appears in the table above.
