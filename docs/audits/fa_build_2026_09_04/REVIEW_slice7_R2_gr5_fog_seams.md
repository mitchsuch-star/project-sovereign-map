# Slice 7 review — lens R2 "GR5, fog, and the executor seams" (at master `764d0ffc`, September 5, 2026)

Probes: all mock parser (`M.parser.llm.use_real_api is False` asserted), all three module seams swapped, real `/command` unless stated. Repository untouched; no git mutation; no sweep; no full suite.

## (1) Findings

| id | sev | file:symbol | probe → measured | fix shape | how |
|---|---|---|---|---|---|
| **R2-1** | **P2** (introduced; GR5-symmetric, silent for the AI) | `prisoners.prisoner_of` + `combat_executor._execute_attack` (the `prisoner` short-circuit ahead of the province fallback) | The 1805 boot has exactly ONE marshal/region collision: `Brunswick` (Prussia) ↔ province `Brunswick` (Hanover). Marshal Brunswick captured by France, Ney at Berlin: `Ney, attack Brunswick` → `"Brunswick is our prisoner at Paris…"`; lever off → `"Choose your war purpose against Hanover…"` (the province attack proceeds). **AI side**: Austria's Charles at Berlin, province Brunswick French-held → lever on `success=False "Brunswick is a prisoner of France at Paris"`; lever off `"ArchdukeCharles marches from Berlin into Brunswick unopposed! … Captured"`. So for the whole captivity the PROVINCE is un-attackable by name for every court. The AI's homeland-defence / liberation / undefended-capture rungs all emit `attack <region name>`, so an AI wanting that province silently fails each turn. | When the typed name is ALSO a region key and the marshal it names is captured, the province is the only live referent — let the region path run. | RUNNING |
| **R2-2** | **P2** | `prisoners.prisoner_refusal` at all three seams and `question_desk._answer_enemy` (captor + tombstone arms) | Kutuzov (Russia, boot at Podolia, visibility unknown) captured by Prussia via `world.capture_marshal`: the `marshal_captured` event is filtered OUT of the campaign log, the intel report never names him. Yet `Ney, attack Kutuzov` / `pursue Kutuzov` / `march to Kutuzov` / `bombard Kutuzov` → `"Kutuzov is a prisoner of Prussia at Berlin, Sire"`; `where is Kutuzov` → `"Kutuzov is a prisoner of Prussia"`. Destroyed by Prussia at unknown Podolia → `where is Kutuzov?` → `"…destroyed at Podolia on turn 1."` (the location of a fogged third-party battle). **`test_a_prisoner_of_another_court` pins the leak.** | Name the captor/tomb only when the cell that holds him is in view; otherwise answer as pre-slice. Own-captor arm stays. Re-target the pin. | RUNNING |
| **R2-3** | **P2** (introduced) | `executor.py` objection battery, PF-4/TUT-F6b block + `_attack_target_location` | Brunswick captured by France, Davout (cautious) at Berlin: `Davout, attack Brunswick` → `awaiting_player_choice "Davout respectfully raises concerns…"`; `insist` → `"You insist… (Trust -8…) Execution failed: Brunswick is our prisoner at Paris"`. Trust **85 → 77** for an order the executor refuses at zero cost. The PF-4 idiom ("a marshal never objects to a move the executor is about to refuse") is broken by this slice's own arm. | Refuse BEFORE the objection (PF-4 pattern). | RUNNING |
| **R2-4** | **P2** (introduced — a false fact answer) | `question_desk._answer_enemy` (`get_visible_enemies` + `_last_report_of` STALE/LAST_KNOWN only) | Boot: Deroy (Bavaria, PEACE) at Franconia at **FULL**; `where is Deroy?` → `"We have no word of Deroy's whereabouts, Sire."` while the report prints `Deroy (Bavaria): 22,000 troops at Franconia`. Austria at ARMISTICE, Ney at Swabia (FULL): `where is Mack?` → "no word". Every non-belligerent court is "unseen" by construction. | Derive visibility from `world.get_region_intel(marshal.location)` for ANY non-player marshal. | RUNNING |
| **R2-5** | **P2** (pre-existing) | `strategic_executor._execute_strategic_command` SUPPORT arm | Ney captured by Austria at Vienna: `Davout, support Ney` → `success=True "Davout moves to support Ney (at Vienna)…"`, AP 4→2, order `SUPPORT Ney` standing — marching toward the captor's capital. | One more `prisoner_refusal` in the SUPPORT arm. | RUNNING |
| **R2-6** | **P3** (introduced) | `question_desk._answer_enemy` (region aggregate band as the man's; `_last_report_of` tier filter) | Archduke John (20,000) moved into Swabia beside Mack: `how many men does Archduke John have?` → `"massive force"` (the REGION's 72,000 aggregate). Mack moved into empty-PARTIAL Frankfurt: `where is Mack?` → `"reported at Frankfurt — no forces."`. Mack moved far into unknown Podolia: `where is Mack?` → "no word" while `who is at Swabia?` → `"Reported: Mack — large force, this turn"` — the desk contradicts itself. | Print the man's band; let `_last_report_of` scan all tiers. | RUNNING |
| **R2-7** | **P3** (introduced) | `question_desk._state_words` on the enemy FULL arm | `what is Mack doing?` → `"…fortified, in square, broken and recovering."` — no surface shows a foreign corps' works/square/rout (they live in the player-only `tactical_state`). | Drop `_state_words` from `_answer_enemy`. | RUNNING |
| **R2-8** | **P3** (introduced claim "every enemy lookup") | `combat_executor._execute_charge`; `movement_executor` scout/move region fallbacks | Mack France's prisoner: `Murat, charge Mack` → `"Mack has no troops to fight!"`; `Ney, scout Mack` / `Ney, move to Mack` → `"Region 'Mack' not found. Did you mean 'La Mancha'?"` — FA-24's exact sentence, still printed by two verbs. | `prisoner_of` in the charge loop; the scout/move region-not-found arm answers a marshal as a marshal. | RUNNING |
| **R2-9** | **P3** (introduced) | `clause_guards.MODAL_LEADS_ARE_QUESTIONS` / `is_question` | `Ney, would you scout Swabia?` → `help`; `Davout, would you please fortify?` → `help`; `Ney, would you attack Mack for me` → `help`. Meanwhile `would Ney attack Mack` (no `?`) still FIGHTS — gold −124. The lead+`?`-or-first-person rule cannot tell the second-person polite imperative from a third-person question. | Modal + `you` = order; modal + a third-person subject = question, regardless of punctuation. | RUNNING |
| R2-10 | P4 | `llm_client._askable_enemy_names` docstring + record §(vi) | "A court's roster is public (the ledger prints it)" — the ledger's intel tab is fog-filtered; no surface prints a full enemy roster. Behaviour defensible (pre-slice `pursue <unseen>` already answered "No intelligence on X's position"), the justification wrong. | Correct the docstring/record. | READING |
| R2-11 | P4 | `question_desk._answer_region` LAST_KNOWN branch | Prints a frozen band at LAST_KNOWN; the report prints names+age without a band. | Suppress the band at LAST_KNOWN. | READING |
| R2-12 | P4 (test gap) | `prisoners.prisoner_of` viewer exclusion | Unpinned — deleting it passes every slice-7 test. | Add the own-prisoner pin. | RUNNING |
| R2-13 | P4 | `naval_executor._admiralty_misaddressed` vs `_execute_set_fleet_posture` | The posture arm refuses on `command.get("marshal")` truthiness; the new arm requires `marshal.nation == actor`, so a foreign marshal on the dict is discarded. Unreachable from the parser. | One predicate for all three verbs. | RUNNING |
| R2-14 | P4 | `meta_executor._execute_status` None fallback | Unreachable in production; if reached it re-creates FA-D25's wall for a classified question. | Answer "I cannot say, Sire". | RUNNING |

## (2) Attempted and NOT reproducible
- `_hostile_first` naming a prisoner / strength-0 / armistice court: no defect. AI naval command carrying a marshal: dormant. `_names_the_admiral` edge cases: no crash. `Berthier, <order>` after `berthier` joined the non-name words: no order executes. Honorific regexes: no regression. Displayed-name register in `prisoner_of`: all reach the prisoner line.
- `Ney, attack Mck` / `Xyzzy` marching at the nearest enemy with a disclosure — **pre-existing** (the ESP-EV-4 `auto_resolved` ruling), lever-independent.

## (3) Fog audit (branch × visibility → what is revealed vs the report / map)
FULL (at war): the desk over-disclosed tactical states (R2-7). PARTIAL: matches the map's live rule; the band was attributed to the man wrongly (R2-6). STALE/LAST_KNOWN: consistent. UNKNOWN (never seen): existence confirmed, defensible (R2-10). Court at PEACE/ARMISTICE at FULL: FALSE "no word" (R2-4). Prisoner of a THIRD court (capture fogged): **leak** (R2-2). Fallen third-party, fogged: **leak** (R2-2). `who_holds` / adjacency: public, ok.

## (4) Record claims checked
- "a PRISONER IS NAMED … (player AND AI viewer, GR5)" — TRUE that the arm runs for both viewers, and it changed AI behaviour on the Brunswick collision.
- "a prisoner named at every enemy lookup" — FALSE for `_execute_charge` and the scout/move fallbacks.
- "answered FOG-HONESTLY … 'no word' when unseen, the tombstone when fallen" — FALSE for the captor and tombstone arms and for PEACE/ARMISTICE courts at FULL.
- "`BASELINE_SERIES` and M1–M7 byte-identical WITHOUT re-record" — TRUE as measured for the parser-side levers; NOT structural for the prisoner arm (AI `attack <region>` goes through `_fuzzy_match_enemy` with `attacker_nation`; identity is a fact about the seed).
- "a court's roster is public (the ledger prints it)" — FALSE.
- Text pins vs behaviour pins: `test_the_fallback_gate_reads_one_list` is a source grep with no behaviour pin on the LLM-fallback gate; lever-off arms that reproduce the PRE-slice behaviour: pursue/bare-retreat/admin-march/desk/dismissed/guarding/naval; weaker ("not success"): plain-speech, support, typo.

## (5) Not checked
The live Anthropic parser; the Godot client; the 40-turn series / M1–M7 (argued structurally); the mutation sweep (read only); the AI's failed-attack cooldown path.

## Disposition (by the builder, September 5, 2026)
R2-1..R2-14 ALL FIXED in the review-round commit — R2-1 as `prisoners.prisoner_is_a_province` (a captive's province namesake means the province, at every seam, both viewers); R2-2 as `prisoners.cell_in_view` (a third court's captive or tombstone is named only where the cell is PARTIAL+; our own captive always); R2-3 as a PF-4 skip in the objection battery; R2-4/6/7/11/14 in the desk; R2-5 in the SUPPORT arm; R2-8 in the charge loop and a `_marshal_not_a_province` arm on move/scout; R2-9 as the subject rule in `is_question`; R2-10 the docstring; R2-12 a pin; R2-13 one predicate. The record's "BASELINE_SERIES structurally identical" is re-stated as "identical as measured; the prisoner arm is on the AI path and a captured-then-attacked-by-name namesake did not occur in 40 turns of the historical seed".
