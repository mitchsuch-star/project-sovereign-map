# The Endgame Program — the fixes, then "The Congress of Paris" (row EP)

> **Status: RULED September 23, 2026 (evening) — BUILD-READY. This document is the routing authority for row EP; `docs/STATUS.md` ▶ NEXT UP points here.** **Progress: ~~F1~~ ✅ LANDED September 23, 2026 (landing record in §1 F1) → ~~F3~~ ✅ + ~~F4~~ ✅ LANDED September 24, 2026 (taken ahead of F2 by the user's direction; landing records in §1 F3 and §1 F4) → ~~F2 "The display-name pass"~~ ✅ + ~~F5 "Bohemia is not empty"~~ ✅ + ~~F6 "Settled once, reopened"~~ ✅ LANDED September 25, 2026 (one commit; landing records in §1 F2, §1 F5 and §1 F6 — **THE SIX FIXES ARE LANDED**) → ~~GE-1 "The Verdict and the Fall"~~ ✅ LANDED September 25, 2026 (with the user's three September 25 additions — the Emperor's death, the generals' death-odds memo, the exile story; landing record in §6; its adversarial review round — 38 surviving findings, 4 P1 — fixed and pinned the same day, and a verification round on those fixes — 27 more, all fixed; both addenda under the landing record) → ~~GE-2 "the client"~~ ✅ LANDED September 25, 2026 (landing record in §6) → ~~GE-3 "The Congress of Paris"~~ ✅ LANDED September 25, 2026 (landing record and its review-round addendum in §6 — the Pressburg arm wins on 4 of 4 seeds, the Premature arm loses on 3 of 3, both pinned DRIVEN) → ~~GE-V "the played campaign"~~ ✅ RUN September 25, 2026 (landing record in §6 after GE-3's; memo `docs/audits/GE_V_PLAYED_CAMPAIGN_2026_09_25.md` — **the reach from the boot is NOT achievable on typed orders in forty turns on two hand-played openings (35 titled by turn 12, best 41), so §2.8's rule fired: `hold_titled` 50 → 45; the table, the prices and the sitting read as designed; a P1 hole found and fixed (`vassalize Austria` on turn 1 subjugated a great power); GE-D1 and GE-D2 both RULED and BUILT under the grant; the ending re-scored 3.0 → 5.5, target ≥ 7 NOT met — the reach gate is GEV-D1**) → ▶ NEXT = the release build (ROADMAP 10).** Every decision below was taken by Claude under the user's delegated grant (*"create a plan to fix all of these, make all decisions, and for the ending make the decision on how to end — but it should be a challenge to force a new status quo, and you should have to hold the win state for X turns, with a mechanic that makes it fun"*). Each decision carries its argument and, where it matters, its re-open condition. The user has not separately confirmed them.
>
> **Order, by the user's direction:** *start with the fixes, end with the end state.* Slices **F1 → F6** (the live review's 21 defect rows and 5 design rows, `BUG_FIXES.md` §Live Review, `DESIGN_REFINEMENT.md` §Live Review), then **GE-1 → GE-2 → GE-3 → GE-V** (the ending). **The release build (ROADMAP 10) follows GE-V**, then Updates 1–5.
>
> **What it supersedes.** `GAME_END_SPEC.md` §7 was a PROPOSAL with six questions; this document RULES them (§2 below) and adds the mechanic the user asked for. `GAME_END_SPEC.md` §1 (the measured facts) and §2 R1, R3–R9 stand as written; R2 (the Verdict at 44) stands as the epilogue card (§4.4). Where this document and §7 differ, this document governs. `GAME_END_SPEC.md` §8 records the same in one paragraph so the two cannot drift.
>
> **Evidence.** `docs/audits/PLAYTEST_LIVE_REVIEW_2026_09_23.md` (the played campaign, the Steam review, the three end-state probes) and the code research summarized in `GAME_END_SPEC.md` §7.0.

---

## §0 The decisions in one page

| # | Question | Ruling |
|---|---|---|
| **D1** | Order | Fixes first (F1–F6, ~3.25 sessions), then the ending (GE-1..GE-3 + GE-V, ~4 sessions), then the release build. Player-facing first-contact fixes (F1) are the very first slice because the release build ships them. |
| **D2** | How the game is won | **The Congress of Paris (§2).** With **50 provinces held by TITLE** the Emperor may *summon the Congress*; every great power then answers — recognize or refuse — and the Congress **sits for 8 turns** during which refusers are driven to war, the paymaster pays them, coalitions form more easily, the marshals and the satellites present their bills, and the player must hold every titled province, Paris and the Emperor's freedom. It ends in **THE IMPERIAL PEACE** only if every great power recognizes, is shut out, or is gone. A failed Congress dissolves with a cost and a cooldown; the player may summon again. |
| **D3** | "Hold X for X" | X = **50 titled provinces** (39.7% of 126; the boot bloc is 35, so fifteen must be won AND settled) held for X = **8 consecutive turns** of the Congress. Both numbers live in the scenario's `campaign_end` block. |
| **D4** | The mechanic that makes it fun | The **summons is the player's choice** (a fuse they light when they judge themselves ready); the **table is public** (four rows, each court's stance, its reason and its PRICE, re-answered every turn); **refusal has teeth** (a refuser's intent climbs +15 a turn toward war; Britain funds every refuser; the coalition threshold drops); **every refuser can be flipped four ways** (beat them, buy their design, pay their price, court them) and the table says which; the marshals and the satellites present their bills at the summons; the Moniteur runs a Congress column; the clock is on every surface. Failure is legible and recoverable. |
| **D5** | Britain | Britain answers like the others but cannot be *beaten* on land. It is **shut out** (not blocking) when the Continental System closes ≥ 60% of the Continent's ports for the whole window and no British corps stands on the Continent. The Continental System finally has its purpose. |
| **D6** | Stabilization of the new status quo | **Title** (§2.2): a province counts only when it is homeland, ceded by a treaty the loser signed, or held 12 quiet turns after conquest. ONE new serialized record `province_title`. A signed cession *reconciles* that province's Revanche while the treaty holds. |
| **D7** | After the victory | Mark and continue, with a **Retire to the Tuileries** button. The Verdict at 44 still grades. |
| **D8** | Defeat | R1's two warned clocks stand (≤1 province or no free corps and no affordable commission, 5 turns; the Emperor captive 10 turns; Paris alone never). **Plus** "The Humbled Peace": ratifying a settlement that cedes Paris, half the homeland, or makes France a vassal stamps a **marked, non-terminal** defeat. A dissolved Congress is **not** a defeat. No third clock. |
| **D9** | Global elimination | E1–E6 of `GAME_END_SPEC.md` §7.5 RULED as written: the Universal Monarchy fires the Imperial Peace at once when no great power remains; the alarm stops climbing with nobody left to alarm; the dead stay dead (no enemy-phase rows, no fleets, no ports, no trade dominance, decks retired); knocking out a great power is a beat and a verdict input; great powers stay eliminable on the battlefield; the AI roster needs no change. |
| **D10** | Screens | ONE scene `campaign_end.tscn` with four registers — **The Imperial Peace** (gold), **The Fall of the Empire** (crimson, terminal: Load / Main Menu), **The Humbled Peace** (crimson-grey, continue), **The Verdict of History** (parchment, continue) — plus the Congress table as a tab of the Diplomatic Ledger and a one-line clock on the war room, the ledger and the end-turn banner. |
| **D11** | The reward curve (LV-D1) | Expectation rises only on a **decisive victory as lead** or a **glory-rank rise**, at most once per marshal per 4 turns, never before turn 6; the collective petition needs turn ≥ 12, three eroding marshals and ≥ 300g unmet; the dispatch's UNMET block appears only within 2 turns of erosion. |
| **D12** | Bavaria's walk-ins (LV-D2, FA-D13 re-opened) | One undefended capture per corps per turn (its `movement_range`), and a "literal" AI marshal takes the "cautious" strength check before walking into a province adjacent to a stronger enemy. `BASELINE_SERIES` re-recorded once, attributed. |
| **D13** | The strategic-orders recap modal (LV-D3) | Retired as a modal. Its lines join the morning dispatch's MARSHAL STATUS; a modal is raised only for an order that needs an answer (the interrupts already have their own). |
| **D14** | The legitimacy sentence (LV-14, LV-D4) | The whole-war blocker names the unbeaten courts and their scores and offers the separate peace as a chip on the table. The predicate is unchanged. |
| **D15** | Series and harness policy | F4, F5 and GE-3 move AI behaviour and each re-records `BASELINE_SERIES` **once, with a flip-experiment attribution**; every other slice must leave it and M1–M7 byte-identical. *(F4 as landed, September 24, 2026: NO re-record — a seven-arm attribution reproduced the recorded series on every arm, with the reason measured; §1 F4.)* |

---

## §1 The fixes — slices F1 … F6

Conventions for every slice: the four-file rule (`STATUS` ▶ NEXT UP struck and advanced, this document's slice table marked, `BUG_FIXES`/`DESIGN_REFINEMENT` rows disposed, `CLAUDE.md` LIVE STATE), a mutation sweep with 0 INERT at close, the XR-1 parse harness + boot smoke on any `.gd` change, and the series policy of D15. Effort is in sessions.

### F1 — "The first ten minutes" (0.5) — LV-1, LV-12, LV-13, LV-7, LV-8 — ✅ LANDED September 23, 2026 (landing record below the table)

| Row | Decision | Seam |
|---|---|---|
| **LV-1** the empty first turn | (a) The boot help moves into `_print_boot_help()` and is printed **after** every world swap that starts or loads a campaign (`_apply_world_swap_response` when `response.new_game` or a load is set), so Begin, Continue and Load all show it. (b) **A turn-1 briefing exists:** `/new_game` calls `build_morning_dispatch(world, boot=True)` — a new flag that runs the *pure* halves (situation, marshal status, the war block, the "what to do today" doors) and **skips every consuming arm** (the sabotage roll, the seen-lists, the once-per-turn beats), so the campaign seed and `BASELINE_SERIES` cannot move. It is stored as `last_morning_dispatch`, so R finds it and the "No dispatch available yet" copy becomes unreachable on a live world (delete it). (c) On Continue/Load the client re-renders the loaded turn's `last_morning_dispatch` in the terminal under "Loaded: …". | `main.gd` (`:799-810`, `:5270-5272`, `:5350`), `dispatch.py:2700`, `main.py:5005-5048`, `dispatch_view.gd:59-61` |
| **LV-12** the swallowed battle report | Reproduce at the command-result seam with a pending `incoming_proposal` AND `incoming_settlement_offer`. Rule: **a command's own result renders before any dialog the same response raises**. The `_post_hud_response_routes` entries that return before `_display_result` must call it first when the response carries `battle_report` / `message` from the player's own order (the BD §14.1 stash-and-raise discipline, applied to the envoy family). Pin: the terminal text contains Berthier's report AND the dialog is raised. | `main.gd` `_on_command_result` / `_post_hud_response_routes` |
| **LV-13** nine fog lines | The no-visible-action branch emits the same collapsed sentence the visible-action branch does: "Nine courts stirred, but their formations remain beyond our sight." Pin: a turn with zero visible actions renders exactly one fog line. | `turn_manager.py` enemy-phase summary builder (the `beyond our sight` producer) |
| **LV-7** "Enemy nations hold 98 regions" | Count only controllers at war with the player (`get_nations_at_war_with`, cached `get_nation_regions`); the line reads "The courts at war hold N regions." Pin on the boot board: 28, not 98. | `dispatch.py:2869-2873` |
| **LV-8** the 28-name war purpose | For a defense objective that covers the homeland, ONE sentence: "the homeland — 28 of 28 provinces held" (`_homeland_held_fraction` + `format_progress`); any other list capped at 8 names + "and N more" (`nation_names.py:91-95`). Sent as a field to the war panel and the war-detail popup; the envoy dialog's War Summary reads the same field. | `dispatch.py:4446-4463`, `war_status_panel.gd:318`, `war_detail_popup.gd:409`, the envoy payload |

Done when: a fresh Begin shows the boot help AND a turn-1 briefing in the terminal and on R; Continue shows the loaded turn's briefing; the LV-12 reproduction renders the report; boot dispatch says 28; the purpose line is one sentence on all four surfaces; `BASELINE_SERIES` + M1–M7 byte-identical.

**✅ Landing record (September 23, 2026 — the commit that carries this record).** Every done-when item is met and driven on the real `main.tscn` (`tools/ep_f1_first_ten_minutes_harness.gd`, fed payloads from the real endpoints; `tests/test_ep_f1_the_first_ten_minutes.py`, 48 tests); rules `SYSTEMS_REFERENCE.md` §56; rows disposed in `BUG_FIXES.md` §Live Review.

- **LV-1, as ruled, plus one addition.** `build_morning_dispatch(world, boot=True)` skips every consuming arm: the expectation latch, the headline-lead memory (`_select_headline(record=False)`), both warnings (they write the rail), Talleyrand's report (its cooldowns), the Session-6 sabotage roll, and the diplomatic-event queue (the next real dispatch reads and clears it). Its only write is `last_morning_dispatch`. **Measured:** the world after `/new_game` differs from a fresh `from_scenario` world ONLY in that field; the next real dispatch is identical with or without it; no module dice are drawn.
  - **Where it is built.** `main.py` `_ensure_first_morning` runs in `_reset_world_state`, so the process boot, `/new_game` and the School are all covered — not only `/new_game`, as the ruling said. It also runs in `/load` for a save written before its first end turn. A stored briefing is never rebuilt.
  - **Where it is read.** `/new_game` and `/load` carry it as `morning_dispatch` through `_readable_dispatch`, which is now also the one reader behind `GET /dispatch`.
  - **The addition, a TODAY section.** "The 'what to do today' doors" became the counsel's own orders (`ai/counsel.what_can_i_do`, at most 4), each sent as printed to `/command` and taken (pinned), plus first contact's `THREE_DOORS` + `CABINET_DOOR`, quoted rather than re-typed.
  - **The client.** `_print_boot_help()` is the one home of the help. The world swap prints the briefing, then the help. The Dispatch screen's empty arm no longer claims "appears at the start of each turn"; it is unreachable on a live world.
- **LV-12, as ruled.** Reproduced for real first: Austria's envoy was delivered, then `Ney, attack Mack`. The response carried both `battle_report` and `incoming_proposal`, and the envoy route returned before `_display_result`.
  - **The fix.** Six `_post_hud_response_routes` entries are marked `result_first`: the paradox, the petition, the proposal, the settlement offer, the sabotage and the rebellion. These are the modals a command does not ask for. `_on_command_result` passes `_render_own_result`, so the order's result prints first.
  - **Out of scope, filed.** The capture route is one the command DID ask for, and it has its own form of the defect. Filed as **LV-22**, owned by F3.
- **LV-13, one refinement.** The fog line is one sentence through PT-E4's `_join_courts` ("Britain, Russia, Prussia and 6 other courts stirred, …"). A single court keeps its own sentence, which names it best.
- **LV-7, as ruled.** The count now reads 28. It stays one region pass, with the at-war set read once; the per-nation cache would read stale on a fixture that re-colours a province without invalidating. Europe-scoped (N1). A new `enemy_regions_are_at_war` flag tells the client which sentence to print.
- **LV-8, as ruled; FIVE client surfaces, not four.** The player's own peace-confirm popup prints the same War Summary. ONE source: `war_status.objective_target_summary`. "The homeland is lost" at none held keeps PR-X1's rule by naming no province. Europe-scoped: the field is absent off the board, so the legacy payloads are byte-identical.
- **Levers:** `dispatch.THE_FIRST_MORNING_HAS_A_BRIEFING`, `dispatch.ONLY_THE_COURTS_AT_WAR_ARE_COUNTED`, `war_status.THE_PURPOSE_IS_ONE_SENTENCE`, `main.THE_FOG_IS_ONE_SENTENCE`.
- **Pins consciously flipped or re-scoped (9):**
  - `test_popup_routing_registry.py::test_on_command_result_uses_route_dispatcher` — the dispatcher call now carries the renderer.
  - `test_iq2_collapse_dispatch.py::TestWarObjectives::test_one_of_many_is_not_held` and `test_iq2_collapse_integration.py::TestTheWarPurposeListsOnlyWhatIsHeld` ×4 — these now run on the list shape that the LV-8 lever-down arm restores. Their levers still govern it. The sentence's PR-X1 guarantee is pinned beside them.
  - `test_pt_f_jealousy_channel.py::TestTheAutonomousAttackIsShown::test_it_is_called_before_the_enemy_phase` — re-scoped, assertion unchanged: it read the FIRST `_display_jealousy_attacks(response)` in the whole file, and `_render_own_result` now calls it earlier; it reads `_on_command_result`'s own call.
  - `test_ux23b_the_desk_is_quiet.py::TestTheDispatchReReadIsNotStale` ×2 — the copy-and-overlay moved verbatim into `_readable_dispatch`; the pins read it there, pin that `GET /dispatch` goes through it, and the never-rebuild guard now covers the shared reader too.
  - `test_wo_slice15_capture_question_holds.py::test_the_world_swap_handler_raises_it_client_side` — its body-length sanity ceiling 4000 → 6000 (the handler grew to 4,101 chars by the briefing and help it now prints).
- **Series:** `BASELINE_SERIES` + M1–M7 byte-identical — neither harness boots through `/new_game`, and every non-boot dispatch arm is unchanged. **Gates:** ruff clean, full suite green, Godot parse harness EXIT=0 (the harness registered in `TOOL_SCRIPTS`), boot smoke 0 `SCRIPT ERROR`, mutation sweep `tools/_sweep_ep_f1.json` **38/38 killed, 0 INERT, 0 BROKEN** (nine of them `.gd` mutations killed by the driven pins).

### F2 — "The display-name pass" (0.75) — LV-2, LV-3, LV-4, LV-6, LV-9, LV-10, LV-11, LV-18, LV-19 (NPC-12's first slice) — ✅ LANDED September 25, 2026 (landing record below the table)

| Row | Decision | Seam |
|---|---|---|
| **LV-2** raw marshal keys | `humanize_entity_name` (+ `formed_display_name` for the controller) at the three scout sites and the covering/shield lines; the client's diorama nameplate and war-table piece label pass names through a new `Utils.display_marshal_name` (the marshal-name sibling of `humanize_nation_keys_in_text`). NPC-12's census gains an AST pin over `movement_executor.py` and the covering builder: no `{m.name}`/`{enemy_marshal.name}` reaches a player string un-humanised. | `movement_executor.py:1085/1087/1150`, `combat_executor.py:6183-6192`, `battle_diorama.gd`, `war_table_piece.gd` |
| **LV-3** raw states and tags | `STATE_DISPLAY` for every diplomatic state in prose (`_ratify_treaty` `:12051`, the downgrade refusal `:11149`); courts through `with_definite_article(formed_display_name(world, n))`; the popup payload gains `from_nation_display` and `choice_display`, which `main.gd:5942/5946` render. The client's tag-skipping list (`utils.gd:265-274`) is left alone — the backend owns the article. | `world_state.py`, `diplomatic_executor.py:7001/5881`, `mailbox_payloads.py:399-415`, `main.gd` |
| **LV-4** the strategic dialog's copy | Every report row carries `command_display = get_strategic_display(cmd)` from the single return (`strategic.py:1294`); `turns_remaining` is sent as an int and rendered with `str(int(...))`; `STATUS_ICONS` gains `active` and `consumed`; `turn(s)` → the plural helper; `[HINT]` → a sentence ("Hungary lies undefended — an attack takes it."). The modal itself is retired in F3 (D13); this slice fixes the copy the dispatch will inherit. | `strategic.py:1236-1262`, `strategic_report_popup.gd:19-90` |
| **LV-6** inverted envoy hints | For an INCOMING offer both hints are blanked (the client-petition arm's pattern, `mailbox_payloads.py:431-432`); the counter-offer path likewise. The player's OWN previews keep them. Pin: an incoming payload carries no `acceptance_hints`. | `mailbox_payloads.py:317-377`, `diplomatic_executor.py:7355-7362` |
| **LV-9** `(s)` everywhere | `display_names.plural(n, noun)` on the backend (absorbing `emergent_designs._turns` and `diplomatic_dialogue._plural`) and `Utils.plural(n, noun)` in the client, applied at all fourteen sites. A census pin forbids the `(s)` literal in player strings. | the fourteen sites listed on the row |
| **LV-10** whom it was taken from | The capture and field-battle branches print " (was X)" like the movement branch. | `enemy_phase_dialog.gd:336-347`, `:510-524` |
| **LV-11** the advance attrition | `advance_losses` recorded at `combat_executor.py:7802`; the report's strength line and the diorama's lead card show "22,181 → 21,863 after the advance" when it is non-zero. The lock point stays. | `combat_executor.py:7798-7812`, `battle_report.py`, `battle_diorama.py:185-188` |
| **LV-18** the bundled defence term | The defence line prints each component `get_defense_modifier` already computes with its own label ("Defensive stance +15%, Personality (cautious) +5%, Outnumbered +11%"). Pin: the labelled sum equals the applied modifier. | `battle_report.py`, `marshal.py` |
| **LV-19** ASCII arrows | One arrow (`→`) in the materiel and route lines. | `combat_executor.py`, `strategic.py` |

Done when: NPC-12's census lists these sites as CLOSED; every string above is pinned by a driven `/command` test rather than a source grep; zero `BASELINE_SERIES` movement.

**✅ Landing record (September 25, 2026 — the commit that carries this record, with F5 and F6).** Every done-when item is met: NPC-12's census lists the named sites CLOSED (its row keeps `game_logic/combat.py` and `ledger.py` open — see below), every string is pinned by a DRIVEN test (the backend half through the real endpoints, the client half through `tools/ep_f2_display_name_harness.gd`, registered in the parse check), and `BASELINE_SERIES` + M1–M7 are byte-identical for this slice's arm — measured, because the first cut MOVED the series (the shadow bug). Rules `SYSTEMS_REFERENCE.md` §61; pins `tests/test_ep_f2_the_display_name_pass.py` (49); rows disposed in `BUG_FIXES.md` §Live Review (LV-2, LV-3, LV-4, LV-6, LV-9, LV-10, LV-11, LV-18, LV-19) and NPC-12's census note.

- **The shadow bug, found by the series.** With every F5 lever down the series diverged at index 13. Bisected against a clean worktree of HEAD (which reproduces the record) → `combat_executor.py` → the covering-name hunk: a function-local `from backend.display_names import humanize_entity_name` some 1,700 lines further down, inside `_execute_attack`, made the name local to the WHOLE function, so the new covering line raised `UnboundLocalError` on every AI attack against a just-retreated corps. All six local imports of the humaniser removed (the module-level import is the one name); pinned by an AST check that fails on a local import used before its line and on ANY local import of it inside `_execute_attack`; sweep EPF2-5 re-introduces it and is killed. Arm 0 then reproduced the record byte-for-byte.
- **LV-2, as ruled.** `humanize_entity_name` + `formed_display_name` at the scout report, the adjacent scan, the covering/shield lines and the muster header (the header printed the key too — not on the row, fixed); `Utils.display_marshal_name` on the diorama nameplate and the war-table piece label (hitboxes keep the marshal dict); an AST census over `movement_executor.py` and a string census over the covering builder. **Scope recorded:** the cover pin reads the [Shield] line only, because `combat.py`'s narration ("ArchdukeCharles holds the line", "[Terrain] ArchdukeCharles …") prints the key on the same response — that file never imports the humaniser and is NPC-12's next slice, not this one's.
- **LV-3, as ruled — with two corrections found by driving.** "Treaty signed: Peace → Open Borders with the Ottoman Empire."; the downgrade guard had named "France" (an AI offer's `target_nation` is the player — it reads `player_counterpart` now: "We already have Open Borders with the Ottoman Empire. A Peace treaty would be a downgrade."); the failed-ratification prefix opens "The Ottoman Empire's terms could not be ratified: …" (article + capital); `from_nation_display` + `choice_display` on the popup payload, rendered by `main.gd`'s "Responding to" echo.
- **LV-4, as ruled.** `command_display` + int `turns_remaining` stamped once at the pass's return in `strategic.py`; the report messages agree in number; the capture hint is a sentence; `strategic_report_popup.gd` reads them (icons for active/consumed/retired).
- **LV-6, as ruled.** Both hints blank on every incoming envoy payload; the player's own previews keep `build_acceptance_hints`.
- **LV-9, as ruled.** `display_names.plural` (absorbing `emergent_designs._turns` and `diplomatic_dialogue._plural`) and `Utils.plural`, at every backend and client site; census pins forbid the `(s)` hedge on both sides (backend allowlist: `parser_eval.py`, `providers.py`, `settlement_helpers.py`, `formations.py`, `modding/validator.py`).
- **LV-10, as ruled.** Both `enemy_phase_dialog.gd` capture branches print " (was X)" — driven.
- **LV-11, as ruled.** `attacker_advance_losses` / `attacker_after_advance` on the report, `advance_losses` / `after_advance` on the diorama's lead card, "22,181 → 21,863 after the advance" on the terminal Strength line and the lead card; the lock point untouched.
- **LV-18, as ruled.** `battle_report.defense_personality_components` — one labelled row per factor; the product of the rows equals the applied modifier, pinned over every personality × stance × outnumbered × holding cell.
- **LV-19, as ruled.** One arrow; " -> " forbidden by pin.
- **Pins consciously flipped (named):** `test_cr7_4_the_engine_says_what_it_heard` + `test_cr7_9_the_conditions_say_what_they_mean` (the `turn(s)` pins re-stated to agreeing plurals — "for 3 turns", "2 turns remaining", "1 turn remaining", "The agreed 1 turn has passed."); `test_mc_q3_command_rally::TestReviewPassSurfaces` ×5 ("Recovery: 3 turns remaining", "Recovery: 1 turn remaining"); and, found by the hook's full run, eight more readers of the retired forms — `test_ai_intent_assurance` ("Alliance with Russia", the printed state), `test_capture_hint` (the hint sentence, no `[HINT]`), `test_cr7_3_the_tail_comes_back` + `test_fa_s17_phase4b` + `test_fa_slice17_d` + `test_fa_slice17_r` + `test_nui_the_admiralty_on_the_map` (agreeing plurals), `test_iq7_review_round::TestClientSourcePins` + `test_review_2026_08_30` (the client source pins now read `Utils.plural`), `test_iq8_the_harness_tells_the_truth` (`_UNUSED` reads "N actions unused" and still accepts a committed pre-F2 digest).
- **Gates:** ruff clean; parse harness EXIT=0; the F2 harness boots the real client with 0 `SCRIPT ERROR`; sweep `tools/_sweep_ep_f2_f5_f6.json` (33 mutations across F2/F5/F6, 33 killed, 0 INERT at close, 0 BROKEN — the one first-pass INERT was F5's cap lever, see F5); series + M1–M7 byte-identical for this slice.

### F3 — "The client layout pass" (0.75) — LV-5, LV-14(b), LV-15, LV-16, LV-20, LV-22, LV-D3 — ✅ LANDED September 24, 2026 (landing record below the table)

| Row | Decision | Seam |
|---|---|---|
| **LV-5** the clipped petition | The body label sizes to `get_content_height()` and is marked `relax_last`, so `clamp_centered_panel` shrinks the options list before the body; a disabled arm's reason renders **above the fold** as one line under the header ("The command arm is closed: no enemy within his reach") — IQ10-X2's pattern — and the duplicate `detail == reason` line is dropped. This modal joins the IQ-10 capture set at both Interface Scales. | `marshal_petition_dialog.tscn/.gd`, `utils.gd:595-661`, `tools/iq10_capture_payloads.py` |
| **LV-14(b)** the settlement overlap | The per-court Press/Ease/Drop rows become their own scroll block under the "Allies and Standing" heading; the third court is never clipped. Shot through the IGR-G harness with the turn-6 payload (Vienna held, three courts). | `settlement_review` panel, `tools/settlement_popup_screenshot.gd` |
| **LV-15** the wizard's chips | Chip labels autowrap; the gate reason moves to a second, smaller line; the horizontal scrollbar goes. Step 2 joins the IQ-10 capture set. | `diplomacy_wizard.gd` |
| **LV-16** the log's letters | The glyph font is bound on `campaign_log.gd`'s row prefix (the letters are the fallback); pinned by the IQ-10 frame. | `campaign_log.gd` |
| **LV-20** the wizard's gap + one chip | The prompt sits directly above the list (the spacer was the old nation-count block); "Sponsor Their Design" for a court whose design is against France reads "Fund the design they already pursue against us" and is hidden while at war with that court. | `diplomacy_wizard.gd` |
| **LV-22** the capture swallows the report (filed by F1) | The rule F1 applied to the envoy family, applied to the one route a command DOES ask for that also discards a result: `_show_capture_choice_dialog` prints `response.message` only, so a battle that ends in a capture loses Berthier's report; the muster road prints the message twice. The capture route renders the order's result first (`_render_own_result`) and its renderer skips the message a rendered result already printed; the muster road's second print goes. Driven like F1's LV-12 pin, on an attack that captures. | `main.gd` `_route_capture_choice_response` / `_show_capture_choice_dialog` / `_on_interrupt_response` |
| **LV-D3** the recap modal | `strategic_report_popup` is no longer raised at turn start. Its rows render inside the morning dispatch's MARSHAL STATUS ("Soult — marching to Vienna, arrives next turn"), and the popup class is kept only for reports that carry a question (an interrupt) — those already route through their own dialogs. The end-turn response drops `strategic_report` from the popup whitelist and adds it to the dispatch payload. | `main.gd` popup whitelist, `dispatch.py`, `strategic.py:1294` |

Done when: the four IQ-10 frames at 1.0 and 2.0 show no clipped text; a turn with three standing orders and no interrupt raises no modal at turn start; parse harness EXIT=0, boot 0 `SCRIPT ERROR`.

**✅ Landing record (September 24, 2026 — the commit that carries this record; taken ahead of F2 by the user's direction, with F4).** Every done-when item is met: the six IQ-10 frames are clean at both Interface Scales (0 buttons off the viewport, 0 labels taller than their box, 0 `SCRIPT ERROR`), a turn with a standing order and no interrupt raises no modal at turn start (driven), parse harness EXIT=0 over 56 scripts, the real `main.tscn` booted and driven with 0 `SCRIPT ERROR`. Rules `SYSTEMS_REFERENCE.md` §60; pins `tests/test_ep_f3_the_client_layout_pass.py` (36, the client classes driven through `tools/ep_f3_client_layout_harness.gd`, registered in the parse check); rows disposed in `BUG_FIXES.md` §Live Review (LV-5, LV-14(b), LV-15, LV-16, LV-20, LV-22) and `DESIGN_REFINEMENT.md` (LV-D3 built).

- **LV-5, as ruled.** `GateLabel` under the header carries "<arm> is closed — <reason>" for every closed arm (an arm shut only by the purse names its price); the body sizes to `get_content_height()` one frame after layout (`_fit_body`, bounded 120–360) and is `relax_last`, so the options list yields first; the duplicate `detail == reason` line is dropped. Frame `docs/audits/IQ10_PETITION_COMMAND_CLOSED_2026_09_24.png` (+`_X2`): "Give him a command is closed — There is no enemy within his reach to send him against." under the header, the whole body above the fold. Staged as the review saw it — Murat sent to Brittany, where no enemy is in reach (the boot board hands every French marshal a quarry).
- **LV-14(b), as ruled.** The Press/Ease/Drop chips are ONE ROW PER COURT (the court's name, then its chips; a last row for coverage suggestions) in a `VBoxContainer`, the rail's floor derived per open from the row count (42px a row, capped at 30% of the logical viewport); the three-column grid that folded the third court under a fixed 96px floor is gone. Frame `IQ10_SETTLEMENT_THREE_COURTS_2026_09_24.png`: Austria, Britain and Russia each on their own row, none clipped, the block under "Allies and Standing". Shot through the IQ-10 harness (the generalisation of IGR-G's) on a staged three-court draft with Vienna held. Observed and left: at 1600×900 the header region's 72px floor shows three and a half lines of the carry verdict (the live 2560-wide monitor gives it room); not this row's. The (a) half — the legitimacy sentence — stays with F5.
- **LV-15, as ruled.** `Button.autowrap_mode = AUTOWRAP_WORD_SMART` (a 4.4 property) with the chip filling the width; the gate reason is its own 11px line under the chip (and the chip's tooltip); the list's horizontal scroll is disabled. Frames `IQ10_WIZARD_STEP2_AUSTRIA` / `_PRUSSIA` (+`_X2`): "Buy Off Their Design (1320g) … the strongest casus belli in Europe." wrapped inside the panel, no sideways scrollbar.
- **LV-16, as ruled.** The row prefix is the category's phosphor glyph (sword / flag / coins / scroll / handshake) tinted the category colour through `Utils.bb_icon`; the letters are the fallback only when a glyph is missing on disk. Frame `IQ10_CAMPAIGN_LOG_GLYPHS_2026_09_24.png`.
- **LV-20, as ruled.** `_lay_out_prompt(1)` pins the step-1 prompt to one line (SIZE_FILL, 40px) so the list sits directly under it; steps 2 and 3 expand as before. The Sponsor chip's copy for a court whose design is aimed at France reads "Fund the design they already pursue against us — a bribe, not a purchase." and the chip is not offered while at war with that court (Britain at boot; Austria too). The wizard gained `refit()` — the live clamp, public — so a surface rendered OFFLINE fits the way the live wizard does. Frame `IQ10_WIZARD_STEP1_2026_09_24.png`.
- **LV-22, as ruled — and driven.** The capture route is `result_first`; `_display_result` stamps `_result_rendered` on the response it prints and `_show_capture_choice_dialog` prints the message only when nothing has; the muster road (`_on_interrupt_response` renders, then routes) inherits the same rule, so its second print is gone. Driven on the real client: an attack that captures prints "--- Berthier's Report ---" once and the message once, then "Plunder or Secure?", on the command path AND the muster road. The staging had to learn two facts the row did not know: a corps destroyed on an ALLY's province takes nothing (Mack stands on Bavaria's soil), and a destroyed remnant is reinforced from the ADJACENT province (Archduke Charles at Carniola marched in with 54,000) — John was cut to 600 on Austrian Tyrol with every other Austrian corps sent to Vienna.
- **LV-D3, as ruled (D13).** `_show_strategic_reports` prints the terminal block and continues straight to `_on_strategic_report_dismissed` — no modal; an order that needs an ANSWER still raises its own interrupt popup from there. The dispatch's MARSHAL STATUS carries each order's reading from ONE arithmetic: `strategic.order_turns_remaining` / `order_eta_phrase` ("Moving to Vienna — 2 turns out.", "— arrives next turn", "— holds 2 more turns", the pursuit and support variants), and the strategic report is re-pointed to the same function (byte-identical values). `strategic_report_popup` stays registered and is never raised at turn start. Driven: after the enemy phase the recap never stands, the terminal shows the block and then MARSHAL STATUS with the row's own note, and the command line comes back unless a stashed diorama or petition legitimately took the flow.
- **Pins consciously flipped (named):** `test_ep_f1::TestTheRouteTable` — `RESULT_FIRST` gains `capture_choice` (the one asked-for modal that still waits behind its result); `test_ui_visual_foundation::test_settlement_tier2_buttons_have_their_own_bounded_rail` — the chips mount as rows inside the scrolled container; `test_ui_visual_foundation::test_both_wizard_open_paths_clamp` — re-stated from "the clamp literal appears twice" to "once, in `refit()`, which both open paths call" (the clamp lives in one place since F3). §9's "strategic-report popup whitelist pin" did not exist (the only pins on that scene are the clamp-call and the keyboard-dismiss pins, which hold); recorded.
- **Gates:** ruff clean; parse harness EXIT=0 (56 scripts, 7 scenes); the IQ-10 group `layout_f3` + six runner rows (`tools/iq10_capture_payloads.py`, `tools/iq10_run_captures.py --payload-dir <dir> --only petition_command_closed,wizard_step,settlement_three_courts,campaign_log_glyphs --date 2026_09_24`); sweep `tools/_sweep_ep_f3_f4.json`: 31 mutations, 31 killed, 0 INERT, 0 BROKEN; `BASELINE_SERIES` + M1–M7 byte-identical (F3 touches no mechanic; the ETA arithmetic is the report's own). ⚠ open for the user: the in-game feel of the six surfaces (the frames are evidence, not sign-off).

### F4 — "The fuse is longer" (0.5) — LV-21 / LV-D1 (the reward curve; UX23-D1..D4 closed by this ruling) — ✅ LANDED September 24, 2026 (landing record below the bullets)

- **Rises.** `expectation` rises only on (a) a decisive victory with the marshal as LEAD, or (b) a rise in his glory RANK; never on a reinforcement, never on a stalemate. At most one rise per marshal per **4 turns** (`EXPECTATION_RISE_COOLDOWN = 4`, serialized `last_expectation_rise_turn` — one new marshal field, `to_dict`/`from_dict`, `SAVE_FORMAT_REFERENCE`). No rise before turn **6**.
- **The collective petition** (ESP-1 Fontainebleau) needs turn ≥ **12**, ≥ 3 eroding marshals and ≥ **300g** unmet (all in-band constants).
- **The dispatch's UNMET MARSHALS block** appears only for a marshal within **2 turns** of erosion; the per-victory "Ney's victories raise his expectation" line stays (it is the tell).
- **GR5:** the AI grant rung reads the same predicates, so `BASELINE_SERIES` moves — re-recorded once with a four-arm attribution (rises / cooldown / petition gate / dispatch gate).
- Measured before landing on the historical seed, played as in the live review (Ulm turn 1, Vienna turn 6): the first expectation rise on or after turn 6, no collective petition before turn 12. Re-open: if a 40-turn commanded campaign never sees a collective petition, lower the gate to turn 9.

**✅ Landing record (September 24, 2026 — the commit that carries this record; taken ahead of F2 by the user's direction, with F3).** Built as ruled, one deviation recorded. Rules `SYSTEMS_REFERENCE.md` §59; pins `tests/test_ep_f4_the_fuse_is_longer.py` (34); rows disposed in `BUG_FIXES.md` §Live Review (LV-21) and `DESIGN_REFINEMENT.md` (LV-D1 built; UX23-D1..D4 CLOSED by this ruling).

- **The rises, as ruled.** `dotation.raise_expectation` is the ONE write; both deeds reach it. (a) The decisive victory as LEAD is decided at the post-combat tail every attack passes — the player's and the AI's alike (GR5) — for `marshal` / `enemy_marshal`, never a participant: `dotation.is_decisive_victory` = the beaten corps broken or destroyed outright (`attacker_victory` / `defender_victory`), or its commander taken or gone from the field by the time the pipeline reaches the seam (the destruction sweep can turn a tactical outcome into a kill), or the war score's own decisive exchange — which became a named predicate, `battle_scale.is_decisive_exchange` (above 10,000 dead, one side bleeding more than 2:1), read by `record_battle` too. Garrison assaults never reach the tail, so a stomp raises nothing (the exemption the glory ladder keeps, CA8-19). (b) The rank rise: `dotation.observe_glory_rank`, called from the jealousy pass after the crowns for every nation's ladder; the first observation is silent; a rank handed over by a rival's decay or capture is not a deed — the man's own positive accrual that turn is required. Cooldown `EXPECTATION_RISE_COOLDOWN = 4`, floor `EXPECTATION_FIRST_TURN = 6`, the cap unchanged.
- **The deviation: THREE serialized fields, not one.** `expectation_steps` (the count cannot stay on `battles_won`, which the ledger and the Generals card print as his RECORD, and a record must not lie to lengthen a fuse; `battles_won` still ratchets for every other reader), `last_expectation_rise_turn` (the cooldown) and `glory_rank_seen` (a rank memory has no exact derivation once the eight-turn glory window prunes). A pre-F4 save backfills `expectation_steps` from `battles_won` at load, so nobody's claim vanishes.
- **The petition and the block.** `jealousy.check_fontainebleau` waits for turn ≥ `FONTAINEBLEAU_MIN_TURN` (12) and ≥ `FONTAINEBLEAU_MIN_UNMET_GOLD` (300) across the petitioners, checked AFTER the re-arm so a count that fell during the wait still re-arms; `dotation.build_unmet_marshals` names a man only within `UNMET_BLOCK_WINDOW_TURNS` (2) of erosion or already eroding (a captive, whose expectations are frozen, is never an alarm). The rail row still opens with the shortfall and counts the whole window; the per-victory "raises his expectation" line stays.
- **Measured, the plan's own case.** The historical seed played as in the live review (`tools/playtest_driver.py --script tools/playtest_scripts/flagship_1805.json --llm mock --turns 14`, Ulm on turn 1 with Mack's 10,986 dead): the first rise is **Lannes on turn 7** (Archduke John taken), the second Massena on turn 11, the first grace clocks open on turns 7 and 8, and **no collective petition fires in 14 turns** — against the review's expectations from turn 1, the UNMET block from turn 2 and the petition on turn 7. What a tactical victory is worth was measured too: a 52,000-man corps attacking 900 men produces 136 to 467 casualties with both corps standing (rightly no deed); a 60-man remnant is destroyed outright.
- **The series: byte-identical, with the reason measured — no re-record.** D15 said F4 "moves AI behaviour and re-records once". A SEVEN-arm attribution (`tools/_f4_series_arms.py`: all four levers down; the deed rule alone; + the cooldown; + the petition gate; the full tree; the petition gate alone; the dispatch gate alone) reproduces the recorded `BASELINE_SERIES` byte-for-byte on every arm. `tools/_f4_ambient_reason.py` says why: under the old curve the ambient 40-turn board produced **8 AI grants** (Austria endowing Charles and John on turn 4, rentes on turn 7, Spain's on turn 10, three estates on turns 26–28) and **4 collective petitions**; under the new curve **0 and 0**, with six marshals rising by deed from turn 6 (Massena 2, Lannes, Bernadotte, Charles, Castanos, Paget 1) — and none of that reaches a threat-bearing decision inside 40 turns. The machinery moved a great deal; the alarm series carries none of it. M1–M7 byte-identical.
- **Pins consciously flipped (named):** 111 fixture assignments across 22 test files rewritten from `battles_won = N` to `expectation_steps = N` (the record no longer prices the claim; assertions on the record itself untouched); `test_estate_second_pass::test_note_fires_on_any_battles_won_increment` → `test_note_fires_on_a_decisive_rise` (the deed on turn 6) and its cap sibling; the ESP-1 fixtures placed at turn 12; `test_fa_slice17_h` FA-N53 — a promise that just bought seven turns of patience takes the man OFF the alarm block (the rows say the number with the lever down); `test_ux23b` — a mid-turn shortfall is the rail's row until the window is down to two; `test_ux23a::test_the_combat_seam_actually_calls_it` follows the renamed local; `test_wo_slice10_enemy_direction_gate` — its UNGATED-board measurements re-recorded (40+7 → 39+6 collisions; `Guyenne → Ney`'s four orders gone, `Gascony → Ney` 14 → 18, `Bern → Brunswick` 14 → 12; the cooldown count 40 → 39; the GATED board — the standing `BASELINE_SERIES` — unchanged), the tenth re-measure in that file's own history, **attributed**: with the four F4 levers down in the child the old figures return exactly (measured through the file's own `_spawn`) — the deed-keyed curve takes the AI's ambient grants from eight to none, which re-times one attack on the ungated trajectory.
- **Levers (the four arms):** `dotation.EXPECTATION_RISES_ON_DEEDS`, `dotation.EXPECTATION_RISE_COOLDOWN_ACTIVE`, `jealousy.THE_COLLECTIVE_PETITION_WAITS`, `dotation.THE_UNMET_BLOCK_WAITS`; all four down is the pre-F4 game byte-for-byte. Sweep `tools/_sweep_ep_f3_f4.json`: 31 mutations, 31 killed, 0 INERT, 0 BROKEN. Re-open condition stands as written (a 40-turn commanded campaign that never sees a collective petition lowers the gate to turn 9).

### F5 — "Bohemia is not empty" (0.5) — LV-D2 (FA-D13 re-opened) + LV-14(a) / LV-D4 — ✅ LANDED September 25, 2026 (landing record below the bullets)

- **Walk-ins.** `_find_undefended_capture` honours a per-corps per-turn cap of `movement_range` captures (one for infantry); the AI's action loop cannot hand a second walk-in to a corps that already took one this turn. A "literal" marshal runs the "cautious" strength check (`enemy_ai.py:5502-5513`) before entering a province adjacent to an enemy corps stronger than his own. **Measured on three seeds:** Bavaria takes at most ONE Austrian province on turn 1, and the Austrian REVANCHE against Bavaria fires on turn 1 on zero of three. `BASELINE_SERIES` re-recorded once, attributed (the cap alone; the strength check alone; both).
- **The legitimacy sentence.** The whole-war blocker's copy names the courts: "London and St Petersburg are unbeaten — a whole-war peace needs their consent (Austria 32/50, Britain 24/50, Russia 24/50). Press Austria alone: the separate peace." The review panel gains a **"Separate peace with <court>"** chip for any covered court at ≥ 50, routed to the existing pair-substitute tier. The predicate is unchanged; the counsel rung says the same thing. Pin: the turn-6 payload renders the named sentence and the chip.

**✅ Landing record (September 25, 2026 — the commit that carries this record, with F2 and F6).** Rules `SYSTEMS_REFERENCE.md` §62; pins `tests/test_ep_f5_bohemia_is_not_empty.py` (28); attribution `tools/_f5_series_arms.py` (+`.json`) — the F4 idiom, levers set IN THE CHILD; rows disposed in `BUG_FIXES.md` §Live Review (LV-14(a)) and `DESIGN_REFINEMENT.md` (LV-D2 built; LV-D4's F5 half built, the GE-3 half stays with the ending).

- **The walk-ins, as ruled.** Cap = `movement_range` walk-ins per corps per phase (`EnemyAI._walk_ins_this_turn`, reset per nation, counted by the action loop off the rung's `walk_in` tag); a literal corps runs the cautious 1.5× counter-attack check (`_evaluate_capture_safety`; the cautious arm byte-identical). Levers `enemy_ai.ONE_WALK_IN_PER_CORPS_PER_TURN` / `enemy_ai.THE_LITERAL_TAKES_THE_CAUTIOUS_STRENGTH_CHECK`; both down = the pre-F5 rung byte-for-byte. **Measured on three seeds:** uncapped, the historical boot hands Deroy Bohemia AND Carniola in one phase and Austria promotes Revanche against Bavaria the same turn; with the levers up Bavaria takes at most ONE Austrian province on turn 1 on historical / ulm / austerlitz (Bohemia falls on turn 3 of the historical run) and the Revanche fires on none (ulm and austerlitz took none either way). On the boot board the literal check refuses Deroy Bohemia outright — 74,000 against 22,000, Charles one march away — which is why the cap's own lever-free pin is staged on a board where the check passes (every Austrian corps far from the frontier and cut to a remnant: two provinces uncapped, one capped).
- **The series: re-recorded ONCE, four-arm attributed.** Arm 0 (both levers down) reproduces the prior record byte-for-byte; the cap alone diverges at index 7, the check alone at index 13, both (the shipped tree) at 7. The recorded run's board at turn 40: the passive France holds 4 provinces (was 9), Bavaria 5 (was 3), Britain 18 (was 24) — a fact about the unattended harness, not a balance claim; F2 and F6 contribute nothing to it (the shadow bug was a defect fixed BEFORE the record was taken, not attributed). Downstream pins re-measured with the files' OWN runners and attributed: `test_wo_slice9_the_courting_cap` (uncapped 12 / capped 15 courting events; the elimination step −12 is now the step from index 6 to 7 — the first cut wrote 7 and read −3, the pin's `steps[i]` is the LOWER index; the capped exit −13 at 13 → 14, rebellion turn 15) and `test_wo_slice10_enemy_direction_gate` (seams `_fuzzy_match_enemy` 63; the collision pairs Leon→Napoleon 7, Gascony→Ney 24, Champagne→Ney 31, Brittany→Ney 1; the ungated-board series; cooldowns ungated 75 / gated 4 — `tools/_f5_wo10_attribution.py`: with the two F5 levers down the prior figures return exactly).
- **The legitimacy sentence and the chip, as ruled — with two corrections found by driving.** (1) "Unbeaten" is the WAR's word, not the table's: the first cut listed Vienna as unbeaten while France HELD it (below the threshold ≠ unbeaten); a holdout is unbeaten only while the pair war score from our side is not positive, and a court we hold the capital of is the one to press alone. (2) `world.war_scores` is keyed alphabetically (`Austria|France`) and holds the FIRST court's view, and `get_war_score_for` flips the sign for the second — so a staging that wants France to READ +58 writes −58; the first probe wrote +58 and read −58 (the F3 capture group carries the correct write now, and the F5 tests assert the read after the write). The sentence: "London and Vilna are unbeaten — a whole-war peace needs their consent (Austria 12/50, Britain 4/50, Russia 4/50). Press Austria alone: the separate peace." on the blocker's heading, both the white-peace and the generic arm. The chip: `settlement_staging.separate_peace_chip` on a covered court's row at ≥ threshold with no hard stop, `seek_bilateral_peace` with ITS court in the structured params (`_handle_pair_peace_substitute_action` honours a COVERED court, a foreign one falls back to the table's); `proposal_confirm_popup.gd` honours `available` / `disabled_reason_display`. **The chip takes the dials' own gate** — the authoring table's per-court rows (PROPOSE mode, the player's editor caller), never a frozen REVIEW row and never an observer table (the hook's full run found it on both: `test_settlement_guided_terms_slice3` and `test_settlement_refront_slice2` are the standing rules, kept). **No shipped board puts a covered court at 50 on the review's staging** (Austria reads 31 with Vienna held, Mack captive and the war score at 100; 12 on the F3 staging), so the chip's gate is pinned through the scorer's stable seam (`settlement_scoring.calculate_common_peace_acceptance` lifted to 70 for Austria → the chip on Austria's row and none on Britain's or Russia's, the sentence naming Austria to press alone) and the routing is pinned by sending the Britain chip's params from a table aimed at Austria (`settlement_pair_substitute_confirm` for Britain, "to treat with Britain alone"). Frame `docs/audits/IQ10_SETTLEMENT_THREE_COURTS_2026_09_25.png` (+`_X2`): the sentence on the header's first two lines, 0 buttons off the viewport, 0 clipped labels, 0 `SCRIPT ERROR` — the F3 capture `settlement_three_courts` extended with the war-score write (`tools/iq10_capture_payloads.py --only layout_f3`, then the runner `--only settlement_three_courts --date 2026_09_25`).
- **Pins consciously flipped (named):** `test_ai_intent_threat_migration::BASELINE_SERIES` (re-recorded, attributed above); `test_wo_slice9_the_courting_cap` (12/15, the elimination index, the exit turn); `test_wo_slice10_enemy_direction_gate` (the ungated-board measurements and series; the gated board IS the standing series); and `test_iq7_review_round::TestR7TheCardTellsTheTruth::test_the_countdown_and_may_petition_are_kept_through_real_end_turns`, found by the hook's full run — its second petition (turn 15) had been THE RELIEF arm firing on a FALLING loyalty forecast the pre-F5 ambient board happened to supply at turn 14 (Bern adjoins only French homeland, so THE PROVINCE arm can never fire for the Swiss); with the walk-ins capped the card read "nothing to ask for" — measured in both lever arms through the test's own loop — so the precondition is now STAGED (a soured lord relation at turn 13) instead of borrowed from the AI board, and the pin reads `{6, 15}` in both arms.
- **Gates:** sweep 33/33 at close — the cap lever was INERT on the first pass because every boot-board pin was masked by the literal check refusing Deroy, and the lever-free check-passes pin kills it now; M1–M7 byte-identical without re-record; ruff clean; parse harness EXIT=0 (the chip renderer's `.gd` line); the three-seed measurement is a standing pin (`TestThreeSeeds`).

### F6 — "Settled once, reopened" (0.25) — LV-17 — ✅ LANDED September 25, 2026 (landing record below the bullet)

- Reproduce turn 5 of the historical seed (two attacks on Archduke John; Davout the reinforcer). If the confrontation was generated by the end-of-turn pass from a `jealous_of` the battle had already resolved, the pass re-reads the relationship at **delivery** (the IGR-2 re-price pattern) and drops a petition whose grievance no longer stands. If it is the ladder's next rung, the petition's first line says so: "Settled once at Vienna — and reopened by …". Either way the two lines cannot contradict on consecutive turns; a pin drives the reproduction.

**✅ Landing record (September 25, 2026 — the commit that carries this record, with F2 and F5).** Rules `SYSTEMS_REFERENCE.md` §63; pins `tests/test_ep_f6_settled_once_reopened.py` (10, the reproduction driven through `/command`); row disposed in `BUG_FIXES.md` §Live Review (LV-17). Zero series movement — display and delivery only; the F5 record was taken on the tree that includes F6, and M1–M7 are byte-identical.

- **The reproduction, and what it found.** The review's exact turn-5 board did not reproduce on the mock driver (`tools/playtest_scripts/ep_f6_lv17_repro.json` + `tools/_f6_lv17_repro.py`: the live-parser board diverged and Davout never fired there), so the mechanism was traced instead: the battle resolves the grievance by action (pipeline step 9.5, `clear_jealousy(resolved_by_action=True)`), and the end-of-turn pass's same-pass suppression (`cooled_this_pass`) remembers only the coolings IT performed, so the same pair re-fires off the rival's fresh laurels the same turn. Both lines were true of DIFFERENT grievances; the trigger is legal and untouched. It is the ladder's next rung — the ruling's second arm; the first arm (a card whose grievance no longer stands is dropped at delivery) was already FA-S17-D4's `petition_is_still_live`.
- **Built.** The card built within `SETTLED_ONCE_WINDOW_TURNS` (1) of a by-action settlement of the same pair opens "Settled once at Tyrol — and reopened by Ney's laurels since." (`jealousy.settled_once_clause`, read off the `jealousy_resolved` event's own `location`, which the battle-time resolver stamps — **all three** `check_battle_resolution` seams: the pipeline, `_execute_attack`, the reckless charge in `world_state`; the first cut patched one and the driven test read an empty field off another, so an AST census over every call site pins the keyword); a timer expiry is not a settlement; a stale undelivered card for the pair is REPLACED (`_retire_pending_petition` clears the world slot and the PopupQueue type) rather than handed over by the latch, and another pair's card is never evicted. Lever `jealousy.THE_REOPENED_QUARREL_SAYS_SO`. No new serialized field (a key on an existing event type; `SAVE_FORMAT_REFERENCE.md` notes it).
- **The staging trap, recorded for the next pin.** A cautious man with a live grievance WITHHOLDS from his rival's battle (the jealousy v3.2 cautious expression), so "Ney attacks with Davout co-located" never puts Davout on the winning side and nothing resolves; the pin stages Davout ATTACKING Archduke John (600 at Tyrol, every other Austrian corps at Vienna) from Munich with Ney devoted (+2, the arrival roll cannot miss) and reinforcing — the shoulder-to-shoulder resolution, "His grievance is settled." on the report, `location == "Tyrol"` on the event, then the re-fire's card opening with the clause.
- **Gates:** sweep EPF6-1..8 all killed (lever, window, the stale-card replacement, the clause, the field at the resolver and on the event, the timer arm, the other-pair guard); ruff clean; zero `.gd`.

---

## §2 The ending — "THE CONGRESS OF PARIS" (RULED)

### §2.1 The shape

Napoleon's problem was never conquest; it was that Europe would not *recognize* the conquest. Amiens lasted a year, Pressburg was undone at Wagram, Tilsit was undone at Moscow. The ending is that problem made playable: **the Emperor must force a new status quo and then make Europe sign it, and hold everything while Europe decides.**

1. **Title.** Conquests count only when settled (§2.2).
2. **The summons.** With 50 titled provinces in the bloc the player may **summon the Congress of Paris** — a deliberate act, priced, and reversible only by failure.
3. **The table.** Every great power answers — *recognizes* or *refuses* — and keeps answering every turn, with its reason and its price on a public table.
4. **The sitting.** The Congress sits for **8 turns**. Refusal has teeth; recognition has a price; the marshals and the satellites present their bills; every titled province, Paris and the Emperor's freedom must be held.
5. **The Imperial Peace.** If, on the eighth turn, every great power recognizes, is shut out or is gone, and the hold never broke — the ending. Otherwise the Congress dissolves, at a cost, and can be summoned again later.

### §2.2 Title — the stabilization of the new status quo (RULED as `GAME_END_SPEC.md` §7.2, with the rider)

A province in the French bloc is **titled** when it is: **homeland** (in `nation_starting_regions` for France or for a satellite that is loyal ≥ 40); **ceded by treaty** (a `territory_cede` / settlement clause the loser signed — title is immediate); **held in quiet possession** (captured by force and held `title_turns` = **12** consecutive turns with no hostile army entering it and the loser not at war with France); or **a client's soil** (a court France created). Everything else is *held*, shown, and not counted.

**The record:** ONE serialized field, `province_title: {region: {"kind": "treaty"|"conquest", "since": turn, "from": nation}}`, written at `capture_region` (conquest) and the three ratify seams (treaty); a hostile army entering a `conquest` province resets `since`. GR5: written for every nation, read only for the player's ending. `Region` is not changed.

**Reconciliation (RULED in):** a court that signs a cession has that province's Revanche weight read 0 while the treaty holds; breaking the treaty re-arms it. Zero new fields.

### §2.3 The summons

- **Verb:** `summon the congress` (typed) and a **"The Congress of Paris"** row at the top of the Diplomacy wizard's step 1 with honest-availability gate terms: `50 titled provinces (43 of 50)` · `Paris held` · `the Emperor free` · `no satellite in rebellion` · `no Congress dissolved within 10 turns`. Cost **2 DP + 1 admin action**. Not a standing order; it cannot be undone.
- **What happens:** `world.congress = {"summoned_turn": t, "ends_turn": t+8, "answers": {...}, "held": true}` — ONE serialized field. A dispatch beat and a Moniteur special ("THE EMPEROR SUMMONS THE POWERS TO PARIS"). The marshals' collective petition fires **at the summons** if any marshal is eroding ("the peace dividend": estates before the peace); every satellite with a pending ask presents it.
- **The great powers** are `_CANONICAL_MAJORS` minus France (Britain, Russia, Austria, Prussia). A great power that is eliminated or a French vassal is counted as recognizing by construction.

### §2.4 The table — how a court answers, every turn

Each great power's stance is **re-derived every turn** of the sitting from `congress.answer(world, court)`:

- **at WAR with France** → *refuses*, unless its war score against France is ≤ **−40** (then it *sues*: the existing P1 sue seam generates its peace offer, which now carries recognition; ratifying it flips the court to *recognizes*). A refuser whose capital France holds sues at once.
- **at peace** → the acceptance formula for a new proposal type `recognition`, threshold **50**: base = relation; `agenda_acceptance_mod` (a deny design unsatisfied −12, an acquire design targeting French-held soil −12, a satisfied or bought-off design +12); hegemony fear −(bloc share − the court's `share_floor`) × 100 (Russia's `arbiter_of_europe` bites here); war weariness ≥ 60 → +10; beaten by France within 15 turns → +10; an alliance +20, a defensive alliance +10, a non-aggression pact or open borders +5; a **sweetener** the player attaches (gold: +10 per 1,000g, cap +20 — a new `recognition_sweetener` clause on the existing instrument transport); seeded jitter ±3 through the campaign-seed helpers. ≥ 50 → *recognizes*, else *refuses*.
- **Britain** answers like the others and additionally reads *shut out* (not blocking) when the Continental System closure is ≥ **60%** for every turn of the sitting and no British corps stands on the Continent. Talleyrand names it: "London will not sign; London need not — close the ports and she is a spectator."
- A recognizer **flips back to refusing** if France declares a new war, takes a province by force, or denies its design further (annexes soil it covets) during the sitting.
- *As built (GE-3 review round, §6): the treaty latch holds only for a peace signed WHILE the Congress sits or a BEATEN court's peace (it ceded provinces to the Emperor's bloc in that treaty) — any other peace feeds the formula, and a truce that runs out never signs; only the Emperor's own capture flips a court back, and only a court that was signing (never one at war or in a truce) withdraws; the war score is printed from the Emperor's side ("our war score +30 — it sues at +40").*

The table (a new **CONGRESS** tab on the Diplomatic Ledger, and the same rows in the war room) shows for each court: the flag, the stance (RECOGNIZES / REFUSES / SUES / SHUT OUT / GONE), the reason sentence from the formula's top negative term, and **the price** — the cheapest lever that would flip it, derived from the same terms: "Vienna: Savoy by right — buy the design (300g/turn), or beat Charles (war score −40)"; "London: 4 more ports of 26 shut, or 12,000g"; "St Petersburg: our share frightens the arbiter — a guarantee to Sweden, or Finland to the Tsar"; "Berlin: Hanover — cede it to Prussia's client, or 6,000g". The price is display-only counsel; it is never a bargain the court is bound to.

### §2.5 The sitting — the challenge

While the Congress sits (turns `summoned+1 … summoned+8`; *as built, the peace dividend on the rentes runs from the summons' own end turn — nine charged turns, priced on the summons; the petitions' ×2 stakes from day 1*):

- **Refusal has teeth.** Each refuser's intent weight against France rises **+15 a turn** (the AI-3 ladder climbs to `fight`; beat 2 fore-warns; a declaration follows within 3–4 turns unless it recognizes) — the "War of the Congress". A refusing Britain sponsors every other refuser at **+200g/turn** (the paymaster generalized). The coalition alarm's formation gate drops from 60 to **40** while any two great powers refuse. Refusers' Revanche designs get +weight.
- **The hold.** Every turn ALL of: titled ≥ 50 · Paris held · the Emperor not a prisoner · no satellite in rebellion · no titled province lost (a titled province taken and retaken the same turn still breaks it) · **France has declared no war since the summons** ("the Emperor who summons the Congress and then draws the sword has answered for Europe") · alarm < 80. A broken hold dissolves the Congress that turn.
- **The bills.** The marshals' collective petition at the summons (§2.3); during the sitting the reward rail's rows are priced ×1.5 (the "peace dividend"); an eroded marshal's refusal to march is likelier (existing trust arms). Satellites: a client petition during the sitting carries double loyalty stakes; a rebellion dissolves the Congress. These are the existing systems with the Congress's multiplier, so the player is juggling four fronts with the scoreboard in view.
- **Counter-play, all existing verbs:** beat a refuser to −40 (or take its capital); buy its design (`buy_off_design`); pay its price (the sweetener); court it (missions, `improve relations`); guarantee a third party (`guarantee_nation`) to satisfy an arbiter; cede a titled province to its client by settlement (title moves, the count drops — a real trade); close ports for Britain (the CS surface); create a client that satisfies a deny design (the formables).
- **Narration.** Beats: 1 the summons; 2 a refuser's fore-warning/declaration ("Vienna answers the Congress with cannon"); 3 a recognition won ("Berlin signs"); 4 the eighth turn. The Moniteur runs a *Congress column* every turn of the sitting; Talleyrand's rung names the biggest blocker and its price; the clock line (`THE CONGRESS SITS — turn 3 of 8 · 50 of 50 titled · Austria REFUSES (Savoy) · London SHUT OUT · Berlin, St Petersburg RECOGNIZE`) rides the war room, the ledger's Territories tab and the end-turn banner from ONE source, `congress.state_line(world)`.

### §2.6 Resolution

- **Eighth turn, hold intact, every great power recognizing / shut out / gone → THE IMPERIAL PEACE.** `record_ending(world, "victory", "imperial_peace")`; the gold register of the end scene (§4); the Moniteur's final special; **Continue the reign** or **Retire to the Tuileries**. Stamped once; a second Congress can never fire it again.
- **Otherwise → the Congress dissolves.** Alarm **+15**; a **10-turn** cooldown before another summons; each refuser gains a 10-turn grudge (the `agenda_grudge` contributor, +1 threat, cap 2, existing); the marshals' expectation rises by one rung (they were promised the peace); a dispatch beat names the courts that would not sign. Not a defeat (D8).
- **E1, the Universal Monarchy:** if no great power stands (all eliminated or French vassals), the Imperial Peace fires **without a Congress** on the first end turn — nobody is left to contest the order. *(As built: the first end turn with the capital held and the Emperor free.)*

### §2.7 Numbers (all in `campaign_end`, all in-band tunable; the SHAPE is the ruling)

`hold_titled: 45` *(50 until GE-V, September 25, 2026 — §2.8's own rule fired on the measured reach)* · `congress_turns: 8` · `title_turns: 12` · `recognition_threshold: 50` · `refuser_weight_per_turn: 15` · `congress_alarm_gate: 40` · `hold_alarm_ceiling: 80` · `cs_shutout: 0.60` · `sue_score: -40` · `sweetener_per_1000: 10 (cap 20)` · `dissolve_alarm: 15` · `congress_cooldown: 10` · `verdict_turn: 44`.

### §2.8 Measurement before landing (GE-V)

- The commanded-accept arm must never be able to summon (35 titled); pinned on the archive.
- The fiat arm must never win (alarm 97, at war with everyone); pinned.
- A scripted **"Pressburg" arm** (Ulm → Vienna → a Pressburg that cedes Tyrol, Carniola and Venetia by treaty, vassalizes Bavaria, Saxony and Hesse → Hanover held twelve quiet turns → summon) reaches 50 titled between turns 25 and 40 and, with Austria and Prussia recognizing and Britain shut out at 60%, wins between turns 33 and 48. If it cannot reach 50 by turn 40, `hold_titled` comes down to 45 before anything else moves.
- A scripted **"Premature" arm** (summon at exactly 50 with two refusers and no preparation) must LOSE the Congress on 3 of 3 seeds — the fuse is the player's judgement.
- The global-elimination probe fires E1 on the first end turn after the last great power falls.
- The played campaign (GE-V) confirms the table reads, the prices are true (each named lever flips the court when applied), and the sitting FEELS like a crisis: at least one declaration of war, one bill, one flip.

---

## §3 Defeat (RULED)

- **R1 stands:** "The Empire Without Soil or Sword" — ≤ 1 province, or no free corps and no affordable commission, for **5** consecutive turns; "The Eagle in Chains" — the Emperor captive **10** consecutive turns unfreed (with the purse-priced exit guaranteed, GE-1). Both warned on the existing defeat-imminent channel with the clock and the exits named; Paris alone never triggers them (PL-31). Terminal: the crimson register, Load / Main Menu, GET endpoints alive (R3).
- **The Humbled Peace:** ratifying a settlement that cedes Paris, or ≥ half the homeland, or makes France a vassal → `record_ending(world, "defeat", "humbled_peace")`, the crimson-grey register, **continue**. The Verdict grades it as an eclipse.
- **A dissolved Congress is not a defeat**; a lost titled province is not a defeat. **No third clock** (grip and treasury already reach R1 through attrition). Re-open: a played campaign showing a hopeless state neither arm reaches within ten turns.
- **GR5:** `fall.get_fall_state(world, nation)` answers for any nation; only the player's ends the game; AI courts keep losing by elimination and by suing.
- **Amended September 25, 2026 (user direction — "or if Napoleon dies"): a THIRD arm, "The Eagle Falls" — the Emperor's death.** Today the sovereign cannot die: `WorldState.destroy_marshal`'s sovereign arm converts every removal to capture (`death_guard:<cause>`) after the Guard's escape toll, and `GAME_END_SPEC.md` §3 had parked the Emperor's death with VP-2. GE-1 decides and builds it under the delegated grant; recommended default = a bounded roll at that ONE seam when his corps is destroyed on the battlefield (`cause` in {battle, charge} only; never attrition, internment, dismissal, teardown, never while captive), `SOVEREIGN_DEATH_CHANCE` in-band tunable behind `THE_EMPEROR_IS_MORTAL`, drawn through the campaign-seed helpers; death is immediate and terminal (1805 has no heir) with its own cause line, a `sovereign_dead` beat above `sovereign_captured`, and a Moniteur special. **Two more asks ride with it:** a measured memo on the generals' odds of death (a general has no personal death roll — he falls when his corps is removed; measure per cause / nation / arm on the driver and RECOMMEND, do not build), and an exile story on the Fall flavored by the campaign's record (§4). The build note = `docs/NEXT_SESSION_PROMPT.md`.

## §4 Screens (RULED)

- **One scene, `campaign_end.tscn` (CanvasLayer 122), four registers**, fed by ONE builder `build_campaign_summary(world, ending)`:
  - **THE IMPERIAL PEACE** — gold, the Proclamation style: the four flags with SIGNED / SHUT OUT / GONE, the titled count, the sitting's eight turns as a strip, the campaign totals (battles fought / won / lost, men lost and inflicted, provinces taken / lost, coalitions faced), the Verdict tier, a final Moniteur line; buttons **Continue the reign** / **Retire to the Tuileries**.
  - **THE FALL OF THE EMPIRE** — crimson: the cause line ("No soil and no sword remain to the Emperor" / "The Emperor, a prisoner these ten turns, is deposed" / "The Emperor is dead"), the totals, the tier; buttons **Load a campaign** / **Main Menu**; input disabled. **Amended September 25, 2026 (user direction): the register carries THE EXILE — `game_end.build_exile_story(world, ending)`'s epilogue**, deterministic (GR6) and flavored by the record: the place of exile from the captor or victor (Britain → the Bellerophon and St Helena; a continental court → its own confinement in that court's voice; deposed but uncaptured → Fontainebleau and Elba), the men who stood with him and the men who fell, the greatest victory and the worst defeat, the provinces lost and the coalition that took them, the Verdict's closing sentence; the death arm renders the funeral variant. GE-1 builds the builder and the `epilogue` payload on `build_campaign_summary`; GE-2 renders it.
  - **THE HUMBLED PEACE** — crimson-grey: the treaty's terms, the totals, the tier; **Continue**.
  - **THE VERDICT OF HISTORY** — parchment: the tier and its three lines, at turn 44 if no ending has fired ("the reign, unfinished, is judged as it stands"), and as the epilogue block inside the other three; **Continue**.
- **The Congress table:** a CONGRESS tab on the Diplomatic Ledger (stance / reason / price per court, the sitting's clock, the hold's seven conditions with ✓/✗), and the same rows in the war room while the Congress sits.
- **The clock line** on the war room, the Strategic Ledger's Territories tab and the end-turn banner (one source). Before a summons it reads the gate: `THE CONGRESS OF PARIS — 43 of 50 titled (7 held, unsettled: Vienna, Bohemia …) · summon from the Cabinet (F1)`.
- **Saves:** the ending stamped in `metadata.ending`; a defeated save loads onto the end screen; the autosave after a fall is the pre-fall turn plus a "Final — <date>" save (R6).
- **Raised** from every end-turn flow, from `/load`, and from the settlement ratify path (the Humbled Peace) by the NA-6b stash-and-raise discipline; never blocks the turn; `/mailbox/activate` gets the "The war is over." guard (R6).

## §5 Global elimination (RULED — E1–E6)

E1 the Universal Monarchy · E2 the alarm producers early-return while `get_qualifying_nations` is structurally empty and Talleyrand says "There is no Europe left to alarm" · E3 the dead stay dead (enemy-phase rows, fleets, ports, trade dominance, decks) · E4 a great power's knockout is a beat and a verdict input · E5 great powers stay eliminable on the battlefield · E6 the AI roster needs no change. All as written in `GAME_END_SPEC.md` §7.5; pinned in GE-1.

---

## §6 The ending slices

| Slice | Contents | Effort |
|---|---|---|
| **GE-1** backend — ✅ LANDED September 25, 2026 (landing record below the table) | `backend/game_logic/fall.py` (R1's two clocks, ONE serialized `fall_clock`, the defeat-imminent copy with clock and exits, the captivity-exit reachability proof); `game_end.py` (`record_ending` + the serialized `ending`, `campaign_totals`, `build_campaign_summary`, the Verdict tiers, the `campaign_end` scenario block + validator + `MODDING_FORMAT`); **`province_title`** at the four seams + reconciliation; the Humbled Peace stamp at the three ratify seams; E2/E3/E4 with pins; WO-D10 (exile commissioning, flip lever, series attribution if it moves); saves (R6); the three end-turn exits and `/load` carry the ending; `first_contact._is_open_ended` re-pointed; tests. **Sept 25 additions (user direction, §3/§4):** the Emperor's death arm at the ONE removal seam (`THE_EMPEROR_IS_MORTAL`, `SOVEREIGN_DEATH_CHANCE`, the `sovereign_dead` beat); the generals' death-odds memo (`docs/audits/GENERALS_DEATH_ODDS_2026_09_XX.md` — measure and recommend, no build, a DESIGN_REFINEMENT row with an owner); `build_exile_story` + the `epilogue` payload with driven pins on staged endings. Build note `docs/NEXT_SESSION_PROMPT.md`. | ~1.25 |
| **GE-2** client — ✅ LANDED September 25, 2026 (landing record below the table, after GE-1's) | `campaign_end.tscn/.gd` with the four registers (the Fall register renders GE-1's `epilogue` — `summary.epilogue.paragraphs`), raised from every end-turn flow, `/load` and the ratify path (every response carries `game_state.endings`; the end-turn and load responses carry `ending`); the clock line reads the warning's structured `fall` key; keep-playing after a marked ending, Load / Main Menu after the Fall; the legacy `_show_game_over_screen` text kept as fallback and de-legacied; the clock line on the three surfaces; XR-1 harness + boot smoke; a Mode-A driver arm that reaches each fall and the verdict; the IQ-10 frames for all four registers; **and (GE-1 review round #10) the Emperor's death on the COMMAND road — the war ends inside the command and no end turn follows, so no dispatch headline and no Moniteur special exist for it: the end screen is its only surface, raised from the `/command` response's `ending` (the funeral register); pin = a driven `Napoleon, attack Mack` at `SOVEREIGN_DEATH_CHANCE_PCT = 100` raising the funeral register; STATUS tracks it on the GE-2 line.** | ~1 |
| **GE-3** the Congress — ✅ LANDED September 25, 2026 (landing record below the table, after GE-2's) | `backend/game_logic/congress.py` (`can_summon`, `summon`, `answer`, `price`, `tick`, `resolve`, `state_line`; ONE serialized `congress`), the `summon_congress` verb through the shared executor (2 DP + 1 admin; corpus rows; the wizard row with gate terms), the `recognition` proposal type + sweetener clause on the instrument transport, the refuser pressure hooks (intent weight, paymaster, alarm gate, Revanche weight), the recognition clause on the sue seam, the hold predicate, the dissolution, E1; the CONGRESS ledger tab + war-room rows; beats 1–4 + the Moniteur column + Talleyrand's rung; the Pressburg and Premature driver arms; `BASELINE_SERIES` re-recorded once (the pressure hooks are dormant with no Congress — attribution must show arm 0 byte-identical); `tests/test_congress_of_paris.py`. | ~1.5 |
| **GE-V** the played campaign — ✅ RUN September 25, 2026 (landing record below, after GE-3's) | The user (or Claude driving the client) plays a campaign to a Congress: the table reads, the prices are true, the sitting is a crisis; the §2.8 measurements published; the pillar "the ending" re-scored (3.0 → target ≥ 7). **Measured: the reach is not on the road (35 titled by turn 12 on both hand-played openings) → `hold_titled` 45; the table/prices/sitting confirmed on the driven arms and the frames; re-scored 5.5, target NOT met; the reach gate = `DESIGN_REFINEMENT.md` GEV-D1.** | ~0.5 |

**✅ GE-1 Landing record (September 25, 2026 — the commit that carries this record).** Built as ruled, with the user's three September 25 additions. Rules `SYSTEMS_REFERENCE.md` §64; pins `tests/test_ge1_the_verdict_and_the_fall.py` (83 — driven through the real `capture_marshal`, `destroy_marshal`, `_ratify_treaty`, `TurnManager.end_turn`, `/command`, `/load`); memo `docs/audits/GENERALS_DEATH_ODDS_2026_09_25.md`; design rows `DESIGN_REFINEMENT.md` GE-D1 (the generals' mortality, awaiting the user), GE-D2 (the spent Guard → GE-V), GE-D3 (the ending's chronicle line — filed, then CLOSED in-slice: `campaign_ending` is a chronicle type, the count 165 → 166). The end screen's payload is on the wire for GE-2: `ending` (with its full `summary` — `game_end.screen_payload`) on the end-turn road, `/load` and a `/command` that ended the war; `GET /campaign_end` for every ending; `game_state.endings` (compact) on every response.

- **The flag (R7).** `campaign_end` authored in `europe_1805.json` (`verdict_turn` 44, `fall_grace_turns` 5, `captivity_grace_turns` 10, `title_turns` 12), validated by `modding/validator._validate_campaign_end` (required `verdict_turn`, integers in range, booleans rejected, unknown keys WARN for GE-3's numbers), documented in `MODDING_FORMAT.md` (+ the missing `start_date` row). `game_end.endings_armed` is the new derived flag; `sandbox_mode` is never written. The tutorial and the bare flag world never arm (pinned). A pre-GE-1 save is armed at load from its scenario's own block.
- **`fall.py` — R1's two clocks, one serialized `fall_clock`.** One ruling the spec left open, taken and pinned: **the Fall is a death in war** — the soil-or-sword clock ticks only while France is at war with someone and resets at a general peace (so a Humbled Peace can continue as the plan requires), and the chains clock advances only on a turn France is at war with the captor, PAUSING (never resetting) during a truce or a vassal treaty. Exits are per disjunct — `GAME_END_SPEC` §5 test 2's "commissioning stops the clock" is true of the sword disjunct only, and the copy says which exit serves which condition. Warned on the existing channel with the clock and the exits (`fall.warning_state`, heading "THE FALL OF THE EMPIRE", a structured `fall` key threaded through `_build_defeat_imminent_warning`); it now also warns for the two arms the collapse never covered. Paris alone never triggers (pinned).
- **The captivity exit's reachability proof (R1).** The scout proved accepting a captor's peace at 0 gold is legal — and that NO captor offer was guaranteed to arrive inside ten turns (P8 needs a pair score above 40, P1 is coalition-gated, the multilateral producer needs three courts and a five-turn cooldown). So GE-1 built the road R1 required before arming the clock: **"The Captor Names His Price"** — a court holding the player's sovereign offers its terms on the clock's own turns 1, 4 and 7, the FIRST rung (ahead of P1, whose armistice would only pause the clock), priced to the purse by the one EC-W4 source, never dropped. Pinned: the offers arrive on [1, 4, 7]; accepting at an empty treasury frees him and the clock resets.
- **"The Eagle Falls" (Sept 25, user direction).** A seeded roll at the ONE seam, `destroy_marshal`, armed worlds only, battlefield causes only (`battle`, `charge`, `bombardment` — the auto-bombardment kill is a battlefield death; never attrition, internment, dismissal, teardown, never a prisoner), `SOVEREIGN_DEATH_CHANCE_PCT = 15`, `campaign_variance.seeded_int` on `sovereign::death::{turn}::{name}::{cause}` (the historical seed still rolls; no module RNG). **A correction to the build note:** "the Guard's escape toll still runs first" is not true at the destroy sites — a corps the fighting zeroes never reaches `_check_marshal_fate`, so the roll covers exactly the case the toll could not buy. His death is immediate and terminal: the tombstone and the event carry `sovereign: true`; the dispatch leads with the new `sovereign_dead` class (102, above `sovereign_captured`'s pinned 101); Le Moniteur prints "THE EMPEROR IS DEAD" (105); the combat copy names him on the defender AND the attacker side (an attacking sovereign's death had been silent); the campaign log says "THE EMPEROR … FALLS". The last action point no longer auto-advances past his death, and the enemy phase stops the moment the Empire falls. GR5 by construction (a foreign sovereign takes the same roll; none is authored in 1805).
- **`game_end.py` — `record_ending`, the serialized `endings` (plural: a Humbled Peace and a Verdict must both stand, §7.10), `campaign_totals`, `build_campaign_summary`, the Verdict.** Terminal endings set `game_over`; marked ones never do. `_check_victory_conditions`' sandbox arm reads the recorded terminal ending (pure — the clocks tick once per end turn in `process_end_of_turn`, never in the twice-a-turn check). The post-advance exit now clears the choices a fallen campaign can no longer answer (it never did).
- **The Humbled Peace.** Stamped once per ratification in the two top-level ratifiers (never inside the headless appliers), from the SIGNED terms plus the applied carve: capital, ≥ half the homeland, or a new vassalage. Marked, never terminal; forces the Verdict's eclipse.
- **`province_title` + reconciliation.** Written at the four seams the plan names plus the two the scout found missing (the reckless auto-charge conquest and an ultimatum yield), popped on a return home, reset by one per-turn pass; the Revanche drops a province its court signed away while the treaty stands, and a fully reconciled Revanche reads inactive, NOT satisfied.
- **E2/E3/E4.** E2 structural `no_court_left_to_alarm` silences the passive producers, the murmurs and the coalition gauge ("There is no Europe left to alarm"). E3: a dead court cannot be declared upon (measured: a declaration on an eliminated Britain had put its 100-sail fleet back on blockade), holds no trade dominance, draws no fleet, gets no "No marshals (eliminated?)" row or duplicate elimination notice; its deck is kept (revival is real); `continental_ports_total` deliberately untouched (NV-10). E4: the existing beat and special fire; the Verdict counts knocked-out great powers.
- **WO-D10.** The richest HELD province when no home soil remains; the refusal now means "we hold none" (both pins that read "No HOME soil remains" still hold, unflipped).
- **Saves (R6).** `metadata.ending`; the autosave is not overwritten after a Fall; a "Final — <date>" save written once (from the autosave door and from a `/command` that ended the war); `list_saves` sorts a Final save after every playable one, so Continue resumes the turn before the fall; `/load` carries the terminal ending; `/mailbox/activate` guarded; `game_state.endings` on every response. The playtest driver now reads the ending and reports a Fall as `game-over` (it had been reported as `blocked`: the main `/command` road dropped `game_over`).
- **The exile story (Sept 25, user direction).** `build_exile_story` — deterministic, every clause sourced in `facts`: the captivity (Britain → the Bellerophon and St Helena; Austria → Olmütz; Prussia → Küstrin; Russia → Schlüsselburg; each with one line in the court's own voice), the abdication (Fontainebleau and Elba, the victor from the capital's holder), the funeral (in state in Paris, or buried where he fell if Paris is lost), and the "signed" variant for a Humbled Peace; the men (the loyal by their bond to the sovereign and trust, the fallen by BATTLEFIELD tombstones only — the memo's §5(c)(i) — the captives, the one who broke with him); the record; the Verdict's closing line. Pinned on staged endings with every named man, court and battle checked against that world's record.
- **Measured on the committed driver (46 turns, the Verdict fires at the end of 44).** `tools/playtest_driver.py` Mode A, three seeds × three arms, every run ending legibly (the driver now reports a Fall as `game-over`):

  | Arm | historical | ulm | austerlitz |
  |---|---|---|---|
  | ambient (passive France, the driver declines every envoy) | **Fall, turn 31 — "The Eagle Falls"** (the Emperor's 63-man remnant annihilated; the seed's turn-31 roll is 12 < 15) | **Fall, turn 40 — the chains** (taken turn 31; the captor's terms offered on its turns 1, 4 and 7 and declined by the driver's policy) | **Verdict, turn 44 — the eclipse** |
  | commanded, `--diplomacy accept` | Verdict — contested | Verdict — contested | Verdict — contested |
  | commanded, `--diplomacy decline` | Verdict — contested | **Fall, turn 24 — the chains** (taken turn 15) | **Fall, turn 38 — the chains** (taken turn 29) |

  The Verdict tier distribution over the five runs that reached it: **contested 4, eclipse 1, ascendant 0, triumph 0** — no arm knocks a great power out by turn 44, and the status quo reads contested by design. The Falls: **chains 3, the Eagle Falls 1, soil-or-sword 0** (no arm reduces France to one province, or strips it of every corps with no commission, inside 44 turns). The Emperor died in 1 of 9 runs — the memo's forecast (his corps destroyed in 12 of 25 campaigns × 15%) is ~7%. **Every chains Fall came on an arm whose driver policy DECLINES diplomacy**: the captor's terms reached the mailbox three times each and a player who accepts walks the Emperor out (pinned at an empty treasury); the arm is a harness fact, not a balance claim.
- **The generals' death-odds memo (Sept 25, user direction).** 25 seeded campaigns, 884 battles: a general has no personal death roll — he dies when his corps is ground below the 50-man rubble floor or his court is eliminated; France loses generals to captivity about fifteen times as often as to death; a beaten defender dies 2.2% of the time and is taken 21.9%; the September 11 "four destroyed" claim does not reproduce. Recommended, NOT built: a bounded wound-or-death roll for a losing lead (1% / 8%), no death off the field. Filed as GE-D1 for the user's ruling.
- **Series and harness.** `BASELINE_SERIES` and M1–M7 byte-identical — measured, with the reason: the ambient board never reduces France to one province, never leaves it without a corps, never takes or annihilates the Emperor, signs no Revanche-province cession, and never reaches a court with no home soil and held conquests, so none of the new mechanics is read inside forty turns. The full suite's only failures were the pins named below (plus the log-type census, answered by making `campaign_ending` a real chronicle type — GE-D3 closed in-slice); the AI-V `ulm` fixture, where the Emperor is taken on turn 32, now receives the captor's terms and its pins hold.
- **Pins consciously flipped (named):** `test_iq2_collapse_dispatch.py::TestTheWarning::test_fallen` / `::test_last_province` / `::test_the_dispatch_carries_the_heading_and_dedupes` (staged UNARMED now — IQ-2's copy pinned verbatim — each with an armed sibling pinning the clock and the exits); `test_iq2_collapse_economy_status.py::TestB3Ledger::test_the_collapse_note_is_the_one_source_plus_the_scope_note` and `::TestB4IntelReport::test_a_collapsed_report_opens_on_the_state_of_the_empire` (the tail is `fall.scope_sentence`, which IS `CAMPAIGN_CONTINUES` unarmed); `test_iq2_collapse_war_room.py::TestWarRoomLeadsWithOurOwnState::test_our_own_state_comes_first_and_the_campaign_goes_on` (the clock armed, the old sentence unarmed) and `::test_lever_down_the_war_room_says_nothing_of_itself` (the pre-IQ-2 silence is now BOTH levers down — the chains arm speaks for a captive Emperor); `test_first_contact_keyless.py::TestTheGoal::test_the_goal_is_answered_in_a_sentence_not_the_manual` (all 7 ids — the Verdict and the Fall; the stub-world test holds); the fourteen `len(CAMPAIGN_LOG_TYPES)` pins 165 → 166 and IQ-7's census over them (`test_iq7_satellites_have_a_position.py::TestT21CampaignLog::test_every_count_pin_moved_with_the_rationale`); `test_economy_ec6_sandbox::test_1805_no_end_screen_at_turn_60` re-blessed with a comment (the Verdict never sets `game_over`); `tools/_sweep_first_contact.json` FC-6's anchor follows the re-pointed branch. `_chronicle` and `_client` held unflipped (the armed copy uses none of their forbidden words).
- **Gates:** ruff clean; mutation sweep `tools/_sweep_ge1.json` **42 of 42 killed, 0 INERT, 0 BROKEN at close** — three were INERT on the first pass and each was a real weakness: the roll-bounds pin counted `seeded_int` directly instead of the production roll (repaired), the end-turn road's `ending` allow-list entry is only load-bearing for a MARKED ending and no pin drove one through `/command` (the Verdict now does, with `GET /campaign_end`), and the captor rung's explicit `_force_send` was redundant — the P8 reducer's own fallback already forces an unwelcome harsh peace through — so the belt was DELETED rather than pinned; `BASELINE_SERIES` + M1–M7 byte-identical; zero `.gd` (the end screen is GE-2's — the client's legacy game-over text is the fallback until then).

**GE-1 review round (September 25, 2026 — the commit after `975f1f13`).** A seven-lens adversarial review of the committed slice (clocks · the death · the Humbled Peace and titles · saves and endpoints · E2/E3/WO-D10/the captor · the exile story and the Verdict · the tests), each finding sent to two refuters on a read-only snapshot: **50 filed, 38 survived both refuters, 4 of them P1**. Every survivor is fixed and pinned — plus eight of the ten the refuters split on, taken on merit — in `tests/test_ge1_review_round.py` (51, each named for its finding), with two pins flipped consciously — `test_ge1_the_verdict_and_the_fall.py::TestTheSoilOrSwordClock::test_falls_on_the_fifth` (the realm arm now names the province it still holds, "The Empire is reduced to Brittany alone — a province, not a realm."; "No soil remains" was false with Brittany French) and the tautology closing `TestProvinceTitle::test_a_signed_province_is_reconciled_out_of_the_revanche` DELETED (`get_active_agenda is not None`; the live view and the not-satisfied guard are pinned in the new file) — and one comment corrected, `test_economy_ec6_sandbox::test_1805_no_end_screen_at_turn_60` (#48: it pins the pure-read victory check, not the Verdict); the NPC-2 census caught the escort-home branch ending an order without clearing its question on the first full run, fixed at the seam. Rules updated in `SYSTEMS_REFERENCE.md` §64.

- **The war is closed at ONE seam (#1/#6/#22/#42, P1).** The Emperor killed on an answered interrupt, a typed interrupt answer, an objection road or a charge popup ended the war SILENTLY — no `game_over`, no `ending`, and the Final save never written, because only the main `/command` builder read the terminal ending, and after the fall every order (so every autosave) is refused. Now `game_end.close_campaign` runs at the head of `main.build_base_response` — the seam every POST passes through — and `_attach_terminal_ending` sets `game_over`/`victory`/`ending` and writes the Final save there (the `/command` builder's own copy is deleted). The `/command` game-over guard's refusal therefore carries the ending too. `strategic.py`'s "Assault failed — orders cancelled, marshal awaits new instructions" is no longer composed for a dead Emperor.
- **The summary is taken on the FINISHED field (#7/#35).** The death is stamped inside `destroy_marshal`, before the battle is counted and before the province it takes changes hands — the end screen read "0 battles" and laid him in state in a Paris that fell in the same fight. `close_campaign` rebuilds the terminal record's summary ONCE (`closed`), after the road that stamped it has resolved: at the response seam, in `TurnManager`'s `_attach_endings` (the enemy-phase death), and in `write_final_save`. And `record_ending` now appends the record BEFORE building its summary, so a Humbled Peace's own stamped Verdict grades itself the eclipse (#34; it had read "ascendant").
- **No question nobody can answer (#23/#4).** `game_end.clear_unanswerable` — the ONE list, run by the close — empties the dialogue queue and the popup queue (the marshal petition included), the capture choice, both objections, the redemption, every player marshal's standing interrupt and an armed charge. The response that ends the war, the Final save and the load of it raise the ending and nothing else. `/load`'s own duplicate terminal attach is deleted (the base builder carries it).
- **The captor's terms arrive (#28, P1).** P1 was the one wartime rung without `proposal is None`, so a captor BELOW its P1 threshold overwrote its own terms with an armistice (which only pauses the clock) or a peace the acceptance filter dropped — the Emperor was deposed with no offer ever sent. Guarded; pinned at −60 and −80. **And the envoy says what it is (#33):** `captor_terms` rides the dialogue context, Talleyrand's text names "the terms of the Emperor's release", and the popup carries the clause "His Majesty is returned to France — any peace with Austria frees the Emperor".
- **The clocks say what is true.** A TICKING arm outranks a paused one for "soonest" — the warning's title, its severity and every surface's scope line name the clock that will actually end the game (#2; a paused 8-of-10 chains arm had hidden a ticking 3-of-5 soil arm), and a paused arm has no fall date (it had slid forward every turn). The chains entry is keyed on the captivity — captor AND the turn he was taken — so freed-and-re-taken is a fresh clock with a fresh offer cadence (#0's chains half; its soil half is the designed "consecutive" sampling and stands). A truce PAUSES the soil clock as it pauses the chains (#5 — signing one at 4 of 5 had bought a whole new clock), with its own line ("the clock stands still while the truce holds"). The chains exits are read from the live relation to the captor (#3): at war, "any peace frees him"; in a truce, "turn the truce into a peace"; at peace, "make war on Austria and storm the city that holds him" — never "any peace" to a France already at peace.
- **No court at peace holds the Emperor (#13).** A lapsed safe passage on the soil of a court the realm is at peace with — the one road into captivity without a war — no longer takes him: `withdrawal._intern` escorts him home through the release seam's own hygiene, his corps interned and at most the released prisoner's escort (5,000) kept. Lever `withdrawal.THE_EMPEROR_IS_ESCORTED_HOME`; False restores the NP-4 capture.
- **The death, named and placed.** The field he fell on (#36): `destroy_marshal(location=)` — an attacker is destroyed before he advances, so his own location was still the province he marched from ("at Lorraine, the Emperor was killed" for the Battle of Swabia); the attack, the charge and the auto-charge pass the battle region, and the tombstone, the event, the ending detail and the epilogue read it. Berthier's report gains the death arm FA-S17-11 never had — "And the Emperor himself fell on that field." (#8; it had read "Hardly an engagement, Sire … and the day moved on"), and the diorama no longer says he "watched this field". A sovereign killed as a PARTICIPANT is named (#9, fixed before the round closed). No prestige moves and no note addresses a dead Emperor (#11 — "The court will hear of it (Authority −5)"); the `sovereign_dead` page stands alone (no sub-beat advising a garrison); the Marshalate is not offered to an Empire that has ended. The killer's turn stops with the Empire — the per-marshal loop breaks on `game_over`, the admin phase is skipped (#12) — and the player's standing orders, the grievance pass and the autonomous marshals do not run after an enemy-phase death.
- **Titles and the Revanche.** A renewed war breaks what either side signed to the other (#15): `game_end.break_signed_titles`, called from the ONE diplomatic-state setter on every entry into WAR, turns the treaty record into a conquest record whose quiet clock starts now — so a truce inside that war no longer re-reconciles the province (the "treaty stands" check was any `active_treaties` row, including the armistice's). A Tilsit carve reconciles like a cession (#14) — the client's `carved_from` read by `reconciled_regions`, broken by a renewed war (`carve_broken`; the origin is kept). A hand-off inside the bloc — a VS-3 grant, the IQ-7 petition's province, a reclaim — carries the record to the new holder (#16; the grant had made a treaty province permanently "held" and woken the ceder's Revanche), and a signed province the receiver's satellite occupies is titled.
- **The treaty record counts what happened.** `note_ratification` counts a signed territory term only when the province is no longer ours, and a carve only from the APPLIED list (#18 — a clause PL-20 skipped had stamped "gave away Paris itself" while France held Paris). A truce is not a peace (#20): `_ratify_treaty` passes `war_ending` false for WAR→ARMISTICE, and an armistice that EXPIRES into peace is counted there (`game_end.count_peace`) — armistice-then-peace is one peace, an armistice collapsing back into war is none. A settlement's Humbled Peace names the courts that SIGNED (#17 — the plan's own parties, not the whole opposite side).
- **The words (#37–#40).** The chains epilogue and cause line say how long he was HELD ("a prisoner these 17 turns — 10 of them at war with his captor") when a truce paused the clock. The Verdict's three lines are chosen from what is true (`game_end._tier_lines`): a France that lost more battles than it won is not told "the coalition is beaten in the field", one that gave ground is not told "the Empire holds what it held", and the eclipse's first line names only the losses that happened; the canonical lines stand for a reign that fits its tier. The record paragraph: a sentence opens on a capital ("The assault on Vienna"); provinces are counted DISTINCT (`lost_regions`) with "One province was lost, to Austria." / "all of them to" / "took the most" / "as many to Austria as to Britain"; "One coalition stood against him — the Third Coalition." (never "the last" of one) and "— the last, the Fourth Austrian Coalition." (a name's own article lower-cased). The realm arm names the province it still holds ("The Empire is reduced to Paris alone — a province, not a realm."), never "no soil remains" while Paris is French.
- **Saves and endpoints.** Two campaigns that fall on the same date keep two Final saves (#25 — the second takes the next free name, `Final _ <date> (2).json`), and the record names its own file BEFORE the write, so the Final save remembers itself. A pre-GE-1 save armed at load is backfilled with a record (#21/#26, `game_end.backfill_record`): the opening, `record_since_turn` (the summary carries it and the epilogue says "(The record was kept from late May 1806 only.)"), and a conquest title record for every province held off its holder's homeland, so an old conquest can still earn title. `/mailbox/activate`'s game-over refusal carries the letter-book's own count (#27).
- **E2/E3 and the goal.** E2 is whole (#30): `coalition.displayed_threat` shows 0 with no court left to alarm — the ledger's gauge and tier, the top bar's `threat_level` and every response's — and the ledger's projection line reads "There is no Europe left to alarm — no court stands that could raise a coalition against us."; the stored scalar is untouched (a freed satellite re-opens the alarm where it stood). A declaration on a dead court is refused at the flow's FIRST step (#29, `diplomacy.dead_court_refusal`, one source for both), before the War Purpose card, the objection and the ally review. A court eliminated inside the enemy phase gets no row (#31; the roster is read live per court). The goal answer after the Verdict is in the past tense — "History has judged the reign, Sire, at the end of Early July 1807 (turn 44): An Empire Contested." — and says the Empire can still fall, "an Emperor held prisoner through 10 turns of war with his captor" (#32).
- **The instrument.** The playtest driver reads the campaign's own record (`GET /campaign_end`) after every drain (#24), so an ending stamped outside the end-turn window — a death on an answered interrupt, a Humbled Peace ratified in the drain — reaches the digest once.
- **Not taken, with reasons.** #19 (half the homeland over two peaces) and #41 (the marshal counters) were refuted by BOTH refuters — the per-ratification test is the contract (a Humbled Peace marks one treaty the player chose), and no surface prints the counters. #47 (one refuter showed the armed and unarmed war-room texts differ only in the scope line) stands. Of the split verdicts #48 is taken as a corrected comment and #49 in part (the exact fall date, the change of captor). #10 (the `sovereign_dead` headline and the Moniteur special never exist on the player-turn roads — the war ends inside the command, and no end turn follows) is **owned by GE-2**: the end screen carries the fall's own headline (`ending.cause_line` + the epilogue); a dispatch and a special edition built at the stamp would be a second surface GE-2 renders over. Completion: GE-2's end screen shows the Eagle Falls from the command road; STATUS tracks it on the GE-2 line; the behaviour test is GE-2's driven death-on-the-command-road screen pin.
- **Gates:** ruff clean; `tools/_sweep_ge1_review.json` **59/59 killed, 0 INERT at close** (3 inert on the first pass, each a pin weakness — GR-18 asserted the carve's break only during the war, GR-41 read `action` where the AI's rows carry `ai_action`, GR-50 passed a turn where the builder takes a nation — all three strengthened and killed); six anchors in older sweeps broken by this round (`_sweep_ge1.json` GE1-3/31/41, `_sweep_fa_slice15r.json` S15R-18/a, `_sweep_fa_slice2.json` FA-N68/a, `_sweep_wo15.json` WO-30) re-anchored and re-swept, 6/6 killed; full suite **25,566 passed / 4 skipped**; `BASELINE_SERIES` + M1–M7 byte-identical; zero `.gd`.

**GE-1 verification round (September 25, 2026 — the commit after `e5d67800`).** The review round's own FIXES were attacked, not its findings: four lenses (the close · the clocks · the titles · the words), every finding reproduced by probe on a read-only snapshot of `e5d67800` and sent to a refuter. **29 filed, 27 survived — ALL FIXED**, pinned in `tests/test_ge1_verification_round.py` (55), each named for its finding (V<n>). Rules in `SYSTEMS_REFERENCE.md` §64.2. The lesson the round keeps teaching held a fourth time: **the regressions were in the fixes** — the close's clearing ran once and a road AFTER it re-seeded the question; the new chains pause and escort branches were each silent somewhere; the carve arm read the client's whole live territory.

- **No question after the Fall (V0, P2).** The fatal end turn's own trust net staged a redemption BEFORE the stamp; the hoist lifted it and `main` wrote it back AFTER the close, whose clearing never ran again on a closed record — so the Fall's response, `GET /pending_redemption` and the load of any later save all raised a question the endpoint refuses. The close now clears on every response; the checker, the standing read, the hoist and every writer are gated; the envelope drops a question staged before the stamp.
- **Nothing moves after an own-order death (V1).** The Emperor killed in his OWN standing order let Soult fight a whole new battle — counted on the end screen as its worst defeat — after he had fallen. The strategic pass breaks on `game_over`, and the end turn re-reads the ending after it.
- **A legacy Final save is adopted (V2)**, not copied into "(2)", "(3)", "(4)" on every load — `load_game` stamps the terminal record with the file it came from. **The lever says what it gates (V3):** the close only.
- **A truce on its last turn is a ticking clock (V4, P2).** The Empire fell at the end of a turn whose warning said "the clock stands still" — the truce collapsed back into war inside that turn's advance. `diplomacy.armistice_resolves_this_turn` reads the war panel's own projection for one pair; a paused arm whose truce ends in war this turn is `resuming` — dated, ranked as ticking, `critical` — and says so. A paused count is stated in turns of war still to come. **A truce at nought is not a peace (V8).**
- **No court out of war with his realm holds the Emperor (V5/V6, P2).** Two more roads into a captivity with no clock: a spent Guard's breakout paid its toll to a zero-strength corps and the attrition sweep's fallback handed him to the strongest court AT PEACE; and a vassal treaty with his captor (WAR→VASSAL) or that satellite's release (VASSAL→PEACE) never freed him. Every change that leaves a pair outside WAR and ARMISTICE frees a sovereign held by the other side (`fall.free_captive_sovereigns`, at the one setter — sovereigns only); on an armed world the fallback offers only courts at war and otherwise sets him down at home; a spent Guard breaks out with its escort.
- **The escort home is on the record (V7/V10).** It reached no surface but the terminal, and the chronicle read "restored to France" from a captivity that never was: now on the briefing (a warning) and in the chronicle ("…'s corps INTERNED at Berlin by Prussia (35,000 men) — the Emperor himself escorted home"), the stale "no nearer home … 0 turns" beat is dropped for a man already home, and every internment line says "Prussian soil".
- **The signature belongs to the courts that signed it (V11–V16, four P2).** The review round matched a renewed war on the two courts' BLOCS: a French satellite's own war unsigned France's cession while France and Austria were allied; the settlement's ARMISTICE→WAR→VASSAL bookkeeping hop broke a carve at birth; a satellite's reclaim popped the record; the carve arm reconciled the client's whole live territory and lapsed at its release; a repudiation without war left the cession reconciled. Every title record now carries its `house`; only `from` and `house` at war (or a repudiation) break it; a hand-off follows the house; a carve is a treaty record for the carved provinces only.
- **The words (V17–V23, V25–V28).** The courts ranked by capture events against a distinct count ("Austria took the most" when Britain took two of three) — now ranked by distinct province, last capturer; a 975f1f13 record counts losses. A Fall is always the eclipse — the Verdict had closed the Emperor's funeral with "an empire ascendant" — and its lines name the Emperor and the capital the count cannot see. The abdication named "no soil left" beside "reduced to Paris alone" (and the #40 pin was vacuous — repaired). The Emperor TAKEN in his own attack was silent on every battle surface — the death roll's 85% branch; now named, and his fate replaces "Hardly an engagement … and the day moved on" (FA-S17-11's sovereign pin flipped consciously; an ordinary capture still appends). The goal names the turn history judged; "A Reign of Triumph"; "the Battle of Munich"; "The court of the Papal States no longer exists"; a clock that merely started late claims no count at war; with no Europe left the ledger raises no "courts are recovering".
- **Not taken:** V9 and V24, refuted (the escort cannot run for a realm with no province — `_intern` needs a road home; the backfilled line was not false on a reachable board).
- **Gates:** ruff clean; `tools/_sweep_ge1_verification.json` **53/53 killed, 0 INERT at close** (5 inert on the first pass — four pins that never reached the branch they guarded, each strengthened and killed, and one DEAD line removed: the second `_fallen` re-read before the autonomous marshals, which nothing between it and the first can change); twelve anchors in older sweeps broken by this round (`_sweep_ge1_review.json` GR-04/07/08/17/18/26/27/28/43/51, `_sweep_fa_slice3.json` FA-68/a) re-anchored and re-swept, 12/12 killed; full suite **25,621 passed / 4 skipped**; `BASELINE_SERIES` + M1–M7 byte-identical; zero `.gd`.

**✅ GE-2 landing record (September 25, 2026 — the commit that carries this record).** The client half of the ending, built to the §6 row and the hand-off (`docs/NEXT_SESSION_PROMPT.md` of the GE-1 session). Rules `SYSTEMS_REFERENCE.md` §65; pins `tests/test_ge2_the_client.py` (60 — the backend clock line, the payloads off the real endpoints, the wiring, the driver, the frames, and the client DRIVEN through `tools/ge2_campaign_end_harness.gd` on the real `main.tscn`).

- **The end screen.** `scenes/campaign_end.tscn` + `scripts/campaign_end.gd` (CanvasLayer 122 — above the diorama, so the tableau of the fatal battle plays first and the verdict on the reign is the last thing the campaign says). ONE scene, FOUR registers, ONE payload (`game_end.screen_payload`; the client recomputes nothing): **fall** (crimson; the cause line, THE EXILE / THE FUNERAL from `summary.epilogue`, THE RECORD, THE VERDICT OF HISTORY — always the eclipse; **[Load a campaign] / [Main Menu]**, the command line closed for good), **humbled_peace** (crimson-grey; THE PEACE paragraphs; **[Continue]**), **verdict** (parchment; the tier as the heading with its three lines and closing, then THE RECORD; **[Continue]**), **imperial_peace** (gold; **[Continue the reign] / [Retire to the Tuileries]** — GE-3's register, taken now). Cinzel headings, IM Fell body; a cue per register (`bell_toll` / `quill_sign` / `parchment_open` / `fanfare`, claimed so it leaves with the card); ESC presses Continue on a marked ending and nothing on the Fall (FA-94); the card fits by `clamp_centered_panel` + a scrolling body (the IQ-10 rule — both Interface Scales pinned on frames); the wheel passes (PC15-18 census).
- **The wiring — stash-and-raise (NA-6b).** `api_client.response_received` fires for EVERY 200-OK body BEFORE its handler (the one seam that sees the ratify road, the mailbox roads and the command family alike) and `main._stash_ending` reads it: a response's own `ending` is queued; a cause the compact list (`game_state.endings`) names that the client has not shown is FETCHED off the record (`GET /campaign_end`, new `api_client.get_campaign_end`) — the Humbled Peace ratified on the settlement road arrives that way. `_show_pending_ending` raises the next queued ending at every control-return tail — `_return_control_to_player` (after the diorama, before the Proclamation), the diorama's, the Proclamation's and the petition's dismissals, the command path's own tail, the world-swap tail — and the record's late answer raises at once when control has already come back. **The six roads that read `game_state.game_over` end at ONE seam**, `_on_campaign_over`: the line closes (`set_input_enabled` refuses to reopen while `_campaign_over` stands — only a world swap lifts it), the ending raises, and with nothing to raise the terminal record prints alone. **Each cause is raised once per client session**; a world swap (Begin / Continue / Load / plain entry) adopts the arriving campaign's MARKED endings as history and keeps only a TERMINAL one to raise — so `/load` of a "Final — …" save lands on the end screen (R6), a plain entry onto a fallen backend fetches and raises its Fall, and a Verdict from an earlier sitting is not news. After the Fall, **Load a campaign** hides the card for the Load dialog (layer 101 sits under 122) and a cancelled load brings it back with the line still closed; **Main Menu** is the pause menu's road. **Continue** on a marked ending resumes the tail the card interrupted.
- **The command-road death (GE-1 review #10) — the end screen is its only surface.** Pinned DRIVEN: `Napoleon, attack Mack` at `SOVEREIGN_DEATH_CHANCE_PCT = 100` — the `/command` response (`success: true`, `game_state.game_over`, `ending` funeral, NO dispatch) raises the crimson card with THE FUNERAL, the record and the eclipse, and closes the line; the refusal that follows ("The war is over.", `game_over` + the same `ending`) raises nothing new. `BUG_FIXES.md` GE1-RV10 → FIXED.
- **The legacy text, de-legacied and kept.** `_show_game_over_screen(game_state, ending)` is the scrollback record beneath the card and the FALLBACK when the card cannot be raised: the register's own title, date and cause line lead (no French-only heading), the figures are the ending's summary (never a hard-coded thirteen-province board), and with no summary at all it prints what `game_state` carries and claims no more.
- **The clock line — ONE source, three surfaces.** `fall.clock_line(world, view)`: "THE EAGLE IN CHAINS — 3 of 10 · the regency falls at the end of turn 10 (7 turns remain)"; a paused clock says the clock stands still and names NO date (`falls_at_end_of_turn` is None exactly then); a truce that ends in war this turn is dated and said (`resuming`); a clock at nought says when it starts. `fall.clock_severity` (critical ≤ 2 left / warning / paused) is decided once. `_arm_payload` stamps both on the warning's `fall.arms[]`; `fall.clock_lines` gives the ledger its `fall_clock` (Territories tab, `strategic_ledger._fall_clock_lines`) and the war room its lines — one per HELD arm, in the ARMS order (the collapse branch had carried the soonest arm alone, so a paused chains clock behind a ticking soil clock was never named there); the end-turn banner prints them under the FALL heading (`main._add_fall_clock_lines`) and the R screen mirrors it (`dispatch_view.gd`). Lever `fall.THE_CLOCK_HAS_ONE_LINE` — False is the GE-1 payloads byte for byte.
- **The fourth register, declared.** `game_end.CAUSE_IMPERIAL_PEACE` with its register, title and cause line ("Europe accepts the order of the French Empire.") — marked, never terminal — so GE-3's `record_ending(world, "victory", CAUSE_IMPERIAL_PEACE)` raises the gold card with no client change; its frame is STAGED through the real `record_ending` (the staging says so). GE-3 adds its own blocks to `summary` (the four flags SIGNED / SHUT OUT / GONE, the sitting's strip, the final Moniteur line).
- **The driver.** The digest prints the END SCREEN's blocks under each ending — the date and register, THE VERDICT's tier and lines, THE RECORD, THE EXILE's paragraphs (`_end_screen_lines`, lever `THE_DIGEST_RENDERS_THE_END_SCREEN`), so a headless arm is evidence about the screen's content; `--stop-on-ending` stops a run at a MARKED ending (status `ending-reached`; a Fall still reports `game-over`). Two STAGED saves (`tools/gen_ge2_ending_fixtures.py` → `tests/fixtures/playtest_saves/fixture_ge2_soil_or_sword.json`, `fixture_ge2_chains.json`; their slot names say "GE-2 staged") reach the two clocks the ambient board never reaches. **The four arms, archived under `docs/audits/playtest_digests/ge2-*`:** `ge2-eagle-falls` (historical ambient, 34 turns — **the Fall on turn 31, "The Emperor is dead."**, funeral epilogue), `ge2-chains` (the chains fixture, 13 turns — **the Fall on turn 10**, Olmütz), `ge2-soil-or-sword` (the soil fixture, 8 turns — **the Fall on turn 5**, Fontainebleau and Elba), `ge2-verdict` (austerlitz ambient, `--stop-on-ending` — **the Verdict on turn 44, an empire contested**). `docs/PLAYTESTING.md` carries the commands.
- **GE-D2 measured, not built.** On the Eagle-Falls arm the Emperor's corps was annihilated at Burgundy by Britain with NO Guard question asked (Massena and Ney received their last-stand asks; the sovereign never does) — the row's own re-open condition. The floor it proposes is a mechanic (NP-4's measured shape) outside this client slice; the measurement is recorded on the row and the ruling stays with GE-V and the user.
- **Frames (IQ-10, both Interface Scales, 0 `SCRIPT ERROR`):** `IQ10_CAMPAIGN_END_{FALL_FUNERAL, FALL_CHAINS, FALL_ABDICATION, HUMBLED, VERDICT, IMPERIAL}_2026_09_25.png` (+ `_X2`), `IQ10_LEDGER_FALL_CLOCK_TERRITORIES_2026_09_25.png` (two arms, two tints, one dated), `IQ10_DISPATCH_FALL_CLOCK_2026_09_25.png` (the R screen scrolled to THE FALL OF THE EMPIRE — the prose, then the line). Every button on screen at 2.0; the card scrolls. ⚠ visual sign-off open: the in-game feel of the four registers and the exile paragraphs.
- **Gates:** ruff clean; the parse harness EXIT=0 (60 scripts, 8 scenes — `campaign_end.gd`, `api_client.gd`, `campaign_end.tscn` and the harness added); the real `main.tscn` booted and driven with 0 `SCRIPT ERROR`; `tools/_sweep_ge2.json` **23/23 killed, 0 INERT, 0 BROKEN at close** (2 inert on the first pass, both real pin weaknesses — the stop flag was source-pinned only, so its guard could be deleted unseen (now ONE predicate `_ending_stop_due`, unit-pinned, and the loop pinned to read it); and the control-return tail's raise was MASKED by the battle tableau's own dismissal path, the Verdict turn fighting real battles (now a "plain" Verdict scenario with every tableau stripped, so the tail itself is the only road) — both strengthened and killed); `BASELINE_SERIES` + M1–M7 byte-identical (the slice touches no mechanic: the backend additions are display-only lines and one register declaration nothing stamps).
- **Pins consciously flipped (named):** `test_ge1_review_round::TestTheDriverReadsTheRecord::test_new_endings_are_noted_once` — the ending is still noted ONCE (the headline count is the pin) and the END SCREEN block follows it; `test_fa_slice17_g_the_client_tells_the_truth_2026_09_11::TestFA94EscapeDismissesOnlyWhatIsReadAndDismiss::test_only_the_proclamation_overrides_it` — the end screen joins the FA-94 ESC census (read-and-dismiss on its marked registers, a null control on the Fall; the split is pinned).
- **Recorded, not built:** the Congress's gate line on the same three surfaces ("THE CONGRESS OF PARIS — 43 of 50 titled …", §4) is GE-3's — it joins `fall.clock_line`'s readers; the imperial register's own blocks are GE-3's.

**✅ GE-3 landing record (September 25, 2026 — the commit that carries this record).** The victory arm, built to §2 and the §6 row. Rules `SYSTEMS_REFERENCE.md` §66; pins `tests/test_congress_of_paris.py` (117 — driven through the real `/command`, `game_end.process_end_of_turn`, `capture_region`, `declare_war`, `_ratify_treaty`, `TurnManager.end_turn`), `tests/test_congress_review_round.py` (118 — the review's findings by number, and the two ARMS driven through the real driver) and `tests/test_ge3_the_client.py` (9 — the client driven on the real `main.tscn` through `tools/ge3_congress_harness.gd`).

- **`backend/game_logic/congress.py` — ONE module, ONE serialized field `world.congress`.** The gate terms (in executor order, each with its condition, its breach and its refusal), the summons (2 DP + 1 administrative action through the shared executor; the peace dividend priced on the summons' own surfaces), the table (`answer` — the ONE derivation the tick, the surfaces and the price finder read), the price (`price` — display-only counsel, every lever the formula's own move when applied, the bundle verified), the sitting's tick (`process_end_of_turn`, after the fall clocks and before the Verdict), the hold (seven conditions), the resolution (THE IMPERIAL PEACE through GE-2's declared register; the dissolution — alarm, cooldown, the refusers' grudge, the marshals' expectation), E1 (the Universal Monarchy), and the pressure readers the shared systems call (intent, the coalition's gate and refuser arm, the War of the Congress through `coalition.join_coalition`, London's purse, the sue rung above P1, the bills). Every hook is DORMANT with no Congress; the numbers are the scenario's `campaign_end` block (twelve keys, validated).
- **The verbs.** `summon_congress` and `recognition_sweetener` through the shared executor (validation, the parser, the corpus — 19 `ge3-` rows — the prompt, the display names, the regenerated addressee words, the first-contact desk's `congress` question, the help). The summons is read only at the head of a line; a question, a hedge or a line put to the minister ending in "?" is answered, never executed; a marshal-led line stays the marshal's order.
- **The surfaces.** The CONGRESS tab of the Diplomatic Ledger (tab 7, KEY_7, deep link `ledger_congress`; each court's card with its stance, reason, price, typed orders and "Open the Cabinet at …"), the wizard's step-1 row in its three phases, the war room's rows, `GET /congress`, the clock line (`congress.state_line` / `clock_payload`) on the end-turn banner, the R screen and the Territories tab, Talleyrand's rung 0, the declare-war objection's warning, the dispatch beats (summoned 94, warning 70, war 88, recognized 71, withdrawn 77, dissolved 89, imperial_peace 103), the `congress` chronicle type (the census 166 → 167, fourteen count pins flipped), Le Moniteur's column every turn of the sitting and its specials, and the gold card's Congress block (the four courts SIGNED / SHUT OUT / GONE, the titled count, THE SITTING's strip, the Moniteur line).
- **The bills.** The marshals' collective petition at the summons (`jealousy.queue_fontainebleau_petition(reason="congress")`), every satellite's ask (`vassal.present_pending_asks`), the peace dividend ×1.5 on every rente (`dotation.get_rente_cost(face, world, nation)` — every quote, charge and refund passes both), a client petition's stakes ×2 from day 1.
- **The arms (§2.8), measured on the committed driver — archives `docs/audits/playtest_digests/ge3-{pressburg,premature}-*`, fixtures `tests/fixtures/playtest_saves/fixture_ge3_{pressburg,premature}.json` (`tools/gen_ge3_congress_fixtures.py`), scripts `tools/playtest_scripts/ge3_{pressburg,premature}.json`:**

  | Arm | historical | ulm | austerlitz | marengo |
  |---|---|---|---|---|
  | **Pressburg** (after Pressburg and Tilsit, turn 28, 55 titled; summons + a 1,200g sweetener for Berlin) | **THE IMPERIAL PEACE, turn 36** — ascendant | **turn 36** — ascendant | **turn 36** — ascendant | **turn 36** — ascendant |
  | **Premature** (exactly 50 titled on turn 1, three courts at war, no preparation) | **dissolved turn 6** (Austria takes Franche-Comté — a titled province lost; Berlin had joined the coalition after its warning) | **dissolved turn 4** (Austria takes Milan, Berlin — at war after its warning — Franconia) | **dissolved turn 5** (Austria takes Franche-Comté, Berlin Franconia) | — |

  The Pressburg arm wins between turns 33 and 48 on 4 of 4 seeds (Russia and Austria sign by the treaties they were beaten into, Prussia at the table, Britain shut out); the Premature arm loses on 3 of 3, each by pressure — the fuse is the player's judgement. **Both arms are pinned DRIVEN** (`TestTheArmsDriven`: the real driver, real end turns). The boot board (35 titled) cannot summon (pinned); E1 fires on total elimination (pinned). §8 items 2 and 3 are discharged; the played reach from the 1805 boot is GE-V's.
- **Series (D15).** `tools/_ge3_series_arms.py` (+ `.json`): three arms (every lever DOWN / the Congress alone / shipped) reproduce `BASELINE_SERIES` byte-for-byte and count ZERO writes to `world.congress` — the ambient board never summons or ratifies a France–great-power peace, so the arms measure dormancy (the live hooks are pinned by the driven arms). **No re-record** — D15's allowance for GE-3 was not needed. The review round's one change outside the Congress, `SIEGE_ENDS_WITH_THE_WAR` (every nation), leaves the series byte-identical too (measured: the full suite's `BASELINE_SERIES` pin green with it). M1–M7 byte-identical.

**The GE-3 review round (September 25, 2026 — the same commit).** Eight lenses on the uncommitted tree (the table · the sitting · dormancy · the verbs · the bills · the narration · the client · the tests): **69 findings, ALL FIXED** (`BUG_FIXES.md` §GE-3 Review Round, rows GE3-RV0..68; pins `tests/test_congress_review_round.py` by finding number). The refuter pass died on the weekly subagent limit, so every finding was re-read against the code before it was built. The ones that changed the design:

- **The treaty latch pre-signed the ending (#0, P1).** The first cut latched EVERY war-ending peace at any time — the commanded arm's turn-4 peace, even a peace in which France gave up a province, settled three of the four answers years before the summons. **Now only a peace signed while the Congress SITS, or the peace of a BEATEN court (one that ceded provinces to the Emperor's bloc in that treaty — Pressburg, Tilsit), latches; any other peace feeds the formula; a truce that runs out never does.** (Amends §2.4.)
- **The Congress marched the courts that had signed (#10, #19, #41, P1).** Its own lowered gate — and even the stock gate on the eighth turn — enrolled a court that recognized by the peace it had signed (relations stay hostile after a lost war); its war broke the latch, un-titled its cessions and dissolved the Congress with no French act. **Now, while the Congress sits, a great power that answers it (recognizing, shut out) or holds a truce with the Emperor is never marched into a new coalition** (`congress.spared_from_coalition`, read first by `qualifies_for_coalition`).
- **A siege outlived its war (#11, P1).** An AI corps besieging a French province completed the siege the advance after its court signed the peace and annexed the province at PEACE. **`SIEGE_ENDS_WITH_THE_WAR`: a siege is lifted when its besieger is no longer at war with the holder** — every nation (GR5), the series byte-identical.
- **The pressure readers read the last tick (#18, P1).** A court the player turned mid-turn (the receipt said "Prussia now RECOGNIZES the order") was still enrolled by the coalition turn that runs inside the advance. **Every reader now asks the LIVE answer.**
- **A marshal's order that mentioned the summons made it (#27, P1).** "Ney, attack Mack so we can summon the congress" summoned the Congress (irreversible, 2 DP) and dropped the attack. **The summons is read only at the head of the line; a marshal-led line stays the marshal's order; a hedge, a musing, a request for instructions or a deferral fails closed, as does a line put to the minister ending in "?".**
- **The Pressburg arm could not win from its own fixture (#61, P1).** Fixed in the fixture (peace with every bloc member, the corps off the ceded provinces), in the stakes (×2 from day 1), and pinned DRIVEN.
- **The War of the Congress promised wars that could not come (#4, #23, #43, #54).** **`congress.march_blocker` — ONE predicate** the join, the fore-warning, the countdown and the tab read (a coalition stands, the court is not allied, in a truce, a vassal, freshly at peace or cooldown-bound, not already a member); the warning always comes one end turn before the war; otherwise the table says what holds the court.
- **The price named what the ratifier refuses and missed what works (#1, #2, #6, #7, #57).** The treaty lever is the best treaty the relation lets `_ratify_treaty` accept; the arbiter's buy-off and London's ports are priced; a truce leads with the signed peace; the sweetener's quote, charge, receipt and move agree on a part-paid court; a chained buy-off says it takes one order per design.
- **The sweetener took gold for nothing (#3, #29, #30, #31, #33).** It refuses free a court that withdrew, signed, is shut out or already recognizes, and gold that buys nothing; it reads the gold figure in the GUARDED line (a negated figure, a turn count or a year is never paid; two sums refuse); a line naming a treaty is the Cabinet's; a seat names its court.
- **The bills were quoted at one price and charged at another (#20, #34–#38, #62).** Every standing quote is re-stated at the summons, every sitting tick and the end; the collective petition's concede arm and the redemption's settle arm are priced at DELIVERY; the owed bill never arrives on the tick that ends the Congress; the summons prices the rentes it multiplies — **the dividend runs from the summons' own end turn (nine charged turns), stated** (amends §2.5's "turns summoned+1 …").
- **The narration (#8, #9, #12, #15–#17, #42, #44–#50, #52, #53, #55, #56, #59, #60).** The war score printed from the Emperor's side; a projected SUES before the summons; a court suing at the end named apart and bearing no grudge; a ceder's war on the Emperor named as what reopened its cessions (GE-1's rule stands); joining an ally's offensive draws the sword (the hold) and is never "Berlin's cannon"; E1 waits for the capital and the Emperor, and is never told as a signature; a Fall ends a sitting Congress (`ended`); the Moniteur's column re-set after the tick; the gate line names the breach; the tab names what broke; the concluded table is the ending's own; the gold card's titled count is the bloc's.
- **Not taken:** none refuted. **Recorded:** "Emperor, summon the congress" parses to the summons (the corpus row) but the command road's addressee rule (CX-R1) refuses an order of state addressed to the Napoleon marshal — pre-existing, not this slice's.
- **Gates:** ruff clean; `tools/_sweep_ge3.json` **73/73 killed, 0 INERT, 0 BROKEN at close** (12 inert on the first pass — each a pin that never reached the branch it named, strengthened and killed; the last needed a report sentence with no leading name, since the addressee rule refuses one before anything executes); the parse harness EXIT=0 and the real `main.tscn` driven with 0 `SCRIPT ERROR` (`tests/test_ge3_the_client.py`); IQ-10 frames re-shot (`IQ10_*CONGRESS*_2026_09_25.png`, both scales); the full suite **25,966 passed, 4 skipped**; `BASELINE_SERIES` and M1–M7 byte-identical.
- **Pins consciously flipped (named):** the fourteen campaign-log count pins (166 → 167, the `congress` type); `test_ge1_the_verdict_and_the_fall`'s unknown-key validator pin (it named a Congress key, now known — it names `a_key_no_build_reads`); `test_iq7`'s census rows; `test_health_check_audit_2026_08_14::TestFontainebleauSurfacePricesOffTheExecutorPredicate` (the census follows the concede arm's pricing into its ONE builder, `jealousy.fontainebleau_concede_detail`); `test_congress_of_paris`'s own first-cut pins re-staged by the review (the real-ratification latch → three rule pins; the lever pin through `_ratify_treaty`; London's purse at a treasury above its floor; the Fall's `ended`; the Moniteur's production column).
- **Still open for the user:** the in-game feel of the Congress's surfaces (frames `IQ10_*CONGRESS*_2026_09_25.png`); GE-D1 and GE-D2 (unchanged); the played reach from the 1805 boot is GE-V's.

**✅ GE-V Landing record (September 25, 2026 — the commit that carries this record).** Memo of record `docs/audits/GE_V_PLAYED_CAMPAIGN_2026_09_25.md` (authoritative); rules `SYSTEMS_REFERENCE.md` §66 (the 45 line) + §67 (VP-M1, the Guard floor, the beaten gate); pins `tests/test_gev_played_campaign.py` (31) + `tests/test_ge_d1_generals_mortality.py` (22); archives `docs/audits/playtest_digests/gev-*` (17 runs). Run under the user's grant: *"do this and make these decisions … review the game, don't be afraid to find errors, make the changes."*

- **The reach, played.** Not on the road. Two differently-shaped openings played by hand in chunks of one to three turns through the real `/command` (Ulm → Tyrol/Bohemia/Vienna with Hanover from the north; the whole body at Munich with one supported strike a turn) and three scripted arms all stalled at **35 titled by turn 12** with the army at about half of 189k; the best any arm reached was 41. **§2.8's rule fired: `hold_titled` 50 → 45** (`congress.HOLD_TITLED`, `europe_1805.json`, the fixtures regenerated, the GE-3 pins re-staged on the line). The number is the plan's own rule and is applied; it does not rescue the road — six campaign-layer mechanics do the blocking (the muster preview overstates arrival by ~⅓; four AP for eight corps; the supply cap taxes concentration; the marshals' own systems cost a third of the army by turn 8; 3–5k landings take undefended homeland; a losing lord's satellites bleed −6 a turn) — recommended with two cheap levers as **`DESIGN_REFINEMENT.md` GEV-D1 "The Road to Forty-Five"**, the user's ruling.
- **The table reads, the prices are true, the sitting is a crisis** — confirmed on the driven arms and the committed frames: Prussia's card arithmetic checks (−17 + 20 + 5 + 42 = 50); the Pressburg arm's named price (*"needs +12: a 1,200g sweetener"*) flipped Berlin the turn it was paid; the Premature arm produced the declaration, the Pressburg arm the flip and the bills. Across two arms, not inside one — a single sitting holding all three needs a reachable road. Two copy nits filed (Russia's seat vs its capital lever; London's Descent leading the price).
- **Found by play, fixed:** **GEV-1 P1** — `vassalize Austria` on turn 1 with no battle fought subjugated a great power and assimilated three archdukes (the typed road's only gates were WAR and the power cap) → `vassal.subjugation_refusal`, a court is subjugated by fiat only once BEATEN (capital held · war score ≤ −40 · no corps standing), the signed clause bypasses at the ratify seam, GR5, eight legacy pins re-staged; **GEV-2** `invest in <eliminated court>` read as a marshal's order → the authored courts stay known and the executor answers the court is gone; **GEV-3** the spent Guard's ask wore "ENCIRCLED"; **GEV-4** two driver dials (`--settlement`, `--decline-from`) the measurement needed. Caught by the suite before landing: VP-M1's first Moniteur caption had no row in the gazette's weight table (a `KeyError` on the first wound in a played turn — the driven GE-1 arm produced one organically); priced and pinned by driving the collector.
- **The rulings, taken and built:** **GE-D2 = YES** — `GUARD_SPENT_FLOOR` 1,000: the toll is refused and the Emperor ASKED before the fatal battle (the row's named pin green; FA-S17-7's pin flipped consciously 1,000 → 3,000). **GE-D1 = (a) YES, bounded — VP-M1 "The Fortunes of War"** (`fortunes_of_war.py`): the losing lead of a real battle at ≥ 25% lost rolls once through the campaign seed, killed 1% / wounded 8% (3 turns on ONE serialized field `wounded_until_turn`; the corps stands, defends, marches; no attack/charge/bombard/pursue/PURSUE-HOLD; the AI stands its man down; the card shows it; not sidelined), a death passes the men to the nearest corps and goes through the ONE removal seam as `killed_in_action`; the sovereign excluded; both boards; beats `marshal_wounded` (log 167 → 168 flipped consciously in twelve files) and the killed arm of `marshal_destroyed`; Le Moniteur's caption. **`BASELINE_SERIES` byte-identical with the lever up** (`tools/_vpm1_series_arms.py` — 39 seams reached, no qualifying draw in forty ambient turns; no re-record); M1–M7 byte-identical.
- **Not measured, said plainly:** the D1 band (no council war on any arm), the naval SHUT OUT arm (no arm reached it), the Eagle-Falls road under the new floor (the archive predates it).
- **The re-score: the ending 3.0 → 5.5; the ≥ 7 target is NOT met.** There is an ending and it is legible; it is not on the road. What would meet 7 is GEV-D1's two levers and a re-measure on the same chunked method.
- **Pins consciously flipped:** the fourteen campaign-log count pins (167 → 168); FA-S17-7's "a Guard that can pay" (1,000 → 3,000); the GE-3 pins reading "of 50"; eight conquest-vassal pins that staged a bare WAR (re-staged with the beaten proof, the design's own line); `test_ge3_the_client`'s two "35 of 50" reads.
- **Gates:** ruff clean; the full suite green in the hook; M1–M7 and `BASELINE_SERIES` byte-identical; the two Congress fixtures regenerated; the parse harness untouched (zero `.gd`).

## §7 Build order and the routing rule

**~~F1~~ ✅ (Sept 23, 2026) → ~~F3~~ ✅ → ~~F4~~ ✅ (Sept 24, 2026 — taken ahead of F2 by the user's direction) → ~~F2~~ ✅ → ~~F5~~ ✅ → ~~F6~~ ✅ (Sept 25, 2026, one commit — the six fixes are landed) → ~~GE-1~~ ✅ (Sept 25, 2026, with the Emperor's death, the generals' odds memo and the exile story) → ~~GE-2~~ ✅ (Sept 25, 2026 — the end screen, the clock line, the driver arms) → ~~GE-3~~ ✅ (Sept 25, 2026 — the Congress, its two arms and its 69-finding review round) → ~~GE-V~~ ✅ (Sept 25, 2026 — the reach measured and found not on the road, `hold_titled` 45, GE-D1 + GE-D2 built, the ending 5.5) → ▶ the release build (ROADMAP 10) → Updates 1–5 (Update 1 now also carries GEV-D1's two cheap levers if the user rules for them).** ≈ 6 sessions before the build. A player report never re-orders the ending; it may re-order F2–F6.

Every slice's commit carries: this document's §6/§1 table marked (✅ + date + commit), `STATUS` ▶ NEXT UP struck and advanced, the rows disposed in `BUG_FIXES` / `DESIGN_REFINEMENT`, `CLAUDE.md` LIVE STATE. A slice is not landed until those four are in the commit.

## §8 Done when (falsifiable, the whole program)

1. A fresh Begin shows the boot help and a turn-1 briefing; no player-facing string in the F2 census carries a raw key, state, enum, float turn count or `(s)`; the four F3 frames are clean at both scales; a winning historical-seed campaign sees no collective petition before turn 12; Bavaria takes at most one province on turn 1.
2. `summon the congress` is refused with named gate terms below 50 titled and accepted at 50; every great power answers every turn; each price on the table, applied, flips the court (driven pins per lever). *(✅ GE-3, September 25, 2026 — `tests/test_congress_of_paris.py::TestTheSummonsDriven` and `TestThePriceIsTrue`; the treaty lever applied through the real ratifier.)*
3. The Pressburg arm wins between turns 33 and 48; the Premature arm loses 3 of 3; the accept and fiat arms cannot summon or win; E1 fires on total elimination. *(✅ GE-3, September 25, 2026, on STAGED fixtures — Pressburg turn 36 on 4 of 4 seeds, Premature dissolved turns 4–6 on 3 of 3, both DRIVEN (`TestTheArmsDriven`); the boot board cannot summon; E1 pinned. The fiat arm is not pinned — its claim stays GE-V's with the played reach from the boot.)* *(✅ GE-V, September 25, 2026: the played reach from the boot measured on two hand-played openings and three scripted arms — 35 titled by turn 12 on both openings, best 41 on any arm — so the rule fired and `hold_titled` is 45; neither reach is pinned as a PASS because neither passed; the fiat arm stays unpinned. The gap is the campaign layer's, not the Congress's — GEV-D1.)*
4. Both fall clocks warn on turn 1 and fire on the 5th/10th; retaking a province, commissioning, or ransom stops them; Paris alone never triggers; the tutorial and the bare flag world never arm.
5. Every ending is stamped once, survives save/load, and raises the right register; a defeated save never loads onto a silent board. *(GE-2, September 25, 2026: the three GE-1 registers raise DRIVEN on the real client — the command road, the end-turn road, the ratify road, `/load` of a Final save; the fourth is declared and its frame staged; the Fall closes the command line until a world swap.)*
6. `BASELINE_SERIES` and M1–M7 byte-identical except the attributed re-records (F5 — ✅ re-recorded once, four-arm attributed, September 25, 2026; **GE-3 needed none** — the three arms count ZERO Congress writes on the ambient board and reproduce the series, `tools/_ge3_series_arms.json`; **F4 needed none** — the seven-arm attribution reproduced the recorded series on every arm, with the reason measured in its landing record; F2 and F6 moved nothing, and F2's first cut moving it was a defect, fixed).

## §9 Pins consciously flipped (to be named per commit)

- ~~The IQ-2 "never terminal" pins (`_dispatch`, `_economy_status`, `_war_room`, `_chronicle`, `_client`) — GE-1.~~ Flipped September 25, 2026 — named in GE-1's landing record (`_chronicle` and `_client` held unflipped: the armed copy uses none of their forbidden words).
- ~~`test_first_contact_keyless` (the open-ended answer, 2 tests) — GE-1.~~ Flipped September 25, 2026 (the 7-id parametrized goal test; the stub test holds).
- ~~`test_economy_ec6_sandbox::test_1805_no_end_screen_at_turn_60` — re-blessed (the verdict never sets `game_over`).~~ Re-blessed September 25, 2026, with a comment in the test.
- ~~The strategic-report popup whitelist pin — F3.~~ No such pin existed; F3 flipped `test_ep_f1::TestTheRouteTable` (RESULT_FIRST + `capture_choice`) and the tier-2 mount pin in `test_ui_visual_foundation` instead (named in its landing record).
- ~~The UX23-A/UX23-B expectation timing pins that assume a rise per victory — F4.~~ Flipped September 24, 2026 — named in F4's landing record (111 fixture rewrites across 22 files, the decisive-rise note, FA-N53's rows, ux23b's mid-turn shortfall).
- ~~The FA-D13/PT-D4 display pins that assume three walk-ins — F5.~~ No such pins bound (September 25, 2026): F5 flipped `test_ai_intent_threat_migration::BASELINE_SERIES` (re-recorded, attributed), `test_wo_slice9_the_courting_cap` (12/15, the elimination index, the exit turn) and `test_wo_slice10_enemy_direction_gate`'s ungated-board measurements instead; F2 flipped the `turn(s)` pins in `test_cr7_4`, `test_cr7_9` and `test_mc_q3_command_rally` — all named in the landing records.
- GE-2 (September 25, 2026): `test_ge1_review_round::TestTheDriverReadsTheRecord::test_new_endings_are_noted_once` — the digest's END SCREEN block follows the one-liner; the headline is still noted once. And the FA-94 ESC census (`test_fa_slice17_g…::test_only_the_proclamation_overrides_it`) — the end screen overrides `esc_control` (Continue on a marked ending, null on the Fall).
- Legacy: 0.
